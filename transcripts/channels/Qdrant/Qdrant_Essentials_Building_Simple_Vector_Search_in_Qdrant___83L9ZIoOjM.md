# Qdrant Essentials | Building Simple Vector Search in Qdrant

- 영상 링크: https://www.youtube.com/watch?v=_83L9ZIoOjM
- 채널: Qdrant
- 업로드일: 2025-10-22
- 자막 언어: ko
- 단어 수: 약 1045개

---

## 스크립트

자, 그럼 이제 아주 기본적인 예제를 통해 첫 번째 컬렉션을 만들고, 거기에 첫 번째 벡터를 삽입하고 , 첫 번째 유사도 검색을 수행하는 방법을 살펴보겠습니다. 방금 생성한 쿼드런트 클라우드 클러스터를 사용하여 전체 설정 과정을 배워보겠습니다. 여기서는 구글 콜랩을 사용할 건데요, 시작하기가 아주 쉽고 설정에 대해 크게 고민할 필요가 없기 때문입니다. 먼저 Quadrant 클라이언트를 설치하겠습니다. 그 다음에는 Quadrant 클라이언트 라이브러리에서 필요한 모듈을 가져와야 합니다. 여기에는 Python 클라이언트와 연결하는 데 사용할 Quantron 클라이언트와, 나중에 사용될 거리 벡터 매개변수 및 포인트 구조와 같은 몇 가지 필요한 클래스를 얻기 위한 모델이 포함됩니다. 그 다음에는 쿼드런트 클라우드에 연결해야 합니다. 지난 영상에서 Quadrant 클라우드 클러스터의 클러스터 URL과 API 키를 얻는 방법을 살펴보았습니다 . 하지만 여기에서 볼 수 있듯이 Quadrant Cloud로 이동하기만 하면 클러스터 URL 링크를 얻을 수 있습니다. 만약 새 API 키를 생성해야 한다면, 아래쪽에 있는 ' 생성' 버튼을 클릭하고 원하는 이름을 설정하여 새 API 키를 만들 수 있습니다. 여기서는 Google Colab의 비밀 탭에 API 키를 설정했습니다. 여러분도 '새 비밀 추가'를 클릭하고 원하는 키 이름과 값을 입력하면 쉽게 동일한 작업을 수행할 수 있습니다. 그래서 우리는 이미 API 키가 사용자 데이터와 동일하다는 것을 확인하고 API 키를 가져왔습니다. 이렇게 하면 셀 내에서 API 키를 공유하지 않고도 쿼드런트 클라이언트에 연결할 수 있습니다 . 자, 그럼 라이브러리를 불러오고 클러스터에도 연결해 보겠습니다 . 쿼드런트 클라이언트를 메모리 모드로 사용할 수도 있는데, 이 모드는 완전히 로컬 RAM에서 실행됩니다. 하지만 이렇게 하면 데이터 영구 저장이 불가능해지므로 시스템이 다운되면 모든 데이터가 손실되고 데이터에 HNSW 인덱싱도 적용되지 않습니다. 하지만 데이터 세트 규모가 크지 않고 로컬 테스트만을 위한 데이터 영구 저장이 필요하지 않은 경우에는 이 방법이 효과적일 수 있습니다 . 그래서 우리는 이제 그것을 사용하지 않을 것입니다. 하지만 이것은 하나의 선택 사항이며, 특히 간단한 첫 번째 컬렉션을 만들려는 이 경우에는 분명히 좋은 선택입니다. 쿼드런트의 컬렉션은 관계형 데이터베이스의 테이블과 유사합니다. 하지만 관계형 데이터베이스에서는 이름, 날짜, 숫자와 같은 구조화된 데이터를 다루게 됩니다 . 그리고 사분면의 컬렉션에서는 벡터 데이터를 사용하게 될 것입니다. 컬렉션을 만들 때는 최소한 세 가지 기본 요소를 지정해야 하는데, 그중 하나가 바로 컬렉션의 이름입니다. 이번 경우는 제 첫 번째 컬렉션입니다. 그다음에는 벡터 구성을 설정해야 합니다. 이를 위해 모델 모듈의 벡터 파라미터 데이터 구조를 호출하여 해당 작업을 수행합니다 . 기본적으로 벡터 크기를 4로 설정하는데, 이는 이 컬렉션에서 사용할 벡터의 차원이라고도 합니다. 그리고 우리는 거리 측정 기준으로 유사도라는 지표를 사용할 것입니다. 이것은 컬렉션을 설정하는 가장 기본적인 예입니다 . 이 컬렉션을 대규모 환경에 맞게 최적화하기 위해 추가할 수 있는 구성과 사용할 수 있는 매개변수가 훨씬 더 많습니다 . 하지만 지금으로서는 이것이 유사성 검색을 시작하는 데 필요한 전부입니다. 컬렉션을 생성하기 전에 먼저 사분면 클러스터를 확인해 보겠습니다. 여기서 우리는 컬렉션 페이지가 비어 있음을 확인할 수 있습니다 . 그래서 저희 클러스터에는 아직 컬렉션이 없습니다. 이제 우리만의 컬렉션을 만들어 봅시다. 이제 웹 UI로 돌아가서 컬렉션이 있는지 확인해 보겠습니다. 페이지를 업데이트합시다. 그리고 보시 다시피 제 첫 번째 컬렉션이 이미 클러스터에 있습니다. 클릭해 보면, 그 안에 아직 아무런 포인트도 없다는 것을 알 수 있습니다. 우리는 여전히 포인트를 컬렉션에 추가할 예정이지만, 벌써부터 그 모습을 상상할 수 있어요. 음, 다시 여기로 돌아가면, client.getOptions 메서드를 사용해서 코드 내에서도 똑같이 할 수 있습니다. 그러면 제 클러스터, 즉 첫 번째 컬렉션이 하나 있다는 것을 알 수 있을 겁니다. 정말 멋지네요. 이제 실제로 점들을 컬렉션에 삽입하는 부분으로 넘어가겠습니다 . 포인트는 사분면의 핵심 데이터 요소입니다. 그리고 지금으로서는 포인트가 주로 세 부분으로 구성되어 있다고 생각할 수 있습니다. 첫 번째는 내 포인트를 고유하게 식별하는 ID입니다. 그다음은 벡터 데이터인데, 이는 벡터 공간에서 내 데이터 포인트를 나타내는 실제 숫자 값 배열입니다 . 그리고 나서 탑재체가 있습니다. 이는 선택 사항이지만, 검색 시 분류 필터링 등에 사용될 수 있는 키-값 쌍 형태의 메타데이터입니다 . 여기서는 point 구조체 데이터 구조를 사용하여 두 개의 점을 정의하고 , 이 두 점에 각각 ID를 부여하고 있습니다. 제가 4차원 벡터를 제공하는 이유는 제 컬렉션에서 벡터의 크기가 4개라고 했고, 각기 다른 카테고리별로 페이로드도 하나씩 있다고 언급했기 때문입니다 . 하나는 예시이고 다른 하나는 데모 카테고리에 속합니다. 이러한 벡터를 데이터베이스에 추가하려면 upsert 메서드를 사용할 수 있습니다. 제가 보내야 할 유일한 매개변수는 컬렉션 이름과 방금 생성한 포인트 목록입니다. 그럼 그렇게 해봅시다. 그리고 웹 UI에서 벡터들이 내 컬렉션에 추가되었는지도 확인해 보겠습니다 . 그리고 여기서 보시다시피, 제 컬렉션에는 이미 벡터들이 있습니다 . 각 벡터와 관련된 페이로드도 확인할 수 있습니다 . 여기서 첫 번째 요점은 카테고리 예시이고, 두 번째 요점은 카테고리 데모입니다. 벡터의 길이가 4라는 것과 나중에 유용할 다른 정보들을 확인할 수 있습니다 . 우리는 이전에 했던 것과 동일한 `client.get` 메서드를 실행하여 이 정보를 얻을 수도 있습니다. 단, 이번에는 컬렉션 이름을 매개변수로 전달하여 특정 컬렉션에 대한 정보를 얻을 수 있습니다 . 여기서 상태가 녹색으로 표시되는 것을 볼 수 있으며, 두 개의 포인트와 컬렉션 의 다른 구성에 대한 자세한 정보도 확인할 수 있습니다 . 다음 단계는 실제로 유사성 검색을 실행하는 것입니다. 여기서는 주어진 쿼리 벡터와 가장 유사한 벡터를 찾으려고 합니다 . 그러니까 쿼드런트는 우리가 선택한 거리 측정 기준을 사용하여 쿼리 벡터를 저장된 모든 벡터와 비교하고, 가장 유사한 벡터를 반환하는 방식으로 작동합니다. 이 특정 함수에서는 가장 유사한 벡터 하나만 반환합니다 . 이를 위해 저는 쿼리 포인트 방식을 사용하고 있습니다. 그리고 저는 여기서 정의한 컬렉션 이름과 쿼리 벡터를 전달하고 있습니다 . 쿼리 벡터는 내 컬렉션 내의 벡터들과 길이가 같아야 합니다. 이 경우에는 4차원입니다. 자, 이 코드를 실행해 보면, 코사인 유사도를 사용했을 때 내 컬렉션에서 쿼리 벡터와 가장 유사한 지점은 ID가 1인 이 지점이라는 것을 알 수 있습니다 . 하지만 특정 카테고리에 해당하는 경우에만 점수를 돌려받고 싶다면 어떻게 해야 할까요? 예를 들어, 저는 예시 카테고리에서 나온 점수만 원합니다. 저는 데모 카테고리에서 점수를 받고 싶지 않습니다. 이를 위해서는 필터링된 검색을 사용할 수 있습니다 . 필터 클래스를 사용하여 필터를 정의할 수 있으며, 키는 '카테고리'로, 값은 ' 데모'가 아닌 '예제'로 지정할 수 있습니다. 그리고 이를 위해 동일한 클래스 쿼리 포인트를 사용할 수 있지만, 지금은 컬렉션 이름과 쿼리 벡터 외에도 방금 정의한 검색 필터인 쿼리 필터를 전달하고 있습니다 . 그래서 우리가 그 코드를 실행해 보면, 예시와 일치하는 쿼리 벡터와 가장 유사한 지점은 ID1이라는 것을 알 수 있는데, 실제로 우리가 가진 유일한 지점입니다. 하지만 이 예제를 데모로 변경 하고 다시 실행하면 이제 ID 번호 2를 얻는 것을 볼 수 있는데, 이는 해당 페이로드를 가진 유일한 지점입니다. 하지만 우리는 이것이 어떻게 작동하는지 알 수 있습니다 . 그리고 만약 제가 이것을 다른 것으로 검색해 보면, 예를 들어 이것을 테스트하고 실행해 보면, 아래 테스트에서 아무런 결과도 얻지 못합니다. 그래서 아무런 결과도 나오지 않아요. 이것은 벡터 탐색을 실험해 볼 수 있는 매우 흥미로운 방법입니다 . 오늘 하루를 마무리하며, 여러분의 과제는 Quadrant 클라우드 계정을 설정하고, 첫 번째 프로젝트를 생성하고, Quadrant에서 첫 번째 유사성 검색을 실행하는 것입니다. 내일 첫째 날에는 임베디드 모델과 그 작동 방식, HNSW 인덱싱, 그리고 검색 성능을 향상시킬 수 있는 컬렉션용 쿼드런트 구성에 대해 자세히 알아보겠습니다 . 첫날에 뵙겠습니다.
