# Power Lines: Scaling Laws for Weight Decay and Batch Size in LLM Pre-training

- 영상 링크: https://www.youtube.com/watch?v=UeHeFUOEY1o
- 채널: Cerebras
- 업로드일: 2025-12-08
- 자막 언어: ko
- 단어 수: 약 673개

---

## 스크립트

안녕하세요, 제 발표를 시청해 주셔서 정말 감사합니다 . 제 이름은 셰인 버크스마입니다. 저는 Cerebra Systems의 연구원이며, 이 글은 LLM 사전 학습에서 가중치 감소 및 배치 크기에 대한 전력선 스케일링 법칙에 관한 것입니다 . 만약 이 논문에 부제가 있었다면, "10 억 달러를 투자하기 전에 최적의 훈련 설정을 예측하는 방법"이었을 겁니다 . 요약하자면, 대규모 언어 모델을 학습시키는 데 현재 수억 달러 에서 수십억 달러가 소요되며, 최종 모델을 학습시킬 기회는 단 한 번뿐입니다. 실행 비용이 너무 비싸서 하이퍼파라미터 튜닝을 할 수 없습니다. 하지만 학습률이나 가중치 감소, 배치 크기와 같은 설정은 최종 모델 품질에 큰 영향을 미칠 수 있습니다. 우리는 모델 크기, 데이터 세트 크기 및 학습 배치 크기를 확장함에 따라 최적의 하이퍼파라미터를 예측하는 것과 같은 스케일링 로그를 연구합니다. 그리고 우리가 발견한 것은 이 흥미로운 비율, 즉 시간 척도의 비율이 다양한 규모에 걸쳐 예측 가능하다는 것입니다. 따라서 최적의 견인력을 예측할 수 있다면, 좋은 학습 결과를 얻기 위해 배치 크기와 가중치 감소율을 어떻게 설정해야 하는지 알 수 있습니다. 두 번째 발견은 최적 배치 크기와 임계 배치 크기 모두 예측 가능하다는 것입니다. 주로 데이터 세트 크기에 따라 확장됩니다. 이러한 결과를 바탕으로 이제 학습 전에 하이퍼파라미터를 미리 구성하여 효율적인 학습을 진행할 수 있습니다. 저희 논문에서 사용한 접근 방식은 최대 업데이트 매개변수화를 이용하는 것이었습니다. 따라서 이 방법을 사용하면 모델 크기가 커짐에 따라 최적의 학습률이 안정적으로 유지됩니다 . 그리고 나서 우리는 수백 개의 언어 모델을 훈련시키는데, 모델 크기, 데이터 세트 크기, 가중치, 감쇠율, 배치 크기 등을 다양하게 조절하면서 수십억 개의 매개변수를 사용합니다. 그런 다음, 결과로 나온 최적 설정에 다양한 멱법칙을 적용합니다 . 우리가 발견한 것은 시간 척도인데, 이는 이전 가중치 중 최종 모델 매개변수에 반영되는 비율로 생각할 수 있습니다. 우리는 이것이 실제로 토큰당 매개변수 비율(d/n)에 대한 멱법칙으로 매우 예측 가능하다는 것을 발견했습니다. 그리고 아시다시피, 이러한 설정과 실행 과정에서 수천 배 더 많은 연산 능력을 소모할 수도 있지만, 이러한 결과는 멱법칙을 따라 매우 잘 맞아떨어집니다. 요점은 이 법칙을 소규모에 적용한 다음 대규모 훈련에 활용할 수 있다는 것입니다. 이 논문의 두 번째 핵심 발견은 최적 시간 척도가 고정되어 있다는 점인데, 이는 학습률과 가중치 감소, 즉 최적 학습률과 가중치 감소가 배치 크기에 비례하여 선형적으로 증가해야 함을 의미합니다 . 하지만 저희는 가중치 감소가 배치 크기에 비례하여 확장되는 반면, 학습률은 그러한 시도를 하다가 실패하는 것을 발견했습니다. 음, 기본적으로 사용해야 하는 학습률이 너무 높아져서 훈련이 불안정해진다는 뜻입니다. 그래서 가중치를 너무 높게 설정할 수도 없고, 너무 낮게 설정하면 초기 가중치에서 충분한 진전을 이루지 못합니다. 결과적으로 학습률을 조정하는 것보다 가중치 감쇠를 조정하는 것이 손실을 줄이는 데 더 효과적이며, 최적 설정이 훨씬 더 예측 가능하다는 것을 알 수 있습니다. 그리고 앞서 언급했듯이 또 다른 중요한 발견은 두 가지 핵심 배치 크기가 있다는 것입니다. 하나는 특정 규모에서 가장 낮은 손실을 가져오는 최적 배치 크기이고, 다른 하나는 해당 규모에서 사용할 수 있는 가장 큰 유효 배치 크기인 임계 배치 크기입니다. 우리는 이 두 가지를 모두 측정했고, 그 결과 데이터 세트 크기 D가 멱법칙에 따라 증가한다는 것을 발견했는데, 이는 이전의 일부 연구 결과와 상반됩니다. 여기서 다시 한번 얻을 수 있는 교훈은 이러한 법칙들을 작은 규모에 적용하고 이를 큰 규모로 확장하여 효과적인 훈련 실행을 할 수 있다는 것입니다. 이 연구의 흥미로운 응용 분야 중 하나는, 예를 들어 최첨단, 세계 최고 수준의 모델을 가능한 한 빠른 시간 내에, 그리고 가능한 한 저렴하게 훈련시키고 싶다고 가정해 봅시다. 즉, 비용이 많이 들기 때문에 너무 많은 연산 능력을 사용하고 싶지 않은 경우입니다. 저희 기술은 이 두 가지 차원을 동시에 최적화하는 최적의 설정을 예측할 것입니다 . 흥미로운 점은, 더 작은 모델이라도 배치 크기를 키우고 더 많은 데이터를 사용하면 이 두 가지 차원에서 모델이 더 최적화된다는 것입니다. 어, 저희가 진행한 또 다른 흥미로운 후속 연구는, 만약 LLM(법학 석사)을 양성하는 데 10억 달러를 투자한다면, 모든 단계에서 제대로 진행되고 있는지 확인해야 한다는 것이었습니다 . 그리고 만약 고정된 파라미터당 토큰 비율로 다양한 스케일에서 학습하고, 해당 TPP에 대한 최적의 시간 스케일을 선택한다면, 손실 곡선은 간단한 정규화만으로도 보편적인 궤적으로 수렴하게 될 것입니다. 그러면 이는 훈련 문제에 대한 매우 유용한 조기 경고 신호가 됩니다. 아시다시피, 훈련 과정이 이러한 보편적인 곡선에서 벗어나기 시작하면 문제가 있는 겁니다. 네, 요약하자면 이제 튜닝 없이도 어떤 규모 에서든 이러한 하이퍼파라미터를 효과적으로 설정할 수 있다는 것입니다 . 더 작은 과적합 모델은 동일한 속도와 비용에서 거의 최적일 수 있으며, TPP에서 고정된 시간 척도로 확장하면 예측 가능한 훈련 손실 곡선을 얻을 수 있습니다. 매우 감사합니다.
