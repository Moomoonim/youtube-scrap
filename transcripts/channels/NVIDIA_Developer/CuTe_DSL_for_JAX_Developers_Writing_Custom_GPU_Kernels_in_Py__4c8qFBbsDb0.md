# CuTe DSL for JAX Developers: Writing Custom GPU Kernels in Python

- 영상 링크: https://www.youtube.com/watch?v=4c8qFBbsDb0
- 채널: NVIDIA Developer
- 업로드일: 2026-05-06
- 자막 언어: ko
- 단어 수: 약 1580개

---

## 스크립트

엔비디아 GPU에서 JAX는 이미 빠르지만, 결국에는 한계에 부딪히게 될 것입니다. 융합 연산이 필요할 수도 있고, XLA 연산을 생성해야 할 수도 있고, 사용자 지정 메모리 레이아웃이 필요할 수도 있고, 아니면 완전히 비표준적인 것을 구축하고 있어서 자체 커널이 필요할 수도 있습니다. 이 비디오에서는 CUTLASS QDSL을 사용하여 Python으로 고성능 GPU 커널을 작성하고 , 이러한 커널을 마치 네이티브 JAX OP처럼 JAX에서 호출하는 실용적인 워크플로를 보여드리겠습니다. 여기서부터 커널이 JAX JIT 내부에서 실행되는 과정을 살펴보고, 한 단계 더 나아가 보겠습니다. JAX Expert를 사용한 사전 컴파일을 소개하여 아티팩트를 직렬화하고 동일한 Python 프로세스 내에서 재추적 없이 사용할 수 있도록 하겠습니다. "JAX 생태계를 벗어나지 않고 자체 GPU 커널을 사용하여 JAX를 확장하는 방법은 무엇인가요 ?"라는 질문을 해본 적이 있다면, 이건 당신을 위한 거예요. 자, 계획은 이렇습니다. 먼저, CUTLASS가 무엇을 하는지, 그리고 그것이 스레드, 블록, 레이아웃과 어떻게 연결되는지에 대한 기본적인 개념을 설명드리겠습니다. 그다음에는 간단한 것부터 시작해서 점점 더 흥미로운 일련의 커널들을 살펴보겠습니다. 커널에서 JAX 호출까지 벡터 덧셈의 전 과정을 보여주고, Saxpy를 사용하여 스칼라 매개변수를 깔끔하게 전달하는 방법을 보여주고, ReLU 활성화 함수와 융합 바이어스 ReLU 활성화 함수를 통해 융합의 중요성을 설명하고, 타일링된 GEMM 예제를 통해 실행 구성과 간단한 구현의 한계를 보여주고 , 멀티 GPU 환경, CUTLASS의 뛰어난 기능이 JAX 샤딩과 어떻게 결합되는지, 그리고 마지막으로 JAX Expert를 사용한 사전 컴파일에 대해 설명합니다 . 컴파일된 연산을 직렬화, 역직렬화한 후 나중에 실행합니다. 또한 상징적인 모양들도 포함됩니다. 코드를 공유하고 개발자들이 이 코드를 처음 시도할 때 흔히 겪는 문제점들을 짚어보겠습니다. CUTLASS는 NVIDIA의 오픈 소스 CUDA 템플릿 라이브러리로, 특히 gem 및 텐서 연산과 같은 고성능 GPU 커널을 구축하는 데 사용됩니다. 모든 것을 처음부터 작성하는 대신 최적화된 구성 요소를 제공합니다. CUTLASS 내부에서 cute는 텐서 및 레이아웃 시스템입니다. 이 기능을 사용하면 텐서를 데이터와 레이아웃이 결합된 형태로 표현할 수 있으므로 스타일링, 인덱싱 및 메모리 매핑을 훨씬 쉽게 이해할 수 있습니다. 이 귀여운 DSL은 그러한 추상 개념들을 파이썬으로 가져옵니다 . 따라서 복잡한 C++ 템플릿을 작성하는 대신, 동일한 고성능 CUTLASS 인프라를 기반으로 하면서도 Python으로 GPU 커널을 작성할 수 있습니다 . 제 동료 마이클이 이러한 도구들이 만들어진 동기에 대해 발표를 했습니다. 여기 링크를 걸어둘게요. 꼭 확인해 보세요. 이 영상에서는 CUTLASS의 성능 기본 요소, 귀여운 텐서 추상화, 그리고 JAX의 구성 가능성을 결합해 보겠습니다. Cute는 인덱스 및 레이아웃 DSL로 생각하는 것이 가장 좋습니다. GPU에서 성능은 종종 스레드가 데이터에 어떻게 매핑되는지, 데이터가 메모리에 어떻게 배치되는지, 그리고 데이터를 레지스터와 공유 메모리로 어떻게 이동하는지에 따라 결정됩니다. Cute는 텐서가 포인터와 레이아웃을 합친 것과 같다는 것을 나타내는 어휘를 제공합니다. 수동으로 오프셋을 계산하는 대신 텐서 좌표를 사용하여 인덱싱을 표현하면 레이아웃이 주소 계산을 처리합니다. 그리고 가장 중요한 실질적인 교훈은 귀여운 커널도 여전히 CUDA 커널이라는 점입니다. 실과 블록은 실제로 존재합니다. 스레드 인덱스, 블록 인덱스 및 제어 실행 형태를 명시적으로 읽게 됩니다. 귀여운 DSL 커널은 두 겹으로 되어 있습니다. 첫 번째 계층인 큐트 커널은 스레드별 프로그램, 즉 각 스레드가 실행하는 명령어를 정의합니다. Cute jit은 실행할 런처, 그리드 블록 및 CUDA 스트림을 정의합니다. 두 번째 사항은 JAX 통합에 있어 매우 중요합니다 . 런처는 첫 번째 인수로 CUDA 스트림을 받으며, 해당 스트림은 XLA에 의해 관리됩니다. 따라서 커널은 JAX 프로그램의 나머지 부분과 동일한 실행 타임라인에서 실행됩니다 . 가장 간단한 완전한 예시인 벡터 덧셈, c = a + b부터 시작해 보겠습니다. 커널에서 각 스레드는 a의 요소 하나와 b의 요소 하나를 레지스터에 로드하고, 이들을 더한 다음 결과를 저장합니다 . 몇 가지 주목할 사항이 있습니다. 우리는 스레드 인덱스와 블록 인덱스를 사용하여 스레드와 블록을 식별합니다. 우리는 레지스터 조각을 사용하여 계산 결과를 레지스터에 저장합니다. 또한 CUDA 복사 도우미를 사용하여 전역 메모리와 레지스터 간에 데이터를 효율적으로 이동합니다. 그런 다음 실행기에서 블록당 스레드 수와 그리드의 블록 수를 선택합니다. 따라서 스레드와 데이터 간의 매핑은 깔끔하고 예측 가능합니다. 자, 이제 핵심 부분인 JAX에서 이 함수를 호출하는 방법을 살펴보겠습니다 . 핵심은 런처를 cutlass.jax.cutlass 호출로 감싸는 것입니다. 이렇게 하면 JAX JIT 내부에서 JAX 사용자 지정 호출로 실행될 수 있는 호출 가능한 객체를 얻을 수 있습니다. 두 가지 중요한 사항입니다. XLA가 그래프를 컴파일할 수 있도록 JAX에 출력 형태와 데이터 유형을 미리 알려줘야 합니다 . 그리고 이 커널의 인덱싱 스타일에서는 1차원 벡터를 3차원 보기, 스레드당 요소 수, 블록당 스레드 수 및 블록 수로 재구성합니다. 해당 재구성은 레이아웃을 재해석한 것이지 데이터를 복사한 것이 아닙니다. 블록 크기의 배수로 패딩을 추가하고, 형태를 변경하고, 커널을 호출한 다음, 원래 형태로 되돌리고 패딩 부분을 잘라냅니다. 잠시 멈추고 여러분이 재사용할 핵심 패턴을 말씀드리고 싶습니다. 커틀러스는 세상을 잇는 다리를 부릅니다. 한쪽에는 CUDA 스트림과 텐서를 요구하는 CUDA 런치 셔츠가 있습니다. 반면에, XLA 내부에서 코드를 작성하려는 JAX 함수가 있습니다. 따라서 Cutlass call은 JAX 런처를 JAX 기본 함수 또는 사용자 지정 호출로 변환합니다. 즉, 더 큰 JAX 치트 함수 안에 넣을 수 있다는 뜻입니다. 다른 JAX 코드나 구성 요소와 결합할 수 있으며, XLA는 이를 컴파일된 프로그램의 일부로 스케줄링합니다 . 여러분이 반복적 으로 수행해야 할 두 가지 책임은 다음과 같습니다. 첫째, 커널에서 예상하는 텐서 레이아웃이 JAX에서 데이터를 표시하는 방식과 일치하는지 확인하십시오. 둘째, 컴파일이 제대로 이루어지도록 올바른 출력 형태와 D 타입을 제공해야 합니다 . 일단 그 두 가지를 제대로 익히면 나머지는 모두 반복 가능해집니다. 다음은 AXPY입니다. 출력은 알파 곱하기 X 더하기 Y와 같습니다. 이는 벡터 덧셈과 같은 도형 개념입니다 . 패딩을 적용하고, 3D 뷰로 재구성한 다음, 커널을 호출하고 , 다시 원래 형태로 재구성합니다. 새로운 점은 스칼라 알파입니다. CUDA 커널에서 알파는 단순히 매개변수일 뿐이지만, JAX 컴파일에서는 알파가 컴파일 시점에 동적인지 정적인지가 문제입니다. 이 데모에서는 알파를 고정된 인자로 취급합니다. 즉, 추적 시점에 알 수 있으며 컴파일된 코드 경로에 포함될 수 있다는 의미입니다 . 실제로, 우리는 JAX JIT에서 alpha를 static으로 표시한 다음, 이를 Cutlass 호출에 키워드 인수로 전달함으로써 이를 구현합니다. 이 패턴은 간단해 보이지만 매우 중요합니다. 이를 통해 동적 디스패치 비용을 지불하지 않고 구성 매개변수 또는 상수를 사용하는 커널을 작성할 수 있습니다 . 자, 이제 머신러닝에서 자주 사용하게 될 패턴인 요소별 활성화에 대해 알아보겠습니다. ReLU 활성화 함수의 경우, 스레드별 3D 요소 레이아웃 대신 평면 1D 인덱싱 방식을 사용합니다. 블록 및 스레드 인덱스로부터 전역 선형 인덱스를 계산하고 , 경계 검사를 수행하고, 로드하고, 0과 X 사이의 최댓값을 적용한 다음 저장합니다. 이는 표준 CUDA 커널 스타일과 매우 유사하며, JAX 래퍼는 훨씬 더 간단해집니다. 입력을 평탄화하고, N을 사용하여 커널을 호출한 다음, 원래 형태로 되돌립니다. 이를 통해 Q가 두 가지 스타일을 모두 어떻게 지원하는지 잘 알 수 있습니다. 보다 구조화되고 레이아웃 중심적인 커널과 직관적인 CUDA와 유사한 인덱싱 커널. 여기서부터 흥미로워집니다. 융합 편향과 ReLU 활성화 함수가 결합된 형태입니다. 딥러닝에서 X에 바이어스를 더한 다음 ReLU 활성화 함수를 사용하는 것은 일반적입니다. 하지만 이들을 개별적으로 처리하면 일반적으로 커널 실행 비용과 전역 메모리에 대한 중간 쓰기 비용이 발생합니다. 그러고 나서 다시 읽어봤습니다. 융합은 그러한 문제를 피합니다. 하나의 커널에서 X를 로드하고, 해당 열에 대한 바이어스를 로드하고 , 더하고, ReLU를 적용하고, 한 번 저장합니다. 그러니까 여기서 전하고자 하는 메시지는 Q가 모든 JAX OP를 대체할 수 있다는 것이 아닙니다. 핵심은 메모리 트래픽이나 실행 오버헤드가 지배적인 핫스팟이 있을 때, 퓨전 기술이 매우 효과적인 해결책이 될 수 있다는 것입니다. 래퍼에서 너비를 정적 매개변수로 전달하는 것을 확인할 수 있는데, 이는 너비가 인덱싱에 영향을 미치고 컴파일 타임 상수처럼 취급하는 것이 유용하기 때문입니다. 좀 더 고급스러운 예시로 타일 보석을 다뤄보겠습니다. 이 섹션의 목적은 JNP matmul에서 JAX의 CuBlas를 완전히 박살내는 것이 아닙니다 . 사실, 이 간단하면서도 훌륭한 교육 자료는 그렇지 않을 겁니다. CuBlas는 매우 최적화되어 있습니다. 여기서 중요한 것은 작동 원리를 보여주는 것입니다. 크기를 결정하는 방법, 블록이 출력 타일에 매핑되는 방식, 그리고 JAX에서 M, N, K와 같은 문제 크기를 실행기로 전달하는 방법에 대한 내용입니다. 래퍼 내부에서는 A와 B를 평면화하고, M과 K를 제공한 다음, 커널을 호출하고 다시 M 과 N으로 재구성합니다. 개발자들이 가장 궁금해하는 질문 중 하나는 "사용자 정의 커널을 추가하면 JAX의 구성 가능성, 특히 멀티 GPU 지원 기능이 손실되는가?"입니다. 이 데모에서는 답이 '아니오'입니다. Cutlass 콜 커널은 여전히 ​​JAX 샤딩에 참여할 수 있습니다. 정신적 모델은 다음과 같습니다. JAX는 배열을 여러 장치에 분산 저장합니다. 각 장치는 로컬 샤드에서 동일한 사용자 지정 호출을 실행하며 , JAX가 조정을 처리합니다. 따라서 우리는 디바이스 메시를 생성하고, 샤딩 사양을 정의하고 , 샤드 맵을 사용하여 커널 코드 자체를 변경 하지 않고 여러 GPU에서 벡터 덧셈을 실행합니다 . 이것이 바로 CUDA 커널을 별도로 실행하는 대신 JAX 통합을 통해 이 작업을 수행하는 핵심적인 장점입니다. 자, 그럼 AOT, 즉 사전 컴파일에 대해 이야기해 봅시다. 일반적으로 JAX를 사용하면 JAX JIT 함수를 작성하고 컴파일은 현재 실행 중인 Python 프로세스에서 수행됩니다. 연구 및 반복 작업에는 좋지만, 실제 제품 생산에서는 다른 워크플로가 필요한 경우가 많습니다. 한 번 컴파일하고, 아티팩트를 직렬화하고, 배포하거나 캐시하고, 나중에 추적 없이 로드할 수 있습니다. 바로 여기서 JAX 전문가의 역할이 중요해집니다. 이 데모에서는 Cutlass 사용자 정의 호출, 요소별 덧셈 커널, 그리고 시그모이드 함수와 같은 일반적인 JAX 연산자를 결합한 함수를 만들어 봅니다. 그런 다음, 이를 내보내고, 바이트로 직렬화한 다음, 다시 호출 가능한 아티팩트로 역직렬화합니다 . Cutlass 커널을 내보낼 때 중요한 두 가지 사항이 있습니다 . 첫째, 내보낸 계산에는 사용자 지정 호출이 포함됩니다 . 우리는 JAX에게 이러한 사용자 지정 호출을 내보낸 출력에 포함해도 안전하다고 명시적으로 알려줍니다. 둘째, 모양이 중요합니다. 구체적인 형태로 내보내기를 하면 결과물은 해당 크기에 고정됩니다. 심볼릭 형태로 내보내면 커널 실행 가정의 제약 조건 내에서 여러 입력 크기에 걸쳐 동일한 아티팩트를 재사용할 수 있습니다. 요컨대, 귀여운 커널을 포함하는 JAX 프로그램을 만들고 , 이를 독립 실행형 직렬화 아티팩트로 내보낸 다음, 다시 로드하여 나중에 실행할 때 원래의 Python 함수 정의를 메모리에 저장할 필요가 없습니다. 기술적인 부분은 이쯤에서 마무리하고, 여러분의 시간을 절약해 줄 몇 가지 실용적인 팁을 드리겠습니다. 첫째, 사용자 지정 호출을 구성할 때는 출력의 형태와 데이터 유형을 항상 명확하게 지정해야 합니다. 둘째, 매개변수가 동적이어야 할지 정적이어야 할지를 초기에 결정하십시오 . 알파와 같은 스칼라 값이나 너비와 같은 구성 값은 정적인 값으로 처리하는 것이 가장 좋습니다. 셋째, 여유 공간 및 시작 제약 조건에 주의하십시오. 커널이 전체 블록을 가정하는 경우 입력에 패딩을 추가하고 출력을 분할하십시오. 마지막으로, 이 접근법이 가장 효과적인 곳에 활용하십시오. 표준 라이브러리에서 제공하지 않는 융합, 특수 레이아웃, 사용자 지정 데이터 이동 및 커널 기능을 제공합니다 . 만약 당신이 커틀러스와 JAX를 이기려고 한다면, 아마도 잘못된 문제를 해결하려고 하는 것일 겁니다. 개념적인 측면에서 더 자세히 알아보고 싶으시다면, 앞서 언급했던 개발 로그 강연 링크를 첨부하겠습니다. 또한, 데모 노트북과 커널 소스 코드 링크도 제공하여 처음부터 끝까지 직접 실행해 보실 수 있도록 하겠습니다. 이 영상이 유용했다면 좋아요를 누르고, Nvidia 개발자 채널을 구독하고, Jax로 작성해 보고 싶은 커널을 댓글로 남겨주세요 . 사람들이 무엇을 만들려고 하는지 정말 보고 싶어요.
