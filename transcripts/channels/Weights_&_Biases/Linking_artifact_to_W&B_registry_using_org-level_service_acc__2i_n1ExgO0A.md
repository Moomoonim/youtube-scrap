# Linking artifact to W&B registry using org-level service accounts

- 영상 링크: https://www.youtube.com/watch?v=2i_n1ExgO0A
- 채널: Weights & Biases
- 업로드일: 2025-01-14
- 자막 언어: en
- 단어 수: 약 1057개

---

## 스크립트

[Music] hey guys I wanted to record you a quick video showing you how to create a weights and bies or level service account and then later use the service account in order to link in artifact to your or level weights and bises registry so the first step to create your um service account is going to be just simply going over to the service accounts inside um under your account awesome and then just pressing create service account awesome let's give it a name in this case we can give it a name called service account that is a great name and then we'll also um we can go ahead and choose the um default entity all the default entity here does is it simply um just chooses the default team that the org level service account will be writing to so in this case I currently have only a single team under this organization that's called um R team demo so therefore if I click into here into job down menu RTM demo is the only thing that will pop up um as the option awesome now that we have created this service account um do note that the only person who can access the service account is an org admin by accessing the service account I do mean I'm just copying this API key right here so if you're not an organization admin you will will need to ask your or admin to copy this API key for you wonderful so from here let's go ahead inside of our registry and make a new custom registry we'll call it um essay as in service account test in this case you'll be able to either make the visibility organization wide or restricted and I'll show you what both do so organization wide all it really does is um adds every single person inside of the org with model or just with regular registry access um access this registry and if it's restricted that means you will get to handpick um the users including the service accounts that can access this registry so in this case just for demo we'll pick restricted awesome and we'll say that we can log or add any types of artifacts into this registry wonderful let's go ahead and create it we have created essay test wonderful from here we will go into settings and add members we will add our service account that we created that's called service account into the registry again if the visibility is organization then every single user will be added automatically but in this case we'll do restricted wonderful and now your service account has access to this registry and can start linking artifacts with that registry so let's try it out okay so I have created a registry. py file here right and I will try to link an artifact to this registry awesome so let's start off by um importing weights and bies and also two things we can do here we can either um log in with the service accounts API key or we can specify service account API key as an environmental variable so in this case we can also import let's try the environment variable path first right so import OS and then we will say os. Environ U we will say 1 DB API key and then the API key we're going to give it is the service account API key which again you can find inside of service account can copy it um paste it in here awesome and now when you make your white and biases run you going to be in it uh project is going to be we'll call it registry just like it's suggesting registry linking and then the entity will be writing to is RTM demo because this is the current entity I'm using the that team RTM demo that's where the run is going to get saved then inside of here we can create an artifact effect equals to 1 DB 1 d v b artifact which will name it um registry test awesome and then we'll we can say that it's a type model awesome so then we will um add a file to the artif right next to add file we will simply just add this file it's called linking artifact. txt we'll copy the full path to it and then we will um log this file two Andes to this artifact two and bies wonderful so from here after it has been longed what we can do is um add this artifact link this artifact to the registry right to the registry we just created the sa test I'm using the latest version of VO and vises so this is the command I'll be using in order to link it to this registry awesome so let's paste it in here run that link artifact we do have the artifact created above on when DB registry say test in the collection name we can uh come up with a collection name if um you don't have a collection in here already um so let's create collection name and it will automatically create a collection inside of the registry so we'll say test collection awesome from here we'll run run now finish and we'll run python our actual see our actual script awesome so the Run has been made perfect we can see that two artifact files have been uploaded wonderful and if we go inside of our essay test as you can see and the collection was not there but because collection was not there and we have called to link our artifact with the test collection text collection has been created wonderful and there is our little artifact with a file inside of it that's called linking artifact. txt linking artifact. txt that we have logged to W and bies and linked with the registry and to triple check that this was a recorded with your service account and not your personal account you can go into the run that created um and logged your artifact go into the overview and you can see that the author is the service account we created so there you go cool let us know if you have any more questions [Music]
