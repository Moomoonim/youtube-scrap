# Enhanced Data Flow in AWS Step Functions

- 영상 링크: https://www.youtube.com/watch?v=aoKt7Aw2a1I
- 채널: AWS Developers
- 업로드일: 2024-11-22
- 자막 언어: en
- 단어 수: 약 670개

---

## 스크립트

Do you wanna hear how the AWS Step Functions has improved data transition and step functions? Hi, I'm Eric Johnson, Principal Developer Advocate for AWS. Developers tell us that when using step functions, they want a faster way to save data and use it later on in the step function. And they also want to be able to quickly query data without having to go through a lot of transitions. I'm excited to say that with two new features, step function workflow variables and JSONata, developers can now move much quicker and access data later in the workflow without a lot of work. Let me show you how this works. I've got a workflow that actually updates a DynamoDB database with an old email to a new email, old owner to new owner. And I use both variables and JSONata to move through this. So let's get started. So in the first task, I have a query task, and in my arguments, I'm actually using JSONata, here you can see in line 11, to do the states.input.oldOwner. So I'm grabbing the old owner that would be passed in, all right? And I'm also setting some variables. First, I'm setting the old owner and I'm setting the new owner. And then I'm also setting the records that are returned when I actually make the query. So I create a variable called records that has that array. When I go into the choice state, I again use JSONata and I use the count function and I say count the number of input items. If there are none, then I just exit out to success. But if there are some, I then go into process them. So first thing I'm gonna do is I'm gonna create a backup and you can see in the arguments that I'm using JSONata to concatenate S12D- and then milliseconds for a date. So that gives me that title plus the milliseconds for that. And then I also set the table name there. In the map state, one thing I'm doing is in the output, I don't need the results of updating the record, I just want the input passed through again. So in the output, I'm just gonna pass the input straight through. So I'm gonna grab the states.input as a full object. When I get to the DynamoDB update, I actually use several things. First of all, to get the ID, I'm gonna use the states.input.id. And then for the new owner, I'm gonna use a variable which I set all the way at the top, if you remember that. I'm also gonna use a JSONata function called now to set the current date so that I can know when I updated this record. Scrolling down, I look for pagination here and I actually say I use a JSONata function, say if this exists, then I wanna look for state.input.LastEvaluatedKey. So let's look and see if that came in. And then if it does, then I go back into the query. If it doesn't, then I'm actually going to create a change set of all the records. So, and how I build that is I actually use a JSONata function for arrays where I take that records variable that is an array, and then I map through it, and I say I want ID, I want old owner, and I want new owner. And it actually allows me to take a full set of records with a lot of data in it and bring it down to a small set of records with just the records that I want. Okay, so that's a quick overview of JSONata and variables in AWS Step Functions. Coming out a little bit later, I'll have a video that goes deep dive into variables and a video that goes deep dive into JSONata. I encourage you to check 'em out. If you want more videos like this, click that subscribe. I'm Eric Johnson and I hope you have a great day.
