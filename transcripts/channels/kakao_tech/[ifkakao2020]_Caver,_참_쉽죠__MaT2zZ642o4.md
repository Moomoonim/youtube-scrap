# [ifkakao2020] Caver, 참 쉽죠

- 영상 링크: https://www.youtube.com/watch?v=MaT2zZ642o4
- 채널: kakao tech
- 업로드일: 2026-06-10
- 자막 언어: ko
- 단어 수: 약 2177개

---

## 스크립트

안녕하세요. 케이버 10십조라는 주제로 발표하게 된 그라운드엑스에서 클레이트 SDK인 케이블을 개발 담당하고 있는 자스민이라고 합니다. 저는 이번 발표에서 케이버가 무엇인지에 대해서 먼저 설명드리고 그 뒤에 커먼 아키텍처가 어떤 것인지 설명드릴 예정입니다. 그리고 그 뒤에는 케이블을 사용해서 어떤 것들을 할 수 있는지 간단한 예제를 실제 코드와 함께 설명드리겠습니다. 혹시 케이버가 무엇인지 아시는 분 계신가요? 네. 많은 분들이 케이버가 무엇인지 알고 계신 것 같은데요. 저희가 내린 케이버의 정의에 대해서 말씀드리겠습니다. 저희는 케이버를 클레이트을 쉽게 사용하기 위한 SDK다라고 정의를 했는데요. 저희는 여기서 쉽게는 단어에 좀 더 초점에 맞추어 생각해 보았습니다. 쉬운 사용성을 위해서 SDK에서 어떠한 것들이 고려되어야 할까라는 질문에 대해서 생각했고 저희는이 고민에 대한 결과로 이번에 케이버에 커먼 아키텍처를 도입하게 되었습니다. 그렇다면 커먼 아키텍처란 무엇일까요? 커먼 아키텍처는 모든 클레이튼 SDK에서 공유하는 클레이트 개발 환경을 위한 새로운 소프트웨어 아키텍처입니다. 전에 쉬운 사용성에 대해서 고민한 결과 끝에 커먼 아키텍처를 도입했다고 했는데 왜 새로운 소프트웨어 아키텍처가 쉬운 사용성을 위해서 필요한지 의문이 두실 수 있는데요. 지금부터 커먼 아키텍처를 케이버에 도입하게 된 이유에 대해서 설명드리겠습니다. 케이블은 현재 자바스크립트와 자바 언어로 제공이 되고 있는데요. 커먼 아키텍처가 도입되기 전에 케이버는 공통된 구조를 공유하고 있지 않았기 때문에 내부적으로 구현된 구조가 달랐으며 하나의 영화에 대해서 케이버 JS와 케이버 자바가 제공하는 기능이 달랐을 정도로 사용하는 용어가 통일되어 있지 않았습니다. 그렇기 때문에 언어별 케이버의 사용성이 굉장히 달랐겠죠. 이러한 이유 때문에 만약에 B앱을 개발한 개발자가 다른 언어로 확장하기 위해서는 또 해당 언어에서 제공되는 케이버의 사용성을 익혀야 했습니다. 같은 클레이트을 사용하기 위한 SDK임에도 불구하고 어떠한 액션을 수행하기 위해서 각 언어별 케이버에서 수행해야 하는 플로우도 다르고 하나의 기능에 대해서 사용하는 방법과 용어도 달랐기 때문에 클레이트 비앱을 개발하는데 어려움이 있었습니다. 저희는 이런 문제점을 인식하고 커먼 아키텍처를 도입하게 되었으며 커먼 아키텍처를 도입함으로써 크게 다음과 같이 세 가지의 목표를 달성하고자 했습니다. 먼저 클레이트 SDK인 케이버의 구조와 용어를 통일하는 것이며이를 통하여 개발자들에게 쉽고 유사한 사용성을 제공하고자 했습니다. 또한 프로그래밍 언어에 종속되지 않는 클레이트 SDK만의 구조를 확립함으로써 다양한 언어의 클레이트 SDK가 개발될 수 있는 토대를 마련하고자 하는게 마지막 목표였습니다. 그럼 커먼 아킥 텍처에 대한 대략적인 구조를 레이어 다이어그램을 통해서 설명드리겠습니다. 오른쪽에 보시는 바와 같이 커먼 아키 텍처는 총 여덟 개의 레이어를 구분하여 정의되어 있습니다. 각 레이어별로 어떤 기능이 제공되는지 간략하게 설명드리겠습니다. 먼저 KCT 레이어에서는 클레이트 컴패터블 토큰 즉 KCT 토큰 컨트랙트를 쉽게 배포하고 실행할 수 있는 기능이 제공되고 있는데요. 현재는 KP 7 펑저블 토큰 스탠더드 즉 대체 가능한 토큰 스탠더드와 KP 17 넌저블 토큰 스탠더드에 대한 기능이이 KCT 레이어에서 제공되고 있습니다. 그다음 컨트랙트 레이어인데요. 컨트랙트 레이어에서는 스마트 컨트랙트를 쉽게 사용할 수 있는 기능을 제공하고 있습니다. 사용자는 스마트 컨트랙트의 ABI와 바이트 코드를 가지고 있다면 컨트랙트 레이어에서 제공하는 기능을 사용하여 쉽게 클레이트 네트워크의 스마트 컨트랙트를 배포하거나 기 배포된 스마트 컨트랙트를 쉽게 실행할 수 있습니다. 그다음 ABI 레이어에서는 어플리케이션 바이너리 인터페이스에 대한 기능을 제공하는 레이어로 ABI를 사용하여 파라미터를 인코딩하고 디코딩하는 기능을 제공하고 있습니다. 그다음 트랜잭션 레이어에 대해서 설명드릴게요. 다들 아시다시피 클레이트에서는 다양한 트랜잭션 타입들이 사용됩니다. 여기 트랜잭션 레이어에서는 클레이트에서 사용되는 다양한 트랜잭션들이 개별 클래스로 구현되어 있으며 트랜잭션과 관련된 디코드하는 함수나 아니면 해싱을 구하는 함수 같은 기능들이 함께 제공되고 있습니다. 원래 레이어에서는 케이버에서 클레이트너카운트를 사용할 수 있는 기능들을 제공하고 있는데요.이 이 레이어에서는 크게 인메모리 원래 기능을 제공하는 키링 컨테이너와 케이버에서 클레이트 계정의 주소와 프라이빗 키드를 저장할 수 있는 키링이 함께 제공되고 있습니다. 그리고 클레이트는 또 하나의 큰 피처가 바로 어카운트에서 사용하는 키를 업데이트 할 수 있다는 건데요. 계정의 키를 업데이트 할 때 필요한 데이터 구조와 기능들이 바로이 어카운트 레이어에서 제공되고 있습니다. 그리고 RPC 레이어에서는 실제로 클레이트 노드와 제이슨 RPC 통신하는 기능을 담당하고 있고 마지막으로 유틸스는 말 그대로 유틸리티 함수들을 제공하고 있습니다.이 발표 시간에 커먼 아키텍처의 자세한 설명을 생략하고 이제 케이버 사용 방법에 대해 설명드리겠습니다. 커먼 아킥 테처에 대한 세부적인 설계 내용은 KP 34에 자세하게 기술되어 있으니 커먼 아키테처에 관심 있으신 분들은 KIP 34를 읽어 보시면 좋을 것 같습니다. 그럼 지금부터 실제로 커먼 아키텍처가 반영된 케이블을 어떻게 사용하는지에 대해서 설명드릴 텐데요. 시나리오 베이스로 먼저 설명을 드리면서 각 절차별로 코드로는 어떻게 구현되어야 하는지 함께 설명드릴 예정입니다. 코드는 케이버 JS로 하는 방법과 케이버 자바를 사용하는 방법에 대해서 함께 설명드리겠습니다. 그럼 먼저 케이블을 사용하여 어떻게 클레이를 전송할 수 있는지 알아보겠습니다. 일단 클레이를 전송하려면 클레이가 있는 계정을 소유한 유저가 있겠죠. 그럼 그 유저는 본인이 소유하고 있는 계정의 주소와 프라이빗 키를 사용하여 키링을 생성합니다. 키링을 생성하는 코드를 한번 보실까요? 이번 예제에서는 키 스토어 파일을 읽어 와서이를 보호화하여 사용할 키링을 생성하고 있는데요. JS와 케이버 자바 모두 디크립트라는 함수를 사용하며 첫 번째 파라미터로는 파일로부터 읽어온 키스토어를 넘기고 두 번째 파라미터로는이 키스토어를 암호할 때 사용했던 패스워드를 넘겨 두면 됩니다. 그럼 사용자는 이렇게 생성된 키링을 케이버의 인메모리 원렛에 추가해 줄 수 있는데요. 키링을 인메모리 원렛에 넣으려면 케이버. 원렛에서 제공되는 애드 함수를 사용하면 됩니다. 파라미터로는 JS와 자바 모두 동일하게 방금 생성한 키링을 넘겨 주면 됩니다. 그럼 이제 사용할 키링이 모두 준비가 되었으니 실제로 클레이를 전송하는데 필요한 트랜잭션을 생성해 줘야 합니다. 그럼 이제 트랜잭션을 생성하는 코드에 대해서 설명드리겠습니다. 여기서는 밸류 트랜스퍼 타입의 트랜잭션을 사용합니다. 왼쪽에 JS 코드에 대해서 먼저 설명드리겠습니다. JS는 생성자의 트랜잭션의 값들이 정의된 오브젝트를 파라미터로 넘겨서 트랜잭션을 생성합니다. 오브젝트 내부에 정의된 값은 트랜잭션에 전송한 프롬의 주소, 그리고 클레이를 전송받을 2의 주소, 그리고 얼만큼의 클레이를 전송할지에 대한 밸류, 그리고 트랜제션을 실행할 때 사용할 최대 가스량을 정의할 수 있습니다. 그럼 이제 자바로는 어떻게 생성하는지에 대해서 설명드리겠습니다. 자바는 빌드업 패턴을 사용하여서 트랜잭션의 값들을 각각 셋해 주는 방식으로 트랜잭션을 생성합니다. 코드에 보시면 JS와 마찬가지로 프롬 계정의 주소 그리고 클레이를 전송받을 2의 주소 전송할 클레이의 양 그리고 트랜잭션을 실행할 때 사용할 최대 가스량을 정의하고 있는 것을 보실 수 있습니다. 참고로 자바의 경우에도 밸류 트랜스퍼 트랜잭션 생성자에 바로 값들을 차례대로 정의해서 생성할 수 있지만 파라미터 개수가 이렇게 많아지고 옵션 파라미터가 많아지면 사용하기가 불편하기 때문에 빌더 패턴을 사용할 수 있도록 따로 제공되고 있습니다. 그럼 이제 트랜잭션을 생성했으니 아까 생성한 키링으로이 트랜잭션을 서명을 해야겠죠. 좀 전에 코드에서 키링을 생성한 뒤에 저희가 인매모리 원렛에 애들을 해 줬습니다. 그렇기 때문에이 예제에서는 인메모리 원렛에 저장되어 있는 키링으로 서명하는 케이버. 원렛의 사인 함수를 사용합니다. 코드에 보시다시피 JS와 자바 모두 사인 이름의 함수를 호출하게 되고요. 첫 번째 파라미터는 사용할 키링의 주소. 두 번째 파라미터는 서명할 트렌제션을 남겨 줍니다. 사인 함수 실행이 완료되면 생성된 서면값이 트랜잭션의 시그니처스에 추가되게 되는데요. 만약 파라미터로 전달받은 트랜잭션에 기존의 서명이 존재하지 않는다면 새롭게 서명을 추가하게 되고 만약 기존의 서명이 존재한다면 그 서명 뒤에 업핸드되도록 동작합니다. 그럼 이제 설명된 트랜잭션을 실제 클레이트 네트워크에 전송해 주면 됩니다. 네트워크로 전송된 트랜잭션이 정상적으로 처리되면 프롬 계정에서투 계정으로 클레이가 성공적으로 전송되게 됩니다. 그럼 어떻게 설명된 트랜잭션을 클레이트 네트워크로 전송하는지 설명드리겠습니다. 좀 전에 커먼 아키텍처 레이어를 설명드릴 때 클레이트 노드와 제이슨 RPC 통신하는 기능을 제공하는 RPC 레이어가 있었죠. 거기서 제공하는 기능을 사용하시면 됩니다. 케이버.rpc를 RPC를 사용하면 클레이트 노드와 제이슨 RPC 통신을 할 수 있고 저희는 그중에서도 클레이 네임스페이스의 샌드로우 트랜잭션 코를 사용합니다.이를 위하여 코드에 보시다시피점, rpc.클레이점 클레이. 샌드로우 트랜잭션 함수를 사용합니다.이 RPC 콜은 네트워크의 트랜잭션을 전송한 이후 트랜잭션 해시를 리턴하게 되며 케이버에서는이 해시값을 통하여 트랜잭션 처리 결과를 확인할 수 있는 트랜잭션 리시을 조회할 수 있습니다. 여기까지 케이블을 사용해서 클레이를 전송하는 방법에 대해서 설명드렸는데요. 굉장히 간단하죠?이 이 정도의 코드만 작성하시면 클레이를 바로 전송하실 수 있습니다. 그럼 다음은 케이버로 나만의 토큰을 만드는 방법에 대해서 설명드리겠습니다. 여기서는 KP7 토큰 컨트랙트를 배포해서 자신만의 토큰을 만들어 보겠습니다. 아까와 마찬가지로 시나리오 베이스로 설명드리면서 절차별 코드도 함께 설명드리겠습니다. 먼저 토큰을 생성하고자 하는 유저가 있겠죠.이 이 유저는 이전과 마찬가지로 클레이가 있는 클레이튼 계정을 소유하고 있습니다. 유저는 좀 전과 마찬가지로 본인이 소유한 클레이트 어카운트를 사용하기 위해 키링을 생성하게 되고 생선된 키링을 인메모리 원렛에 추가해 줍니다. 키링을 생성하고 인메모리 원렛에 추가하는 코드인데요. 아까와 동일한 내용이므로 자세한 설명은 생략하겠습니다. 그럼 이제 유저는 KB 7 토큰 컨트랙트를 클레이트 네트워크에 디플로이 해야 하는데요.이를 위해서 사용자는 KP7 클래스의 디플로이 메소드만 호출해 주면 KP7 토큰 컨트랙트가 클레이트 네트워크에 배포되게 됩니다. 여기서 참고로 KF7은 KF7 펑자블 토큰 컨트랙트를 쉽게 배포하고 실행하기 위해서 케이버에서 제공하는 클래스의 이름입니다. 유저가 이렇게 KP7의 디플로이 메소드를 호출하게 되면 내부적으로 KP7 토큰 컨트랙트의 ABI와 바이트 코드를 사용하여 토큰 컨트랙트를 디플로이 하는 스마트 컨트랙트 디플로이 트랜잭션을 생성하게 되고요. 그다음 초반에 인메모리 원렛에 추가한 키링을 사용하여이 스마트 컨트랙트 디플로이 트랜잭션에 서명하게 됩니다. 그렇게 서명된 트랜잭션을 마지막으로 실제 클레이트 네트워크로 전송하여 KP7 토큰 컨트랙트의 배포가 완료되게 됩니다.이 트랜제션이 네트워크에서 처리가 완료되면 토큰 컨트랙트가 정상적으로 배포된 것을 의미하고요.이 이 결과로는 네트워크에 배포된 토큰 컨트랙트의 주소가 담겨 있는 KP 7 인스턴스를 리턴 받게 됩니다. 사용자는 리턴 받은 KP 7 인스턴스를 사용하여 클레이트 네트워크에 배포된 토큰 컨트랙트를 사용할 수 있고요. 여기에서 노란색 글씨는 사용자가 직접 수행하는 작업이 아니라 내부적으로 동작하는 작업을 의미하게 됩니다. 그럼 배포하는 코드는 어떻게 되는지 한번 볼까요? 디플로이하기 앞서 사용자는 어떠한 토큰을 네트워크에 배포하고 싶은지 먼저 정의해야 합니다. 여기 코드에 보시면 토큰의 이름 자스민 토큰의 신볼 자스 그리고 사용되는 소수점 자릿수를 나타내는 대시말 18짜리 그리고 초기 발행량이 정의된 것을 확인할 수 있습니다. 토큰에 정의되는 내용들은 동일하며 JS의 경우 왼쪽에 코드와 같이 오브젝트를 사용하여 정의하게 되고 자바의 경우에는 KRP7 디플로이 파람스를 사용하여 정의합니다. 그럼 이제 설명드렸다시피 KP 7 클래스에서 제공하는 디플로이만 호출하면 되는데요. JS와 자바 모두 동일하게 KP 7 클래스에서 제공하는 스태틱 메소드인 디플로이를 호출해 주면 됩니다. 디플로이 메소드를 호출할 때 파라미터로는 토큰의 정보를 담고 있는 객체와 토큰 컨트랙트를 배포하는 계정의 주소를 넘겨 줍니다. 그럼이 디플로이 메소드는 전달받은 계정을 사용하여 토큰 컨트랙트를 배포하게 되며 토큰 컨트랙트를 배포할 때 사용된 계정은 배포된 KP7 토큰 컨트랙트의 오너가 되게 됩니다. 배포가 완료되면 디플로이 함수는 사용자에게 배포된 컨트랙트의 정보를 담고 있는 kf 7 인스턴스를 리턴하게 되고 사용자는 1를 사용하여 클레이트 네트워크에 배포된 컨트랙트를 사용할 수 있습니다. 이제 배포된 컨트랙트의 정보를 담고 있는 KP 7 인스턴스를 사용하여 어떻게 토큰 컨트랙트를 실행할 수 있는지 설명드리겠습니다. 좀 전에 토큰 컨트랙트 배포의 결과로 KIP7 인스턴스를 리턴 받은 시점부터 설명을 시작하겠습니다. 사용자는 리턴 받은 인스턴스를 통해 배포된 토큰 컨트랙트를 실행할 수 있는데요. 좀 전과 마찬가지로 간단하게 메소드를 호출해 주면 컨트랙트에 정의된 함수가 호출되게 됩니다. 그럼 단순히 컨트랙트의 값을 읽어는 함수를 호출하는 방법에 대해서 설명을 드리겠습니다.이 예제에서는 컨트랙트의 메타데이터인 이름을 읽어는 네임 함수를 호출하는 방법에 대해서 설명드리겠습니다. 아까 말씀드렸다시피 네임이라는 KP7 메소드를 사용하면 되는데요. 이렇게 컨트랙트의 값을 읽어는 메소드를 호출하면 내부적으로 메시지 호출을 하는 제이슨 RPC 코를 네트워크로 보내게 됩니다. 그럼 이제 코드를 설명드리겠습니다. 실제 네임이라는 함수를 사용하는 코드인데요. 앞에서 설명드렸다시피 KRP 7의 네임 메소드만 호출해 주면 KRP7 토큰 컨트랙트의 네임을 읽어올 수 있습니다. 좀 전에 JS와 자바 모두 자스민이라는 이름으로 토큰을 배포했기 때문에 콘솔에는 더 네임 오브 토큰 자스민이라고 찍히겠죠. 그럼 다음은 단순하게 저장된 값을 읽어는 것이 아니라 실제 컨트랙트에 값을 쓰거나 삭제 혹은 수정을 하는 함수를 호출하는 방법에 대해서 설명드리겠습니다. 컨트랙트에 저장되는 값을 변경하는 함수를 호출하기 위해서는 실제로 스마트 컨트랙트를 실행하는 트랜잭션을 발생시켜야 하기 때문에 내부적으로 동작하는 방식이 악화하는 조금 다른데요. 사용자 입장에서는 좀 전과 동일하게 KP7의 메소드를 호출해 주면 됩니다. 여기 예제에서는 프롬에서 2로 토큰을 전송하는 트랜스퍼라는 함수를 예로 들어 설명드리겠습니다. 이렇게 트랜스퍼 메소드가 호출이 되면 내부적으로 스마트 컨트랙트를 실행할 수 있는 스마트 컨트랙트 큐션 트랜잭션을 생성하게 되는데요. 그다음 입매모리 원렛에 저장된 키링으로 1 트랜잭션에 서명하게 됩니다. 이렇게 서명된 트랜잭션은 네트워크로 전송되게 되고 트랜잭션이 처리될 때 EVM을 통해서 토큰 컨트랙트가 실현되게 됩니다. 그럼 이걸 코드로 어떻게 해야 하는지 설명드리겠습니다. 좀 전과 마찬가지로 KP7 인스턴스의 트랜스퍼 메소드를 호출해 주면 됩니다. 트랜스퍼 메소드의 파라미터로는 실제 KP7 토큰 컨트랙트의 트랜스퍼 함수의 파라미터를 차례대로 넣어 주면 되는데요. 코드에서 보면 트랜스퍼의 파라미터인 2의 주소를 첫 번째 파라미터로 전송하고자 하는 토큰의 양을 두 번째 파라미터로 넘겨 줍니다. 그럼 마지막에 파라미터가 하나 더 넘겨지는 것을 볼 수 있는데요.이 이 마지막 파라미터에는 트랜잭션을 전송할 때 필요한 정보가 정의되어야 합니다. 여기서는 트랜잭션을 전송하는 주체인 프롬 계정의 주소를 정의해 놓았는데요. JS는 왼쪽과 같이 바로 오브젝트의 트랜잭션 전송에 필요한 정보들을 정의하고 자바의 경우 오른쪽과 같이 샌드 옵션스를 사용하여 정의하게 됩니다. 이렇게 함수를 호출하게 되면 내부적으로 정의된 프롬의 계정을 사용하여 스마트 컨트랙트 exec큐 트랜잭션을 생성하며 이때 트랜스퍼 호추를 위한 인코딩도 전달된 파라미터와 함께 자동적으로 수행되게 됩니다. 그렇게 컨트랙트 실행 트랜제션이 생성되면 좀 전에 설명드렸다시피 인메모리 원렛에 저장되어 있는 키링을 사용하여 서명하게 되고 서명된 트랜잭션을 네트워크에 전송하게 됩니다. 트랜잭션이 처리 완료되면 트랜잭션 리시을 결과로 리턴해 주게 되는데요. 트랜잭션이 정상적으로 처리가 되었다면 실제 자스민이라는 토큰이 프롬에서 2트로 성공적으로 전송이 되었을 것입니다. 지금까지 실제로 나만의 토큰을 만들고 사용하는 방법에 대해서 설명드렸는데요. 원래대로라면 KP7 토큰을 배포하기 위해서는 토큰 스탠더드에 대해서 먼저 이해를 하고 1을 준수하는 스마트 컨트랙트를 작성하여 1을 컴파일한 후 배포하는 트랜제션을 전송하는 이런 1년의 과정을 거쳐야 하는데 케이버에서 제공하는 기능을 사용한다면 이렇게 쉽고 간단하게 KP7 토큰 컨트랙트를 배포하고 사용할 수 있습니다. 커먼 아키텍처 반영 후에 케이버 JS와 케이버 자바에서 사용하는 용어가 통일되었고 두 라이브러리의 사용성이 비슷해졌다는 것을 직접 보여 드리고 싶어서 두 벌의 코드로 함께 설명을 드렸는데요. 많은 도움이 되셨으면 좋겠습니다. 그럼 마지막으로 케이버에서 남은 과제들에 대해서 설명드리고이 발표를 마치겠습니다. 클레이트 SDK를 위한 커먼 아키텍처를 설계하고 구현한 가장 큰 이유 중에 하나가 바로 다양한 언어로 케이버가 개발될 수 있는 토대를 마련하기 위해서였는데요. 고나 파이썬, 시# 등 다양한 언어의 케이버에 대한 니즈가 있기 때문에 이번에 설계된 커먼 아키텍처를 기반으로 차차 새로운 언어의 케이블을 개발해야 합니다. 그리고 다음은 컨트랙트와 관련된 이야기인데요. 케이블에서 제공되고 있는 컨트랙트는 내부에서 자동적으로 트랜잭션을 생성하고 서명하여 1을 네트워크로 전송하는 기능이 구현되어 있습니다. 하지만 아직까지 내부적으로 베이직 트랜잭션 타임만을 사용하고 있는데요.이 말은 측수료 대납 트랜잭션은 사용하지 못한다는 의미입니다. 앞으로는 컨트랙트를 사용할 때에도 수수료 대납을 할 수 있는 기능을 케이버에서 지원해야 합니다. 그리고 다음은 KCT에 대한 내용입니다. 지금 케이버는 KLP 7 펑저블 토큰 스탠더드와 KLP 17 넌 펀저블 토큰 스탠더드만 지원되고 있는데 최근에 KP에 새롭게 성의된 멀티토큰 스탠더드인 KP 37도 케이버에서 지원이 되어야 합니다. 마지막으로는 항상 저희의 숙명인 사용성 개선 그리고 버그 수정 등이 있겠네요. 여기서는이 발표 자료에서 언급된 내용과 관련된 것들만 말씀드렸는데요. 이외에도 케이버에서 해야 할 일은 많습니다. 하지만 저희 케이버는 오픈 소스이기 때문에 언제나 여러분들의 컨트리뷰션을 환영합니다. 이슈가 있거나 궁금한 점이 있으면이 케이블리의 이슈에 올려 주시거나 혹은 아래 클레이트 포럼에 글을 남기시면 되고요. 버그픽스나 개선할 사항 같은게 있으면 바로 PR로 올려 주셔도 됩니다. 저의 발표는 여기까지고요. 이번 발표를 통해서 여러분들이 케이블에서 좀 더 깊은 이해와 관심을 가질 수 있는 계기가 되었으면 좋겠습니다. 감사합니다.
