# Create collections - Weaviate Python client v4 in action

- 영상 링크: https://www.youtube.com/watch?v=xlmgYh1DakY
- 채널: Weaviate
- 업로드일: 2024-02-06
- 자막 언어: ko
- 단어 수: 약 520개

---

## 스크립트

안녕하세요, 저는 JP이고 we8의 교육자입니다. 이 영상에서는 we8의 새로운 V4 파이썬 클라이언트를 사용하여 컬렉션을 만드는 방법을 보여드리겠습니다. 저는 이 클라이언트를 정말 좋아하는데, 컬렉션 생성과 같은 작업을 할 때 인지 부하를 줄여주고 작업의 어려움을 덜어주기 때문입니다. 이 영상을 다 보실 때쯤이면 여러분도 저와 같은 생각이 드실 거라고 생각합니다. 그럼 시작해 볼까요? 저는 TMDB에서 가져온 영화 데이터셋을 사용하고 있습니다. 영화 데이터셋과 리뷰 데이터셋이 있는데, 이 두 가지에 맞는 컬렉션을 만들어 보겠습니다. 먼저 wave8을 임포트하고 연결을 생성하겠습니다. 편의상 내장 we를 사용하겠습니다. 먼저 리뷰 컬렉션을 만들어 보겠습니다. 이제 속성을 생성해야 하므로 다음과 같이 속성 목록을 지정해야 합니다. 사용할 사용자 지정 클래스인 `property`가 있습니다. 이 클래스들을 임포트해야 하므로 ` wave8 classes`에서 세트로 임포트하겠습니다. WC로 설정했으니 이제 이 설정을 활용해서 속성 클래스를 호출 하고 이름을 지정해 보겠습니다. 이름은 ' title'로 하고, 데이터 유형은 'T'로 지정하겠습니다. 방금 가져온 데이터를 사용해서 열거형 'tyab'을 텍스트로 지정할 수 있습니다. 아, ' username'이어야 하는데 'content'로 지정했네요. 리뷰 내용도 'content'로 하고, ID는 사용자 이름인데, 사용자 이름은 그다지 중요하지 않으니 벡터화는 건너뛰겠습니다. 전에는 벡터화 매개변수를 기억해야 했지만, 이제는 ' skip'이라는 단어를 찾아서 선택하고 true로 설정하면 됩니다. ID도 같은 방식으로 설정하겠습니다. 좋습니다. 벡터 라이저를 지정하고 싶으니 벡터라이저 설정 매개변수를 찾아보겠습니다. 편리하죠? 'configure do vectorizer'를 사용해서 coh를 선택하겠습니다. 생성형 구성도 마찬가지로 벡터 라이저 대신 생성형 모듈을 설정합니다. generative.를 입력하고 OpenAI를 사용하겠습니다. 끝입니다. 리뷰 모음 설정이 완료되었습니다. 영화에도 동일한 방식 으로 또는 비슷한 방식으로 파일을 생성할 수 있습니다. 이름은 당연히 'mov'로 지정하고, 이전처럼 속성을 지정해 보겠습니다. 속성 이름은 ' titled'로, 데이터 형식은 'WC'( 텍스트)로 설정하겠습니다. 몇 가지 속성을 가져와야 하므로 복사해서 붙여넣겠습니다. 슬로건, 개요, 평균 평점( 평균 점수이므로 부동 소수점 숫자, 10진수)이 있습니다. 개봉일( 물론 날짜 형식이므로 작업 가능), 상영 시간( 정수), IMDb ID( IMDb에서 조회할 때 유용함, 텍스트 형식)가 있습니다. 벡터화는 건너뛰고, IMDb ID를 추가하겠습니다. IMDb ID도 추가하겠습니다. 정수이므로 다음과 같이 지정하겠습니다. 모듈에도 동일한 설정을 사용해 보겠습니다. 아, 목록 맨 끝에 들어가야겠네요. 이제 한 가지 더 할 일이 있는데, 바로 역 인덱스를 설정하는 것입니다. 다시 말하지만, 올바른 매개변수를 찾기만 하면 됩니다. 저기에 역인덱스 설정이 있네요. 동일한 configure gen Factory를 사용하겠습니다. 역 인덱스 함수가 ​​있는 것 같은데, 여기를 보겠습니다. 속성 길이의 인덱싱을 반전시키겠습니다. true로 설정하겠습니다. 그리고 아래에서 벡터 인덱스 설정을 하는 방법을 보여드리겠습니다. 다시 WC configure Vector index입니다. hns W를 사용해 보겠습니다. 이 함수를 호출하면 거리 측정법과 같은 작업을 할 수 있습니다. 표준 측정법을 사용하겠지만, 어디서 설정하는지 보여드리겠습니다. WC Vector distances에 있습니다. En num 형식입니다. 코사인으로 설정하겠습니다. 보시다시피 입력해야 할 내용이 거의 없습니다. 정말 편리하죠. 마지막으로 한 가지만 더 하겠습니다. 영화와 리뷰 간의 상호 참조를 생성하기 위해, 참조 관계를 설정하겠습니다. 클래스 이름이 정확히 기억나지는 않지만, WC subm 모듈에서 '참조'라는 단어를 검색하면 찾을 수 있습니다. ' movies'라는 클래스가 있고, 'review'라는 이름의 클래스가 있습니다. 이 클래스는 리뷰 컬렉션을 포함하고 있으므로 ' review'라고 부르겠습니다. 대상은 'review' 컬렉션이 될 것입니다. 이제 마지막으로 연결을 닫겠습니다. 생략해도 큰 문제는 아니지만, 새로운 V4 API에서는 연결을 닫는 것을 권장합니다. 이렇게 하면 두 컬렉션과 그 사이의 참조, 그리고 모든 속성을 단 몇 분 만에 생성할 수 있습니다. 보시다시피, 이전처럼 매개변수 등을 수동으로 지정할 필요가 거의 없습니다. 새로운 Python 클라이언트를 사용하는 가장 큰 장점 중 하나는 이러한 매개변수들을 기억해야 하는 부담이 줄어든다는 것입니다. 데이터를 다루면서 사용하는 매개변수 이름인데, 여러분에게도 도움이 될 거라고 생각합니다. 한번 확인해 보시고 어떻게 진행되는지 알려주세요. 다음에는 몇 가지 데이터 입력 예시를 살펴보겠습니다.
