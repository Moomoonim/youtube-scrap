# Qdrant x LlamaIndex | Advanced RAG Patters and Agent Workflows

- 영상 링크: https://www.youtube.com/watch?v=ytWskQWsAA4
- 채널: Qdrant
- 업로드일: 2025-10-22
- 자막 언어: ko
- 단어 수: 약 1374개

---

## 스크립트

안녕하세요, 저는 Llama Index의 Toana입니다. 이번 영상에서는 Llama Index 와 Quadrant를 다양한 방식으로 활용하는 방법을 살펴보겠습니다 . 이번 간단한 소개에서는 두 가지 매우 간단한 에이전트를 살펴보겠습니다. 하나의 기능 에이전트와 하나의 워크플로우를 처음 부터 구축합니다. Quadrant와 Llama Index를 함께 사용하는 방법은 다양합니다 . Quadrant를 로컬에서 실행하거나, Quadrant 클라우드를 사용하거나, 또는 Quadrant 클라우드를 동기화 대상으로 설정한 후 Llama 클라우드를 사용할 수도 있습니다. 그래서 저는 로컬에서 실행되는 Quadrant 사용 방법을 보여드리고, 마지막에는 Llama Cloud를 인덱스로, Quadrant Cloud를 동기화 도구로 사용하는 방법도 살펴보겠습니다. 자, 우선 몇 가지 필수 프로그램을 설치하고 환경 변수를 설정해야 합니다. OpenAI를 사용하려면 OpenAI API 키가 필요합니다 . 그럴 필요 없어요. 그리고 마지막 단계에서 참여하시려면 Llama Cloud API 키도 필요합니다. 이러한 작업을 완료하면 최종적으로 두 가지 작업 중 하나를 수행할 수 있는 매우 간단한 에이전트를 만들게 됩니다 . 데이터베이스를 조회 하거나 데이터베이스에 새로운 문장을 작성할 수 있습니다 . 그러니 데이터베이스 관리와 함께 사용하는 에이전트형 도구라고 생각하면 됩니다. Quadrant를 로컬에서 실행한 후에는 여기 있는 지침을 따라 진행하시면 됩니다. 이미 실행 중입니다. 그런 다음 클라이언트에 연결 하여 연결을 생성할 수 있습니다. 그래서 저는 여기에서 클라이언트와 비동기 클라이언트를 모두 설정하고 있습니다. 원하시면 이 단계를 건너뛰셔도 됩니다. 이 코드가 하는 유일한 일은 ' my collection'이라는 이름의 컬렉션이 이미 존재하는지 확인하고, 존재한다면 삭제하는 것입니다 . 이 과정을 수강할 때마다 새롭게 시작할 수 있도록 하기 위함입니다. 그리고 나서 저는 간단하게 벡터 스토어를 만들고 있습니다. 쿼드런트를 사용할 때 할 수 있는 또 다른 한 가지는 하이브리드 검색입니다. 희소 임베딩과 밀집 임베딩 모두 사용할 수 있습니다 . 그리고 여기서는 희소 임베딩 모델을 설정하고 있습니다. 괜찮은. 그걸 얻고 나면, 다음으로 할 일은 몇 가지 전역 설정을 정의하는 것입니다 . 여기서는 노드 일시 정지기를 만들고 있습니다. 그러니까 기본적으로 이는 Quadrant에게 다양한 크기와 다양한 중첩을 가진 청크를 생성하고 싶다고 알려주는 것입니다. 노드를 이러한 문서 덩어리라고 생각할 수 있습니다. 그래서 저는 이 설정을 만들고 나서 더미 문서도 몇 개 만들겠습니다. 이것은 아주 간단한 소개입니다. 음, 그래서 우리는 아주 간단한 문서를 사용할 겁니다. 저희는 라마 지수에 대한 설명이 담긴 문서 하나와 사분면에 대한 설명이 담긴 문서 하나를 가지고 있을 뿐입니다 . 이제 이 값들을 벡터 저장소 인덱스에 저장할 수 있습니다. 그래서 우리는 이 문서들을 기반으로 색인을 만듭니다. 또한, 우리는 OpenI 임베딩을 밀집 임베딩 모델로 설정했습니다 . 이 경우에는 기본 임베딩 모델인 ADA 모델이 사용됩니다. 그리고 우리는 그렇게 합니다. 이제 우리는 OpenAAI를 사용하여 벡터화된 벡터 인덱스를 갖게 되었으며, 이 인덱스에는 문서 노드가 두 개만 포함되어 있습니다. 그래서 우리는 이미 이것을 활용해 몇 가지 작업을 시작할 수 있습니다. 우리는 간단한 시뮬레이션을 할 수 있고, 저는 "사분면이란 무엇인가?"라고 질문할 수 있으며, 이상적으로는 제가 벡터 인덱스에 가지고 있는 맥락에 기반한 답변을 받아야 합니다. 이 대신에, 우리는 그것이 답할 수 없는 질문을 던질 수도 있습니다 . 투아나는 누구인가요? 이상적으로는 투아나가 누구인지 모른다는 답변을 받아야 합니다 . 이것은 아주 좋은 예시입니다. 왜냐하면 다음 단계에서는 정보가 누락된 경우 해당 정보를 우리가 보유한 컬렉션에 추가할 수 있는 에이전트를 만들고 싶기 때문입니다. 에이전트를 구축하기 위해 가장 먼저 하고 싶은 일은 간단한 기능 에이전트를 만드는 것입니다. 함수 에이전트는 기본적으로 도구 목록을 받아들일 수 있는 에이전트입니다. 이러한 도구들은 파이썬 함수일 수도 있습니다. 이 경우에는 이러한 파이썬 함수가 두 개 필요합니다 . 하나는 '문장 쓰기'라고 불리는 것으로, 문장을 입력받아 인덱스에 삽입하는 역할을 하고, 다른 하나는 '쿼리 컬렉션'이라고 불리는 것으로, 위에서 보셨던 래그 앱과 매우 유사하며, 단순히 쿼리 엔진에 쿼리를 보내는 역할을 합니다 . 그럼 이 두 함수를 만들어 보겠습니다. 그리고 여기서 제가 하는 또 다른 일은 몇 가지 설명을 제공하는 것입니다. 이러한 설명은 매우 중요합니다. 왜냐하면 우리가 에이전트에 사용할 기본 LLM(로컬 라이프사이클 모델)이 들어오는 사용자 쿼리에 따라 어떤 것을 선택할지 결정할 때 이러한 설명을 사용하기 때문입니다. 여기서 쓰기 문은 컬렉션 에 문장을 쓰는 데 유용하다고 설명하고, 다른 하나는 컬렉션을 쿼리하는 데 유용하다고 설명합니다. 그리고 마지막으로 함수 에이전트를 생성합니다 . 저는 두 가지 기능을 도구로 제공하고 시스템 프롬프트도 정의합니다. 이 시스템 프롬프트는 기본적으로 LLM에게 그 목적과 역할이 무엇인지 설명하는 방법입니다. 그래서 저는 그렇게 하고 , 쿼리하기 전에 ( 원하시면 이 부분을 완전히 건너뛰셔도 되지만) ' 에이전트 단계 보기'라는 도우미 함수를 만들었습니다. 이렇게 생겼습니다. 기본적으로 에이전트가 실행되는 동안 수행되는 각 에이전트 단계를 출력하는 역할을 합니다 . 그러니까 도구 호출에 대한 입력값, 도구의 응답값 등을 정의해 보겠습니다. 예를 들어, 이 toana를 llama index의 derel에 저장한다고 가정해 봅시다 . 우리는 이전에 그것이 내가 누군지 모른다는 것을 확인했었습니다. 그러니 대신 이렇게 해봅시다 . 그리고 이번에는 함수를 호출하여 문장을 작성했고, 그 문장은 Tana가 상대적인 라마 인덱스라는 것을 알 수 있습니다. 그래서 이제 제가 " 타나는 누구야?"라고 질문하면 " 타나는 법률 인덱스의 악마야"라는 답변을 받아야 합니다. 완벽한. 다음으로는 여러분이 직접 맞춤형 워크플로우를 구축하는 방법을 보여드리겠습니다. 음, 이것은 상담원의 동작을 완벽하게 제어할 수 있는 상담원 워크플로우를 생성하는 방법입니다. 저는 모델의 구조화된 출력 기능을 활용하여 들어오는 쿼리의 의미, 즉 사용자가 무엇을 요구하는지 분류하는 에이전트를 만들 예정입니다 . 그리고 저는 그것을 행동이라는 피데안틱 모델을 만들어 정의합니다. 그리고 작업은 문서에 저장 하거나 질문하는 두 가지가 될 수 있습니다. 그리고 'ask'는 단순히 쿼리 목록이고 'save to docs'는 명령문입니다. 그래서 저는 그것을 정의한 다음 워크플로를 만들 수 있습니다. Llama 인덱스의 워크플로는 크게 두 가지 구성 요소로 이루어져 있습니다. 사건 및 단계. 이벤트는 단계 실행 과정에서 발생할 수도 있고, 외부 세계에서 발생할 수도 있습니다 . 그리고 단계가 이벤트에 구독되어 있으면 해당 이벤트가 발생하면 그 단계가 실행됩니다. 또한 단계 자체도 이벤트를 발생시킬 수 있습니다. 그래서 우리에게는 시작 이벤트와 종료 이벤트라는 두 가지 특별한 이벤트가 있습니다 . 하지만 그 외의 다른 모든 것은 기본적으로 우리 자신을 정의하는 방식입니다. 그래서 저는 쓰기 명령 이벤트 와 인덱스 쿼리 이벤트, 이렇게 두 가지 이벤트를 만들었습니다. 쓰기 문에는 문장만 있고, 쿼리 인덱스에는 쿼리 목록이 있습니다. 이제 저는 쿼드런트 문서 에이전트 워크플로를 초기화할 때, 이 특정 구조로 응답하도록 설계된 구조화된 LLM도 정의합니다. 그런 다음 저는 조금 더 자세한 시스템 메시지를 표시합니다. 그리고 여기서 가장 먼저 할 수 있는 일은 시작 이벤트가 수신되면 LLM에게 몇 가지 작업을 반환하도록 요청하는 것입니다. 따라서 여기서 LLM은 이상적으로 사용자의 들어오는 쿼리를 분류합니다. 그런 다음 평가된 액션에 포함된 액션의 내용에 따라 쓰기 문 이벤트 또는 쿼리 인덱스 이벤트를 발생시킵니다 . 다음 두 단계는 인덱스 쿼리 및 인덱스에 저장하는 것입니다. 따라서 쿼리 인덱스는 쿼리 인덱스 이벤트가 있을 때만 실행되고, 인덱스에 저장은 올바른 문장 이벤트가 있을 때만 실행됩니다 . 지금으로서는 단순히 명령문을 출력해서 올바른 단계가 선택되었는지 확인하는 것뿐입니다. 또한 구조화된 LM에서 생성된 작업도 출력하겠습니다. 그럼 그렇게 해봅시다. 워크플로우를 만들어 봅시다. 이번에는 다른 질문으로 넘어가서, 캐스퍼는 누구인가라고 해봅시다 . 보시다시피, 여기에는 캐스퍼가 누구인지 묻는 액션이 ​​있습니다. 질문 이벤트가 열렸어요. 그래서 우리는 다음과 같은 쿼리를 사용하여 인덱스를 조회하라는 요청을 출력했습니다 . 완벽한. 이제 우리는 실제로 효과가 있는 것들로 이러한 단계를 채울 수 있습니다 . 다음 단계는 위에서 보셨던 함수들과 매우 유사한 방식으로 이러한 단계들을 채워 넣는 것입니다. 이제 우리는 실제로 인덱스에 기록 하거나 사용자 쿼리에 따라 응답할 것입니다. 그럼 질문으로 넘어가죠. 일단 이건 삭제해 두죠. 캐스퍼가 누구였더라? 네, 그래서 저희는 올바른 조치를 취했고 캐스퍼에 대한 언급은 전혀 없습니다. 그러니 대신 이렇게 해봅시다. 구하다. 앗. 이것을 저장하세요. 캐스퍼는 사분면의 데브렐입니다 . 좋아요. 이제 우리는 다른 질문을 해볼 수 있을 것 같습니다 . 우리는 아마 두 가지 질문을 할 수 있을 거예요. 캐스퍼는 누구인가요? 그렇다면 타나는 누구일까요? 네, 이번에는 두 개의 쿼리가 있었고, 이 쿼리는 쿼리 인덱스 단계로 전달되었으며, Casper가 개발 중이라는 응답을 받았습니다. 제가 지금부터 보여드릴 내용은 사분면 지표를 활용하여 실제 데이터를 더욱 효과적으로 사용하는 방법입니다 . 이것들은 모두 더미 데이터였습니다. 이건 마치 간단한 한 문장짜리 진술과 같았어요. 하지만 실제 데이터를 쿼드런트 인덱스에 기록하려면 Llama Index에서 제공하는 다양한 데이터 로더 및 리더와 Llama Hub에서 찾을 수 있는 통합 기능을 활용할 수 있습니다 . 이것은 웹 페이지를 읽고 그 내용을 지정된 인덱스에 기록할 수 있는 리더의 매우 간단한 예입니다. 예를 들어, 여기서는 Llama 인덱스 워크플로에 대한 URL을 작성해 달라고 요청할 수 있습니다. 예를 들어, 동일한 워크플로를 실행하면 " Llama 인덱스 워크플로란 무엇인가요?"라는 질문 이벤트를 받을 수 있고, 해당 URL의 내용을 기반으로 응답을 받을 수 있습니다. 마지막으로 쿼드런트 클라우드를 어떻게 활용할 수 있는지 살펴보겠습니다. 쿼드런트 클라우드를 사용하는 한 가지 방법은 라마 클라우드를 이용하는 것입니다 . 그럼 여기서 한번 살펴보겠습니다. 보시다시피, 저는 사분면 지수라는 것을 가지고 있습니다. 이 인덱스의 특별한 점은 Quadrant를 벡터 데이터베이스로 사용할 뿐만 아니라 Google Drive를 데이터 소스로 설정했다는 것입니다. 그리고 제게는 워크플로 문서 페이지가 담긴 PDF 파일이 하나 있는 Google Drive 폴더가 있습니다 . 즉, 이를 통해 Llama Path의 고급 일시 정지 기술을 사용하여 내용을 벡터화하고 Quadrant 벡터 저장소에 저장할 수 있습니다. 이제 제가 이 기능을 사용하는 방법은 컬렉션 이름과 프로젝트 이름을 제공하는 것입니다. 그러면 이전에 로컬 인덱스를 만들 때와 마찬가지로 " LLAMA 인덱스 워크플로란 무엇인가요?"라는 질문에 대한 답을 얻을 수 있습니다. 이렇게 하면 동일한 답변을 받게 됩니다 . 이처럼 쿼드런트를 라마 인덱스와 함께 사용하거나, 사용자 지정 에이전트 워크플로를 구축하거나, 간단한 함수 에이전트 도구를 사용하는 등 다양한 방법으로 활용할 수 있습니다.
