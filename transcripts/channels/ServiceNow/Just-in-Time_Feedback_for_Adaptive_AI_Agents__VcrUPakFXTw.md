# Just-in-Time Feedback for Adaptive AI Agents

- 영상 링크: https://www.youtube.com/watch?v=VcrUPakFXTw
- 채널: ServiceNow
- 업로드일: 2026-03-05
- 자막 언어: en
- 단어 수: 약 2337개

---

## 스크립트

Welcome everyone to a brand new AI research byte. Thank you so much for joining us. My name is Mary Liz and I will be your host for this session. For those of you who are new here, AI research bytes is a series of short informative talks where we showcase cutting edge research from ServiceNow's AI research team. These sessions are open to everyone, especially those of you who want to stay connected with the fast-moving world of AI research. Today's session featured a 15-minute talk from Hadi Nekoui and the presentation will be followed by a 10-minute Q&amp;A. Before we dive in, let me share a few words about today's speaker. Hadi Nekoui is a visiting researcher here at ServiceNow AI research. He's currently doing a PhD in computer science from University of Montreal and Mila. His research focuses on generalization and adaptation in machine learning with a focus on multi-agent systems and memory augmented LLM agents. Hadi's research is supported by NRNSERC and FRQNT. With that, I'm thrilled to hand things over to our amazing speaker, Hadi. The floor is yours. Uh thank you so much for the introduction, Mary Liz. I hope you can see my screen. Yes. Okay, perfect. Uh okay, so let's get started. Uh today uh I want to talk about the Jeff Hunter which was uh our recent project at ServiceNow uh AI research which is basically about a memory augmented approach to fine-tuning your LLM agents without really fine-tuning them. If you're interested, uh you can find the paper on archive by scanning the QR code. Uh I will keep the QR code at the end of the presentation as well. So, just a quick disclaimer, this is an ongoing uh research um whatever is presented here. Uh sorry. Okay. So, uh as you uh all know, like LM agents are starting to do a lot of our daily interactions with with the computers uh uh and they are kind of working, but the thing is they are still failing in unfamiliar domains, mainly because of not not being able to reason properly with new in tasks. And they can also be unreliable, and mainly because of the hallucinations that they have when they face new domains. So, there are existing solutions, for example, updating the weights of your LM agent with reinforcement learning or supervised fine-tuning. This can be too costly for open-source models and impossible for closed-source models because we don't have access to the weights of the models. Um at the same time, these approaches can result in catastrophic forgetting, which means that when you learn something new in the in your domain task, the the LM agents will forget the the knowledge that it had about the other tasks. Another family of approaches which is around like retrieval augmented generation methods. They are about basically retrieving the most relevant interactions of the agents that previously had with the with the tasks and try to replay them while the agent is interacting with the with the current task. But the thing is like as we are getting into the domain of more and longer and longer horizon tasks, these these raw replays can be quite noisy, and the agent can be actually confused by adding these replays to the agent. So, the question that we asked was can we design a mechanism to learn from previous experience um without fine-tuning the weights of the model or relying on a noisy uh rag approach. So, let me try to explain our approach with a simple example. So, here we have a uh a work arena uh sort hardware list task uh which uh is basically about sorting um um uh a list of items uh by like a specific color. For this specific task, we have the goal of sorting the hardware list by eligible for refresh column and uh due column. And both should be ascending. What agent does uh by default, it will add these columns because it wasn't already part of the list. And it starts to uh sort these items by clicking on the headers. But the thing what happens is like when the agent uh sorts the second column, the first column that was sorted gets unsorted. And then it starts to sort the first column again. Uh and the agent gets stuck into a never-ending loop of uh sorting one column and then sorting another column. So, uh now to solve this task, what we found was that you just by saying a simple hint to the agent, basically adding this hint that use the filter panel to sort multiple columns instead of clicking on headers, uh the the agent um were able to uh directly go and uh use the uh filter panel first of all. And the the rest of the actions, the agent is already capable of doing it. So, basically as you can see here, the agent uh opens the filter panel. Now, uh it needs to add uh another two two uh uh sorting criteria. The first one for eligible for refresh. So, it already adds the eligible for refresh. For the due column, it clicks on the add sort and it will enter the the due in in the field. And because the these columns are already sorted A to Z, the agent really doesn't need to do anything else other than just running the filter and the agent successfully stopped the task. So, as you can see like the what what the agent needed to know basically was just that for this specific platform of Work Arena, it's better to use the filter panel when you want to sort multiple columns instead of playing with the with the headers. Okay. Now, the question is like how we can extract these hints ideally short hints and also being useful. So, that's where Jeff Hinder comes in. Jeff Hinder is basically about turning messy offline traces into clean and reusable guidance. And how it works is basically the we have Hinder agent which looks at the traces of the base agent and um converts them into the hints and these hints are stored in a hint database which is going to be retrieved by the agent at test time. A bit more concretely, let's say we have some trajectories from the previous interaction of the agent with the tasks. Some of them can be successful, some of them can be failures. Our Hinder agent uh goes and looks at these traces and tries to find the most important steps in each trace. And by creating like structured prompt of what what happened in the first case, what happened in the second trace, it tries to reflect on these steps and generate a hint. And then these hints are saved into a database with alongside semantic key, which is basically a short summary of the situation that the agent were in. And then when we finalize this hint database at test time, the what agent needs to do is only basically retrieve the most relevant hint for the given task. So and it does that by basically comparing the the the embedding of the current like let's say we have a query for the current situation, we compare it with the semantic key in the database and we retrieve the most relevant hints. If we know the task ID, that's much easier, you can just take grab the the hints that are related to that specific task and retrieve the the most relevant hints. So the key innovation here is that this zoom and reflect pipeline, it really finds the most important steps that was either the failure points that the agent couldn't solve the task or it it was the successful trace and the agent tries to put summarize this to a hint that would help the agent to repeat it with those patterns more reliably in the future. Okay, so let's see how Jeff Mentor compares with the previous related work. So, two most important and related work that we had was first one is agent workflow memory by Wang et al. Which basically what it does is it looks at the previous traces and only generate the workflows from successful trace. The as you can see the limitation of this approach is that if you don't have any successful trace for the task, you won't get any workflow. So, in that sense the it is it cannot learn from the failures. Another work which is also very relevant is AutoGuide by Fu et al. What this work does is basically instead of limiting to successful traces, it looks at the pair of successful and failure pairs. It's still limited to the cases that you at least have one successful trace, but at least the agent can compare the successful and failure traces and try to generate hints that would help the agent to avoid the failure scenarios. But the thing is with AutoGuide that the hints are given step-wise and this step-wise guidance means that at every time a step of agent interacting with the task, you need to add an LLM call to generate a query for each step and then retrieve the most relevant hint from the database for that specific step. And this makes the AutoGuide very quite expensive at inference time. In comparison like Jeff Mentor generate episodic feedbacks from offline traces and it does it by uh, zooming on critical steps. This, uh, this means that the the agent only retrieves the hints and in the beginning of the episode, uh, and it makes it super cheap at inference time. So, it means just one simple retrieval, uh, step. And also, Jeff Hinton can learn from both successful and failures. Uh, so even if all traces are failed, the the um, the agent uh, still generate hints for those scenarios. So, like a short, uh, summary of our results, we we tested Jeff Hinton and AutoGuide under React baseline on three uh, standard benchmarks of, uh, web-based navigation and, um, like Enterprise task of Work Arena and like a MiniWoB. And we see that the Jeff Hinton, uh, which is the red line, uh, is able to, um, improve the average reward, uh, compared to React, uh, agent, which is the baseline, uh, while adding very minimal, uh, inference cost. On the other hand, like AutoGuide, uh, also improves the performance a little, uh, compared to React, but the cost that it adds, uh, at inference time, uh, it's much higher than Jeff Hinton. Another aspect of this, uh, the evaluations that we were interested in was to see how uh, generalizable the hints are. Um, and we had, uh, like an interesting mix of results. Uh, on Work Arena, uh, we observed that Jeff Hinton can generate hints that, uh, generate generalize to new tasks that the the agent, uh, has never seen, uh, when generating the hints. Um, but, um, uh, on Web Arena, uh, this wasn't the case and the the improvements were mostly, uh, within a standard deviation of the baseline. Um, so this can be explained by the fact that work arena uh, different tasks in work arena sometimes share the same patterns and same style. So, the the agent is was able to generate a hint that would generalize. But, this isn't the case for web arena because the web arena is basically solving tasks in different websites. So, this could be like interesting future research direction to how how to make the hints generalize to to unseen tasks. Um Also, quickly like looking at like qualitative analysis of how these hints were useful. We looked at like the effect of zooming mechanism and we we found that this zooming mechanism is actually very important. It can isolate the minimal causal workflow behind this the task solving the task and like by zooming on the most important steps in successful and failure traces, it was able to generate a clean and like structured hint. On the other hand, the without zooming um the hint generated the generated hint was mostly unfocused and it didn't help. At the same time, zooming become more essential as the tasks become more complex and long horizon because it's hard to fit all the steps of a trajectory in inside the context of the hint generator model. So, we need the zooming mechanism to find the most important steps. Okay, so finally, as key takeaways why why Jeff enter is important. So, Jeff enter is designed to be efficient and also interpretable compared to like standard supervised fine tuning or RL methods. You can go and see which hints were useful and even like what was the sources of the hints and which traces were used to generate those hints. So, this can help you to understand how these hints is really helping the tasks. Um, the Jeff Enter idea is basically distilling offline traces into context of our hints with Zoom and Reflect pipeline. And um, in terms of like future work, one aspect is like refining the hints. So, in in the Jeff Enter paper we uh, only did like a zero-shot generation of hints, but you can think of like having a like iterative process to refine the hints as you evaluate them in in the environment. Another interesting future work, as I explained, could be like to think how to make the hints more generalizable to to unseen tasks. For example, you you generate a hint on like sorting tasks and now how it can help the another task in for example, filtering another filtering task. Uh, okay. Thank you so much. Again, you if you are interested to know more details, you can find the paper on archive by scanning the QR code. Thank you so much and I'm open to questions. Yes, thank you Hadi. Thank you for your great presentation.
