# Domain-Specific Multi-Modal Machine Learning with CLIP

- 영상 링크: https://www.youtube.com/watch?v=uqRSc-KSA1Y
- 채널: Pinecone
- 업로드일: 2022-10-01
- 자막 언어: en
- 단어 수: 약 9877개

---

## 스크립트

Welcome everyone to this uh workshop. So today we're going to have a look at building domain specific clip models uh with an in-depth look at actually training and implementing open air clip. Um so I'm James. I'm the staff developer advocate over at Pine Cone. Um if you're unfamiliar with Pine, we're a vector database that makes it super easy to build vector search um application. Um so just before I introduce uh Feder Rico, I want to just say a couple of things very quickly. Uh if you miss anything, we are going to record this presentation. So you can you'll be able to find everything uh over on our YouTube channel on Pine YouTube channel um after this and we will also send you an email with a link to that um afterwards as well. So you can you can share that wherever. Um if you do have any questions uh put those in the Q&amp;A box. I know the chat isn't working right now. I'll try and get that fixed as soon as possible. Um so without any anything further I will pass over to Federico uh who's a postdctoral researcher at Stanford NLP lab. So go ahead go for it. Yeah sure let me share my screen. Okay so thanks a lot for inviting me. So today we talk about domain domain specific multimodel uh machine learning with clip and the general idea is that I'll go over uh what is clip and how it is trained and I'll show you uh a specific use case on which we fine-tune clip on that is a fashion use case and I'll show you a bit of the things we've been working on how we somehow implemented those I have some uh some code on collab that I'm gonna show you a bit to to explore some some details somehow of of the uh implementation and then I go also over a demo we have on streaml uh one thing to say that is that like everything uh we I'm going to show uh is open source or is going to be open source as soon as like uh we get uh communication of the destiny of our paper and also like the data set we're going to use u farfetch that is the company we've been working with is going to release it. So soon everything uh is that's explained in this presentation we will be open source. So, so hi everyone. I'm as as James said, I'm Bob Stock researcher at Stanford NLP. I'm working mainly on NLP and multimodal methods. I did a couple of different things uh during uh my uh previous research years. I have like some public uh GitHub project on on topic modeling. I was also part of the team that trained clip in Italian. I work on on reck list that is like kind of like a ballot box tool to evaluate recommener system. So if you are ever interested about like this topic feel free to send me an email and get in touch. So what we'll talk about today is clip an application of multimodel models to fashion. What I'm what I'll be talking about today is the results of a collaboration between different people. uh Patrick uh Yakopo and Shiro are the one that have like the chibi logo. So are the one for which I show the chibi logo. Uh but there are also like other people like Sylvia, Anna, Joseo that are part of different university and different institution that allowed us to somehow this collaboration between companies and university allowed us to bring what is fashion clip to life. So let me go directly into the question we want to answer. So um we know that like recent research at least in the last couple of years have collected a lot of good results for text model we know we have bird we have GPT and the last decade was the decade of like good image models so imageet and uh models trained on image that that can classify a lot of things and more recently the vision transformers. The question we want to try to answer now now is that can we use somehow model that combine language and vision for domain specific use cases. So most of the data we have is actually unstructured. If you think of for example of cataloges in the fashion domains, you don't often have annotation. You have images for example the dress and a caption that describes the dress. Is there a way the question is to build to bring these two words together so that we have a model that can use this information at the best and provide a way to build a general classifier over a different kind of task? And the answer is yes. What I'm going to talk about is a subset of the things that have been done in the last couple of years. I'll talk about clip that stands for contrasted language image pretraining and clip was introduced one year and a half ago and in the last year there have been a lot of improvement over clip but the general idea in clip is still fundamental in all the models and that's what I'm going to uh talk about I have in the slides some recent literature I'll share the slide after the talk so like if people are interested in looking at like more recent development of like language and video models like blip for example or fava or group vit by uh nvidia I think uh this could be like a a very high level sum up of what has happened in the last year for vision and language models. So clip that is the main component we are going to talk about is these big model that connects text and images. What clip does essentially is to bring images and text in the same backlo page in a way that we can compute the similarities between an image and a description of the image. Clip has been trained on 400 million example. So it's like it has been trained on data from the web and this already should like give an indication of the the idea behind clip. So it's to somehow apply a distance superfusion paradigm in which we don't need annotation. We just collect for example data from the web and we train a model that should be they should be able to generally encode text and images. The idea in clip is as follows. If we have an image and this is the image of like two people on the mountain and this is this description. What we would like to have is that we can somehow move the image in a vector space. This is a 2D vector space but you can generalize this to n dimension. And we also want the description of two people on the mountain train to uh somehow be close to these in the vector space. And this is the general idea of uh how clip is designed. What you need to build clip and this is like the basis for all somehow uh recent visual language model is an image encoder that gets the image pass through a deep neural network that can be CNN or vision transformers and gets you a vector in chosen dimensionality and you also need a text encoder. So starting from two people on the mountain you have a text encoder that could be bird or LSTDMs in for principle and you get a vector out of this. So you have two somehow towers each of one each one is going to output a vector for the modality of interest. Which data do you need to train clip is batches of images and capture. So you need for example let's assume we have a batches of size in this case two four if we consider all the elements we have the image of two people during sunset and the image of two people uh on the mountain these are the description for example two people during sunset and two people on the mountain so we have an image and the matching caption another image and the matching caption and we can look at the images and the caption in this way so we can somehow create a table uh that is that It's going to be our similarity matrix in which we want to somehow learn that the matching caption for this image is to people during sunset and the matching caption for this image is to people on the mountain. So we have like this diagonal kind of representation in which we want to eye similarity. So these two should these two should match but this element should be zero because the caption to people on the mountain does not describe this image. And this is going to be our somehow target representation matrix. So how do you actually train clip? Well, this is uh now the the the idea of the contrast develops is pretty simple and it's like super easy to implement if you can think about it. It just like once you have the text and image encoder it's going to be like super easy to implement this because we start from the text two people during sunset two people on the mountain. We pass this through the text encoder and we generate vector representation of two people during sunset and two people on the mountain. So these are two embedded representation or two vectors representing T1 and T2. We do the same thing for the images. So we pass the images to an image encoder and out of the image encoder we get image one and image two. So vector representation for the two images. What we can now do is that we know that we have this target representation. So we know that image one should match with target one sorry with text one. What we do is that we compute the dot product or the some other in this case is similarity because these embeddings are normalized but this is the idea of the similarity between the image one and text one. Image two and text one image one text two image two text two. And the idea is that we want this to be closer to one and this to be closer to zero so as to somehow represent this um idea of matching the caption with the respective image. So what happens during training is that we have this similarity matrix and we want to make this similarity matrix more similar to this target representation and we can do this with standard categorical proentropy loss and what will happen is that this similarity will go up. this similarity will go up, this will go down and this will go down. So the general idea is that we're going to use information in the batches to learn to represent images and text. What happens somehow underneath the training is the following. Think we think of like having like this image that is two people on the mountain, the description of two people on the mountain and the image of two people during sunset. What happens here is that the training is going to move the red dot far away from the other two. So this is what is going to happen. And the general idea is that after training we will have a representation that puts the image of two people on the mountain close to the caption two people on the mountain and the somehow unrelated image will be far away from this kind of uh situation. And uh this is done by obviously training on the on the similarity as we saw in the previous slide. And this is somehow a self supervised. It's not like super self-s supervised because you still need the caption but it might be like somehow effective as distantly supervised because data is being collected from the web and uh training just requires collecting this data and just run it through this pipeline. So once you have a vision encoder and a text encoder that could be like uh if you use a phase you could take a face for deer from acting phase and the vision transformer from phase you just need to generate a vector representation of for each of the of the two tower and just train this similarity uh matrix to represent these embedings. So what happens after training is that obviously what you pull out from clip is the encoder. So you have an image you generate a vector and you have a text encoder and you generate a vector. So you generate vector representation and if the training uh was successful it should be that for example this image and this caption in the vector space are similar. So they can be compared you can compute the similarity and you can use it for many different possible tasks. Now talking about task what do we need click for? why we go through this kind of uh pipeline to generate this model. And the idea is that we can use this for for example zeroot classification. Once you once we know to embed images and caption, it's super easy to actually learn uh to actually classify things because we have the image of a cat and we embed it with the somehow uh image encoder. We have the labels a cat, a dog, and a boat in the sea. And we're going to embed this with the text encoder. And again, if the training is successful, the label a cat that described this image is going to be close to the image of a cat. And so this eventually is doing classification and just looking at the nearest neighbor of uh the label with respect to the image and bad things. And this is I think one of like the the main properties of clip is that it allows you to do zero shot classification on many data sets without training on the actual data set. So it generalize confidently well. We'll see that like uh in some cases for example in the fashion domain as we apply this uh it's better to fine-tuning if you want to get better results. But the general idea is this one. You can do zero shot classification just by looking at the similarities between images and labels. Another thing you can do is language image retrieval. So that is somehow the same kind of tax as the classific classification but swapped. So we start from the description two people on the mountain. We have a vector space. We embed all the elements and eventually what we're going to see that two people on the mountain is going to be closer to this image than this image and this is somehow the effect we would expect to see. So model has never been trained on this task. The model has been trained just to put together in the vector space uh captions and images but it can somehow learn to do this kind of task uh through this kind of proxy training. So we can do both zerot classification and language mage retrieval just through this kind of distantly or self-supervised training. So as long as you have images and caption you don't need other data to train a clip model. You need a lot of data to train clip. So uh people at openai at use 400 million images. Older models recently show that you can lower this number a bit to uh 12 million images. That is still a lot of data. One thing you can instead do is that you can fine-tune the model and that is like uh using the the the recent kind of uh model that have been released. It's very easy. There is like there are script to fine tune this on a game face but also like on the original open AI kind of uh GitHub. But it is that you just need somehow uh data to do this and clip has been trained to do one thing but can do actually many more like it can be used in many different ways. So quick summary of open eyes clip clip general generalized very well on different data set it reached uh an accuracy on imaget of I think 0 70% if I remember correctly and that means the clip does this in a zero shot manner. So without seeing one example of image net and these are just some example of the classification you can do with clip. But again this shows how useful clip can be in many use cases in which you have a data set of images you want to label it but you somehow uh getting annotator is too costly for you. Uh clip is also very useful in other kind of task. This is just an example of uh image generation in which clip is used as somehow a a guiding element to generate images but also like the recent stable diffusion use a a clip large if I remember well uh to actually um learn to generate images. So there are a lot of things that you can do once you have like a general purpose model uh like CL. Now let's get into some of the the main part of uh our uh talk today. So the idea is that uh we wanted to create a domain specific multimodel model for a fashion use case and the work is currently uh under review but I'm going to show you uh most of the things we did and we will play a little bit with this but uh first of all I have like a couple of slide of like what you need to what you can look at if you want to fine-tune uh your clip. So there are a couple of different scripts that I think are or like somehow repository that I think are relevant. The first one is the original open code that allows you to load the pyarch model and uh then you can fine-tune it yourself. You just need to define uh the uh pyarch data loader load caption and your images and you can just like back propagate starting from the model paper. There is also the aging face contrastive language image uh training scripts and these are the one that have been used to uh train clip Italian. These are like very simple and they somehow are more representative of it's not a family of model but of like what is usually called hybrid clip that is not starting from clip but using a already trained image encoder for example Google vision transformer and already trained uh text encoder for example BERT to train an embedding model that puts image and text together in the vector space and this is generally referred to as uh somehow hybrid models I guess. And the last one is open clip. There is a a very nice project that has been done to replicate uh clip training on uh openly available publicly available data set for example the lion data set. There is the entire training code that replicate the the the training process of the original open eclipse. And the good thing about this is that all these are very easy to use. So you just actually need your data and you just need to change a couple of lines of code to actually uh fine-tune your clip model. There are a couple of things you might want to to keep in mind. For example, having a large batch size is very important in clip. So you might need to get good GPUs and you need at least a good amount of data to actually fine-tune this. What we used in fashion clip is one million almost one million fashion images. But but still I think like you can fine-tune it pretty easily and it doesn't cost much as we'll see uh later. Doesn't cost much to like tra take the original clip and fine-tune it on your actual data set. So what do you need as a summary is good quality data. So this is essential to get good result and I think it's the most important part of most uh machine learning processes. The code is there. You can fine-tune easily with clip but also with more recent models. But you need that to have good quality data. So good images and good captions definitely. So now let's get into the more fashion clip part. The question was can we build a clip-l like model for fashion? So fashion has somehow a more specific language than general language and we want to um our language and vision model to be able to somehow support this. So we we need a somehow model that can be trained on fashion data with low vision and that can be applied to different downstream task. So you can think of for example if you train fashion clip you might have a new client with a new catalog of images. What you can do is that you can pre-anotate all the images from the catalog using a zeros model. So you can predict if the image is uh of a a a shoe, a red shoe or I don't know a shirt or a dress whatever you can you can pre-anotate the data reducing the effort from the annotation team to actually go over all the images and annotate them by end to then put them into your uh website. And then the idea is that the fashion l fashion clip is also an interesting an interesting kind of look for research in our somehow point of view. The idea is that it is a domain specific representation of language in which we can prop the model for language understanding in a way that is not super easy to do with clip that has been trained on tons of data and for which there might be a lot of like confounding variable when we evaluate the model itself. So fasion clip has been brought by a couple of different people uh from different uh university and different institution. Farfetch allow us to use 800,000 fashion products with image and text pair and uh uh thanks to them we have been able like to to train this fashion clip that is clip fashion aware somehow and the idea of having fashion clip is that since fashion clip is not trained on classification it can actually be used on a lot of use cases and as I said before you can pre-anotate the data and for example you and classify product for styles. For example, you can predict if something is an elegant kind of shoes or it's more of a sporty shoes. And this scale up manual labor by a lot. So it scale up like the merchandiser work by a lot. But also there is another kind of uh contribution that is the fact that since fashion clip has not been trained on a domain spec on on a some of a specific brand it should be able to generalize to a lot of different data sets. So it's not been trained on a specific brand has been trained over the entire catalog of a big retailer like uh Farfetch. So it should be able to do some kind of out of the box context understanding can be used for search can be used for classification in many different ways. So what the data set that like uh the people at farfetch gave us was of 800,000 image with description and metadata and we worked somehow to build uh good caption and this is another thing that is very important. So if you have metadata, it's important to use the metadata to build the caption. And eventually what we came up with is caption of this kind of type uh in which we swapped some elements to make the model more resilient to kind of noise. But it is that this is a a green polo uh with a modern kind of style and we train the model with this kind of caption. So for training, we just use the original OpenAI model and we fine-tune the entire model. So both the image encoder and the text encoder on our data set and we tend to use a low learning rate to avoid uh catastrophic forgetting in the model. So we use this and we train the model. So the question now is is our implementation of fashion clip better than clip. So the idea is that we trained a a model on fashion data but it is better than the original clip. This somehow very important question because it can happen that when you fine-tune clip you might overfit on the data. So you don't you have a model that does not generalize or worse you end up having catastrophic forgetting and the model forgets a lot of what it has learned during the original training and that makes it like not as useful as it could be. So we tested these on zero classification task. This the image is just to remind you of how the task is somehow done. So we have the image of uh the cat, a cat, a dog and a boat in the sea. We put all together in the vector space and we look at the similarities uh with fashion clip. This is essentially the same. So we have an image and the labels. We generate the embeddings for both. So we generate the embedding of the image with fashion clip and we compute the similarities between the embedding of the image and the labels. And what we expect is that the image is more similar to the label dress since this is an actual dress. We use a couple of different data set in the literature uh fashion m the kaggle data set and deep fashion and we do classification on the categories. So we try to predict um the actual category of an item and we also use our our own in domain data set. So like the the category scheme given by far what we can see is that like fashion clip works um much better like several points in F1 with respect to the normal clip on this data set and they suggested like in this particular use case fine-tuning clip on your data is an effective way to get a better model that you can use to do uh zeroot classification on unseen data. So kaggle fist and deep are completely unseen by the model and it show that like the model can generalize to kind of this new data. The other task is like uh this is not the shot this sorry this should be like image uh retrieval is again getting the caption and finding the the corresponding image in the data set and the scheme is most mostly the same the same as the one before. So we have a red address. We generate the embed links. We embed all the images and we find the image that is most similar to the captioner address hoping to find this kind of uh image for example. And again the result these are mainly on our uh farfetch data set show that the model can actually uh learn retrieval much better than clip. So on our test set it performs much much better. Then we had like some out of category. So what we did was kind of like a data set ablation in which we removed uh a c an a category on the data set that was I think sportsear and we remove all sportsware from the the data set and then we used clip the fine-tuned clip to somehow classify sportsear and it worked better than clip but also we tested by removing brand. So we removed two brands from the data set. We fine-tuned the model and then we compared the performance in zeroot brand classification on uh the sorry in zeroot category classification on ret sorry zero shot retrieval and we actually saw that fashion clip performed better even if it had an access to that data uh over any kind of similar data over training. So that suggest again that like fashion clip generalize and uh it works well on retrieval but we'll see some of these I guess in in in the demo I have uh ready for you. So summary of uh fashion clip so uh the first result is like fashion clip bits clip in several fashion related benchmarks also on Lout data set but also on like out of distribution fashion data. But the cool thing is that like training fashion clip is relatively cheap. uh training clip. This clip for a couple of ebooks costed $31. Uh and you have like there are more more details in the paper, but the general idea is that it doesn't cost much to fine-tune clip on your uh domain specific data set and the improvement is actually a lot. This is just fine tuning. Obviously, training clip from scratch requires much more time. But if you just need to fine-tune, doing this is actually easy. and effective to do. So now what can we do with fish and clip? So we know that it works well but are there like some kind of thing we can do that are fun to play with with f clip and the answer is obviously yes. One very cool thing is that it works super well for product search. So when we search for example for t-shirt with a cat, what we find is actually like an image of a t-shirt with an actual cat. And this does not happen when we do like text search on the search engine. So it brings a brings a huge improvement on the search engine because it cannot it actually learned to map specific properties of object to the vector space. Look at for example light red dress in which it actually finds a light red dress but this does that for text only search that has no content understanding of what light red means. So the good thing is that clip learns uh somehow to and let me use the word understand this kind of concept and helps you to find this um representation in the vector space. Another thing is actually uh product search. So we have built this uh demo app that I'm going to show you in a bit but it is that you can search for different objects and find interesting object in the vector space. This is like for example strip charts and it should indeed find strap chart. Like I said everything is open source. This web app will be open source soon as soon as we get back from the people that manage the data set. But you can already see why this can be useful for like a domain specific application uh for example search on a search engine website. Other very interesting thing is that we can study the relation between language and images and this is more interesting I think under a point of view of um research but it's also cool that we can explore the space and better understand what the model is looking at. So one thing that like uh Rafael that was uh invited I think a couple of weeks ago to speak here at Pineon uh was this occlusion algorithm for linguistic localization. The idea is that we want to understand what the model is looking at when we input the query a cat. So for example, if we have the image of a cat, we generate a vector representation. We have the query a cat, we generate the vector representation. And if we compute the similarity between the image and the text, we get for example 0.8. What happens if we start to put black patches on the cat, it happens that the similarity drops. So the more we cover the cat, the more the similarity drops. For example, this is a bit higher because we can see more cat. And this the similarity is at the lowest because we cannot see the cat. With this process, what we can actually do is that we can actually learn where the cat is by looking at what happens when we put black patches on the image. So when the similarity drops when there is a black patches, it means that the algorithm has found something. So in this case uh since the similarity is very low behind the black patch there is a cat. And this for example can be like couple of example. For example, if we search for the word cat ear, what the model will do by combining different patches is that it's going to highlight that behind ear that was actually a cat. And obviously we can do the very same kind of thing with dog. So same image we search for the word dog and the model is going to like this part that is actually phase of the dog itself. So now some example of like the linguistic capabilities of fashion clip and the idea is that you can perform obs with major retrieval. This is a shared with a tiger on it where the caption has never mention tiger. So the model learn this from the actual training process and generalizes across different kind of concept. But the interesting thing is that we can explore the space and we also can classify the space by looking at how clip recognize concept. For example, this is a an image of a of a polo. And when we search for short sleeves, the model is going to highlight this part. When we add the image of like this uh jumper, it happens that if we search for long sleeves, the model is going to like this part and these parts that are the actual long sleeves. In this example, we search for the word bag and the model actually highlights this part. And if instead we search for the word jacket, the model is going to highlight another part. So the model actually with this process we can probe the model for like understanding of um fashion specific terms and see if the model learns actually to recognize where things are in uh the image itself. This is for example the recognition of a logo. And this is the same logo but smaller on an image. And again you can see that the model is able to recognize where the logo is and detect uh it in the image. And this is how the model is probably able also to classify stuff because this can be used also to classify along the brand axis of uh the data set. Another important thing I guess to look at is uh the uh generalization of this model. So the entire thing we have been explaining here um rest on the idea that fashion clip generates good representation for images and captions and we wanted to a bit open some of this fashion clip model to better understand what's behind it and what can be used uh to do multimod sorry domain specific multimodel machine learning. First example is the figure on the right in which there is the plot of like uh clothing concepts by embedding images between fashion clip and clip. Uh there's the silhouette score is not reported but it's higher for fashion clip. So fashion clip generates better for example image embeddings than clip and it cluster together things that are similar so that it can somehow be used to do this kind of like computational representations. The idea is that we are able to ground somehow content and this is what makes somehow fashion clip very useful because as we saw before we can recognize different objects. For example, we can recognize where is the ankle strap, where is the actual sandal, where is the actual high heel of the object and this is what we can use to actually probe content in our representations. Another thing we did to better understand the generalization is to test fashion clip on things that do not exist that we called improbable products. And um this comes more from likely how somehow children learn that they generalize to new concept very quickly. And the question is can this model generalize to new fashion concept very quickly. So for example the idea was uh to generate novel products. For example, this is a product that does not exist. That is a night a Nike uh cocktail dress, a black Nike cocktail dress. And the idea is that we want the model to we want to see if the model is able to effectively predict that this is a Nike long dress with respect to other kind of the structure capture. So again, this dress does not exist. We have these labels uh some of which are obviously the structures and when we tested the model on a bunch of different product we saw that like the model effectively is able to detect the correct somehow uh description and in this contest is Nike brass so the model somehow has this kind of ability to ground the language and to deal with the language composition to some extent uh this does not work in all cases but it's nice to see that to some extent this model is able to capture uh some kind of like linguistic compositionality. That is eventually what we would expect to get from our somehow uh deep language model or deep language models. Just to show this is what happens when we uh do the search of the uh brand and we find the actual brand image. Another thing we did was like to see if we can apply this in another context. For example, um there are sometimes you want to find you you find an for example a nice t-shirt, but you want to get it darker or you want it, for example, longer. So, can you somehow walk through the space to find um items that are similar to your original items, but that have just one attribute that has changed somehow in uh in in some representation. And uh what you want to do is that you want to move in the space by keeping many attribute fixed but you want to move along the axis of one relevant attributes. For example, you have a shirt and you want to get it like of another color without changing the shirt itself. The idea is that what we saw is that like fashion clip but in general also clips encode geometric properties of the space in which we can actually explore the space and we can actually better understand uh the representation and the relationship between different images. But we can also search in the space for things that are darker or things that are longer by just moving along the axis of uh the space. So what we do behind this is that in general we uh move across the axis of one of the attributes and try to find similar things. But what you see here is that for example from women high heels we want to find women flats. So more flat kind of things but we want to keep somehow the style consistent. This is for example uh the changing color of of a polo going from being dark blue to more light blue. Uh this is a somehow what zero shot recommener we implemented. uh the paper describe everything more in detail, but it's somehow a a cool application of what uh a your fine-tuned model can do uh easily on uh on novel data. So I I almost reached the last part of uh this part of the talk. I want to mention a couple things that I think are super important to mention. First of all, clip is like I said not the the somehow latest model in literature is definitely one of the most uh uh important one if you want to use this word but the idea is that there are no innovative kind of additional uh losses that you can add to the contrastive loss and I suggest uh to look at blip that is a very interesting model that reach very strong performance on a couple of different task training code is online and again you just need uh your data and um to to start training this model. Uh but there are like in the first couple of slide there was a list of like the recent literature that I suggest to to go over to actually get a better idea of what different research teams have been doing on this thing. The last thing is I want to mention is that these models are still biased. So there are works in liter that show that like there are problematic and stereotypical association of images and text done by these models and uh you can for example see these in the openi clip paper in which they somehow probe the model for different kind of attributes and you show that like the model has learned bias from the data and this is somehow expected since they got data directly from the web probably without super hard check. uh even public data set like the lion data set contains semi stereotypical data set. Um it's it's hard to tell people not to use these models. I think it's but I think it's relevant that we are all aware that like this model have problems and when we use this in for example production it's important to check uh some of these issues and see if we can somehow reduce the bias in in our models in production. So quick notebook and demo time I guess. Now let me move this. So I prepared a couple of line of code here. So this is my clip proper. So what I'm going to show you is that uh we can create embeddings with fashion clip and we can use this to do some kind of search. So for example this is I what I'm using here is the KGO data set. So it's a data set of uh I think it's 10,000 images not much more I guess. So it's not very informative but it's small enough that I can run it on collab and do some uh minor experiments. So what I can show is that I have a function that takes um clip image encoder and generates an embedding. So I'm generating the padding for uh the image number 10 that is like is this um is this image. So let me do this wh so the shape of this object is 1 512 where 512 is the dimension of the embedding of uh used by clip. So let me remove this and just show a couple of these are the dimension inside uh the embedding. Oh yeah forgot this. So this is the dimension inside the embedding. What we can do for example now is to create uh text embedding. For example get text embedding of for example this is a a black shirt and a red shirt. These are text embeddings. Again the shape should be two uh by 512 exactly. Now what we can do is that we can do zero shot classification. So we want to see if we can classify this correctly as a black shirt. I mean it's a black with yellow lines but this is definitely more of a black shirt than a red shirt. So what we do is that from ship py space shelter distance I'm going to import cosign function to compute the similarity between this minus coine of image uh this is the first image and the first text and we going to do the same thing for the second text. And as you can see the similarity between a black shirt that is text zero is higher the similarity uh between the image and the red shirt that is that is text one. So this is shows like a bit of classification. You can also like do this for just color classification and I guess you can also do uh short sleeves versus long sleeves. I did not try this. I hope this work. Yeah, it works perfectly. So the idea is that this is a short sleeve kind of shirt and this is and obviously it's not a long sleeve. So the first score is high. Look at how simple it is once you have like the model trained to generate the embeddings and start doing this kind of classification which you can just get like this big catalog of image and you just pre-an you can just pre-anotate it with a bunch of different factors. So I have the embeddings already computed for the entire data set. So let me see how many images I have here. Let me just check that like these are normalized. Okay. So what I've just done is that I I've previously computed the embeddings for all the 10,000 images in the data set. So we don't lose time loading the data set itself. Uh but what we can do now is that we can try to do some text retrieval. So how text retrieval works in this way is that we get the embeddings of for example a green shirt that has we said textshape mistype again shape it's one times it's 1 by 12 and we compute the similarity between all the text embed sorry between the tax embeddings of interest that is a green shirt and all the image embeddings things. So what text image scores is going to hold is the similarity between our caption of interest a green shirt and all the images that are as follows. So what are we going to do is that we are going to get the most similar image ID that is the argument with the maximum value. So it's the the the ID of the highest score and it's image with ID 8716 and we show the image and it's indeed a green shirt. If we want to do a red shirt we can do the same here and it should be a red shirt uh an elegant shoe for example. I mean we can see this in better in the team I guess and we get an elegant shoe. So we can actually search the vector space for image of interest but we can also do uh zero shot classification. So let me try to green shirt again. And this is our green shirt. So let's take the green shirt again and let's try to do zero shot classification a bunch of like kind of uh labels for example colors sleeves uh kind of thing. So these should be colors and if we look at the representation we see that like green is the one with the highest color. So I am applying a softmax to just get to a probability distribution but it is that green has the highest score instead of leaves. This should have like short sleeves or at least is expected and yes it this item has like highest probability of being short sleeves uh than long sleeve. So this is idea of like how you can do things with just the embedding space. Now I have the emo that like it it might be more interested uh interesting to look at. So one thing we can look at is for example shoes. So these are shoes. We can do shoes for women for example. Sorry again. These are shoes for women. Uh we can do elegant shoes for women for example. We can do elegant shoes for men and we get elegant shoes for men. But we can also do sport shoes for men. And again here we are searching on a on a embedding data set of like 20,000 images. And look at how precise the model is in finding like uh images that match. For example, you can have a blue shirt. You can have a red shirt. Maybe I want a red shirt with long sleeves. Maybe I want a red shirt with long sleeve just for women. And you see like all these item tend to be pretty good prediction. Or I want a red shirt or long drink with for men. You can also search for like red jumper. But yeah, you get the the idea I guess behind this. And um the the the big value I think behind this is that it offers the possibility of doing this kind of task in a manner that does not require annotation. You can easily fine-tune your model using standard scripts that are online. The loss itself is very easy to interpret and it's very easy to explore the space even if you are uh uh not like kind of a a core ML person because it's just like having things that are close together close together and having like image that don't matter far away. Another thing I can quickly show is like the classification part if this works. For example, here we have um Nike dress. So again, this is the classification and we have Adidas dress, black night dress and Nike tank top as classification labels and the model correctly tells that this is a black Nike dress. We can also add another label that can be I don't know that doesn't make any sense. But I guess that again the model is going to predict uh black uh Nike dress. Yeah, I think that's it from my side. I'm happy like to answer any question regarding like uh what we have done and like uh which are like the the things we we did or like if you have any any question I'm happy to answer that maybe close this I guess. Yeah, that was really interesting. Thank you, Federico. Um, so yeah, you have a you have a lot of questions, so we'll we'll go through them. Um, so we'll start right at the start. Um, so from Abanov, uh, he asks, "Do do you also create native samples during training?" No. Uh, that's a very good question. So no. Uh, negative sample can actually increase a lot. Uh, the the performance and also like reduce training currently. No, because like it's not super easy to find negative sample for both the text side and the image side for uh the fashion domain. Uh one thing we could have done was like search for in batch uh difficult image to classify but that also requires a lot of computational power because every time you need to recmp compute the similarities between all the images. So it's not super efficient. Oh no, that is a great suggestion and like if you have computational power to do this and negative sample is like a a very good good solution to apply to to these kind of things. Cool. Thank you. Um so another one from Hesh. So he just wants a bit of clarification. Um so he understands that text and images have different embeddings but the same dimension and in the same vector space. Um and he wants to know if training using contrastive loss helps both of those vector spaces align. Yes. Yes, that is the idea. And also like if you I I didn't mention this but actually if you look at the loss of clip you see that when you reconstruct the similarity matrix the loss that the openi folks have implemented but also like comes from a a a paper of like a couple of years ago on um medical imaging is a contrastive loss that look at the classification score both row wise on the actually image uh sorry on the actually image versus all the caption but also like column wise that is caption against all the images. So yes, that is the idea of like somehow the contrasted loss. Oh, thank you. Um, so one from Joy Deep. Um, so I'm trying to normalize two vector spaces, one for voice and the other for speech text both using the same voice into one embedding space run a neuronet network for sentiment classification. Um will dot product work in this use case as demonstrated by you? Um isn't dot product a scalar number and where is the normalization? Sorry, can can you I I'm not sure I understood the process like which is yeah so he has two vector or he has two items or two vector spaces. One is for voice and the other for speech detect. um he's trying to put those both in the same embedding space and then or or both put both of them into the same vector maybe um to run a neuronet over those sentiment classification. Oh, okay. So, I think he's asking how should he put those two vectors together? How should he put the voice vector and the speech to text vector into the same vector space? Would you suggest using dot product? So or should you normalize is so if I understood currently there are two modalities and both information had to be used to do prediction on for sentiment is this like task that's yeah I think so I'm not sure if like the the the the task requires this kind of contrastive alignment like if you already have the data and you want to do sentiment classification you have like sentiment uh annotations, you can train the model to do this kind of classification. If instead your task is like to compute somehow the similarity between the two different modalities you have, that's where I think the dot product can indeed be helpful. I'm not sure if like if for a a future sentiment classification task bits can be useful, but like I'm I'm not like very uh knowledgeable of like uh speech data, so I might be like getting this wrong. Yeah, no worries. Um, okay. Thank you. So, another one from from Abinov. Um, he wants to know if if he wants to retrieve images from a query using clip and partly using a sparse retriever on captions from a database like using BM24 25. um is there a way for ranking the retrieved images in order for which one is better? So I I think trying to merge both the the dense vectors and the vectors and and ordering them in some way that depends a lot I think on the the specific domain. Uh there are domain in which I think like sparse retrievers still work uh very good. what I think that like the dense retriever like clip is able to capture is properties like colors for example that are often not mentioned. So the the thing is that like I think the best thing is like maybe to combine the scores with a weight so that you can somehow uh get the best of both worlds. But it's hard to say without looking against at the actual data but you can definitely like take the scores normalize them and then create a combined score to to order all your uh resulting images. they they definitely look at at different things I guess. Uh the the retriever the clip retriever is going to look at the properties of the images while BMP5 is going to look at like description in the text. So the best way to to combine these depends a lot on on on the data itself I guess. Cool. Interesting. Uh thank you. So um one from we only have a couple of minutes left so we'll try and go through them quickly. uh from emit uh he wants to know if you faced any overfitting issues while fine-tuning both the encoder weight released by open AAI um what learning rate did you use and did you use any data augmentation yes so the answer is yes uh it's not only overfitting but it's also like catastrophic forgetting that is an issue and we used like the learning rate it was like one exponential minus five and the things that I suggest that people tend to suggest now is that if you have a good image encoder such as uh the vision transformer from clip you should freeze that and just train the text encoder. This was introduced by the delete paper. There is one it's called locked image tuning I think the title of the paper. Uh but in this paper they show that like if you lock the the image tower a good image tower you can just fine-tune the text encoder and get the result for data augmentation. No. And the answer is uh the difficulty of the documentation in this context is that uh we don't know what happens like we had like very standard images and we didn't want like to have some issue for example with positional elements. So if you applied for example horizontal somehow flipping you completely destroy the concept of left and right and clip. So it's always kind of hard to understand which kind of uh argumentation to use. You can definitely use prospective uh perspective augmentation uh but we didn't use augumentation in this context. We already have this I guess. Okay, cool. It's interesting. Thank you. Uh so if we have enough time for one more question that would be that would be great. Um and then the rest of them we we'll try and get around to them after the after the talk if if possible. Um so quickly from um oh from uh Casilus uh how did you deal with potentially duplicate data and and how does duplicate affect a model? So I don't have an answer to this question because the data was cleaned by people at Farfetch. So I'm not sure which is the process they actually use to uh clean the actual data. I think duplicate duplicated data is not super bad if it's not too much, but I don't have a real answer to this. Okay, thank you Federico. That was a really interesting talk and there there are a ton of questions. I think you definitely captured a lot of interest there. So, uh yeah, thanks again. Uh it was really interesting. Thank you so much. Everyone else, thank you for joining. It's a great talk. Thank you. Thank you. Goodbye.
