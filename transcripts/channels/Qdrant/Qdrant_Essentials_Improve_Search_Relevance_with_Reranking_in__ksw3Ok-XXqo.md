# Qdrant Essentials | Improve Search Relevance with Reranking in Qdrant

- 영상 링크: https://www.youtube.com/watch?v=ksw3Ok-XXqo
- 채널: Qdrant
- 업로드일: 2025-10-22
- 자막 언어: en
- 단어 수: 약 606개

---

## 스크립트

When we use quantization methods like scalar, binary or product quantization, we are compressing our vectors to save memory and improve performance. However, the compression can slightly reduce the accuracy of our similarity searches because the quantiz vectors are approximations of our original data. So to mitigate this loss of accuracy, you can use oversampling, rescoring, and re-ranking which can help improve the accuracy of the final search results. So let's say you are performing a search in a collection with binary quantization. So quadrant retrieves the top candidates using the quintise vectors based on their similarity to the query vector as it is determined by the quintise data. So this tab is fast because we are using the quintise vectors. But since some relevant matches could be missed in the initial search to compensate for that we will apply oversampling which means that you will retrieve more vectors more candidates increasing the chances that the most relevant vectors make it into the final results. So for example, if your desired number of results is four and you set an oversampling factor of two, then quadrant will retrieve eight candidates four * 2 and more candidates mean a better chance of containing higher quality top key results at the end. So after the oversampling together more potential m matches, each candidate is then re-evaluated based on the similarity criteria to ensure a higher relevance to the query. So this process is actually the rescoring process. So it actually works by mapping the quintise vectors to their corresponding original vectors allowing you to consider additional factors like context metadata or additional relevance that was not included in the initial search and this actually leads to more accurate results because you're going to be then comparing to the original vectors. So during this rescoring process, one of the lower ranked candidates from the oversampling might actually turn out to be a better match than some of the original top K candidates without the oversampling. And even though rescoring uses the original larger vectors, the overall process still remains very fast because it's only a very small portion of vectors that are being compared. After that, we get into the reranking. So with the new similarity scores from the rescoring reranking is where basically the final top candidates are determined based on the updated similarity scores. For example, if we're retrieving a top K of four with an oversimpling of two, then a candidate is ranked in the eighth place with the initial quantise search, it might improve its score after the rescoring because the original vectors capture more context. And as a result, this candidate couldn't move into our final top K after the reranking um replacing a less relevant option from the initial search that we had. So if quantization is affecting performance in an application that still needs high accuracy, then combining it with oversampling and rescoring and reranking is a great strategy. But of course, this all increases the amount of latency in each query. So if you need faster searches and can tolerate like some loss in accuracy, you might choose um oversampling without rescoring or adjust the oversampling factor um and play around with those parameters to see uh which type of settings works best for your use case. In short, scaling to large data sets about smart configurations. In the next video, theory will talk about best practices for large scale injections in Quadrant uh configuration tunings and everything you need to know to work um with large data sets in Quadrant so you can confidently scale up your Quadrant uh pipelines.
