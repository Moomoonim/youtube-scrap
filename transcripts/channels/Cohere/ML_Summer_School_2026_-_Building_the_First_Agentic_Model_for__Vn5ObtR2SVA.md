# ML Summer School 2026 - Building the First Agentic Model for Edge Devices with Maxime Labonne

- 영상 링크: https://www.youtube.com/watch?v=Vn5ObtR2SVA
- 채널: Cohere
- 업로드일: 2026-07-29
- 자막 언어: en
- 단어 수: 약 8096개

---

## 스크립트

[music] All right. Um, so in this talk I want to talk about how we designed our upcoming model that is a small agentic model and I want to do it not only from a training point of view but also from a harness point of view. So hopefully you will find this presentation useful um with a comprehensive approach uh from architecture design to downstream application. Okay. Um let's start with a simple question. Why are we interested in edge models at all? Um if you look at the headlines, I think frontier really big models are really the most popular ones. But edge models have a lot to offer. And the first thing is cost because the tokens are free. You do not pay an API token here. Uh you just care about um the inference on your own device. So basically the tokens are free to generate. A second thing that is very interesting is the latency. Uh because it's hosted locally, it means that you don't have the latency of a remote model. Plus these models are small. So usually they're also faster by design. The third thing is connectivity. Um with API models, you cannot really power applications um that cannot rely on a stable connection. So for example, you can think about automotive applications. Um this is very risky uh to rely on a stable internet connections here. And the last one is privacy. um because your data never leaves the device. It's always private and you don't have to uh worry about what you share with the model. Everything stays private. Um all these properties are very nice but it also comes with three major challenges that I want to talk about now. So the first one is that it must uh fit on a phone and that can be quite difficult um because it it means that the memory usage that you have is very limited. So it will constrain the size of your models which is why edge models are typically very small. The second thing is that it must be very fast. Um if you run this on a device um like a phone or like an old CPU basically most of the deployment scenarios involve um more like low-end hardware resources. It involves a lot of CPU. So you need to have a model that run fast mostly on CPU. And the final thing is that it must be great at agentic task if you want to use it at for this purpose. So if you want to summarize these three challenges, uh the first one is is about memory usage. The second one is about inference speed and the third one is about quality. And we want to make sure that we maximize this triangle um and we optimize the model from the architecture design to the training and downstream deployment uh to make sure that um everything is as good as possible. So let's start with the architecture. And by architecture I want to talk about mostly the layers that I used inside of the model uh from a inference speed point of view mostly. So let's take two models um GMA 3270M and quen 3.50.8B and you can see that they both have a high architecture where they um interle some kind of um uh attention uh with another type of layer. So basically you want to have something like full attention like GQA to have recall and be able to remember all the past context um inside of your input but you also want to interate with some kind of efficient um layer like sliding window attention so it's not too expensive to run. So both of them uh run this kind of hybrid architecture and both of them are really good architecture. They're very fast. A problem that you see if you dive a bit deeper into the embedding layer is that um they're very big. Uh basically the vocabulary size of the tokenizer is huge for such small models which means that the embedding layer is most of the model for GMA 327TM and that's an issue. Um it's probably due to distillation. They train these models by distilling them from bigger models which is why they have these huge uh vocap sizes. But it means that in practice the effective size of these models is very small. If you look at Gemma 3, it's mostly 100 million parameters that are actually used for reasoning and not just for embedding. And for Quen 3.5, it's about 600 uh million parameters. So that means that the model will have fewer model fewer parameters to reason with and it means that the quality will suffer. So when we design our model uh we try to take it with a small vocap size uh to make sure that the effective size is pretty much in line with the total number of parameters. Here you can see it's about 90% of the model params. And we created a particular layer that we interled with GQA which is this shortcom layer. And to design this shortcom layer, what we did is that we really evaluated it um on device with real hardware doing profiling in loop and we really monitored um the inference metrics that this layer um would provide us. So here are two hardware targets that we um optimize uh the mole for and with this inference metrics we have like real signal because there's often a gap between the theoretical um gains that you have from a layer and the practical ones where it can be a lot worse than you imagined which is why this ondevice profiling was really essential here. And here you can see a um representation of this shortcom um also called gated short convolution block. And if you want more information about this, we have a LFM2 technical report online. So you can find all the uh relevant information about how it was designed. But I want to talk about the gains that we have here. And you can see on the left the short conf layer and the cost of each other layers. So you can see that on this M4 max CPU uh for decode speed it's uh pretty much twice as uh efficient as GQA and you can compare it with the other layer types that we saw uh for Gemma and Quen. So we really optimize it for speed and this is something that you see if you really benchmark the model with CPU inference metrics here with prefield decode but also memory and you can see that compared to similar size models it is a lot faster not only on CPU um but also on GPU here you can see it um with um different levels of concurrency as well. All right, that's it for the architecture side. As you can see here, we talked about the size of the model which corresponds to the memory usage and we also talked about the inference metrics uh because we wanted the model to be fast to be really deployable on device. This is particularly true if you have an agentic model that needs to handle long context and also be very responsive because that's what people are used to. Let's now talk about the quality aspect of it. So this is mostly about push training and if we look about if we look at the post training history the timeline you can see that we had the dark ages with the SFT era where push training was basically supervised fine tuning and we have like a key papers and releases um from this era and then we had the policy optimization the XPO era uh with a lot of papers published about direct preference optimization and a lot of different flavors of it. Um, so that was nice. And then DeepSync R1 made a big splash about the reinforcement learning with verifiable rewards um strategies that you can take. [snorts] And uh this era is um already over because we started the new one and this new one is about multi-dommain on policy distillation or MOPD and this is what we're going to talk about here. So this is our training recipe at liquid for upcoming 2.6P model. We use multi-dommain on policy distillation with agent reinforcement learning. You can see the top row is about how we train the specialists the teachers. So instead of training the model directly on math knowledge code um etc we train some teachers on them and then we merge everything into one single student you can see at the bottom with multi-dommain on policy distillation. Then we add agency reinforcement learning because we want to make sure that this model is really good at using um agency harnesses and that is how we got the final model. So let's talk a bit about SFT. SFT is mostly about how you create a lot of highquality samples which is something that is often overlooked and in this presentation we will overlook it once again. uh we will mostly talk about the consequences of SFT and a particular problem um especially which is doom looping. So here you can see an example of a doom loop. You prompt the model um with an instruction and you can see here that in the answer we have a lot of repetitive uh um substrings and this is something that often happens with small models. It also often happens when you have reasoning traces. So when you have a thinking model and finally it often happens when you have complex task. So when you have a small agentic model you have all three of them at the same time which is why um dunlooping can be a real issue. So at liquid we developed a solution uh for this uh we called it anti-doom. And the idea here is that you're going to use a data set of prompts and you're going to generate answers with uh your model and then when it starts doom looping you're going to look at the token the first token of the doom loop and you're going to down sample it. You're going to reject this token and you're going to upsample uh alternative tokens. So here's an example where you have um this generation from the model that is starting and then after the weight token it um creates a doom loop which is a very common scenario. So what we do here is we teach the model to reject this particular weight token and we want it to replace it with let's yes or okay tokens and that is very um successful because you can see the doom loop rate uh growing from 10.2% with our model to 1.4% 4% and even with coin 3.54b it goes from 22.90% to only 1% of doom loops. So there are other ways of um solving this issue or trying to reduce them the doom loops. A very common one is repetition penalty uh which is a generation parameter that you can add but it's more like a patch after the model is already released and here we really want to train the model to refuse doom loops. So I would say that this approach is a lot more robust and you do not need to force users to have very specific generation parameters uh because the model has been trained not to do loop. So if you're interested about this technique, um we released the code, released the data set and we wrote a blog post about it. Uh so everything is available online. All right, let's go back to your training recipe. Uh so we talked a bit about the SFD stage and mostly about the consequences of it. Um let's now see the reinforcement learning stage that we use to train our specialist and the MOPD stage that we talked about. So um by reinforcement learning here I'm mostly talking about reinforcement learning with verifiable rewards which is a very common setting uh that we saw with dipsy car one and related works. And here you can see a prompt um and a answer from the model. And what we do is very simple. We just take we just extract the answer from the response. We have a ground truth answer from the data set. We compare them and we see they're correct or not. So that's kind of the general framework. The intuition behind it. A little problem that we see with this is that you only get the signal at the end of the answer. So here we generated 24 tokens and we have only like one bit of signal. Um this is not really a problem. But if you have super long generations that multi-turn and like 100k tokens, this becomes quite inefficient. So a way of making this uh reward this signal a bit denser is using on policy distillation. In on policy distillation, we still have this answer from the student, but instead of extracting the final answer, what we want to do is that we have this teacher model that does a forward pass and that basically rates um each token that the um student generated. And it's going to use its own probability distribution for each token to rate them. And you can see here that the first two tokens, fantastic question, have a very high KL. And this is because it's probably slop and the teacher model is not very happy about it. So the teacher model is telling us no, you shouldn't uh answer fantastic question to this prompt and you should probably answer like all the tokens at the very beginning. So here you can see that the reward is very dense because we get um one piece of signal for every token. Um so it's so dense that actually it can be a problem if your teacher is not very well calibrated. It means that it might reject everything. And this is a common problem with on policy distillation is that if your teacher is too far away from your students distribution, it means that it might think that everything the student says is wrong. For example, in this case, if all you get is like this is a wrong token, this is a wrong token, this is a wrong token, there's not much signal that is created and training can just derail and crash. So now that we talked about this um a natural way of expanding on policy distillation is using several teachers instead of one and basically you have different flavors of it but you can just route um each prompt to a particular teacher. So if you have a math question you can route it to your um math teacher. If you have a code question you can route it to your code teacher etc etc. So the idea behind MOPD is that with reinforcement learning with verifiable rewards, we have some degradation that happens. For example, if you train the same model on math, you're going to have writing style that deteriorates. Then you try to do um reinforcementing with human feedback and then insert and following is going to drop. So each stage kind of fight each other. So it's not very nice and you keep losing and gaining scores in across different domains. Um MOPD is a much cleaner approach that ensures that everything kind of rises together and you don't have this problem of fighting different domains. um the requirements as I mentioned is that the teacher uh should be very much um close to the student but it should also be significantly better than the student. So usually people assume that okay if I have a very good teacher it doesn't matter I can just reuse it um here and it's going to make my student a lot better. This is not true. This is not enough. This is not sufficient. You also need the teacher to be uh very close to the students um distribution otherwise you're not going to learn much and the payoff of this technique is also that it converges very very quickly. Um so that's super interesting. Um and with just like 30 steps you can basically uh finish training. So you spend a lot of compute training each teacher but once they're trained it's quite efficient to merge them into uh the same student. And the little um thing that I wanted to to mention like based on on um practical experience is the reverse KL versus the forward KL. So we have two ways of uh comparing the output distribution of the student with a teacher and if you have reverse scale it's called mode seeking and um I would say I would recommend it if you have many teachers um this is something that works better and for KL is mass coverings you can see the on the graph um it means that it's going to um be a lot more um covering in terms of um distribution And this is usually better when you have a single teacher because you're not going to have uh too many different modes. Um and here I would also recommend using the teachers top K uh logit um to do it. But there are many many different flavors of it and I don't think that we converge uh toward a single unified solution yet. All right, we talked about um MOPD. So the final stage that we have here is uh what I call agency reinforcement learning. So this diagram might not be like super accurate but I think it gives you like a high level of understanding of what we're trying to achieve here. And [snorts] what we're trying to achieve is we say okay models are not consumed as chatbots anymore and they are consumed through a harness most of the time. Even a chatbot can be thought of as a very very simple harness right. So why not instead of training the model directly we can train it through a harness. So instead of getting the response from the raw model, you get the response from the model through a harness. And that's very nice because it teaches the models all the built-in tools from these harnesses. It teach them how to use the system prompt correctly. And if you have seen um open close system prompt for example, um it's very very big. It's over 7,000 tokens even if it in its most uh minimalistic form. So it means that it is quite challenging um to for a small model to really understand everything and training it directly on it makes sense because it it gives the model um some understanding of what's in there already built it in the weight so it doesn't have to understand it uh during inference time. Here you can see that we use different harnesses and the idea is that okay I'm going to have a bunch of task um and I'm going to store them. So in this task I want to have like realistic productivity things like creating I don't know like spreadsheets or um a PowerPoint presentation. I want to do some kind of web search. I can design any task and then we're going to give it to the reference uh model and the policy model here and they're going to interact directly with the harness in a containerize environment where the model can use a bunch of different tools like web search and come back with the answer and then we're going to use here it's like a GPO setting uh to calculate the loss and propagate it back to the policy model to train it. So this is the high level intuition behind it and behind what we want to do. Um some more practical considerations and and lessons from this work is that the reinforcement learning infrastructure matters more than you think and doing this kind of agentic reinforcement learning is really a stress test of your reinforcement infrastructure. Uh so be prepared. Another important lesson is that you really need to properly initialize the model on uh diverse SFT traces that basically cover um a lot of task that you want the model to learn. If the model especially if it's a small model is not properly initialized during SFT on this agentic traces it is probably not going to be very good at learning it during agentic RL. So the SFT stage and this is something that is as I said me uh before really overlooked is it's essential like everything that you want to train on you should have called start SFT data to properly initialize the model and make sure that you're going to really learn during the reinforcement learning stage. if the task is brand new, if the model has never seen something like this, it's going to be very difficult for it to really get better at it. Um, so that's a uh lesson uh that I want to highlight. And finally, um, training algorithms don't matter too much from our experience. Uh, you can see that the previous slide I I showed Gopio, um, which is not supposed to be the best performing one in this setting, but it works. Um, DAPo also works. So, uh, it doesn't seem to matter too much um, in this setting. Um, but yeah, that's the big picture around agency reinforcement learning and the entire training pipeline. how you get your student model, SFT it, train um many teachers, merge everything back with MOPD and then applied a final polish stage with agency reinforcement learning to make sure that the models the model that you train is really good at using these harnesses. And speaking of harnesses, I want to talk a bit about it because I think that it's not enough to just train the model. Now, you really need to have a very close feedback loop with the harness. So, we saw it with the reinforcement learning stage during training, but it's also true with the harness itself. Basically, you should adapt your agentic harness to your model and the other way around as much as possible. Um so I call it model harness um code design and it's a bunch of very concrete and practical tips and tricks. For example, okay, you want you have a predictable failure mode. Um something that I realized that the model would use bash commands even on Windows. Um but it should use PowerShell, right? So I can just tell the model I can just like pass it and detect this failure mode and add an hint. uh to make sure that the model uses PowerShell instead of bash. This is a very simple way of making the end user experience a lot more consistent and reliable. Um so why not really like the end user will only care if the model managed to achieve the task or not. So that's a nice little trick. Same with two call passing. If you have problems with um uh two call being wrongly passed, you can create fallbacks. you can make it more robust and that way uh you can basically improve the end user experience once again uh because this thing doesn't really matter like the agency caress should be able to help the model as much as possible. on the left here in the demo, you can see that we have an agent that spawn other agents to be able to um try to book a wedding and organize everything. And you can see that all the agents are trying to do some kind of searches. It doesn't have the API key, but it's it's trying its best. Um so a bunch of things happen in the background. And this is something that is super interesting because as I said before, like it's an edge model. So tokens are free here. like infinence is free. Um it means that you can really token max you can spend like over 1 million uh tokens per hour and that opens a lot of opportunities. So in the next slide I want to show like a different um a different demo and in this demo you can see a 2.6b 6B model powering a little application um that has an agent. And in this application, we decided to say like, okay, let's try to have like background agents. We don't want to do something super complex because it's a small model, but we want to be able to just like do triage with your emails, um, send you a notification when it's needed, uh, digest a newsletter, uh, make a search for you, organize something for you, and be very proactive, be very template driven. It's easy to spin up an agent. Here you can see that there's a little daily brief and we have already like a lot of agents uh trying to take care of things for you during the day and they are creating a lot of tokens. So here you can see one that is uh checking if the uh the beach is okay uh to have a date tonight. And here you can see all the agents and what they've done throughout the day. can see that they generated a lot of tokens, 27 uh million tokens. And yeah, that's not really something that you can do with a uh cat model uh today because it's just too expensive. And I think that this is just the beginning, right? I think that um and this is one of the main takeaways that I have. Edge agents are not just a compressed version of the cloud API models that we've used. It's essentially a new paradigm and the idea behind it is that infinance is free so you can generate as many tokens as you want and it raises a lot of questions that we're currently working on at Liquid where we're like okay like we have the potential of running these agents all the time. How can we use it? How can we exploit it to the fullest? Um, so as you can see, we have this background proactive agents and we create a lot of them and we give them a lot of different task and it's okay if they fail because we can have redundancy, we can have uh some reviews and that makes the system a lot more robust. Um, so it's really interesting to see how they're going to be used in the future in my mind. um how we're going to change the way we're doing track with models by having access to these um unlimited free tokens and this is part of like why I think small models are so interesting. They have a lot of unique challenges like the doom looping that I showed and many many others and I think the way going forward is really to think about agentic harness as an extension of the model um and really co-design them together to make sure that the harness are really useful and help the model support it when needed uh to be able to fully fully exploit it. So that's it for me. Um thank you everyone. uh for listening and I think that we have a bit of uh Q&amp;A now. &gt;&gt; Uh I'll paste some questions here in the chat so you can see. All right. Yeah, feel free to uh post your question in the chat if if it's not disabled. All right. Um I see first question I wanted to know how do you work on hallucinations? Um this is an interesting question. So um this is not something that I really talk about here um in the in the presentation. Um, but we do have a teacher that is specialized in abstension for the 2.6P model that we're going to release. And here what we want to do is we want to teach the model to answer when it knows the answer and to not answer when it doesn't know the answer, which is very tricky because as you know u models tend to be very happy to answer any questions and they have a poor understanding of their internal knowledge as well. Um so with AOPD we actually created a teacher to get better at that and um this is quite successful. Um a good benchmark to track this for example is aa omnisense. All right. Um other question what parameter range is considered edge deployable? It really depends on what you mean by the edge. uh it has a very loose definition so it's difficult to see. Um it can be phone, it can be a lot of different things. Um I would say that it's probably under 10 billion parameters. Um, so here you have uh tiny tiny models with 250M and that's something that you can deploy like really on a tiny tiny chip, but it can go up to 8B and with 4bit quantization uh you get something like around 4 GB or 5 GB of memory usage and that's kind of the maximum I would see. Um, but of course edge can mean many many things to different people. So it's it's kind of loose ended. Other question um what why is short convolution um replacing sliding window attention in 112.5 um so yeah it's a good question um you can see here um the main reason is about the cost of each uh layer and sang window attention is a very good layer but it's still quite costly um it has nice properties um but for decided to go as fast as possible with the LFM2 and 2.5 architecture. Um, so yeah, we just decided to go with an operator that is still quite expressive but is much faster. All right, next question. Could the ID behind FTPO uh so this is the um training technique uh behind anti-doom. Uh could this ID be extended beyond preventing doom loops? uh for example to selectively improve hallucination reduction or reasoning quality by optimizing only specific critical tokens instead of entire responses. Yes. Uh this is a very good question. Um actually this idea is comes from another paper um that we had at liquid. Um and this is kind of the idea. Yes. Um you can use it for many many different things. It's very flexible. um this anti-doom is just like one use case, but indeed FTPO uh could be used for this purpose and could be used every time that you're not very happy with a token and you have like a failure mode that you want to fix in a surgical way. Um so that's that's a nice approach. Um I would encourage you to to give it a try. All right. Uh next question. Outside of coding agents, where else does reinforcement learning with verifiable reward work? Um yeah, coding agent is a good uh application of it, but you have many many other ones. So here you have it with math. Um math is a very uh natural uh use case for VR because you probably know the answer in advance. Um so that's a nice way of doing it. Um the abstension mechanism I talked about to prevent hallucination is also another one because you know the answer and you can detect if it's uh correct or incorrect. So that's another way uh where you can use uh verifiable rewards. Tool use is another one. Um you might know which tool you are supposed to use in a particular context. Um so yeah just uh having it as a ground truth is enough uh to be able to um to get better at it. Okay, next question. Is the MOPD sequential distillation from what one teacher to the next or do we interle teachers? I should probably use this. Um, it can be anything. Uh, basically it's a it's a very loose name, a naming convention and different labs have different implementations of it. Um so it can be everything at the same time. It can be um in sequential way. The MOPD stage can also be divided into like two sequential stages. For example, you might want to um do the agentic related domains at the end to make sure the model is really good on it before doing agentic reinforcement learning. So yeah, I would say it's very free form and there are a lot of tech reports that detail their own implementation. So if you're interested in this topic, I would probably recommend um the paper from Xiaomi um or Neotron Deepseek V4 or Kim K3. All right. Uh next question. I was curious to get your perspective on the best way to get hands-on experience on boost training, especially on low compute budgets. It's an excellent question. This is how I got into push tuning. Actually, I didn't have the budget to do uh something like more comput intensive than supervised fine tuning. So, there's a lot of cool work that you can do with very tiny budgets. Um, push tuning is kind of scrappy and there's a lot of ideas that I think you can try and they're not too expensive. Of course, if you want to do something like agency reinforcement learning, it's going to cost a lot because it's a ton of infrastructure. It's a ton of environment. Uh you will need talkers, you will need like a lot of things that are difficult to orchestrate together. But you can for example focus on model merging which is something that I've done in the past. And model merging um for people who are not aware is this idea that you can take the weights of different models, same same checkpoints but trained differently and you can merge them together. So you can just average all the weights. Um this is called a model soup or linear averaging. And we see that it is a very good regularization factor. It really helps the model um when you do that most of the time and especially in some scenarios and it can raise performance. So here I do not mention it in our training recipe but model merging is something that we do use quite a lot and that's a great way if you have for example um many many different checkpoints at the end of your agency reinforcement learning stage here you might want to upcycle them and by merging them you can evaluate it and see like oh okay actually this makes a um mixture that is a lot more um balance across different domains and this is how we got the final model uh for um this specific recipe. So yeah, model merging is a very good one. I think that everything related to evaluation is also super interesting to explore. And finally, data is probably the most overlooked aspect but also the single most important and high leverage um aspect of post training. So data uh yeah we we need more All right, next question. Um, can you please suggest a research paper that you would recommend from liquidi about its edge LM architectures? Yes, we have um many papers around this, but the one that I would recommend the most is the LFM2 technical report. Um, so this comes from December last year. The LFM 2.5 architecture is still the same, so it's still very relevant. And in this paper we talk a lot more about uh the experiments that we did uh to create this specific architecture. So it's a very interesting read if you are interested in this topic. There's been an increased focus on looped models recently. Has that made its way over to the edge LLM scene yet or is it still mostly sequential? I think it's still mostly sequential. Uh but it's super interesting. I think looping models. So the idea is that you're going to repeat some layers during inference um is super interesting because it gives you a lot of flexibility. And here when we talk about um small models um you can see that we actually have a a very very wide range. Um here you can see a comparison where we have a 350 million parameter model but also a 1B model. So it's basically three times as big. Um so there's a huge range um in terms of log uh here. And um being able to loop some layers is super interesting because it means that the same model can cover more range. Um it's still difficult in terms of like latency maybe. um if you really want to deploy it on the phone for example it's kind of difficult to justify uh sometimes um the the trade-off that you can uh create here but uh in general I think we are going to see more and more uh looping models uh on the edge um because of this reason all right next question were the teacher models also customtrained or did you use publicly available teacher models yeah this is something I and maybe be super clear about um you can see here that all the teachers are initialized from the same SFT checkpoint. So why that um there are two main reasons. One is about the tokenizer. Um if you want to do MOPD cross tokenizer so for example you want to train a LF model and you teach a quen model it's not the same tokenizer at all. You're not going to have the same vocabulary at all. and reconciling the two vocabularies either by switching a tokenizer or by doing like some kind of token surgery is quite difficult and doesn't work very well in practice. Um so you can try but it's it's very difficult. And the second aspect of it of why we don't choose publicly available model is that the output distribution of your teacher should be very close to the output distribution of your student. And here it wouldn't be the case at all. Um so the teacher might overcorrect the student and because of that there's not too much signal that you infer from this. Um so this is why we really want to use our own teachers and yeah from experience it works a lot lot better. Um and um this can be even initialized from not just the SFT checkpoint but different stages um in your training pipeline. Uh so this is something that is very robust and um very high leverage. All right, next question. As you said, edge models needs a helpful or native harness to work more efficiently like in your smartphone example. How can I secure the system to not let models go in system configs etc and can be edgent friends be sandboxed? Um it's very simple. Um you control the tools that you give the model. So the model if you do not give it the tools it needs to change your systems um your configuration it's fine like it's not going to um escape it. Um, it can be sandboxed indeed, but I think the the main security layer here is not providing the tools that might be bad for the model to use. And then you can add classifiers on top to make sure that um the command that is run or the tool that is used is not harmful in any way. So there are different ways of uh doing it. Um, but just not giving it access to settings is probably the most most robust one. All right. Um, next question. 28 trillion tokens on 350 million model parameter model. Um, did you hit a hard loss plateau or is reasoning capacity still scaling linearly? Yes. So for the 350M model that I presented here, um, we actually train overtrain it quite a lot. So if you're familiar with um, uh, Chinchilla scaling laws, um, it is massively overtrained. Like I I don't remember the ratio, but it's it's it's something really stupid. um you shouldn't be able you shouldn't have to use that many tokens to train the model in an optimal way um but we did it nonetheless and indeed we did it because we saw that the model still improved um so I think that as long as you have um high quality tokens um you can probably keep training and even at this um 28 trillion token uh limit the model was still learning. Um, so no, it's not plateauing. Uh, you can still add more tokens. And to be fair, this is also what Chinchilla predicts. Like Chinchilla laws predict that it is it becomes less and less efficient um to add more tokens, but it still improves, right? Um, I think what we see is that higher data quality especially is um a big lift and allows you to really overtrain the model and still see uh massive gains. Um, so yeah, pre-training uh can go very far. All right. Um, next question. What would you say is efficient way to restrain LFM models on PI harness in terms of verifiers? Uh what would say what would you say preference for rubrics based LLM as a judge versus deterministic one like to cause they are doing? Okay. So um a PI harness um so I'm not sure what you mean by an efficient way of training LFM models on PI harness but um you can directly like have your agentic reinforcement learning loop and give the model a task and send the task to the model through the PI harness and check the answer. If the answer is correct you reward the model otherwise you you you don't reward the model. Um so that's the way that we do it with many many harnesses. And then instead of verifiers um you ask the question okay like should I um use rubrics based LM as a judge. So having a model reviewing the answer or should I use um uh something that is more uh like a hard rule like here it's just a comparison with between the final answer that is extracted and the ground truth. It really depends on the domain. Um you can try to do a bit of both. Um sometimes for example with instruction following you have py python test usually to make sure that the answer that the model generated corresponds to the constraints that you um inserted as um in the input. But that might not be enough because we see that models can easily reward hack this Python test. And uh that way you might be tempted to add a judgem to not only make sure that the constraints are respected in a very hard way but also that the answer that the model generated actually makes sense is actually useful. From experience when you do that you realize that the model starts reward hacking the judgem and for example your model might insert at the end of its response that it is an excellent answer that satisfies all the constraints and the judgem might see that and be biased towards saying like okay this is a good answer then um so yeah there's no very good answer here I think It really depends on the domain. Something that is more free form like creative writing obviously benefits a lot more from having a particular JLM or a rule model. Um and something more like math doesn't benefit from it uh very much. &gt;&gt; All right, next question. How do you know uh that the teacher is close to the student in terms of distributions? Yeah. So this is what I talked about here. Um with the on policy distillation um yeah it really it's not easy. Um you can you can run some test. You can like just generate answers and like generate the output distributions with uh two models and see the K divergence. See how much they overlap. Uh that's a very nice way of doing it. If [snorts] you've trained the models from the same checkpoint and it's not like pre-training scale, it it's probably going to be very close uh to each other. So here you don't even need to to check it. [snorts] Um we see in some tech reports um like I believe it's Neotron Cascade 2 that they had a problem with this and um they have a solution actually because the student model and the teacher model um diverged too much during the post training pipeline. So what they decided to do is that they decided to generate answers with the teacher models and SFT the student model on them. So they would get closer in terms of distribution. So that's a nice way if you really have this problem to to fix it. Um but it's more like a patch and not ideal I would say. All right, next question. Is it feasible to predict the expected output length? So the number of tokens of a language models response before the generation begins. So based solely on the input prompt in question. This is very difficult. Um I haven't tried to do it to be fair. I think some domains are naturally a lot more verbous than others. For example, math is super verbous and it it's going to generate a lot of tokens. Um, so like on domain basis, you can have like a relatively good average score. Um, obviously this is confounded by the presence of doom loops potentially. If you have doom loops, well, it's going to explode um the [snorts] uh token generation budget. And the other thing where um the other common pattern that we see is that um unsuccessful answers produce more tokens on average than successful ones. Um so it's also going to depend on on that. I think this is something that you could try to predict. I think you could even train the model to predict its like internal um response uh budget. actually co-rote a paper um where this is something that we we tried and I believe it it can work. It just depends like on how accurate you want it to be. Okay, next question. How can we combine multiple teachers for the same task? For example, one teacher focused on functional correctness of code and another focused on code security. Yes. um that is a completely correct way of doing it. Um especially if you want to create a coding model. I think it makes a lot of sense to really um have different teachers uh focus on on different things. The same teacher could also provide both um signals. Um it's something that you see that yeah it's not necessarily better to have two teachers. Um but in general I would say it's probably a good idea to ablate it and do a quick experiment with it. All right. Uh coherabs people interrupt me if we don't have any more time but otherwise I will take another question. Is model merging applied to MOS? Yes. Um exactly same principle. Uh it works very well with MOS. Um no nothing to know in particular about this. It just like applies very well to this architecture as well. Um next Next question. Okay, maybe final question. Um, what architectural and design decisions make one AI agent more lightweight than other while maintaining comparable performance and accuracy? Um, I think it's the size of the model. Um, of course it's going to be more lightweight if you talk about memory usage here. And the second one is about um the KV cache. So if you use full attention, if you use like something like GQA, the the KV cache is going to grow quite a lot, right? Um based on the input length. So because you have an agentic task, your input length is probably very big. Uh so it's probably going to explode. And if you have something that compresses uh the input like short convolution, um it's going to be like a fixed length, so it's going to not grow as as much. Um, so yep, that's a way of doing it. [music]
