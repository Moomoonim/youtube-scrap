# Create Your Own AI Agent with NVIDIA NeMo Agent Open Source Toolkit

- 영상 링크: https://www.youtube.com/watch?v=NsogD7UhZ4Q
- 채널: NVIDIA Developer
- 업로드일: 2025-05-08
- 자막 언어: en
- 단어 수: 약 1915개

---

## 스크립트

Hi, my name is Matt, and today we'll be discussing&nbsp; the Agent Intelligence Toolkit, which is an&nbsp;&nbsp; open-source Python library that's designed to&nbsp; seamlessly integrate your enterprise agents,&nbsp;&nbsp; regardless of framework. You'll be able to bring&nbsp; together various data sources and tools. We treat&nbsp;&nbsp; agents, tools, and workflows as simple function&nbsp; calls, which allows you to get access to a lot of&nbsp;&nbsp; composability. Once you build your agent and its&nbsp; components, you can reuse these across agents or&nbsp;&nbsp; distribute these to other developers as everything&nbsp; we build is considered a plug-in, which is really&nbsp;&nbsp; nice. So, what we're looking at here on the right&nbsp; is the UI that comes with the toolkit, and then&nbsp;&nbsp; on the left, we have an IDE that we've cloned&nbsp; the GitHub project in, and we've used the AIQ&nbsp;&nbsp; workflow create command in order to generate some&nbsp; scaffolding for the purposes of today. Today's&nbsp;&nbsp; goal is to use the toolkit in order to generate a&nbsp; multi-RAG agent with an emphasis on the agent. We&nbsp;&nbsp; want to be able to define a custom agent that can&nbsp; have access to each of these RAGs. Each of these&nbsp;&nbsp; RAGs is tools and reason about the problem and&nbsp; draw the information it needs as it goes through.&nbsp;&nbsp; For the purposes of the RAGs, we've created&nbsp; a synthetic or a made-up company called GPU&nbsp;&nbsp; Experts Incorporated. And within each of the&nbsp; RAGs, we put various corporate policies from&nbsp;&nbsp; PTO policies to dress codes, and we actually&nbsp; have it here, as well as payroll policies. So,&nbsp;&nbsp; let's go ahead and take a look at the&nbsp; command we used to create the scaffolding. You'll find this command really helpful getting&nbsp; started since every toolkit workflow built with&nbsp;&nbsp; the toolkit can be wrapped up as a package, and&nbsp; each of the components ends up being plugins.&nbsp;&nbsp; These plugins can be used with your other&nbsp; agents locally or distributed to others,&nbsp;&nbsp; and all they have to do is pip install them.&nbsp; So, this command really gives you the entire&nbsp;&nbsp; Python package, the PyProject.toml that&nbsp; specifies all the pieces that'll be plugins,&nbsp;&nbsp; and you can just start from that scaffolding. So,&nbsp; that's what we did here. A few pieces of this to&nbsp;&nbsp; pay attention to is every workflow is defined by a&nbsp; configuration file. So, here we have a config.yaml&nbsp;&nbsp; that defines all the pieces of our agent. I'll&nbsp; walk through that just really quickly. The first&nbsp;&nbsp; is a general section that we're using to specify&nbsp; a front end in particular. So, our result of our&nbsp;&nbsp; work will create a FastAPI microservice that we&nbsp; can make POST requests to to invoke the agent.&nbsp;&nbsp; We have one LLM that we'll use, one of the llama&nbsp; instruct models, our 70 billion parameter model,&nbsp;&nbsp; as well as an embedding model. The embedder&nbsp; will be used by the RAGs, so will the LLM. The&nbsp;&nbsp; LLM will be used by our agent. We have our three&nbsp; RAGs defined here in our function section. So,&nbsp;&nbsp; each of these functions will be basically making&nbsp; a call to the RAG, which is being hosted locally&nbsp;&nbsp; but could be run remotely, which is what you would&nbsp; probably see in a production setting. Here we have&nbsp;&nbsp; one for our dress code, one for our payroll,&nbsp; and one for our PTO RAG. You'll see here that&nbsp;&nbsp; we're using the same underlying implementation&nbsp; but creating different runtime instances with&nbsp;&nbsp; different parameters. Right, so this is the&nbsp; beauty of our composability in action here.&nbsp;&nbsp; We're using our same kind of llama index-based&nbsp; RAG system in order to do that. And then the last&nbsp;&nbsp; section is a workflow. We can think about this&nbsp; as equivalent to a function but the main entry&nbsp;&nbsp; point for our microservice. Everything comes with&nbsp; an underscore type that looks things up by name,&nbsp;&nbsp; which is why you can see these are all the&nbsp; same here, right, but they have different&nbsp;&nbsp; runtime instance names, and each of these runtime&nbsp; instance names will have their own parameters. So,&nbsp;&nbsp; all of this is backed by Pydantic models for type&nbsp; validation. Let's go ahead and see that in action&nbsp;&nbsp; with the boilerplate code that was generated for&nbsp; us for this application. So, I'm going to go ahead&nbsp;&nbsp; and make the terminal a little bit smaller here&nbsp; and just walk through this really quickly. So,&nbsp;&nbsp; we have some imports on the top. We have&nbsp; our Pydantic model that has a name here. So,&nbsp;&nbsp; you'll see this maps to what we were looking at in&nbsp; the config file, right? We've got our underscore&nbsp;&nbsp; type name here and then some parameters that are&nbsp; attributes of that. Go ahead and take a look here,&nbsp;&nbsp; and you'll see that that matches up. Right, so&nbsp; you'll see that pattern for every function that&nbsp;&nbsp; you define or every component that you define,&nbsp; and then we have this register function decorator.&nbsp;&nbsp; This passes in that object, right, that&nbsp; my_agent_demo_function_config object,&nbsp;&nbsp; and lets that get registered with the local system&nbsp; so we can look it up by name in our YAML file,&nbsp;&nbsp; and then within the body of this, we have some&nbsp; arguments that get passed in. We have the ability&nbsp;&nbsp; to access the configured runtime configuration&nbsp; via this config input, and then this builder&nbsp;&nbsp; lets us construct at runtime, you know, LangChain&nbsp; wrapped objects, to LlamaIndex wrapped objects,&nbsp;&nbsp; etc., and construct clients and functions&nbsp; at runtime so that we can use these,&nbsp;&nbsp; and then in the middle here is the logic that&nbsp; would be executed when this particular function&nbsp;&nbsp; is invoked. So, with that, let's go ahead and&nbsp; update this because we don't actually have&nbsp;&nbsp; any agent. It's effectively a pass-through.&nbsp; We're concatenating the input message. This&nbsp;&nbsp; isn't any good. So, now let's go&nbsp; ahead and update this so that we&nbsp;&nbsp; can do something useful. Have to&nbsp; bring in a few different imports. Okay, so got a few imports here. The first two&nbsp; are data types that we'll use in the configuration&nbsp;&nbsp; object. The last one is an enum we'll use in order&nbsp; to wrap things in the framework that we want.&nbsp;&nbsp; So, this parameter input is no longer something&nbsp; that we need, and we're going to update this to&nbsp;&nbsp; support our agent. So, the first one is a list of&nbsp; tools that we're going to provide to our agent.&nbsp;&nbsp; The next one is going to be our LLM name. Right,&nbsp; so the LLM that we've named in our configuration&nbsp;&nbsp; was NIM_LLM. So, that's what would be passed in&nbsp; here at runtime if we configure it that way. Okay,&nbsp;&nbsp; and max history. Right, so this will be the&nbsp; historical messages to pass to the agent. And&nbsp;&nbsp; the last one will be a description. So, we could,&nbsp; if we wanted to, we could define this function&nbsp;&nbsp; just like we reused our RAG implementation with&nbsp; different parameters for each of the implemented&nbsp;&nbsp; RAGs. We could have a higher-level agent call&nbsp; this agent as a tool. So, this description allows&nbsp;&nbsp; us to define what this agent actually does in&nbsp; configuration, which is really, really nice. So,&nbsp;&nbsp; that's what this last parameter does. So, then&nbsp; now that we've done that, we can get move on to&nbsp;&nbsp; implementing our custom agent. So, we'll need&nbsp; a few imports in here before we get too far,&nbsp;&nbsp; and a few additional toolkit imports. Okay, going to take a chunk of this. All&nbsp; right, so what we've done here is we've&nbsp;&nbsp; done some imports. In this top row, we're going&nbsp; to instantiate a list of tools to provide to&nbsp;&nbsp; our agent that are all wrapped as LangChain.&nbsp; Here, we're going to instantiate an LLM client&nbsp;&nbsp; that's going to communicate with a remote LLM&nbsp; that'll be wrapped in LangChain. We're going&nbsp;&nbsp; to give that both of these to our agent. In&nbsp; order to use our agent, we need a prompt. So,&nbsp;&nbsp; we use an open-source prompt here. We instantiate&nbsp; a React agent providing that LLM client,&nbsp;&nbsp; the tools, the prompt, the stop sequence, and&nbsp; we're going to set up an agent executor next,&nbsp;&nbsp; providing it the agent and a few other&nbsp; things. Let's go ahead and check these&nbsp;&nbsp; to make sure that that got set up&nbsp; properly. I think that looks decent. Okay, so now that we have those things set&nbsp; up, we're going to change our input types. So,&nbsp;&nbsp; we're going to use these AIQ chat request and&nbsp; response. These kind of map to the OpenAI schema.&nbsp;&nbsp; We're not going to use this. We're going to&nbsp; implement something slightly different here. We're&nbsp;&nbsp; going to get access to the most recent message&nbsp; first. Then we're going to get the chat history. All right, so next thing we're going to do is, now&nbsp; we have our chat history and our latest message.&nbsp;&nbsp; We're going to invoke this agent executor. And&nbsp; the last thing we'll do is return the result. All&nbsp;&nbsp; right, just reviewing this. I think that looks&nbsp; good. And okay, so we can use this here. We'll&nbsp;&nbsp; just leave; we don't need this anymore. So, we'll&nbsp; just get rid of that. All right, so now what we&nbsp;&nbsp; can do is tear down this microservice, and we'll&nbsp; need to update our configuration file and we'll&nbsp;&nbsp; need to update the configuration file in order to&nbsp; match this new function that we've defined as our&nbsp;&nbsp; entry point. One great way to do that, you know,&nbsp; over time, you'll have many, many, many registered&nbsp;&nbsp; functions, is to use our AIQ info components&nbsp; command-line utility. We'll filter for functions&nbsp;&nbsp; that have the name my_agent_demo. Right, this&nbsp; maps to what we've just created. Oops, add an s. So, this will load and provide us with what&nbsp; needs to be supplied in our YAML file. Right,&nbsp;&nbsp; so this is the function we're after. We&nbsp; want this update to be our new entry point. Okay, so we've got these function names&nbsp; that map to above. We've got our LLM name,&nbsp;&nbsp; which is our LLM NIM from above. That&nbsp; looks good. We want our max history to&nbsp;&nbsp; be 10 for the chat history. And we've got&nbsp; a description. So, that all matches. So,&nbsp;&nbsp; we can quit that command-line utility and try to&nbsp; bring up this new service that we've just created. All right, so now let's regenerate this response&nbsp; with our updated agent, and it should now be able&nbsp;&nbsp; to call our RAG tools. So, here we've got some&nbsp; great information back from our RAG. Looks like&nbsp;&nbsp; Monday through Thursday is business casual, and&nbsp; on Fridays, you can dress down a little bit. So,&nbsp;&nbsp; let's say it's a warm day, and it's Thursday,&nbsp; but it's hot out. Can I wear a tank top and&nbsp;&nbsp; bathing suit to work? So, we're hoping&nbsp; that this will invoke some of our tools,&nbsp;&nbsp; and afraid not. Right, so this maps to the&nbsp; above. Well, that's good to know. You know,&nbsp;&nbsp; let's say I like the heat and maybe I like&nbsp; the beach. So, let's say I need to plan a&nbsp;&nbsp; new vacation. That's fine. Maybe I should plan&nbsp; a vacation. What is the process for scheduling? Okay, so here we've demonstrated our new agent&nbsp; is functional. We did not use a built-in agent&nbsp;&nbsp; that's included with the toolkit, although we&nbsp; could have. What we've shown here is bringing&nbsp;&nbsp; in a custom agent and using that with a&nbsp; number of tools that are provided. So,&nbsp;&nbsp; I hope this was helpful. It shows how&nbsp; to extend the toolkit for integrating&nbsp;&nbsp; custom agents. You're welcome to include those&nbsp; agents as functions in the function section and&nbsp;&nbsp; call those by other agents, demonstrating&nbsp; the flexibility of the library. Thanks.
