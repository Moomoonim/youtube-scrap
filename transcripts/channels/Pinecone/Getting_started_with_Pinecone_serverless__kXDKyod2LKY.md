# Getting started with Pinecone serverless

- 영상 링크: https://www.youtube.com/watch?v=kXDKyod2LKY
- 채널: Pinecone
- 업로드일: 2024-07-25
- 자막 언어: en
- 단어 수: 약 536개

---

## 스크립트

hi my name is Rick from Pine Cone and today I'll be going through a brief demo of how to get started on our platform I'll be walking through how to create an index load data and execute a query so let's get started what we have here is a pine cone one in one notebook and the first thing we're going to do in this notebook is execute the PIP install commands so we have the libraries needed so here we're installing all the libraries that we're going to need the pine Pon client and the pine cone notebooks uh Library also now we're going to follow that up with executing the import statements all right here we go now we're going to import the required python libraries and for this we need to a pine cone key so here I'm going to use my pine cone key I authorized that that's loaded and now I'm going to create a connection to Pine conon serverless and also Define the uh index name that we're going to be using yeah we create an index so what this code does is it checks if the index is existing if it's not existing we'll create it and now I'm going to get the data so this data set is a sample data set of doctor's notes so it includes notes from doctors uh specific to patients and then we're going to do a search query later against those notes choosing the file that I'm going to import all right now we took the head of that file so we're seeing the first few records notice that the data set is already embedded so this is some of the embeddings uh for that data set and then here we have some metadata now let's go ahead and upsert that data here we're using this upsert function to upsert the specific data frame this helps us run more efficiently and much faster because the data frame is in the format that we want for the upsurt objects now I'm going to embed the query so I'm using a embedding model from hugging face that's the embedding model I'm going to be utilizing and now I'm going to embed this query so in order to perform a search against embedded data I need to also embed the Quarry so embedded the query and executed the search and now we're getting the top three results and here the top three results based on that query the query was about knee pain so what if my patient has knee pain based on that data set we're going to get results for that query as you can see these scores are pretty High because the metadata includes NE in in in the search criteria basically so the metadata coming back includes knee in the results and that's why these uh results are so high and then we also have a less uh a lore matched result for just general joint pain so pretty quick demo again what we did was created an index loaded some embedded data in this case Doctor notes and then executed a query based on that embedded data thank you
