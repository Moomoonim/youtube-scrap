# Pinecone AWS Reference Architecture with Pulumi deployment Part 2

- 영상 링크: https://www.youtube.com/watch?v=X17MJe9OU7k
- 채널: Pinecone
- 업로드일: 2023-12-01
- 자막 언어: en
- 단어 수: 약 1644개

---

## 스크립트

for example when we come in here and we say uh you know export pine cone index is going to be Zack test for example if this uh index doesn't exist when the reference architecture initially stands up it will create it and if it does exist it will not uh conflict with it or uh throw any errors about the fact that it already exists so the uh you can either create it here manually as we're going to do just for the purposes of showing um but if you don't do that and you attempt to run the reference architecture after setting up the correct environment variables and as long as you have valid API Keys then the reference architecture is front end microservice will actually spin up this index if it doesn't already find it so um for the purposes of this let just call it Zack test uh I happen to know that the front end um and the other microservices as well for this reference architecture using 384 dimensions and in order to just keep things simple I'm going to stick with just S1 and create one index so now that we have this first index provisioned I'm going to come into here which is my local clone of the reference architecture you might have a different name for your repo but this is the same repository that is here and that is fully open source I just uh cloned it here earlier and so the first thing I'm going to do is export my first required environment variable which is going to be pine cone index and we'll call it zct test just as we did here the next piece of data I need is this environment value so this is the pine cone environment and then finally I need my API key so we come over in the left rail to API Keys grab your API key and then that gets exported as pine cone API key um the other thing to note is that if you were to forget to say export your pine cone index uh environment variable and then attempt to run palumi up here uh we do have code that is in this utils function and it will check for required environment variables and it would throw an error and tell you which one's missing but uh at this point these are the only three environment variables that you actually need to set to get started and so at this point we uh close out this credentials file and we can run uh the first thing that we need to do before we deploy and uh try to run palumi up is we need to initialize a new stack so in palumi uh a stack is a logical grouping of all the resources that we're going to deploy for this particular reference architecture along with the configuration and the uh stack infrastructure is is kind of nice because it actually is is maintained partly through the palumi cloud back end and allows you to sort of just move between machines a little bit easier as well so uh we're going to do palumi stack in it and we can just say palumi stack in it and then the name the arbitrary name that you want so we'll just say deploy demo okay now that we've created a new stack which you can we can verify by running palumi stack LS we can see that we are on the deployed demo stack right here there are no resources we haven't deployed it yet um and so now in this new stack because configuration is separate I need to run pumi Set uh palumi config set AWS profile palumi and now we can run palumi up and so now palumi is looking into our AWS account and it's asking uh do any of these resources that are defined in the index.ts file already exist it's asking via the API doing lists and reads and indeed none of them exist and so what we get is that we're going to attempt to create the entire reference architecture start to finish and that comprises you know 93 resources so you could either um say no I don't want to do this now you can look at the details or you can say yes and to kick this off we'll just say yes and we'll start typically speaking the reference architecture is is uh pretty likely to come up cleanly in in one shot everything has defined his code including the docker files and the docker build processes for all three of the microservices so now you'll see that the uh that palumi is actually performing the docker builds pushing those images up to ECR and then even handling for us under the hood the authentication um so that we don't need to actually worry about that you don't need to do any manual Docker building everything is handled via code which is really nice because it makes it easier to tear down as well which we'll see later so also typically speaking at the time of this recording this current iteration of the reference architecture takes about 17 and a 12 minutes to come up from scratch and that's partly just due to there's a couple resources particularly the Gat gateways of which there's three in the custom VPC those each take several minutes to provision as does the RDS instance that is Central to the central database that uh kind of coordinates um the functionality of the whole system that RDS instances also take a while to come up so all told uh we'll probably be you know waiting here for about 17 and a 12 minutes so if you've gotten to this point and things are creating smoothly like so then you can also feel free to walk away get a coffee um stretch go outside touch some grass and come back in in 15 or 20 minutes and so as we wait here for some of these things to uh provision so everything's coming up the custom VPC the various Autos scaling policies the sqsq itself load balancer now the the database and its own Security Group um might be nice to just quickly talk through this architecture and what we're actually deploying so what this reference architecture comprises is a front-end microservice so this front end microservice exposes a table UI uh that's built in react and nextjs to end users and that allows users to perform semantic search by querying uh they can also live edit any of the records in the table editing the records in the table is what causes the front end to issue SQL queries to postgress and when postgress has a change it adits that using PG notify the Pelican microservice is listening to postgress directly when it gets changed records off postgress it places them on the the sqs jobs queue the separate microservice emu for embeddings and upserting is listening uh also uh polling from that same sqs job queue and as soon as it gets jobs it converts the description of the product to an embedding and then upserts that embedding to the pine cone index along with metadata that ties That Vector back to the postgress record so for example the metadata contains ID equals 11 1093 if the postgress record ID was 11093 and so that is how we can actually uh maintain an end to end from front end all the way through the database two databases database and then Vector database um synchronization so as soon as a user edits a product description that goes all the way through the system and gets embedded and then upsert it into pine cone but with metadata that attaches it back to the original postgress record and that's how when users are searching in the front end if they issue a query such as products with AI or you know products that help me clean my floor whatever the case may be then they're getting back the exact same view of the data that's consistent throughout the system um they're also able to uh we we in code we go from the pine cone results that were queried based off the natural language or the ambiguous natural language query that the user sent in so the query vectors and then what comes back in the results the matches for those query vectors includes the metadata that has all of the actual postgress IDs and then we're able to just directly select on those postgress IDs in order to get back the data that we um want to display in the UI okay so still waiting here the looks like just about everything on the supporting resource side so Q DB subnet security groups is is up and now we're actually starting to provision the Emu service the RDS instances about halfway through provisioning if we come in here and look at our ECS panel in the AWS dashboard we should now see clusters and yep indeed we do see this cluster so there's the Pelican cluster the front end cluster the Emu cluster and there's one service now provisioned for emu and we are pretty soon going to attach um we've got the policies for the service the actual IM resources that allow emu to do the jobs that it needs to do to read from the sqsq for example and then um pretty soon we should see uh the ECS service coming up and starting to spin up separate emu workers so then those will all be here as well yep and so now we've got two pending so the Emu service itself is performing a deployment within ECS to bring the
