# Training BioMolecular Models on BioNeMo

- 영상 링크: https://www.youtube.com/watch?v=en2Vl6u-umk
- 채널: NVIDIA Developer
- 업로드일: 2023-12-05
- 자막 언어: en
- 단어 수: 약 775개

---

## 스크립트

Nvidia biion Nemo Powers endtoend AI drug Discovery pipelines with instant seamless access to generative Ai and large language models an AI powered cloud-based service it provides pre-trained models with billions of parameters at supercomputing scale these models include generative chemistry with mega molart and moflow protein language models to generate learned embeddings for protein structure and properties with esm1 and esm2 pose prediction with diff do protein generation with prot gpt2 and deep learning models for 3D protein structure prediction with open fold Alpha fold and esm fold biion Nemo is based on Nvidia Nemo Megatron and provides what researchers and developers need to quickly build and deploy generative AI drug Discovery pipelines including a robust set of libraries and tools that streamline the process of model development these include functions for automatic data loading and pre-processing examples with model configurations and scripts to quickly build pre-training and fine-tuning pipelines documentation that details the step-by-step process of running training and inference tasks and an extensive API for simple integration into custom applications let's take a look at the configuration for training the esm1 model and see how we can quickly configure and run a multi-node training job on Nvidia dgx cloud using base command platform NGC is a public catalog that offers access to the Nvidia AI Enterprise software suite containers Helm charts for deployment pre-trained models and resources like the NGC command line interface NGC also gives organizations access to a private registry where they can store their own private collections of containers and software Nvidia base command platform includes a central user interface with a set of managed compute resources this can be used to manage data sets workspaces and jobs as well as users and teams within an organization this creates a convenient hub for monitoring job execution viewing metrics and logs and monitoring resource utilization the reference script that we'll use can be run locally to launch a job using the NGC CLI the NGC and BCP run commands generated by the script can also be used in the NGC web UI to configure and launch a job interactively here we can see the layout of the Bemo library with the core functionality in the bio directory a set of pre-trained models in the models directory [Music] and a set of sample scripts and configurations in the examples directory now let's look at a reference script to launch a two- node training job on base command platform here we'll configure the bio Nemo esm1 training command to run pre-trained small with the UNF data set as input we'll also Define the experiment workspace number of nodes and gpus per node and other parameters like bat size and training steps the training command defines the esm1 workspace and configures the pre-train command to include the input data set and data files the experiment directories as well as the parameters of the Run including batch size the number of training steps validation intervals number of workers and other parameters the BCP run command is used to launch the multinode training on base command platform specifying the number of nodes and the number of gpus per node we then tie it all together with an NGC batch command this is where we Define our organization and team The Accelerated compute environment to be used the instance type which in this case is NVIDIA dgx A1 100s the number of replicas or nodes the workspace and other job parameters like Max runtime we then execute this script to submit the job to NGC and kickoff training on base command platform here we'll see the output from the NGC launch and then we can verify in the base command UI that our job has been submitted for execution after launching the job using the NGC CLI we can monitor its status in the BCP jobs pane in this example we can see the command used to launch the job we can view Telemetry showing resource utilization during the Run we can look at the replicas or nodes that were used to execute the training and we can also view logs produced during the run this Central overview gives developers a simple way to evaluate performance and efficiency of jobs over the course of many experiments in this walkthrough we've demonstrated the biion Nemo tools that allow researchers and developers to pre-train and fine-tune their own custom models using the biion Nemo training framework we then showed how easy it is to execute these models in base command platform on dgx cloud to learn more about biion Nemo visit nvidia.com to join the Early Access program
