# Get Started with cuTile Python

- 영상 링크: https://www.youtube.com/watch?v=cNDbqFaoQ9k
- 채널: NVIDIA Developer
- 업로드일: 2025-12-04
- 자막 언어: en
- 단어 수: 약 778개

---

## 스크립트

Today I'm going to show you how to get&nbsp; started with cuTile Python. To get started,&nbsp;&nbsp; we'll need a couple of things. First, we're&nbsp; going to need a Blackwell GPU. That means&nbsp;&nbsp; a GPU with compute capability 10 or compute&nbsp; capability 12. We're going to need the NVIDIA&nbsp;&nbsp; driver version R580 or later installed. We're&nbsp; going to need the CUDA Toolkit version 13.1 or&nbsp;&nbsp; later installed. We'll also need the bin path&nbsp; of the CUDA Toolkit in our system search path.&nbsp;&nbsp; More on that in a moment. And finally, you're&nbsp; going to need Python version 3.10 or later&nbsp;&nbsp; installed. So now we're looking at a terminal&nbsp; on my development workstation. I'm going to&nbsp;&nbsp; type NVIDIA SMI and we'll see that we are on&nbsp; driver version 580 and the GPU we'll be using&nbsp;&nbsp; today is a Blackwell RTX 5060. Now I mentioned&nbsp; that you need the CUDA Toolkit installed and in&nbsp;&nbsp; your system path. The reason you need that is&nbsp; that cuTile Python is going to make use of a&nbsp;&nbsp; program from that called Tile IRAS . This is the&nbsp; optimizing assembler for the CUDA Tile platform.&nbsp;&nbsp; So if we just type tile IRAS and it finds the&nbsp; application. We see the help here. So it found&nbsp;&nbsp; the application. We're good to go. So now let's&nbsp; install cuTile Python. All we need to do is pip&nbsp;&nbsp; install CUDA - Tile. That finds it. We've&nbsp; got cuTile Python. That's all there is. Now,&nbsp;&nbsp; let's look at some code. So, all the code I'm&nbsp; going to show is on the GitHub repository for&nbsp;&nbsp; cuTile Python. I'll put the address on the screen.&nbsp; We'll also put a link in the description of the&nbsp;&nbsp; video down below. Vector Edition is kind&nbsp; of the hello world of GPU programming. So,&nbsp;&nbsp; we're going to start with a vector ad using cuTile&nbsp; Python. So, here's our code. We start with some&nbsp;&nbsp; imports. The most important import I want to talk&nbsp; about is this one. Import CUDA.tile. That's cuTile Python as ct. That just means we're going to call&nbsp; it ct in this. The next line says at ct.kernel&nbsp;&nbsp; that indicates that the next function we define&nbsp; is going to be a cuTile kernel. So our vector&nbsp;&nbsp; addition is going to be code that's going to run&nbsp; on the GPU. It's going to take three arrays A,&nbsp;&nbsp; B, and C. These arrays need to already be on the&nbsp; GPU. How you get them there is kind of up to you.&nbsp;&nbsp; This example shows it in cuPy. Some of the other&nbsp; samples in the repository show it using Torch. You&nbsp;&nbsp; can use either one. Now we're going to launch&nbsp; this kernel as a grid of blocks. Just like in&nbsp;&nbsp; uh conventional CUDA where we would launch a grid&nbsp; of thread blocks, we're going to launch a grid of&nbsp;&nbsp; tile blocks. So the first thing we're going&nbsp; to do is figure out which block we are,&nbsp;&nbsp; which is going to determine what work we're&nbsp; responsible for. Knowing that, we can then load&nbsp;&nbsp; data from memory from these input vector, excuse&nbsp; me, these input arrays into tiles. Tiles are data,&nbsp;&nbsp; remember? So, we have an A tile and a B tile.&nbsp; These are one-dimensional tiles. They're vectors.&nbsp;&nbsp; And then we're going to do something with the data&nbsp; we've loaded. That is, we're going to add it add&nbsp;&nbsp; them together. That's going to create a new tile&nbsp; called result that stores the result of adding&nbsp;&nbsp; A to B. Then with that result tile, what we're&nbsp; going to do is we're going to store it. We're&nbsp;&nbsp; going to store it out to the array C. That's our&nbsp; vector add kernel in cuTile. If we scroll down,&nbsp;&nbsp; I'll show you the important bits of the host&nbsp; code is we create the arrays on the GPU using&nbsp;&nbsp; CP is cuPy. We instantiate A and B as random&nbsp; uniformly distributed vectors. and then C is&nbsp;&nbsp; just zeros of the same shape. We launch our&nbsp; kernel and then at the end we copy all of the&nbsp;&nbsp; arrays back to the host so that we can add the&nbsp; numbers together on the host and verify that&nbsp;&nbsp; the GPU answer and the CPU answer are identical.&nbsp; And if they are, we will get this uh happy print&nbsp;&nbsp; that says the vector ad example passed. So if we&nbsp; want to run this, so I'm already in the folder&nbsp;&nbsp; for the repository for cuTile Python. We just&nbsp; type Python and then this is in the samples&nbsp;&nbsp; folder under quick start and it's called vector&nbsp; add quickickstart.py. If we run it, we get our&nbsp;&nbsp; happy print. So that means it worked. There you&nbsp; go. That's your first kernel in cuTile Python.
