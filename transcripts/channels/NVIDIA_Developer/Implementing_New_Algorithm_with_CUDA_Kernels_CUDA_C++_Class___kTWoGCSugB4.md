# Implementing New Algorithm with CUDA Kernels | CUDA C++ Class Part 3

- 영상 링크: https://www.youtube.com/watch?v=kTWoGCSugB4
- 채널: NVIDIA Developer
- 업로드일: 2025-11-06
- 자막 언어: ko
- 단어 수: 약 8000개

---

## 스크립트

이 강의를 시청하는 동안 연습 문제를 풀기 위해 정기적으로 영상을 일시 정지해야 합니다. 해당 연습 문제를 완료할 수 있는 플랫폼이 두 개 있습니다 . 가속 컴퓨팅 허브에 하나 있습니다. DI 플랫폼에 두 개 있습니다. 더 자세한 내용은 첫 번째 영상의 소개 부분을 참고해 주세요. 모든 링크는 아래 설명란에서 찾으실 수 있습니다. [숨을 헐떡이며] 알았어. 안녕하세요 여러분. 이전 섹션에서는 동기 병렬 알고리즘이 성능을 어떻게 더욱 향상시킬 수 있는지 살펴보았습니다. 하지만 특정 사용 사례에 맞는 병렬 알고리즘이 없다면 어떻게 해야 할까요 ? 그래서 CUDA 라이브러리를 전혀 사용할 수 없습니다. 저는 신뢰를 사용할 수 없습니다. 제가 하려는 작업이 매우 특수하기 때문에 복잡한 이터레이터를 사용할 수도 없습니다 . 바로 이때 cud 커널을 사용하여 직접 알고리즘을 작성해야 하며, 이것이 이 섹션의 목표입니다. 음, 우선 CUDO 커널이 무엇인지에 대해 이야기해 보겠습니다 . 우리는 원자의 작동 원리에 대해 배우게 될 것입니다. 우리는 스레드 간 동기화라는 개념에 대해 배우게 될 것입니다 . 다음으로는 공유 메모리와 그것이 성능을 어떻게 향상시킬 수 있는지에 대해 이야기해 보겠습니다 . 마지막으로, 우리가 협력 알고리즘이라고 부르는 것에 대해 논의해 보겠습니다 . 그러니까 일반적으로 CPU는 호스트 함수를 호출하고 GPU는 장치 함수를 호출한다는 점을 기억하세요. 따라서 CPU에서 GPU 작업을 호출하려면 특별한 메커니즘이 필요합니다 . 지금까지는 딱히 신경 쓸 필요가 없었어요. 우리는 추진력을 요청하기만 하면 추진력이 알아서 일을 해낼 겁니다. 하지만 이제 우리는 어떤 라이브러리도, 심지어 스레드나 Thrust조차 사용하지 않고 자체 알고리즘을 구현하고 싶기 때문에 GPU를 실행하는 방법, 즉 GPU에서 코드를 직접 실행하는 방법을 이해해야 합니다. 그럼 실행 공간에 대해 간단히 정리해 보겠습니다 . 음, 지금까지 호스트 기능에 대해 논의했습니다. 그래서 함수에 `host`를 추가하면 이 특정 함수가 호스트에서 실행될 것이라고 지정하는 것입니다. 따라서 컴파일러는 이 함수에 대한 CPU 명령어를 생성해야 합니다 . 또한 장치를 지정하는 것에 대해서도 이야기했는데 , 이 경우 함수를 입력할 때 컴파일러에게 해당 함수에 대해 GPU 명령어를 생성해야 한다고 알려주어 GPU에서 실행될 수 있도록 합니다. 그리고 우리가 아직 논의하지 않은 또 다른 것이 있는데, 그것은 바로 글로벌이라고 불리는 것입니다. 음, 그러니까 `global` 태그를 사용하면 컴파일러가 GPU 명령어를 생성하여 해당 함수를 GPU에서 호출할 수 있게 됩니다. 하지만 이번에는 함수에 `global` 태그가 붙어 있기 때문에 CPU에서 호출됩니다. CPU가 이 함수를 호출하지만, 함수 실행은 GPU에서 이루어집니다. 이것이 바로 우리가 cudo 커널이라고 부르는 것입니다. CPU에서 cuto 커널을 호출하는 방식은 괄호 세 개 안에 숫자가 들어가 있어서 특이해 보입니다. 오른쪽 상단에서 볼 수 있는 것은 다음과 같습니다. 그럼 저 숫자들의 의미에 대해 이야기해 봅시다 . 저 숫자들의 의미에 대해서는 수업 후반부에 자세히 설명하겠습니다. 그리고 커널을 실행할 때 또 하나 중요한 점은 마치 컷처럼 동기적으로 실행된다는 것입니다. 그러면 CPU가 커널을 실행합니다. 그래서 커널은 GPU에서 실행되지만, CPU는 계속해서 작업을 처리합니다 . Thrust처럼 GPU에서 커널 응답이 돌아올 때까지 기다리지 않습니다 . 그러니까 사실상 잘린 것과 같아요. 동기식입니다. 자, 그럼 우리가 직접 cudo 커널을 작성하기 전에 현재 시뮬레이터가 어떻게 작동하는지 다시 한번 살펴보겠습니다. 앞서 설명드린 것처럼, 온도 격자의 2D 뷰를 얻기 위해 MD 스팬을 사용하고, 그 다음 cub 장치 변환을 사용하여 셀 인덱스를 새로운 온도로 변환합니다 . 내부적으로는 cudo 커널을 사용하여 장치 변환을 구현하는데, 이것이 바로 동기적으로 실행되는 이유입니다. 그럼 컴퓨터 커널이 실제로 어떻게 작동하는지 살펴보겠습니다. 그러므로 이미 익숙한 코드부터 시작하는 것이 가장 좋습니다 . 그럼, Thrust에 의존하는 대신 직접 커널을 실행하는 시뮬레이터를 다시 작성해 보겠습니다. 슬라이드에서 모든 셀을 순회하며 해당 셀의 인덱스를 새로운 온도로 변환하는 간단한 cur 커널 예제를 보실 수 있습니다. 커널을 실행할 때 세 개의 꺾쇠괄호(*) 구문을 사용하고, `force` 매개변수로 `cer stream`을 지정하는 것을 알 수 있습니다. 여기에 스트림을 전달하면 `cup`을 사용할 때처럼 지정된 스트림에서 비동기적으로 계산을 실행할 수 있습니다. 하지만 아시다시피 GPU는 코드를 자동으로 마비시키지 않습니다. 즉, 이 예제에서는 단일 GPU 스레드가 각 셀을 직렬로 계산합니다. 따라서 전체적으로 이 커널은 ` cup`보다 약 10,000배 느립니다. 왜냐하면 여기서는 단일 스레드로 `cer` 커널을 실행하기만 하고, 이 단일 스레드가 전체 계산을 수행하기 때문입니다. 당연히 GPU의 모든 스레드를 사용하는 것보다 훨씬 느립니다. 물론 커널이 사용하는 스레드 수를 늘리는 것은 어렵지 않습니다. 예를 들어 스레드를 하나 대신 두 개 실행하려면 두 번째 매개변수에 2를 전달하면 됩니다 . 이 트리플 셔블 구문에서 두 번째 매개변수는 실행하려는 스레드 수를 나타냅니다 . 그러니까 스레드를 두 개만 실행하면 되는 거죠 . 오른쪽에서 볼 수 있듯이, 스레드를 하나 대신 두 개 사용하면 커널 속도가 거의 두 배로 빨라집니다. 이는 두 배 더 많은 스레드를 사용하기 때문에 당연한 결과입니다. 하지만 단순히 스레드를 더 많이 실행하는 것만으로는 충분하지 않습니다. 만약 모든 프로세스가 동일한 셀을 처리한다면 실제로 속도 향상은 없습니다. 이 문제를 해결하려면 각 스레드가 스스로를 구분하고 서로 다른 셀 하위 집합을 처리할 수 있도록 커널을 수정해야 합니다. 두 스레드가 동시에 같은 작업을 수행할 수는 없습니다 . 오른쪽? 첫 번째 스레드는 배열의 일부를 처리해야 하고, 나머지 스레드들은 배열의 다른 부분을 처리해야 합니다 . 그래서 우리가 스레드 idx.x라고 부르는 것이 필요한 겁니다. 스레드 idx.x는 cudo 커널 내부에서 사용할 수 있는 내장 변수로, 현재 스레드의 인덱스를 저장합니다. 따라서 두 개의 스레드를 실행하면 첫 번째 스레드에서는 스레드 ID x가 0이 되고 두 번째 스레드에서는 1이 됩니다. 따라서 이 접근 방식을 사용하면 각 스레드 프로세스가 서로 다른 세포 하위 집합을 처리하게 됩니다. 그래서 커널에서는 먼저 스레드 ID x를 읽어서 현재 스레드 인덱스가 무엇인지 확인합니다 . 그런 다음 각 스레드는 자신의 스레드 인덱스와 일치하는 인덱스를 가진 셀부터 처리를 시작합니다. 즉, 첫 번째 스레드는 0번 셀에서 작업 하고 두 번째 스레드는 1번 셀에서 작업합니다 . 그래서 한 셀의 작업을 완료한 후에는 해당 셀 인덱스의 스레드 총 개수를 더하여 다음 셀로 넘어갑니다. 이러한 방식으로 첫 번째 스레드는 두 번째 셀과 그 다음 셀을 처리하고, 두 번째 스레드는 세 번째 셀로 이동하는 식으로 진행됩니다 . 그래서 첫 번째 반복 과정에서는 스레드 0과 1이 각각 두 개의 데이터를 처리하고, 그 다음에는 또 다른 두 개의 데이터를 처리하는 식으로 진행됩니다 . 이러한 방식을 통해 모든 셀을 병렬로 계산할 수 있었고, 하나의 셀이 여러 스레드에 의해 처리되는 일은 절대 없었으며, 하나의 스레드는 항상 하나의 셀만 처리하고, 하나의 셀은 항상 하나의 스레드에 의해서만 처리된다는 점에 주목하십시오 . 따라서 우리가 확인할 수 있는 또 다른 사항은 코드에 스레드를 두 개로 제한하는 구체적인 내용이 없다는 것입니다 . 그래서 우리는 두 개 이상의 스레드를 사용할 수 있습니다. 스쿠터 커널에서 작동하는 스레드 수를 늘려보도록 하겠습니다. 따라서 스레드 수를 2개에서 최대 256개까지 늘리면 속도가 훨씬 빨라지는 것은 당연합니다. 그래서 저희는 더 많은 스레드를 사용함으로써 성능이 상당히 향상되었지만, 보시다시피 Cub이 달성할 수 있는 수준에는 아직 한참 못 미칩니다. 그렇다면 위협 요소를 더 추가하는 건 어떨까요? 그래서 만약 우리가 24개나 48개의 스레드로 커널을 실행하려고 하면 갑자기 " 잘못된 구성 인수"라는 알 수 없는 오류 메시지가 나타납니다. 뭔가 우리가 이해하지 못하는 부분이 있는 것 같습니다. 왜냐하면 우리는 원하는 만큼 많은 스레드를 실행할 수 있어야 한다고 생각하지만, 실제로는 그럴 수 없기 때문입니다. 알고 보니 하나의 스레드 블록에 포함될 수 있는 스레드 수에는 제한이 있더군요 . 그렇다면 스레드 블록이란 정확히 무엇이며, 커널 실행에 어떤 영향을 미칠까요 ? CUDA에서는 스레드를 스레드 블록이라고 부르는 것으로 그룹화하고, 모든 스레드 블록의 전체 모음을 그리드라고 부릅니다. 따라서 각 스레드 블록에는 32개, 64개, 128개부터 최대 1,024개까지의 스레드가 포함될 수 있으며, 그 이상은 불가능합니다 . 따라서 컴퓨터 커널을 실행할 때 모든 스레드 블록은 정확히 동일한 수의 스레드를 갖게 됩니다. 그리고 thread ig.x를 사용할 때 얻게 되는 것은 이 로컬 블록 내의 스레드 인덱스입니다 . 따라서 스레드가 블록 0의 0번째 위치에 있으면 0이 반환됩니다. 만약 첫 번째 블록에 있는 스레드 0이라면, 스레드 ID.x 인덱스도 0이 될 것입니다. 왜냐하면 이것은 블록 내의 인덱스를 반환하고 블록 간의 인덱스는 반환하지 않기 때문입니다. 예를 들어, 이 화면에서 보시다시피 세 개의 블록이 있습니다. 그래서 여기 블록 0 스레드 0과 3 대 1에서 0과 1을 얻습니다. 마찬가지로 스레드 0 과 스레드 1은 각각 0과 1을 할당받고, 블록 2도 마찬가지입니다. 왜냐하면 이러한 인덱스는 해당 스레드 블록에 한정되기 때문입니다. 또한 ` block dim x`를 사용하여 블록의 너비를 가져올 수 있는데, 여기서는 블록당 스레드가 두 개이므로 값이 2입니다. 그리고 그리드의 길이도 가져올 수 있는데, 여기서는 `grid dim.x X`를 사용하면 3이 됩니다. 왜냐하면 블록당 스레드가 세 개이기 때문입니다. 마지막으로 현재 블록의 ID를 가져올 수도 있습니다. 자, 여기 첫 번째 블록에서는 idx.x가 0이고, 두 번째 블록에서는 idx.x가 1이며, 세 번째 블록에서는 idx.x가 2입니다. 이 모든 정보를 조합하면 전체 그리드에서 현재 위치를 계산할 수 있습니다. 따라서 블록 dx는 각 블록에 있는 스레드 수를 알려줍니다. 그래서 여기에는 2개의 블록이 있지만, 격자는 3개이므로 블록 수는 3개입니다. 따라서 블록 dx와 그리드 dmx를 곱하면 그리드 내 해당 요소의 총 개수를 구할 수 있습니다. 예를 들어 여기에는 세 개의 블록이 있습니다. 우리는 그리드 디믹스가 있고, 블록 DMX를 사용하여 각 블록의 길이를 곱하면 이를 알 수 있습니다 . 그래서 두 개. 그래서 2에 3을 곱하면 6이 되고, 6은 실제로 격자에 있는 실의 총 개수입니다 . 따라서 전체 스레드 수를 아는 것 외에도 전체 시스템에서 각 스레드의 고유 인덱스가 무엇인지 알아야 합니다. 이를 위해 블록 ID를 저장하는 블록 IDx.x를 사용할 수 있습니다. 음, 보시다시피 이 첫 번째 블록 안에 있는 모든 스레드는 모두 블록 ID가 0이 됩니다. 다른 블록에 있는 모든 스레드는 블록 ax 값이 1이 됩니다. 따라서 스레드의 전역 인덱스를 계산하려면 블록 idx 값을 곱하면 됩니다. 그럼 내 블록의 인덱스는 무엇이고, 그 인덱스에 블록의 길이를 곱하면 됩니다. 이렇게 하면 정확한 블록 에 도달하기 위해 필요한 오프셋 값을 알 수 있습니다 . 그리고 거기에 도달하면 로컬 스레드 ID를 추가하기만 하면 됩니다. 여기 스레드 ID x.x가 있습니다. 예를 들어 마지막 블록의 두 번째 스레드는 블록 ID인 x.x를 사용합니다. 그럼 그게 내 블록의 인덱스라는 거죠? 우리는 그것을 한 블록의 길이(여기서는 2)로 곱합니다. 그래서 이렇게 하면 오프셋을 1에서 2로 조정하여 현재 블록에 도달할 수 있습니다. 그리고 나서 마지막 스레드에 있는 제 스레드 ID x를 추가하면 됩니다. 그리고 저는 5개를 받아요. 실제로 전체 그리드에서 이 스레드는 다섯 번째 스레드입니다. 여기에는 0번 실, 1번 실, 2번 실, 3번 실, 4번 실이 있고, 여기에는 5번 실이 있습니다. 그러니까 다시 말해, 내 블록 ID에 그리드 크기에서 한 블록의 길이를 곱하고 스레드 ID를 더하면 전체 그리드에서 내 스레드의 전역 ID를 얻을 수 있습니다. 이제 그리드 내에서 각 스레드의 고유 인덱스를 찾는 방법을 알았으니, 전체 스레드 수도 알 수 있습니다. 한 가지 남은 질문은, 어떤 블록 크기를 사용해야 할까요? 방금 말씀드렸듯이 저는 32, 256, 1,024 크기의 스레드 블록을 실행할 수 있습니다 . 어떤 걸 선택해야 할까요? 그래서 안타깝게도 모든 경우에 가장 적합한 마법의 숫자는 없습니다 . 그래서 보통은 이 특정 수치를 미세 조정해야 합니다. 따라서 블록당 스레드 수는 문제에 따라 달라집니다. 블록 크기를 정할 때 유용한 지침 중 하나는 항상 32의 배수를 사용하는 것입니다. 예를 들어 50이나 60을 사용하는 것은 그다지 적절하지 않습니다. 항상 32의 배수를 사용해야 합니다. 일반적으로 저희가 권장하는 기본값은 256입니다. 하지만 이는 해결해야 할 문제에 따라 다르 므로 어떤 값이 가장 적합한지 살펴보시기 바랍니다 . 하지만 정말 모르겠고 신경도 안 쓴다면 그냥 256을 사용해도 됩니다. 그럼 두 번째 질문은 몇 개의 블록을 실행해야 할까요? 네, 제 블록은 256 스레드 크기입니다. 자, 그럼 몇 개의 블록, 그러니까 몇 개의 스레드 블록을 실행해야 할까요? 따라서 병렬 처리를 최대화하기 위해 그리드 크기는 일반적으로 문제 크기에 따라 달라집니다 . 그래서 제 문제는 제 문제의 요소를 어떻게 여러 스레드로 나누어 처리하느냐는 것입니다. 예를 들어 각 스레드가 정확히 하나의 요소만 처리하도록 하려면 간단한 정수 나눗셈을 사용하면 제대로 작동하지 않습니다. 처리해야 할 셀이 6개 있고 블록 크기를 4개 스레드로 선택했다고 가정해 보세요. 6을 4로 나누면 1이 됩니다. 그래서 우리는 스레드 블록을 하나만 사용할 것입니다 . 하지만 만약 6개의 셀을 처리하기 위해 4개의 스레드로 구성된 하나의 스레드 블록만 사용하고 각 스레드가 하나의 요소만 처리한다면, 마지막 두 요소를 처리하는 데 필요한 스레드 블록 하나가 부족하게 된다는 것을 알 수 있습니다 . 그래서 CUDA 내부에서는 CUDA seal div라는 기능을 제공하여 위의 정수로 반올림합니다. 따라서 이 기능을 사용하면 요소당 정확히 하나의 스레드를 사용하려는 경우 필요한 스레드 블록의 정확한 양을 자동으로 계산할 수 있습니다 . 그러니까 6과 4를 예로 들었을 때, seal div를 호출하면 하나가 아니라 두 개가 반환됩니다. 따라서 이 예제에서는 각각 4개의 스레드가 할당된 2개의 스레드 블록이 올바르게 구성되어 필요한 6개의 셀을 모두 처리할 수 있습니다 . 따라서 사용하려는 스레드 수 또는 스레드 블록 수를 커널에 전달합니다 . 여기서 첫 번째 매개변수입니다 . 먼저 블록 크기를 지정할 수 있습니다 . 그래서 우리는 평소처럼 256을 선택했습니다. 그런 다음 몇 개의 블록이 필요한지 알고 싶습니다 . 그래서 다시 한번 말씀드리지만, 저는 Cudacial div를 사용하여 전체 문제 크기와 각 블록의 크기를 전달하고 있습니다. 이렇게 하면 필요한 총 블록 수를 정확하게 얻을 수 있습니다. 그리고 커널을 호출할 때 먼저 그리드 크기를 사용하여 경로를 지정합니다. 그러니까 필요한 실 블록의 개수 와 각 블록의 크기를 알려주세요. 이러한 변경 사항 덕분에 이제 약 5백만 개의 스레드를 실행할 수 있게 되었고, 마침내 컵 대회 수준의 성능에 근접하게 되었습니다 . 그래서 당연히 cup에는 내부적으로 더 발전된 최적화 기술이 숨어 있습니다. 그래서 아마 CUPS의 속도에는 미치지 못할 수도 있겠지만, 이렇게 간단한 커널에서 스레드를 추가하고 ID와 스레드 블록을 올바르게 계산하는 것만으로 이 정도의 속도 향상을 달성했다는 것은 이미 매우 훌륭한 결과입니다. 음 , 저희가 시뮬레이터를 수정했는데, 아직 제대로 작동하는지 확인은 못 했습니다. 우리의 장치는 대칭 구조이기 때문에 격자의 위쪽과 아래쪽 온도가 동일합니다. 그래서 대칭성을 확인하는 간단한 테스트를 해볼 수 있습니다. 아이디어는 간단합니다. 주어진 셀에 대해, 해당 셀의 대칭 행을 찾으려면 그리드 높이에서 행의 인덱스를 뺍니다. 따라서 모든 것이 정확하다면 대칭 세포의 온도는 거의 동일해야 합니다. 화면에는 이러한 검사를 수행하는 C++ 함수가 표시됩니다. 따라서 이 연습의 일환으로, 여러분은 이 CPU C++ 함수를 커 커널로 변환 하고 트리플 셔블 구문을 사용하여 실행해야 합니다. 그래서 블록과 실 등의 양을 적절하게 선택함으로써 이 연습을 할 수 있습니다. 지금부터 5분 동안 진행하겠습니다. 대칭 검사 함수를 절단 커널로 바꾸려면 전역 지정자를 사용하여 주석을 달아야 합니다. 그러니까 ` global`을 사용하면 GPU에서 코드를 실행할 수 있다는 점을 기억하세요. 따라서 이 함수는 전역 변수를 지정했기 때문에 GPU에서 작동할 수 있도록 컴파일됩니다. 하지만 전역 변수를 사용하므로 이 함수는 CPU에서 비동기 방식으로 호출할 수 있습니다. 그 후에는 앞서 설명한 트리플 셰브론 구문을 사용하여 함수를 실행해야 합니다. 여기서는 실제 병렬 작업이 없으므로 단일 스레드로 단일 블록만 실행하면 됩니다. 이러한 검사가 올바른 스트림에서 실행되도록 하려면 마지막 매개변수로 사용할 스트림을 지정합니다 . 자, 생각해 보면, 행 전체가 대칭이어야지, 특정 셀 하나만 대칭이면 안 되잖아요. 이번 연습에서는 각 스레드가 정확히 한 열만 처리하도록 커널을 수정할 것입니다. 연습 시간은 지금부터 8분입니다. 자, 시간이 다 됐습니다. 이전과 마찬가지로 블록 인덱스와 스레드 인덱스를 사용하면 스레드의 전역 ID를 계산할 수 있습니다. 그래서 우리가 여기 맨 위에서 하는 일은 우리가 지금 어느 열에 있는지 알기 위해서입니다 . 우리는 블록 IDX를 사용합니다. 그래서 블록 인덱스에 블록 너비를 곱하고, 블록 안에 있는 현재 스레드 ID를 더합니다. 이렇게 하면 열 인덱스가 무엇인지 계산할 수 있습니다. 그래서 이제 실제 커널을 호출할 때 이전과 마찬가지로 블록 너비를 선언할 수 있습니다. 기본값인 256을 사용했죠. 그리드 크기를 계산하려면 문제의 전체 크기(이 경우에는 너비)를 CUDA의 `div` 함수를 사용하여 블록 크기로 나눕니다. 커널을 호출할 때는 첫 번째 매개변수로 그리드 매개변수를, 두 번째 매개변수로 블록 매개 변수를 전달합니다. 이렇게 하면 각 스레드가 정확히 한 열씩 처리하고 전체 행을 병렬로 검사할 수 있습니다. 하지만 이렇게 하면 첫 번째 경계 초과 오류가 발생할 수 있습니다. 각 스레드가 최소한 하나의 요소를 처리하도록 반올림을 하기 때문에 실제 문제 크기보다 너무 많은 스레드가 실행될 수 있기 때문입니다. 이 경우를 생각해 보면, 이 예시에서는 각각 4개의 스레드를 가진 2개의 스레드 블록을 실행하지만, 전체 요소는 6개뿐입니다. 따라서 첫 번째 블록은 네 개의 스레드를 사용하여 처음 네 개의 요소를 처리합니다 . 두 번째 블록에도 스레드가 네 개 있습니다. 모든 블록이 동일한 수의 스레드를 가지고 있지만 처리해야 할 요소는 두 개뿐이기 때문입니다. 따라서 마지막 두 스레드는 실제로 존재하지 않는 요소에 접근하려고 시도할 것입니다. 따라서 이 마지막 블록의 마지막 두 스레드에 의한 두 번의 접근은 범위를 벗어난 접근이 될 것입니다. 그래서 우리는 그 문제를 해결할 방법을 찾아야 합니다. 그래서 이 문제를 해결하는 쉬운 방법 중 하나는 커널에 경계 검사라고 부르는 것을 추가하는 것입니다. 전역 스레드 ID를 계산한 직후, 이 값을 전체 열 개수와 비교하여 수행할 작업이 없으면 건너뛰면 됩니다. 그래서 여기서 이중 열 ID를 계산한 후에는 해당 열이 MD 스팬에서 확장 1을 통해 찾을 수 있는 너비보다 작은지 확인합니다. 즉, 열 ID가 이 너비보다 작으면 해당 영역으로 들어가 검사를 수행합니다. 하지만 이러한 경계 검사에 대해 알지 못했다면 어떻게 스스로 이를 발견할 수 있었을까요? NVIDIA에서는 CUDO 커널에서 범위를 벗어난 접근 및 기타 메모리 문제를 감지하는 데 도움이 되는 Compute Sanitizer라는 특수 도구를 제공합니다. 슬라이드에 나와 있는 플래그를 사용하여 이 시뮬레이터 코드를 컴파일하면 다음과 같습니다. 여기 맨 위쪽에 보이는 것처럼, 컴퓨터 분석기는 불법 접근이 발생한 정확한 라인 위치를 파악해 줍니다. 예를 들어 여기에서 보시 다시피 필요한 모든 정보를 실제로 갖추고 있습니다 . 우리는 잘못된 전역 읽기 작업을 수행했음을 알고 있습니다. 그러니까 이건 권리가 아니라, 우리가 부동소수점을 사용하고 있기 때문에 어떤 크기가 적절한지에 대한 판단입니다. 우리는 이것이 특정 커널, 즉 대칭성 검사 커널에서 비롯된다는 것을 알고 있습니다. 우리는 파일 row symmetry check.cu를 알고 있으며 심지어 7번째 줄까지 알고 있습니다. 우리는 어떤 블록의 어떤 스레드인지 알고 있으며, 심지어 가장 가까운 유효한 할당이 무엇이었는지도 알고 있습니다. 그래서 우리는 이것이 실제로 우리가 범위를 벗어난 접근을 수행했던 이전 블록에서 비롯된 것임을 알 수 있습니다. 그래서 이런 식으로 컴퓨팅 새니 타이저는 우리가 필요로 하는 모든 정보를 제공해 주었고, 덕분에 우리가 놓쳤던 것은 경계 검사 조건이었다는 것을 알게 되었습니다. 따라서 실제로 엔드 스팬은 범위를 벗어난 접근도 자체적으로 감지할 수 있습니다. 기본적으로는 비활성화되어 있지만, 컴파일할 때 위에서 보여드린 것처럼 `cccl enable` 어설션을 정의하면 mpan이 접근 시 범위를 벗어난 경우를 감지하게 됩니다. 그렇게 하면 보시는 것처럼 어설션이 발생합니다. 그러니까 다시 말씀드리지만, 어떤 블록의 어떤 스레드가 뼈 접근을 만들어냈는지 알게 되실 겁니다. 이 방법은 MD 스팬을 통해 배열에 접근할 때, 접근하려는 데이터의 크기가 MD 스팬의 크기보다 큰지 확인하는 방식으로 작동합니다 . 경계 검사를 추가함으로써 버그를 수정했으니 이제 당연히 "왜 스레드, 스레드 블록, 그리드 등과 같은 복잡한 스레드 계층 구조가 필요한가?"라는 질문이 나올 수 있습니다. 이 모든 것이 왜 필요한지 이해하려면 먼저 문제를 약간 수정하여 스레드 블록 계층 구조가 실제로 왜 매우 타당한지 살펴보아야 합니다 . 그럼 이제 시뮬레이터가 생성한 온도에 대한 히스토그램을 만들어 보겠습니다. 히스토그램은 특정 온도 범위가 얼마나 자주 발생하는지, 즉 빈도를 파악하는 데 도움이 됩니다. 그럼 이게 어떻게 작동하는지 간단히 복습해 볼까요? 먼저 전체 온도 범위를 두 개의 서로 다른 구간으로 나눕니다. 우리 경우에는 10개의 서로 다른 온도가 있으므로 시뮬레이션의 각 셀에 대해 온도가 어느 구간에 속하는지 결정합니다. 온도를 특정 구간으로 나누는 간단한 방법은 반올림하는 것입니다. 예를 들어 세포의 온도가 4°라면 10으로 나누면 0이 됩니다. 그러므로 이 4°는 0번 칸에 들어가야 한다는 것을 알 수 있습니다 . 하지만 만약 셀 온도가 예를 들어 15도이고, 첫 번째 칸에 15도를 넣고 싶다면, 마찬가지로 15를 10으로 나누면 됩니다. 그러면 1이 나옵니다. 그러니까 아시다시피 이 온도 15는 첫 번째 칸에 들어가야 합니다 . 이게 바로 여러분이 보고 계신 예시입니다. 첫 번째 구간에는 0과 10 사이의 값들만 넣고 싶습니다. 따라서 입력 값인 2나 4를 10으로 나누면 0이 나옵니다. 그럼 그걸 0번 칸에 넣겠습니다. 반면에, 우리가 원하는 15개의 값들은 모두 10과 20 사이의 첫 번째 구간에 속하게 됩니다. 따라서 15를 10으로 나누면 1이 나옵니다. 그래서 우리는 그것을 1번 칸에 넣어야 한다는 것을 알고 있습니다. 음, 그래서 각 세포를 올바른 칸에 배정하고 나면, 각 칸에 들어간 세포의 수를 세면 됩니다. 그래서 그 개수가 히스토그램에서 보이는 막대의 높이가 됩니다. 그래서 우리 예시에서는 첫 번째 범주에 속하는 세포가 8개이고 두 번째 범주에 속하는 세포가 4개입니다. 그래서 이 최종 시각 자료를 통해 패턴을 쉽게 파악하고 시뮬레이션에서 가장 흔하게 나타나는 온도 범위를 확인할 수 있습니다 . 자, 이제 이 히스토그램을 생성하기 위한 자체 커널 시그니처를 구축하는 방법을 생각해 봅시다. 음, 히스토그램은 한 차원만 있으면 되니까요. 따라서 MD 스팬은 필요하지 않습니다. MD 스팬은 다차원 스팬인데, 우리는 1차원이기 때문에 차원이 하나뿐입니다. 그래서 여기서는 std md span 대신 cuda uh cuda std span만 사용하면 됩니다. 그래서 CUDA에서는 C++에서와 마찬가지로 행 포인터를 사용하는 것보다 span을 사용하여 데이터에 접근하는 것이 더 안전합니다. 왜냐하면 앞서 살펴본 것처럼 span은 범위를 벗어난 접근을 감지할 수 있기 때문입니다. 포인터를 가지고 이상한 짓을 하는 것도 피할 수 있고, 크기 등에도 접근할 수 있습니다 . 따라서 스팬을 건설하는 방법은 ND 스팬을 건설하는 방법과 동일합니다. 기본 컨테이너의 데이터를 스팬으로 표시하고 크기를 지정하면 됩니다. 그래서 그것에 접근하는 방법은 벡터와 같은 일반적인 컨테이너를 사용하는 것과 똑같습니다. 중괄호를 사용하면 접근할 수 있습니다. 그래서 벡터와 마찬가지로 요소의 크기에 접근할 수 있습니다 . 그리고 다시 말씀드리지만, 접속하실 때에도 보호받으실 수 있습니다. 음, 온도와 히스토그램이 모두 커널에 전달되었으므로, 다시 한 번 블록 ID, 블록 크기, 그리고 세 번째 ID를 사용하여 항상 사용하는 동일한 공식으로 전역 스레드 ID를 계산하기만 하면 됩니다. 그런 다음 각 스레드는 해당 항 값을 로드하고 이를 빈 너비로 나눕니다. 앞의 예시에서는 10이었지만, 여기서는 다를 수도 있습니다. 그리고 우리는 다시 한번 올바른 빈 인덱스를 찾기 위해 그렇게 하는데, 이때 우리의 각도를 기준으로 1을 더할 것입니다 . 따라서 실제 운영 환경에서는 이러한 메모리 접근이 DA 범위 내에 있는지 확인해야 합니다. 하지만 슬라이드의 간결성을 위해 경계 검사는 생략하고 문제 크기가 블록 크기의 배수라고만 하겠습니다. 다음으로 각 스레드는 할당된 값에서 현재 카운트를 읽고 값을 증가시킨 다음 히스토그램에 다시 기록합니다. 자, 여기서 먼저 전체 히스토그램 값을 불러옵니다. 우리는 그것을 증가시킵니다. 예를 들어 제 체온이 4도라고 가정해 볼게요. 그래서 저는 첫 번째 칸에 들어가고 싶어요. 그래서 저는 구간 너비로 나누어 계산을 하고 있습니다. 4는 0과 10 사이에 있으므로 결과는 0이 됩니다. 따라서 빈 값은 0이 됩니다. 그럼 이제 히스토그램 에서 13번째 빈의 값이 무엇인지 가져오겠습니다 . 그럼 이제 0이라는 새로운 값을 추가하고 싶기 때문에 값을 1씩 증가시키겠습니다. 그런 다음 결과를 메모리에 다시 저장하면 되죠. 그런데 그렇게 하면 히스토그램에 아무것도 나타나지 않습니다. 뭔가 잘못된 것 같습니다. 그리드에는 약 4백만 개의 셀이 있는데, 막대 그래프는 백만 단위여야 하잖아요. 그런데 아무것도 보이지 않습니다 . 그렇다면 여기서 무슨 일이 일어난 걸까요? 문제는 커널에서 여기 강조 표시된 세 줄의 코드에서 데이터 경쟁이 발생한다는 것입니다. 그러니까 수백만 개의 스레드가 모든 곳에서 동일한 메모리 위치를 읽고 쓰는 거죠 . 그럼 여기서 정확히 무슨 일이 벌어지고 있는지 살펴보죠. 그러니까 스레드가 40억 개가 아니라 400만 개, 딱 두 개만 있다고 가정해 봅시다. 자, 여기 스레드 0이 있습니다. 어, 첫 번째 스레드는 0번 스레드에서 읽으려고 하고, 두 번째 스레드도 0번 스레드에서 읽으려고 합니다 . 그래서 모두 동일한 메모리 읽기 요청을 보냅니다. 좋아요. 따라서 히스토그램에서 첫 번째 스레드가 메모리 읽기를 가장 먼저 수행합니다. 좋아요. 그러면 메모리 하위 시스템은 히스토그램 빈에 저장된 현재 값(이 예시에서는 0)으로 응답합니다. 그러면 스레드 0에서 가지고 있던 이전 값인 val은 0으로 업데이트됩니다. 히스토그램이 방금 응답했기 때문입니다. 0번째 구간의 값이 0입니다. 따라서 스레드 1은 해당 변수에 1을 더하고 결과를 new에 저장합니다. 하지만 스레드 0이 그 작업을 수행하는 동시에 스레드 2는 히스토그램을 읽는 작업도 동시에 진행합니다. 하지만 이 추가 사항이 아직 메모리에 반영되지 않았기 때문에 스레드 2도 히스토그램에서 0 값을 읽게 되고, 따라서 0 값도 스레드에 다시 전달되어 이전 값으로 돌아갑니다. 그러면 두 번째 스레드는 기존 값에 1을 더하고 다시 저장하려고 하는데, 동시에 첫 번째 스레드는 히스토그램에 값 1을 기록하고 있습니다. 그래서 여기 있는 1이 히스토그램에 기록되고, 이제 히스토그램 값은 1이 됩니다. 하지만 문제는 두 번째 스레드도 값이 1이라는 것이고, 이 스레드 역시 히스토그램 값에 1을 기록하기 시작한다는 것입니다 . 따라서 이 스레드는 현재 이 스레드가 값 1을 쓰고 있다는 정보가 없으므로, 이 스레드는 해당 값 1을 사용하여 자체 카운터를 업데이트해야 합니다. 하지만 이미 너무 늦었어요. 이미 값이 읽혔으므로 이 히스토그램에도 1이 기록됩니다. 따라서 최종적으로 히스토그램에는 1만 남게 되는데, 이는 두 개의 서로 다른 스레드가 모두 이 히스토그램 값에 1을 더하려고 시도했기 때문입니다. 두 개의 스레드에서 이런 일이 일어났습니다. 이제 수백만 개의 스레드가 동시에 그 작업을 시도할 때 어떤 일이 벌어질지 상상해 보세요 . 그래서 히스토그램이 비어있는 것처럼 보이는 겁니다. 값을 증가시킬 때 다른 스레드에서 시도하는 작업을 고려하지 않기 때문인데, 이를 데이터 경쟁이라고 합니다. 따라서 이러한 경쟁 조건을 해결하려면 읽기-수정-쓰기 순서 전체가 분할 불가능한 단일 메모리 작업으로 처리되도록 해야 합니다 . 오른쪽에 수정된 내용 외에는 더 이상 읽고 싶지 않습니다. 우리는 전체 과정을 하나의 블록으로 완료하고 싶습니다 . C++에서는 원자 연산을 사용하여 이를 수행할 수 있습니다. 따라서 원자적 비교는 단순히 바이트를 저장하는 것이 아니라 명령어 자체를 저장하는 것으로 생각할 수 있습니다 . 우리는 모든 걸 한 번에 끝내고 싶어요. 오른쪽 예시에서는 1을 더한 값을 메모리에 저장합니다. 그리고 연산이 메모리 시스템에 도달하면 현재 값을 읽고, 값을 증가시킨 다음, 결과를 다시 메모리에 쓰는 단일 단계 작업을 수행합니다. 이 과정은 왼쪽에 표시되어 있습니다. 이전에는 먼저 읽고, 지역 변수를 업데이트한 다음 쓰는 방식이었습니다. 지금 우리가 하고 있는 것은 데이터를 가져오는 작업인데, 이것 역시 C++에서 할 수 있는 작업입니다. 모든 작업이 하나의 플랫폼에서 이루어집니다 . 이렇게 함으로써 우리는 어떤 스레드도 동시에 읽거나 수정할 수 없도록 방지합니다. 따라서 각 스레드는 단일 원자적 작업을 수행하고, 세 개의 읽기, 수정, 오른쪽 작업은 모두 단일 시퀀스로 수행됩니다. 다행히 CUDA는 CUDA std atomic ref라는 기능을 통해 원자성을 제공하며, 이를 통해 기존의 모든 메모리 위치를 원자적 변수처럼 다룰 수 있습니다. 따라서 현재 사용 가능한 모든 메모리 위치를 원자적 참조로 볼 수 있습니다. 이 예제에서는 count 0을 원자적 참조로 감싸는 것만으로 count 0에 대한 원자적 참조를 생성합니다. 즉, 우리는 원자적 참조인 이 참조의 길이를 통해 수정함으로써 이 카운트 0에 대해 원자적 연산을 수행할 수 있습니다 . 이제 우리는 원자적 덧셈, 원자적 부분 연산, 원자적 끝 연산을 수행할 수 있으며, 이러한 연산을 수행할 때마다 해당 연산은 모두 원자적으로 이루어집니다. 따라서 이 모든 시술은 단 한 번의 수술로 진행됩니다. 세 개의 스레드가 수정하는 것을 방지할 수 있으므로, 이를 통해 여러 스레드가 병렬로 카운트를 수정할 수 있으며, 경쟁 조건 없이 작업할 수 있습니다. 그럼 이제 그걸 우리 커널에 어떻게 넣을 수 있는지 살펴보겠습니다 . 따라서 커널에서 이제 스레드 1은 원자적 참조에 대해 fetch add one을 사용하여 히스토그램에 1을 더하고, 스레드 2도 히스토그램에 대해 fetch add one을 수행하려고 할 것입니다. 그러면 이제 스레드 1이 데이터를 가져와서 추가하고, 데이터가 도착하면 읽기, 수정, 쓰기 작업을 모두 단일 시퀀스로 수행하고 결과를 메모리에 다시 저장합니다. 이제 스레드 1이 fetch 광고를 실행하려고 할 때, 해당 광고가 메모리에 도착하면 새 값인 1을 정확하게 읽게 됩니다. 왜냐하면 fetch 광고는 첫 번째 fetch 광고보다 먼저 실행될 수 없기 때문입니다 . 그래서 이 광고 요청이 도착하면 첫 번째 광고는 완전히 완료된 상태입니다. 그래서 우리는 그 하나를 정확하게 볼 수 있는 것입니다. 그러면 1씩 정확하게 증가시킨 다음, 최종 히스토그램 값인 2를 메모리에 정확하게 업데이트할 수 있습니다. 따라서 이제 우리는 결과가 정확하고 그 과정에서 어떠한 증분도 손실되지 않는다는 것을 알게 되었습니다 . 따라서 이번 연습에서 여러분이 해야 할 일은 현재 히스토그램 코드를 수정하는 것입니다. 기존 코드는 세 줄로 읽기/수정을 바로 수행하여 데이터베이스를 생성합니다. 이제 방금 이야기했던 원자 참조(atomic ref)를 사용하여 코드를 작동시켜야 합니다. 이를 위해 5분의 시간이 주어집니다. 자, 시간이 다 됐습니다. 수정 사항은 단 몇 줄에 불과합니다. 사실 예시에서 보여드린 것처럼, 우리가 쓰려는 히스토그램 빈을 원자적 참조(atomic ref)로 감싸기만 하면 됩니다. 그래서 이제 이 원자 참조를 통해 기록할 때 히스토그램 빈에 기록하지만, 원자를 통해 기록하는 것입니다. 그래서 우리는 여기서 무언가를 실행할 때, 즉 `fetch add` 작업이 원자적 작업이며, 모든 읽기/ 수정 권한을 처리하는 단일 시퀀스임을 확신합니다. 이렇게 하면 우리가 직접 처리하여 경쟁 상태를 만드는 대신, 모든 읽기/수정 권한을 한 번에 처리할 수 있습니다. 따라서 이 수정 사항을 적용하면 최종적으로 결과가 정확해집니다. 그래서 우리는 어느 정도 이해가 되는 히스토그램을 볼 수 있습니다 . 그러므로 초기 온도가 거의 0도에 가깝다는 것을 알 수 있는데, 이는 첫 번째 콩에 가장 많은 세포가 있다는 것을 의미합니다. 열이 격자를 통해 퍼져나가면서 더 많은 콩이 채워지는 것을 볼 수 있습니다 . 하지만 지금 또 다른 문제가 있는데, 바로 성능이 매우 나쁘다는 것입니다. 현재 우리는 겨우 6GB밖에 활용하지 못하고 있는데, 이는 GPU 메모리 최대 대역폭과는 매우 거리가 멉니다. 그래서 우리는 6GB보다 훨씬 더 나은 용량을 원합니다. 지난번에 성능이 저조했던 이유는 직렬화 때문이었습니다. 모든 스레드를 사용하지 않고 일부 스레드만 사용했기 때문이죠. 하지만 이번 코드에는 반복문이 없죠 , 그렇죠? 저희는 모든 스레드를 사용하고 있습니다. 저희는 parallelm을 사용하고 있습니다. 저희는 스레드 블록을 사용하고 있습니다. 도대체 여기서 무슨 일이 벌어지고 있는 거죠? 하지만 이번에는 병렬화와 직렬화가 원자 연산 자체에서 비롯됩니다. 네, 맞습니다. 이러한 방식은 데이터 경쟁 조건을 해결합니다. 왜냐하면 설계상 병렬 실행이 불가능하기 때문입니다. 애초에 병렬 실행을 원하지 않기 때문입니다 . 모든 스레드가 동일한 메모리 위치를 대상으로 하고 있습니다. 그래서 지금 우리는 아주 아주 많은 원자적 연산들을 하나씩 순차적으로 실행하고 있습니다. 그래서 약 256개의 스레드를 실행하면 약 16,000개의 블록이 생성됩니다. 총 400만 건의 원자적 작전이 실행되고 있으며, 이 모든 작전은 순차적으로 개시되고 있습니다 . 음, 이건 분명히 너무 과한 것 같아요. 원자적 연산을 너무 많이 실행하면 코드가 매우 느려지기 때문에 그렇게 하고 싶지 않습니다. 그렇다면 이 문제를 해결하기 위해 우리가 선택할 수 있는 방법은 무엇일까요? 한 가지 해결책은 우리가 민영화라고 부르는 것입니다. 따라서 모든 스레드가 동일한 전역 히스토그램을 업데이트하여 모든 비트에 대해 너무 많은 원자적 연산이 대기열에 쌓이는 대신 , 각 스레드 블록에 대해 작은 개인 히스토그램을 할당할 수 있습니다. 각 스레드 블록은 원자 연산을 안전하게 사용할 수 있는 자체 히스토그램을 갖게 됩니다. 따라서 전체 원자 연산량은 줄어들지 않지만, 모든 스레드가 동일한 메모리 위치에 쓰는 대신 연산이 더 많은 메모리 위치에 분산됩니다. 이제 스레드 블록은 먼저 자체 메모리 위치에 쓴 다음 전역 히스토그램으로 전파합니다. 작업이 완료되면 블록은 자체 개인 히스토그램의 카운트를 집계하고 하나의 메모리 원자적 연산을 사용하여 다시 메인 히스토그램을 업데이트합니다 . 그래서 우리의 간단한 twob 블록 예시에서, 두 블록 각각은 자체적인 로컬 블록 히스토그램을 증가시킨 다음, 그 결과를 최종 전체 히스토그램에 다시 반영합니다 . 이 예시에서는 블록이 두 개밖에 없어서 그다지 도움이 되지는 않지만요 . 실제로 그런 사례가 16,000건이나 된다고 상상해 보세요 . 따라서 이 시나리오에서는 16,000개의 개인 히스토그램이 병렬로 업데이트된 후 16,000개의 원자적 연산만 수행되면 됩니다. 이전에는 전역 메모리에서 4백만 개의 원자적 연산을 수행했는데, 이 모든 연산이 대기열에 쌓여 있었습니다. 각 스레드 블록은 하나의 작업만 수행하기 때문에 전역 메모리에서 수행되는 원자적 연산은 총 16,000개에 불과합니다. 따라서 이러한 개인 정보 보호를 구현하기 위해 커널에 블록 히스토그램이라는 새로운 매개변수를 추가해야 합니다. 다음으로, 개인화된 히스토그램의 현재 스레드 블록 부분에 대한 하위 스팬을 생성합니다 . 따라서 서브스팬 함수는 오프셋과 크기를 인수로 받으며, 블록 인덱스에 히스토그램 크기를 곱하여 오프셋을 계산합니다. 따라서 첫 번째 블록의 오프셋은 0입니다. 두 번째 블록은 오프셋이 10입니다. 이런 식으로 각 블록이 자체 메모리 영역에 쓰도록 합니다 . 따라서 각 블록은 이 개인화된 히스토그램의 자체적인 부분을 갖게 됩니다. 그러면 이전과 마찬가지로 각 스레드가 핀 인덱스를 계산합니다. 하지만 이제 전역 히스토그램 대신 개별 블록 히스토그램을 증가시키는 이유는 각 블록별로 개별 히스토그램을 증가시키고 싶기 때문입니다. 왜냐하면 전역 메모리에서 수행하는 원자 연산의 양을 줄이고 싶기 때문입니다. 우리는 이를 개인화된 히스토그램에서 수행하여 모든 사람이 공유 히스토그램에 다시 기록하는 원자적 데이터의 양을 줄이고자 합니다. 그 후 각 블록은 자체적으로 추가한 부분적인 개인 정보를 더하고, 각 블록은 이 개인 정보의 부분적인 카운트를 병렬적으로 전역 히스토그램에 다시 추가합니다. 그래서 남작에게서 볼 수 있는 것은 바로 이것입니다. 잘라낸 코드 조각에서 블록의 처음 10개 스레드는 각각 광범위한 개인 히스토그램에 대한 빈 하나를 처리 하고 전역 히스토그램에 대한 권한을 무력화합니다 . 그래서 그들은 해부학적 참조를 다시 사용하여 로컬 카운트를 최종 히스토그램에 안전하게 추가합니다. 하지만 안타깝게도 그렇게 함으로써 새로운 버그가 발생했습니다. 그럼 지금 무슨 일이 벌어지고 있는지, 그리고 왜 이런 버그가 발생하는지 좀 더 자세히 살펴보겠습니다. 따라서 코드에서는 모든 스레드가 다음 코드 세션이 시작되기 전에 로컬 블록 히스토그램 업데이트를 완료한다고 가정했습니다. 따라서 모든 스레드가 전역 블록 히스토그램에 쓰기 전에 로컬 블록 히스토그램에 먼저 쓴다고 가정합니다. 하지만 CUDA 스레드는 블록 내에서 동시에 실행되며 순서가 보장되지 않습니다 . 이는 스프레드 블록의 일부 스레드는 아직 시작조차 하지 않았지만 다른 스레드는 이미 커널을 종료하기 시작했을 수 있음을 의미합니다. 따라서 스레드 블록에 있는 모든 스레드가 예약될 것이라는 보장은 없습니다. 그래서 어떤 사람은 코드의 맨 처음 부분에 있을 수도 있고, 어떤 사람은 코드의 맨 끝 부분에 있을 수도 있습니다. 가능한 시나리오 중 하나는 일부 스레드가 블록 내의 모든 스레드가 작업을 완료하기 전에 블록 히스토그램을 읽어 들인 후, 그 결과를 전역 히스토그램에 기록하는 경우입니다 . 이 문제를 해결하려면 모든 스레드가 로컬 블록 히스토그램 업데이트를 완료한 후에야 히스토그램을 읽을 수 있도록 해야 합니다. 이를 위해 CUDA는 우리가 '씽 스레드'라고 부르는 것을 제공합니다 . 그러니까 스레드는 블록 내의 모든 스레드에 대한 장벽 역할을 하는 특별한 함수라고 생각하면 됩니다. 따라서 스레드가 이 장벽에 도달하면 스레드 블록 내의 다른 모든 스레드도 이 장벽 에 도달할 때까지 기다립니다 . 그런 의미에서 표준 C++ 라이브러리에 있는 std-barrier라는 것과 유사합니다. 하지만 여기에는 중요한 차이점이 하나 있습니다. 블록 내의 모든 스레드는 동일한 논리적 경로를 통해 think 스레드를 호출해야 합니다. 음, 스레드를 사용하면 기능적인 문제는 해결될 것 같지만 성능 문제가 여전히 남아 있습니다. 자, 그럼 립립쿠++ 개요를 다시 살펴보면 두 가지 다른 원자 참조 체계를 발견할 수 있을 겁니다 . 하나는 cud 네임스페이스에 있고 다른 하나는 cuda 네임스페이스에 있습니다. 그래서 자연스럽게 둘의 차이점이 무엇인지 궁금해졌습니다. 따라서 CUDA std atomic ref와 CUDA atomic ref는 본질적으로 동일한 인터페이스를 제공합니다 . 하지만 CUDA atomic ref는 스레드 범위 템플릿 매개변수를 추가하여 CUDA std atomic craft를 확장합니다. 따라서 적절한 나사산 범위를 선택하는 것은 성능에 상당한 영향을 미칠 수 있습니다. 그럼 스레드 스코프가 무엇인지 좀 더 자세히 알아봅시다. 따라서 스레드 범위는 주어진 원자적 연산을 사용하여 동기화할 수 있는 스레드 집합을 나타냅니다. 따라서 범위는 전체 시스템, 장치 또는 스레드 블록일 수도 있습니다. 예를 들어 해부학적 구조와 관련된 모든 스레드의 경우 CUDA 스레드 스코프 시스템을 사용합니다. 즉, 어떤 GPU의 스레드든 GPU 내의 다른 스레드 또는 CPU 스레드와 동기화할 수 있다는 뜻입니다. 전체 시스템이 문제예요. 그러니까 CUDA std Atomic Craft는 std가 없는 CUDA Atomic Craft와 사실상 동일하지만 시스템 스코프를 의미합니다. 그래서 문제가 전체 시스템이 아니라면, 네스트 스코프는 기기에 해당합니다. 그러니까 디바이스 스코프란 단일 GPU 내의 모든 스레드를 의미합니다 . 따라서 이 범위에서는 여러 GPU가 동일한 메모리 위치에 원자적 명령을 실행할 수 없으며, CPU와 GPU가 동시에 동일한 메모리 위치에 쓸 수도 없습니다. GPU 내의 모든 스레드가 특정 위치에 자동으로 쓰기를 수행할 수 있으며, 동시에 동일한 GPU 내의 다른 스레드도 동일한 메모리 위치에 쓰기를 수행할 수 있다는 의미입니다. 마지막으로, 스레드 범위는 우리가 스레드 범위 블록이라고 부르는 것으로, 원자 적 연산을 단일 블록 내의 스레드로 제한합니다 . 따라서 여러 스레드가 동일한 스레드 블록 내에 있는 한, 동일한 메모리 위치에 대해 서로 다른 원자적 연산을 수행할 수 있습니다. 하지만 스레드가 다른 스레드 블록에서 오는 경우에는 이 방법이 작동하지 않습니다. 우리 경우에는 각 스레드 블록이 자체 로컬 히스토그램을 업데이트하기 때문에 이 스레드 스쿱 북을 사용하기에 완벽하다는 것을 알 수 있습니다. 자, 그럼 다음 연습 문제로 넘어가겠습니다. 이번에는 스레드 블록 동기화를 사용하여 모든 스레드가 블록 히스토그램 업데이트를 완료한 후에야 해당 히스토그램을 읽고 전역 히스토그램에 쓸 수 있도록 해야 합니다 . 시간이 허락한다면, 스레드 범위를 추가하여 원자적 연산의 효율성을 향상시킬 수도 있습니다. 자, 이 연습을 위해 여러분에게는 8분이 주어집니다. 시간이 다 됐습니다. 그럼 해결책을 살펴보겠습니다. 먼저 CUDA 표준 원자 참조를 CUDA 원자 참조로 바꿉니다. 그런 다음 블록 히스토그램에 대한 원자적 연산 범위를 CUDA 스레드 범위 블록으로 제한합니다. 왜냐하면 각 블록에 고유한 이 히스토그램에 쓸 때 동일한 스레드 블록 내의 서로 다른 스레드 간에만 원자적 협력이 이루어진다는 것을 알고 있기 때문입니다 . 그래서 우리는 이 나사식 SC 블록을 사용할 수 있는 것입니다. 그런 다음 생각 스레드를 추가합니다. 광고 가져오기 작업을 완료한 후 모든 스레드가 전체 히스토그램을 업데이트하기 전에 이 블록 히스토그램 업데이트를 모두 완료했는지 확인하기 위해서입니다 . 따라서 이 전역 히스토그램을 업데이트할 때 기본 시스템 원자 연산을 사용하는 대신, 장치 스코프를 사용할 수 있습니다. 왜냐하면 히스토그램을 장치에서만 기록한다는 것을 알고 있기 때문입니다. 하지만 블록이 아닌 장치를 사용해야 하는 이유는 이 히스토그램이 전역 적이며 모든 블록의 모든 스레드에서 공유되기 때문입니다 . 그래서 우리는 블록이 아니라 스코프 장치를 사용해야 합니다. 이러한 변경만으로 초당 6GB에서 100GB까지 처리량이 크게 향상되었습니다. 이는 원자 연산 의 범위를 변경하는 것만으로도 달성한 엄청난 개선입니다 . 하지만 우리는 훨씬 더 잘할 수 있습니다. 자, 그럼 히스토그램 커널을 더욱 최적화하기 전에 , 지금까지 배운 내용을 다시 한번 살펴보겠습니다. 지금까지 원자 작동, 스레드 스코프, 그리고 배우자에 대해 다뤘습니다. 그런데 우리의 처음 질문은 어떻게 된 거죠? 애초에 왜 스레드 계층 구조가 필요한가라는 질문이 제기되었습니다. 스레드 블록이 왜 필요한가요? 자, 그럼 스레드 블록 동기화와 스레드 블록 범위 원자 연산에 대해 다시 한번 살펴보겠습니다. 이 두 가지는 주어진 스레드 블록 내의 스레드 간에만 사용 가능합니다 . 여러 스레드 그룹 간의 동기화는 훨씬 더 많은 비용이 듭니다. 그래서 스레드 계층 구조를 사용하는 데에는 이점이 있습니다. 블록 내의 스레드만 동기화하는 것이 모든 블록 내의 모든 스레드를 동기화하는 것보다 훨씬 비용이 적게 듭니다 . 그런데 특정 스레드 그룹 내에서 이용 가능한 다른 시설이 있나요 ? 그럼 알아보기 위해 GPU 아키텍처에 대해 좀 이야기해 보겠습니다. GPU는 스트리밍 멀티프로세서( SMS)라고 부르는 균일한 구성 요소로 이루어져 있습니다 . 화면 에서 초록색으로 보이는 부분이 바로 이것입니다 . 따라서 하나의 GPU에는 수백 개의 SM이 포함될 수 있습니다. 그래서 각 SM에는 많은 코어가 있지만, 로컬 N1 캐시도 있습니다. 여기서 각 SM이 자체 L1 캐시를 가지고 있음을 알 수 있습니다. 그래서 메인 글로벌 메모리, 즉 메인 GPU 메모리는 이러한 SM 외부에 위치합니다 . 그래서 그것을 글로벌 메모리라고 부르는 겁니다. 따라서 전역 메모리에 접근하는 것은 L1 캐시에 있는 데이터에 접근하는 것보다 훨씬 더 큰 지연 시간을 발생시킵니다. 왜냐하면 전역 메모리는 SM(소프트웨어 마스터)에 로컬로 저장되기 때문입니다. 그리고 당연히 L2 캐시에 접근하는 것이 전역 메모리에 접근하는 것보다 빠릅니다 . 보시다시피 GPU는 CPU에 비해 대역폭은 매우 좋으면서 지연 시간은 매우 짧습니다. 따라서 L2 및 L1 캐시를 활용하여 지연 시간을 최대한 줄이는 것이 중요합니다. 따라서 스레드 블록 동기화와 같은 일부 기능은 SM 하드웨어에 특화되어 내장되어 있습니다. 음, 그 이유는 스레드 동기화를 다시 수행할 때, 하나의 스레드 블록에 대해서만 동기화가 이루어지기 때문입니다. 우리는 전체 전력망을 동기화하는 것이 아닙니다. 그러니까 SM 레벨에서 실행하기만 하면 되는 겁니다. 그래서 훨씬 빠른 겁니다. 자, 이제 히스토그램 커널이 하드웨어에 어떻게 매핑되는지 생각해 보겠습니다 . 각 스레드는 단일 SM에서 블록을 읽고 블록 히스토그램은 전역 메모리에 저장됩니다. 그래서 우리가 맨 아래에서 볼 수 있는 것이 바로 이것입니다. 그러면 각 스레드는 L2 캐시에 의해 지원되는 개인 블록 히스토그램에 원자적 연산을 실행합니다. 그래서 커널 실행이 완료되면 이 블록 히스토그램을 유지할 필요가 없기 때문에 그다지 효율적이지 않습니다 . 그러니까 L1 캐시가 SM에 바로 설치될 때까지 임시로 저장하는 공간일 뿐이고, 현재는 사용하지 않고 있는 겁니다. 따라서 이 블록 히스토그램을 LM 캐시에 직접 할당할 수 있다면 매우 효율적일 것입니다. 우선 전역 메모리를 낭비하지 않을 뿐 아니라 A1 캐시에서 직접 읽고 쓰기 때문에 속도도 빨라질 것입니다. 다행히 CUDA에서는 이를 구현할 수 있습니다. CUDA는 SH 메모리라고 하는 명시적인 프로그래밍 가능한 스레드 블록 로컬 메모리를 제공합니다 . SH 메모리는 L1 캐시가 있는 위치에 있으며, 따라서 L1 캐시와 유사한 지연 시간 과 대역폭을 가집니다. 그래서 이 SH 메모리의 유일한 단점은 용량이 제한적이라는 것입니다. SM 당 L1 캐시 용량이 제한되어 있기 때문에 SH 메모리를 무한정 사용할 수는 없습니다 . 따라서 공유 메모리를 할당하려면 변수에 shared 지정자를 추가하기만 하면 됩니다. 이 예시에서는 크기가 4인 배열을 선언하는 것뿐입니다. 그래서 sh 메모리에 4개의 정수가 저장됩니다 . 하지만 안타깝게도 메모리에 있는 변수는 복잡한 생성자를 가질 수 없습니다. int, float 등과 같은 기본적인 데이터 타입만 사용할 수 있습니다. 따라서 스레드 블록 내에서 다른 배열과 마찬가지로 해당 배열을 읽고 쓸 수 있습니다 . 이 예시에서는 각 스레드가 자신의 스레드 인덱스를 배열의 해당 요소에 기록합니다. 그러니까 첫 번째 스레드는 1을 쓰고, 제로 스레드는 0번 위치에 0을 쓰고 있는 겁니다. 첫 번째 스레드가 첫 번째 위치에 데이터를 쓰고 있습니다. 따라서 데이터를 잊지 않으려면 해당 메모리에 쓰기 작업을 완료한 후 읽기 작업을 수행하기 전에 반드시 think 스레드를 호출해야 합니다. 이전처럼 전역 메모리에서 작업할 때와 마찬가지입니다. 이 공유 배열이 실제로 모든 스레드 블록에서 공유된다는 것을 강조하기 위해, 여기에서 첫 번째 스레드가 배열의 모든 값을 출력하는 것을 볼 수 있습니다. 그래서 첫 번째 스레드는 이 시점에서 다른 모든 스레드의 업데이트를 보게 되므로 0, 1, 2, 3과 같은 값을 출력합니다. 왜냐하면 우리는 'thing' 스레드를 사용했기 때문입니다 . 그래서 이 스레드는 우리가 여기서 다루는 세 가지와 네 가지 스레드를 완료하도록 강제할 뿐만 아니라 모든 사람이 변경 사항을 볼 수 있도록 합니다. 그래서 세 개의 흰색 부분을 처리하고 동기화 스레드를 완료한 후에는 스레드 0이 볼 수 있게 되고, 결과를 출력했을 때 0, 1, 2, 3이 정확하게 표시되는 것을 보면 볼 수 있다는 것을 알 수 있습니다. 그 이유는 스레드 동기화 기능 덕분에 스레드 블록 내의 모든 스레드가 같은 스레드 블록 내의 다른 스레드가 수행한 모든 수정 사항을 볼 수 있기 때문입니다. 자, 그럼 이제 다음 편으로 넘어가 보겠습니다. 이번에는 블록 히스토그램을 전역 메모리가 아닌 공유 메모리에 할당해야 합니다. 이렇게 하면 코드 성능이 향상될 것으로 기대합니다. 자, 이 연습을 위해 여러분에게 5 분이 주어집니다. 자 , 시간 다 됐습니다. 그럼 이제 해결책을 살펴보겠습니다. 우선 함수 시그니처를 변경해야 합니다. 그래서 이제 블록 스팬 히스토그램이 더 이상 필요하지 않습니다. 왜냐하면 더 이상 전역 메모리를 사용하지 않기 때문입니다. 우리는 공유 메모리를 직접 사용합니다. 따라서 이 공유 메모리를 할당하려면 각 스레드 블록에 로컬인 블록 히스토그램이 필요합니다 . 그래서 우리는 빈의 크기(여기서는 빈 개수)로 할당하는 것입니다. 그리고 히스토그램이 0으로 초기화되어 있는지 확인해야 합니다. 왜냐하면 그 후에 1을 점진적으로 더해 나갈 것이기 때문입니다. 그러므로 0에서 시작해야 합니다 . 그래서 여기서는 스레드를 사용하여 첫 번째, 즉 끝쪽 빈을 0으로 할당하고, 메모리에 쓰기 작업을 수행해야 하므로 동기 스레드를 사용하는 것을 잊지 말아야 합니다. 따라서 메모리에서 읽기 전에 먼저 생각해야 합니다. 이것이 바로 데이터 경쟁이 발생하지 않도록 하기 위한 생각 단계가 필요한 이유입니다. 마지막으로, 이전과 마찬가지로 이 공유 블록 히스토그램을 다시 원자적 참조로 감싸면 나머지 코드는 변경 없이 그대로 사용할 수 있습니다 . 우리는 동일한 가져오기 앱, 동일한 스레드, 모든 것을 동일하게 수행합니다. 다만 이제 전역 메모리에서 수행하는 대신 sh 메모리에서 수행하는 것뿐입니다. 이렇게 작은 변화를 다시 주니 초당 처리량이 100GB에서 400GB까지 증가했습니다. 그러니까 속도가 4배 빨라지는 거죠. 이제 드디어 커널이 제대로 작동하고 성능도 향상되었습니다. 문제는 우리가 더 큰 차원에서 실수를 저질렀다는 겁니다. 우리가 처음 GPU를 사용하기 시작했을 때, 가속 라이브러리에 의존했습니다 . 하지만 지금 우리는 자체 커널을 작성하고 있기 때문에 라이브러리를 사용할 수도 있었음에도 불구하고 모든 것을 처음부터 다시 구현하려고 하고 있습니다. 따라서 CER 커널을 작성한다는 것은 모든 것을 새로 만들어야 한다는 것을 의미하지는 않습니다. 네, 특정 사용 사례가 매우 특수할 수도 있습니다. 그러니까 큐블라스트도, 스러스트도, 큐브도 사용할 수 없다는 거죠. 그래서 여러분에게는 치료용 씨앗이 필요합니다. 하지만 CUD 커널 내부에도 여전히 여러분을 위한 도구들이 있습니다. CUDA는 커널 속도를 향상시키고 개발 시간을 단축하는 데 도움이 되는 다양한 협력 라이브러리를 제공합니다. 예를 들어, Thrust나 Cub 처럼 호스트에서 실행되는 병렬 알고리즘 외에도 디바이스에서 실행되는 버전도 있습니다. 저희는 Qblast DX 및 QFTDX와 같은 도구를 사용하여 장치에서 직접 블래스트 작업이나 변환 작업을 실행할 수 있습니다. 아, 그리고 SC도 마찬가지입니다. 그렇다면 이 경우에 '협력적'이라는 말은 정확히 무엇을 의미하는 걸까요? 우리는 왜 이것을 협동 도서관이라고 부를까요? 그래서 우리가 협력적이라고 ​​말하는 이유는 옥수수에 적용하는 알고리즘이 지금까지 여러 종류가 있었기 때문입니다. 정렬을 예로 들어보겠습니다. 그러니까 도시 정렬이라고 하면, 그건 직렬 알고리즘을 말하는 겁니다. 따라서 직렬 알고리즘은 단일 스레드에 의해 호출되고 실행됩니다. 따라서 서로 다른 두 스레드가 각각 직렬 알고리즘을 호출하는 경우 , 한 스레드의 입력은 다른 스레드의 출력에 영향을 미치지 않습니다. 반면 협력 알고리즘은 여러 스레드에 의해 호출되고 모든 스레드에 의해 동시에 실행됩니다 . 따라서 이 협력 알고리즘의 입력은 여러 스레드에 분산되어 있는 하나의 큰 가상 배열이라고 생각할 수 있습니다 . 출력 결과도 마찬가지입니다. 슬라이드의 예시에서 협력적 정렬은 첫 번째 스레드에서 1번째 요소(세 번째 요소)를 가져오고 두 번째 스레드에서 2번째 요소(네 번째 요소)를 가져옵니다. 그래서 다시 협력적으로 정렬한 후 첫 번째 스레드의 출력은 12가 됩니다. 왜냐하면 방금 배열을 정렬했기 때문입니다. 그리고 두 번째 스레드의 출력은 34가 됩니다 . 이 2라는 값은 원래 첫 번째 스레드에서 나온 것이지만요. 그래서 우리는 그것을 협력적이라고 ​​말하는 겁니다. 입력과 출력은 모든 스레드에서 공유됩니다 . 그래서 우리는 단일 스레드에서 호출되지만 백그라운드에서 여러 스레드에 의해 실행되는 병렬 알고리즘도 사용했습니다 . 사용자 관점에서 보면 이 알고리즘은 직렬로 실행되는 것처럼 보입니다. 왜냐하면 다른 함수처럼 호출하기 때문입니다. 하지만 내부적으로는 작업을 더 빠르게 처리하기 위해 여러 스레드에 분산되어 실행됩니다. 지금까지 사용해 온 병렬 알고리즘의 예로는 프레스 변환이나 웹 장치 변환 등이 있습니다. 자, 그럼 이제 협력 알고리즘을 좀 더 자세히 살펴보겠습니다. 이 슬라이드에서 협력 적 감소가 어떻게 작동하는지 확인할 수 있습니다. 따라서 여기서는 모든 스레드가 협력적 축소에 단일 값을 제공합니다 . 따라서 첫 번째 스레드는 0이라는 값을, 두 번째 스레드는 1이라는 값을 생성하는 식입니다. 그리고 이러한 값들은 협력 알고리즘 내부에서 sh 메모리에 기록됩니다. 그리고 스레드 블록 동기화 후에, 더 적은 수의 스레드가 부분합을 계산하고 그 결과를 다시 출력합니다. 그리고 한 번 더 동기화가 완료되면 총합, 즉 예시에서는 6이 첫 번째 스레드에 다시 기록됩니다. 또한, 이것은 매우 기본적인 모델이지만 기업 알고리즘에 대한 두 가지 핵심 사항을 보여줍니다. 이들은 스레드 간 데이터 교환을 위해 sh 메모리에 의존하며 동기화 기능을 포함할 수도 있습니다. 어, 여기서 우리는 축소 작업을 수행하기 위한 두 개의 생각 스레드가 있는 것을 볼 수 있습니다 . 이는 블록 내의 어떤 스레드라도 협력 알고리즘을 호출하지 못하면 전체 커널에 문제가 발생할 수 있음을 의미합니다. 이 협력 알고리즘을 호출할 때 모든 스레드가 포함되어야 합니다. 자, 그럼 이제 이 직관이 실제 코드에 어떻게 적용되는지 살펴보겠습니다. 화면에서 보시는 것처럼 큐브가 제공하는 협력적 감소가 나타납니다. 그래서 기존의 기능 중심 인터페이스처럼 cub는 협력 알고리즘을 템플릿 구조로 제공합니다. 따라서 템플릿 매개변수는 당면한 문제에 맞게 알고리즘을 특화하는 데 사용됩니다. 예를 들어, 여기서는 특정 유형(여기서는 정수)을 줄이고 싶다고 지정할 수 있으며, 블록 너비를 256 스레드로 지정해야 합니다. 어, 그래서 큐브 협력 알고리즘은 중첩된 임시 저장소 유형을 가지고 있는데, 이는 협력 알고리즘이 내부 통신을 수행하는 데 필요한 임시 저장소의 유형과 양을 지정합니다. 그래서 우리는 이 유형의 인스턴스를 공유 메모리에 할당합니다 . 그런 다음 협력 알고리즘을 구성할 때 해당 공유 메모리 인스턴스에 대한 참조를 전달합니다 . 마지막으로, 멤버 함수는 특정 협력 알고리즘의 다양한 변형을 제공합니다 . 이 모든 것을 종합해 보면 협력 알고리즘의 사용법은 다음과 같습니다 . 먼저 정수 값을 줄이겠다고 지정하고 스레드 블록 크기를 4로 설정하여 블록 축소를 인스턴스화합니다. 그런 다음 공유 메모리에 임시 저장 공간을 할당합니다 . 다음으로, 이러한 협력적 u 알고리즘 구조의 인스턴스를 구성합니다. 그리고 마지막으로 이 객체를 사용할 수 있게 되면 블록 내의 모든 스레드에서 협력 알고리즘을 호출할 수 있습니다 . 그래서 CUPS는 스레드 블록 수준에서 다양한 범용 알고리즘을 제공합니다 . 보시다시피, 저희는 교환 히스토그램, 로드 MER 스캔, 셔플 등을 가지고 있습니다. 그리고 여기에서 필요한 모든 것을 찾을 수 있습니다. 블록 히스토그램을 직접 계산하여 최적화가 제대로 되지 않은 코드를 생성하는 대신 , cap 블록 히스토그램을 사용할 수 있습니다. 따라서 이 블록 히스토그램 알고리즘에는 몇 가지 템플릿 매개변수가 있습니다. 우선 히스토그램의 유형을 지정해야 합니다 . 자, 여기 우리 경우에는 in입니다 . 그런 다음 스레드 블록 크기를 지정해야 합니다. 그래서 우리 경우에는 256개입니다. 그런 다음 각 스레드가 몇 개의 빈을 생성할지 지정해야 합니다 . 그리고 마지막으로 히스토그램에 콩이 몇 개 있는지 지정해야 합니다. 따라서 이 협력적 기본 요소의 사용법은 방금 살펴본 축소 사례와 유사합니다. 자, 그럼 이 섹션의 마지막 연습 문제로 넘어가겠습니다. 음, 이번에는 히스토그램 커널에서 직접 구현하는 대신 Cub 블록 히스토그램을 사용해야 합니다 . 자, 이번 연습에는 8분이 주어집니다. 자, 시간 다 됐습니다. 그럼 해결책을 살펴보겠습니다. 먼저 이전과 마찬가지로 그리드에서 셀을 불러옵니다 . 이것은 이전과 마찬가지이며, 이번에는 SAP 스팬 대신 C 언어와 유사한 배열을 사용하여 스택에 로컬로 빈을 할당합니다 . 우리가 여기서 하고 있는 일이 바로 그겁니다. 그런 다음 블록 히스토그램 유형을 인스턴스화하고 공유 메모리에 임시 저장소를 할당합니다. 그런 다음 히스토그램 메서드를 호출하여 히스토그램을 생성할 수 있습니다. 그리고 마지막으로, 메모리에서 읽기나 쓰기 작업을 하기 전에 동기화를 해야 한다는 점을 잊지 말아야 합니다. 자, 그럼 지금까지 이 단원에서 배운 모든 내용을 정리해 보겠습니다. 때때로 우리는 Blast, UFT 또는 Thrust와 같은 기존 라이브러리에서 제공하지 않는 기능을 구현하기 위해 사용자 지정 CER 커널을 작성해야 할 수도 있습니다. 이렇게 할 때는 스레드 간에 장벽을 설정하여 모든 스레드가 특정 데이터에 대한 수정 작업을 완료한 후에야 다른 스레드가 해당 데이터에 대한 수정 작업을 수행할 수 있도록 스레드 간 경계를 설정하는 것이 적절한 경우에 스레드 사고 방식을 사용하는 데 주의해야 합니다. 이는 우리가 흔히 데이터 경쟁이라고 부르는 현상을 방지하기 위한 것입니다. 음, 우리는 데이터 경쟁을 발생시키지 않고 스레드 간 통신을 위해 원자성을 사용할 수 있습니다 . 그리고 원자 연산을 사용하더라도 블록 내 스레드 간에 연결을 설정하기 위해 여전히 스레드를 사용해야 하며, 메모리에 대해서도 이야기했습니다. 따라서 스레드 블록 내에서 임시 저장소가 필요하거나 스레드 블록 내에서 스레드 간에 데이터를 교환해야 할 때, 캐시가 있는 위치에 있는 셸 메모리를 사용할 수 있습니다. 이는 전역 메모리의 L2 캐시에 의존하는 것보다 빠릅니다. 또한 커널을 개발할 때 장치 측에서 직접 사용할 수 있는 라이브러리가 있으므로 이러한 기능을 활용할 수 있습니다. 그래서 우리는 블록 수준, 즉 블록 히스토그램이나 블록 리 듀스에서 보았듯이 블록 수준에서 작동하는 협력 알고리즘을 제공하는 CUB를 가지고 있습니다. 저희는 선형 대수를 위한 qlblast dx나 변환을 위한 qfdx와 같은 더 구체적인 라이브러리도 제공합니다. 따라서 절대 추측하지 마시고 항상 성능을 측정하여 속도 저하의 원인이 무엇인지 확인해야 합니다. 또한 코드가 제대로 작동하는지 여부에 대해서도 추측하지 마세요. compute sanitizer와 같은 도구를 사용하여 자동 접근이 발생하지 않는지 확인하고, cudgd와 같은 도구를 사용하여 코드를 디버깅하거나 Inside System을 사용하여 전체 애플리케이션을 시각화하고 프로파일링할 수 있습니다 . 그럼 이제 오늘 배운 모든 내용을 복습해 봅시다. 그래서 저희는 실행 공간이 무엇인지 알아냈고, 간단한 냉각 시뮬레이터를 작성했습니다. 그런 다음 우리는 고급 이터레이터를 사용하여 속도를 높이고 불필요한 구체화를 방지하며 모든 작업을 단일 호출로 처리하는 방법을 살펴보았습니다. 음, 우리는 쿠다 프로그래밍 경험을 더 쉽게 만들어 줄 수 있는 풍부한 어휘 유형에 대해 배웁니다. 또한, 우리는 통합 문제를 해결하고 적절한 알고리즘을 선택 하고 적절한 스레드 수를 사용하여 이를 피하는 방법을 배웁니다. 또한 메모리 공간에 대해서도 알아보고, 유니버설 메모리를 사용할 때 호스트와 장치 간의 메모리 전송에 시간이 걸릴 수 있으므로 성능 향상을 위해 메모리를 직접 분리하는 것이 더 낫다는 점도 알게 되었습니다 . 또한 우리는 동기화를 발견했고 CPU에서 계산과 입출력 작업을 동시에 수행하는 방법을 배웠습니다. 또한, 귀여운 스트림이 프로그램의 동기화를 제어하고 계산 및 메모리 전송을 중첩할 수 있도록 하는 방법을 살펴보았습니다. 그리고 우리는 내부 구조를 살펴보고 가상 메모리가 어떻게 작동하는지, 그리고 페이징 메모리와 핀 메모리가 어떻게 존재하는지 알아냈습니다 . 그리고 우리는 핀 메모리를 사용하면 호스트에서 장치로의 메모리 전송을 더 빠르고 비동기적으로 수행할 수 있다는 것을 확인했습니다. 결론적으로, 이 부분을 통해 우리는 자신 만의 CUDA 알고리즘을 작성하는 방법을 배웠습니다 . 그래서 우리는 데이터 경쟁을 방지하기 위해 원자적 연산과 동기화 속성 기본 요소가 필요하다는 것을 알게 되었고, 동일한 메모리 위치에서 많은 원자적 연산을 수행할 때 프라이빗 프라이버시화를 사용하면 경합을 줄일 수 있다는 것을 확인했습니다. 어, 그리고 이러한 개인 정보 보호를 통해 성능을 향상시키기 위해, 하지만 이는 또한 올바른 위치 에서 일부 동기화 작업을 수행해야 한다는 것을 의미했습니다 . 또한, 전역 메모리 대신 공유 메모리를 사용하여 블록 단위 데이터를 더 빠르게 저장하는 방법을 발견했습니다 . 그리고 마지막으로, cub과 같은 협력 알고리즘을 커널 내부에 직접 사용하여 속도를 더욱 높이고 모든 것을 처음부터 구현할 필요가 없도록 하는 방법을 살펴보았습니다 . 자, 이제 시간이 되시면 최종 평가를 진행해 주세요. 이번 연습에서는 전자기학에 대한 맥스웰 방정식 시뮬레이터 코드가 제공됩니다. 그래서 여러분의 임무는 이 CPU 코드를 GPU로 이식하는 것입니다 . 그래서 이 과정을 세 단계로 나누어 진행하시면 됩니다. 그래서 첫 번째 단계에서는 Thrust 컨테이너를 사용하여 ST 벡터에서 명시적 메모리 공간으로 데이터를 포팅합니다 . 또한 올바른 실행 공간 지정자를 추가하고 GPU 가속 병렬 알고리즘을 사용해야 합니다. 음, 그러니까 표준 변환 대신 추력을 사용하세요. 그다음 2단계에서는 불필요한 구체화를 방지하기 위해 고급 이터레이터를 사용합니다 . 따라서 예를 들어 transform과 reduce를 호출하는 것을 피하고, 더 이상 필요하지 않은 임시 메모리 할당도 제거할 수 있습니다. 왜냐하면 우리는 멋진 이터레이터를 사용했기 때문입니다. 마지막으로 3단계에서는 큐브 블록 리듀스를 사용하여 스레드 블록에서 평균 온도를 계산하고 그리드를 최적화하여 입출력 트래픽을 줄입니다. 이것으로 오늘 수업은 모두 끝났습니다. 이번 세션을 통해 많은 것을 배우셨기를 바라며, CUDA 프로그래밍 도구와 C++ CUDA 프로그래밍 시 GPU를 올바르게 사용하는 방법에 대한 새로운 지식을 많이 활용하시기를 바랍니다. 그리고 혹시라도 의견이 있으시면 TLI의 피드백 질문란에 주저 없이 남겨주세요.
