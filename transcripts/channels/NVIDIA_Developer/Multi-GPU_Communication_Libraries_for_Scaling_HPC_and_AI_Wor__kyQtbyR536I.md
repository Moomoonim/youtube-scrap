# Multi-GPU Communication Libraries for Scaling HPC and AI Workloads | NVIDIA GTC 2025

- 영상 링크: https://www.youtube.com/watch?v=kyQtbyR536I
- 채널: NVIDIA Developer
- 업로드일: 2025-04-22
- 자막 언어: ko
- 단어 수: 약 4242개

---

## 스크립트

[박수] 네. 응. 소개해주셔서 감사합니다. 그리고 환영합니다. 음, 오늘 오전에 있었던 강연 중 하나에서 나왔던 동기 부여 내용을 다시 한번 말씀드리겠습니다. 여러분 모두 아마 알고 계실 겁니다. 왜 여러 개의 GPU를 사용해야 할까요? 네, 때로는 날씨 예측처럼 서비스 품질 요구 사항을 충족하기 위해 더 빠른 계산이 필요할 때가 있습니다. 날씨 예측의 경우 실시간보다 빨라야 미래를 예측할 수 있기 때문이죠. 또는 더 크고 해상도가 높은 CFD 모델이나 LLN 모델을 계산해야 할 때도 그렇습니다 . 매트가 오늘 아침에 보여준 것처럼 데이터가 폭발적으로 증가할 겁니다. 그래서 실제로 데이터를 제대로 활용하려면 멀티 지블 GPU가 필요합니다 . 다시 말씀드리지만, 이것은 3부작 시리즈의 일부이며, 멀티 GPU 프로그래밍 환경을 반과학적인 축에 놓고 일반성을 x축으로 하여 제시하는 것입니다 . 그렇다면 얼마나 특정 분야에 특화되어 있는 걸까요 ? 알고리즘을 얼마나 일반적인 방식으로 표현할 수 있을까요? 그리고 y축에는 생산성을 나타낼 수 있을까요? 아마 이 점들을 정확히 어디에 찍어야 할지에 대해 많은 논쟁이 있을 수 있을 거예요. 음, 누군가는 항상 반대하고 누군가는 항상 동의하는 방식을 찾았다고 생각해요. 그래서 오늘 아침 8시에 Matt가 대부분의 작업을 추상화하여 생산성을 극대화할 수 있도록 해주는 고수준 라이브러리에 대한 소개를 했습니다. 또한, 작업이 즉시 대기 가능한 경우 매우 뛰어난 성능을 발휘할 수 있습니다. 음, 바로 직전에 마놀루스가 매트가 이전에 소개했던 상위 수준 런타임을 구축하는 데 사용되는 작업 기반 런타임에 대해 이야기했고, 이제 생산성 측면에서는 아래로, 일반성 측면에서는 위로 내려가 통신 라이브러리를 사용한 고급 멀티GPU 확장에 대해 이야기하겠습니다 . 그렇다면 이 모든 것이 실제로 여러 GPU를 프로그래밍할 때 필요한 데이터 이동을 수행하는 데 사용하는 것은 무엇일까요 ? 네, 그래서 제가 이 대화가 무엇에 관한 것인지 물어봤죠. 여러 개의 GPU가 하나의 문제를 해결하기 위해 협력적으로 작업하는 경우, 동기화 상태를 유지하고 동일한 문제를 효율적으로 해결하기 위해서는 데이터 교환이 필수적입니다. 음, 그래서 이 글은 바로 그런 내용입니다. 니켈, MPI, 그리고 비쉬미는 어떻게 하면 GPU 통신을 통해 효율적으로 조율하고 구성하는 데 도움을 줄 수 있을까요 ? 여기 있는 모든 모델은 CUDA를 지원합니다. 즉, 화살표에서 볼 수 있듯이 GPU 메모리와 직접 상호 작용할 수 있습니다 . 그러니까 가장 효율적인 데이터 경로를 통해 GPU 메모리에서 GPU 메모리로 데이터를 직접 이동하는 겁니다 . NPI를 사용한 고수준 API의 양방향 통신 부분을 살펴보면, 데이터를 가진 NPI(이 경우 0)가 GPU 메모리의 배열에 대해 NPI send를 호출하고, 데이터를 수신하는 GPU는 Nickel을 사용하여 receive를 호출합니다. 이 또한 매우 유사해 보입니다. 즉, 양방향 송수신 통신 패턴을 가지고 있습니다. 여기서 중요한 차이점은 stream 인수가 마지막에 나타난다는 점입니다. 따라서 이는 GPU 스트림과 관련하여 GPU에 대해 차단되는 작업입니다. 따라서 스트림 내에서 실행되지만 호스트에 대해서는 비차단 방식으로 실행됩니다. 이러한 호출이 처리된 직후 CPU3는 다른 작업을 수행하기 위해 더 많은 커널을 스케줄링하는 등의 작업을 진행합니다. 마지막으로, nvishme는 니켈만큼 간단하지는 않지만 호스트 API입니다. 나중에 디바이스 API에 대해서도 설명하겠지만, 이것 역시 스트림 기능을 가지고 있어 CPU에 대해 비차단 방식으로 작동합니다. 또한, 보시다시피 파트가 하나뿐인 단방향 통신 모델입니다. 나중에 더 자세히 설명하겠지만, 이는 대칭 힙을 통해 구현됩니다. 자, 이 모든 프로그래밍 모델의 공통점은 무엇일까요? 바로 멀티 노드 멀티 GPU 프로그래밍 모델이기 때문에 실행하는 데 약간의 도움이 필요하다는 것입니다 . 그러니까 실행 파일을 실행하기만 하면 되고, 실행 파일 안에 GPU당 하나의 프로세스가 실행되도록 모든 것을 담을 수 있는 일반적인 Mele 방식과는 다르다는 거죠. 그래서 일반적으로 런처를 사용하는데, 슬럼 클러스터의 경우 보통 SR Run을 사용하지만, 프로그래밍 모델에도 NPI Run이나 NVISHIM NISHME Run과 같은 런처가 함께 제공되는 경우가 많습니다. 음, 여기에는 프로세스를 리소스에 정확하게 매핑할 수 있는 다양한 옵션이 있지만, 저는 가장 중요한 옵션만 보여드리겠습니다. 인스턴스가 몇 개 있나요 ? 따라서 런처 프로를 사용하려면 앱에 인수를 제공해야 합니다. 이 라이브러리는 각 GPU에 대해 애플리케이션 인스턴스를 하나씩 배포하고, 초기화가 완료되면 NPI의 경우 순위를, 니켈 또는 처리 요소(PE) ID를 0부터 프로세스 수에서 1을 뺀 값까지의 값으로 반환합니다. 이를 통해 프로세스는 안전하게 자신을 식별하고, 다른 프로세스의 수를 파악한 후, 이를 사용하여 통신을 조율할 수 있습니다. 자, 그럼 첫 번째 프로그래밍 모델인 MPI( 메시지 전달 인터페이스)부터 시작해 보겠습니다. 이는 프로세스 간 데이터 교환을 위한 오랜 표준으로, 메시지를 통해 데이터를 주고받으며, 이를 위해 지점 간 통신을 위한 API를 정의합니다 . 여러분은 이미 NPI를 송수신하는 것을 보셨을 뿐 아니라, 다양한 집단들이 모든 것을 축소하는 등의 기능도 보셨습니다. 여러분이 관심 있는 거의 모든 언어에 대한 바인딩을 제공하는 다양한 애플리케이션 구현체가 있습니다. 음, 오픈 소스 와 상용 제품 몇 가지만 여기에 나열해 보겠습니다. 음, 잠시 기초 기술로 돌아가서, 모든 라이브러리를 활성화하거나, 기능을 구현하거나, 기능을 가속화하는 데 필요한 기술에 대해 이야기해 보겠습니다. CUDA MPI의 가장 중요하고 기본적인 구성 요소는 통합 가상 주소 지정이며, 이는 우리가 오래전에 컴퓨팅 기능 2.0과 함께 도입한 것입니다. 0번 항목은 CUDA 프로그램에서 메모리를 할당할 때 포인터가 스튜디오 가상 주소 지정( 통합 메모리 범위와 혼동하지 마세요)의 일부라는 것을 의미합니다. 포인터의 가상 주소를 통해 물리적 위치를 식별할 수 있으며, CUDA 드라이버에 해당 포인터가 어디에 있는지 쿼리할 수 있습니다. 이를 통해 NPI와 같은 라이브러리가 CUDA를 인식할 수 있게 됩니다. 포인터를 전달하기만 하면 라이브러리가 해당 포인터가 CPU 포인터인지, GPU 포인터인지, 어떤 GPU에 있는지 쿼리하여 데이터를 효율적으로 이동하기 위한 적절한 조치를 취할 수 있기 때문입니다 . 그러니까 그게 바로 핵심 기술입니다. 하지만 제가 언급하고 싶은 다른 두 가지가 있습니다 . 어, 하나는 GPU Direct 제품군의 일부입니다. 하나는 P2P 전송용으로, PC Express 또는 (사용 가능한 경우) NV 링크를 통해 동일 노드 내의 한 GPU에서 다른 GPU로 데이터를 직접 이동할 수 있도록 합니다. NV 링크는 아마 이전에 들어보셨을 겁니다. Envy Link는 멀티오드 기능도 지원합니다. 따라서 GPU 간에 고대역폭 앰뷸링크 연결을 사용할 수 있고 CPU 메모리 등의 중간 단계에서 발생하는 병목 현상을 피할 수 있으므로 훨씬 더 높은 대역폭을 제공합니다 . 또한 GP는 RDMA를 지원하여 타사 PC Express 장치가 GPU 메모리에서 직접 읽고 쓸 수 있도록 합니다. 따라서 인피니티브 네트워크 어댑터의 경우 CPU 메모리에서 데이터를 직접 읽어 네트워크를 통해 목적지로 전송할 수 있으므로 성능 저하를 유발할 수 있는 중간 단계를 생략할 수 있습니다. 그럼 CUDA MPI를 사용한 아주 간단한 예제를 통해 이것이 어떻게 실제로 구현되는지 살펴보고, 성능을 비교하고 더 잘 이해할 수 있도록 하겠습니다. 음, 저는 NPI 랭크 0이 GPU 버퍼에서 MPI 전송을 수행하는 것을 보고 있습니다 . NPI 랭크 1은 GPU 버퍼로 전송되는 NPI 수신 매칭을 수행하여 CUDA와 NPI가 원칙적으로 어떻게 작동하는지 보여줍니다 . 이는 두 개의 GPU와 두 개의 노드를 살펴보는 것입니다. 따라서 MPI 구현 방식, 메시지 크기, 시스템 설정에 따라 다른 결과가 나타날 수 있습니다. 하지만 저는 이것이 CUDA MPI와 같은 통신 라이브러리 내에서 발생할 수 있는 잠재적인 문제와 성능에 대해 추론하는 데 여전히 매우 유용한 사고 모델이라고 생각합니다 . 자, 그럼 먼저 최상의 경우부터 살펴보죠. 저희는 GPA, RDMA 및 이를 지원하는 네트워크를 갖추고 있습니다. 우리는 송수신 호출에 장치 버퍼를 전달하기만 하면 라이브러리가 P Express 네트워크를 통해 데이터를 직접 전송하는 등의 작업을 수행합니다 . 또한 시스템 없이 실행하고 프로파일링을 통해 실제로 무슨 일이 일어나고 있는지 확인해 보면, 여기서 가장 중요한 부분은 바로 보이지 않는 부분이라는 것을 알 수 있습니다. GPU 활동이 전혀 보이지 않습니다. CUDA API 호출이 전혀 표시되지 않습니다. 그 이유는 모든 전송이 네트워크 어댑터에 의해 처리되기 때문입니다 . 하지만 보시는 바와 같이, 바로 그 이유 때문에 제가 GPU 행렬 샘플링을 활성화한 것입니다. PC 익스프레스 버스에서 활동이 감지됩니다 . 이는 네트워크 어댑터가 GPU 메모리에서 데이터를 읽거나 GPU에 데이터를 쓰는 것을 의미하며, 데이터 수신 또는 전송 여부에 따라 달라집니다 . 그래서 아주 효율적입니다. 만약 노드 토폴로지 시스템 설정 문제 등으로 GP를 사용할 수 없는 경우에도 디바이스 포인터를 사용할 수 있으며, MPI가 필요한 스테이징 작업을 처리해 줍니다. 이 과정에서 CPU에 두 개의 버퍼가 사용될 수 있습니다. 하지만 이 모든 것이 파이프라인 방식으로 잘 처리될 것이며, 내부 시스템 타임라인에서도 볼 수 있듯이 디바이스에서 호스트로의 전송이 여러 덩어리로 나뉘어 실행됩니다. 실제로 네트워크 전송도 파이프라인 방식으로 실행되므로 정확한 제약 조건에 따라 적절하거나 우수한 성능을 얻을 수 있습니다. 그렇다면 CUDA MPI가 없는 경우에는 어떻게 해야 할까요? 먼저 CUDA me copy device 명령어를 호출하여 장치를 호스트로 복사하고, 완료될 때까지 기다린 후, CPU MPI를 호출하여 완료될 때까지 기다려야 합니다. 마지막으로 cudm copy 명령어를 다시 호출하여 장치를 호스트에서 장치로 복사해야 합니다. 음, 이 방식은 단계가 훨씬 더 많다는 것 외에도, CUDM 복사 및 NPI 이후에 파이프라인이 중단되는 큰 단점이 있습니다. Insight Systems를 살펴보면 알 수 있듯이, 한 번의 대규모 복사 작업이 수행된 후 이를 기다려야 하며, 네트워크 복사와 호스트-장치 복사 사이에는 전혀 중복되는 부분이 없습니다 . 네 , 방금 살펴본 OOU 벤치마크를 예로 들어보겠습니다. 과학자들이 비교를 위해 사용할 수 있는 시스템인 ULIC의 Jules Booster를 사용하여 기본 크기 범위로 벤치마크를 실행하고 CUDA MPI와 RDMA 칩을 비교했습니다( 녹색 선). 보시다시피, 이로 인해 HDR 200 인피니밴드 네트워크의 대역폭이 포화 상태가 됩니다. Kudo MPI를 사용하면 성능이 약간 떨어지지만 여전히 괜찮습니다. 무대 연출은 사실상 공연에 악영향을 미칩니다. 그러니까 Kudo MPI를 사용하려면 시스템이 올바르게 구성되어 있어야 한다는 말씀이시죠? 만약 우리가 노드 내부에서 NVLink를 사용할 수 있다면, PC Express와 NVLink 간의 성능 차이가 훨씬 커지기 때문에 차이가 더욱 두드러집니다. PC Express는 네트워크 대역폭에 맞춰 작동 하지만 NVLink는 대역폭에 맞춰 작동하지 않기 때문에 NVLink가 GP 생산에 더 큰 이점을 제공합니다 . 좋아요. 그렇다면 이것이 실제 애플리케이션 성능에도 영향을 미치나요? 네, 그렇습니다. 음, 제가 여기서 가져온 것은 오디케이션 사용 사례 아이콘입니다. 이것은 ICO IO 비정수압 모델로, 기후 및 기후 시뮬레이션에서 수치 기상 예측에 사용됩니다. 이 시스템은 독일과 스위스에서 개발되었으며, 두 나라의 기상 관측소에서 운영되고 있을 뿐 아니라 여러 다른 분야에서도 사용되고 있습니다. 이 벤치마크에서는 일반적인 설정인 수평 해상도 10km, 수직 모서리 190개를 사용하는 입방형 워크로드를 사용합니다. 스크린샷에서 볼 수 있듯이 CPU MPI와 비교했을 때 CUDA MPI4 아이콘은 파이프라인 처리되지 않은 전송을 방지합니다. 여기서는 CPU MPI 활동과 호스트에서 장치로의 데이터 복사를 볼 수 있고, 그 후 GPU 활동이 전혀 없는 긴 공백이 나타납니다. 음, 그리고 NPI가 완료된 후 호스트에서 장치로 다시 복사가 이루어집니다 . 그리고 이 경우 CPU 버전을 살펴보면, 단일 노드에서 실행되는 프로필을 통해 피어 투 피어 전송을 확인할 수 있습니다. 호스트에서 장치로의 복사가 누락된 것을 볼 수 있고, 피어 투 피어 전송이 이루어지는 것을 확인할 수 있습니다. 또한 하이랜드에서 볼 수 있듯이 전체 타임라인이 훨씬 더 촘촘하고 간격이 훨씬 적어 실행 효율성이 향상되었습니다. 이 두 가지를 비교하는 확장성 실험을 진행해 보면 , 실제 프로덕션 환경에서 중요한 규모인 약 8~12개 노드에서 1.4배의 속도 향상을 확인할 수 있습니다. 그리고 저희는 규모 확장을 위한 학술적 연구도 진행했습니다. 그래서 32개 노드에서는 H100 GPU와 NDR400 네트워크를 갖춘 DJX 슈퍼컴퓨터에서 EOS 시스템의 경우 1.2배, 1.6배, 6배의 성능 향상을 확인할 수 있습니다 . 좋아요, 이제 화제를 바꿔서 엔비디아 집단 커뮤니케이션 라이브러리의 작은 부분을 살펴보죠. GPU와의 효율적인 통신을 위한 라이브러리입니다. 예를 들어, 2.8 버전 부터는 데이터 병렬 딥러닝에 필요한 모든 축소 기능을 비롯한 집합 연산부터 시작했습니다 . 또한 GPU 간 전송 영수증을 지원하므로 NPI와 비교하여 모든 GPU 간 자동 전송 등을 포함한 임의의 통신 패턴을 효과적으로 표현할 수 있습니다. 이 라이브러리는 GPU에서 실행되며 모든 GPU가 접근 가능한 데이터에 액세스할 수 있습니다. 그래서 통신 호출은 제가 이미 말씀드린 것처럼 스트림에서 실행되는 GPU 커널로 변환됩니다. 따라서 해당 호출은 제출된 GPU 스트림에 대해서는 블로킹되지만 호스트에 대해서는 비블로킹됩니다. 이는 일부 CPU-GPU 통신을 방지하고, 특히 집합 연산의 경우 컴퓨팅 처리량을 활용하여 집합 연산을 최대 속도로 실행할 수 있도록 해줍니다. 니켈에 더 관심이 있으시다면, 제 동료 실버 저거의 발표 녹화본을 시청하시는 것을 강력히 추천합니다. 그는 월요일에 니켈의 최신 소식에 대해 발표했는데, 슬라이드 중 하나에 최신 버전의 니켈로 세대별로 달성한 전체 통신 대역폭을 보여주는 자료가 있습니다. 보시 다시피, 특히 NVLink와 SRAP를 사용하면 노드 간 멀티 GPU 및 멀티 노드 모두에서 매우 높은 대역폭을 달성할 수 있습니다. 따라서 높은 집단 성능과 동기화를 피할 수 있다는 이점 외에도 여러 장점이 있습니다. 소스 코드를 통해 이를 보여주고 싶었습니다. 마지막 슬라이드에 더 큰 링크를 넣어두겠습니다 . 혹시라도 슬라이드 맨 아래쪽을 보실까 봐 말씀드리는데, 음, 이건 GitHub에서 볼 수 있는 예제 코드를 좀 더 간소화한 버전이에요. 왼쪽에는 MPI 버전이 있는데, 여기서는 컴퓨팅 커널을 동기화해야 합니다. 통신을 시작하기 전에 컴퓨팅 커널이 생성한 데이터에 대한 통신을 시작해야 하기 때문입니다. 송수신 측면에서 니켈과 매우 유사해 보이지만, CUDA 스트림 동기화는 생략할 수 있습니다. 통신이 컴퓨팅 커널과 동일한 스트림으로 제출되므로 컴퓨팅 커널이 작업을 완료하기 전에 시작되지 않고, 작업을 큐에 추가할 수 있기 때문입니다. CPU는 다른 작업을 수행할 수 있으므로 GPU에 작업을 더 적극적으로 스케줄링할 수 있고, 타임 라인을 전반적으로 더 촘촘하게 만들 수 있습니다. 여기서는 니켈을 활용한 사용 사례도 보여드리겠습니다. 물론이죠. 컨퍼런스에 참석해 보면 거의 모든 딥러닝 애플리케이션에서 니켈을 사용하고 있습니다. 그래서 저는 이 청중들에게 잠재적으로 새로운 것을 소개하고 싶었습니다. 바로 VASP, 즉 비엔나 아피니오 시뮬레이션 패키지입니다. VASP는 분자 역학의 제1원리 기반의 AIO 계산을 수행하는 매우 다재다능한 패키지로, 예를 들어 재료 연구에 사용됩니다. 처음에는 Kudo MPI를 사용했지만 나중에 니켈 통신 옵션이 추가되었고, 이를 통해 여러 가지 기능을 활용할 수 있게 되었습니다. 한 가지는 CUDA 스트림 인식 인터페이스 덕분에 GPU 가속 콜렉티브 연산으로 성능이 향상되었다는 점입니다. 이는 CPU와 GPU 간의 동기화를 최소화하고 , 그 결과로 비동기 CPU 실행을 통해 통신 및 연산 중복을 더 쉽게 표현할 수 있게 해줍니다. 따라서 니켈 버전에서는 통신 연산 중첩이 구현되었지만 NPI 버전에서는 구현되지 않았습니다. 제가 지금부터 말씀드릴 비교는 100% 공정한 비교는 아닙니다. MPI는 그곳에서 작업이 제대로 이루어진다면 더 나아질 수 있을 것입니다 . 하지만 이는 적어도 이 코드에서는 작업을 쉽게 만드는 과정에서 발생하는 부작용입니다. 여기서 저는 비교하고 있는데, 왼쪽에는 CUDAPI가 있는 것을 보실 수 있습니다. 빈 공간이 많고 모든 것이 순차적으로 배열되어 있는 것을 볼 수 있습니다. 그리고 오른쪽에는 니켈 버전이 보이는데, 거기에 알갱이가 두 줄로 배열되어 있습니다. 따라서 통신 니켈 커널은 컴퓨팅 커널과 겹칩니다. GPU는 항상 바쁘게 작동합니다. 제가 여기서 강조하고 싶은 것은 바로 축소된 커널입니다. 그래서 왼쪽에 공백이 있는 이유는 CPU에서 축소 연산이 수행되고 효율적인 니켈 커널을 사용하지 않기 때문입니다. 216개의 원자를 가진 H와 헤프늄 시뮬레이션을 예로 들어 EOS에서 노드 수를 4개에서 32개로 늘리면, CUDAPI 대신 니켈을 사용함으로써 최대 2.6배의 속도 향상을 볼 수 있습니다. 맨 아래에는 종료 사용 사례 및 구현 방법에 대한 자세한 정보를 제공하는 블로그 게시물 링크가 있습니다 . 좋습니다. 그럼 이제 오픈 소스 표준의 구현체인 nvishme로 넘어가 보겠습니다. nvishme는 전역 데이터 공간(GDS)을 기반으로 구축되었는데, 핵심 원칙은 통신에 사용하는 모든 배열이 대칭 힙에서 할당된다는 것입니다. 따라서 통신이 필요한 경우, 해당 배열은 nvishme의 MLOC(최대 허용 메모리)로 할당되어야 접근 가능합니다. 또한 MVMO(모바일 메모리 관리)는 메모리가 제대로 정렬되고 네트워크 어댑터에 등록되어 NV 링크를 통한 프로세스 간 통신이 가능하도록 보장하는 등의 역할을 합니다. 이러한 방식으로 단방향 통신이 가능해집니다. 음, 이건 집합적인 호출이기 때문에 모든 프로세스에서 동일한 크기로 호출해야 합니다. 그래서 대칭 힙이라고도 합니다. 따라서 할당을 수행하면 각 GPU에 동일한 크기의 할당 영역, 즉 동일한 크기의 청크가 할당되므로 오프셋 등을 전달하기 위한 통신이 필요 없다는 이점이 있습니다. 음, 왜냐하면 그냥 알잖아요, 내 동료에게 이 데이터를 보내야 하는데, 그 동료는 이런 값을 가지고 있고 오프셋은 X라고요. 그리고 굳이 추론할 필요 없이 이 계산을 로컬에서 할 수 있으니까요. 양측 검정 모델처럼 " 이 데이터를 어디에 넣어야 할까요?"라고 동료에게 물어볼 필요가 없습니다. 상대방이 다음에 필요로 할 위치에 데이터를 직접 작성하시면 됩니다 . 음, CPU API가 블로킹 방식과 스트림 감사 방식으로 구현되어 있는데, 첫 번째 슬라이드에서 보여드렸습니다. 하지만 무엇보다 중요한 것은, GET 연산을 통한 데이터 읽기, PUT 연산을 통한 데이터 쓰기, 아토믹 연산을 통한 동기화, 그리고 메모리 일관성 API를 제공한다는 점입니다. 특히 메모리 일관성 API는 매우 큰 장점인데, 이를 통해 작업 완료를 기다려야 하는 시점을 정확하게 제어할 수 있어 프로세스 간 동기화에 필요한 시간을 줄일 수 있습니다. 아, 제가 깜빡하고 말씀드리지 않았는데, 니켈의 경우 이 모든 프로그래밍 모델은 완전 상호극성입니다. 따라서 MPI 프로그램에서 Nickel을 사용할 수 있고, npm을 사용할 수도 있습니다. 이는 기존 코드베이스를 가속화하려는 경우에 유용합니다 . 음, 좋습니다. 그럼 장치 주도 통신의 특징은 무엇일까요? CPU 주도 통신을 사용하는 Schwam 프로그램이 있다고 가정해 보겠습니다. 일반적으로 연산은 GPU에서 수행 하고 통신은 CPU에서 이루어집니다. 따라서 경계에서 동기화가 필요합니다. 커널을 시작하고, 완료될 때까지 기다린 다음, 출력을 수행하고, 완료될 때까지 기다린 후 다음 반복을 시작합니다. MPI와 유사하게 흔히 사용되는 방식은 커널 실행 지연 시간을 줄이고, 중요 경로에서 데이터를 동기화하는 것입니다. 이렇게 하면 통신과 연산이 겹치지 않습니다. 통신과 연산을 각각 표현할 수 있습니다. 소스 코드에 예제가 있습니다. 마지막 슬라이드에 링크를 제공해 드리겠습니다. 하지만 이 방식은 코드를 더 복잡하게 만들고, API 호출도 많이 추가됩니다. 반면에 GPU 주도 통신은 연산 과 통신 모두를 GPU에서 수행합니다. 따라서 데이터가 계산되는 즉시 전송을 시작할 수 있습니다 . 음, 그 이점은 분명히 실행해야 하는 커널의 수가 줄어들고 작업 횟수가 줄어든다는 것입니다. GPU에서 오프로드 지연 시간이 줄어들면 스레딩을 통해 자연스러운 통신 및 연산 중복이 발생합니다. 전역 메모리 접근 지연 시간을 숨기는 데 사용하는 것과 동일한 기술을 사용하면 됩니다. 즉, 동일한 SM에서 여러 스레드를 실행하면 됩니다. 일부 알고리즘의 경우, 인라인 통신을 통해 표현하는 것이 더 쉬울 수 있습니다. 그렇다면 코드로 구현하면 어떻게 될까요? 어, 스레드 수준 통신이요. 음, 그러니까 각 스레드가 단일 요소만 처리하는데, 여기 아주 세밀한 부분이죠. 스레딩을 사용하면 통신과 연산 간의 최대 중첩을 얻을 수 있고, DGX 시스템에 있는 것과 같은 NVIM 패브릭에서 매우 효율적인 매핑이 가능합니다. 즉, 로드 저장을 하는 대신 전송 하려는 데이터에 대해 NVM float p를 호출하여 소스 데이터와 대상 처리 요소 중 원하는 위치에 데이터를 배치하기만 하면 됩니다 . 또한, InfiniB와 같은 네트워크 에 필요한 더 큰 그룹을 형성하는 API도 있습니다. InfiniB는 MBLink와 같은 메모리 패브릭에서 달성할 수 있는 만큼 높은 메시지 전송 속도를 제공하지 않기 때문입니다. 워프와 블록 간에는 여전히 중복이 발생 하지만, 더 큰 메시지를 형성하여 네트워크와 유사한 작업이 더 효율적으로 작동할 수 있도록 합니다. 이것이 하나의 이점이지만, 실제로 코드를 한 단계 더 발전시킬 수 있는 것은 커널 초기화 통신을 통해 특정 커널 제약을 피할 수 있다는 점입니다. 따라서 커널을 융합하여 더 많은 상태를 등록 메모리 또는 공유 메모리에 유지하고 전체 메모리 왕복을 방지하여 별도의 통신 커널을 만들고 다시 로드할 수 있습니다 . 그리고 이 과정에서 내부 동기화가 필요할 수 있는데, 커널을 올바른 방식으로 실행한다면 장치 주도 통신을 통해서도 가능합니다. 그래서 전체 반복 루프를 커널 안으로 옮길 수 있습니다. 이 경우는 당신의 경우에 비해 비교적 간단합니다. 아마도 전체 반복 루프 자체보다는, 서로 통신하는 두 개의 커널이 더 이상 개별적으로 실행할 필요 없이 함께 처리할 수 있는 작업들을 수행하는 데 문제가 있을 가능성이 높습니다. 네, 여기 HPC 관련 UKa 사용 사례도 하나 더 있습니다. Wilson D 슬래시 연산자의 강력한 확장성을 살펴보고 있습니다. CUDA는 CUDA 상의 QCD를 의미합니다. 음, 이건 Latis QCD 시뮬레이션에 필요한 계산을 위한 구성 요소를 제공하는 오픈 소스 라이브러리입니다. 그래픽 처리 장치에서 작동합니다. 음, 이는 환경 변수에 의한 장치 시작 통신을 활용하여 강력한 확장 시 발생하는 지연 시간 장벽을 극복합니다 . 다행히 대부분의 세부 사항은 상당히 안정적입니다. 자세한 내용은 제 동료가 GTC 2020에서 발표한 내용을 참고하시면 됩니다 . 음, 이것이 하는 일은... 제가 말씀드렸듯이, 많은 작업을 실행해야 하는 필요성 때문에 발생하는 지연 시간 문제를 극복하는 것입니다. CUDA에서는 통신이 필요할 때, 먼저 데이터를 패킹하고 통신한 다음 연산을 수행합니다. 통신과 연산을 동시에 처리하려면 내부와 외부를 위한 별도의 커널이 필요합니다. CUDA와 MPI를 사용한 단일 노드 실행 프로필을 살펴보면 패킹 커널, 내부 커널, 데이터 복사 등이 있는 것을 확인할 수 있습니다. 문제는 그 사이에 간격이 있다는 것인데, 이는 이러한 작은 커널을 실행하는 데 너무 오랜 시간이 걸리기 때문에 발생하는 것입니다 . 그리고 통신에 걸리는 시간이 내부 커널 실행 시간보다 더 길다는 것입니다. 그리고 그것은 통신 속도가 충분히 빠르지 않아서도 아닙니다. 개별 복사본을 실행하는 데 시간이 너무 오래 걸립니다. 그러니까, 만약 모든 P2P 복사본을 하나로 합친다면, 내부에서 실행되는 것보다 훨씬 더 빠르게 작동할 겁니다. 음, 니쉬메와 비교했을 때, 우리는 완전히 융합된 커널을 구축할 수 있습니다. 그래서 우리는 커널을 하나만 실행하는 것이 아닙니다. 커널 실행은 다음 반복을 위해 미리 실행될 수도 있습니다. 그래서 현재 커널이 실행되는 동안 이미 다음 커널에 필요한 모든 것을 GPU에 제공하고 있으므로 GPU의 공백을 방지하고 확장 시 성능 향상으로 이어집니다. 이 슬라이드는 비교적 복잡한 슬라이드입니다. DJ EOS와 DJX H100 400GB NDR을 다시 한번 살펴보겠습니다. 주어진 문제들을 해결하기 위해, 우리는 주어진 문제 규모를 최대 512개의 GPU까지 강력하게 확장할 수 있습니다. 여기서는 NPI와 NVME에서 서로 다른 세 가지 정밀도를 살펴보고 있습니다 . 차이점은 정밀도가 낮을수록 가장 크게 나타나는데, 그 이유는 계산량이 적기 때문입니다. 상대적으로 볼 때 소통이 더 중요합니다. 그리고 그 부분을 확대해서 보면, 규모가 커질수록 성능 차이가 더 커진다는 것을 알 수 있습니다. NVME의 경우, 더 많은 리소스를 투입할 가치가 있는지 의문이 들기 시작하지만, 256개에서 512개 GPU로 업그레이드하면 MPI 대비 최대 1.7배의 속도 향상을 볼 수 있으므로 여전히 더 큰 이점을 얻을 수 있습니다. 또한 NPI는 확장성이 부족하다는 것을 알 수 있습니다. 128에서 256으로의 증가는 여러 커널을 실행해야 하는 필요성과 밀접한 관련이 있는데, 이는 해당 시점에서 파티션 변경이 발생하여 MPI 버전이 더 많은 경계 커널을 실행해야 하기 때문입니다. 네, 그럼 마무리하겠습니다 . 음, 이 컨퍼런스의 다른 CUDA 개발자 세션을 찾을 수 있는 링크가 있습니다. 일부는 아직 방송되지 않았지만, 대부분은 이미 방송이 종료되었으므로 녹화된 영상을 시청하실 수 있습니다. 오늘 오후 3시에 열리는 CWE 행사를 홍보하고 싶은데, 좀 뻔뻔스럽네요. 이곳에서는 저를 비롯한 GPU Comes 팀의 동료들이 MPI, 니켈, 엔비히, 메모리, GPU Direct 등에 대한 여러분의 질문에 답변해 드릴 것입니다. 음, 저도 비슷한 제목의 강연을 홍보하고 싶습니다. 저는 작년에 HBC 및 AI 모델을 위한 multiGGP 프로그래밍에 대해 발표했는데, 그 발표에서 NPI 니켈 및 환경 분야도 다뤘습니다. 하지만 저는 링크된 멀티 GPU 프로그래밍 모델 소스 코드를 " Hello World" 예제로 사용하여 소스 코드가 어떻게 생겼는지, 동기화 가능한 예제를 통해 통신 및 연산 모델을 다양한 방식으로 어떻게 표현하고, 성능을 비교하는지 단계별로 살펴보고 있습니다 . 네, GitHub와 Nvidia On Demand에서 작년 녹화 영상을 찾아보실 수 있습니다. 요약하자면, GPRC(피어 투 피어)와 RDMA(NPI 및 니켈)는 활용될 경우 성능을 향상시키지만, 라이브러리 내부에서 NVME용 스테이징을 수행할 수 있기 때문에 사용 불가능한 경우에도 작동합니다. 단방향 모델은 데이터의 소스나 대상을 거치지 않고 작업을 수행하려면 매핑 설정이 필요하기 때문에 이러한 모델이 필수적입니다. MPI, 니켈, 엔비샴은 CUDA 스트림을 인식하므로 CUDA 그래프와도 연동할 수 있어 그래프에서 통신을 캡처하여 API 오버헤드를 더욱 줄일 수 있습니다. 오늘부터 NVMe를 통해 통신을 시작하는 Colonel은 니켈에서도 통신을 지원할 예정이며, 월요일 니켈 관련 발표에서 말씀드렸듯이 곧 니켈에서도 지원될 것입니다. 지원되는 통신 모델은 콜렉티브( collective)라고 불리는 2S(양방향), 1S( 단방향)입니다. NPI 니켈은 곧 이 세 가지 모두를 지원할 예정이며, NVMe는 콜렉터와 단방향을 모두 지원합니다. 그럼, 뒤쪽에 있는 질문들을 받겠습니다. 이야기 나눠주셔서 감사합니다. 좋은 대화였습니다. 한 가지 명확히 하고 싶은 점이 있습니다. 그래서 엔비디아는 Doka GPU net.io라는 또 다른 패키지도 가지고 있습니다. 그것은 이 표의 어디에 해당될까요 ? 음, 그러니까 net io는 저수준 라이브러리라고 할 수 있는데, Aerial 같은 프레임워크에서 사용되지만 대규모 클러스터에서 실행되는 HPC 애플리케이션에서는 그다지 많이 사용되지 않습니다. 그래서 더 자세히 설명하자면, 이 표에는 잘 맞지 않을 것 같습니다. 다른 사용 사례를 대상으로 하기 때문입니다. 예를 들어, 데이터를 계측 장비로 직접 스트리밍해야 할 때 엣지 컴퓨팅이 중요해집니다. 그러니까 방금 보여주신 GPU가 시작하는 통신 방식이 그거랑 비슷한 거 아닌가요? 예. 예. 예. 예. 기술 스택에 중복되는 부분이 많지만, 엣지 컴퓨팅이라는 다른 사용 사례를 위해 설계되었습니다. 음, 가장 대표적인 예는 AIAL입니다. 좋아요. 감사합니다. 응. 다른 질문 있으신가요? 질문 하나 드리겠습니다. 어, 젠슨이 다이나모 운영 체제에 대해 발표한 기조연설입니다. 당신이 여기에 제시한 해결책들이 향후 다이나모 솔루션의 일부가 되어 자동화에 기여할 것이라고 생각하십니까 ? 음, 그러니까 추론 측면에서 말하자면, 모든 멀티 GPU 애플리케이션은 데이터를 서로 주고받아야 하는데, 그래야 GPU 들을 함께 사용할 수 있죠. 물론 MPI 와 니켈은 우리가 고수준의 모든 것을 구축하는 데 사용하는 기본 구성 요소입니다. 다만 MPI와 니켈을 어떻게 함께 사용할 수 있는지 이해하고 싶었을 뿐입니다. 예를 들어, LLM 추론에서 사전 채우기와 디코딩을 하는 경우를 봤는데, GPU 그룹이 사전 채우기 작업을 하고 다른 그룹이 디코딩 작업을 하는 방식이 있더군요. MPI와 Nickel을 함께 사용하는 코드를 봤는데, 언제 Nickel만 단독으로 사용해야 하는지, 아니면 그런 호출은 어떻게 해야 하는지 잘 모르겠습니다. 코드를 어떻게 시작해야 할까요? MPI를 먼저 사용하고 그 안에 Nickel을 도입하는 게 좋을까요? 가장 좋은 방법은 실행 가능한 기본 코드부터 시작해서 최적화를 진행하는 것일까요? 그냥 몇 가지 알고 싶었어요. 응. 그래서 음, 제가 생각하기에 NPI 니켈을 결합하는 가장 큰 이유는 이미 NPI 코드가 있고 성능이 중요한 부품에 니켈을 사용할 수 있기 때문입니다. 따라서 상호 운용성이 중요합니다. 이미 NPI를 사용하고 있다면 전체 코드를 니켈로 다시 작성할 필요가 없습니다 . 가장 큰 가치를 제공하는 부분에만 적용하고 성능에 중요하지 않은 부분은 건드릴 필요가 없습니다. 따라서 니켈이 필요한 기능을 제공한다면 니켈을 사용하는 것이 좋습니다. 제 생각에는 처음부터 코드를 작성한다면 니켈로 작성하는 것이 좋을 것 같습니다. NPI는 부트스트래핑에 도움이 될 수 있으므로 좋은 선택일 수 있지만 다른 선택지도 있을 수 있습니다. 하지만 데이터가 항상 GPU에 있다면 굳이 니켈을 사용할 필요는 없습니다. 물론 CPU와 GPU 간 통신이 혼합된 환경이라면 MPI도 고려해 볼 만하겠지만, 일반적으로 Nickel이 GPU 데이터 처리를 지원한다면 Nickel부터 시작하는 것이 좋습니다. 통신 라이브러리 관점에서 볼 때, IPv6 주소 체계를 사용하는 네트워크에서 사용할 때 어떤 문제가 있을까요? IPv4와 IPv4를 구분하는 기능이 통신 라이브러리 자체에 포함되어 있나요, 아니면 라이브러리 내부의 전송 계층에서 추상화되어 처리되나요? 특히 이더넷 환경에서 실행할 때 성능 측면에서 중요한데, 이더넷 제공업체가 RDMA over Ethernet을 지원하는지 여부가 중요합니다. 하지만 저는 저수준 프로그래밍에는 익숙하지 않습니다 . 제 생각에는 그건 IP V6 및 IP V4와는 별개의 문제인 것 같습니다. 그러니까, 라이브러리가 완전히 추상화되어 있다는 건데, 저는 이런 일이 일어난다는 얘기는 들어본 적이 없어요 . 이게 역할을 한다는 것도요. 혹시 다른 동료나 사용자 중에서 이더넷 네트워크를 통해 이러한 라이브러리를 사용한 사례를 접하신 분이 있나요? 해당 네트워크는 IPv6 주소 체계를 지원하지만, Rocky 프로토콜을 사용합니다. 저도 잘 모르겠으니 전문가와 상담해 보세요. 좋아요. 저희가 도와드릴 수 있을지도 몰라요 . 확신하는. 감사해요. 첫 번째 각주에 대해 간단한 질문 하나 드리겠습니다. 음, NPI가 송수신에 스트림 인식 기능을 위한 추가 매개변수를 추가할 계획이 있는지 아시나요? 사실 그 워크그룹을 운영하는 분이 CWE에 참석할 예정이라 거기서 직접 이야기하는 게 좋을 것 같습니다. 감사합니다. 그리고 니켈 자체 런처가 있나요? 제가 지금까지 해 온 모든 작업은 니켈을 통신 프레임워크로 사용하는 애플리케이션을 MP NPI로 실행하는 것이었는데, NPI 없이 니켈을 완전히 실행할 수 있는지 궁금합니다. 니켈 커뮤니케이터를 부트스트랩하는 데 필요한 것이 NPI 실행이 필요 없는 건가요? Slurm을 사용하거나 다른 통신 라이브러리를 사용할 수도 있지만, 부트스트랩이 꼭 필요한 건 아니잖아요. 니켈에는 자체 런처가 없군요. 네, 감사합니다. 좀 순진한 질문이었네요. 음, 윈도우용 CUDAware MPI가 있나요? 죄송합니다만, 윈도우용 CUDA MPI가 있나요? 저는 잘 모르겠어요 . 음 , 죄송합니다. 저는 아직 확인하지 못해서 확답을 드릴 수는 없지만, 그렇다고 부인할 수도 없습니다. 제가 그 부분에 대해 조사를 해보지 않았기 때문입니다 . 응. 네, 좋은 말씀 감사합니다. 그래서 저는 월요일에 니 키코의 아주 훌륭한 강연에 참석할 예정입니다. 그래서, 제 생각에는 스빈이 니 키코의 새로운 기능인 대칭형 메모리에 대해 언급했던 것 같습니다. 응. 그래서 저는 그 기법과 구상 디자인의 차이점이 무엇인지 궁금합니다. 그러니까 아주 높은 추상화 수준에서 보면, 그것은 대칭형 메모리라는 개념과 완전히 똑같습니다 . 음, 그러니까 어... 네네네, 제발 여기로 와주세요. 음, 몇 가지 차이점이 있습니다. 구현 방식에 몇 가지 차이점이 있을 것입니다. 왜냐하면 저희가 니켈 장치를 개발하고 계획하는 방식은 애플리케이션 배포 요구 사항에 분명히 초점을 맞추고 있기 때문입니다. 음, 그래서 nvishme와 비교했을 때 고려해야 할 절충점이 몇 가지 있지만, 개발자 입장에서의 사고방식은 대칭형 메모리와 완전히 동일합니다. 그러니까 put과 get을 사용하는 단방향 통신이 될 거고, 음, 알겠습니다. API가 다르게 보일 거고, 몇 가지 다른 작업을 해야 할 것 같은데, 충분히 높은 추상화 계층 수준이라 이와 동등한 것으로 생각할 수 있을 것 같습니다. 그리고 환경이나 대칭 메모리 기능을 사용할 때, 특히 컴퓨팅 규모가 매우 큰 경우 부작용이 있는지 아시나요? 이전에 OpenCenter를 사용했을 때, 예를 들어 수많은 GPU나 CPU로 확장하려고 하면 문제가 발생했던 경험이 있습니다. 가능하시다면 CWE에 오셔서 자세히 살펴보는 것이 좋을 것 같습니다. 지금은 시간이 좀 걸리네요. 답변 감사합니다. 모두 감사합니다. 이게 마지막 질문이었습니다. 유리 씨와 이야기 나누실 수 있습니다. 다음 세션을 위해 자리를 비워주셔야 합니다 . 참석해 주셔서 진심으로 감사드립니다.
