# Let's Use AI to Create Data!

- 영상 링크: https://www.youtube.com/watch?v=KYk00HuZLkU
- 채널: Weaviate
- 업로드일: 2023-12-04
- 자막 언어: ko
- 단어 수: 약 1638개

---

## 스크립트

AI를 활용하여 애플리케이션에 필요한 데이터를 생성해 보겠습니다. 안녕하세요, we8의 Adam입니다. 'build with we8' 시리즈의 새로운 에피소드에 오신 것을 환영합니다. 이번 에피소드에서는 Jupiter Notebook을 사용하여 가상의 호텔 객체를 생성하는 방법을 살펴보겠습니다. 호텔의 랜드마크나 편의시설과 같은 데이터 포인트를 무작위로 생성하고, OpenI 채팅 자동 완성 API를 통해 대규모 언어 모델을 활용하여 이러한 JSON 객체를 생성합니다. 생성된 JSON 객체는 특정 디자인에 맞춰 설계되며, 이후 데이터를 추출하여 활용할 것입니다. 또한, AI 기반 벡터 데이터베이스인 we8을 사용하여 벡터 임베딩을 생성하고, 이를 우리가 만든 스키마에 저장하는 방법을 배우겠습니다. 이 임베딩은 검색된 데이터를 기반으로 마케팅 이메일을 생성하는 RAG 애플리케이션의 기초가 될 것입니다. 이를 위해 we8에 통합된 모듈인 벡터 검색과 생성 검색을 활용합니다. 마지막으로, 초기 프롬프트를 채팅 GPT로 확장하여 설명적인 이미지를 생성하고, 이를 확산 모델에 사용하여 이미지를 생성하는 방법도 살펴보겠습니다. 흥미롭다면, 그럼 시작해 볼까요? 자, 시작해 볼까요? 이 비디오에서는 Open과 Replica를 포함한 여러 SAS 서비스의 API를 사용하는 Jupyter Notebook을 단계별로 살펴보겠습니다. 계정을 등록하거나 API 키를 찾는 데 시간을 낭비하지 않도록 단계적으로 진행할 것입니다. Notebook에서 특정 서비스에 새 API 키가 필요한 부분이 있으면 알려드리겠습니다. 그러면 해당 키를 가져와 환경 변수에 추가할 수 있습니다. 자, 이제 Chat GPT에서 이 기능이 제대로 작동하는지 테스트해 보겠습니다. 우리 프로젝트에서는 호텔을 나타내는 JSON 객체를 생성하려고 합니다. 객체는 평면형으로 주소, 이름, 주변 랜드마크 등의 정보를 포함해야 합니다. 자, 제가 정리한 목록을 JSON 객체 형식으로 대략적으로 구성해 보았습니다. 각 필드의 데이터 유형도 지정했습니다. 다음으로 Chat GPT의 채팅 인터페이스에서 이 형식의 객체를 생성하는 것이 가능한지 확인해 보겠습니다. 코드를 작성하기 전에 Chat GPT 인터페이스에서 방금 생성한 JSON 객체 유형을 사용하여 호텔을 생성해 보겠습니다. 자유롭게 참여해 주세요. 이 프롬프트를 읽으려면 비디오를 잠시 멈추세요. 기본적으로 Chat GPT에게 이 JSON 객체를 생성하고 샌프란시스코를 참조하도록 지시하는 것입니다. 그리고 아래에 템플릿을 포함시켰습니다. 자, 전송해 보겠습니다. 꽤 잘 되네요. 이제 좀 더 복잡한 요소를 추가해 보겠습니다. 예를 들어 최상위 편의 시설을 구체적으로 지정하고 랜드마크를 참조하도록 하겠습니다. 새 프롬프트, 업데이트된 프롬프트에는 " 샌프란시스코 랜드마크: 골든 게이트 브리지"라고 적혀 있고 최상위 편의 시설은 " 프라이빗 스파"로 지정되어 있습니다. 물론 JSON 템플릿을 다시 전송합니다. 어떤 결과가 나오는지 보겠습니다. " 골든 게이트 뷰 호텔 앤 스파: 최상위 편의 시설, 프라이빗 스파". 좋습니다. 꽤 만족스럽네요. 결과가 꽤 만족스럽고 Chat GPT가 이 작업을 수행할 수 있다는 확신이 듭니다. 이제 Jupyter Notebook으로 넘어가서 방금 한 작업을 코드로 변환해 보겠습니다. 먼저 GitHub에 게시된 Jupyter Notebook을 통해 실행하겠습니다. 즉, 여러분 도 복제하여 따라 할 수 있습니다. 이 저장소로 이동하면 되고, 링크는 아래 설명란에 있습니다. 이 비디오 설명에 있는 GitHub 링크를 열면 이 GitHub 저장소로 이동합니다. 저는 코드 탭을 클릭 하고 https URL을 복사한 다음 터미널을 엽니다. 터미널에서 `get clone` 명령어를 입력하고 링크를 붙여넣은 후 Enter 키를 누릅니다. 이렇게 하면 저장소가 로컬 컴퓨터에 복제됩니다. 이제 저장소에 접근하여 파일 목록을 확인해 보겠습니다. Jupyter Notebook이 있고, requirements.txt 파일에는 종속성 목록이 있으며, README 파일 과 라이선스 파일도 있습니다. 먼저 새 Python 가상 환경을 시작하고 활성화한 다음 requirements.txt에 있는 모든 종속성을 설치합니다. 좋습니다. 이제 Jupyter Notebook을 입력하여 노트북을 실행하기만 하면 됩니다. 이제 바로 노트북을 실행해 보겠습니다. 궁금한 점이 있으면 언제든지 비디오를 일시 정지하세요. 자, 이제 Jupyter Notebook을 열어 보겠습니다. 먼저 OpenAI API를 설정해야 합니다. 그러려면 OpenAI API 키가 필요합니다. 아직 API 키가 없다면 다음 단계를 따르세요. open.com에 접속하여 계정을 등록하고 API 키를 생성하세요. 생성된 API 키를 EnV 파일에 추가하고, 파일 이름이 ' open AI API key'인지 확인하세요. 이제 첫 번째 셀을 실행하면 Open AI 클라이언트가 생성됩니다. 좋습니다. 이 경고는 중요하지 않으니 안심하세요. 다음으로 고급 편의시설 목록을 생성하여 호텔을 더욱 흥미롭게 만들겠습니다. 스파 및 웰니스 센터, 개인 인피니티 풀, 미슐랭 스타 레스토랑 등 다양한 편의시설을 무작위로 배치할 것입니다. 이제 실행해 보겠습니다. 또한 샌프란시스코의 랜드마크 목록을 생성하겠습니다. 골든 게이트 공원, 페인티드 레이디스, 제드 델리 스퀘어 등이 있습니다. 이제 이 셀을 실행해 보겠습니다. 이 셀은 호텔을 생성하고 이전에 chat GPT에 전달한 프롬프트를 사용하여 채팅 완료 API를 호출하는 함수를 정의합니다. 첫 번째 단계는 선택 항목을 무작위로 선택하는 것입니다. 편의 시설과 랜드마크를 모두 출력하여 가시성을 높인 다음, ChatGP35 Turbo 1106을 사용하여 OpenAI에 채팅 완료 호출을 합니다. 여기서 '응답 형식'이라는 속성을 사용하고 JSON 객체 유형을 전달하여 Chat GPT를 JSON 모드로 설정합니다. 이는 2023년 11월 OpenAI 개발자 데이에서 공개된 새로운 기능입니다. 이제 특정 프롬프트를 전달하여 VIIC를 생성하고, 가상의 Tell을 생성하는 등의 작업을 수행합니다. 특정 랜드마크를 지정하려면 다음과 같이 문자열을 사용하여 랜드마크를 전달합니다. 편의 시설도 마찬가지로 최상위 편의 시설 객체에 포함하도록 지정합니다. 그런 다음 Chat GPT가 어떤 형식으로 표시해야 하는지 알 수 있도록 JSON 객체를 전달합니다. 완료되면 JSON 딕셔너리 형식으로 객체를 반환합니다. 이제 이 셀을 실행하면 호텔 스토어가 생성됩니다. 호텔 딕셔너리에 저장한 다음 결과를 출력해 보겠습니다. 한번 시도해 보죠. 여기 Golden Gate Park라는 호텔이 있습니다. 주소는 Park Avenue 112-34번지이고, 무료 Wi-Fi가 제공됩니다. 최고의 편의 시설은 애완동물 서비스이고, 여기에 표시된 것처럼 무작위로 선택된 랜드마크는 Golden Gate Park입니다. Golden Gate Park... 좋습니다. 이제 파이썬 주피터 노트북에서 이러한 호텔 객체를 프로그래밍 방식으로 생성할 수 있습니다. 다음으로 we8의 임베디드 인스턴스를 생성하겠습니다. 즉, 컴퓨터에서 로컬로 실행하고 데이터는 디스크에 저장되지만 벡터 조회는 메모리에서 이루어집니다. we8의 임베디드 인스턴스를 생성하려면 먼저 we8을 가져오고 wev8에서 임베디드 옵션을 가져와야 합니다. 임베디드를 사용하면 이러한 임베디드 옵션을 전달하고 OpenAI API 키를 추가 헤더로 전달하여 We8 클라이언트를 생성할 수 있습니다. 이렇게 하면 We8 클라이언트가 OpenAI와 상호 작용하여 벡터 임베딩을 생성하고 쿼리를 벡터화할 수 있습니다. 자, 이제 이 셀을 실행해 보겠습니다. 이제 We8 임베디드 인스턴스가 로컬에서 실행 중입니다. 이전에 실행했던 적이 있으므로 완전히 새로운 데이터베이스에서 실행하기 위해 클래스를 삭제하겠습니다. 하지만 이전에 실행한 적이 없다면 컴퓨터에 Hotel 스키마가 없기 때문에 오류가 발생합니다. 이제 완료되었습니다. 이 셀은 이전에 정의한 JSON 객체를 기반으로 스키마를 생성합니다. 이 경우 JSON 객체가 있고 Hotel이라는 클래스가 있으며 벡터화 도구로 "useTextToVectorizer"와 "useTextToVectorizer"를 지정했습니다. We8 클라이언트에 OpenAI 키를 전달해야 하는 이유는 We8 클라이언트가 이러한 객체를 받아 "useTextToVectorizer"와 "useTextToVectorizer"를 사용하여 데이터를 벡터화하기 때문입니다. 그런 다음 각 항목을 지정합니다. 우리는 호텔 속성에 이름, 주소, 주변 명소와의 근접성 등의 정보를 가지고 있습니다. 저는 벡터 검색을 하지 않으려는 일부 필드에 대해서는 벡터화를 건너뛰도록 설정했습니다. 여기서는 이름, 접근성 옵션, 호텔 유형, Wi-Fi, 조식, 수영장, 스파 등을 벡터화합니다. 원하시면 이 스키마를 자세히 살펴보실 수 있지만, 저는 Jupyter Notebook에 결과를 저장하기 위해 셀을 실행하고, we-embedded 클라이언트에 대한 스키마를 생성하겠습니다. 이제 we-embedded 클라이언트에 대한 스키마가 생성되었습니다. 여기서는 for 루프를 사용하여 10개의 호텔을 생성하고, 이 클라이언트의 `data object.create` 메서드를 사용하여 호텔 데이터를 전달하고 스키마를 지정하여 we-embedded에 저장합니다. 이제 10개의 호텔에 대해 실행해 보겠습니다. 좋습니다. 이제 완료되었으니 다음 단계로 넘어가 보겠습니다. 이 10개의 호텔을 벡터 데이터베이스에 추가했으므로 벡터 검색과 같은 다양한 작업을 수행할 수 있습니다. 다음 셀은 특정 매개변수를 지정하여 특정 호텔을 찾는 벡터 검색을 실행한 다음, 특정 프롬프트를 사용하여 생성 검색을 적용하고 각 호텔의 세부 정보를 해당 프롬프트에 전달합니다. 여기에서 생성 프롬프트를 보여드리겠습니다. 예를 들어, 특정 이름의 호텔이 출장객에게 적합한 이유를 이메일로 설명하는 프롬프트, 비즈니스 관련 편의 시설, 휴식을 취할 수 있는 좋은 방법 등을 설명할 수 있습니다. 그리고 가장 인기 있는 편의 시설도 포함됩니다. 이러한 정보는 스키마의 각 객체에서 속성으로 전달되며, 생성 프롬프트는 `with generate` 속성에 전달됩니다. 이 기능이 작동하는 이유는 ` Generative` 모듈이 스키마와 웹 내장 인스턴스에 포함되어 있기 때문입니다. 여기에서 어떻게 작동하는지 보여드리겠습니다. `Generative Open`을 사용하고 있으며, 특정 모델(GPT 35 Turbo 16k)과 최대 토큰 수를 지정했습니다. 이렇게 하면 표시된 특정 프롬프트를 사용하여 `Open`에 대한 호출이 자동으로 수행됩니다. 그리고 응답을 받으면... 저희는 검색 결과를 두 개로 제한하고, 이를 대규모 언어 모델에 전달하여 생성한 다음, 생성된 결과를 출력합니다. 자, 여기서는 비즈니스와 관련된 특정 콘셉트를 가진 호텔을 검색하고 있습니다. 실행해서 어떤 결과가 나오는지 확인해 보겠습니다. 안녕하세요, 수신자님. 이메일을 잘 받으셨기를 바랍니다. 비즈니스 여행객에게 좋은 선택이 될 만한 호텔을 소개해 드리고자 합니다. 차나트 타운 리트리트 호텔(Chanat Town Retreat Hotel)과 코이 타워 호텔(Koi Tower Hotel)입니다. 비즈니스 여행객에게 이상적인 숙박 시설입니다. 이메일을 잘 받으셨기를 바랍니다. 곧 있을 출장에 완벽한 최고급 숙박 시설인 코이 타워 호텔을 소개해 드립니다. 이처럼 생성형 검색 모듈을 사용하면 대규모 언어 모델을 호출하여 모든 종류의 텍스트 응답을 생성할 수 있습니다. 생성형 모듈을 통해 제공되는 벡터 검색 쿼리에 프롬프트를 직접 전달할 수도 있습니다. we8에 통합된 이 모듈을 사용하여 앞서 생성한 호텔 중 하나를 선택 하고 그 위에 설명을 추가해 보겠습니다. 그래서 저는 채팅 완성 라이브러리를 사용하여 일반적인 설명과 묘사적인 이미지에 초점을 맞춘 프롬프트를 수동으로 생성하겠습니다. 자, 다음 단계로 넘어가서 복제(Replicate)로 이동하여 API 키를 생성하고, EnV 파일에 API 키를 넣고 화면에 보이는 이름과 동일하게 지정하세요. 먼저 이전에 생성한 호텔 중 하나를 가져와서 첫 번째 셀에 출력하고 화면에 표시하겠습니다. 미션 오아시스 호텔입니다. 여기에 또 다른 프롬프트가 있는데, 특정 단어 수 이하로 다음 JSON을 묘사적으로 설명하라는 내용입니다. 그리고 응답을 출력합니다. 자, 한번 해보겠습니다. 미션 오아시스 호텔은 샌프란시스코에 있는 중형 부티크 호텔입니다. 맞춤형 시티 투어 등을 제공합니다. 이제 이 응답을 안정 확산 모델(Stable Diffusion Model)에 전달하여 호텔의 모습을 시각화할 이미지를 생성할 것입니다. 또한 채팅 GPT를 호출하여 호텔의 모습을 묘사하는 이미지를 생성하도록 요청하고, 이를 시각화에 활용할 것입니다. 자, 이것도 해볼까요? 미션 오아시스 호텔은 샌프란시스코 미션 스트리트 123번지에 위치한 중형 부티크 호텔입니다. 모던한 인테리어와 미션 지구의 문화적 영향을 접목한 매력적인 호텔을 상상해 보세요. 멋지네요! 설명이 꽤 길죠? 앞서 언급했듯이, 이 시점에서 복제 API 키가 필요합니다. 여기에 표시된 것처럼 환경 변수 파일에 키가 있는지 확인하세요. 이 셀은 전달된 API 키를 사용하여 복제 클라이언트를 생성합니다. 실행해 보겠습니다. 좋습니다. 여기서는 복제본의 특정 모델인 Stability AI를 호출합니다. 구글에서 검색해 보시면 Stability AI 안정 확산 모델이라는 것을 알 수 있습니다. 특정 프롬프트를 전달합니다. 이 경우, 프롬프트를 수동으로 작성하여 최고의 편의 시설과 주변 명소와의 근접성을 지정합니다. 그런 다음 여기에 표시된 것처럼 가로와 세로 600으로 이미지를 화면에 출력합니다. 실행해 보겠습니다. 두 번째 이미지에는 전체 설명을 전달합니다. 이미지, 그러니까 여기 있는 이 전체 텍스트 블록이 될 거예요. 이 Jupiter 노트북을 편집해서 이전에 만든 설명도 전달할 수 있지만, 저는 그렇게 하지 않을 거예요. 잠시 기다리면 세련되고 현대적인 고급 호텔 이미지가 나타날 거예요. 특정 명소 와의 근접성도 표시되어 있죠. 호텔이 멋지네요. 뒤쪽에는 금문교가 보이고, 멀리에는 트랜스 아메리카 피라미드가 보이네요. 어쨌든 이렇게 생겼어요. 자, 이제 이전에 만든 전체 설명 이미지를 전달해 볼게요. 샌프란시스코 느낌이 물씬 나네요. 텍스트도 조금 있는데, 아주 훌륭하진 않지만 미션 지구의 분위기가 느껴지네요. 자, 이제 여러분은 여러 OpenAI 모델의 새로운 JSON 모드 기능을 사용하여 JSON 객체를 생성하고, WEA에 저장하여 벡터 검색과 생성 검색을 적용하고, 확산 모델을 실행하여 모의 데이터에 대한 이미지를 생성하는 데 사용할 수 있는 설명을 만드는 방법을 알게 되었습니다. 앞으로 이 새로운 기술을 활용해서 자신 만의 애플리케이션 데모를 만들거나, 무엇이든 할 수 있습니다. 생성된 이미지 기반 몰입형 애플리케이션에 대한 영상입니다. 시청해 주셔서 감사합니다. 유익한 시간이었기를 바라며, we8을 활용한 멋진 개발 작업 되시길 바랍니다. 궁금한 점이 있으시면 커뮤니티 슬랙 채널에 문의해 주세요. 슬랙 채널 정보는 아래 설명란에 있습니다. 다음 영상에서 뵙겠습니다!
