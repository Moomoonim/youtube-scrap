# Evaluate LLMs with NeMo Evaluator and Docker Compose | Step-by-Step Setup Guide

- 영상 링크: https://www.youtube.com/watch?v=Fo9kNJE5nC8
- 채널: NVIDIA Developer
- 업로드일: 2025-07-22
- 자막 언어: en
- 단어 수: 약 926개

---

## 스크립트

Hey, what's up everybody? My name is Chris,&nbsp; and I'm a product research engineer here at&nbsp;&nbsp; NVIDIA. Today, we're going to talk about Nemo&nbsp; Evaluator. More specifically, we're going&nbsp;&nbsp; to talk about how we can quickly get started&nbsp; with Nemo Evaluator by using Docker Compose. So, what is Nemo Evaluator? It is a microservice&nbsp; that simplifies the end-to-end evaluation of&nbsp;&nbsp; GenAI, like LLMs, retrieval-augmented generation,&nbsp; and agents, with an easy-to-use API. However,&nbsp;&nbsp; for the Docker version, we're going to&nbsp; focus on LLM as a judge capabilities,&nbsp;&nbsp; custom LLM as a judge, which is going to help us&nbsp; determine how well our AI system is doing when&nbsp;&nbsp; we evaluate its performance. The following setup&nbsp; is best used for local development and testing. First things first, we're going to need to grab&nbsp; an API key from build.nvidia.com, which you can&nbsp;&nbsp; obtain through your "Manage API keys" widget after&nbsp; logging in to the website. Now, we're going to&nbsp;&nbsp; head to our terminal where we can do the rest&nbsp; of the flow. The first thing we're going to do&nbsp;&nbsp; is export our API key using the following command.&nbsp; Just make sure you substitute your actual API key. Once we've done that, we're going to log into&nbsp; Docker using the following command. Again, we're&nbsp;&nbsp; going to key into that NGC API key environment&nbsp; variable, which is why we did the export above.&nbsp;&nbsp; The next thing we want to do is make sure that&nbsp; we're in a directory that has our Docker Compose&nbsp;&nbsp; YAML and our init scripts. We can do this through&nbsp; the documentation or use the live evaluation&nbsp;&nbsp; example on the Generative AI Examples repository.&nbsp; Links will be available in the description. The next thing we're going to do is&nbsp; specify what version we want for our&nbsp;&nbsp; actual microservices. In this case, we want&nbsp; 23.07 (though the transcript says 25.07,&nbsp;&nbsp; it is likely referring to a date&nbsp; in the format YY.MM) for both our&nbsp;&nbsp; evaluator microservice and our data&nbsp; store microservice. Additionally,&nbsp;&nbsp; we're going to do some bookkeeping to make&nbsp; sure that we're ready to run Docker correctly. In the directory with our Docker Compose file,&nbsp; we can simply run the Docker Compose command,&nbsp;&nbsp; and it's going to spin up our microservices.&nbsp; Again, we don't need to pause here and wait&nbsp;&nbsp; for extended time periods while this&nbsp; system spins up. We can actually see&nbsp;&nbsp; it before our eyes. So, this is very quick&nbsp; and very suitable for local development. Now that we have this microservice&nbsp; spun up, let's go ahead and look at&nbsp;&nbsp; how we may evaluate this using the&nbsp; microservice that we just spun up.&nbsp;&nbsp; In the following notebook, we're actually going&nbsp; to leverage the work that we just did to do a&nbsp;&nbsp; live evaluation. We're going to do two types of&nbsp; evaluation. The first is a simple string check,&nbsp;&nbsp; which is just to demonstrate that it&nbsp; works quickly. The second is custom&nbsp;&nbsp; LLM as a judge on some synthetically created&nbsp; medical summaries as well as consultations.&nbsp;&nbsp; We're basically going to see if the summary&nbsp; is a correct record of the consultation. The first thing we need to do is set&nbsp; up some necessary configurations,&nbsp;&nbsp; which we've already done. Next, we're going&nbsp; to use the Python SDK for Nemo microservices.&nbsp;&nbsp; Instead of needing to submit&nbsp; complicated curl commands&nbsp;&nbsp; or use the requests library, we can use the&nbsp; Nemo microservices client to do this for us. By default, the evaluator is going to be spun up&nbsp; at this endpoint when we work through the Docker&nbsp;&nbsp; flow. We're going to do some simple string parsing&nbsp; by setting up this evaluation job. We use our&nbsp;&nbsp; `client.evaluation.live`, pass in a config, pass&nbsp; in a target, and we're good to see the evaluation. Let's look at a more complex example&nbsp; to see what we can do. In this example,&nbsp;&nbsp; we're going to use Llama2 70B. This is going to be&nbsp;&nbsp; powered through build.nvidia.com. We already&nbsp; have our API key for this, so we don't need&nbsp;&nbsp; to provide it additionally. We can just&nbsp; use the same one we used to log into nvcr. We also need to set up a correctness&nbsp; prompt. This prompt is going to be what&nbsp;&nbsp; the judge uses to determine whether or&nbsp; not our metric is correct. In this case,&nbsp;&nbsp; we're looking for just if it correctly&nbsp; reflects the consultation. We're going to&nbsp;&nbsp; give this a score from 0 to 4, where&nbsp; 0 is the worst and 4 is the best. Finally, we're going to set up a&nbsp; config and a target. For our config,&nbsp;&nbsp; we're going to use the type custom because&nbsp; that's what's compatible. We're going to&nbsp;&nbsp; create a task called correctness of type data.&nbsp; For our metric, we're going to use correctness&nbsp;&nbsp; liker. This is going to be an LLM judge metric,&nbsp; and it's going to point to our judge model. Then we're going to provide a bunch of rows.&nbsp; These rows are just going to contain content,&nbsp;&nbsp; which is a consultation, and summary, which&nbsp; is a summary of that consultation. We'll see&nbsp;&nbsp; in this instance that we're providing five&nbsp; unique rows. And we can go ahead and click&nbsp;&nbsp; play. It's going to evaluate live right&nbsp; in front of us for a total score of 2.8,&nbsp;&nbsp; which is not amazing but&nbsp; still a decent enough job. This is how you use the Docker&nbsp; deployment for live evaluation&nbsp;&nbsp; using Nemo Evaluator. If you want to learn more,&nbsp;&nbsp; you can check out the links to documentation&nbsp; and other resources in the description below.&nbsp;&nbsp; Thanks so much for your time. I've been&nbsp; Chris, and we'll see you in the next one.
