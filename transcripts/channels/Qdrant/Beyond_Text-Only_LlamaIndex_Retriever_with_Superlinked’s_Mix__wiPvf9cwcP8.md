# Beyond Text-Only: LlamaIndex Retriever with Superlinked’s Mixture of Encoders | Filip Makraduli

- 영상 링크: https://www.youtube.com/watch?v=wiPvf9cwcP8
- 채널: Qdrant
- 업로드일: 2025-10-28
- 자막 언어: en
- 단어 수: 약 2781개

---

## 스크립트

So luckily we can go even beyond um text and images. So that's slightly an intro of what this talk is about. Um this is me. I am Philillip. Uh I am a devil engineer a background in machine learning did a bit of data science a bit of uh research in like biomedical AI and now I'm at superl working in uh search and kind of accurate retrieval. So what even is super linked? Um many of you maybe have heard of superl haven't. Uh some have heard of it but don't know what exactly it is. Um, are we a vector database? Um, no we're not. Are we a framework? Probably yes. And are we an embedding model? Well, we are, but not maybe in the conventional sense, but we can generate embeddings. So, I guess in a way we're an embedding model that can construct your embeddings. And how do we construct these embeddings? We use this concept of uh mixture of encoders. So the idea is that you would use different types of encoders to generate your embedding. And this embedding would be one long vector which has different sub vectors within it that can encode different aspects like temporal elements like time, recency, user interaction data, images, text, uh different sorts of metadata, numbers. So it goes beyond just image and text. And this gives you the ability to actually do accurate retrieval by biasing this sub vectors and being able to actually cut out the re-ranking steps, the post filtering, the boosting, the fusion uh rank fusion and integrating stuff in different queries. So it's a very cool powerful concept. So we do have an open source repo. you can try it out and test things and that's all cool, nice and well. However, um users want to try different things and build different things. So, basically this is uh the title of my talk and the next step beyond just having a repo and having this concept live is how do you put that concept out there so people get to try things out and build more with it. So that's why I'm today mentioning our integration with llama index which would help you actually use this um retrieval concept in your agents in your rag in your pipelines with lama index and also um lang chain. So the idea today is how can you I use mixture of encoders in my work with um whatever I'm doing that's a bit of agentic or uh related to some more complex workflows. So that's the reason why we did this with lama index and lang chain basically to make this concept more accessible to let the people know okay this is something you can try out and it works and we're happy for people to try things out. So this is llama index. You know about it probably. Uh it has this structure where it has documents and notes like the key kind of data structure of the library and um you can also have indices and retrievers. So what we do is the retriever part. So basically we've implemented custom retrievers for llama index. So now I'll go through the process a bit on how we've done this and kind of what are the requirements. Um so the key implementation points are the following. So you need a specific return format where you need to have a list of nodes which with with scoring. So that's how llama index works. Um and you have a retrieve method that you kind of need to implement yourself. um it needs to accept a query bundle which uh contains the string and the metadata stuff and you can implement callback support and async stuff if you want but that's not essential. Um so the main thing is that you have this base retriever um in the llama index library and you just need to inherit from this. So that's the first step uh in which you implement a custom uh retriever and the input would be this query bundle and the output as I said this uh list of node with score and this is also part of an article that we did where we look through a specific use case around um search through steam games so you can actually try things out and have a look at like a full endto-end example and you need to implement the abstract method. Basically, this is the code to kind of roughly do that. Uh, and that's the key kind of driver when you want to do a custom retriever. And this is roughly how the architecture looks like. So you can see that you have the user application above that has the user query that goes into the query bundle and then that goes into the actual superl retriever implementation. So we have this superlin um app which is the query client and the query descriptor um and then that goes to the superlink framework where kind of we we do our stuff. Um and basically as you can see also on the right part um you you have the base retriever the one that you have to implement and then the text node and the node with score because you also want to kind of have a way of ranking your results and have that thing in mind. So this is the query bundle. Then we have as I mentioned this integration and as you can see the um line that's like the dotted line it just means you kind of have to reimplement that part. Um and that there is this inheritance relationship. Uh the other parts are like data flow. Um and that's that's the general idea. This is how uh the llama index score works. As I said, the key kind of the key thing is to remember to have the right input and output and then you can implement your stuff. Um, and that's kind of how we approach things. Um, and yeah, this goes back again to the user query. Um, and it's also important when building stuff um, especially in search to have of course to encode the query in the same way as your data. But that goes a bit beyond of how you would implement a custom retriever. And this would be the final product. So you can find this uh in llama hub. You can do a pip install. So this whole process the way you contribute to llama index is that you it generates a pi package that you can just install. So feel free to do this. There are some tutorials on this but we're building more stuff around this. Um and we would be happy to see people to try this out, test it, build some things with it. And then uh as this grows, we will build more stuff, more functions, more um kind of ideas on how to actually integrate a mixture of encoders with all the all the other things in the ecosystem. So this is kind of the basic uh overview but what else can you do that might be a question. So I think this is like where I um speak more directly to you where I tell you okay this this is the basic implementation but this is where I'd want you to build stuff and maybe expand this idea. So one example is you can do agentic workflows in lama index. So that's kind of their new way um of doing things. So have like a multi-turn conversation and have like some form of persistent state management because initially you wouldn't have that. So you want uh if you want a more com complex agentic workflow or something of that sorts um you need that multi-step nature but also the ability to have persistent state. And you can use the custom retriever that we've built in workflows like this. Um, so yeah, these are some examples of how you might do that. Like some toy demo examples. Obviously, in a specific use case, this would be much more complex and slightly different. Uh, but it's a cool concept to have in mind because uh, LM index provides this. Um and it's uh quite an interesting uh use case especially for a gentic stuff and as we know the retrieval part is um a key aspect to agents like eventually you would end up with the retrieval problem whatever your workflow is you would most likely need to have accurate retrieval. Um we also have an equivalent langchain integration. So this is also a pi package you can install and the difference is um that here you inherit from their base retriever also and there is u the client and query so it's slightly different um and of course all of these use cases. So for example, the lang chain implementation uses the default in-memory index implementation where you use um the in-memory index instead of a specific vector database. But then after this you can kind of hook up your own uh quadrant and uh build on that. Uh this is just how um the specific implementation is done. And you need the schema space definition down there in the bottom. That's the part about superl where you define basically this custom encoders that I spoke about. So these are uh we call them spaces. So if you want to encode some user interaction data, if you want to encode some uh temporal elements, some numbers, some images, you would use an image space, a number space, etc. Um and you can also supply your own custom models as encoder. So we have this functionality too. So you can say okay I will just supply my vectors and use them as part of this mixture encoders architecture. So you can derive them by hand if you want and then have a space that would kind of supply those um those encoders and then generate the vector. um and what you can maybe find as a starting point of um pre-built work or work that can inspire you to test this out and go beyond. Um we have these two articles that um explain a bit um kind of some stuff around the integration and what we've done and how we've made this work. The llama index one is around steam games. So you can see the example test queries. I'm looking for a strategic sci-fi game. What's a good uh cooperative game for teamwork? Stuff like that. And of course you can combine um just you can go beyond textual queries. So it doesn't have to be just text. Uh as you can see for example in the lang chain example which is around uh retrieving uh financial reports like 10k 8k and so on. So it uses a data set like like that. You can see a lot of these examples have this waiting. So what that means is that using superlengths uh mixture of encoders when you end up with the final vector embedding, you can bias your search results for what you're trying to actually retrieve. So that would be useful if you're trying to see okay which companies can I bias more towards revenue or I care more about um revenue and net income and so on. So this can be if you're building a different use case, it can be stuff around pricing, stuff around location, stuff around how um maybe people have interacted with that or popularity. So the idea is that if you want to have something that's a bit popular but also has a good rating, you don't want to do like multiple rankings, multiple steps, then fusion of queries and so on, but have a way to do one step and retrieve the right context initially. So we think that's a very useful thing you can have and we want to make that happen for your agentic workflows for your um rag pipelines and that's why we've extended this to lama index and lang chain and this is the link um to one of the articles the lum index one with the steam games I think uh you'll get access to this so you'll be able to actually click this uh and we have another one as I said with uh lang chain. Um so this is um how um the the architecture of that article goes, but you can find this in more details in the actual pipe package that implements the the lang chain custom retriever. And one big question is like okay cool this sounds nice but why should I even use this packages? Why should I even test out mixture of encoders? Uh what we do have is we've done a benchmark. We've done our custom data set which has um this multimodal aspect. So basically all of the benchmarks that are out there like MTB um beer and so on. They're text only. So ret retrievers compete on text. And what we're trying to say is that if you have metadata also, if you have images, um, numbers, other aspects of your data, you're kind of doing it a disadvantage to to the whole process by testing it out only with a text embedding model. So, we created a data set that is e-commerce-based and combines these queries where you have price included, where you have more complex descriptions, more simple descriptions. Um, and I did a talk, maybe some of you were at Haststack a few days ago, kind of diving deeper into this, but the idea is that you want to test your retrieval models in this um situation where there is a realistic problem in mind. So what happened is that we got uh these numbers. This will be available on our landing page probably in less than 24 hours from today. Um, so we've tested with Vert.Ex X AI and their filters with Azure with the bottom one is only a text based um encoder when you need to retrieve stuff that includes metadata the performance is low and we've also tested um kind of natural language querying and u parameterizing the query in order to make uh make them performance. So we didn't want to give a disadvantage to Azure to Vert.Ex text and uh them saying oh you didn't use us in the right way we just picked the highest score but this was the case so that's why we believe in such use cases that include different sorts of data that's beyond text beyond text and image you could benefit of this mixture of encoders approach and we want you to to try this test things out and uh tell us how it goes um these are some useful links So you can see the integrations there, the llama index and the lang chain one. This is a link with a few useful stuff. Uh one of them is an article explaining this mixture of encoders concept in more detail and how you can retrieve based on um all of this aspects all together without needing to rerank fuse queries and so on. Um and we also do on LinkedIn we do office hours. So um you'll you can check this out on our LinkedIn page but basically we have um like um video call chat where we talk about all things around retrieval your problems around this pipelines like it doesn't necessarily have to be about superl but um the whole kind of area of u search and how you build your agents around search how you generate your embeddings what are the problems that you have so we're happy to chat to you um about this um always and also I'm personally happy to chat to you. So feel free to add me on LinkedIn, talk to me about potential integrations, about u what we've done well, what we haven't done well. So happy to listen to your comments. So feel free to connect and um let me know how how it's going. Maybe there's a functionality or an integration that you think you'd benefit from and we're happy to look into this and uh build it. So I guess this is uh the announcement for lunch. So thank you for listening to my talk. Thank you for attending and feel free we have also a booth and we'll have a lightning talk later because we were part of the hackathon. [snorts] So stay tuned, talk to us at the booth and thank you.
