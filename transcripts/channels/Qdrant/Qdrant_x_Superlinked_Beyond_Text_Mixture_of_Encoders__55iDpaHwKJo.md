# Qdrant x Superlinked | Beyond Text: Mixture of Encoders

- 영상 링크: https://www.youtube.com/watch?v=55iDpaHwKJo
- 채널: Qdrant
- 업로드일: 2025-10-22
- 자막 언어: en
- 단어 수: 약 1087개

---

## 스크립트

Hi, I'm Phillip. I'm an ML engineer at SuperL and we are an open- source framework um in Python that solves the problem of accurate retrieval in AI search um and recommendation systems. We have an open source repo which you can try. Um so we have a quick start and a lot of notebooks. Um but the quick overview would be this. So we basically sit as a layer on top of uh vector databases and um we do different types of encodings depending on different data types. So um what this means is that with quadrants um we were able to build very cool use cases and we have a deep dive article on it. So we have this um hotel search example that you can have a look at. Um, so we handle complex queries, um, like this one, affordable luxury hotels near the Eiffel Tower with lots of good reviews. And here you can add free parking or price below $200 and so on. Um, and that enables you to do things like this where you can ask a query that has a lot of um different contexts on different data types. For example, cheap is related to pricing. So, you would need to include pricing data. Highly rated um hotels is related to rating. So you would need specific data related to ratings also London to location then there is negation and also if you add price if you add categories if you add images this can get quite complex uh especially with this complex queries. So what we do is we have this approach of mixture of encoders where depending on the data type we use a different encoder to encode that data and then aggregate everything into one vector where we concatenate the embeddings generated by each encoder. So for numbers we use a numeric encoder. for categories um we use a categorical encoder, a text encoder, image encoder and so on. So we have lots of different types of encoders that then we mix we encode the data and generate one concatenated vector which has um this embeddings um this sub embeddings that relate to different aspect in the data. And um in the hotel demo, this means that when you also search for something like cheap, we can weigh this sub vectors generated by different encoders. So here you can see that the price weight has a weight minus one. So we were able to recognize that cheap relates to lower price. So we're able to bias the ranking of the retrieved results towards this. and we do purely vector search. So that's why quadrant has been very useful because um we aim to think that the problem of accurate retrieval can be solved if you generate good enough of embeddings and also um we have a lot of um integrations. So you can have a look in our core repo. So we have um this quadrant connector you can find it in frameworks and storage. So this is a mix of um query translation. So we have um the quadrant query query builder basically that um translates the superl um internal um code basically the superlinks internal search parameters into quadrant specific ones. Then we have search execution. Then we have data mapping of course um where there is some um entity data transformation between the superl format and quadrants um I think point structure that's what's it called. So we have also index management. So superlink manages the quadrant index. Um so you can have a look at um all of these here in in different in different files. So um the search and the index management that I mentioned and in an applied scenario in the hotel search example that I mentioned, you would look into something like this where you would need to install if you work with um the super linked server, everything is bundled in together. So you just need to install this. However, if you're using um like a notebook to run quadrant, you would need to install super linked and then brackets quadrant in order to run um run things. And this is the example of the hotel demo that I just showed. We also have a deep dive with quadrant where I explain in more detail how that works. So feel free to have a look at that and also look at the article that I mentioned that also explains the core components here and this preference and in the hotel search we have um a more applied use case of quadrant. So you can see that in the config we set up the URL and the API key and there is an example config that you can use. Um we also have the API and this is the quadrant vector database class which is implemented um in the code um and the section that I showed previously with all the uh query translation and connector building and our collaboration has been very useful where we were able to look at a lot of um different use cases in search and recommend recommendations and you can go to our repo. So here this is your one-stop um contact or onetop point for superl where you can have a look at also at our docs. So we have a lot of resources that would help you build with uh superl and how we run in production. So we do everything from schema design to a custom DSL to then um there ways to deploy this as an API and then we have a cloud offering that you can build on top. So it's a it's a nice combination of resources that you can find um in our docs but the repo has a lot of uh resources in itself. So this is the minimum quick start example and then there are a lot of um notebooks here that you can learn to to build stuff with super linked and then run them in production with quadrant. So I'm happy to answer any questions. Feel free to try these notebooks and learn how to build recommendation systems and how to use this approach of mixture of encoders to generate uh better embeddings. And then when you're ready, when your embeddings are generated, you can run things with Quadrant Cloud in production. And I'm excited to see what you can build uh with your AI search and recommendations use cases. Thank you.
