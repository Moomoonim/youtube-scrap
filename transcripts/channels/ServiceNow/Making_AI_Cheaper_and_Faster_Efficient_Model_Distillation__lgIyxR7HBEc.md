# Making AI Cheaper and Faster: Efficient Model Distillation

- 영상 링크: https://www.youtube.com/watch?v=lgIyxR7HBEc
- 채널: ServiceNow
- 업로드일: 2026-04-01
- 자막 언어: en
- 단어 수: 약 2053개

---

## 스크립트

Welcome everyone to a brand new AI research bite. Thank you so much for joining us today. My name is Marylis and I will be your host for today. For those of you who are new here, the AI research bites is a series of short informative talks where we showcase cutting-edge research from ServiceNow's AI research team. These sessions are open to everyone, especially those of you who want to stay connected with the fast-paced moving world of AI research. Today's session features a 15-minute talk from Raymond Li and the presentation will be followed by a 10-minute Q&amp;A. Before we dive in, let me share a few words about today's speaker. Raymond Li is a senior research developer here at ServiceNow AI research. His research focuses on foundation models, more specifically on model efficiency. With that, I'm thrilled to hand things over to our amazing speaker, Raymond. The floor is yours. Oh, you're on mute. Yes, that's better. You hear me? Yes. Okay, thank you, Marylis. Yes, so today I'm going to talk about efficient model distillation. Or in other words, how we work to make AI cheaper and faster. So just everything I'm going to talk about today is research work. Okay, so one thing Core LLM and the Foundation Models Lab have collaborated on recently is the April model family. Last year we have released three models, April Nematron, April 1.5, and April 1.6. The three are great reasoning models, especially given their moderate size. The latest April 1.6, which is an artificial analysis index of 57. Which is like really amazing for its size. They are pretty typical language models, but they've been trained with exceptionally well-curated data. Which is what makes them very good at reasoning. Transformer language models, so large language models, are behind all the modern AI systems, from customer service chatbots to code assistants. But there is a a hidden tax. The longer the conversations, the more expensive and the slower they become. So the the first token you might generate with a model like this will be relatively cheap, but the more tokens you generate and and the more expensive it becomes. And I'll explain why in just a bit. But just before I want to emphasize the point that emphasize why we focus on making these models faster and it's not just about receiving your tokens in 0.1 second instead of 0.2 seconds. This speed, especially for longer sequences, creates capability. If you think of your agents, for example, that means you can have more tool calls before your memory has to be compressed. Or in reasoning models, that means you can have longer and deeper reasoning chains and thus better accuracy. For retrieval augmented generation, you can have larger contexts. And for reinforcement learning, you can get more rollouts per dollar spent on infrastructure. So that means you have denser feedback and better policies in your model. So by making the models faster, we also make them cheaper and better, actually. So yeah, this is like a very important point to to understand and to analyze for any organization that that wants to to use and understand these AI systems. So to give some background on why I say that transformers are so slow. So the the transformer is the architecture behind most state-of-the-art models and it uses what we call full attention. So these language models represent text as a sequence of tokens. You can think of tokens as, for example, words in a vocabulary. And the role of the full attention is to learn how to make connections between all the pair of words in a sequence. So when the new word comes in, the the attention mechanism tries to stand out which parts of the previous sequence is relevant. Um And so as you can imagine, as your sequence grows, the number of pairs of words in that sequence grows quadratically. Um When we serve these models, we use a key-value cache. That's to avoid recomputing previous states. So it saves computation at inference by storing the previous keys and values. This cache grows linearly with the sequence length. So that at each new token you have to store new keys and values. And at every time step, you'll have to to load this KB cache again in your GPU memory. And as your sequence grows, this this cache becomes a memory bottleneck. It puts increasing pressure on your GPU memory bandwidth. Um And what happens is that basically your GPU spends most of its time just moving data around and loading this huge key-value cache in its memory instead of doing actual computation that's useful. So this is really something we want to avoid. And one solution that's been proposed in research is efficient mixers. So a mixer is just a module that's able to operate on the sequence level. So full attention is one example of mixer. But as we just said, it uh uses a KB cache and is becoming increasingly slow. And so another class of mixers, which is efficient mixers, um like Mamba, DeltaNet, or others, eliminates this growing KB cache by maintaining a fixed-size state. For those of you who are into machine learning for long enough, you may probably remember, for example, recurrent neural networks. And so these more recent architectures like Mamba are are in the same let's say class of algorithms where they use this forget, write, read templates. So all these architectures they use a fixed-size state. And what they do is they learn how to forget some part of that state at each time step in your sequence. How to write to that state also given the current input. And finally, how to read that state to output for the next layer or for the output of the model. These efficient mixers have many benefits. So they remove the KB cache. This means they have lower memory usage and much faster inference compared to full attention. They have some drawbacks, however. Capabilities like retrieval and lookup are degraded due to the compression of the state. And they are also more prone to long-range forgetting. That's why in most approaches recently, we try to train hybrids between efficient mixers and full attention. Um So the idea is that the efficient mixers will make the model faster. And retaining a few full attention layers allow us to kind of keep these crucial capabilities like looking up far away the past. And so that's also the approach that will show today. And so this uh is just to maybe visualize a bit by what I mean when generation is getting slower and slower as your sequence grows. And so if you were to plot the latency per token as a function of your sequence length, then an efficient mixer here in green would keep a constant latency, whereas a transformer that uses full attention will just have its latency that that shoots up. So to to come back to what we were doing this project, so we have this great model, which is April 15B Thinker. And we want to make a hybrid version of it that is faster. We don't have the luxury of doing pre-training for trillions of tokens. And so the approach we choose to use is distillation. So, the distillation is a technique that's usually used to learn the behavior of a larger model, so the teacher, in a smaller model, the student. In our case, the student is not a smaller model, it has the same number of parameters, so 15 billion, but it uses a different architecture. So, some of the layers from April are replaced, so they are full attention and in the hybrid we'll replace these layers with Mamba layers. And so, our goal here is to make the model as fast as possible while preserving the quality of the original model. And we ask two questions in this um in this project. So, first is how many full attention layers should remain? And the second is the placement, so which layers in the 50 layers of the April model should remain full attention and which should be converted to Mamba layers. And so, just to yeah, reiterate our goal here is that yeah, we want to replace as many layers as possible to make the model faster, but also we see that these layers tend to they have a risk to degrade performance and so, the goal is to find the right balance between the two. Previous research in this area used smaller models and gave very promising results trying to distill a full attention model into a more efficient version of that. But at our scale of 15 billion parameters, we found that a normal distillation approach wouldn't give satisfying results. So, our approach was a three-phase distillation approach. In the first phase, we do static scoring, so we have a heuristic that allows us to rank the importance of the different layers in our model. So, the April model has 50 layers and we rank these layers by order of importance. And what we say is that the least important layers we can safely replace those with Mamba. And so, what we see is that we can replace 25 layers with Mamba layers and keep the performance relatively close to that of the teacher. And if we try to replace more layers than that, then the performance drops really quickly. In the second phase, we do stage distillation. So, here instead of replacing 25 layers at once, we will do smaller batches. So, we replace two or three layers at once, then re-distill the whole model to make it behave like the teacher, and then repeat the process. So, from the H25 that has 25 Mamba layers, we go to H27, H30, etc. all the way to H40 with 40 Mamba layers. And in a third phase, we do just regular supervised fine-tuning on reasoning data. The goal is just to um yeah, improve the performance of the model on reasoning tasks where the teacher shines. Now, here are our our results with our hybrid of 30 Mamba layers, which we found was a good trade-off between speed and performance. So, with 30 Mamba layers, our hybrid uh is twice as fast as the teacher model. So, here in green on the graph. Um So, notice here the output sequences are up to 16,000 tokens. And if you go to sequences longer than that, you get even larger speed-ups with the hybrid model. And in terms of benchmark performance, so our hybrid model here in green performs very closely to the teacher on most of the benchmarks here. So, so this is yeah, so we are really happy with those results because that means our model keeps the same performance, but it's twice as fast. And this final plot is to show the trade-off that exists between model performance and throughput. So, if you have performance on the Y axis and throughput on the X axis, you see that the the base model is in top left here. So, it's very good at benchmark performance, but it's a bit slow. And as we go through the distillation and replace more and more full attention with Mamba layers, we see that throughput increases a lot. However, there is a slight drop in performance that we see. So, it's really a trade-off between the two. So, to conclude, we we released April H1 model that we distilled with our three-phase distillation procedure. The model is twice as fast as the teacher model, which is April 15B ticker. thinker. And it reaches similar performances on benchmarks. In future works, which is actually ongoing currently, we look into supernets. And so, here I think the the the goal is really to put the emphasis even more on this trade-off between performance and speed. Because by serving this one supernet, we are able to um to serve different points on that trade-off. So, you could either query for a model that's really intelligent, but maybe a bit slower, or you could also, maybe for easier tasks, use a model that's really, really fast, or maybe something in between. So, yeah, that's it. Thank you.
