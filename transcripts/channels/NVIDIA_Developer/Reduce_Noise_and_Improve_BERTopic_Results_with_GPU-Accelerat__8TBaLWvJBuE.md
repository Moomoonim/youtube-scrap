# Reduce Noise and Improve BERTopic Results with GPU-Accelerated Topic Modeling

- 영상 링크: https://www.youtube.com/watch?v=8TBaLWvJBuE
- 채널: NVIDIA Developer
- 업로드일: 2025-06-16
- 자막 언어: ko
- 단어 수: 약 1651개

---

## 스크립트

안녕하세요 여러분, 오늘 가속 데이터 과학 시리즈의 일환으로 진행되는 토픽 모델링에서 노이즈 클러스터를 최소화하는 혁신적인 접근 방식에 대한 세션에 오신 것을 환영합니다 . 이번 세션에서는 노이즈 클러스터를 줄이고 토픽 명확성을 향상시키며 NVIDIA GPU를 활용하여 처리 속도를 높이는 더욱 스마트한 토픽 모델링 접근 방식을 살펴보겠습니다. 수천 개의 문서에서 주요 내용을 찾아내려고 하는데, 그중 절반은 그냥 잡음이라고 상상해 보세요. 이 때문에 진정한 통찰력을 얻기가 어렵습니다. 이 슬라이드에서는 버드 토픽 모델이 생성한 토픽 분포를 확인할 수 있습니다 . 특히, 데이터의 50% 이상이 '음성'으로 표시된 노이즈 클러스터에 할당되었습니다 . 이러한 노이즈 클러스터는 일반적으로 식별된 주제와 명확하게 일치하지 않는 문서를 포착하므로 귀중한 원천 데이터의 상당 부분이 활용되지 못하게 됩니다. 이러한 상황을 해결하기 위해, 주제의 일관성과 다양성을 유지하면서도 노이즈를 크게 줄일 수 있는 방법을 소개합니다. 이 슬라이드에서는 기존 방식과 저희 방식이 생성한 클러스터 분포를 비교했습니다. 오른쪽에 제시된 접근 방식은 데이터의 60%를 활용하는데, 이는 왼쪽에 제시된 기준 접근 방식보다 23% 증가한 수치로, 데이터 보존율이 크게 향상되었습니다. 이를 통해 더욱 조밀한 클러스터 분포에 반영된 더 많은 주제를 발견할 수 있습니다 . 추가된 주제들은 기존 접근 방식에 비해 세부적인 내용, 주제적 다양성 및 의미적 다양성을 향상시킵니다 . 어떻게 해냈는지 궁금하실 수도 있겠죠. 본질적으로 우리의 목표는 세 가지 핵심 측면을 최적화하는 것입니다. 일관성, 다양성 및 노이즈 감소. 이것들은 서로 어울리지 않는 경우가 많습니다 . 따라서 우리는 방정식 에 나타낸 바와 같이 가중 계수를 사용하여 균형을 맞추는 일반적인 목적 함수를 도입합니다 . 우리의 목표 함수는 세 가지 지표의 선형 조합입니다 . 우리는 일관성과 다양성이 높은 모델에 보상을 제공 하지만, 노이즈로 분류된 데이터 비율이 높은 모델에는 불이익을 줍니다. 이 용어는 그 과정에서 귀중한 데이터가 너무 많이 손실되는 것을 방지합니다. 그런 다음, 하이퍼파라미터 최적화(HPO)를 통해 최적의 구성을 탐색하는 가속 머신러닝 모델을 사용하여 이 함수를 최대화합니다 . 이 모든 것은 확장성과 속도를 보장하기 위해 NVIDIA의 QML을 사용하여 구현됩니다. 본 연구에서는 실례를 보여주기 위해 IMDb 영화 리뷰 데이터셋을 사용하여 조류 주제에 본 방법을 적용했습니다. 이 데이터셋은 본 접근 방식의 유효성을 검증하기에 현실적이고 노이즈가 많은 환경을 제공합니다 . 그럼 새에 대한 주제 소개를 시작해 볼까요? 버드 토픽은 대규모 세금 데이터 세트에서 토픽을 식별하고 구조화하기 위해 트랜스포터 임베딩을 사용하는 토픽 모델링 기법입니다 . 단어 빈도 분포에 의존하는 LDA( 잠재적 디레이트 할당)와 같은 기존 토픽 모델링 기법과는 달리 , BERT, robber 또는 disvert와 같은 언어 모델을 학습시켜 문서 임베딩을 생성함으로써 문맥을 인식하고 교실과 유사한 주제에 대해 더 정확한 결과를 도출할 수 있습니다 . 다이어그램에 제시된 워크플로를 따릅니다 . 턱 텍스트는 버드 로버트와 같은 변환기를 사용하여 밀집 벡터 표현으로 변환됩니다 . 그러면 um mapap과 htbcn을 포함하여 두 개의 인스턴스가 생성됩니다 . Um은 더 나은 클러스터링을 위해 고차원 임베딩을 저차원 공간으로 축소하는 데 사용되며, http scan은 미리 정의된 클러스터 수 없이 자연스러운 주제 클러스터링을 찾는 데 사용됩니다. Bird 토픽은 두 가지 사례를 입력받아 클러스터로부터 해석 가능한 토픽과 시각화를 생성합니다. 예시를 통해 새 관련 주제 코딩을 함께 살펴보겠습니다. 이 예시에서는 IMDb 영화 리뷰 데이터 세트의 토픽 모델링에 '새'라는 토픽을 사용하는 방법을 보여줍니다 . 먼저 데이터 세트 라이브러리를 사용하여 데이터 세트를 불러오고 세금 검토 결과를 추출합니다. 기본 차원 축소 및 클러스터링 방식 대신, UMAP을 이용한 차원 축소와 HTTP 스캔을 이용한 클러스터링을 통해 성능을 향상시켰습니다. UMAP은 5개의 구성 요소와 15개의 이웃으로 구성되고, HTTP 스캔은 최소 샘플 크기가 10으로 설정됩니다. 이러한 모델은 bird 토픽으로 전달되어 클러스터링 품질과 확장성을 향상시키는 맞춤형 파이프라인을 구성합니다. 그런 다음 모델을 데이터 세트에 맞춰 적용하고 주제와 확률을 추출합니다. 마지막으로, 내장된 시각화 도구를 사용하여 발견된 주제를 시각화합니다. 이 주제 간 거리 지도에서 각 원은 클러스터를 나타냅니다. 두 클러스터가 멀리 떨어져 있으면 서로 다른 클러스터입니다. 두 단어가 가깝다면 의미가 겹치는 부분이 있습니다. 또한 '주제 정보 가져오기' 기능을 사용하여 주제 ID와 해당 주제 키워드를 추출할 수 있습니다. 다음으로, 우리 접근 방식의 가장 중요한 구성 요소인 목적 함수에 집중해 보겠습니다. 이것이 왜 중요할까요? 토픽 모델링, 특히 노이즈가 많은 실제 데이터의 경우, 결과물의 품질은 최적화하려는 기준에 따라 좌우되기 때문입니다. 일관성, 다양성 및 노이즈에 대한 가중치를 명시적으로 제어하지 않으면 모델은 주제 품질, 용기 및 데이터 보존 간의 균형을 효과적으로 우선시할 수 없습니다. 우리의 접근 방식은 높은 일관성과 다양성을 유지하면서 동시에 노이즈로 빠르게 변환되는 데이터의 비율을 최소화함으로써 이 기능을 최적화합니다. 우리는 하이퍼파라미터 최적화(HPO)를 통해 이를 달성합니다. HPO는 다음 함수를 최대화하는 구성을 탐색합니다. 이 목적 함수에서 일관성은 식별된 각 주제 내 단어들의 의미적 유사성을 평가하는 지표입니다 . 다양성은 식별된 주제들이 얼마나 서로 구별되고 과도하게 중복되지 않는지를 측정하는 지표로서, 다양한 사안에 대한 보도를 촉진하는 역할을 합니다. 노이즈 감소는 클러스터링에서 과도한 노이즈에 불이익을 주는 지표로, 관련성 이 없거나 일관성이 없는 주제를 최소화하는 데 도움이 됩니다. 임계값은 클러스터링 결과에서 페널티가 적용되기 전에 허용되는 최대 노이즈 백분율 목표값입니다. Omega C, Omega D 및 Omega N은 최적화에서 각 항의 상대적 중요도를 제어하는 ​​계수 또는 가중치입니다 . 이러한 값들은 매트릭스의 우선순위와 클러스터의 원하는 품질을 기반으로 미리 정의됩니다 . 토픽 모델링 품질을 얻기 위해 목적 함수를 구성하고 적용하는 방법을 예시를 통해 살펴보겠습니다. 가중치를 오메가 c = 0.4, 오메가 d = 0.3, 오메가 n = 0.3으로 설정하고 노이즈 임계값을 20%로 설정하면 다음과 같은 목적 함수가 생성됩니다. 각 구성 요소가 최종 점수에 어떻게 기여하는지 살펴보겠습니다. 전처리 단계에서는 HTML 텍스트와 알파벳 이외의 문자를 제거하고, 텍스트를 소문자로 변환한 후 단어로 토큰화합니다. 이는 분석 전에 텍스트를 정리하는 데 매우 중요합니다 . 노이즈 페널티 계산은 노이즈 클러스터에 할당된 문서의 비율을 계산하고, 이 비율이 20% 임계값을 초과할 경우 페널티 점수를 계산합니다. 이는 노이즈가 많은 클러스터를 너무 많이 생성하는 모델에 불이익을 줍니다. 주제어들이 얼마나 의미론적으로 의미 있는지를 나타내는 응집성 지표를 계산합니다 . 다양성 계산은 주제별로 단어의 고유성을 평가합니다. 다양성이 높을수록 반복이 적어 더욱 풍부한 주제 모델링이 가능해집니다. 학습 및 평가 단계는 파이프라인의 핵심 역할을 하며, 지정된 매개변수를 사용하여 um map과 HTTP scan을 초기화하여 차원 축소 및 클래스 분류를 수행합니다. 그런 다음 제공된 문서에 대해 주제별로 자동으로 주제 할당을 생성합니다. 그 후 해당 함수는 세 가지 핵심 평가 지표인 일관성, 다양성 및 노이즈 페널티를 계산합니다. 최종 점수는 04 * 일관성 + 0.3 * 다양성 - 0.3 * 노이즈 페널티라는 공식을 사용하여 계산됩니다. 이 페널티는 모델의 의미 품질과 노이즈에 대한 견고성을 나타냅니다 . 다음은 하이퍼파라미터 최적화(HPO)입니다. 우리는 다음과 같은 하이퍼파라미터에 걸쳐 HPO 검색을 활용합니다 . N개의 구성 요소는 차원 축소를 위한 UMAP 구성 요소의 개수입니다. N개의 이웃은 클러스터링에 고려되는 이웃의 수입니다 . 이는 UMAP에서 두 지점 사이의 최소 거리를 의미합니다. 평균 샘플은 HTB 스캔 클러스터링에서 최소 샘플 수입니다 . ` Gen mean span tree`는 최소 신장 트리를 생성할지 여부를 나타냅니다. 예측 데이터는 예측 데이터를 모델에 추가해야 하는지 여부를 나타냅니다. HPO 검색은 목적 함수를 최대화하기 위해 이러한 매개변수를 최적화합니다 . 이제 Optuna를 사용하여 UMAP 및 HTTP 스캔의 매개변수를 미세 조정하여 B 토픽에 대한 최상의 클러스터링 및 토픽 모델링 결과를 얻는 하이퍼파라미터 최적화(HPO) 코딩 단계로 넘어가 보겠습니다. 먼저 목적 함수를 정의합니다. 함수 내부에서는 지금까지 탐색하고자 하는 하이퍼파라미터 범위를 지정합니다 . 여기에는 UMAC 구성 요소 수, 이웃 수, 최소 거리 및 평균 샘플과 같은 HTTP 스캔 매개변수가 포함됩니다. 또한 두 가지 범주형 옵션인 J 평균과 예측 데이터를 포함합니다. 이러한 범위 와 선택지는 Optuna가 탐색할 수 있는 포괄적인 검색 공간을 제공합니다. 다음으로, 목적 함수 내에서 이러한 하이퍼파라미터를 전달하여 핵심 평가 함수인 `train`과 `eval`을 호출합니다. 이 함수는 일관성, 다양성 및 노이즈 페널티를 기반으로 한 가중 점수를 반환합니다. Optuna는 이 점수를 사용하여 각 매개변수 세트의 우수성을 판단합니다. 그런 다음 optuna.create study를 사용하여 연구를 수행합니다 . 우리는 점수를 최대화하고 연구에 이름을 붙이고 싶다고 명시합니다. 이 경우에는 옵투나 새에 관한 주제입니다. 재현성을 위해 고정된 종자를 사용하는 샘플러를 사용합니다. 마지막으로 study.optimize를 사용하여 총 40회의 시뮬레이션을 실행하여 최적화 프로세스를 시작합니다. 각 실험은 고유한 매개변수 조합을 평가하며, Optuna는 자동으로 최적의 구성을 추적합니다 . 최종 출력 결과에서 Optuna는 최적의 시험 매개변수와 해당 점수를 표시합니다. 이를 통해 UMAP 및 HTTP 스캔에 대한 최적 설정을 얻을 수 있으며, 이를 사용하여 출생 토픽 클러스터링에서 가능한 최고의 성능을 달성할 수 있습니다 . 이제 결과를 비교해 보겠습니다. 기준 비교를 위해 임시 조류 주제 모델링을 적용하고 일관성, 다양성 및 노이즈 비율이라는 세 가지 주요 지표를 기준으로 테스트했습니다. 본 연구에서 제시하는 접근 방식을 사용하면 노이즈로 표시되는 데이터의 양을 60%에서 40%로 줄일 수 있으며, 이는 거의 20%의 노이즈 감소 효과에 해당합니다. 이 표는 세 ​​가지 지표를 비교한 것입니다. 이번 실험에서 일관성 및 다양성 점수는 약간 감소했지만, 잡음 비율은 상당히 감소했습니다. 이러한 잡음 감소는 우리의 접근 방식이 일관성이나 다양성을 크게 손상시키지 않으면서 더 깔끔하고 해석하기 쉬운 주제 구조를 우선시하여 세 가지 지표의 균형을 효과적으로 유지함을 나타냅니다 . 제시된 시각화 자료는 기준 모델과 본 논문에서 제안하는 접근 방식 모두의 내향적 거리 지도를 보여줍니다 . 기준 시각화는 노이즈 제거 후 남은 37.91%의 데이터를 기반으로 한 주제 분포를 반영합니다. 반면, 본 연구에서 제시하는 접근 방식의 시각화는 나머지 60.81%의 데이터를 기반으로 구축되었으며, 이는 사용 가능한 원본 데이터가 약 23% 증가했음을 의미합니다 . 본 연구에서 제시하는 접근 방식의 시각화 결과는 기준 접근 방식 과 비교하여 노이즈 감소 전략을 활용하여 의미 있는 주제 발견을 극대화함으로써 데이터 환경을 보다 포괄적으로 탐색할 수 있음을 보여줍니다 . 이제 NVIDIAQML을 사용한 GDU 가속 성능에 대해 구체적으로 이야기해 보겠습니다 . 여기 보이는 것은 10만 건의 제품 리뷰 데이터 세트를 기반으로 B 코픽 잉크 이송의 전체 실행 시간을 비교하는 벤치마크 결과입니다. 왼쪽은 모든 작업이 CPU에서 실행되는 기준선입니다. 이 설정 과정을 완료하는 데 1,500초가 걸립니다. 중간 부분에서는 본사에서 트랜스포머 임베딩만 GPU로 옮기는 과정에서 개선된 점을 확인할 수 있습니다. 이것만으로도 실행 시간이 단 900초로 단축됩니다. 하지만 진정한 가속은 NVIDIA QML을 통해 GPU 쿼치와 GPU 가속 UMAP 및 HTB 스캔을 결합할 때 발생합니다. 이렇게 하면 실행 시간이 85초로 dramatically 단축됩니다. 이는 실행 시간을 90% 이상 단축한 것입니다. 따라서 대규모 세금 데이터를 다루고 모델 품질과 처리량 모두를 중요하게 생각한다면 QML에서 GPU 가속을 사용하면 엄청난 성능 향상을 얻을 수 있습니다. 이를 통해 실시간 타이핑 모델링, 더욱 빠른 실험 및 수백만 개의 문서에 대한 확장성이라는 새로운 가능성이 열립니다 . 예제를 통해 GPU 가속 UMAP 및 HTTP 스캔을 사용하여 실행 시간을 크게 줄이는 방법을 살펴보겠습니다. CPU에서 GPU로 전환하는 것이 얼마나 쉽거나 어려운가? NVIDIA의 QML 제로 콜론 변경 방식은 최상위 percent load_ex QML.xl에 간단히 드롭인 코드를 추가하기만 하면 기존 코드를 수정하지 않고도 동일한 알고리즘을 GPU 가속으로 실행할 수 있도록 해줍니다 . 보시는 바와 같이 총 실행 시간이 8 분 12초에서 56.2초로 단축되었습니다. 본 논문에서 제안하는 접근 방식은 데이터에 존재하는 노이즈를 효과적으로 관리하면서 클러스터 품질을 향상시키기 위한 간단하고 계산 효율이 높으며 견고한 최적화 전략을 제공하는 데 사용됩니다 . 이 접근 방식은 새로운 목적 함수를 생성하고 하이퍼파라미터 최적화를 활용하여 토픽 모델링 결과를 개선함으로써 높은 일관성, 의미 있는 다양성 및 낮은 노이즈를 보장합니다. HPO 프로세스는 최적의 클러스터링 품질을 달성하기 위해 매개변수를 체계적으로 선택합니다 . 엔비디아의 GPU 가속 기능을 활용하면 이러한 최적화 프로세스 전체를 훨씬 더 효율적이고 신속하게 진행할 수 있습니다.
