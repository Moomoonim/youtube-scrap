# Agents Are Just Loops

- 영상 링크: https://www.youtube.com/watch?v=3zvWHe94xCY
- 채널: AWS Developers
- 업로드일: 2026-05-13
- 자막 언어: ko
- 단어 수: 약 1311개

---

## 스크립트

설정 코드 두 줄, 함수 하나, 그리고 호출 한 줄. 저것은 제대로 작동하는 AI 에이전트입니다. 질문을 받으면 계산기가 필요하다는 것을 파악하고 , 파이썬 함수를 호출하여 답을 얻은 다음 응답합니다. 모든 게 바로 여기 있어요. 두 줄의 설정 코드, 눈 감고도 작성할 수 있을 만큼 간단한 함수, 그리고 단 한 번의 호출이면 충분합니다. 현재 AI 에이전트에 대해서는 알려지지 않은 부분이 많습니다. 설명을 찾아보면 에이전트 파이프라인, 다중 에이전트 조정, 군집 지능 같은 단어들이 나오는데, 마치 박사 학위가 있어야 만들 수 있을 것 같은 느낌이 들지만 , 사실 그렇지 않습니다. 여러분에게 필요한 것은 단 하나의 개념, 바로 '순환'을 이해하는 것입니다 . 챗봇이 텍스트를 생성합니다 . 에이전트는 실제로 여러 작업을 수행하고, 함수를 호출하고, API를 요청하고, 현실 세계에서 행동을 취할 수 있습니다. 차이점은 반복문에 있습니다. 모든 에이전트는 동일한 메커니즘으로 작동합니다 . 이 모델은 사용자의 입력을 받아 실제 세계에서 어떤 작업을 수행해야 하는지 판단합니다. 예를 들어 함수를 호출 하거나 API를 호출하는 것과 같은 작업을 수행하고, 결과를 확인한 후 추가 작업이 필요한지 결정합니다 . 만족하면 반응합니다. 그것이 바로 에이전트 루프입니다. 이유 파악, 선택, 실행, 반복. 이 영상에서는 각각 이전보다 조금씩 더 뛰어난 기능을 가진 에이전트 네 개를 구축할 것이며, 이 과정이 실시간으로 실행되는 것을 보실 수 있습니다 . 모든 코드는 설명에 링크된 GitHub 저장소에 있습니다. 저는 Strands Agent SDK를 사용할 예정입니다. 이건 오픈 소스이고 AWS에서 나온 건데, 제가 좋아하는 점은 모델 기반이라는 거예요. 모델은 무엇을 언제 할지 결정합니다. 도구와 프롬프트만 제공하면 됩니다. Strands는 다양한 모델 제공업체와 협력하고 있습니다. 이 글에서는 Claude 와 Amazon Bedrock을 사용하겠지만, 무료로 로컬에서 실행하고 싶다면 OpenAI나 OLlama를 사용하거나 그 외 20여 가지 옵션을 활용할 수도 있습니다. 코드는 어느 쪽이든 동일하게 유지됩니다 . 처음부터 시작해 봅시다. 자, 상황은 이렇습니다. 파이썬 파일이 하나 있는데, 그 안에 무엇이 들어 있는지 하나씩 설명해 드릴게요. 맨 위에서는 가닥에서 에이전트와 도구를 가져옵니다 . 그게 바로 SDK입니다. 그다음에는 add라는 함수를 정의합니다. 두 정수를 입력받아 그 합을 반환합니다. 툴 데코레이터는 일반 파이썬 함수를 에이전트가 사용할 수 있는 함수로 변환하는 역할을 합니다. 그리고 docstring을 보면 두 숫자를 더하는 것을 알 수 있습니다. 그건 중요해요. 모델은 해당 설명을 읽고 이 도구가 언제 유용한지 파악합니다. 모델에게 건네 주는 사용 설명서라고 생각하세요 . 그런 다음 저희 도구를 사용하여 에이전트를 생성하고 질문을 전달하여 호출합니다. 자, 바로 그거죠 . 상담원은 답변을 했습니다. 그렇다면 실제로 무슨 일이 일어났을까요? 더 잘 보이도록 한 줄 추가하겠습니다 . 보이시나요? 응답이 오기 전에 도구가 인쇄되었다고 표시되었습니다 . 모델은 질문을 살펴보고, 덧셈 도구가 필요하다고 판단한 후, 42와 58을 인수로 받아 파이썬 함수를 호출하여 100이라는 응답을 받았고, 그 결과를 바탕으로 응답을 작성했습니다. 함수 호출 또는 도구 사용이라고 부르는 것을 들어보셨을 수도 있습니다. 그게 전부예요 . 모델이 함수 실행을 요청하면 프레임워크가 해당 함수를 실행합니다. 그게 바로 순환 고리예요. 한 주기. 그 모델은 생각하고, 행동하고, 반응했다. 그게 바로 에이전트의 전부입니다. 이 도구는 흥미롭긴 하지만 그다지 유용하지는 않습니다. 그렇다면 에이전트에게 선택권이 있을 때는 어떻게 될까요? 도구를 몇 개 더 추가해서 살펴보죠. 글자 수 세는 기능을 추가하겠습니다. 단어를 입력받아 그 단어에 포함된 글자 수를 반환합니다 . 다시 말하지만, 데코레이터와 독스트링이 추가된 단순한 파이썬 함수일 뿐입니다 . 그리고 날씨 정보 도구도 있습니다. 이 코드는 실제로 미국 국립기상청의 API를 사용하는데 , 이 API는 무료이며 API 키가 필요하지 않습니다. 좌표를 입력받아 날씨 API를 호출하고 일기 예보를 가져옵니다. 세부 사항은 여기서 그다지 중요하지 않습니다. 요점은 간단히 말해서 데코레이터와 독스트링이 있는 파이썬 함수라는 것입니다. 다른 것들과 마찬가지입니다. 자, 이제 세 가지 도구를 모두 한 명의 요원에게 제공하겠습니다. 이제 모델은 선택을 해야 합니다. 제대로 선택되는지 한번 봅시다 . 글자 수를 세었습니다. 계산기를 사용하려고 하지도 않았고, 날씨를 확인하지도 않았습니다. 문서 문자열을 읽고 질문에 맞는 도구를 파악한 다음 , 적절한 도구를 호출했습니다. 이제 날씨 도구를 선택했습니다. 심지어 제가 좌표를 제공하지 않았는데도 마이애미의 위도와 경도를 스스로 계산해냈습니다 . 위치 정보만으로 추론해낸 거죠. 질문에 하나 이상의 도구가 필요할 때는 어떻게 해야 할까요 ? 음, 그것은 두 개의 도구를 연결해 주는 역할을 합니다 . 모델은 첫 번째 부분에는 계산기가, 두 번째 부분에는 문자 카운터가 필요하다고 판단하여 두 장치를 순서대로 호출한 후 두 결과를 하나의 답으로 통합했습니다. 저는 그에 대한 라우팅 로직을 작성하지 않았습니다. 나는 어떤 도구를 먼저 사용해야 하는지 알려주지 않았다. 모델은 문서 문자열을 읽어서 스스로 계획을 파악했습니다. 도구 디자인에 대해 제가 이해하는 데 시간이 좀 걸렸던 부분이 바로 이겁니다. docstring은 여러분과 모델 간의 API 계약입니다. 모호한 docstring을 작성하면 모호한 도구 선택 결과가 나타납니다. 명확한 값을 작성하면 모델은 언제 그 값을 사용해야 하는지 정확히 알게 됩니다 . 그러므로 더 나은 설명은 더 나은 행동으로 이어질 수 있습니다. 그래서 우리는 그 순환 과정이 작동하는 것을 보았지만, 지금까지는 모델이 이러한 결정을 내리고 있다고 믿고 기다려 왔습니다. 훅을 사용하면 실제로 그 일이 일어나는 것을 볼 수 있습니다. Strands에는 에이전트의 생명주기에서 발생하는 이벤트를 구독할 수 있는 후크 시스템이 있습니다 . 도구가 호출되기 전에 실행되는 훅 하나 와 호출된 후에 실행되는 훅 하나, 이렇게 두 개의 훅을 추가할 예정입니다. 이벤트 객체는 도구 이름과 모델이 선택한 입력값을 제공합니다. 제가 출력하려는 ​​게 바로 그 내용입니다. 이제 에이전트가 실행되면 터미널에서 에이전트가 내리는 모든 결정을 볼 수 있습니다. 다용도 도구에 대한 질문을 다시 해 봅시다. 저기 있네요 . 모든 과정을 볼 수 있습니다. 모델은 먼저 입력값 7 과 6을 더하여 결과를 얻었고, 그 다음에는 단어 42를 사용하여 글자 수를 세어 결과를 얻은 후, 두 결과를 결합하여 최종 답을 도출했습니다. 숨기는 것은 아무것도 없다. 반복되는 루프를 볼 수 있습니다. 모델은 생각하고, 도구를 선택하고, 도구를 실행하고, 결과가 모델로 돌아가면, 모델은 다음에 무엇을 할지 결정합니다. 일단 문제를 파악할 수 있다면, 디버깅을 할 수 있습니다. 에이전트가 잘못된 도구를 선택했을 때 무엇이 ​​잘못되었는지 추론 하거나 무한 루프에 빠지는 이유를 파악할 수 있습니다. 그 시점에서는 그냥 코드일 뿐입니다. 모호한 값을 하나 주고 어떻게 처리하는지 살펴봅시다. 이것이 어떻게 분석되는지 보세요. 단어의 길이를 알아야 하므로 글자 수를 세는 함수를 호출합니다. 하지만 숫자 비교에는 도구가 필요하지 않습니다. 이미 42라는 단어가 숫자 42를 나타낸다는 것을 알고 있습니다. 따라서 그 부분은 스스로 추론하여 답을 제시합니다. 모델은 도구를 사용할 시점과 이미 충분한 정보를 확보한 시점을 결정합니다. 좋아, 마지막이야. 요원 한 명으로는 부족할 때 무슨 일이 벌어질까요? 답은 다른 에이전트를 사용하는 에이전트를 만드는 것입니다. 같은 루프인데 중첩된 형태입니다. 저는 두 명의 전문 에이전트를 만들 예정입니다. 한 명은 인내심을 갖고 풀이 과정을 보여주는 수학 과외 선생님이고 , 다른 한 명은 간단한 문법 조언을 해주는 작문 도우미입니다. 각각은 우리가 사용해왔던 것과 동일한 툴 데코레이터를 사용하여 툴처럼 래핑되어 있습니다. 그런 다음, 질문을 적절한 전문가에게 전달하는 역할을 하는 오케 스트레이터, 즉 에이전트를 만듭니다 . 오케스트레이터는 수학도 모르고 문법도 모릅니다. 다만 어떤 에이전트가 수학과 문법을 아는지 알 뿐이죠. 수학 문제가 들어오면 수학 튜터가 호출되고, 수학 튜터는 자체 루프를 사용하여 자체 에이전트를 실행합니다 . 글쓰기 관련 질문이 들어오면 글쓰기 도우미가 호출되고, 기존에 사용하던 방식과 동일하게 도구들을 활용합니다. 진행자는 이것이 수학 문제임을 알아차리고 수학 강사에게 전달했습니다 . 수학 튜터는 자체 루프를 실행하고, 덧셈 도구를 사용하여 계산을 수행한 후 결과를 다시 전송했습니다 . 이건 글쓰기 도우미에게 갔어요 . 오케스트레이터는 도큐먼트 문자열을 읽고 적절한 전문가를 선택한 다음, 전문가가 작업을 처리하도록 했습니다. 그러니까, 같은 루프인데 중첩된 형태인 거죠. 오케스트레이터의 루프는 도구를 호출하고, 그 도구는 다른 에이전트이며, 그 에이전트는 자체 루프를 실행합니다. 이것들을 필요한 만큼 깊게 쌓을 수 있습니다. 패턴은 동일합니다. 자 , 그럼 시야를 넓혀봅시다. 우리는 오늘 네 가지를 만들었습니다 . 하나의 도구만 사용하는 에이전트, 모델이 선택할 수 있는 여러 도구를 사용하는 에이전트 , 모든 결정을 감시할 수 있도록 후크가 있는 동일한 에이전트, 그리고 전문 에이전트로 작업을 라우팅하는 오케스트레이터가 있습니다 . 네 가지 모두 동일한 메커니즘으로 작동합니다. 이 모델은 입력을 받아 도구가 필요한지 판단하고, 도구를 실행하고, 결과를 얻은 후 다음에 무엇을 할지 결정합니다. 그리고 이 반복문의 특징은 실행 위치에 관계없이 변하지 않는다는 것입니다. 동일한 코드가 노트북, 컨테이너, 서버리스 함수 또는 Amazon Bedrock Agent Call Runtime과 같은 관리되지 않는 인프라에서 작동합니다. 이러한 에이전트를 실제 운영 환경에 배포하는 것이 어떤 모습인지 알고 싶다면, Morgan이 Agent Call Runtime에 배포하는 방법에 대한 훌륭한 가이드 와 운영 환경 에이전트 아키텍처에 대한 또 다른 가이드를 제공하고 있습니다 . 설명란에 두 링크 모두 넣어드릴게요. 하지만 출발점은 언제나 같습니다. 반복문, 몇 가지 도구, 그리고 나머지를 알아내는 모델. 오늘 사용한 모든 코드는 아래 링크된 GitHub 저장소에 있습니다 . 클론하고, 데모를 실행하고, 몇 가지 문제를 해결해 보세요. AWS 계정 없이 사용해보고 싶다면 Strands는 Ollama와 연동되어 모델을 로컬에서 무료로 실행할 수 있습니다. 에이전트를 구축하기 위해 인공지능에 대한 모든 것을 이해할 필요는 없습니다. 반복문을 이해해야 하는데, 이제 이해하셨군요 . 이 영상이 도움이 되었다면 구독해주시고, 댓글에 무엇을 만들 계획인지 알려주세요 . 다음에 또 뵙겠습니다 .
