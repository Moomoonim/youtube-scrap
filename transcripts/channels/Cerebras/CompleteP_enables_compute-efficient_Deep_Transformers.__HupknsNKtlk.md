# CompleteP enables compute-efficient Deep Transformers.

- 영상 링크: https://www.youtube.com/watch?v=HupknsNKtlk
- 채널: Cerebras
- 업로드일: 2025-12-08
- 자막 언어: en
- 단어 수: 약 979개

---

## 스크립트

Hello, my name is Nolan and I'm going to give a five-minute overview on our NEURB's paper. Don't be lazy. Complete P enables compute efficient deep transformers. The TLDDR is that we introduce complete P which offers depthwise hyperparameter transfer, fewer flops to reach the same loss with deeper models and a larger range of compute efficient widthto ratios. First, let's start with the problem. So if you want to train a very deep transformer, the first thing is that deep transformers are unstable and this kind of introduces risk into large scale runs. Second thing as you vary depth models will not share the same optimal hyperparameters. So you're going to need to retune things or just make bad decisions. And the third is that deeper narrower models are not competitive with shallower or wider models. So everyone just sort of uses width to depth ratio of roughly 100. There have been a few proposals for infinite depth parameterizations for resonance. Um so first consider the form of the resonant that we have in this equation. So h of l plus one equals h of l plus f of l. But we have this uh these green terms l to the negative alpha for the forward pass and then l to the alpha minus one to multiply the learning rate. So these green terms are what can implement an infinite depth parameterization. Um but we need to set the value of alpha. So Yang at all argue that setting alpha to 0.5 is best in practice and also that depthwise hyperparameter transfer is not possible for any alpha and several works have adopted this alpha half prescription in practice. Borderline at all find that setting alpha equals to one allows for better learning as L or depth goes to infinity. In this work we're going to study these two promising candidates alpha half and alpha 1. We find that only alpha equals to one ensures depthwise hyperparameter transfer. So here we just have uh the x axis is learning rate and the y- axis is loss and we've trained models for uh two layers all the way up to 128 layers and you can see that only the complete p parameterization or alpha equals to one allows the same optimal hyperparameters to be shared by every single depth beyond transfer in this work we also study compute efficiency of deep transformers in the plot on the left we have the x-axis as the width to depth ratio or n to l and the y-axis as the loss increase relative to some very compute efficient scaling law. Different lines correspond to different parameterizations. So green is complete P, blue is MUP and orange is alpha half. As you move to the left or in other words get deeper and narrower, complete P's advantage over MUP and alpha half grows. In other words, complete P enables more compute efficient deep transformers than MUP or alpha half. In the plot on the right, the X-axis corresponds to the number of non-mbedding parameters and the Y-axis corresponds to the width to depth ratio. The shaded area corresponds to the range of aspect ratios that are within 1% of the optimal loss achievable for that parameterization. You can see as we scale the parameter count, the blue area for MUP shows that you want to go wider and shallower. Whereas if you look at the pink area complete P, you can see that as you scale the parameter count, we can stay much deeper and narrower than MUP can stay while remaining compute efficient. Now that we've looked at the empirical results, let's get into how complete P works. So both alpha half and alpha 1 stabilize training dynamics. In this test, we look at the activation norm of the final residual stream H of capital L. On the very left column, we we have the standard parameization or MUP or in other words, there's no depth scaling. And we find that as you scale the depth of the network, the activation norms at the final residual stream are going to be exploding. If you also test alpha half uh from literature for a transformer, you find that alpha half also has exploding activations. But with our derived corrections for the bias learning rate and the lair aphine parameter learning rates, you can find that alpha half is also able to stabilize training dynamics. And finally uh we find that complete p or alpha 1 stabilizes training dynamics. We've shown that multiple alpha values can stabilize train dynamics. So how do we theoretically explain the success of alpha equals to one? Let's start with a minimal example. a resnet with a two-layer linear MLP block. If we consider delta H of L+1, the change in H of L+1 after a weight update, we can tailor expand this delta H of L+1 term. And you can note that the first order term of the tailor expansion has a scaling proportional to L to the 1, whereas the second order term is L to the alpha minus 2. Our desert out of three that we add complete feature learning is that for all layers we want the higher order terms to have the same asmtoic scale even as L goes to infinity only alpha equals to one will ensure this complete feature learning deserata. Thus we call it complete P. Alpha equals to half on the other hand will actually have vanishing higher order terms or be lazy with respect to the first order term as L goes to infinity. To summarize, the benefits of complete P are stable optimal hyperparameters across depths, significant flop savings over MUP for deep models, complete feature learning, and a very easy implementation. It's just two lines of forward pass code. Here are the references. Thank you very much.
