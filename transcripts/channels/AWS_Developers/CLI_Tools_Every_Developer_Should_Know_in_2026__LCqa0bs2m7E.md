# CLI Tools Every Developer Should Know in 2026

- 영상 링크: https://www.youtube.com/watch?v=LCqa0bs2m7E
- 채널: AWS Developers
- 업로드일: 2026-04-03
- 자막 언어: ko
- 단어 수: 약 3101개

---

## 스크립트

음, 만약 제가 그냥... 흠. 현재 수신 대기 중인 포트를 보여주는 명령어, 좀 무섭게 생겼죠? 음, 무섭지 않아요. 사실 이것은 특정한 순서로 연결된 여러 개의 작은 명령어들이 모여서 만들어내는 멋진 결과물입니다. 요즘 명령줄 도구가 다시 유행이니까, 네. 이번 영상에서는 명령줄의 기본 사항 몇 가지를 알려드리고자 합니다 . 스위치가 뭐예요? 매개변수란 무엇인가요? 파이프가 뭐예요? 리다이렉션이란 무엇인가요? 이 영상의 마지막에는 이 모든 것들 과 더불어 제 하루, 그리고 어쩌면 여러분의 하루까지 조금 더 즐겁게 만들어 줄 정말 멋진 명령줄 유틸리티 몇 가지를 소개합니다. 그럼 기본부터 시작해 볼까요? 명령 프롬프트를 사용해 본 경험이 있다면 이미 사용하고 있을 가능성이 높은 몇 가지 명령어부터 시작해 보겠습니다 . 그리고 저는 여기서 ls부터 시작할게요. `ls` 또는 `list stuff`는 파일이나 폴더 등의 목록을 보여주는 명령줄 명령어입니다 . 참고로. 이 명령어가 리눅스, macOS, BSD 시스템 모두에 존재하는 이유는 POSIX라는 규격 때문입니다 . 포식스. 이는 기본적으로 특정 명령줄 유틸리티가 어떻게 동작하고, 어떻게 상호 작용해야 하는지를 규정하는 표준입니다 . 그러니까 제가 리눅스 명령어를 배우고 있다고 말할 때, 기본적으로는 50년 된 표준 명령어를 배우고 있다는 뜻입니다 . 그 명령어는 다양한 시스템에 적용되어 왔습니다. 응. 덧붙이자면, 이 명령어들이 왜 이렇게 짧은지 궁금해 본 적 있으신가요? 왜 ls인가요? 왜 rm이죠? 왜 CD죠? grep 같은 것들 말이죠? 제대로 된 걸 해보는 게 어때요? 자, 이 규격이 만들어지던 시절, 즉 이러한 명령어들이 초기 유닉스 시스템에서 만들어지던 당시에는, 명령어를 만들던 사람들 이 시스템과 상호작용하는 방식이 텔레타이프라는 것을 통해서였습니다. 기본적으로 타자기처럼 생겼고, 엄청나게 느리며, 긴 명령어를 입력하기에는 적합하지 않습니다. 그래서 사람들은 그 이야기에 따르면 아주 짧은 명령어를 사용하는 데 동의했다고 합니다. 그러니 알면 알 수록 좋죠. 좋아요, 다시 ls로 돌아가죠. ls 명령어를 사용하면 디렉토리, 파일, 폴더 목록이 가로로 표시되는데, 저는 여기에 추가 정보를 포함한 보기 좋은 목록을 원합니다. 음, ls 명령어에 몇 가지 매개변수를 추가할 수 있습니다. 그러니까, `ls {dash} l`이라고 입력하면 목록이 표시됩니다. 와, 저것 좀 봐. 목록입니다. 파일, 폴더뿐만 아니라 파일 크기를 포함한 훨씬 더 자세한 정보 도 제공합니다. 하지만 숨겨진 파일을 포함하여 모든 명령과 모든 파일에 접근하고 싶다면 어떻게 해야 할까요 ? 리눅스와 유닉스 시스템에서는 숨겨진 파일 앞에 점(.)이 붙습니다. 자, 만약 제가 `ls {dash} l a`라고 입력해서 `a` 매개변수를 `para` 매개변수에 추가하면 어떻게 될까요? 저도 dot 파일들을 가지고 있습니다 . 훌륭한. 만약 제가 cargo.lock 파일의 크기를 76,140바이트 대신 사람이 읽기 쉬운 크기로 선택하고 싶다면 어떻게 해야 할까요 ? 또 다른 매개변수, ls {대시} l a h. 저것 좀 봐 . 이러한 명령에서 H는 일반적으로 사람이 읽을 수 있는 읽기 쉬운 형식을 의미합니다. 그래서 이 경우에는 이러한 크기를 76,140 대신 75k와 같은 값으로 변환합니다 . 그러니까, ls 같은 간단한 명령줄 유틸리티를 선택하고 몇 가지 매개변수를 추가하면 다른 작업이나 더 많은 작업을 수행할 수 있습니다. 그리고 ls만 그런 게 아닙니다 . 예를 들어 ' cat'과 같은 명령어를 입력하고, 그 명령어를 자세히 살펴보면 다양한 추가 명령어를 볼 수 있습니다. 예를 들어 ` mkdir` 명령어로 디렉토리를 생성하고, 그 디렉토리를 확장하면, 이 명령어 에 전달할 수 있는 추가 매개변수가 더 많아집니다. 한 가지 더 강조하고 싶은 점이 있습니다. 여기에는 몇 가지 다른 유형의 매개변수가 있을 수 있습니다 . 음, 하이픈(-) 모드가 있고 하이픈(-) 모드가 있어요. 일반적으로 앞에 하이픈이 두 개 있으면 두 개 이상의 문자로 이루어진 매개변수임을 나타냅니다. 그러니까, 이건 하이픈(- ) 모드 또는 그냥 하이픈(-) 모드입니다. 조심하세요. 예를 들어, {dash} {dash} 도움말에는 {dash} {dash} h가 없다는 것을 알게 될 것입니다. 그러므로 어떤 매개변수를 사용할지 결정할 때는 항상 이러한 사항들을 고려해야 합니다. 그리고 사실, 그거 아세요? {dash} {dash} 도움말에 대해 이야기해 봅시다. 실제로 대부분의 Linux Unix POSIX 명령어에 대해 도움말을 입력하면 어떤 매개변수가 있는지, 그리고 정확히 어떤 역할을 하는지 확인할 수 있는 훌륭한 사용법 출력을 제공합니다. 때로는 이러한 매개변수가 어떻게 사용되는지 예시까지 볼 수 있습니다. 그럼 다음으로 넘어가서 명령줄 유틸리티에 내장된 기능을 활용하여 도움을 받는 방법을 알아보겠습니다. 명령줄 도구의 옵션과 매개변수는 때때로 조금 복잡하게 느껴질 수 있죠? 제가 몇 가지 예를 보여드렸습니다. 아마 여러분은 `ls l` 이나 `mkdir b` 같은 명령어를 기억하실 겁니다. 그중 일부는 아주 흔한 것들이라 자연스럽게 몸에 배겠지만, 어떻게 도움을 받을 수 있을까요? Docker 명령어 에서 TLS를 사용하는 방법을 어떻게 찾을 수 있을까요 ? 음, 앞서 말씀드렸듯이 이러한 명령어들 중 상당수는 내장된 {dash} {dash} 도움말 명령어를 가지고 있습니다. 그래서 제가 ` docker {dash} {dash} help`라고 입력하면, docker 명령어에 대한 훌륭한 도움말 문서나 참조 자료를 볼 수 있습니다 . 그리고 여기 보이는 하이픈(-) TLS가 바로 도커에서 TLS를 설정하는 방법입니다. 다시 말씀드리지만, ` ls {dash} {dash} help` 명령어를 사용해도 같은 작업을 할 수 있으며, 제가 방금 bash 자동 완성 기능을 이용해 모든 매개변수를 확장해 보여드린 것보다 더 자세한 정보를 얻을 수 있습니다 . 상관없어요. 하지만 보시면 아시겠지만 ls에 사용할 수 있는 옵션이 정말 많아요. 그런데 우리는 지금까지 세 가지만 봤잖아요, 그렇죠? 그러므로 {dash} {dash} 도움말 명령어가 여러분의 친구입니다. {dash} {dash} 도움말을 잊지 마세요. 하지만 더 많은 것을 원한다면 어떻게 해야 할까요? 이것보다 더 많은 것을 원한다면 어떻게 해야 할까요? 이것보다 더 필요한 게 뭐가 있겠어요? 음, 때로는 좀 더 자세한 설명이나 예시 한두 가지가 필요할 때도 있죠. 다행히 리눅스를 비롯한 대부분의 유닉스 시스템에는 매뉴얼 페이지가 제공됩니다. 그리고 이것은 남성의 명령과 함께 옵니다. 자, 그럼 제가 남자라고 하고 나서, 음, ls라고 해볼게요. 남자 ls. ls에 대한 올바른 설명서를 참조하십시오. 그리고 대부분의 POSIX 규격 명령어에는 매뉴얼 파일이 있습니다. 모든 프로그램이 그런 것은 아니지만, 많은 프로그램이 특정 시스템 호출과 같은 특정 요소를 포함하여 특정 기능을 지원합니다. 그 부분에 대해서는 자세히 언급하지 않겠지만, 당신이 사용하는 일반적인 명령어처럼, 당신도 보통 그런 명령어를 사용하게 될 겁니다. 그러니까, 어쩌면 남자 CD 같은 것도 괜찮을지도 몰라. 저것 좀 봐. 작업 디렉토리를 변경합니다. 자, 여기에는 예시가 많지 않을 거라고 생각합니다. 아주 간단해요. 가장 간단한 명령입니다. 하지만 아시 다시피, `man docker`를 검색해 보시면 도움말 예제보다 훨씬 더 많은 내용이 도커 매뉴얼에 나와 있는 것을 보실 수 있을 겁니다. 사실은 아니에요. 사실 아주 간단합니다. 만약 제가 ` docker docker run` 명령어를 실행한다면, ` docker run` 명령어와 관련된 많은 내용과 더 많은 예시들을 볼 수 있을 겁니다. 그래서, 맨페이지는 아주 좋은 방법입니다. 마찬가지로 마우스 휠을 사용하여 스크롤할 수 있습니다. 화살표 키로 이동할 수 있습니다. Vim의 단축키를 사용하여 이동할 수도 있습니다. 가장 중요한 것은, 예를 들어 {슬래시} 명령어처럼 뭔가를 할 수 있다는 것입니다. 슬래시(/) 명령어를 입력하면 검색 창이 열립니다. 그러니까, 만약 제가 ' 태그' 같은 걸 한다면, 앱은 ' 태그'라는 문구를 찾아서 알려줄 거예요. 그리고 만약 제가 계속해서 n을 누르면, 다음 단계로 넘어갈 거예요. 이건 마치 Less TextViewer 같은 걸 사용하는 것 같은데, Less 키 바인딩이 전부 없지만, man 페이지는 특정 명령줄에 대한 모든 정보를 찾는 데 아주 좋은 방법입니다. 하지만 다른 대안이 있습니다. 그리고 man 페이지는 일반적으로 시스템에 기본적으로 포함되어 있습니다 . 때로는 매뉴얼 페이지를 표시하기 위해 패키지를 설치해야 할 수도 있지만, 제가 즐겨 사용하는 훌륭한 도구가 있습니다. 자, 유용한 도구 소개, 첫 번째 도구에 오신 것을 환영합니다. ( 너무 길어서 읽지 않으셨다면 요약은 생략합니다.) 그러니까, tldr은 수동으로 설치해야 하는 추가 패키지이지만, 기본적으로 Docker에서 tldr을 사용할 수 있게 해줍니다. 그러니까, 제가 이렇게 하면 tldr이 인터넷에서 뭔가를 다운로드하게 됩니다 . 인터넷 연결이 필요합니다 . 그러면 가장 자주 사용해야 하는 기능들을 알려줄 거예요. 정말 예쁜 색깔로 나왔어요 . 만약 제가 tldr이나 ls를 입력하면, tldr 사용법 , 즉 ls 명령어 사용법에 대한 일반적인 예시들을 많이 보여줄 겁니다. 명령어에 {dash} {dash} 도움말 매개변수를 추가하면 해당 명령어에 대해 더 자세히 알아볼 수 있는 훌륭한 방법입니다. 그렇다면 제가 사용할 수 있는 다른 매개변수는 무엇일까요 ? 어, man 페이지, 그러니까 `man command name`은 특정 명령어에 대한 설명서를 보는 데 아주 좋은 방법입니다. 하지만 두 가지를 섞은 듯한 느낌이나 좀 더 간결하고 선별된 내용을 원한다면 tldr 명령어를 사용하세요. 그리고 요약본 링크를 좋아요 버튼 바로 아래에 남겨둘게요. 좋아요. 자, 방금 말씀드린 건 도움말 명령어였습니다. 음, 명령에 대한 도움을 받는 거죠. 그럼 이제 파이프 이야기로 넘어가 볼까요? 리눅스, 유닉스 또는 POSIX 명령줄의 강력한 기능. 자, 재밌는 사실 하나 알려드리자면, 파이프는 1973년에 더그 맥일로이가 켄 톰슨에게 유닉스 시스템에 파이프를 도입해달라고 졸라댄 데서 시작되었습니다. 그리고 그 이후로 POSIX 표준의 일부가 되었습니다 . 그러므로 파이프는 당신의 지휘에 있어 중요한 초능력입니다. 파이프를 배관이라고 생각해 보세요. 네, 배관이요. 한쪽의 출력이 다른 쪽의 입력으로 들어갑니다 . 제가 보여드리겠습니다 . 여기 웹 접속 로그가 있습니다. 이건 그냥 로그 파일인데, 음, 보여드릴게요. 그러니까 `cat web access log` 명령어는 웹 서버에 대한 접근 로그, 즉 웹 로그를 보여주는 겁니다. 여기에는 다양한 요청들이 있습니다. 그리고 여기 있는 `cat` 명령어는 우리가 예전에 사용해 본 적은 없지만, 파일의 내용을 터미널에 출력하는 명령어입니다. 아주 좋은 일이죠. 지금 제가 할 수 있는 건, Shift 키와 백슬래시 키를 동시에 누르는 순간 바로 위에 있는 직선 모양의 파이프(|) 문자를 이용해서 웹 접속 로그를 확인하는 겁니다 . 응. 그렇게 하시면 제가 그걸 grep으로 파이프합니다. grep은 검색을 수행하는 명령줄 도구입니다. 기본적으로 이 기능은 추출한 모든 내용을 검색하여 예를 들어 특정 패턴을 가진 줄을 추출합니다. 그러니까, 제가 grep 404를 실행하면, 404라는 문자열이 포함된 모든 줄을 찾게 됩니다. 자, 그럼 한번 보세요. 전체 액세스 로그 대신 액세스 로그를 grep으로 파이프했더니 grep이 알아서 다음과 같은 결과를 출력했습니다 . 환상적이에요. 만약 내가 다른 일을 하고 싶다면 어떡하지? 예를 들어 , 여기에 다른 파이프를 연결하면 어떻게 될까요? 그리고 저는 wc-l을 할 수 있습니다. 그리고 wc는 단어 수를 의미합니다. 그리고 또 다른 매개변수인 -l은 단어 수를 세는 대신 줄 수를 세도록 지시하는 것입니다. 그래서 이 파이프들을 이용하면 실제 로그를 얻는 대신 , 액세스 로그에서 404 오류가 몇 번 발생했는지 확인할 수 있습니다. 정말 놀랍습니다. 정말 마음에 들죠, 그렇죠? 그리고 이것 또한 여러분이 원하는 만큼 복잡하게 만들 수 있습니다 . 제가 200점만 기록하면, 200점이 얼마나 많은지 알 수 있을 거예요. 하지만 제가 할 수 있는 건... 음, 우리가 뭘 할 수 있을까요? 우리는 헤드 파이브를 할 수 있어요. 이렇게 하면 이 중에서 가장 중요한 다섯 가지만 알 수 있겠죠? 전체 대신에. 그리고 저는 이런 식으로 배관 작업을 많이 할 수 있어요 . 정말 엄청나게 유용한 명령어입니다. 그리고 흔히 파이프를 통해 전달되는 예로는 grep, head, sort, tail, unique, wc 등이 있으며, 그 외에도 다양한 예가 있습니다. 제가 리눅스를 처음 접했을 때, 항상 제일 먼저 사용했던 명령어가 ps였습니다. ps -aux 명령어를 사용하면 현재 실행 중인 모든 프로세스 목록을 볼 수 있었죠. 만약 이 모든 과정을 실행하는 대신 특정 프로세스 하나만 실행 중인지 확인하고 싶다면 , `ps aux grep Firefox` 명령어를 사용하면 됩니다. 저것 좀 보세요 . 저는 파이어폭스 탭을 두 개 열어놓고 실행 중입니다. 따라서 파이프는 엄청나게 강력하며 명령줄 도구 모음에서 가장 중요한 도구가 될 것입니다. 자, 이제 좀 더 복잡하고, 좀 더, 음, 훨씬 더 유용한 것으로 넘어가 볼까요? 리디렉션이나 명령어를 이용한 작업 저장에 대해 이야기해 보겠습니다 . 그리고 두 번째로 아주 유용한 기능인 '배트'의 예를 하나 들어보겠습니다. 앞서 살펴본 예시에서 `cat` 명령어는 단순히 파일 내용을 터미널에 출력합니다. 음, 야구 방망이도 비슷한 역할을 하지만, 좀 더 화려하게 하죠. 그래서 제가 `cat web access log` 명령어를 실행하면 터미널에 데이터가 출력됩니다. 네, 괜찮습니다. 배관 연결용으로는 훌륭합니다. 하지만 그냥 보기만 하려고 한다면 그다지 좋지는 않네요. 그래서 저는 bat이라는 도구를 사용할 수 있습니다. 그리고 만약 제가 웹 액세스 로그 파일을 실행한다면, 그 내용을 확인해 보세요. 색상, 구문 강조, 스크롤, 검색 등 모든 기능. 훨씬 낫죠, 그렇죠? 음, 제 생각엔 배트가 생기기 전에는 고양이를 좀 더 적게 또는 더 많이 잡아야 했을 것 같고, 그런 다음 거기서 뭔가를 해야 했을 것 같아요. 하지만 야구 방망이는 고양이의 매력을 한층 더 높여주는 훌륭한 방법입니다. 음, 이해가 되셨으면 좋겠네요. 완전 하고 완벽한 대체품은 아닙니다. 저는 배트가 일반적인 스타일과 완벽하게 어울릴 수 있는 능력을 가지고 있다고 생각합니다. 제 생각엔 박쥐 와 고양이를 완벽하게 호환시키는 방법이 있을 것 같아요. 하지만 저는 그걸 전적으로 신뢰하지는 않겠습니다. 따라서 POSIX 시스템에서 정의한 대로 cat 명령어의 기능을 사용하려면 cat 명령어를 사용하십시오. 하지만 멋진 출력을 위해 bat를 로컬에 설치할 수도 있습니다. 리디렉션으로 돌아가 보겠습니다. 그리고 음, 이것은 POSIX 규격 준수의 일부이기도 합니다. 리디렉션은 명령줄 환경에서 매우 강력한 기능으로, 파일의 입력이나 출력을 리디렉션할 수 있게 해줍니다. 파이프와 비슷하지만 완전히 똑같지는 않습니다. 이것은 단순히 명령의 출력을 파이프로 전달하는 것이 아니라 디스크에 있는 실제 파일과 같은 것을 다루는 것과 관련이 있습니다 . 여전히 일종의 배관 작업이겠지만, 예를 하나 들어볼게요. 자, 여기 ' 정렬되지 않은 데이터'라는 파일이 있습니다. 그러니까, 정렬되지 않은 데이터를 실행하면 , 그냥 이것저것 잔뜩 들어있는 파일이라는 걸 알게 되실 겁니다. 그리고 저는 이 문제를 해결하고 싶습니다. 이름순 으로, 알파벳순으로 정렬하고 싶어요 . 그래서 저는 실제로 정렬 명령어를 사용할 수 있습니다. 따라서 `sort` 명령어는 입력값을 받아서 출력값으로 정렬해주는 아주 훌륭한 명령어입니다. 아주 간단합니다. 그래서 제가 여기서 할 수 있는 일은 기술적으로 다음과 같습니다. 정렬되지 않은 데이터를 cat 명령어로 가져와서, 파이프를 통해 sort 명령어로 전달하면 바로 정렬할 수 있습니다. 하지만 리디렉션을 활용하면 더 나은 결과를 얻을 수 있습니다. 그러니까 정렬 되지 않은 데이터의 출력을 정렬된 상태로 리디렉션할 수 있다는 거죠? 그리고 나서 출력을 정렬된 데이터(텍스트 파일)로 리디렉션하는 거죠? 저것 좀 봐. 좋아요. 정렬된 데이터를 입력하면 정렬 된 상태로 저장됩니다. 아주 훌륭하네요. 그래서 여기서 우리가 한 일은 두 가지 유형 의 리디렉션을 사용하여 기본적으로 정렬되지 않은 data.txt에서 데이터를 추출하고 , 정렬된 파일로 리디렉션한 다음, 거기서 나온 결과를 다시 정렬된 data.txt라는 파일로 리디렉션하는 것입니다 . 환상적이에요. 자, 이건 좀 더 복잡한 예시라고 할 수 있겠습니다. 이런 일을 자주 할 필요는 없잖아요. 대부분의 경우 이 중 하나만 사용하거나, 아니면 이 중 하나만 사용하는 경우를 보게 될 겁니다, 그렇죠? 마치 두 개의 부등호(&gt;)나 하나의 부등호처럼요. 자, 예를 하나 보여드리겠습니다. 제게 app.log라는 파일이 하나 있죠? 따라서 이 파일에 무언가를 추가하고 싶다면 다음과 같이 하면 됩니다. 저는 화면 에 무언가를 출력하는 에코 기능을 할 수 있습니다 . 저는 에코 푸라고 말할 거예요. 그리고 저는 그 결과를 '&gt;' 기호 두 개를 붙여서 app.log에 출력할 겁니다. 그렇게 하고 파일을 다시 배치하면 foo라는 줄이 생깁니다. 그래서 여기에 한 줄을 추가했습니다. 이것은 아주 흔한 예입니다. 보시면 알게 될 거예요 . 요즘 코딩 도우미를 사용하다 보면 , " 문자열을 파일로 리디렉션"과 같은 명령어를 자주 보게 될 겁니다. 그건 코딩 보조자들이 작업을 더 쉽게 할 수 있도록 도와주는 아주 흔한 패턴입니다. 그게 한 가지입니다. 하지만 여기서부터 위험해집니다. 자, 여러분은 '&gt;' 기호와 '&gt;' 기호를 보셨을 겁니다. 하지만 '&gt;' 기호도 하나 있습니다. 부등호 하나만 사용하는 문제점은 그것이 기존 설정을 덮어쓴다는 것입니다. 그러니까, 지금은 위험한 시기입니다. 그리고 저 표지판은 사용하기에 정말 위험해요 . 자, 그럼 보여드리겠습니다. 그렇게 하면 다시 bat app.log를 실행합니다. 훌륭한. 좋아요. 음, 하지만 만약 제가 echo oops를 하고 나서 app.log에 리디렉션을 위한 큰 기호 하나만 입력한다면요. 좋아요. 괜찮아 보여요. 알았어, 좋아. 내 통나무를 쳐라. 앗. 한 줄. 따라서 리디렉션을 사용할 때는 올바른 리디렉션을 사용하고 있는지 매우 주의해야 합니다. 파일에 데이터를 추가하려는 건가요, 아니면 파일을 덮어쓰려는 건가요? 왜냐하면 저도 이런 실수를 여러 번 저질렀거든요 . 네, 그렇습니다. 자, 마무리하기 전에 청취자, 개발자, 그리고 코딩 보조자분들께도 매우 유용하고 중요한 명령줄 유틸리티 하나를 더 소개해 드리겠습니다. 그럼 jq에 대해 이야기해 볼게요. jq는 POSIX 규격 준수의 일부로, 표준의 일부로, 모든 시스템에 기본적으로 탑재되어야 한다고 생각 하지만, 현실은 그렇지 않습니다. 하지만 jq는 기본적으로 명령줄을 통해 JSON 파일을 잘 파싱할 수 있도록 도와주는 JSON 프로세서입니다. 예를 하나 들어드리겠습니다. 자, 그럼 여기에 명령어를 하나 입력하겠습니다. 이것은 내 모든 인스턴스를 설명하는 AWS CLI 명령어입니다 . 하지만 제가 여기서 하는 일은 특정 매개변수를 사용하여 해당 값을 jq로 전달하는 것입니다. 기본적으로, 제가 하려는 것은 반환되는 JSON 객체에서 인스턴스 ID를 추출하는 것입니다 . AWS EC2 describe-instances 명령을 실행하면 JSON 형식으로 결과가 반환되기 때문입니다. 자, 이렇게 하면 이제 인스턴스 관련 사항만 볼 수 있을 겁니다. 그래서 정말 훌륭한 도구입니다. 시스템에 반드시 있어야 하는 프로그램입니다. 지금 가지고 있지 않다면, 지금 바로 하세요. 설치하세요. 기다릴게요. 자, 그럼 이 모든 것을 어떻게 하나로 합칠 수 있는지 실제로 보여드리겠습니다. 여기서 여러분께 보여드릴 훨씬 더 중요한 명령어가 하나 더 있습니다 . 또한 AWS CLI와도 매우 밀접한 관련이 있습니다. 그래서 좀 더 신속하게 진행하기 위해 여기서 복사해서 붙여넣으려고 합니다. 자, 이렇게 해봅시다. 복사. 팔 . 좋아요. 반죽. 괜찮은. 여기서 무슨 일이 벌어지고 있는 거죠? 화면에는 정말 많은 일들이 벌어지고 있네요. 자, 그럼 보여드리겠습니다. AWS EC2 describe-instances 백슬래시. 이건 그냥 bash 스크립트에서 새 줄에 넣는 간단한 작업일 뿐입니다. 우리는 여러 개의 서로 다른 명령어가 포함된 명령어를 실행하고 있습니다. 그러니까, AWS EC2 describe-instances 명령어를 사용하면 됩니다. 이것들은 명령의 하위 명령입니다. 다음으로 대시 대시 대시 쿼리 매개변수를 추가합니다. 그리고 해당 특정 쿼리 매개변수는 일종의 입력을 받는데, 우리는 거기에 쿼리 경로와 같은 것을 제공하고 있습니다. 그리고 또 다시 슬래시를 넣었습니다. [목을 가다듬으며] 그냥 한 줄 아래로 내려가는 것을 강조하고 싶어서요. 다음으로 출력 텍스트라는 매개변수를 하나 더 추가합니다. JSON 형식으로 출력하는 대신, 그냥 일반 텍스트로 출력해 주세요. 그다음은 파이프가 들어오는 부분입니다. 우리는 파이프를 통해 grep에 연결하고 있습니다. 저희는 ' running'이라는 단어가 포함된 문장을 찾고 있습니다. 아주 간단하죠, 알겠죠? 마지막으로, 해당 출력을 awk로 전달합니다. 지금까지 awk에 대해서는 언급하지 않았지만, awk는 정말 놀랍고 강력한 도구입니다. 지금 자세히 설명하고 싶진 않지만, awk는 기본적으로 이 특정 출력에서 ​​어떤 열을 출력할지 지정하는 명령어입니다. 기본적으로 첫 번째 열과 세 번째 열을 인쇄하는 것입니다. 그리고 마지막으로, 이 출력을 running instances.txt라는 파일로 리디렉션합니다 . 그럼, 이 코드를 실행해도 아무 일도 일어나지 않겠죠? 하지만 화면에는 아무런 변화도 나타나지 않았는데, 그 이유는 해당 명령의 출력을 실행 중인 instances.txt 파일로 리디렉션했기 때문입니다. 그러니까, 만약 우리가 bat 실행, 즉 인스턴스를 실행하는 작업을 한다면 , 사실 저는 cat 실행 인스턴스를 실행해야 합니다. 저것 좀 봐. 네, 첫 번째 열인 인스턴스 ID와 세 번째 열인 해당 인스턴스의 IP 주소를 출력했습니다 . 네 , 그래서 이처럼 매우 복잡한 것들을 볼 수 있습니다. 자, 그럼 제가 예전에 보여드렸던 아주 복잡한 명령어를 살펴봤던 때로 잠깐 돌아가 보겠습니다. 그 명령어를 좀 더 자세히 분석해 볼 수 있을 것 같습니다. 보시는 바와 같이, 저희는 `ss tul pn` 명령어를 실행하고 있습니다. 이 명령어는 가능한 경우 모든 TCP/UDP 수신 포트와 프로세스 이름을 표시합니다 . 여기서는 awk를 사용하여 첫 번째 행이나 listen이 포함된 모든 항목을 찾습니다. 그런 다음 여기서 열을 사용하여 보기 좋게 열별로 정렬합니다. 그리고 마지막으로, grep을 사용하고 있는데, 단순히 필터링하는 용도가 아니라, 올바른 IPv4 주소가 있는 모든 항목에 색을 입히는 용도로 사용하고 있습니다. 보시다시피, 색상은 항상 유지되고, 우리는 이러한 요소들을 찾아내기 위해 복잡한 정규 표현식을 사용하고 있습니다 . 그러니까 파이핑, 리다이렉션 , 매개변수에 대해 알게 되면 사용자 입장에서 그렇게 무섭지 않을 겁니다. 자, 이 점을 강조하고 싶습니다. 음, 요즘 왜 명령줄 도구에 대해 이렇게 많이 이야기하는 걸까요? 키로 CLI나 명령줄 유틸리티 같은 것들이 등장하면서 코딩 도우미나 사용자를 위해 작동하는 에이전트들은 명령줄 도구를 많이 사용하게 되었죠? 종종 그들은 여러 명령어를 실행하는 것을 볼 수 있는데, 그중에는 파일 목록을 표시하거나, 파일을 변경하거나, 내용을 추가하는 등의 작업을 위한 여러 가지 내장 리눅스 명령어가 있습니다. 이런 상황을 자주 보게 될 텐데, 무슨 일이 일어나고 있는지 알아두셨으면 합니다. 저는 여러분이 그 명령들을 알아차리길 바랍니다. 또한 여러분이 코딩 도우미에게 "자, 이걸 실행해"라고 말할 수 있는 자신만의 명령어를 만들어 보기를 바랍니다. 예를 하나 들어 설명해 드리겠습니다. 여기서 키로를 열고 간단한 명령을 내리겠습니다. 음 , 키로 CLI 채팅이라고 말할게요. 음, 뭐라고 해야 할지 모르겠지만, 여러 명령어를 사용해서 제 GitHub 저장소 목록을 모두 나열해 달라고 해야겠어요. curl과 jq를 사용해서 darko.mesaros 아래에 있는 모든 저장소를 보여주세요. 명령어를 실행해 주세요. 물론 실제로 해볼 필요는 없지만, 이 예시를 통해 키로( Kiro)와 같은 코딩 도우미들이 명령줄 인터페이스를 사용하여 어떤 작업을 수행하는지 알 수 있을 겁니다. 그러니까, 아마 곧 그렇게 될 거예요 . 네, 명령어가 실행될 겁니다. Curl을 사용해서 제 저장소의 URL을 가져오고, 그걸 jq로 파이프해서 이름을 출력할 겁니다. 네, 맞습니다 . 잠시만 기다려, 저것 좀 봐. 제 모든 저장소는 제 GitHub 계정 내에서 공개적으로 이용 가능합니다. 환상적이에요. 그래서 중요합니다. 그래서 저는 여러분 모두가 명령줄 도구를 배우고, 익숙해지고, 실험하고, 별칭에 대해 배우고, 자신만의 복잡한 것을 만들어보고, 댓글에 여러분이 없으면 안 될 정도로 좋아하는 오픈 소스 명령줄 도구가 무엇인지 알려주시기를 권장합니다 . 오늘 몇 가지 예를 들었지만, 예를 들어 조옥사이드(zoxide)에 대해서도 언급하고 싶습니다. 그게 뭔지는 여러분이 알아내 보세요. 음, 여러분, 이와 같은 기술적인 콘텐츠를 더 원하시거나 , 2부에서 더 고급 명령줄 유틸리티를 보고 싶으시다면 댓글로 알려주세요. 이 영상에 좋아요를 눌러주시고 , 채널 구독도 잊지 마세요. 다시 한번 말씀드리지만 , 저는 벤 다코이고, 다음 영상에서 뵙겠습니다.
