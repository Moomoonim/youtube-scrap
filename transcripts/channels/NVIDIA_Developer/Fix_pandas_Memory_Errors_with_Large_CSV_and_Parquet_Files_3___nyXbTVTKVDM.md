# Fix pandas Memory Errors with Large CSV and Parquet Files | 3 Easy Solutions

- 영상 링크: https://www.youtube.com/watch?v=nyXbTVTKVDM
- 채널: NVIDIA Developer
- 업로드일: 2025-06-05
- 자막 언어: en
- 단어 수: 약 1673개

---

## 스크립트

What do you do if you don't have enough&nbsp; memory for your data set? Let's go over&nbsp;&nbsp; three common solutions and the&nbsp; pros and cons of each. The three&nbsp;&nbsp; solutions we will cover are 1. Increasing Swap Space&nbsp; 2. Reduce your Data Size via Sampling 3. using Unified Virtual Memory from NVIDIA&nbsp; This is by far the easiest&nbsp; solution. Let's dive in. Solution1. Increasing Swap Space: With this free command, you can see here, I have&nbsp; 2 GB of swap in my Ubuntu 24 OS. I'll try to load&nbsp;&nbsp; my 1B row data set here while sharing my top&nbsp; output. CPU memory is here in the top progress&nbsp;&nbsp; bar. This is going to fill up in a few seconds and&nbsp; then we'll see memory spill to disk or swap. And&nbsp;&nbsp; then kernel crash. Here are my notes for modifying&nbsp; swap space. Remember, this changes your underlying&nbsp;&nbsp; operating system. I chain the commands so I&nbsp; can copy and paste. I'll kill top, paste my&nbsp;&nbsp; swap commands. All right, now I should have a&nbsp; swap space of 128 GB. I'll rerun top, limited&nbsp;&nbsp; to my top process, Upper M to sort by memory use,&nbsp; lower M to show memory and swap. I get that going. Now let me go ahead and run these cells again. Or&nbsp; these, yeah… with 128 GB of swap space. I’ll go&nbsp;&nbsp; ahead and run everything. Let me pull over the&nbsp; top view. All right, this time, you'll see the&nbsp;&nbsp; same thing as before. Memory will fill up and it&nbsp; should fill up any second now. Now we will spill&nbsp;&nbsp; to disc or swap space, but the kernel won't crash&nbsp; because we have lots and lots of swap space. It&nbsp;&nbsp; will just be annoyingly slow. I'll speed up the&nbsp; video here. That took 36 seconds. It's not great.&nbsp;&nbsp; I'll inspect the data frame. This should be 1&nbsp; billion rows. That looks right. Working but slow. Trying to sample data that does not fit&nbsp; into memory in the first place comes with&nbsp;&nbsp; its own set of problems. If your data's&nbsp; in a CSV file or a relational database,&nbsp;&nbsp; you can do some pre-coding to try and get&nbsp; that data smaller to begin with. But if you&nbsp;&nbsp; want to do stratified sampling, which you very&nbsp; much should, now you have even more coding on&nbsp;&nbsp; top of that. All of this coding has to be&nbsp; run somewhere, and we're already resource&nbsp;&nbsp; constrained. That's how we're in this predicament&nbsp; in the first place. But let's give it a shot. Solution 2, we'll do sampling. We'll do a couple&nbsp; types of sampling here. You see here, I've got the&nbsp;&nbsp; documentation open, because pandas has a pretty&nbsp; nice sampling API here. So you got groupby,&nbsp;&nbsp; sample. It's going to sample from within&nbsp; the groups and the groups are your strata,&nbsp;&nbsp; essentially. So right here we have&nbsp; groupby A, and we're going to do n=1,&nbsp;&nbsp; but I prefer to do it more like this one where&nbsp; we take a fraction or a percentage of the strata,&nbsp;&nbsp; typically maybe to make sure you get&nbsp; the right amount of data per target. Now, if we go back over here, let's see here,&nbsp; let me open up the right tab? Okay. So I'm&nbsp;&nbsp; going to go ahead and run this. Let me make&nbsp; sure.. Okay. So I'll go ahead and run this. And I'm just going to do a simple, let me just&nbsp; actually do 1%, 10% is a lot. This is a billion&nbsp;&nbsp; rows. I'll just do 10 million&nbsp; rows, and I'll let this run. I'll pop up my top here so we can see. Now, memory&nbsp; is going to fill up. And then it's going to use&nbsp;&nbsp; swap space to sit here and do the sampling.&nbsp; This is going to take a minute. I'll go ahead&nbsp;&nbsp; and speed up the video. All right, we should be&nbsp; done here. There we go. All right, two minutes&nbsp;&nbsp; and 39 seconds. This is… this is not great. All&nbsp; right, let me. All right, let me think about this.&nbsp;&nbsp; Okay. So two minutes and 39 seconds. And oddly,&nbsp; we're not using all of our memory. That's kind&nbsp;&nbsp; of strange. That's one of the things with swap&nbsp; space. We don't know what the operating system&nbsp;&nbsp; is going to do. All right. So that is two minutes&nbsp; and 39 seconds. Let me just check it out here,&nbsp;&nbsp; make sure I have 10 million. Let's see here.&nbsp; 1 thousand, 1million, 10 million rows. Okay,&nbsp;&nbsp; so I've got 10 million rows. That worked. It took&nbsp; two and a half over two and a half minutes. Okay. Now we're going to do stratified sampling. We're&nbsp; going to pretend that A is our target. And again,&nbsp;&nbsp; let me do 1% here instead of 10%. All right,&nbsp; everything looks good. I'm going to go ahead and&nbsp;&nbsp; restart the kernel. Let's see. Memory should clear&nbsp; out. There it goes, all right. Memory's cleared&nbsp;&nbsp; out. Let me go back. Yeah, everything looks&nbsp; fine. Okay, let me re-import pandas and I'll run&nbsp;&nbsp; this stratified sampling. This is going to take&nbsp; forever. I'm going to actually go ahead and get&nbsp;&nbsp; some stuff done and check on this periodically,&nbsp; and I will speed up the video because this is&nbsp;&nbsp; going to take a very, very long time. All right,&nbsp; 20 minutes. Wow. You can see here, right here, 20&nbsp;&nbsp; minutes, I'll go ahead and look at it. Make sure&nbsp; we have, that looks good. Let's see here. 10… 10&nbsp;&nbsp; million rows, okay. So that seems fine. Let's go&nbsp; ahead and do a little pretend feature engineering.&nbsp;&nbsp; Great, right? Because we're only using 1% of&nbsp; our data. Memory looks fine. Okay. So that is&nbsp;&nbsp; sampling. Obviously, very, very slow with swap&nbsp; and we lose a lot of information from our data. So what is NVIDIA Unified Virtual Memory or&nbsp; UVM? Unified Virtual Memory is a system that&nbsp;&nbsp; allows both CPUs and NVIDIA GPUs to access single&nbsp; shared memory address space. It automatically and&nbsp;&nbsp; transparently moves between CPU and GPU memory&nbsp; as needed based on where it is accessed. It leads&nbsp;&nbsp; to cleaner code as memory management, and&nbsp; heterogeneous systems are abstracted away,&nbsp;&nbsp; and very importantly, applications can&nbsp; use data sets larger than GPU memory,&nbsp;&nbsp; with the system managing data movement&nbsp; between CPU and GPU as needed under the hood.&nbsp;&nbsp; Using Unified Virtual Memory does not require any&nbsp; code changes, swap space is turned off to prove&nbsp;&nbsp; no disk is being used. Free command shows zero.&nbsp; Always, always, always run. The Magic Command&nbsp;&nbsp; ‘%load_ext cudf.pandas’ before importing pandas to&nbsp; ensure GPU acceleration. I will go ahead and show&nbsp;&nbsp; the GPU memory widget from nv_dashboard like this.&nbsp; CPU memory is empty. As this runs, GPU memory will&nbsp;&nbsp; fill up here and then spill to CPU memory, not&nbsp; disc or swap. You see, CPU memory starting to&nbsp;&nbsp; be consumed. I'll speed up the video here. Okay,&nbsp; 13 seconds, way faster and way easier than using&nbsp;&nbsp; swap or sampling. Your current pandas code will&nbsp; work as it is written today to use this method. Just a quick side note on nv_dashboard.&nbsp; This is one of my favorite technologies&nbsp;&nbsp; from NVIDIA. nv_dashboard is a Jupyter&nbsp; Lab extension for displaying GPU usage&nbsp;&nbsp; and other profiling dashboards. It visualizes&nbsp; hardware metrics like this within Jupyter Lab,&nbsp;&nbsp; like GPU compute utilization, GPU memory&nbsp; consumption, PCIe throughput, and NVLink&nbsp;&nbsp; throughput if you have these technologies under&nbsp; the hood. To get started with nv_dashboard,&nbsp;&nbsp; it is a simple ‘pip install’ like&nbsp; here, or conda, whatever you like. To get started right away with NVIDIA Unified&nbsp; Virtual Memory, you can use Google Colab or&nbsp;&nbsp; Kaggle notebooks. As long as you make sure to&nbsp; use the free GPU. So we'll do that right here.&nbsp;&nbsp; Go to Change Runtime type, I'll go with the T4&nbsp; and click connect. This might take a second.&nbsp;&nbsp; Secondly, you have to make sure, always,&nbsp; always, always run this ‘%load_ext&nbsp;&nbsp; cudf.pandas’. And that's CUDA-X libraries&nbsp; for data frames. That is…like, so. Now,&nbsp;&nbsp; you have to do that before you do your import of&nbsp; ‘pandas as pd.’ That's kind of cool. All right,&nbsp;&nbsp; so the way we know, we're using Unified Virtual&nbsp; Memory and also CUDA for data frames, is if I&nbsp;&nbsp; look at the output here, let's see if I can zoom&nbsp; in here. All right. So this says ‘fast=cudf’,&nbsp;&nbsp; and this is where we're talking about your data&nbsp; frame is going to be synced between the GPU here&nbsp;&nbsp; and also CPU here. If we let me actually restart&nbsp; this to show what it looks like without it,&nbsp;&nbsp; let's see here. ‘Restart session’, I'll say ‘yes.’&nbsp; And I'll add… here we go. I'll do ‘import pandas&nbsp;&nbsp; as pd.’ And this is what it normally looks&nbsp; like, you see ‘module,’ ‘pandas.’ And then&nbsp;&nbsp; where it's installed, but you have to run&nbsp; this before ‘import pandas, ‘pandas as pd,’&nbsp;&nbsp; and see, this is how we know we're using Unified&nbsp; Virtual Memory and CUDA for data frames or cuDF. To quickly recap the three solutions, solution&nbsp; number one, increasing your swap space, this is&nbsp;&nbsp; not ideal. You're changing the fundamental&nbsp; nature of your operating system. Plus,&nbsp;&nbsp; as you perform operations on your data, you’re&nbsp; reading and writing from disk, instead of memory,&nbsp;&nbsp; this will be sluggishly slow. Solution number&nbsp; two, sampling. In theory, you can go as fast&nbsp;&nbsp; as you want. You can sample all the way down to&nbsp; just a single row of data, but you lose all the&nbsp;&nbsp; information, which might be critical for your&nbsp; analysis or modeling. The other problem is,&nbsp;&nbsp; you have to do stratified sampling, which is even&nbsp; more coding and more computational resources,&nbsp;&nbsp; which you're already lacking. Solution&nbsp; number three, NVIDIA Unified Virtual Memory.&nbsp;&nbsp; I highly recommend starting here if you have&nbsp; this ‘too much data for your memory’ situation.&nbsp;&nbsp; Unified Virtual Memory, you don't have to do&nbsp; anything different. The code in your pandas&nbsp;&nbsp; doesn't have to change. It comes already baked&nbsp; into CUDA for data frames or NVIDIA cuDF. I&nbsp;&nbsp; hope you try it out, I hope it works out for&nbsp; you, and as always, cheers and happy coding.
