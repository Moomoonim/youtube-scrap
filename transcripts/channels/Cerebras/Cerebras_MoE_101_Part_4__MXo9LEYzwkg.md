# Cerebras MoE 101 Part 4

- 영상 링크: https://www.youtube.com/watch?v=MXo9LEYzwkg
- 채널: Cerebras
- 업로드일: 2025-09-03
- 자막 언어: ko
- 단어 수: 약 1511개

---

## 스크립트

안녕하세요 여러분, 저는 다리아이고 MOE 101 가이드 시리즈에 다시 오신 것을 환영합니다. 지난 에피소드에서 우리는 아주 작은 가속 노드에서 자체 MOE 모델을 학습시킬 수 있었고, 심지어 몇몇 분들은 노트북에서도 학습이 가능했다고 보고했습니다. 우리는 그곳에서 전문가 네 명만 활용했지만, Deepsequ3와 같은 실제 운영 시스템에서는 수백 명의 전문가가 사용됩니다. 이전의 간단한 구현 방식으로 해당 규모에 맞춰 확장하려고 하면 메모리가 금방 부족해질 것입니다. 이번 에피소드에서는 확장성 문제에 대해 심층적으로 논의하겠습니다. 먼저 GPU가 이 문제에 접근하는 방식 과 그 한계에 대해 논의한 다음, Cerebras Vapor 스케일 엔진이 이 문제를 어떻게 해결하는지 살펴보겠습니다. 먼저 문제를 정의하는 것부터 시작해 보겠습니다 . 파트 3에서 보여드렸던 구현 방식을 기반으로 전문가 수를 4명에서 64명으로 늘려보려면, 제가 하는 것처럼 학습 실행을 시작하고 어떤 결과가 나오는지 확인해 보세요. 우리가 문제를 파악하는 데는 보통 시간이 좀 걸립니다. 단계별 설명은 생략하고 최종 결과를 보여드리겠습니다. 메모리가 부족해지고 있습니다. 그럼 이제 그 이유를 알아봅시다. 파트 3에서 전문가 믹싱이라는 함수를 정의하고, 이 함수는 교육 목적으로만 사용되며 효율적이지 않다고 경고했던 것을 기억하시나요? 그 이유를 좀 더 자세히 살펴보겠습니다. 215번째 줄을 보시면 모든 전문가를 아우르는 루프가 있는 것을 알 수 있습니다. 잠깐만요, 특정 토큰에 대해 전문가 중 일부만 활성화하는 것이지, 모든 전문가를 활성화하는 것은 아니잖아요? 그렇다면 왜 이 모든 전문가들의 정보를 메모리에 저장해야 하는 걸까요? 네, MOE 시리즈 2부에서 말씀드렸듯 이 업계 표준 라우팅은 확정적이지 않기 때문에 어떤 대화가 어떤 전문가에게 연결될지 미리 예측하는 것은 불가능합니다. 그러므로 나중에 필요할 수도 있으니 이 모든 전문가들을 메모리에 저장해 두어야 합니다 . 이렇게 생각해 보세요. DeepSequ 3는 특정 토큰에 대해 400억 개의 활성 파라미터만 사용했지만, 전체 파라미터 수는 6천억 개에 달했기 때문에 모델을 학습시키기 위해서는 모든 파라미터를 로드해야 했습니다. 이것이 바로 저희처럼 간단한 구현에서 메모리 부족 문제가 발생하는 이유입니다 . 일반적으로 GPU에서 모델을 실행할 때 메모리 부족 문제가 발생하면 모델 가중치를 여러 GPU 장치에 분산하는 방법이 여러 가지 있습니다. 그러니까 실제로 훈련시킬 수도 있다는 거죠. 우리는 특히 전문가 병렬 구조를 살펴볼 것입니다. 전문가 병렬 처리는 그림 8에 정의되어 있으며, 일반적으로 텐서 병렬 처리나 파이프라인 병렬 처리 등 다른 유형의 모델 병렬 처리와 함께 사용됩니다. 전문가 병렬 처리는 구체적으로 어떤 역할을 하나요? 각각의 전문가 네트워크를 서로 다른 GPU 장치에 배치하고 병렬로 실행해 봅시다. 진행 방식은 다음과 같습니다. 토큰 묶음이 라우터 네트워크로 이동하면 라우터는 해당 묶음에 있는 각 토큰을 처리하는 데 가장 적합한 전문가를 결정합니다. 그런 다음 모든 전문가와 그들의 장치에 해당 토큰을 배포하는 전체 통신을 수행합니다. 그런 다음 각 전문가는 자신의 토큰을 병렬로 실행합니다 . 그 후 추가적인 전문가 혼합 작업을 수행해야 하며, 이를 위해 활성화된 전문가 출력이 특정 토큰에 대해 동일한 장치에 존재하도록 또 다른 allto 통신 작업을 실행해야 합니다. 전문적인 병렬 처리는 우리가 실제로 대규모로 모델을 학습시킬 수 있는 방법입니다. 하지만 한계점도 있습니다. 첫째, 우리는 전문가 병렬 처리에서 네트워크의 각 전문가가 거의 동일한 양의 데이터를 수신하고 배치를 균등하게 분배한다고 가정합니다. 하지만 현실에서는 이러한 조건을 충족하지 못합니다 . 기억하시겠지만, 이 시리즈의 2부에서 업계 표준 라우팅이 균형을 보장하지 않는다는 것을 보여드렸습니다. 실제로 우리는 각 전문가가 특정 분야에 전문성을 갖추기를 원하며, 따라서 각 전문가 네트워크가 처리해야 할 데이터의 양이 불균형적으로 많을 수 있습니다 . 그 때문에 토큰을 적게 받는 전문가가 있는 일부 GPU 장치는 유휴 상태로 남아 클러스터의 전체 활용률이 낮아질 수 있습니다. 그뿐 아니라, 전문가 수를 계속 늘려감에 따라 각 전문가의 크기를 줄이고 더 많은 전문가를 활성화하는 것이 컴퓨팅 효율성 측면에서 더욱 유리해집니다 . 그 때문에 전문가 수준의 병렬 처리를 사용할 경우 통신 대비 연산 비율이 악화됩니다. 전문가 네트워크에서 우리는 유용한 연산을 수행하는 대신 대부분의 시간을 데이터를 이동하는 데 소비합니다 . 엄청난. 이제 여러분은 GPU 장치에서 전문가 수준의 병렬 처리를 통해 모델을 확장하고 학습시키는 방법을 알게 되었습니다 . 그렇다면 다른 액셀러레이터들은 MO 모델을 어떻게 다룰까요 ? 특히 세레브라의 웨이퍼 스케일 엔진을 살펴보겠습니다. 다음은 CS3 머신의 GPU와 비교할 수 있는 몇 가지 중요한 수치입니다 . 제가 특히 강조하고 싶은 매개변수는 CS3 장비에 탑재된 온칩 메모리의 용량입니다 . 저희 제품에는 44GB의 온칩 메모리가 있습니다. GPU 상의 SRAMM이라고 생각하면 됩니다. 아시다시피 최신 GPU 장치들은 대략 50메가바이트 정도의 메모리를 가지고 있습니다. 그러니까 거의 1,000배나 적은 거죠. 그 정도 용량의 온칩 메모리가 있다면, 모델 병렬 처리 없이도 매우 큰 모델을 칩 자체에 탑재할 수 있습니다. 증기 용량을 초과하는 모델의 경우 , 실제로 베이드 스트리밍이라고 하는 특정 유형의 실행을 유도합니다 . 베이트 스트리밍을 사용하면 연산 과 스토리지를 분리하고 메모리 X라는 별도의 노드를 생성하여 칩에 직접 연결합니다. 메모리 X에는 대부분 의 고성능 텐서가 저장되며, 칩 자체에는 특정 시점에 이러한 텐서가 저장되지 않습니다 . 칩이 기울기를 계산하면, 해당 기울기 계산 단계를 거친 후 가중치를 업데이트하기 위해 메모리 X 노드로 스트리밍합니다 . 이제 제가 무슨 말을 하려는지 아시겠죠? 우리는 사실상 전문가 수준의 병렬 처리나 다른 유형의 모델 병렬 처리 없이도 단일 CS3 머신으로 수조 개의 매개변수를 가진 모델을 학습시킬 수 있습니다. 하지만 하나의 CS3 머신에 거대한 모델을 탑재할 수 있다고 해서 효율적으로 학습된다는 의미는 아닙니다. 이 시리즈의 1부에서 MO 모델을 사용하면 밀집 네트워크에 비해 많은 연산량을 줄이고 학습 속도를 높일 수 있다고 언급했던 것을 기억하시나요? 특히, 32명의 전문가를 활용하면 동일한 손실 값을 기준으로 촘촘한 네트워크에 비해 블롭스(blops)를 약 60% 절감하고 모델 학습 속도를 3배 향상시킬 수 있다는 점에 대해 논의했습니다 . 이제 실제로 실험을 진행하여 처리량에 어떤 변화가 발생하는지 확인해 보겠습니다. 최근 출시된 모델 중 하나인 Gwen 3 모델을 사용하여 희소성 수준을 변경하고 CS3 머신의 처리량 변화를 관찰해 보겠습니다. 여기서는 활성 매개변수 수가 동일한 밀집 네트워크와 비교하여 MO 네트워크의 속도가 어떻게 변화하는지 확인할 수 있습니다 . 우리는 동일한 플롭스를 사용하기 때문에 MO 네트워크와 밀집 네트워크 간에 거의 동일한 처리량을 보일 것으로 예상했지만, 실제로는 속도 저하가 발생하고 있습니다. 왼쪽에서는 전문가 수를 줄이고 활성 전문가 수를 8명으로 고정합니다. 전문가 수를 늘리거나 우파로 치우치면 교육부 모델에 더 큰 희소성이 발생합니다 . 그림의 오른쪽에서는 전체 전문가 수를 128명으로 고정하고 토큰별로 활동하는 전문가 수를 다르게 설정합니다. 상위 K 값을 낮추면 모델이 더 간소해지고, 밀집된 네트워크에 비해 속도가 7배 느려지는 것을 확인할 수 있습니다. 즉, MOA 모델을 밀집 네트워크보다 더 빠르게 학습시킬 수 없으며, 안타깝게도 이러한 연산량 감소가 실제 실행 시간 단축으로 이어지지는 않을 것입니다 . 그렇다면 문제가 어디에 있는지 파악해 봅시다. 본질적으로 우리가 여기서 보고 있는 것은 컴퓨팅 자원 활용률 문제입니다. 우리는 많은 컴퓨팅 자원과 메모리를 보유하고 있지만, 어쩐지 효율적으로 활용하지 못하고 있습니다 . 여기서 중요한 점은 각 전문가 네트워크마다 배치 크기를 세분화해야 한다는 것입니다. 따라서 각 전문가는 해당 배치의 특정 부분을 전문적으로 담당하게 됩니다 . 그렇기 때문에, 특히 각 전문가의 크기를 줄일수록 전문가 네트워크에서 수행하는 연산량은 매우 최소화됩니다. 하지만 네트워크의 다른 계층을 살펴보면 , 텐션 계층은 배치 크기를 세분화하지 않으며, 큰 배치 크기는 실제로 성능에 큰 악영향을 미친다는 것을 알 수 있습니다. 왜? 주의 집중 계층은 실제로 활성화 메모리에 묶여 있기 때문입니다. 네트워크의 시퀀스 길이를 늘릴 수록 활성화 메모리가 제곱에 비례하여 증가합니다 . 결과적으로, 데이터 부족에 시달리는 전문가 계층이 할당된 컴퓨팅 리소스를 낭비하며 유휴 상태로 남아 있게 됩니다 . 동시에, 어텐션 레이어는 더 많은 데이터를 처리하는 데 어려움을 겪고 있습니다. 대량의 데이터를 처리하는 데는 그다지 효율적이지 못합니다. 이 문제를 해결하는 가장 간단한 방법 중 하나는 어텐션 레이어와 FFN 블록에 대해 배치 크기를 분리하는 것입니다. 어텐션 레이어는 효율성을 높이기 위해 더 작은 배치 크기가 필요하고, 전문가 네트워크는 더 큰 배치 크기가 필요합니다. 이제 실제로 Cerebra CS3 기기에서 구현해 봅시다. 저희는 어텐션 기반 일괄 스타일링이라는 기술을 개발했습니다 . 입력 데이터가 있고, 이 데이터는 배치 크기 B(시퀀스 길이)와 특정 은닉 차원을 가진다고 가정해 봅시다. 우리는 그것을 어텐션 레이어로 옮겨야 합니다. 그러니 어텐션 레이어에 막대한 예산을 투입하기 전에, 이를 G개의 개별 블록으로 나누어 병렬로 처리해 봅시다. 이렇게 하면 어텐션이 소프트맥스에서 나오는 거대한 텐서를 저장할 필요가 없으므로 더 효율적으로 작동합니다. 동시에, 저희 전문가 네트워크는 더 큰 배치 크기를 원합니다. 어텐션 레이어가 G 타일을 처리한 후에는 이를 더 큰 배치 크기로 다시 결합하여 전문가 네트워크로 이동합니다. 이렇게 하면 전문가들은 더 많은 데이터를 활용할 수 있으며, 사용자는 설정에 따라 이 G 값을 구성할 수 있습니다 . 이 기술은 실제로 GPU에서 효율적인 플래시 어텐션에서 영감을 받았습니다 . 주의력 활성화 메모리 문제를 해결하기 위해 순차적 차원에서 래쉬 주의력 타일을 사용합니다 . 본 연구에서는 이러한 접근 방식을 확장하여 전문가 네트워크에도 더 큰 배치 크기를 적용했습니다 . 그러면 어텐션 레이어와 전문가 네트워크 자체 간의 컴퓨팅 활용도를 균형 있게 조절할 수 있습니다. 시원한. 그럼 실제로 어떻게 작동하는지 알아보기 위해 몇 가지 실험을 해보겠습니다. 기존 MOE 구현 방식이 제대로 작동하지 않았다는 점을 기억하세요. 실제로 동일한 플롭 수에서 밀집 네트워크보다 속도가 느렸습니다. 여기서는 이를 기본 사항으로 간략하게 설명합니다 . 그러니까 빨간색 곡선이죠. 이제 저는 BTA를 이용한 실험도 진행하고 있으며, 이를 여러 차원에 걸쳐 확장하고 있습니다 . 전문가 수 또는 활동 중인 전문가 수. 보시다시피 BTA를 사용하면 플롭 수 또는 활성 파라미터 수가 동일하게 유지된다는 점에서 실제로 예상과 일치합니다. BTS의 성능은 밀집 모델 성능과 일치하며 다양한 희소성 수준에서도 안정적인 성능을 보이고 있습니다. 따라서 이를 통해 단일 CS3 머신에서 수조 개의 매개변수를 가진 모델을 적합시킬 수 있을 뿐만 아니라 효율적으로 학습시킬 수도 있습니다. BTA는 만능 해결책이 아니며, CS3 머신에서 사용했던 더 광범위한 최적화 툴킷의 한 기능일 뿐입니다. 이는 딥 시퀀서 3의 가중치에 대한 FP8 압축 이나 GPU에서 사용하는 고급 병렬 처리와 같은 다른 기능과 쉽게 결합될 수 있습니다. 여기서는 각기 다른 전문가 또는 전문가 그룹을 별도의 WC 장비에 배정하여 병렬로 가동하면 됩니다. 사실 그게 전부입니다. 이제 실제 하드웨어에서 모델의 크기를 조정하는 방법을 알게 되었습니다. 그럼 지금까지 이 시리즈에서 배운 내용을 정리해 보겠습니다. 1부에서는 MO의 기본 사항에 대해 배웠습니다. 2부에서는 작업량에 가장 적합한 라우터를 선택하는 방법을 알아보았습니다 . 3부에서는 AMA 모델을 처음부터 학습시켜 실제로 작동시키는 방법을 익혔습니다. 그리고 이제 당신은 세레바 하드웨어의 컴퓨팅 활용률 문제도 해결했습니다. 다음 에피소드에서는 학습에서 배포로 초점을 옮겨, emoy 모델을 배포할지 아니면 밀집 아키텍처를 고수할지에 대한 정보에 입각한 결정을 내리는 데 사용할 수 있는 실용적인 도구를 공유할 예정입니다. 감사합니다.
