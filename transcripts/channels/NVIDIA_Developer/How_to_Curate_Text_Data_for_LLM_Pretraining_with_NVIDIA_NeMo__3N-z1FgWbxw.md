# How to Curate Text Data for LLM Pretraining with NVIDIA NeMo Curator

- 영상 링크: https://www.youtube.com/watch?v=3N-z1FgWbxw
- 채널: NVIDIA Developer
- 업로드일: 2025-05-16
- 자막 언어: ko
- 단어 수: 약 1290개

---

## 스크립트

안녕하세요 여러분, 저는 모란 마구미입니다. 오늘은 Nemo Curator를 활용한 모델 사전 학습 사용 사례에 대한 개요를 소개해 드리겠습니다. 핵심은 LLM 사전 학습을 위해 일부 데이터를 선별하는 것입니다. 이 튜토리얼은 저희 GitHub 저장소(URL)에서 공개적으로 이용 가능하며, tiny stories 데이터 세트를 정리하는 데 중점을 두고 있습니다 . 이 데이터 세트는 GPT 3 및 GPT 4 계열 모델로 생성된 단편 소설 모음 이며, 3~4세 어린이가 이해할 수 있는 단어로 구성된 어린이 이야기입니다 . 이 데이터 세트는 다음 URL의 hugging face에서 이용할 수 있으며, 학습용 200만 행과 검증용 약 2만 행으로 구성된 두 부분으로 나뉩니다 . 설명을 간소화하기 위해 검증 데이터 세트를 선별하는 데 집중하겠습니다 . 그럼 코드를 살펴보고 이 데이터 큐레이션 파이프라인을 구성하는 다양한 요소들을 모두 이해해 보겠습니다. 간략히 말하자면, 우리가 해야 할 일은 먼저 이 데이터 세트를 확보하고 네모 큐레이터가 이해할 수 있는 형식으로 변환하는 것입니다 . 그 후, 이 데이터 세트에 대해 수행될 처리 단계를 정의할 수 있습니다. 이러한 처리 단계가 모두 완료되면 모델 학습에 적합한 데이터 세트를 얻게 됩니다 . 먼저 다운로드 및 변환 단계에 집중해 보겠습니다. 이 작업은 main.py 파일 안에 있는 다운로드 및 JSONL 변환 함수에서 수행됩니다. 이것이 이 튜토리얼의 주요 진입점입니다 . 우리가 하려는 것은 프레임워크가 해당 데이터 세트를 찾을 수 있는 경로를 알려주고, 그 데이터를 JSON L 형식으로 변환하는 것입니다. JSON 형식을 사용하는 이유는 이 형식이 널리 사용되고 다양한 프레임워크에서 지원되며, Neo Curator에서도 지원하는 가장 간단한 형식 중 하나이기 때문입니다. 이 데이터 세트를 다운로드하고 처리하는 작업은 문서 파일에 기록되어 있습니다 . 그럼 저쪽으로 가보죠 . 저희가 보유한 것은 Nemo Curator의 문서 모듈 중 일부를 구현한 것입니다. 기본적인 아이디어는 먼저 데이터 세트를 다운로드한 다음, 해당 데이터 세트 내의 레코드를 반복 처리하는 방법을 정의하는 것입니다. 그런 다음 이 로직을 사용하여 Neo Curator가 해당 데이터 세트를 적절한 형식(이 경우 JSON L)으로 변환할 수 있습니다. 따라서 데이터 세트 다운로드는 상당히 간단합니다. 우리가 할 일은 Nemo Curator의 문서 다운로더 클래스를 구현하고, 다운로드 함수 내부에 로직을 구현하는 것뿐입니다. 그 작업이 완료되면, 네모 큐레이터에게 이 데이터 세트를 어떻게 순회해야 하는지 알려주고 싶습니다. 이는 문서 반복자 클래스와 반복자를 구현하고 반복 로직을 구현함으로써 이루어집니다. 그래서 저희는 파일 전체를 순회하면서 구분자 토큰을 찾는 for 루프를 사용합니다. 이 구분자 토큰은 데이터 세트별로 특정 데이터를 해석하는 데 필요한 정보를 제공하는 토큰입니다. 또한, 파싱하는 모든 레코드에 파일 이름, ID 등의 메타데이터를 추가합니다. 이는 추적성을 위한 것이며, 마지막으로 문서 추출기를 구현하는데, 이는 Nemo Curator에게 이 데이터 세트에서 레코드를 추출하는 방법을 알려주는 역할을 합니다 . 이 구현이 완료되면 나머지 코드에 통합하기만 하면 이 데이터 세트를 다운로드하고 작업할 수 있습니다. 이 튜토리얼에서 해당 단계를 수행하면 텍스트 파일이 생성됩니다. 이 텍스트 파일에는 기본적으로 모든 원시 레코드가 포함되어 있으며, JSON 파일로 변환이 완료되면 파싱된 레코드에 해당하는 JSON 파일이 생성됩니다. 여기서도 보시다시피 여러 개의 JSONL 파일이 있습니다. 각 파일에는 대략 10,000개의 레코드가 포함되어 있으며, JSON 파일에는 기본적으로 각 레코드의 내용과 함께 텍스트 필드가 있고, 선택적으로 파일 이름 및 해당 레코드에 할당된 ID와 같은 일부 메타데이터가 포함됩니다 . 따라서 다음 단계는 데이터 큐레이션 파이프라인이 이 데이터 세트에 대해 어떤 종류의 작업을 수행할지 정의하는 것입니다. 제가 여기서 한 일은 이 데이터 세트를 다루기 위해 일련의 필터와 수정자를 정의한 것입니다. 이러한 모든 작업은 이 실행 큐레이션 파이프라인 함수 내에 포함되며, 특히 여기에서 큐레이션 단계를 정의합니다. 간략히 말씀드리면, 저희 파이프라인은 네 가지 단계로 구성되어 있습니다 . 데이터 세트를 정리하고 통합하고 , 데이터를 필터링하고, 중복된 데이터를 제거하고, 모든 개인 식별 정보를 삭제합니다. 이것들을 좀 더 자세히 설명해 드리겠습니다. 첫 번째 단계는 이 데이터 세트를 정리하고 통합하는 것입니다. 그래서 이 데이터 세트를 다운로드하면 바로 데이터에 몇 가지 불일치가 있다는 것을 알게 될 것입니다 . 예를 들어, 이 데이터 세트 내의 따옴표 중 일부는 Mac 운영 체제에서 흔히 볼 수 있는 방향 따옴표이고, 나머지는 Linux나 Windows 운영 체제에서 일반적으로 볼 수 있는 일반 따옴표입니다. 우리가 하려는 것은 이러한 다양한 것들을 모두 하나로 통합하는 것입니다. 그리고 추가적으로, 이 데이터 세트 내의 모든 단일 노드 문자가 균일하고 동일한 인코딩 방식을 사용하는지 확인하고 싶습니다. 이를 위해 두 가지 문서 수정자를 구현합니다. 인용문 수정자, 인용문 통합자 및 통합자 포맷터. 그럼 하나씩 살펴보겠습니다 . 앞서 언급했듯이, 인용문 통합자는 문서 수정자의 구현체입니다. Curator에서 문서 수정자는 기본적으로 데이터 세트에서 레코드 하나를 가져와 어떤 방식으로든 수정하는 모든 작업을 의미합니다. 문서 수정자를 만들려면 해당 클래스를 서브클래싱하고 문서 수정 함수를 구현하기만 하면 됩니다 . 여기서 우리는 단순히 방향을 나타내는 작은따옴표와 큰 따옴표를 모두 찾아서 그것들을 작은 따옴표나 큰따옴표 기호로 바꾸기만 하면 됩니다. 이건 꽤 간단합니다. 다음은 고유 리 포맷터인데, 이는 이미 네모 큐레이터에 구현되어 있으므로 바로 사용할 수 있습니다. 이 부분을 직접 구현할 필요는 없습니다. 하지만 이 단계가 완료되면, 즉 데이터 세트를 정리 및 통합 함수에 입력하면, 모든 레코드가 정리되고 균일해진 데이터 세트를 얻게 됩니다. 돌아가자. 이 파이프라인의 두 번째 단계는 필터링 작업입니다. 여기서는 Nemo Curator에서 제공하는 몇 가지 필터를 소개하고, 자신 만의 필터를 구현하는 방법도 보여드리겠습니다 . 이 경우 우리가 하려는 것은 데이터 세트에서 길이가 80단어 미만인 모든 레코드를 필터링하는 것입니다. 다시 말해서, 이 데이터 세트에서 단편 소설을 모두 걸러내고 싶다는 뜻입니다 . 실제 사용 사례에서는 너무 길거나 너무 짧은 문서를 삭제해야 하는 상황이 발생할 수 있습니다. 그리고 이 단어 수 필터는 Nemo Curator에서 이미 해당 목적으로 제공하는 필터 중 하나입니다. 또한, 저희는 자체적으로 ' 미완성 스토리 필터'라는 필터링 기능을 구현했습니다. 이것은 사용자 지정 문서 필터를 만드는 방법을 보여주기 위한 예시 필터입니다. 자신만의 문서 필터를 만들려면 문서 필터 클래스를 상속받고 두 가지 함수를 구현하기만 하면 됩니다 . 점수를 매기고 문서를 보관하세요 . 문서 점수 지정은 문서 필터에게 주어진 문서에 점수를 매기는 방법과 해당 문서를 유지할지 폐기할지 결정하는 데 사용되는 점수 기준을 알려줍니다. ` keep document` 함수는 기본적으로 부울 값을 반환하며, 해당 문서를 유지해야 할지 아니면 삭제해야 할지를 모듈에 알려줍니다. 이 구현에서 우리가 가지고 있는 것은 불완전한 스토리 필터 클래스인데, 기본적으로 모든 기록에서 결말을 찾는 역할을 합니다 . 만약 해당 기록이 느낌표, 물음표, 따옴표 또는 마침표와 같은 종료 문자로 끝나지 않으면 해당 문서는 폐기됩니다. 보시다시피 문서에 점수를 매기는 작업은 상당히 간단합니다. 여러분이 해야 할 일은 문서가 여러분이 원하는 결말로 끝나는지 확인하고, 그렇지 않다면 false를 반환하는 것입니다. 만약 이 클래스가 문서 유지 함수에서 false를 반환하면 해당 문서는 폐기됩니다. 자, 그럼 이제 나머지 필터링 작업들을 다시 살펴보겠습니다. 그래서 저희는 Nemo Curator에서 제공하는 필터 중 일부, 특히 반복되는 최상위 엔그램 필터를 사용하기로 했습니다. 이는 특정 비율의 반복 엔 그램을 가진 문서를 걸러내는 잘 알려진 필터입니다. 그리고 우리는 이 필터를 서로 다른 매개변수를 사용하여 여러 번 적용할 수 있습니다 . 필터링 단계를 모두 정의했으면 이제 함수 호출을 통해 데이터 세트에 적용하기만 하면 됩니다. 자, 이제 파이프라인의 정의로 돌아가 보겠습니다. 지금까지 우리는 정리와 통합, 그리고 필터링에 대해 논의했습니다. 이제 데이터 큐레이션 파이프라인에서 중요한 작업 중 하나인 데이터 복제에 대해 논의해 보겠습니다. 이 예시에서는 정확한 복제 과정을 보여드리겠습니다. 이 아이디어는 지금까지 우리가 가지고 있는 모든 문서를 검토하여 다른 문서와 완전히 동일한 문서가 있으면 해당 문서를 폐기하는 것입니다. 이는 데모 큐레이터 내에서 정확한 복제 클래스를 사용하여 구현됩니다 . 음, 기본적으로 중복된 문서의 ID를 모두 찾은 다음 해당 ID를 기준으로 중복된 문서를 제거합니다 . 그리고 이 처리 과정의 마지막 단계는 개인 식별 정보를 삭제하는 것입니다. 물론 이 데이터 세트는 예시 데이터 세트이며 제거해야 할 개인 식별 정보(PII)가 많지는 않지만, 이름이 많이 포함되어 있습니다. 여기서 우리가 하려는 것은 네모 큐레이터 모듈에게 모든 사람 이름을 찾아서 익명 토큰으로 바꾸도록 지시하는 것입니다. 그리고 이렇게 필터를 정의하고 나면 함수 호출을 통해 데이터 세트에 간단히 적용할 수 있습니다. 좋아요. 자, 다시 돌아가서 모든 큐레이션 단계를 살펴보고, 여기에 표시된 것처럼 이러한 단계를 순차적으로 데이터 세트에 적용할 수 있습니다. 그리고 Nemo Curator 에서 내부적으로 사용하는 Dask는 작업을 지연 실행 방식으로 수행하기 때문에, 그래프를 계산하고 결과를 반환하도록 지시하기 위해 persist 함수를 호출해야 합니다. 그렇게 하면 파이프라인의 마지막 단계는 모든 데이터 세트를 디스크에 저장하는 것인데, 이는 몇 가지 API 호출을 통해 수행됩니다. 이 튜토리얼을 실행하면, 허깅페이스에서 다운로드한 검증되지 않은 데이터 세트가 들어 있는 데이터 폴더가 생성되는 것을 볼 수 있습니다 . 그러면 JSON 폴더가 있고, 그 안에 JSONL 폴더가 있는데, 앞서 보여드린 것처럼 그 안에는 정리 되지 않은 JSONL 파일과 정리된 버전의 JSONL 파일이 있습니다. 그리고 그 파일들을 자세히 살펴보시면, 이 파일들이 이미 그러한 처리를 거쳤다는 것을 알 수 있을 겁니다. 그리고 모든 1인칭 이름이 익명 토큰으로 대체되어 있는 것을 확인할 수 있을 것입니다. 자, 이것으로 이번 튜토리얼을 마치겠습니다.
