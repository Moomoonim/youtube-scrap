# Pinecone #3 - Inserting Data

- 영상 링크: https://www.youtube.com/watch?v=HjeW6ed2dmI
- 채널: Pinecone
- 업로드일: 2022-01-03
- 자막 언어: en
- 단어 수: 약 667개

---

## 스크립트

in this video we're going to have a look at how we can upsert our vectors into a pine cone index so we start by first if you haven't already you need to put install the pine cone client and then you can import pine cone and initialize your connection to Pine Cone using your API key which you will need to put here you can find your API key over here once that's done we will need to create create an index to upsert our vectors to and we can do that using the create index method we're going to be creating vectors with dimensionality of 128 so I've included that in there now I'm going to create some some random vectors using the random module so I'm going to write random. random and I'm just going to create uh 128 random values for for each Vector okay so we now have five IDs and five or we will have five vectors to go with them so I need to add that in here okay now to upsert our vectors we first need to actually connect to an index so to do that we're going to connect to our example index up here and all we do is write index equals Pine cone. index and we specify name of our index here and once we've done that all we do is index. upset we use the vectors parameter and all we need to do is pass both out IDs and our vectors and we can see that we've just upsert five vectors there so that's the simplest way of upserting but of course most of the time you're not only going to be upserting five vectors you're going to want to be upserting more so what you can do is batch your upserts over several requests so what we're going to do here we're going to create 10,000 vectors and what we have here is an example generator that will will generate those IDs and and Vector pairs so you've seen here we just have the same code that we use up here to create a vector so that will create our IDs and vectors and what we're going to do is use this chunks function and what that will do is chunk our generated vectors into batches of 100 set by this batch size variable so we can we can run that and all we do is go through that so for trunk in in Trunks and we want to pass our example data generator into there we are just going to write index. upsert we have our vectors and we are just passing in that trun same exactly the same as what we did up here so we'll run that and that took us 10 seconds to upset using our truns now what we can do is if you have a lot of data that you you want to upsert you can actually do your upserts in parallel so using the pine con client we can use asynchronous upsets so what we do is we have our pine cone index again we specify the number of threads we will use and we're specifying that as index just like we did up here okay but this time we modify the the function a little bit to allow a synchronous upsets we then send those requests in parallel so all we're doing here is we have our async results and here we just do our upset making sure that we add async required equals true we do that for each chunk so before we had chunk like this in our chunks so we can remove the size that's the default and that will do what we did before but it will do everything as serously so then we can run that and we're done straight away so it is much faster so that's how you would insert data or upsert data into your pine cone index
