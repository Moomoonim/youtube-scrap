# [ifkakao2021] 블록체인도 영수증이 있다니! Receipt로 TX 결과를 확인해 보자

- 영상 링크: https://www.youtube.com/watch?v=38kb7K8CxfU
- 채널: kakao tech
- 업로드일: 2026-06-11
- 자막 언어: ko
- 단어 수: 약 1135개

---

## 스크립트

안녕하세요. 그라운드 X의 세바시입니다. 오늘은 블록체인도 영수증이 있다니 리시브로 TX 결과를 확인해 보자라는 주제로 세션을 진행하겠습니다. 목차는 리십이란 클립이 리십을 확인하는 방법, 발생했던 이슈, 향후 발전 사항 순으로 진행됩니다. 우리 사회에서 영수중은 거래에 대한 증명서입니다. 영수 중에는 누가, 언제, 어디서, 무엇을, 얼마에 구입했는지에 대한 정보가 나타납니다. 그렇다면 블록체인 세상의 영수증은 어떤 정보를 담고 있는지 지금부터 설명해 드리겠습니다. 영수증을 발급받기 위해서는 거래를 하여야 합니다. 블록체인의 트랜잭션은 유저간 송금 스마트 컨트랙트 실행을 위해 사용합니다. 이렇게 생성된 TX는 각각 고유한 해시값을 갖게 됩니다. TX는 블록에 반영이 되면 거래가 완료되며 그 이후부터 해시값을 통해 리십을 확인할 수 있습니다. 리십은 클레이겟 트랜잭션 리십 RPC 코를 이용해 얻을 수 있습니다. 해당 RPC는 클레튼 2 EN을 직접 띄우거나 그라운드 X에서 제공하는 카스 서비스를 이용해 접근하실 수 있습니다. 카스를 사용하면 EN을 직접 띄우지 않고 플레이튼 블록체인과 관련된 API를 편하게 사용하실 수 있으니 카스 사용을 관장드립니다. RPC를 호출하면 아래와 같이 제이슨 형태의 리십을 얻을 수 있습니다. 리십에는 블록 정보, TX 정보, 보낸 사람, 받는 사람, 금액, 처리 결과, 수수료, 컨트랙트, 이벤트 로그 등의 정보가 들어 있습니다. 다음과 같이 블록체인의 리십은 누가 언제, 얼마를 어떻게 보냈는지, 그리고 처리 결과에 대한 정보들을 가지고 있습니다. 저희 클립은이 정보들 중 처리 결과인 스테이터스와 로그스를 확인하여 필요한 작업을 합니다. 그러면 클립 서비스와 클립이 리시브을 어떻게 확인하고 추가 작업을 하는지 알아보겠습니다. 클립은 카카오톡과 연결된 블록체인 지갑 서비스입니다. 클립을 통해서 플레이트 네트워크상의 자산인 클레이와 NFT 등을 쉽게 관리하실 수 있습니다. 또한 앱투 앱, 드롭스 같은 서비스를 통해 다른 블록체인 애플리케이션과 NFT의 사용 경험도 얻으실 수 있으니 저희 클립 지갑을 많이 예용해 주시면 감사하겠습니다. 클립은 지갑 서비스를 제공하기 위해서 많은 블록체인 TX들을 생성합니다. 그리고 생성한 TX들의 결과를 확인하고 필요한 처리를 하기 위해 TX 리시브를 확인하고 있습니다. 클립이 TX를 생성하는 과정입니다. 오늘의 주제인 리시을 확인하기 위해선 TX가 필요합니다. 그래서 클립이 TX를 만드는 과정에 대해 간단하게 설명하겠습니다. 유저는 클립에 원하는 TX의 전송을 요청합니다. 그러면 클립은 해당하는 타입의 TX를 생성한 후 TX를 클레이트 블록체인에 전달합니다. 하지만 이렇게 전달된 TX가 실제 블록에 반영되기 위해선 약 1에서 2초가 소요됩니다. 이로 인해 TX 결과를 바로 알 수 없고 결과에 대한 동기적 처리가 불가능합니다. 결국 블록이 생성되기까지 걸리는 시간이 있었기 때문에 클립은 TX 결과 처리를 비동기적으로 할 필요가 있었습니다.이를 처리하기 위해 클립은 리시브 체커라는 모듈을 구현하였습니다. 리십 체커는 메시지 큐와 워커스라는 백그라운드 쓰레드들로 이루어져 있습니다.이를 사용하여 저희 클립은 리십과 관련된 비동기 작업을 처리합니다. 리십 체커의 동작은 다음과 같습니다. 왼쪽부터 API, 메시지 Q큐, 워커스의 동작이며 새로의 흐름을 나타냅니다. 유저의 TX 생성 요청이 있을 경우 API는 요청에 해당하는 TX를 만들고 클레이트 네트워크에 전송합니다. 그 후 생성한 TX의 해시값을 메시지 큐에 푸시합니다. 이렇게 메시지 큐에 추가된 TX 해시는 백그라운드에서 돌고 있는 워커스레드가 가져옵니다. 워커스레드는 TX 해시를 이용해 리십을 가져오고 필요한 비동기 로직을 처리합니다. 워커스레드는이 작업을 계속 반복하며 클립에서 생성하는 TX들의 결과를 확인하고 처리합니다. 다음은 클립에서 처리하는 비동기 로직들입니다. 클립은 DV 동기와 카카오 메시지 전송, 캐시 관리 등을 비동기로 처리하고 있습니다. DB 동기화란 블록체인과 DV의 데이터를 동기화하는 작업입니다. 클립 앱투앱, 드랍스 등의 서비스는 TX 결과 확인 후 데이터베이스의 결과를 업데이트하는 작업이 필요합니다. 해당 작업은 컨트랙트 이벤트 로그를 확인해야 하기 때문에 리십 체커가 필요했습니다. 다음은 카카오 메시지 전송입니다. 저희 클립은 유저가 요청한 TX의 결과를 카카오톡 메시지로 전성해 주는 기능이 있습니다.이 기능을 위해선 TX의 결과 상태를 알아야 하기 때문에 리시 확인이 필요합니다. 클립은 빠른 응답을 위해 유저의 자산 정보를 캐싱에서 들고 있습니다. 자산 정보는 유저의 TX 결과에 따라 변하기 때문에 리십을 확인하고 캐시를 리프레시하는 작업도 하고 있습니다. 클립은 이렇게 블록체인 TX 결과에 따라 필요한 로직들을 모두 리십 체커를 사용해 처리하고 있습니다. 결과를 처리할 때 컨트랙트에서 발생한 이벤트를 기반으로 필요한 코드를 실행시킬 때가 있습니다.이를 이를 위해선 리시브의 로그스 값을 통해 어떤 이벤트들이 발생했는지를 확인해야 합니다. 컨트랙트에서 발생한 이벤트들은 로그스의 베열 형태로 저장됩니다.이 중 토픽스의 0번 인덱스 값은 이벤트의 시그네처로 해당 이벤트가 어떤 이벤트인지를 나타냅니다.이를 통해 백핸드에서 어떤 이벤트가 발생했는지 알고 필요한 대처를 할 수 있습니다. 토픽스의 나머지 인덱스는 발생한 이벤트의 인덱스드 파라미터입니다. 이벤트를 발생시킬 때 원하는 매개 변수들을 넣어 주면 백엔드에서 해당 값을 확인할 수 있습니다. 이벤트 시그네처를 구하는 방법입니다. 이벤트는 다음과 같은 문법으로 컨트랙트에 선언됩니다.이 이벤트를 이벤트 이름과 외계변수 타입만 남긴 문자열로 수정하고 문자열의 캐착오르 해시를 적용하면 이벤트 시그네처를 구할 수 있습니다. 이렇게 구한 시그네처를 사용하면 TX에서 내가 원하는 이벤트가 발생했는지 알 수 있습니다. 이제 지금까지 클립비 리십 체커를 구현하며 있었던 이슈들을 공유해 드리겠습니다. 지금 세션을 들으시는 분들은 저희와 같은 상황에서 동일한 이슈가 없으시길 바랍니다. 첫 번째는 다른 환경 버전의 백엔드가 같은 메시지 큐를 사용했던 경우입니다. 저희 클립은 스테이징 환경 테스트를 위해 프로덕션과 동일한 환경에서 세니티 체크를 진행합니다. 이때 발생했었던 이슈로 프로덕션과 스테이지 환경이 같은 메시지 큐를 사용하며 이슈가 발생하였습니다. 배포한 스테이징 환경에서 테스트를 진행할 때였습니다. 스테이징 환경에서 생성한 TX를 리십 체커가 추가 처리를 못 하고 사라지는 현상이 생겨 개발팀에서 분석을 했었습니다. 분석 결과 원인은 프로덕션과 공인되어 있는 메시지 큐였습니다. 저희가 원했던 작업은 그림처럼 스테이징 환경에서 만든 TX를 스테이징 환경의 워커가 처리하는 거였습니다. 하지만 공유된 메시지 키로 인해 프로덕션 환경의 워커가 작업을 받아 문제가 되었습니다. 해당 버전에는 없는 작업이었기 때문에 아무 작업도 없이 사라진 것처럼 보인 것이었습니다. 해당 이슈는 스테이징 환경의 메시지 큐를 변경한 후 바로 사라졌습니다. 간단한 환경 이슈였지만 미리 파악하지 못했던게 아쉬웠던 케이스입니다.이는 이는 메시지 큐와 워커프를 활용하는 모든 케이스에 적용되기 때문에 비슷한 구조를 사용하시는 다른 분들은 이런 경우를 겪지 않았으면 좋겠습니다. 다음은 그레이스풀 셧다운입니다. 클립 백엔드가 오토 스켈링, 롤링 업데이트 등으로 기존에 인스턴스가 내려갈 때 생기는 이슈입니다. 백엔드가 내려갈 때 아직 워커들이 작업 중일 수 있습니다. 워커가 작업 중에 인스턴스가 내려가면 해당 작업은 유실되어 0 마무리할 수 없게 됩니다.이를 이를 막기 위해 저희 클립은 종료 시그널이 들어왔을 때 워크가 현재의 작업까지만 마무리하도록 코드를 수정하였습니다. 아래는이를 위한 섀도 코드입니다. 메인 쓰레드에서는 종료 시그널 시 워커 플래그를 벌스로 바꾸고 워커가 모두 종료될 때까지 기다립니다. 워커 쓰레드는 작업 시작시 카운트를 증가시키고 필요한 작업을 계속 반복합니다. 반복하다 메인 쓰레드에서 워커 플래그를 버스로 바꾸면 현재의 작업만 마무리하고 더 이상의 추가 작업 없이 반복을 종료하는 구조입니다. 이후 쓰레드가 종료되었다는 의미로 워커 카운트를 감소시키면 메인 쓰레드에서 모든 워커 쓰레드가 종료된 걸 확인할 수 있습니다. 백그라운드에서 도는 쓰레드들의 관리는 아주 중요합니다.이 세션을 들으시는 분들도 쓰레드들이 원하는 대로 잘 종료되는지 확인해 보시면 좋을 것 같습니다. 향후 발전 사항입니다. 앞으로 클립이 리십 체커를 어떻게 발전시키려 하는지 공유하겠습니다. 현재 클립 백엔드는 다음과 같이 API와 워커가 한 인스턴스에 같이 붙어 있는 구조입니다.이는 코드 관리가 쉬웠으나 다음과 같은 단점이 있었습니다. 순간적으로 유저의 요청이 몰렸을 경우 백엔드는 오토 스케일링을 통해 인스턴스를 늘리게 되어 있습니다. 하지만 API와 워커가 붙어 있는 백엔드 구조로 인해 원하지 않던 워커의 수가 같이 늘어나게 됩니다. 저희는이 문제를 해결하기 위해 API와 워커팟을 분리하려고 합니다.이를 통해 API가 필요한 경우 API 판만 증가시키고 워커가 필요한 경우 워커판만 증가시켜 자원을 좀 더 효율적으로 사용하는 구조를 만들려고 합니다. 오늘 세션의 요약입니다. 블록체인도 현실 세계처럼 영수 중이 있습니다. 영수 중에는 누가 언제 얼마를 어떻게 보냈는지 그리고 그에 대한 결과 정보가 들어 있습니다. 클립은 카카오톡과 연동된 블록체인 지갑입니다. 클립을 사용하면 클레튼 네트워크상의 자산을 쉽게 관리할 수 있으며 앱투앱, 드롭스와 같은 서비스도 사용하실 수 있습니다. 클립은 리십 체커라는 모듈을 사용해 TX 결과를 확인합니다. 리십 체커 모듈의 구조와 발생했던 이슈 그리고 앞으로 어떻게 발전시키려 하는지를 오늘 공유드렸습니다. 많은 블록체인 애플리케이션이 TX의 결과를 확인하는 과정을 필요로 합니다. 오늘 저의 세션이 다른 블록체인 개발자들에게 많은 도움이 되었으면 좋겠습니다. 이번 발표는 여기까지입니다. 들어주셔서 감사합니다.
