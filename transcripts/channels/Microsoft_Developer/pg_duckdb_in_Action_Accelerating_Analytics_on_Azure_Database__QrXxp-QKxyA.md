# pg_duckdb in Action: Accelerating Analytics on Azure Database for PostgreSQL | POSETTE 2026

- 영상 링크: https://www.youtube.com/watch?v=QrXxp-QKxyA
- 채널: Microsoft Developer
- 업로드일: 2026-06-18
- 자막 언어: ko
- 단어 수: 약 2558개

---

## 스크립트

안녕하세요 여러분, 저는 니틴 자드하브입니다. 마이크로소프트의 선임 소프트웨어 엔지니어 이며, Azure Database for PostgreSQL 팀에서 근무하고 있습니다. 오늘 여러분이 시청하실 영상은 "pg_duckdb 활용 사례: Azure Database for PostgreSQL에서 분석 속도 향상"입니다. PostgreSQL 데이터베이스에서 데이터를 다른 곳으로 내보내지 않고도 분석 쿼리에 더 빠르게 응답할 수 있기를 바라셨던 분들이라면 이 영상이 도움이 될 것입니다. 이 영상을 통해 pg_duckdb가 어떻게 이러한 기능을 구현하는지 정확히 이해하고 , Azure Database for PostgreSQL Flexible Server 에서 실제로 실행되는 모습을 확인하실 수 있습니다 . 자, 그럼 "pg_duckdb 활용 사례: Azure Database for PostgreSQL에서 분석 속도 향상"을 자세히 살펴보겠습니다 . 먼저 pg_duckdb가 무엇인지 간단히 소개하고 , PostgreSQL을 대체하는 것이 아니라 PostgreSQL 의 장점을 간략히 설명한 후, pg_duckdb가 채워주는 PostgreSQL 분석의 한계점에 대해 이야기하겠습니다 . 다음으로 DuckDB를 소개하고, PostgreSQL과 DuckDB 엔진을 나란히 비교한 후, PostgreSQL이 쿼리를 처리하는 내부적인 과정을 살펴보겠습니다. 그리고 pg_duckdb가 이러한 파이프라인에 어떻게 연결되는지 알아보겠습니다. 그 후, Azure Database for PostgreSQL 데모를 통해 pg_duckdb를 사용하면 어떤 작업을 수행할 수 있는지 설명하겠습니다 . 마지막으로 핵심 내용을 정리해 보겠습니다. 먼저 pg_duckdb가 무엇인지부터 설명하겠습니다. pg_duckdb는 DuckDB의 분석 엔진을 PostgreSQL 데이터베이스 내부에 통합하는 PostgreSQL 확장 프로그램입니다 . 따라서 데이터베이스를 변경하거나 데이터를 동기화할 필요가 없습니다 . 확장 프로그램을 활성화하기만 하면 동일한 PostgreSQL 서버 에서 컬럼형 벡터화 엔진을 사용하여 분석 쿼리를 실행할 수 있습니다. 또한 트랜잭션 워크로드에도 동일한 안정적인 PostgreSQL 서버를 사용할 수 있습니다. 마치 OLTP와 OLAP 기능을 한 곳에서 사용할 수 있는 것과 같습니다. 이것이 왜 중요한지 설명하기 전에, 먼저 PostgreSQL의 장점에 대해 간략히 살펴보겠습니다. PostgreSQL은 세계에서 가장 신뢰받는 오픈 소스 관계형 데이터베이스 이며, 그 명성을 얻을 자격이 충분합니다. ACID를 준수하므로 데이터 손실이나 불완전한 트랜잭션 발생을 방지할 수 있습니다. 또한, 풍부한 확장성, 사용자 정의 타입, 사용자 정의 연산자, 그리고 pg_duckdb와 같은 기능을 구현할 수 있는 확장 모델을 제공합니다. 뿐만 아니라, 활발한 오픈 소스 생태계를 자랑합니다. 수많은 기여자와 수십 년에 걸친 보안 강화가 PostgreSQL의 기반입니다. PostgreSQL은 OLTP, 높은 동시성, 많은 사용자, 초당 많은 소규모 트랜잭션 처리 등 다양한 환경 에서 검증된 성능을 보여줍니다 . B-트리, GIN, BRIN과 같은 다양한 접근 패턴에 맞는 풍부한 인덱스 세트를 제공하며, 은행, 항공사, 그리고 여러분이 즐겨 사용하는 SaaS 애플리케이션 등 전 세계의 핵심 업무용 애플리케이션에서 사용되고 있습니다 . 하지만 PostgreSQL은 분석 분야에서는 다소 취약한 모습을 보입니다 . 이는 매우 타당한 설계상의 이유 때문입니다. 이 부분은 나중에 자세히 살펴보겠습니다. 그 전에 OLTP와 OLAP가 무엇인지 먼저 간단히 이해해 보겠습니다. OLTP는 온라인 트랜잭션 처리(Online Transaction Processing)의 약자로, PostgreSQL이 탄생한 목적이기도 합니다. INSERT, UPDATE, DELETE 쿼리가 많이 사용되지만, 각 쿼리는 몇 개의 행만 처리합니다. 여기서는 쿼리당 지연 시간을 최소화하고 데이터를 행 단위로 저장하는 데 최적화합니다. 또한 수천 명의 사용자가 동시에 데이터베이스에 접근하기 때문에 높은 동시성을 요구합니다 . 반면 OLAP는 온라인 분석 처리(Online Analytical Processing)로, OLTP와는 정반대입니다. 쿼리 수는 적지만 각 쿼리의 규모가 매우 큽니다. SUM, AVG, COUNT와 같은 집계 함수를 사용하는 대규모 SELECT 쿼리가 대표적입니다. OLAP에서는 처리량을 최적화하고, 데이터는 컬럼형 스토리지에 저장됩니다. 일반적으로 많은 열 중에서 일부 열만 필요하며 , 단일 쿼리가 수백만 또는 수십억 개의 행을 스캔할 수 있기 때문입니다. OLTP는 수백만 개의 단일 행 쿼리와 같고, OLAP는 수백만 개의 행을 처리하는 단일 쿼리와 같다고 할 수 있습니다 . 같은 데이터이지만 처리 방식이 다릅니다. 그렇다면 PostgreSQL에 두 번째 작업(OLAP와 같은 작업)을 맡겼을 때 실제로 어떤 문제가 발생할까요? 세 가지 이유가 있습니다. 이것들 중 하나일 뿐, 버그는 아닙니다. 모두 OLTP에는 적합하지만 OLAP에는 적합하지 않은 설계상의 선택입니다. 첫 번째는 컬럼형 스토리지를 사용하지 않는다는 점입니다. PostgreSQL은 쿼리에서 여러 행 중 일부 열만 필요로 하더라도 전체 행을 읽습니다. 죄송합니다. 여러 열 중에서 하나를 선택해야 합니다. 이는 모든 분석 스캔에서 낭비되는 I/O입니다. 또한 벡터화된 실행이 지원되지 않습니다. 데이터를 한 번에 한 행씩 처리하므로 CPU 캐시와 SIMD 명령어가 대부분 사용되지 않습니다. 병렬 처리 능력도 제한적입니다. PostgreSQL에도 병렬 쿼리 기능이 있지만, OLAP 전용 엔진의 성능과는 비교할 수 없을 정도로 떨어집니다. 이러한 문제들은 은행 잔고 조회에는 큰 문제가 되지 않지만, 수십억 행이 넘는 집계 작업에는 심각한 문제가 될 수 있습니다. 행 기반 I/O 낭비가 어떤 것인지 보여드리겠습니다. 양쪽에서 `SELECT SUM of revenue FROM orders` 쿼리를 실행한다고 가정해 보겠습니다 . 왼쪽에는 행 기반 저장소인 PostgreSQL이 있습니다. 합계를 계산하기 위해 엔진은 모든 행과 4개의 열을 모두 읽어야 합니다. 실제로는 수익 열만 필요하지만 말입니다. 즉, 모든 페이지, 모든 블록, 모든 디스크 탐색에서 3개의 열에 대한 불필요한 I/O가 발생합니다. 오른쪽에는 컬럼 기반 저장소인 DuckDB가 있습니다. 각 열은 자체적인 연속된 청크에 저장됩니다. 따라서 엔진은 압축된 배치에서 수익 열만 읽습니다. 이것이 바로 성능이 크게 향상되는 이유입니다. 이제 DuckDB 자체에 대해 이야기해 보겠습니다. DuckDB는 프로세스 내 분석 SQL 데이터베이스입니다. 분석용 SQLite라고 생각하시면 됩니다. 크기가 작고, 내장 가능하며, 설정이 필요 없고, 분석 쿼리 속도가 엄청나게 빠릅니다. 게다가 오픈 소스입니다. 불과 몇 년 만에 프로세스 내 OLAP의 기본 선택지가 되었습니다. 이제 그 이유를 살펴보겠습니다. DuckDB를 특별하게 만드는 네 가지 요소가 있습니다. 첫째, 프로세스 내 실행 방식입니다. DuckDB 서버가 없습니다. 내장 라이브러리로 애플리케이션 내에서 실행됩니다. 배포해야 할 데몬도 없고, 포트를 열 필요도 없고, 클러스터 크기를 조정할 필요도 없습니다. 둘째, 컬럼형 및 벡터화된 실행 방식입니다. 데이터는 컬럼에 저장되고 한 번에 수천 개의 값 배치로 처리됩니다. 셋째, 자동 병렬 처리 기능입니다. DuckDB는 쿼리를 분석하여 가능한 모든 CPU 코어를 활용하려고 합니다. 네 번째는 바로 데이터 레이크를 기본적으로 지원한다는 점입니다. 이는 본 논의에서 매우 중요한 부분입니다. DuckDB는 로컬 디스크 또는 Azure Blob Storage에 저장된 Parquet, CSV, JSON, Iceberg 파일을 직접 읽고 쓸 수 있습니다 . 이제 두 엔진을 나란히 비교해 보겠습니다. 두 엔진 모두 동일한 관계형 모델을 사용하지만, 설계 방식에는 몇 가지 차이점이 있습니다. 첫 번째는 워크로드입니다. PostgreSQL은 OLTP 트랜잭션 처리에, DuckDB는 OLAP 분석에 적합합니다. 두 번째는 스토리지 방식입니다. PostgreSQL은 행 중심 스토리지를 사용 하는 반면, DuckDB는 컬럼형 스토리지를 사용합니다. 두 번째는 실행 방식입니다. PostgreSQL은 기존의 Volcano 반복자를 사용하여 한 번에 한 행씩 처리합니다. DuckDB는 벡터화된 배치 처리를 사용합니다. 세 번째는 동시성입니다. PostgreSQL은 MVCC(다변량 복합 쿼리) 를 통해 여러 사용자가 동시에 접속하도록 설계되었고, DuckDB는 한 번에 하나의 큰 쿼리를 처리하도록 최적화되어 있습니다. 어느 쪽이 더 우수하다고 단정할 수는 없습니다. 두 엔진은 각각 다른 문제를 해결하기 위해 설계되었습니다. 마지막 으로 병렬 처리 방식을 살펴보겠습니다. PostgreSQL은 병렬 쿼리를 지원하지만, 그 범위가 제한적입니다. 반면 DuckDB는 모든 CPU 코어에서 병렬 처리를 수행합니다. 데이터 소스 측면에서 보면 , PostgreSQL은 PostgreSQL 데이터베이스에 저장된 테이블을 읽지만, DuckDB는 Azure Blob Storage와 같은 객체 스토리지에 저장된 Parquet, CSV, JSON, Iceberg 파일도 로드하지 않고 읽을 수 있습니다 . 아키텍처 또한 다릅니다. PostgreSQL은 고전적인 클라이언트-서버 데이터베이스인 반면, DuckDB는 호출하는 프로세스 내부에 내장되어 실행됩니다. 그리고 pg_duckdb가 바로 그런 방식입니다. 이제 핵심 내용을 살펴보겠습니다. 왼쪽에는 ACID, MVCC를 지원하고 성숙한 생태계를 갖춘 OLTP 강자인 PostgreSQL이 있습니다. 유일한 약점은 분석 기능입니다. 오른쪽에는 컬럼형 아키텍처와 벡터화 방식을 채택하고 OLAP 처리 속도가 매우 빠르며 Parquet과 Iceberg를 기본적으로 지원하는 DuckDB가 있습니다. 단점은 트랜잭션 처리가 불가능하고 OLTP 동시 실행이 제한적이라는 것입니다. 이 두 가지 장점을 결합한 것이 바로 pg_duckdb입니다. 하나의 엔진으로 OLTP와 OLAP의 강점을 모두 누릴 수 있습니다. ACID 안전성이 유지되고, DuckDB 수준의 분석 속도를 제공하며, 일반 SQL로 Parquet, Iceberg 또는 Blob 스토리지를 읽을 수 있고, 무엇보다 새로운 시스템을 배우거나 운영할 필요가 없습니다. 애플리케이션이 연결하는 PostgreSQL은 여전히 ​​동일합니다. 그렇다면 실제로 어떻게 작동할까요? 먼저 PostgreSQL 쿼리 파이프라인을 살펴보겠습니다. 100번을 봤더라도 간단히 다시 설명드리겠습니다. PostgreSQL은 쿼리를 입력받으면 파서가 SQL을 토큰화하고, 구문 트리를 구축하고, 구문을 검증하고, 테이블과 열 이름을 해석합니다. 그런 다음 플래너는 비용 기반 최적화 도구입니다. 통계를 분석하여 조인 순서를 결정하고, 인덱스를 사용할지 순차 조인을 사용할지 결정한 후 실행 계획을 생성합니다. 실행기는 이 계획을 받아 Volcano 모델을 사용하여 실행하고, 연산자 트리를 통해 한 번에 한 행씩 처리합니다 . 이렇게 행 단위로 처리하는 방식은 OLTP(온라인 텍스트 기반 프로그래밍)에는 매우 적합합니다. 즉, 일치하는 고객을 찾아 즉시 결과를 반환하는 것입니다. 하지만 분석 쿼리의 경우, 수백만 개의 행이 반복적인 트리를 통해 한 번에 하나씩 처리되는 것이 병목 현상의 원인이 됩니다. 이제 pg_duckdb가 이 파이프라인에 어떤 영향을 미치는지 살펴보겠습니다. 그러니까, 여기서도 같은 파서와 같은 파스 트리를 사용하지만, 플래너가 연결되어 있는 겁니다. 파서에서 파스 트리가 나오면 pg_duckdb가 이를 살펴보고 " 이 쿼리는 내가 처리할 수 있는 분석 쿼리인가?" 라고 판단합니다 . 만약 그렇다면, Postgres 플랜 대신 DuckDB 플랜을 생성하여 DuckDB 실행기에 전달합니다. DuckDB 실행기는 벡터화된 실행 엔진에서 쿼리를 실행한 후 Postgres를 통해 클라이언트로 결과를 반환합니다. 따라서 클라이언트는 아무것도 변경된 것을 알지 못합니다. 동일한 연결, 동일한 와이어 프로토콜, 동일한 SQL을 사용합니다. 만약 쿼리가 적합하지 않다면, 예를 들어 INSERT 쿼리 이거나 DuckDB에서 지원하지 않는 기능을 사용하는 경우, pg_duckdb는 조용히 Postgres 플래너로 쿼리를 다시 넘깁니다 . 이것이 자동 폴백입니다. 시스템이 중단되는 일은 절대 없고, 경우에 따라 더 빠른 응답을 얻을 수 있습니다. 자, 이제 이 연결이 실제로 작동하도록 트리거하는 조건을 살펴보겠습니다. 트리거는 세 가지가 있습니다. 첫 번째는 `duckdb.force_execution`을 켜는 것입니다. 이렇게 설정하면 모든 적합한 SELECT 쿼리가 DuckDB로 라우팅됩니다. 즉, 먼저 DuckDB에서 실행 가능한지 확인합니다. 실행 가능하면 DuckDB에서 실행하고, 그렇지 않으면 PostgreSQL로 전환합니다. 두 번째는 `read_parquet`, `read_csv`, `read_json`과 같은 함수 호출을 포함하여 데이터 레이크 파일을 읽는 쿼리입니다. DuckDB는 이러한 작업을 자동으로 처리합니다. 세 번째는 어떤 이유로든 DuckDB가 쿼리를 처리할 수 없는 경우, 앞서 설명했듯이 오류 없이 PostgreSQL로 전환하여 애플리케이션이 중단되지 않도록 투명하게 처리한다는 것입니다. 그렇다면 이러한 기능은 실제로 무엇을 할 수 있게 해줄까요? Azure Database for PostgreSQL에서 사용할 수 있는 세 가지 강력한 기능이 있습니다. 첫 번째는 기존 PostgreSQL 테이블의 속도를 향상시키는 것입니다. `duckdb.force_execution`을 켜고 기존에 사용하던 GROUP BY 쿼리를 실행하면 DuckDB가 이를 처리합니다. 같은 테이블, 같은 SQL을 사용하지만 응답 속도가 더 빠릅니다. 두 번째 장점은 Azure Blob을 직접 읽고 쓸 수 있다는 것입니다. 복사도, ETL도, 관리해야 할 추가 서비스도 필요 없습니다. 세 번째는 제가 가장 좋아하는 기능인데, 바로 하이브리드 분석입니다. 예를 들어 고객 테이블과 같은 실시간 PostgreSQL 테이블 과 Blob에 저장된 Parquet 파일( 3년치 주문 내역)을 같은 SQL 쿼리로 결합할 수 있습니다. 즉, 실시간 운영 데이터와 과거 아카이브 데이터를 기존에 사용 중인 엔진에서 결합할 수 있습니다. 정말 강력한 기능이죠. 자, 슬라이드는 이쯤 하고 데모를 보겠습니다. 앞으로 몇 분 동안 보실 내용은 다음과 같습니다. 총 5단계로 구성되어 있습니다. 먼저 새로운 Azure Database for PostgreSQL Flexible Server를 프로비저닝하고, 그 다음 새로 생성된 서버에 pg_duckdb 확장 프로그램을 설치합니다. 그리고 나서 Postgres 엔진으로 TPC-H 쿼리를 실행하고, 같은 쿼리를 DuckDB 엔진으로 실행해서 비교해 보겠습니다. 네 번째로, Azure Blob Storage에서 Parquet 파일을 직접 읽고 써 보겠습니다. 다섯 번째로, 하이브리드 분석을 해보겠습니다. Postgres 테이블과 Blob을 단일 쿼리로 조인해 보겠습니다. 따라서 여기서 주목해야 할 세 가지는 두 엔진 간의 쿼리 시간 차이입니다. 자, DuckDB 실행 엔진과 PostgreSQL 엔진에서 쿼리를 실행할 때 차이점을 살펴보세요. 실행 계획, duckdb.force_execution이 정확히 언제 작동하는지 , 그리고 실제 SQL 자체는 변경되지 않고 엔진만 변경된다는 점을 알 수 있습니다. 자, 이제 데모를 시작하겠습니다. pg_duckdb 확장 프로그램을 사용하여 Azure Database for PostgreSQL Flexible Server 에서 분석 속도를 높이는 방법을 보여드리겠습니다 . 먼저 새 Flexible Server 인스턴스를 생성합니다. 서버 이름은 pgduckdbdemo로 지정하겠습니다. 이제 서버 버전 과 관리자 자격 증명과 같은 필요한 구성 정보를 입력합니다 . 구성이 완료되면 서버를 생성할 수 있습니다. 플랫폼에서 서비스를 할당하고 초기화하는 데 시간이 다소 걸릴 수 있습니다. 이제 서버 프로비저닝이 완료되었고 Flexible Server 인스턴스가 실행 중입니다. 다음으로 Flexible Server에서 pg_duckdb 확장 프로그램을 사용하려면 서버 수준에서 활성화해야 합니다. 먼저, azure.extensions 매개변수에 pg_duckdb를 추가하여 서버 구성을 업데이트하겠습니다 . 그런 다음 shared_preload_libraries 매개변수에도 pg_duckdb를 포함해야 합니다. 이제 설정을 저장하고 서버를 다시 시작하여 새 설정을 적용합니다. 서버가 정상적으로 종료되고 업데이트된 구성 매개변수로 다시 시작되는 동안 잠시 시간이 걸립니다. 이제 재시작이 완료되어 서버가 온라인 상태가 되었습니다. 이제 pg_duckdb를 사용할 준비가 되었습니다. 이제 psql을 사용하여 서버에 연결해 보겠습니다. 연결되면 CREATE EXTENSION 쿼리를 사용하여 데이터베이스에서 확장 프로그램을 활성화합니다. 확장 프로그램이 성공적으로 생성되었습니다. 설치된 pg_duckdb 확장 프로그램 버전은 1.1.0이며, 내부적으로 DuckDB 버전 1.4.3을 분석 엔진으로 사용합니다. 이 데모에서는 pg_duckdb 확장 프로그램의 두 가지 사용 사례를 보여드리겠습니다. 첫 번째는 Flexible Server에 저장된 데이터에 대해 DuckDB의 벡터화된 실행 엔진을 사용하여 분석 쿼리 속도를 향상시키는 것입니다. 첫 번째 시나리오에서는 이미 스케일 팩터 5로 TPC-H 벤치마크 데이터를 로드했습니다. 여기에는 품목 테이블에 약 5GB, 주문 테이블에 약 1GB, 고객 테이블에 약 145MB의 데이터가 포함됩니다. 이제 몇 가지 TPC-H 쿼리를 실행하고 성능을 비교해 보겠습니다. 기본적으로 duckdb.force_execution은 꺼져 있으므로 쿼리는 PostgreSQL 실행 엔진을 사용하여 실행됩니다. 먼저 표준 PostgreSQL 실행 엔진을 사용하여 쿼리를 실행해 보겠습니다. TPC-H 쿼리 1부터 시작합니다. 이 쿼리는 품목 테이블에 대한 집계를 수행 하고 상당한 양의 데이터를 스캔하므로 분석 실행 성능을 관찰하기에 적합합니다. 쿼리가 실행되는 동안 PostgreSQL은 기존 실행 모델을 사용하여 행을 스캔하고 필요한 집계를 계산합니다. PostgreSQL 실행을 사용하면 쿼리가 약 26.6초 만에 완료됩니다. 이제 duckdb.force_execution을 켜짐으로 설정하여 DuckDB 실행 엔진을 사용하도록 강제해 보겠습니다 . 동일한 쿼리를 다시 실행하면 11.1초 만에 완료되는 것을 볼 수 있는데, 이는 2.4배 빠른 속도입니다. 상당한 성능 향상 이며, 중요한 것은 SQL 쿼리를 변경하지 않고 달성했다는 점입니다. 이제 PostgreSQL 실행으로 전환하여 TPC-H 쿼리 13을 실행해 보겠습니다. 이 쿼리는 7.7초 만에 완료됩니다. 이제 DuckDB 실행을 활성화하여 동일한 쿼리를 실행해 보겠습니다. 이번에는 쿼리가 약 4.1초 만에 완료되는데, 이는 거의 2배 빠른 속도입니다. 이 분석 작업 부하에 있어서도 매우 훌륭한 성능 향상입니다. 이제 다른 예제를 살펴보겠습니다. 먼저 PostgreSQL 실행으로 전환합니다. 다음으로, 고전적인 분석 패턴인 그룹별 여러 카운트 집계를 보여주는 쿼리를 보여드리겠습니다 . 이 쿼리에서는 1993년에서 1996년 사이의 주문을 필터링합니다. 그런 다음 주문 우선순위별로 결과를 그룹화합니다. 각 우선순위별로 고유 고객 수, 고유 담당자 수, 고유 주문 수를 계산합니다. 이러한 유형의 쿼리는 기존의 행 기반 실행 방식에서는 상대적으로 비용이 많이 듭니다. PostgreSQL에서 이 쿼리를 실행하면 해당 데이터셋에서 15.7초가 걸립니다. 이제 pg_duckdb를 사용하여 동일한 쿼리를 실행해 보겠습니다. DuckDB는 데이터를 벡터화된 배치로 처리하므로 벡터화된 집계를 매우 효율적으로 적용할 수 있습니다. 이번에는 쿼리가 2.3초 만에 완료되어 거의 7배 더 빨라졌습니다. SQL을 변경하거나 데이터를 PostgreSQL에서 옮기지 않고도 상당한 성능 향상을 얻을 수 있습니다 . 이제 데모의 두 번째 부분으로 넘어가 pg_duckdb 확장 프로그램을 사용하여 Flexible Server에서 Azure Blob Storage에 저장된 Parquet 파일을 직접 읽고 쓰는 방법을 보여드리겠습니다 . DuckDB가 Blob Storage의 파일에 액세스할 수 있도록 먼저 Azure Storage 암호를 생성해야 합니다. 이 단계에서는 create_azure_secret 함수를 사용하여 Azure Storage 자격 증명을 등록합니다 . 암호가 생성되면 pg_duckdb는 Blob 컨테이너에 저장된 파일에 안전하게 액세스할 수 있습니다. 이 데모에서는 Blob Storage에 Parquet 파일 로 저장된 과거 주식 거래 데이터셋을 사용합니다 . 먼저 `read_parquet` 함수를 사용하여 데이터셋을 빠르게 읽어보겠습니다. 이 함수는 Parquet 파일에 접근 가능한지 확인 하고 데이터 미리보기를 제공합니다. 이제 조금 더 흥미로운 쿼리를 실행해 보겠습니다. 여기서는 동일한 Parquet 파일을 Blob 스토리지에서 직접 쿼리하여 가격 변동이 가장 큰 상위 10개 거래 항목을 검색합니다. 중요한 점은 데이터가 Blob 스토리지에 그대로 남아 있다는 것입니다. Flexible Server는 이 데이터셋을 수집하거나 로드하지 않으며, DuckDB가 Parquet 파일을 직접 스캔하여 분석 결과를 반환합니다. 데모의 다음 부분에서는 PostgreSQL에 저장된 거래 데이터와 Azure Blob 스토리지에 Parquet 파일로 저장된 분석 데이터를 결합하는 하이브리드 분석을 시연합니다. 먼저 포트폴리오 관심 목록을 나타내는 간단한 PostgreSQL 테이블을 생성해 보겠습니다. 모니터링 중인 주식에 대한 레코드를 삽입합니다. 이 테이블은 일반적으로 PostgreSQL에 저장되는 운영 또는 거래 데이터를 나타냅니다 . 이제 흥미로운 부분입니다. PostgreSQL에 저장된 관심 종목 테이블과 Blob Storage에 있는 Parquet 데이터 세트의 최신 종가를 가져와 단일 쿼리로 결합합니다. 결과는 다음과 같습니다. 즉, 단일 SQL 문을 통해 운영 데이터베이스 데이터와 Blob Storage에 저장된 외부 분석 데이터를 효과적으로 결합하는 것입니다 . ETL 파이프라인도, 중간 데이터 수집 단계도, 데이터 중복도 없습니다. 이는 하이브리드 분석 워크로드에 매우 강력한 기능입니다. 마지막으로 결과를 Parquet 파일로 Azure Blob Storage에 다시 저장해 보겠습니다. 파일이 생성된 것을 확인할 수 있습니다. read_parquet 함수를 사용하여 파일 내용을 읽어보겠습니다 . 여기에 저장된 데이터가 있습니다. 정말 놀랍습니다. PostgreSQL은 외부 Parquet 데이터를 읽고 분석하고 외부 테이블과 결합한 다음 결과를 Azure Blob Storage에 다시 저장할 수 있습니다. Azure Blob Storage에 저장된 대규모 데이터 세트를 원활하게 분석하고 PostgreSQL의 트랜잭션 데이터와 결합하여 표준 SQL을 사용하여 분석 결과를 생성할 수 있습니다. Azure Database for PostgreSQL Flexible Server에서 pg_duckdb를 사용하면 강력한 분석 기능을 활용할 수 있습니다. 이 기능은 Flexible Server 버전 14부터 18까지 사용할 수 있습니다. 이 기능을 직접 사용해 보고 Flexible Server에서 분석 워크로드를 간소화하고 분석 쿼리 속도를 향상시키는 방법을 확인해 보시기 바랍니다 . 감사합니다. 데모를 즐겁게 시청하셨기를 바랍니다. 이제 주요 내용을 정리해 보겠습니다. 네 가지 주요 사항을 살펴보겠습니다. 첫째, 하나의 데이터베이스에 두 개의 엔진이 있습니다. PostgreSQL은 OLTP용이고 DuckDB는 OLAP용으로 동일한 관리 서버에서 사용됩니다. 둘째, 데이터 이동이 필요 없습니다. SQL에서 Azure Blob에 저장된 Parquet 데이터를 직접 쿼리할 수 있습니다. 데이터 레이크에서 데이터베이스로 이동할 필요도 없고 , 데이터베이스에서 데이터 레이크로 이동할 필요도 없습니다. 셋째, 하나의 SQL에서 하이브리드 분석을 수행할 수 있습니다. 즉, 실제 PostgreSQL 테이블과 Blob 스토리지에 저장된 데이터를 단일 쿼리로 결합할 수 있습니다. 동일한 SQL로 더 빠른 결과를 얻을 수 있습니다. DuckDB의 force_execution 설정을 true로 변경하기만 하면 됩니다. 기존 쿼리는 그대로 유지됩니다. 제가 드릴 말씀은 이게 전부입니다. 감사합니다.
