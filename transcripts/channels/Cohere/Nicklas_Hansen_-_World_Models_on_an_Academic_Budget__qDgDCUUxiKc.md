# Nicklas Hansen - World Models on an Academic Budget

- 영상 링크: https://www.youtube.com/watch?v=qDgDCUUxiKc
- 채널: Cohere
- 업로드일: 2025-08-29
- 자막 언어: en
- 단어 수: 약 9162개

---

## 스크립트

[Music] Um hello everyone. Thanks for joining us today. Today we have Nick. Uh he's a PhD candidate at the University of California, San Diego. Advised by Professor Wang and Professor Sue. Their research focuses on developing generalist AI agents that learn from physical interaction. Nick has spent time at NL research, meta AI as well as Berkeley research and received their BS and MS from technical university of D. They were a recipient of the 2024 Nvidia CR graduate fellowship and their work has been featured at top in machine learning and robotics. this all goes the people get &gt;&gt; okay yeah Rahul thank you so much for the introduction and for the opportunity to to come and talk about world models um so you might have heard world models a lot in um academic society in the last couple of years um there's more and more attention um and focus on world models and and building increasingly larger world models um but a lot of this progress has happened industry. And so I want to focus on um world models more specifically in academia and what people on an academic computational budget um can can participate. So first before talking about world models, excuse me, let's talk about AI models more generally. Um, so a lot of the more creative digital AI agents that we've seen already, um, like LLMs and VLMs, they're fairly general in the sense that you can snap a photo and give a question for like, um, room decoration tips, and it's able to generate a response that kind of matches the um, um, the question and and the photo that you took. You can also prompt uh these models with with text and they're able to generate pretty high resolution um images or videos that that match your description. One of the key um takeaways here is that it's usually a single model or a single algorithm that can be applied to a variety of different problems without the user of the algorithm or model necessarily knowing much about AI or the specific domain that they're applying the model to. um this is a pretty new paradigm and it's quite different from what we've seen um say five years ago. So generally speaking there's three key ingredients to building such a model. Um usually we need pretty big diverse data sets. Um usually it's data scraped from the internet like uh pictures and videos and text from the worldwide web. We need uh scalable architectures which in LM world is usually transformers. Um the key thing about the architectures is that it needs to be scalable in the sense that it's able to consume these um everinccreasing uh data set sizes. And then I think the most critical part is that we need um training objectives that are general enough um let's say next token prediction and and language modeling that we can take these models that we train on lots and lots of data and be able to apply the same model to new tasks by simply changing the prompting. So we don't quite have this embodied AI yet. Um, but I want to talk a little bit about what are some of the components that that that might go into building such a model. And I want to focus specifically on world models because they are the models that allow us to reason about the physical world much like LMS can do planning in in language space. And I would argue that predicting the future given the past is probably the most general objective that we can come up with in the realm of embodied AI. There's been some work in industry on this already like 1x uh technologies has prototyped a world model that is trained on lots of robot data from their specific robot. And here's um a short video that is generated by this model. Similarly, uh wave have come up with models like this for autonomous driving. Um this is a video of their world model which is conditioned on a short sequence of frames and then a sequence of driving um actions and it's able to simulate what would happen um plausibly if you took those actions in in a real car. And Google D mine has done this as well for video games where it's um a single world model that is trained on lots of different video games and it's able to um generate short clips again of um pretty realistic looking game dynamics given again key commands. And one of the biggest things that make world models different from um the typical video generation models we have is that world models are able to generate counterfactuals because they're conditioned on actions. So given the same frame here you can see the the the Google deep mind world model um being able to generate different sequences conditioned on different actions but all starting from the same frame. Um so this is what makes world models um small neural simulatable worlds that we can train agents in. Most of the models that exist already are built in industry and are typically closed source. So we don't have access to the specific um data that they used or the architectures or even the training details. Um so what I focus on in this presentation and in my research more generally is trying to crowdsource a lot of these um same tools and and data sets that people have been using in injols already. We have good simulators like Mojoko Isac and and many scale that run fast. They're GPU paralyzable and they generate a lot of data in very little time. We also have fairly good open algorithms. Um lots of our algorithms exist and are um open openly available like soft critic, dreamer, tdmc2 and many others. Um there's still more work to do here. I think the algorithms that we have are not quite robust enough to be applied out of the box and that they tend to still focus mostly on these single task um environments. And arguably one of the things that we're lacking the most as a community is sharing the artifacts of the experiments. Um to give you an example for TDMC2 our algorithm that we uh released a couple of years ago we released more than 300 checkpoints basically a checkpoint for every single experiment that we ran for that paper. And this is very unusual, but it has given a lot of people the ability to not have to recomputee everything that we did. They can just take the checkpoint and use it for their purposes. And this makes um research much more accessible. So we want to see more of that as well. And so these two parts will be um the focus of today. So let's talk about how to build a world model. Um my definition of a world model is a model of the transition function of the environment. And meaning that it's a mapping from the current state of the environment and then also condition on an action and it's able to map that to a future state of the environment. Um because we're doing RL, we also usually include a reward function in the transition function. So we're predicting both the future states and also the future rewards. And given that definition, we can use world models to do planning. Meaning that we can train a world model from environment interaction. This gives us an approximate model of the real world. And then we can sample a lot of different action trajectories and evaluate the rewards using our world model and simply just select the action that maximizes this um reward. the specific components of a world model. Usually we have an observation space that might be low dimensional states or images or even a combination of them. Um we encode the observations that we are given um using some encoder. This is um part of the the world model that produces some kind of latent representation that we can use to model the dynamics in the latent space. So typically we do all of our modeling the latent space rather than in the observation space. Um the dynamics function here is is a model of the transition function essentially but in the latent space. So it's conditioned on the current latent state and an action it's predicting the latent state of of the future. And then in order to train this model typically in an end to end manner we need some kind of supervision for this world model. And then also typically we have some downstream tasks that after training the world model we want to use the world model for something and this could be for example for supervision it could be reward prediction. It could be self predictions or predicting the future latence and comparing those to the expected latence. You could also be adding an a decoder to the um dynamics function and then using for example pixel pixel differences or pixel reconstruction as your objective. So that's usually how you would train a generous model. So one of the things that differ between different algorithms is usually how you do your supervision and the different downstream applications. Um one popular way of supervising your model dynamics is a generative model. So this is for example dreamer. um many of you might have heard of. It's a world model that is trained to predict um future frames given the um the latent dynamics. So it's a generative model that generates pixels for example if if this database is um images. The models that I'm going to be talking about today are self-predictive models. Um an example of this is TDMPC which is um my previous work and it works similarly in most of the components of the world model but the key difference here is that the supervision for the dynamics is is different. Um the way that TDMC works is that we don't have a decoder. In fact, we're just comparing the um the latent embeddings that we get for our states um when we roll out the model versus if you take those ground truth future observations and encode those with your encoder. And we're going we're going to see um some examples of this. Um but that's just the key difference. One of the the main works of my PhD was uh TDMC2. Um in this work we took our TDMC algorithm that we built initially um the world model algorithm and improved some of the different um components in the model to make it um faster to train uh more accessible to people by making sure that you don't actually need a lot of domain knowledge to use it. And we've also made it scalable which I think is almost a first when it comes to reinforcement learning algorithms. And so here specifically when talking about world models and RL by scalable I mean that we as we get more and more data and we increase the number of parameters in the model we would expect better performance. This has not been historically the case in RL um but it has in in areas like LMS for example. And the robust part here is what I talked about initially that we want at least an algorithm that can be applied to a lot of different problems and and domains without any specific user um domain knowledge. So it should work pretty much out of the box. I'm going to talk first about how to do planning with a TDMC algorithm um the TDMPC world model. Then we're going to look at how to build the the world model itself and how to learn it. And then we're going to look at some examples of how to use it for for actual problems. So the planning is pretty simple. It's more or less what I mentioned previously. So given an observation that we receive from an environment, we can encode that into a latent representation that we call Z here and then condition on Z and a sequence of actions that we can sample from some prior distribution that allows us to simulate um futures like future trajectories in the laten space of this model. And because our world model also has a reward hat, we can predict reward for each of these steps that we wrote out the models. Now, we can't feasibly roll out the model too far. Um, so we also learn a value function that essentially just truncates the um reward prediction here um from the planning horizon and and beyond. So the return that we're maximizing here is return is the the sum of rewards is the rewards for the planning horizon that we do plan and then we use the the value function here to bootstrap from the planning horizon and and beyond. The way that we train the model is very similar. We have a data set of transitions or trajectories. Um this might be collected online or it might be an offline data set that is provided to us. We sample a sequence and then in that sequence we sample a frame S1 here. we encode that into its latent representation C1 and then conditioned on this latent um state. We predict the optimal action the reward they would we would get and also the value of this state and condition on the actions that we took in the data set. We can recurrently predict future latent states and all of their quantities as well. And so we can do this for a fixed number of steps. Now you'll notice that this world model is not yet grounded in the dynamics. So there's no um component here mapping the latent space back to the observation space. The way that we supervise that is a little bit more implicit than a generative models. Um but as I mentioned previously, we minimize the difference in the latent embeddings that we have here. So for this specific example, if you were to take the embedding of S1, so C1, and we currently roll out that latent state with our dynamics function, a few steps into the future, we get C3 here on on the right. We would expect this embedding to be very similar to the embedding that you would get if you took S3 and encoded that with your same encoder into it lat representation. So, this is the Sorry, I think I dropped for a second. You all hear me? &gt;&gt; Yes. &gt;&gt; Yep. Okay, great. Um, yeah, hopefully you didn't miss too much, but we would expect the embedding C3 that we get. Let me just share again. Right. Sorry about that. Um, we would expect the embedding C3 that we get from rolling out our dynamics in the latent space to be very similar to the embedding that we would get if you took S3 and encoded that into its latent representation using the exact same encoder. Um, so this is the main source of supervision in in the self-predictive and MTDMPC world models and there's some computational advantages to this. It's much cheaper than having a decoder, especially if you work with very high dimensional um data like images or video. Um, some so purely from a computationally um computational uh perspective, this is this is a lot cheaper and more accessible for for academia. Um, you can write out the equations like this. I might skip that unless there's um specific interest. We can come back to this but essentially it's a world model that is trained with these three terms reward value and self-prediction objectives and we just recurrently roll out this objective for um uh whichever many steps we we simulate for. So now that we've seen how this algorithm works, let's see what we can use it for in practice. Um, you can use TDMPC because it's an RL algorithm. You can use it for online RL. And in online RL uh settings here, usually we start from scratch. So we have an unknown task that the dog here um we want the dog to run, but we have no model pre-training. So we're starting from from scratch in terms of model parameters and we also don't have any data. So we need to collect our own data and you'll see here that TDMPC it can iteratively um collect data using our world model and and then planning with the world model and then on the data that we get we train the model gradually um on this gradually increasing data set and eventually when we're at the 10 million steps we have a working um dog running policy. You'll probably notice that this dog is not uh likely to transfer to a real dog. It would probably break its angles very quickly. Um this is kind of an example of RL overoptimizing the simulation a little bit. Um there's some joint angles here that I think are not entirely realistic in the simulation. Um but overall it's an optimization framework that is very very powerful for for this kind of locomotion or manipulation or other other problems. And crucially that doesn't this doesn't require any domain knowledge about um dogs or even locomotion at all. And I think what's coolest about this is that even with a pretty slow simulator and no paralyzable environments, um you can actually train this working dog policy and one day on a fairly old GPU. Um so this is definitely something that that most people in academia can participate in. Um during this project that we did a little while ago, we took this RL algorithm in an online setting and applied it to more than 100 different tasks spanning different uh simulators and domains that that people in the RL community usually care about. Um so some examples are DM control and meta world and uh with many skill as well my suite and some other domains. And if you recall our initial objective we wanted a single algorithm that can solve all of these problems without domain knowledge and without any um tuning of hyperparameters. So we're going to look at some single task RL results where we just apply our algorithm repeatedly for each and every task in our benchmark without any hyper render tuning. So it's a single algorithm single set of hyperparameters applied um out of the box on all of these domains. And so here on the left here we have um six domains that we split up the tasks in terms of um which simulator or benchmark they come from. And we have some y-axis here which is what we call a score. Um so it's the success rate whenever we have a defined success function. And otherwise we just use um the reward and normalize it. And we compare to some classic R algorithms like soft critic. This baseline here is actually tuned to most of the different tasks. Um dreamer v3 does not require hyper tuning. Um it's been shown to work very well on for example Terry games which are discrete actions but when we apply it to a lot of these highdimensional continuous control problems it didn't really perform that much better than soft as a critic. We also benchmark against TDMPC1 which is the first iteration of our algorithm. Um this TDMPC algorithm did require hyperameter tuning. So for each of these domains we did um tune the algorithm to those problems. And then TDMC2 here is the latest iteration of the algorithm. It does not require hyperimeter tuning and you'll see that it performs pretty consistently um across many of the different tasks especially compared to TDMC1. You see that it's always um similar or better but without any tuning. Um so this is much more approachable and accessible for for people. Um we also performed some offline RL experiments. So this is an uh example of um a pseudo scaling log for a small set of tasks. So we have 80 tasks here from two different problem domains. Um so we take these two um problem domains. We collect a bunch of data um so like offline or data. So exploratory, random, um, beginner policies, expert policies, uh, collect a bunch of data and then we train progressively larger models on this data set. And what you'll see here um is that as we increase the model parameters, so the x-axis here is in log scale, you'll see that you get progressively better um, normalized score as well. And this is to my knowledge um the first time that someone showed this in in a purely offline setting. And I also want to highlight here that the second largest model that we trained with 48 million parameters is still pretty small compared to LLMs. Um but it's pretty big in RL space. And this model was actually trained in just 5 days on on a single GPU. Um, so this is definitely also something that most people in academia can can feasibly train themselves. Um, you might be asking so why is TDMC2 better than TDMC1 and and why does it scale? There's a few key design choices that I want to highlight. And we did some ablations on uh things like the planning, regularization, and variance of the model. And these are all ablations on the same multitask data set. And one thing that we notice is that if you do planning the brown and the red uh bars here, you actually scale significantly better than if you were to take your world model and then learn a model free policy using your world model. So there seems to be something inherently in the planning algorithm that exploits the world model more um and squeezes out more performance than if you were to train a simpler model free policy. Um but crucially this only happens when you have large amounts of data and multest setting and and high dimensional problems. We also add a lot more regularization to the world model. I would assume that as you increase the n the amount of regularization in the model, it becomes more robust to these um works in the data and especially when you have offline data, you might sometimes be out of distribution when you do planning downstream. And it seems like regularization is something that that that helps in that sense. And this bar plot here just showed that as you add more and more regularization to your model, you get better performance. There's um one other trick that we used in the in the paper is that we instead of doing continuous regression on the rewards and values um we instead use a discrete regression um problem or formulation of the of the prediction problem. And the reason that we do this is that if you're working with RL data for many different tasks, the different tasks have widely different reward scales and the expected values of all of those different states in the data set would also vary a lot. And so if you want to make sure that all the tasks are equally represented in terms of reward prediction, um you need to normalize your rewards in some way. And the way that we do that is simply um just model everything with discrete regression. So predicting which reward bin you would be in instead. And there's some mathematical tricks you can do to go back and forth between continuous and discreet. So you can still um recover the original values. Um so those are some of the tricks. There's there's more tricks in the paper if you're interested. Um I also want to say that for this paper we did a lot of open sourcing. So we released the code for a training evaluation. I think that's pretty standard in the community. Um we also released our data set. It's a really large data set half a billion transitions um that we used for the multitask experiments. This is all open source and we also released every single checkpoint in that previous figure that I showed. So this is more than 300 different checkpoints that are openly available and anyone can use them. For example, if they want to benchmark against TDMC2 or if they just want to um if they want a policy that can generate demonstration data for the specific problem that they're working with, they can download any of these checkpoints. Now I want to shift gears a little bit and talk about more practical problems in the remainder of my my talk. So specifically assuming that we have this algorithm TMPC or a similar algorithm that has scaling and learns pretty efficiently. Um how can we use that to get more data and how can we use it to solve uh practical problems in robotics? So firstly, if you think about data collection, we have a few different options. One of them is just RL interaction. Um like I showed with the dog, we usually as we get more and more interaction data, we're able to improve our policy. Um the problem with that is that it's um very cheap in simulation to do because our simulations generate a lot of data and it's pretty safe. But the diversity that we get from the data is mostly limited by the data that the simulation um provides. So this gives us proficient policies for very narrow scales. But it doesn't give you the kind of diversity that we would expect from a world model. We can also collect demonstrations for example on real robots. Um this is pretty costly and timeconuming. you need to buy all the hardware and you also need to have humans um collect all of these demonstrations um repeatedly. This does give us more diversity. It's easier to instead of building simulation environments, you can just buy a bunch of objects and and man manipulate those objects on a tabletop for example. And it also provides uh stronger supervision because we have a human expert that is trained to collect um good demonstrations. we usually get more signal out of demonstrations than we do with um RL interactions where it's more like trial and error. So if we were to design a wish list wish list for um the training data for world models, I would say that we need a scalable data collection method. Um so we can't just scrape the internet because there's no internet of robotics or interaction data. Um but what we can do is that we can um have other methods of scalable data collection or we can combine for example interaction and demonstrations um to get more diversity. And so one simple idea that we tried um is to for a specific task collect a few demonstrations and then do online interaction. And the reason that this is a pretty symbiotic combination is that imagine if you have a task here um like the one on the on the left where you have a hammer and a nail and you need to pick up the hammer using this um dextrous hand and then hammer the nail into the board. Um, this is not particularly complicated or long horizon, but if you are to do it with image inputs and sparse rewards, it's very likely that you would never be able to get a working policy if you simply start with random interactions. And so, one example here of random exploration is the the hand here doing some random motions, but it's very unlikely that this would ever pick up the hammer and hammer the nail. um human demonstrations, even with just one demonstration, you can already provide some learning signal to the algorithm. Um and humans are pretty good at hammering nails. So um you get a lot more supervision. And one simple thing that we did is just to combine um human demonstrations and and RL interaction. So we build a simple pipeline here based on TDMPC um and TDMPC2 where we collect some number of demonstrations let's say five demonstrations we pre-train our world model on those five demonstrations and then we let our world model interact with the world after and practice the task more and so as we collect more and more interaction data we add that to a separate data set that we call a buffer and then anytime we make gradient updates on our model we just sample 50/50 from the demonstrations that we um provide to the algorithm and the interaction data that it collected itself. And it turns out that this very simple idea is actually pretty powerful. Um so here's examples on real robots where we set up a robot in in the lab. We have three RGB cameras. Um you can see one of them in the frame at least that provides high resolution RGB observations. And then we have a sparse reward signal that is simply detecting whether the task is successful or not. And we have a human collect 10 demonstrations for each task. And then after that we do just one hour of autonomous interaction using our our TTMC algorithm. So it's progressively um training the world model and then doing planning um to collect more data. And something that I want to highlight is that these policies are not perfect. But if you do just one hour of fine-tuning, it's a lot better than what you would get just from the demonstrations alone. Here is examples of two tasks that the bin pegging and the reorientation of the water bottle. And you'll see that if you do behavior cloning on those 10 demonstrations, you get about 20% success. Um, we have pretty good signal in terms of solving the task. So the policy is able to solve the tasks if you initialize it very close to where it has been u seen in the demonstrations but if you venture outside of those um very narrow state spaces and you have a new configuration it doesn't really work out of the box. Um so that's mostly what we get by doing online fine-tuning is that we take a policy that is already uh decent and knows how to solve the task sometimes and then broaden the state and action space for which our policy is proficient. Now how can we improve upon this idea? One thing is that while we have some demonstrations to provide supervision, we're still doing RL with sparse rewards. Um so one simple idea here is that based on those demonstrations that we are providing can we use that to learn a dense reward function that would speed up the RL interaction uh phase as well. And for example here if you have um a fairly short horizon task here but a little bit more long horizon that what we saw previously um a task here where you need to pick up this peg and then insert it into the box. Um, it's very unlikely that this would work with sparse rewards. Um, if you only provide reward when it's inserted, but if you have an improved recipe here where you coarsely define some task stages, say picking up the the peg, moving it towards the the the box, and then finally inserting it. Um, you can learn a dense reward function from demos by simply predicting which state you're in, which stage you're in here, one of these three stages. And it turns out that this dense reward function is good enough that you can run the exact same algorithm that I showed before and get pretty sizable improvements. And so crucially, you are learning the dense rewards from those 10 demonstrations. But as your RL agent interacts with the environment and gets more and more proficient, you can also train the reward function on that new data. So the reward function itself gets better, but also the policy gets better. And here's an example of a successful trajectory um that we see here on the left. And then the plot next to it is the um predicted reward using our dense reward function as a function of the time step. And so you'll see here that it's giving more reward when the the peg is being picked up. And then when it's finally inserted, it gives the highest reward of three. Um so this is directly comes from the stage prediction. So reward of three here means that we are in stage uh three where it's inserted. You can do the same for a failure and you'll see that it's able to detect when the robot fails and eventually it gets a reward below zero here when it uh it drops the dep out of its reach. We do some uh benchmarking on some different simulatable environments. Um we have some tabletop manipulation. We also have um a humanoid manipulation benchmark. And the blue one here is is the method with the dense reward function. You compare that to the orange one which is um the one that has sparse reward function. So that's what I showed previously one. Um you see there's a pretty sizable improvement. Um although you probably would get the same dent performance if you were to run the same algorithms for long enough. But if you move back to real robots again this would be a lot more desirable. Um on the different benchmarks that we have we see a pretty consistent pattern that using this learn dense reward function is doing um a lot better with this budget that we have defined than the other alternatives. The problem with this kind of approach is that we're still doing single task problems and we need task specific demonstrations for each of those problems that we're working with. Um, so anytime we have a new object we want to manipulate, we would have to probably collect some demonstrations for that object. And one way to get around that would be to rely more on existing data sets. So in this last part here, I want to talk about how we can leverage human data as a prior for interaction in in highdimensional problems. The thing about humanoids specifically is that we often want our humanoids to mimic humans and human behaviors, meaning that we want them to be natural in their emotions. and natural motions are pretty easy to demonstrate because humans are pretty proficient at being humanlike. Um, but it's very difficult to actually write a reward function for example of being humanlike in in in code. So if you're working with locomotion problems like this where you have a locomotion policy that is controlling a highdimensional humanoid, you want it to be conditioned on images um so that we can see what's in front of it. um and you wanted to be able to to manipulate or like um um run across these different um randomized environments is a very complicated problem in itself and you also probably wanted to do that in a way that is natural especially if you want to deploy it on a real robot. And the question that we ask in in this work here is whether we can use existing vocap data to give the world model an idea of what is uh humanlike. And we want to do this before we ever um think about the task that we want to do downstream. The way we set this up is in a two-stage um pipeline. So first we have a big human mocap data set of um humans doing a lot of different motions. We import those motions into a simulator um Mojo specifically and then we train our human um our humanoid here in orange to follow motions um that we sampled from the data set. So we do some retargeting to this humanoid environment that we're working with and then we're simply just learning to track those movements. And we do that with a single world model. We just do RL and simulation learning to track all of these um motions and we train a single model on more than 800 different um human motions. Um so this is all just RL um online RL um from scratch. Every single episode we sample a motion and we give a reward that is proportional to how well we're tracking that motion. Now given given this tracking world model, we are not able to do downstream tasks downstream um um out of the box. We're able to simulate or like track all of these different motions, but we are not really capable of of solving new problems. Um but what we can do is that we can save this tracking world model and now have a pretty good world model of humanoid motions that are natural and are good at tracking. Um we save this world model and then we can reuse it for new downstream tasks. Um before showing that I want to show just some tracking results. This is the it's a little bit laggy for me but it's um tracking results for the um single TDMC2 tracking world model that we trained here. Um so this is all the same model that is just conditioned on different motions and it's not able to track perfectly but it's doing a a decent job. Now in the second stage here we take our pre-trained tracking world model. This is a model that is specific to the humanoid but it's general in terms of tracking any kind of motion and then we have a simple idea of just training a different world model that is providing those tracking um conditioning to the tracking world model. So this is a more high level world model that takes images as input. It has um a task specific reward signal. So it's trained on downstream tasks and then um the action space of this higher level world model is simply to um provide commands for this lower level model to track. So they both take proper perception as input. The puppeteer world model also takes images and a reward function. And then um it does online RL on these downstream tasks and essentially just learns to use the tracking world model to solve tasks. Um so this is the the the two-stage pipeline. The puppeteer world model we train individually for each task that we want to do downstream. So we're optimizing um those tasks. Um but the tracking world model is fixed and and constant across all of the different downstream tasks that we're doing. I hope that's somewhat clear. Um I'm not going to go too much into detail of the technicalities of training this. Um but I want to show some results um on the puppeteer model which is basically just TDMPC but with human data in this two-stage approach and we can compare that to a algorithm like TDMPC 2 that is trained just on the downstream tasks without any human data uh in the in the pre-training and what you'll notice here is that both of them actually run more or less the same. that have similar reward, but one is definitely preferred by humans over the other. Um, just in terms of how natural the motions are. And here's an example for a different task with hurdles. Again, the reward is pretty similar for these two policies. Actually, the one on the right is converging to a SL slightly higher reward. But in terms of naturalness, I I would definitely prefer the one on the left. And if I were to deploy this policy eventually on a real humanoid, I definitely would prefer the one on the left. So to wrap up, I want to just summarize some of the key points that we discussed here. Um, we've discussed a little bit about world models and why they're promising for this kind of generalist embodied AI. Um, we have seen evidence that they're able to simulate a lot of different futures given the same initial conditions. Um, they give us counterfactuals which is really powerful if you want to do planning. For example, you can sample different trajectories and then optimize in that way. Um, you can also use it to train policies much like you would train in in a classical simulator. Um, we've seen that combining multiple different sources of data can be really powerful. Um, and especially if you want to do kind of academic research on smaller scales with um, less robots and um, less time probably to collect demonstrations. Um, providing some demonstrations and then just doing interaction after um, can be really powerful for this um, data collection pipeline. And the good thing is that all the new data that you're collecting uh by doing online interaction, you can feed back to um your world model data set and gradually you will have a bigger and bigger um data set at your disposal. And at least just one idea for how to scale um these kind of world model approaches to a lot more embodiment um is that you could simply train tracking world models that are really proficient at simulating all of these different embodiment. And then you could train a big multitask tier world model for example that knows the highle dynamics and the specifics of the different tasks and then it simply learns to control the low-level um tracking world models for each of the different embodiment such that they solve the task that it wants them to solve. And so I want to end on a note um positive note and just say that embodied AI has never been easier. This is a really good time to enter the field. The hardware is way more um accessible and much cheaper than when I started my PhD. And we finally have policies, whether it's big VLMs or small um world models like TDMPC that are able to solve um fairly complicated problems with very little data. And so yeah, I want to end and just say thank you to my adviserss and all of the different collaborators that have worked on the projects that I discuss here. I'm also happy to take questions. Um, there's one question in the chat actually too. Let me see if I can open the chat. Okay, so one question from Motes. Why would there not be a collapse if we have loss on latent embeddings of two different timestamps? Um, referring to the TDMPC training, I believe. Um, yeah. So here, why is there not a collapse? Um there could be I think there's there's two reasons that it doesn't happen in practice. One is that we borrow a little bit from the idea of self-prediction and um vision domain where people have been training models like BL that are predicting um different versions of the same input. Um the way that we do it here is that we use a stop gradient on the future observation um and and that encoder. So S3 here is encoded with the same encoder but then we apply a stop gradient so that the gradient only is back propagated through the dynamics modeling and back to the original um state and I didn't really explain that in because I skipped over this slide kind of but if you can see my cursor here um the first term here is the joined embedding prediction. Um, &gt;&gt; hi. Hi. Uh, hey Nick. Uh, you're actually not audible. You've been dropping on and off. &gt;&gt; Oh, is it better now? &gt;&gt; Yeah, can uh answer the question again. Um, it was not clear not &gt;&gt; Oh, okay. Let me just see if I can Is this better? &gt;&gt; Yeah. Okay, let me try this. &gt;&gt; Can you go back to the frames? &gt;&gt; Yes. &gt;&gt; Okay. Yeah. So the question was why there's no collapse here when when modeling the um self-prediction objective and we take some inspiration from methods like BL that has worked previously for images so nonsequential um and the way that they do it in that work is that they have a very similar self-predicting objective but where the target that they're trying to predict they apply stop gradient after that encoding and that means that the gradient has to flow only one way back. Um we do that as well here. So we apply a stop gradient at S3 when we encode that but not when we encode S1. So the gradients here will naturally throw uh flow back from the right to the left and then down to the S1 observation eventually. Um so that's one way to do it. Another reason is also that we're also doing reward prediction. And so when we're modeling the reward signal, that provides a different objective that also prevents this constant embedding um collapse that you sometimes see in in other literature. But because we have this these two terms, we're both modeling rewards and also dynamics. Um we don't see the collapse. Hello. &gt;&gt; Hi. &gt;&gt; Oh, actually we couldn't hear you in front of Yeah. &gt;&gt; Oh, you still can't hear me? &gt;&gt; Yeah. &gt;&gt; Oh. Some people can hear me I think. Okay. I hope it's good enough that I can continue. Um if not then I'll I can write the answer in chat as well. Um the second question here is how difficult is it to model a specific reward function? Is there a specific mathematical foundation or is it mostly trial and error? Um I think there's some theory. Uh but usually the RL theory that we do have lacks behind the models that we're actually producing. Um so a lot of the theory is making assumptions um whether the reward function is linear or the model is linear um or some other um assumptions like that. So I think that tells us a little bit and it forms a little bit our decisions when designing algorithms and objectives but it doesn't translate one to one. Um so in general I would say that as an RL researcher I tend to not change the reward function. Um I usually work with benchmarks where they're already predefined. And for most locomotion problems those reward terms us there reward functions usually has somewhere between one and maybe four or five terms. So for the dog, you just one term which is forward velocity. For um a slightly more realistic dog or if you want to transfer to a real robot, you might add some additional terms that minimize the energy spent um penalizes when the joint limits are reached. Um things like that. And so that's something that that usually is important for real world robotics, but less important in simulation. Um, for manipulation, it's a lot harder to come up with reward functions. So, you can write dense reward functions, but they often end up being hundreds of lines of code. Um, so what we do in our real robot experiments, um, all of the manipulation that you saw, we just use forward functions. Um, it's fairly easy to design the success detector whether it's whether it's scripted or it's learned with supervised learning. Um and nowadays you can even use a VLM for example to um you can prompt it an image and a question whether the task for example is solved and then it can provide a a binary signal on on on that. Um so those are all options. I would say for locomotion it's a fairly easy problem for manipulation is a lot harder. All right. Any last questions before &gt;&gt; um a few more questions if you if you I'm seeing in the chat. Oh yes, I see. Um, okay. So, one question is specifically regarding the collapse here. Um, whether I'm using a momentum encoder. U, we did do that in TDMPC1. Um, but it adds a lot of extra parameters. So especially when we did the scaling part um it became kind of inconvenient to have these um additional parameters that are just a momentum of the of the online parameters. So what we decided to do instead is to just reduce the learning rate on the encoder compared to the rest of the model. Um, so the dynamics are moving uh faster than the encoder uh parameters and that's actually enough to get the same effect as the momentum encoder but without the extra parameters. Are you learning the embeddings to predict also the reward Q&amp;A? Um yeah so everything is trained end to end with except for the actions. um we learn the actions just as a function of the um laten embeddings. So that's a model free policy that is learned in the in the laten space and the function of this uh policy prior is just to speed up the planning process. Um because we want fast realtime online planning. Um so that speeds up that process a little bit. Everything else that pertains to the world model itself is all um learned end to end. And that's also part of why we don't have this collapse. Um because we're predicting both rewards and also dynamics. Um have I seen if the TDM2 architecture is stable without rework prediction? um it's stable but the representations are are worse um qualitatively um at least for the for the task of control. So because we're not doing um we're not generating future images um it's a little bit harder to evaluate the quality of the representation um besides control. But if you care about control, you probably do want reward prediction or some other similar metric in there just to make sure that the the model and um the policy is is supervised um on a way that is task specific. So purely from a like compression perspective, for example, if you have a fixed amount of information that you can store in your latence, you want to prioritize the things that are relevant for a downstream task and and control specifically. Um that's not something you would get if you remove the reward prediction. Um but if you leave it in there, you can actually learn very small models that are very um task centric um and throwing away some other information. Um, but you can fit it a lot more information, a lot more tasks in very little parameters. Um, so there's some trade-off there. &gt;&gt; Um, the current trend is to investigate self-predict models rather than generative ones. Is there another perspective such as a hybrid approach or any other alternatives that you know of? Um I would say actually there's in RL there's more work on self-predicted models right now just because they're cheaper but in industry I think all of the models that you're seeing are all generative um because they have access to more resources and I think the recipe for training generative world models is very mature. um you can more or less just take the same pipeline that you use for LLMs and BLMs and just apply it to world models and you'll get something that works um but it's going to be very expensive to train and so self-predictive models are of more of interest to academia just because they're more accessible and usually are the models are much smaller they require less compute to train um to get the same performance um I think you could get something in between probably and that would be trading some parameters and some compute for more interpretability, for example. Um, and also would help with with things like learning the dynamics in a way that doesn't collapse or or lose information. Um, so I haven't seen much of work like that, but I think that would be interesting. Yeah, I hope that's a satisfying answer. Have we encountered problems with distribution shift when training a multitask model and applying it to new tasks downstream? So I would say that we don't get a lot of zero shot generalization right now. um the models only train on 80 tasks which is a lot from an RL research perspective but if you compare that to the amount of data that you have in LMS for example where you get generalization I think we're several orders of magnitude away from that um and as a result I think we get some signal that fine-tuning is better so if you take a pre-trained model and you fine-tune on new tasks um even when it's cross embodyment and um or cross task. We do see some improvements, but it's nowhere near the point of of being zero shot. How did models different from MPO? Um as far as I recall that line of algorithms they train a world model and then treat the world model as a differentiable simulator for policy optimization. So online um online on policy optimization using the world model and I would say it's very similar um we basically just learn the world model in a very similar way but then when you do the policy extraction so saying going from a world model that is able to simulate the world into a working policy um there's many ways that you can go about that you can learn for example just a soft critic policy using your world model. You can use policy optimization like PO or you can use a gradient based method that differentiates um through the world model. Um or you can just do planning which is what we do in CDMC2. Um I think uh we don't have a verdict on what is better. I suspect that it might be more nuance that it's depending on the specific problem and like the size of the model for example um some policy extraction methods might be better. Um, but yeah, I think for now there's still an open problems. &gt;&gt; Okay. Um, thank you. Uh, &gt;&gt; all right. Yeah, maybe that's all. Thank you so much for all the questions and being here, I think. Yeah. So if if you have any questions uh please free to reach out to Nick. I think he is happy to answer those questions. &gt;&gt; Yeah, please send me an an email. I'm happy to chat. &gt;&gt; Yeah, thanks for joining. It was great uh session. Thank you. Thank you. &gt;&gt; Yeah, thank you for for organizing. Heat.
