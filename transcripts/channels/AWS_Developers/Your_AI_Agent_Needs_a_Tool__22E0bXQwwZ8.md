# Your AI Agent Needs a Tool

- 영상 링크: https://www.youtube.com/watch?v=22E0bXQwwZ8
- 채널: AWS Developers
- 업로드일: 2026-06-23
- 자막 언어: en
- 단어 수: 약 2584개

---

## 스크립트

An AI agent without tools is useless, and I'll prove it to you. Let's look at this code here. I'm running a [music] strands agents, which is an open source agentic framework, and I'm asking a very basic question. [music] I'm flying to Vegas this month, and what's the weather going to be like? When I run this code, [music] we will see that strands, the agent, is trying to figure out what's happening. It's trying to figure out what the weather in Vegas looks like, but it actually doesn't know that that's the answer. So, it's taking a hunch, and it's saying that in spring it's going to be mild and pleasant. It's saying that in summer it's going to be very hot, and it's making an approximation based on the data that it initially has been trained on. This isn't the answer that I'm actually looking for, though. But instead, if I actually wanted to figure out what the weather in Vegas looked like, and I wanted today's weather, the actual answer, what I would do is just change one small line, where I would give it access to tools. Now, how can we write a tool? Let me show you a quick example, and then we can go into the details of it. First, I would import tool, and then I would also have request because I'm going to be writing uh I'm going to be calling the weather API, &gt;&gt; [music] &gt;&gt; and so this is this connects to uh a live uh weather API that I can now call. And then in my agent, I can say tools equals get weather. [music] Now, if I rerun this, we will see that it it's going to be [music] calling this tool one get weather, and this will connect to this geocoding API, and then we will actually get the weather. So, it says, "Currently, it's 88° F, 31° C, and it's telling giving us an idea about what to do in Vegas." So, now [music] we actually get real-time information. Now, what does this mean for us? One tool. That's the difference between an agent that guesses and an agent that knows. &gt;&gt; [music] &gt;&gt; And this is just one tool. Give it file access and it reads your code base. Give it your schedule and it knows your events. Give it documentation and it writes code from the latest APIs. [music] Every tool you add makes it more capable. By the end of this video, you'll know how to build these tools yourself. I'll show you the mechanism, how to create custom tools, and how to connect to external services. Let's go. Why do tools exist? A large language model is trained on a snapshot of the internet at a point in time. That snapshot is frozen. The model knows what it read during training, and it knows nothing else. And this gives us three limitations. Firstly, the data is stale. If something happened last week, the model has no idea. Secondly, it has no access to your data, your files, your schedule, your internal documents. None of that was in the training set. And thirdly, it cannot take action. It cannot write a file, it cannot run a command, it cannot look something up. The only thing it can do is produce text. And so, now you might think, "Can't I just paste all my data into the prompt?" Technically, yes. But, there are two problems with that. First thing is that it gets expensive really fast. Every token you send costs money, and if you're stuffing thousands of lines of context into every request that adds up. Secondly, models get worse with more context. This is a very well-known problem called the lost in the middle where the model pays attention to the beginning and the end of a long prompt but misses information buried in the middle. So, even if you do give it everything, it might not necessarily find what it needs. And tools solve this the right way. Instead of dumping everything up front, you give the agent the ability to look things up on demand. It only pulls in what it needs when it needs it. A search tool gives it current information. A file tool lets it read specific data. And a shell tool lets it run commands. Tools connect the agent to the real world without blowing up your context or your budget. And that gives us the key distinction. Without tools, you have a chatbot. With tools, you have an agent. An agent is the model plus tools plus [music] a loop. The model reasons about what to do, calls a tool, observes the result, and decides what's next. That loop is what makes it an agent. How does tool calling work? The model inside the agent never executes anything. It cannot run code, it cannot call an API, and it cannot touch your [music] file system. What it can do is make a request. It sends a structured message to the agent framework saying, "Call this function with these arguments." The framework executes the tool and sends the result back to the model. That back-and-forth reason, request, observe, decide is what's called the genetic hoop. The model reasons, the framework executes, and together it becomes your agent. In practice, when you give an agent tools, the SDK sends the model a menu of what's available. Just names and descriptions and not code. &gt;&gt; [music] &gt;&gt; The model reads the menu, reads the user's question, and generates a structured message like, "I'd like to call the get weather with city equals Las Vegas." The SDK runs the Python function and sends the result back. The model then reads it and either makes another tool request or writes its final response. What's inside a tool? A tool has three parts. The name and schema, which is essentially the name, the parameters, and their types. This tells the model what the in what inputs the tool needs. The description, which is a short explanation of what the tool does and when it's useful. And finally, the handler, which is the actual code that executes when the tool is called. The model sees the result that comes back, but not the code itself. What actually determines when the agent uses a tool correctly? The system prompt. The system prompt is where you tell the agent the strategy. You have a tool called get weather, use it when someone asks about the weather. It bridges the gap between having tools available and knowing when to reach for them. The schema tells the agent what exists. The system prompt tells it when and how to use each tool. If you had to pick which one to get right, pick the system prompt. That's your instruction manual the agent. And one more principle, pay attention to what your tool returns. That return value is the context the agent gets for its next decision. So, let's get started with building our first tool. The fastest way to give an agent tools is the community package strands agents tools. Two imports and one tools list. This agent can now see files &gt;&gt; [music] &gt;&gt; and run commands. We see it calling the file read, and then we also see what directories and files it's got, [music] so we can look at all the list of all the files in this current directory that I've got here. The agent figured out which tools to use, in what order, and with what arguments. We provided the capabilities, and the agent handled the orchestration. Building custom tools. Now, let's build our own tools. And this is where it actually gets interesting. I'm going to build the weather tool from the opening for girl, and chain it with a second tool that recommends what to wear. If you remember from a few minutes ago, I've got the exact same get weather tool that I wrote earlier, and to that we're adding another tool, which is clothing recommendation. And this just has recommendations on what to wear given the temperature that it receives from the first tool. Now, let's run this and see what we get. It's first calling tool one, which is get weather, and then it's calling tool two, which is clothing recommendation. And it's telling us, "Hey, it looks like the clothing recommendation had a bit of a bit of a mix-up given the actual conditions in Vegas, which is 89° Fahrenheit and light winds and no rains, here is what I'd suggest. And it's recommending the light, breathable clothing that [music] we initially established over here when when it's supposed to be hot. It's picking the one that's closest to the range that we recommended, and it's giving us the best one that's the closest [music] fit. Now, we did not program the order. But notice the system prompt. We told it always check the weather first, then use that info to recommend clothing. That guidance in the system prompt is what makes the agent use the tools correctly and reliably. The act tool decorator is the interface. The function name becomes the tool name, the type hints become the schema, and the docstring gives a basic description. But the system prompt is where you give the agent real instructions, when to use each tool, in what order, and what strategy to follow. Now, let's talk about giving the agent access to your own private data. Over here, I've got an events.json file, which just has two places, two cities, and a bunch of events that are happening in each of these cities. Now, this is my own data. None of this is true. Don't hold me on it, but I've written this down. And I'm going to be calling this from as a different tool, from here. We've taken it a step further, and we've actually written a class around it, and all I'm doing here is I'm defining find events, and I'm connecting it to and I'm making my tools look up.find_events, so that it actually does a lookup on events.json. And then what I will do is I will run the exact same system prompt that tells the agent that it's supposed to be a helpful in finding events. It's telling it about the find events tool and telling it when to use it. When I run this, we will see that it prints out Cirque du Soleil, Adele, UFC 315. All of this is from our local JSON file. Data that was never there in the training set. We gave the agent a tool that can access it and told it in the system prompt when to use it. This is the pattern for proprietary data. You don't need to retrain anything. &gt;&gt; [music] &gt;&gt; You just give the agent a tool to access your data and tell it in the system prompt when to reach for it. Before you build everything yourself, check what's already available. The Strands Agents Tools package has pre-built tools for common tasks. There's file read, file write, and editor for working with files. Then there's shell for running any Unix command. There's Python wrapper for executing Python, HTTP request for calling APIs, and use AWS for accessing any AWS service. There's also current time so the agent always knows the date. You pick what you need and drop it in the tools list. MCP, the universal tool standard. Everything so far runs in our Python process. But what about tools that are maintained by another team or written in another language or hosted in as a service? This is where MCP comes in. MCP stands for Model Context Protocol, and the simplest way to explain it is this. MCP is to AI agents what REST [music] is to web APIs. Before REST, every web service had its own custom interface with custom formats and custom methods. [music] You had to write unique integration code for every service you wanted to connect to. REST standardized all of that, and connecting to any service became the exact same pattern. MCP now does that for AI tools. Without MCP, if you want your agent to talk to GitHub, you write a custom tool. If you want Slack, you write another custom tool. Documentation, another one. You'd spend all your time writing wrappers. With MCP, providers publish a server that speaks [music] a standard protocol. Your agent connects, discovers what tools are available, and uses them directly. No integration code needed. In our example here, I'm connecting my code to the GitHub MCP client, and I'm going to ask it a very simple question and say, "Hey, find me a information about Strands, which is an open source framework, and summarize what it does." &gt;&gt; [music] &gt;&gt; And let's run that. We see here that it's connecting to the GitHub MCP server on a standard IO, and it's doing a bunch of different tool calls. So, it's trying to find the relevant pages, it's reading what it needs, and it's writing the correct output [music] based on what it found here. So, it's telling us, "Hey, these are the core features, um and it's multi-agentic, it's bidirectional. This is how you quickly um start." And it's giving us an example. So, it's actually summarizing what Strands does. [music] Now, what did we do here? We created an MCP client, told it how to start the server, and put it in the tools list. The same tools list everything [music] else. Strands connects to the server, discovers the available tools, and makes them accessible to the agent. The agent doesn't know or care which tools are local Python and which come from an MCP server. They all look the same. There are hundreds of MCP servers out there for databases, cloud services, and developer tools. Instead of writing a wrapper for every service you want to connect to, you plug into their server and go. Let me recap what we just covered. We started with an agent that could only guess. We gave it a weather tool, and it returned real answers. &gt;&gt; [music] &gt;&gt; We gave it file access, and it could see our system. We built custom tools, and it chained them together in the right order. We gave it our own private data, and it answered questions about information that it was never trained on. We connected an MCP server, and it gained access to entire documentation with zero integration code. All of that went through just one interface. Tools. And the system prompt told it when and how to use each tool. Here's what I want you to do next. Think of one thing you wish AI could look up or do for you. Maybe it's a file you keep checking, or a lookup you do every day, or an internal API that your team uses. Write the function, add the @tool decorator, and tell the agent when to use it in the system prompt. That's all it takes. All the code from this video is in the GitHub repo linked in the description. Clone it, run the examples, and build your own on top. And if this was useful, subscribe. [music] There's more coming. And if you build something with this, drop it in the comments. I want to see what you make. See you in the next one.
