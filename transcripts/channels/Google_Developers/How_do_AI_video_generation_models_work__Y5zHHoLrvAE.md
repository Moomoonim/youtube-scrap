# How do AI video generation models work?

- 영상 링크: https://www.youtube.com/watch?v=Y5zHHoLrvAE
- 채널: Google Developers
- 업로드일: 2026-04-09
- 자막 언어: ko
- 단어 수: 약 1431개

---

## 스크립트

AI 동영상 생성 모델은 사용하기 매우 쉽습니다. 프롬프트를 입력하면 비디오가 반환됩니다. 그렇다면 AI 영상 생성은 실제로 어떻게 작동하는 걸까요? 자연어 음성을 단 몇 초 만에 비디오 클립으로 변환하는 방법은 무엇일까요 ? 그 질문에 답하기 위해 몇 단계 뒤로 돌아가 보겠습니다. 생성형 AI는 새로운 콘텐츠를 생성 하도록 설계된 머신러닝 모델의 광범위한 범주입니다 . 하지만 그러한 콘텐츠를 생성하는 데에는 다양한 신경망 아키텍처와 기술이 존재합니다 . 예를 들어, 최첨단 텍스트 생성 모델의 대부분은 자기회귀(AR) 방식이라고 알려져 있습니다. 그들은 토큰을 하나씩 생성합니다. 각 토큰은 이전 토큰의 내용에 따라 결정됩니다. 하지만 대부분의 최첨단 이미지, 비디오 및 오디오 생성 모델은 서로 다른 방식으로 작동합니다. 픽셀을 하나씩 생성하는 방식이 아닙니다 . 대신 Imagen이나 VEO 같은 모델은 확산이라는 기술을 사용합니다. 자기 회귀 모델의 핵심 메커니즘은 데이터를 한 번 에 하나의 토큰씩 생성하는 것이지만, 확산 모델은 무작위 노이즈를 반복적으로 정제하여 일관된 출력을 생성하는 방식으로 작동합니다. 단일 이미지의 경우 어떻게 보이는지 살펴보겠습니다. 확산 과정에는 순방향 확산과 역방향 확산의 두 단계가 있습니다. 순방향 확산 과정에서는 여기 보이는 새 사진과 같은 이미지에서 시작하여 이미지에 소량의 가우시안 노이즈를 추가합니다. 기본적으로 이미지의 모든 픽셀을 가져와서 색상과 밝기를 아주 미세하고 무작위적으로 조정한다고 상상해 보세요. 그것이 바로 가우시안 노이즈이고, 여기서 '가우시안'이라는 단어는 그 무작위성의 분포를 설명하며 , 픽셀들이 어떻게 뒤섞이는지를 정의합니다. 그런 다음 약간 노이즈가 있는 이미지에 가우시안 노이즈를 조금 더 추가하고, 또 조금 더 추가합니다. 그리고 노이즈를 계속 추가하다 보면 , 충분한 시간 간격이 지난 후에는 무작위 노이즈와 구별할 수 없는 이미지가 생성됩니다. 그것은 순방향 확산입니다. 이 단계에서는 학습이 이루어지지 않는다는 점에 유의하십시오. 목적은 다양한 수준의 노이즈를 가진 이미지 데이터 세트를 만드는 것입니다. 시간 간격이 길어질수록 이미지는 새의 원본 이미지와 구별하기 어려워집니다. 이 데이터 세트를 확보하면 이를 사용하여 역확산, 즉 이미지 에서 노이즈를 점진적으로 제거하는 과정을 수행할 수 있는 모델을 학습시킬 것입니다 . "모든 돌덩이 속에는 조각상이 숨어 있고, 조각가의 임무는 그 조각상을 발견하는 것이다"라는 말을 들어보셨나요? 알고 보니 무작위 노이즈 이미지 속에는 모두 일관성 있는 이미지가 숨어 있고, 신경망의 역할은 바로 그 이미지를 찾아내는 것이었습니다. 그렇다면 이러한 역확산 과정을 수행하는 모델을 어떻게 훈련시킬까요 ? 네, 우리는 특정 시점에 노이즈가 섞인 이미지를 모델에 제공하고, 모델의 과제는 이전 시점에 추가된 노이즈 패턴의 정확한 노이즈 정도를 예측하는 것입니다 . 이 모델은 예측된 노이즈와 순방향 확산 과정에서 실제로 추가된 노이즈 사이의 거리를 최소화하려고 합니다. 그것이 바로 우리가 사용하는 기준 레이블입니다. 그런 다음 예측된 노이즈를 이미지에서 빼서 노이즈가 약간 줄어든 이미지를 얻습니다. 충분한 시간 간격과 충분한 데이터를 사용하여 이 과정을 반복하면, 모델은 완전히 노이즈로 가득 찬 이미지를 입력받아 노이즈를 제거하여 일관성 있는 이미지를 반복적으로 생성하는 방법을 학습하게 됩니다. 추론 과정에서 역확산 과정은 일련의 단계를 거쳐 발생한다는 점에 유의하십시오. 모델을 한 번만 순방향으로 통과시켜 노이즈에서 일관된 이미지로 바로 변환하는 것은 아닙니다 . 대신, 우리는 여러 번의 순방향 전달을 수행하며 , 매번 점진적으로 노이즈가 적은 이미지를 입력합니다. 지금까지 우리는 단순히 무작위 이미지 생성기를 만들었습니다. 멋지긴 하지만, 우리가 원하는 건 단순히 무작위 이미지를 생성하는 모델이 아니기 때문에 그다지 유용하지는 않습니다. 우리는 모델이 중절모를 쓴 까마귀나 열대 과일 접시, 또는 우리가 생각해내는 그 밖의 어떤 이미지든 만들어내도록 유도하고 싶습니다. 바로 여기서 조건부 확산이 등장합니다. 모델이 특정 유형의 이미지를 생성하도록 유도하려면 학습 과정에서 단순히 이미지를 입력하는 것이 아닙니다 . 또한 해당되는 텍스트 설명도 함께 전달합니다. 텍스트를 삽입하면 모델은 프롬프트의 단어가 이미지의 다양한 특징과 어떻게 대응하는지 학습합니다. 이러한 방식으로 우리는 모델이 노이즈로부터 어떤 유형의 이미지를 생성할지 제어할 수 있습니다 . 네 , 지금까지 이미지 확산에 대한 개괄적인 설명을 드렸습니다. 이로써 비디오 생성 원리를 이해하는 데 한 걸음 더 다가섰습니다. 우선, 비디오란 무엇일까요? 사실 이건 그냥 이미지 모음일 뿐이에요. 따라서 초당 24프레임의 프레임 속도를 가진 비디오가 8초 길이이면, 24 * 8 = 192개의 이미지 모음일 뿐입니다. 간단하게 설명하기 위해, 우리 비디오의 길이가 단 네 프레임이라고 가정해 보겠습니다. 실제로 비디오 생성 모델은 이보다 훨씬 더 많은 이미지 프레임을 출력하지만, 4개를 사용하면 계산과 시각화가 조금 더 간단해집니다. 앞서 우리는 이미지 하나를 촬영하고 순방향 확산 과정에서 이미지에 노이즈를 점진적으로 추가했습니다. 이 과정을 확장하여 비디오를 구성하는 네 개의 이미지 모두에 대해 순방향 확산을 실행해 보겠습니다 . 역확산을 수행하도록 모델을 학습시키려면 특정 시간 단계의 네 이미지를 모두 하나의 배치로 묶어 입력 학습 데이터로 사용할 수 있습니다. 레이블은 이전 시간 단계의 약간 더 노이즈가 적은 이미지를 얻기 위해 각 이미지에서 제거해야 하는 노이즈 패턴의 정확한 노이즈입니다. 이전과 마찬가지로, 모델 학습의 목표는 예측된 노이즈와 실제 노이즈 사이의 거리를 최소화하는 것입니다. 그러니까 기본적으로 이미지 묶음이 모델에 입력되면 제거해야 할 해당 노이즈 묶음이 출력됩니다. 이미지에서 노이즈를 제거하면 노이즈가 약간 줄어든 이미지 시퀀스를 얻을 수 있습니다. 이상적인 상황이라면, 단일 이미지에서 여러 이미지로 확산 효과를 확장하는 이 과정은 추가적인 수정 없이도 문제없이 작동할 것입니다. 추론 시에는 무작위 노이즈가 포함된 4프레임으로 시작하여, 모델이 이러한 이미지들을 반복적으로 노이즈 제거하고, 빠르게 연속 재생했을 때 새가 날아오르는 모습을 보여주는 이미지 시퀀스를 반환합니다. 하지만 안타깝게도 상황은 그렇게 간단하지 않습니다. 비디오 생성에는 두 가지 큰 어려움이 있기 때문입니다. 첫 번째는 시간적 일관성입니다. 만약 여러분이 물리 법칙이 다소 비현실적인 초기 비디오 생성 모델들을 본 적이 있다면 , 그것은 당시 모델들이 물체가 시공간을 통해 어떻게 움직이는지 제대로 이해하지 못했기 때문입니다 . 사실, 현재의 비디오 생성 모델이 아무리 훌륭하더라도 여전히 이러한 부분을 잘못 예측하는 경우가 종종 있습니다. 화면 깜빡임, 불필요한 색상 변화, 벽을 통과하는 물체, 그리고 물리 법칙을 무시하는 듯한 현상들을 생각해 보세요. 만약 우리가 "새가 날아오르는 모습"이라는 텍스트와 함께 네 개의 무작위 노이즈 이미지를 확산 모델에 입력한다면 , 서로 다른 나뭇 가지의 서로 다른 위치에 있는 네 개의 서로 다른 이미지를 얻게 될 것입니다. 영상 제작에 있어서 우리는 단순히 개별 이미지가 보기 좋게 보이는 것만 신경 쓰는 것이 아닙니다. 이미지들은 동영상 으로 재생될 수 있도록 순서대로 배열되어야 합니다 . 또한, 모델은 이전 프레임이 이후 프레임에 어떤 영향을 미치는지 이해할 수 있는 방법이 필요합니다 . 예를 들어 영상 속 인물이 테이블에서 커피잔을 집어 쓰레기통에 버렸다면, 그 커피잔이 다음 장면에서 마법처럼 테이블 위에 다시 나타나서는 안 됩니다. 동영상 제작이 가져오는 두 번째 큰 과제는 연산 능력에 대한 요구 사항입니다. 동영상은 엄청난 양의 데이터입니다. 앞서 우리는 초당 24프레임 * 8초 = 192개의 이미지가 있다고 말했습니다. 만약 각 이미지의 해상도가 720x1280p라면, 무려 1억 7600만 화소에 달합니다. 픽셀 수가 너무 많아요. 그래서 우리는 단일 이미지를 다룰 때 적용했던 것과 거의 동일한 확산 전략을 사용할 것이지만, 이러한 비디오 특유의 문제를 해결하기 위해서는 몇 가지 추가적인 기술이 필요합니다. 동영상은 이미지들의 모음이기 때문에 3차원 매트릭스 또는 큐브로 생각할 수 있습니다 . 각 이미지 프레임을 개별적으로 전달하는 대신, 이 큰 큐브를 더 작은 큐브 또는 패치들의 연속으로 나눌 것입니다. 이러한 패치는 시퀀스 내의 여러 이미지에 걸쳐 있기 때문에 시공간 패치라고 합니다. 이 전략을 통해 시간적 일관성을 더욱 향상시킬 수 있을 것입니다. 만약 '시퀀스'라는 단어를 듣고 변압기를 떠올리신다면, 제대로 생각하신 겁니다. 확산 모델은 일반적으로 비전 트랜스포머라고 불리는 트랜스포머 기반 신경망입니다. 모델을 학습시킬 때 , 우리는 이러한 노이즈가 섞인 시공간 패치 배치들을 입력으로 사용합니다. 트랜스포머 아키텍처를 사용하는 장점은 모델이 어텐션 메커니즘을 활용하여 특정 패치와 연관되고 관련 있는 다른 패치들을 파악할 수 있다는 점입니다. 이러한 방식으로 모델은 새가 1번째 프레임에서 날기 시작하면 160번째 프레임에서 공중에 떠 있을 것이라는 것을 학습하고 그 사이의 프레임들을 보간할 수 있습니다. 이것이 바로 모델이 시간적 일관성을 학습하는 방식입니다 . 네, 그래서 첫 번째 문제, 즉 모델이 프레임 간 일관성을 유지하도록 돕는 문제를 해결했습니다. 하지만 이 모든 과정이 지나치게 많은 계산량을 요구한다는 문제가 여전히 남아 있습니다. 우리는 잠재 확산이라는 기술을 사용할 것입니다. 이 기술을 사용하면 전체 과정을 좀 더 계산적으로 처리할 수 있습니다. 기존의 트랜스포머 확산 모델 외에도 오토인코더라는 두 번째 머신러닝 모델을 추가할 예정입니다 . 오토인코더는 이미지를 잠재 공간으로 압축하는 신경망의 한 유형입니다 . 인코더와 디코더라는 두 가지 구성 요소로 이루어져 있습니다 . 기본적으로 이미지를 입력받아 압축된 이미지 표현, 즉 인코딩을 생성합니다. 디코더 모델은 인코딩된 표현을 입력으로 받아 원래 이미지를 반환할 수 있습니다. 인코더는 이미지를 압축하면서 점점 작아지는 여러 겹의 레이어로, 디코더는 원래 이미지를 복원하면서 점점 커지는 여러 겹의 레이어로 생각할 수 있습니다. 네, 이미지를 압축한 다음 압축을 해제하여 원본 이미지를 반환했습니다. 이것만으로는 그다지 도움이 되지 않습니다. 하지만, 여기 있는 이 행렬, 즉 잠재 공간에 이미지를 인코딩한 것이 바로 우리가 필요한 것입니다. 원본 이미지를 압축한 버전이라 크기는 작지만 중요한 정보는 그대로 유지하고 있습니다 . 이미지 픽셀에 걸쳐 확산을 적용하는 대신 , 잠재 공간에 걸쳐 확산을 적용할 것입니다 . 이 잠재 벡터는 원본 이미지보다 훨씬 작기 때문에 전체 과정이 계산적으로 처리 가능해집니다. 자, 이제 모든 걸 종합해 봅시다. 확산 모델을 학습시키기 위해 먼저 이미지 프레임 시퀀스를 준비합니다. 우리는 이미지를 공간과 시간에 걸쳐 패치로 나누고 , 그 다음 잠재 공간에서 이러한 패치의 더 작은 인코딩된 표현을 얻습니다 . 이러한 패치들은 확산 모델로 전달되어, 확산 모델은 패치에서 제거해야 할 노이즈를 예측합니다. 그리고 이 모델은 예측된 노이즈와 실제 노이즈 사이의 거리를 최소화하려고 합니다. 이것이 바로 우리가 확산 모델을 훈련시키는 방법입니다 . 추론 단계에서는 확산 모델이 제대로 학습된 후, 잠재 공간의 차원에 무작위 노이즈를 투입합니다 . 이 매트릭스는 여러 패치로 분할됩니다. 이러한 잠재 공간 조각들은 확산 모델을 거치면서 반복적으로 노이즈가 제거되고, 충분한 반복 과정을 거치면 잠재 시공간 조각들이 디코더를 통과하여 잠재 공간에서 이미지 픽셀로 변환됩니다. 자, 이렇게 해서 새가 날아오르는 영상을 얻었습니다 . VEO를 이용한 비디오 생성 기능을 사용해 보려면 아래 문서를 참조하세요. 다음에는 어떤 내용을 배우고 싶은지 댓글로 알려주세요. 제가 연구 논문을 읽어볼게요. 당신은 안 읽으셔도 돼요.
