# Jaisidh Singh  - Almost Free Modality Stitching of Foundation Models

- 영상 링크: https://www.youtube.com/watch?v=tKvm9VYpqVw
- 채널: Cohere
- 업로드일: 2025-09-19
- 자막 언어: ko
- 단어 수: 약 3130개

---

## 스크립트

[음악] 안녕하세요 여러분, 저는 제이스드입니다. 저는 현재 독일 튀빙 대학교에서 머신러닝 석사 과정을 밟고 있습니다. 저는 엘리스 튜브 연구소에서 안토니오 오비에르 박사님과 함께 연구 조교로 잠시 일했는데, 이것이 그 연구의 결과물입니다 . 저희 ENLP 논문인 ' 기초 모델의 거의 무료 양식 연결'에 대해 여러분과 이야기 나눌 수 있게 되어 기쁩니다. 저희 연구의 주요 동기는 오픈 소스 사전 학습 모델 출시의 증가입니다. 아시다시피 사전 학습 모델은 특히 다양한 언어에서 거의 매주 출시되고 있습니다. 이 그래프에는 Kuhir, Luther, Google, Meta, Microsoft와 같은 주요 연구소 몇 곳만 나와 있는데, 이들이 출시하는 모델 수는 지난 몇 년 동안 꾸준히 증가해 왔습니다. 이제 그 어느 때보다 더 많은 선택지가 있습니다. 제가 이전 슬라이드에서 보여드린 몇몇 연구소의 경우 , 지난 3년 동안 누적된 허깅 페이스 저장소 출시 건수도 증가했습니다. Tim의 경우와 거의 동일한 수준으로 성장했는데, 이는 매우 중요한 점입니다. Tim은 소수의 인원이 관리하고 있으며, 이미지 모델 또는 이미지 인코더, 분류기 저장소라고 할 수 있는데, Tim 역시 빠르게 성장했습니다. 그래서 저희는 필요에 따라 사용할 수 있는 사전 학습된 모델의 수가 줄어들 것으로 예상하지 않습니다 . 그리고 이러한 사전 학습된 모델의 멋진 점은 학습에 사용된 모달리티에만 국한되지 않고 다양한 분야에서 활용될 수 있다는 것입니다 . 유니오델이라고 하면 특정 모달리티에 대해 학습되었기 때문에 이미지 분류기와 같은 의미를 내포합니다. 그것은 이미지라는 특정 양식을 기반으로 작동합니다. LLM과 같은 것도 텍스트라는 단일 양식에 기반하여 작동하기 때문에 유니오드 모델이라고 할 수 있습니다. 이러한 모드 모듈들이 출시됨에 따라, 우리는 그것들이 점점 더 다중 모달 시스템에 통합되고 있다는 것을 알게 됩니다. 예를 들어 CLIP과 같은 멀티모델 시스템에서 모달리티별 모듈 역할을 하는 것이 바로 이것입니다. 텍스트 인코더는 단일 모델이고 이미지 인코더도 단일 모델이지만, 이들은 표현 공간을 공유합니다. 이것이 바로 우리가 활용할 수 있는 풍부한 기초 지식으로 이어지는 것입니다 . SAM에도 비슷한 기능이 있는데, 이미지 인코더 와 프롬프트 인코더가 있고, 둘 다 경량 마스크 디코더에 입력됩니다. 마찬가지로 Lava에서는 단일 모달 언어 모델과 단일 모달 비전 인코더가 있으며, 이 두 모델의 표현은 프로젝션 또는 커넥터를 통해 융합됩니다. 그리고 두 모달리티에 걸쳐 융합된 표현은 다중 모델 시스템에서 다중 모델 목표를 달성하는 데 사용됩니다. 그래서 좋은 점은 단일 모델 사전 학습 시스템이나 다중 모델 시스템 에 연결할 수 있는 사전 학습 모델 등 다양한 옵션이 있다는 것입니다 . 유니모델 모델을 사용하는 이유는 처음 부터 학습시키지 않기 위함이죠, 그렇죠? 이는 컴퓨팅 자원을 절약하고 이미 풍부한 지식을 담고 있는 시스템을 활용하기 위한 것입니다. 하지만 어떤 형태의 융합, 즉 다중 모델 목표를 안내하는 표현 융합 없이는 단일 모델 모델을 다중 모델 시스템에서 직접 사용할 수 없습니다 . 우리가 말하는 바느질이 바로 이런 거죠, 그렇죠? 스티칭은 커넥터라고 불리는 특수 학습 가능 네트워크를 사용하여 사전 학습된 모델의 특징을 정렬합니다 . 클립에서 할 일은 텍스트 인코더 위에 작은 MLP를 추가하여 텍스트 임베딩을 이미지 임베딩과 동일한 공간으로 투영하는 것입니다. 그런 다음 정보와 손실만을 사용하여 커넥터만 학습시키면 됩니다 . 다층 모델(MLM)에서는 이미지 인코더 위에 커넥터를 사용하고, 패치별 특징을 LLM의 임베딩 공간으로 투영한 다음, 언어 모델링 손실에 대해서만 커넥터를 학습시킵니다. 따라서 커넥터는 표현들을 연결하는 역할을 하며, 멀티모델 시스템 개발의 초기 단계에서 학습 가능한 유일한 모듈입니다 . 물론, 세부 조정은 나중에 이루어지며, 그것이 우리 작업의 기반이 될 것입니다 . 다시 말하지만, 선택지가 많다는 건 좋은 일이지만, 너무 많다는 것도 문제입니다. 우리가 알고 있는 몇 가지 사실은 단일 모달 표현의 품질이 다중 모달 성능에 직접적인 영향을 미친다는 것입니다. 그리고 선택지가 너무 많기 때문에 각 단일 모달 모델을 하나씩 선택하는 것은 실험 비용을 증가시킬 것입니다. 예를 들어 CLIP에서 이미지 인코더가 4개, 텍스트 인코더가 7개 있다면, 성능을 정확히 예측하기 어려운 27쌍의 모델이 생성됩니다. 따라서 이제 문제는 실무자의 관점 에서 이러한 단일 모델 백본을 어떻게 선택하느냐입니다. 간단한 가설을 세워 보겠습니다. 가장 성능이 좋은 이미지 인코더 와 가장 성능이 좋은 텍스트 인코더를 선택합니다. 이 두 인코더를 결합하면 아마도 최상의 멀티모델, 즉 CLIP -L과 같은 모델을 얻을 수 있을 것입니다. 이 그래프에서 보시다시피, E2L은 최고의 이미지 인코더이고, RO와 함께 사용하면 E2L은 EVA가 너무 크다는 의미이고, RO는 Roberta가 크다는 의미입니다. 그리고 이 두 부분을 꿰매면 최상의 멀티모달 성능을 얻을 수 없습니다. DEIT3 large와 mpnet base를 함께 사용하면 최상의 멀티모달 성능을 얻을 수 있습니다. 그래서 이것은 우리가 성능이나 매개변수 개수와 같은 단순한 이단적 개념이나 아이디어만으로는 다중 모드 성능을 예측할 수 없다는 것을 알려줍니다. 이건 실제로 검색이 필요합니다. 이상적으로는 모달리티 1의 모델 n개와 모달리티 2의 모델 m개가 주어졌을 때, 이들 사이의 연결 고리( 그리드 검색이라고 함)를 n * m개 학습하는 것입니다. 기본적으로 전체 교차곱 공간을 학습하고 가장 성능이 좋은 단일모달 쌍을 찾습니다. 이것이 바로 멀티모달 시스템의 최적 초기화 지점이 되고, 이제 미세 조정할 수 있습니다. 하지만 멀티모달 이미지 캡션 데이터 세트가 엄청나게 크다는 것을 고려하면 이는 사실상 불가능합니다. Lion은 50 억 개의 이미지, Red Caps는 1,200만 개, YFCC는 1억 개, Ion Conceptual Captions도 1,200만 개입니다. 따라서 만약 이렇게 큰 데이터 세트를 사용하고, 모달리티 1에 대한 모델이 n개, 모달리티 2에 대한 모델이 m개 있다고 가정해 봅시다. 이 경우 모달리티별 모델에 대한 그리드 검색을 통해 발생하는 비용은 계산적으로 불가능해집니다. 실무자의 관점에서 우리가 원하는 것은 비용 대비 최고의 효과입니다. 따라서 각 이미지 및 텍스트 모델에 대해 다양한 옵션을 제공하는 것이 중요합니다 . 이상적으로는 최적의 조합을 매우 저렴하고 정확하게 찾아낸 다음, 원하는 만큼 미세 조정을 하고 싶습니다 . 오른쪽? 실무자 입장에서 보면, 우리는 일종의 플롭 예산을 가지고 있는데, 최고의 페어를 찾는 데 플롭 예산의 대부분을 쏟아붓고 싶지는 않습니다 . 우리는 빠르고 정확하게 검색한 다음, 특정 용도에 가장 적합한 모델을 얻기 위해 세부 조정을 거치고자 합니다. 오른쪽? 이것이 바로 우리 논문의 동기입니다. 우리는 저렴하고 정확한 검색 서비스를 제공합니다. 이에 대한 해결책은 하이퍼 네트워크를 통해 모든 커넥터를 학습하는 것입니다. 오른쪽? 커넥터를 표현이라고 생각할 수 있습니다. 따라서 n * m개의 커넥터가 있다는 것은 모든 커넥터의 가중치로 이루어진 큰 공간에 대해 n * m개의 표현이 존재한다는 것을 의미합니다 . 그래서 우리는 n * m개의 모든 커넥터의 가중치를 예측하도록 공동으로 학습하는 하이퍼 네트워크를 훈련시킵니다. 다시 말해, n * m개의 설정에 걸쳐 커넥터 가중치의 분포를 학습하는 것입니다. 오른쪽? 그렇다면 하이퍼넷 네트워크란 무엇일까요? 하이퍼넷 네트워크는 확산 모델의 이미지 생성 영역에 익숙한 분들이라면 아실 수도 있겠지만, 현재로서는 그다지 주류 기술은 아닙니다. 따라서 하이퍼렛 네트워크는 다른 신경망의 가중치를 생성하는 신경망입니다 . 매우 메타적인 표현이지만, 바로 그게 핵심입니다. 이는 자연 속 추상화 의 유전자형과 표현형에서 영감을 받았습니다 . 우리 모두는 특정한 유전자형을 가지고 있으며, 이 유전자형은 표현형이라고 불리는 신체적 특징으로 나타납니다. 그러니까 유전자형은 하이퍼 네트워크이고, 주 네트워크의 매개변수는 표현형이 되는 거죠. 하이퍼 네트워크는 아주 흥미로운 응용 분야를 많이 가지고 있어요. 예를 들어, 대규모 모델의 전체 매개변수 공간을 예측할 수 있습니다 . 여기 있는 그림은 보리스 나드브가 쓴 '그래프 하이퍼넷 네트워크 2(GN2)'라는 논문에서 가져온 것입니다. 나드브는 저희 논문의 공동 저자이기도 합니다. 이 논문에서는 신경망의 계산 그래프를 학습하고, 하이퍼넷 네트워크( 실제로는 그래프 신경망)를 사용하여 계산 그래프 의 모든 피연산자의 가중치를 예측합니다 . 기본적으로 이들은 전체 규모 이미지 모델의 가중치를 예측합니다 . 또 다른 응용 분야는 다른 모델을 조건화하기 위해 작은 매개변수 공간을 예측하는 것입니다 . 이는 현재 하이퍼렛 네트워크 분야, 특히 이미지 확산 분야에서 매우 인기 있는 기술입니다. 쑨의 작품도 매우 흥미롭습니다. 어, 올해 출시된 하이퍼스티어(Hyper Steer)라는 건데요, 특정 프롬프트가 주어지면 언어 모델의 스티어링 벡터를 생성해서, 참조 프롬프트를 언어 모델에 직접 입력했을 때보다 더 나은 결과물을 만들어낼 수 있게 해줍니다. 기본적으로 각 프롬프트마다 비밀 소스나 비밀스러운 전문 지식을 추가하는 것입니다 . 그리고 저는 이러한 애플리케이션들이 앞으로 널리 보급될 것이라고 생각합니다 . 자, 이제 우리의 경우를 살펴보자면, 우리는 전체 파라미터 공간을 예측하고 싶습니다. 더 구체적으로 말하자면, 멀티모달 시스템에서 모달리티별 모듈들 사이에 있는 학습 가능한 모듈인 커넥터의 파라미터를 예측하고 싶습니다 . 그럼 이것에 대해 간단한 추상화, 아주 간단한 수학적 개념을 생각해 봅시다. 우리는 입력값 i를 함수 fi에 매핑하려고 합니다 . 오른쪽? 여기서 i는 단봉 형 모델 쌍의 인덱스입니다. 어, 기억하시겠지만 우리는 n * m 쌍을 가지고 있었죠. 그래서 쌍을 생각할 때, 이미지 모델 공간에서 J 모델을, 텍스트 모델 공간에서 k번째 모델을 생각하면, i는 쌍 j k의 인덱스입니다. 마치 툭 떨어지는 것 같아요. 음, 원하시면 모델링을 해보세요. 따라서 fi는 if 쌍을 연결하는 역할을 합니다. MI는 첫 번째 양식 특징의 차원이 되고, MI2는 두 번째 양식 특징의 차원이 됩니다. 따라서 커넥터는 한 모달리티 공간에서 다른 모달리티 공간으로 투영함으로써 제 역할을 수행합니다. 이제 I를 하이퍼 네트워크 HI의 조건으로 사용하면 함수의 매개변수인 HI로부터 θI를 생성할 수 있으며, 이를 매개변수 θI에 대한 함수 G의 합성으로 모델링할 수 있습니다. 따라서 fi는 매개변수 θi가 주어졌을 때 입력에 대한 함수 g가 됩니다 . 이것이 바로 하이퍼렛 네트워크가 하는 일입니다. 겉보기에는 상당히 복잡해 보이는 구조이지만, 실제로 사용해 보면 그렇게 복잡하지는 않습니다. 핵심은 다양한 차원의 매개변수 공간을 예측할 수 있도록 하는 것입니다. 예를 들어, 세 개의 레이어로 이루어진 MLP가 있다고 가정해 보겠습니다. 우리가 하려는 것은 각각 세 개의 레이어로 이루어진 n * mLP를 예측하는 것입니다. 레이어를 저장하기 위해 임베딩 조회 테이블(EH)이라는 조회 테이블을 사용합니다. 아래 첨자 오메가(ω)는 가중치를 나타냅니다. EH는 예측할 레이어를 조회하는 데 사용되고, WH는 예측할 단일 모드 모델 쌍의 인덱스, 즉 모델 조합을 조회하는 데 사용됩니다. E1, E2, E3에 CK를 더하면 특정 커넥터 설정과 특정 레이어 구성에 따른 조건부 입력을 얻게 되고, 이를 디코딩합니다. 우리는 그것들을 MLP에서 사용 가능한 가장 큰 매개변수 공간으로 디코딩합니다 . 이 경우, MLP에서 가장 큰 매개변수 공간은 D2 * D3입니다. 그런 다음 우리는 그것들을 정확한 모양으로 자르기만 하면 됩니다. 슬라이싱 방식이 다소 투박해 보이지만 실제로는 상당히 잘 작동합니다. 왜냐하면 이 방식은 파라미터 디코더가 가변 크기의 MLPS에 대한 매우 압축된 표현을 학습하도록 유도하기 때문입니다 . 저희의 주요 교육은 신속하고 정확하게 검색하는 것을 목표로 하는 저희의 핵심 교육 계획이며, 이 계획은 저희의 목표와 매우 잘 부합합니다 . 따라서 하이퍼넷 네트워크는 유사한 단일 모델들을 서로 연결할 수 있으며, 우리는 이 요소를 데이터 효율성의 기준으로 활용한다는 것이 핵심 아이디어입니다. 핵심은 데이터를 절대 중복해서 사용하지 않는 것입니다 . 이 아이디어는 모델 쌍을 반복하여 커넥터 비율에 대한 대표적인 지식을 추가하는 것입니다. 보시는 바와 같이, 데이터 배치(파란색으로 1부터 9까지 표시된 것들)를 얻는 타임라인을 상상해 보면, 가장 먼저 하는 일은 모델 쌍으로 이루어진 미니 배치( 빨간색으로 표시된 것들)를 만드는 것입니다. 따라서 첫 번째 데이터 배치에 대해 첫 번째 모델 배치를 적용한 다음 해당 임베딩을 사용하여 하이퍼 네트워크를 업데이트합니다 . 두 번째 데이터 배치에 대해서는 두 번째 배치 모델을 적용합니다. 셋, 셋, 넷, 넷. 하지만 다섯 번째 데이터 배치에 대해서는 다시 첫 번째 미니 배치 모델로 돌아갑니다. 오른쪽? 따라서 이는 하이퍼넷 네트워크가 각 모델 쌍으로부터 수신하는 데이터 양을 효과적으로 줄이는 역할을 합니다. 만약 그리드 서치를 실행한 다음 모든 모델 쌍을 독립적으로 학습시킨다면 전체 데이터 세트를 볼 수 있을 것이고, 예를 들어 5 에포크 동안 학습시킨다면 모델 쌍당 5배의 데이터 세트 샘플 수를 볼 수 있을 것입니다. 하지만 여기서는 그렇지 않습니다. 데이터 효율성을 실제로 향상시키는 것은 쌍당 처리되는 데이터 양을 효과적으로 줄여 하이퍼 네트워크가 단일 모드 모델 간의 기능적 유사성을 연관 짓는 방법을 학습하고, 매번 일반적인 지역 최소값으로 한 걸음씩 나아갈 수 있도록 하는 아이디어입니다 . 자, 이제 저희 실험에 대해 말씀드리자면, 저희 실험은 아주 간단합니다. 본 연구에서는 널리 사용되는 두 가지 멀티모델 설정에서 하이퍼 네트워크 연결 예측 기법의 성능을 평가하고자 합니다. 첫 번째는 클립 스타일의 VLM으로, 대조 정보 손실과 이미지 인코더 및 텍스트 인코더라는 두 개의 인코더를 사용합니다. 음, 두 번째 계획은 영감을 받은 계획에 있는 용암입니다. 음, 그건 멀티모델 LLM에서 나오는 거죠. 그러니까 저쪽 생각은 이렇습니다. 잠깐만요, 제가 방금 여기 도착했어요. 그러니까 핵심 아이디어는 이미지 인코더가 패치 별 특징을 출력하고, 경로별 특징을 LLM의 임베딩 공간으로 투영한 다음, 인과 모델링 손실을 사용하여 연결자만 학습시키는 것입니다. 두 설정 모두에서 전체 모델 쌍의 양은 서로 다릅니다. 따라서 VLM의 경우 9 * 3, 즉 9개의 이미지 인코더와 3개의 텍스트 인코더로 총 27쌍이 있습니다. 두 번째 설정에서는 3+3개의 이미지 인코더와 3개의 LLM을 사용합니다. 그리고 기본적으로 다양한 커넥터 매개변수 규모에 대한 하이퍼 네트워크를 평가하기 위해, 커넥터를 선형 레이어, 1 층 MLP에서 2층 MLP로 변경했습니다. 따라서 VLM의 기본 아이디어는 사전 학습된 이미지 인코더 n개와 사전 학습된 텍스트 인코더 m개를 보유하고, 이미지를 이미지 인코더에 걸쳐 임베딩하여 인코더별로 차원 d의 벡터를 얻는 것입니다. 그러면 n * d 형태의 텐서를 얻게 됩니다. 마찬가지로 텍스트의 경우에도 m * d개의 별 모양 값을 얻게 되고, 그 다음에는 커넥터를 사용하여 차원 d개의 별 모양 값을 d로 매핑합니다. 이 커넥터의 가중치는 하이퍼넷 네트워크에서 생성되므로, 기본적으로 클립 손실을 하이퍼넷 네트워크까지 역전파하지만, MLMS용 사전 학습된 인코더에는 절대 전달하지 않습니다. 이것도 마찬가지로 매우 비슷합니다 . 저희는 이미지 인코더를 가지고 있으며, 이미지 인코더는 다양한 LLM(로컬 라이프사이클)을 사용하기 때문에 다양한 모달리티 커넥터로 전달되는 임베딩을 출력합니다 . 이것은 1 + 3 설정입니다. 따라서 이미지 인코더 하나와 LLM 세 개가 있는 경우, 커넥터가 세 개 필요합니다. 그런 다음 커넥터는 이미지 특징을 LLM 임베딩 공간에 매핑합니다. 이러한 특징들을 연결하여 LLM으로 전송한 다음, LLM은 언어 모델링 손실 함수를 사용하여 학습됩니다. 하지만 손실은 하이퍼렛 네트워크로만 역전파되고 다른 어떤 것에도 전파되지 않습니다. 그래서 저희는 먼저 소규모로 실험을 진행합니다. 우리는 이것이 소규모로 어떻게 작동하는지 살펴보고, 그 결과를 바탕으로 더 큰 규모의 구현 방안을 마련하고자 합니다. 그러니까 여기서 핵심은 딱 세 쌍만 사용하는 거예요. 우리는 세 개의 이미지 인코더와 하나의 텍스트 인코더를 가지고 있으며, 클립과 유사한 최적의 모델을 찾고자 합니다 . 여기서 커넥터는 1층 구조의 MLP이며, 평가 작업은 이미지 분류입니다. 클립이 이미지 분류에서 어떻게 평가되는지는 여러분 모두 잘 알고 계실 거라고 생각합니다 . 그럼 그 부분은 넘어가도 될 것 같네요. 그래프에서 보시다시피, 하이퍼 네트워크 또는 저희 방법인 히메머를 훈련시키면 매우 효율적인 것으로 나타납니다. 실제로 그리드 서치보다 플롭 효율이 4.4배 더 높습니다. 그래프에서 볼 수 있는 모든 점은 훈련의 한 에포크를 나타냅니다. 그래서 10개의 시대가 있습니다. 우리는 그리드 검색과 우리 방법인 himer 모두에 대해 동일한 값을 유지합니다. 그리고 우리는 HIMA가 발열 방지용 플롭을 사용한다는 것을 알게 되었습니다 . 이건 정말 흥미로운데, 그 주된 이유는 저희 교육 파이프라인이 구성된 방식 때문입니다. 아시 다시피, 데이터 효율적인 설정에서 우리가 요구하지 않는 것은 하이퍼넷 네트워크에 모든 모델의 모든 샘플을 보여주는 것입니다. 이러한 설정 덕분에 우리 네트워크는 효율성을 높이고 실제로 그리드 검색과 동등한 성능을 발휘할 수 있습니다. 이미지에서 가장 높은 지점을 보면 두 모델 모두 두 가지 방법 모두 27%의 정확도를 달성하는 것을 알 수 있습니다. 이는 한 쌍의 데이터셋만 학습시키는 것보다 약간 저렴하지만, 모든 데이터셋을 학습시키는 것보다는 훨씬 저렴합니다. 9+3의 모든 모델에 걸쳐 대규모로 고차 모드 의 정확도를 측정하는 새로운 방법을 정의했습니다 . 우리가 확실히 아는 것은 그리드 검색이 가장 정확한 방법이라는 것입니다. 모든 모델을 그리드 검색과 짝지어보면, 모든 모델의 성능과 순위가 실제로 그렇게 결정될 것입니다. 그래서 저희는 HIMA가 최적의 커넥터를 찾는 과정에서 생성하는 순위를 살펴보고, 이를 그리드 검색에서 예측한 순위와 비교합니다. 저희는 Kmetric의 정규화된 할인 누적 이득과 스피어만 상관 계수를 통해 이를 수행하며, 모든 커넥터의 모든 작업에서 스피어만 상관 계수가 그리드 검색과 매우 잘 일치함을 확인했습니다. 이전 실험에서는 himemer가 그리드 검색보다 비용이 저렴하다는 것을 보여주었고, 이번 실험에서는 himemer의 순위가 그리드 검색과 동일하다는 것을 보여줍니다. 따라서 이는 여러분이 얻게 될 모델의 실제 순위와 본질을 매우 잘 반영합니다 . 그러므로 이것은 우리가 저렴하고 정확하게 검색한다는 조건을 충족합니다. n * m= 27 규모에서, himemer를 통한 검색의 평균 비용이 그리드 검색보다 10배 저렴하다는 것을 알 수 있습니다. 하지만 순위는 거의 정확합니다. 특히 상위 5위권, 5위에서 10위까지의 순위는 항상 같습니다 . 자, 여기서 흥미로운 점은, 음, 어쩌면 우리가 여기서 약간 반칙을 하고 있는지도 모르겠습니다. 우리는 하이퍼넷 네트워크에 모든 데이터를 보여주지 않고 효율적인 모델 배치 방식을 적용하는 매우 효율적인 훈련 설정을 가지고 있습니다. 그러니 우리가 효율적인 것은 당연하죠. 이제 먼저 필요 없는 모델의 수를 줄여서 그와 Grit 검색을 동등한 위치에 놓아보도록 하겠습니다 . 이상적으로는, 우리는 최고의 모델에만 관심이 있는 것입니다. 우리는 빠르고 저렴하며 정확하게 검색하려고 노력하고 있습니다. 그래서 우리는 최고의 모델에만 관심이 있습니다. 그리드 검색을 해봅시다. 그리고 두 에포크마다, 즉 전체 학습 과정의 1/5마다 중간 성능보다 낮은 모델을 제거할 수 있습니다. 이렇게 하면 그리드 검색을 수행할 수 있는 플롭이 더 많아집니다. 음, 이것은 기본적으로 커넥터에 대해 더 나쁜 검사 지점을 제공하는 것입니다. 이를 통해 우리는 커넥터가 더 오랜 시간 학습하고 더 높은 플롭 소모량을 통해 실제로 이점을 얻는다는 것을 알 수 있습니다. 하지만 그에게는 해당되지 않는 이야기죠, 그렇죠? 그래서 우리는 상위 nm에 해당하는 12개의 커넥터를 사용하고, 각 커넥터에 대해 성능 차이를 그래프로 나타내 거나, 이 가지치기된 자동 쌍 GST 하에서 모든 작업과 모든 커넥터에 걸쳐 성능 차이를 보고합니다. 이미지 분류 설정에서 보시는 것처럼 저희의 정확도는 11%에 달합니다. 오른쪽? 이미지-텍스트 매칭에서 5점 만점에 최대 11점의 재현율을 기록했습니다. 이는 그리드 검색이 실패 예산 내에서 작동하면서 동시에 충분히 좋은 최소값을 효과적으로 찾아내지 못한다는 것을 보여줍니다 . 오른쪽? 그래서 그의 플롭 예산에서 그리드 검색을 사용하고 유효 비용을 높이기 위해 가지치기를 해도 , 그가 제시하는 만큼 충분히 좋은 커넥터를 얻을 수 없습니다 . 동등한 위치에 놓는다는 의미입니다. 우리가 하는 일은 그리드 검색에 보여주는 데이터 양을 줄이는 것입니다. 왜냐하면 저희 교육 프로그램에서는 바로 이것을 활용하기 때문입니다. 우리는 모델 간의 관계를 활용하여 모델당 필요한 데이터 양을 줄입니다. 따라서 우리는 Himer 에서와 마찬가지로 모델 쌍당 동일한 비율의 데이터를 그리드 검색에 보여줍니다 . 이로 인해 커넥터 검사 지점이 더욱 악화됩니다. HIM이 예측한 커넥터는 제약 조건이 있는 그리드 검색이 예측한 커넥터보다 훨씬 더 나은 성능을 보이며, 제약 조건은 데이터 샘플 측면에서 두 커넥터가 동등한 수준임을 나타냅니다. 자, 이제 두 번째 상황인 다단계 마케팅( MLM)으로 넘어가 보겠습니다. 여기서도 같은 경향을 발견할 수 있습니다. 하지만 이번에는 효율성 향상 폭이 약간 더 낮아 3배입니다. 여기서 주목해야 할 점은 MLM 환경에서 히머 순위가 실제 그리드 검색 순위와 일치하지 않는다는 것입니다. 하지만 성능 면에서는 동등합니다. 왼쪽 차트에서 보시다시피 MSCO 그리드 검색에서 이미지 캡션 생성의 복잡성을 분석한 결과, 참조 이미지 인코더와 이미지를 합치는 데 가장 적합한 모델은 PIA였지만, 다른 분석에서는 Quen이 더 적합한 것으로 나타났습니다. 하지만 퀸과 파피아의 최고 연기는 동일합니다. 오른쪽? 따라서 HIMO는 그리드 검색과 완전히 동일하지는 않지만 성능 면에서는 유사하며, 전체 규모에서는 여전히 3배 더 저렴하다는 것을 알 수 있습니다 . 정규화된 누적 할인율, 즉 정규화된 누적 할인 이득과 스피어만 계수는 다시 한번 문제를 겪습니다. 그 이유는 해당 기능이 그리드 검색과 일치하지 않고, 더 큰 커넥터에 대해서만 일치하기 때문입니다. MLP 1과 MLP 2의 경우를 예로 들어보겠습니다. 따라서 순 효율성 향상은 여전히 ​​크지만, 핵심은 MLM 설정이 HIMA에 전반적으로 덜 적합하다는 것입니다 . 그리고 우리가 여기서 조사하는 것은 이것이 멀티모델 시스템이 생성되는 방식의 기능적 차이 때문일 가능성이 높다는 것입니다. VLM에서는 출력 간 스티칭이 이루어집니다. 이미지 인코더 출력은 텍스트 인코더 출력에, 텍스트 인코더 출력은 다시 이미지 인코더 출력에 연결됩니다. 그리고 이러한 커넥터는 제한된 로컬 임베딩 공간에서 학습됩니다 . 하지만 MLM에서의 융합 연산, 즉 투영된 패치별 이미지 특징을 LLM 임베딩에 연결하는 작업은 출력을 입력에 연결하는 작업입니다. 그래서 저희는 이러한 기능적 형태가 그에게 덜 적합하다고 생각합니다. 왜냐하면 기본적으로 훈련의 질을 떨어뜨리고 실제 경기력을 제대로 반영하지 못하기 때문입니다. 앞서 언급했듯이 소프트웨어 학습과 관련하여 HIMA에서 직면한 주요 문제는 특히 모델이 매우 다를 경우 학습이 민감하다는 점입니다. 그래서 배치 크기를 높여 학습시키면 학습이 다소 불안정해지고, HIMER 학습은 학습률에 매우 민감하다는 것을 알게 되었습니다. 또한, HIMO 학습에 사용하는 모델 풀이 매우 다양하고 기능적으로 매우 이질적일 경우 손실 값이 NA로 나타나는 것을 발견했습니다. 그래서 저희는 EVA, VIT, DIS 풀에 최대 VIT를 포함시켜 NA 손실 값을 찾았습니다. 이는 기본적으로 Max VIT가 어텐션과 머신러닝 피드포워드 모듈에 컨볼루션을 포함하고 있기 때문이며, 기능적으로 VIT와 매우 다르기 때문입니다. 그들의 임베딩 공간은 VIT와 매우 다르며, 이는 HyperET 네트워크가 우리의 UM 모델 미니 배치 설정에서 제대로 작동하기 위해 관계를 학습하는 것을 허용하지 않을 가능성이 높습니다 . 이러한 점이 저희에게 불안정한 하이퍼넷 네트워크 학습 문제를 해결하도록 영감을 주었고, 이를 통해 하이퍼넷 네트워크는 더욱 널리 적용되고 보편화될 수 있을 것입니다. 또한 오디오 클립이나 오디오 LLM과 같은 모델들이 존재하므로 오디오나 비디오와 같은 다양한 모달리티를 실험해 보는 것도 흥미로울 것입니다. 그래서 그 점도 저희에게 매우 매력적입니다. 핵심은 많은 소규모 네트워크를 최대 컴퓨팅 활용률로 학습시키는 대신, 하나의 대규모 네트워크를 학습시켜 소규모 네트워크들을 예측하는 것이 더 효율적인 탐색 방법이라는 점입니다. 핵심 아이디어는 검색에 소요되는 플롭스(flops) 양을 줄이고, 검색을 통해 찾은 최적 모델의 후속 적응 에 사용할 플롭스 양을 최대화하는 것이며, 이것이 바로 그가 해결하고자 하는 문제입니다. 하지만 주목할 만한 점은 그가 VLM 환경에서는 이 문제를 훨씬 더 잘 해결하고 MLM 환경에서는 오히려 더 못 해결한다는 것입니다. 또한, 큰 근육을 훈련시키는 것은 어렵습니다. Adam 베타 2 버전을 조정하는 것이 도움이 되었지만, 이는 여전히 해결되지 않은 문제이며 이에 대한 의견을 매우 기대하고 있습니다. 감사합니다. 네, 그게 전부인 것 같아요 . 그럼 이제 질문으로 넘어가도 될까요? 네 , 궁금한 점이 있으면 언제든지 편하게 질문해주세요. 예.
