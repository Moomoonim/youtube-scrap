# Developing Local AI Copilots with LangChain, NVIDIA NIM, and FAISS | LLM App Development

- 영상 링크: https://www.youtube.com/watch?v=KmEX-1mS8Ps
- 채널: NVIDIA Developer
- 업로드일: 2024-09-17
- 자막 언어: ko
- 단어 수: 약 1781개

---

## 스크립트

안녕하세요 여러분, 제 이름은 메스이고 이번 튜토리얼에 오신 것을 환영합니다. 이 튜토리얼에서는 오픈 소스 도구와 프레임워크를 사용하여 데스크톱이나 노트북에 로컬로 배포할 수 있는 맞춤형 Q&amp;A 채팅 보드 애플리케이션을 구축하는 방법을 안내합니다. 여러분의 자체 데이터를 기반으로 질문에 답변할 수 있는 도우미/보조 기능을 만드는 단계를 보여드리겠습니다. 목표는 GPU 사용량이 적은 장치에 로컬로 배포할 수 있는 채팅 보드 애플리케이션을 만드는 방법을 보여주는 것입니다. 자, 그럼 시작해 볼까요? 이것이 전체적인 워크플로입니다. 저희는 증강 현실 기반 워크플로인 RAG(Retrieved Assessment Generation)를 사용하고 있으며, 이 워크플로는 LLM 모델을 자체 데이터 소스에 연결할 수 있는 기능을 제공합니다. 이 슬라이드에서 볼 수 있듯이, 전체적인 워크 플로는 여러 구성 요소가 통합되어 있습니다. 전체 워크플로는 크게 두 단계로 나눌 수 있습니다. 첫 번째 단계는 PDF, 텍스트, 매뉴얼 등의 기업 데이터를 벡터 임베딩으로 변환하여 벡터 데이터베이스에 저장하는 것입니다. 이 단계는 한 번만 수행하면 되는 오프라인 단계이며, 필요에 따라 추가 작업을 진행할 수 있습니다. 두 번째 단계는 애플리케이션을 실시간으로 실행할 때입니다. 이 슬라이드에서 프롬프트가 표시됩니다. 사용자는 벡터 데이터베이스에서 의미론적 검색을 수행하여 쿼리와 관련된 문서를 찾습니다. 이러한 문서와 프롬프트는 파운데이션 모델로 전달되고, 파운데이션 모델은 사용자에게 응답을 생성합니다. 이제 전체적인 개요를 알았으니, 데이터를 임베딩으로 변환 하고 벡터 데이터베이스에 저장하는 첫 번째 단계로 넘어가 보겠습니다. 이를 위해서는 임베딩 모델과 벡터 데이터베이스 정의, 그리고 청크 크기 및 오버랩과 같은 다른 매개변수도 고려해야 합니다. 청크 크기 매개변수는 문서를 그룹으로 나누는 데 도움이 되며, 오버랩 매개변수는 이러한 청크 간의 오버랩을 정의하는 데 사용됩니다. 임베딩 모델 관점에서 어떤 임베딩 모델을 사용할지 고려해야 하는데, 두 임베딩 모델과 청크 크기, 오버랩과 같은 다른 매개변수의 선택은 GPU 메모리에 크게 좌우됩니다. 마지막으로, 이러한 모든 구성 요소(임베딩 모델, 벡터 데이터베이스)는 오케스트레이션 프레임워크로 연결됩니다. 앞서 언급했듯이 이 과정은 일회성 오프라인 프로세스입니다. 이제 벡터 데이터베이스 설정이 완료되었으므로 다음 단계는 실시간 사용자 통합입니다. 이 단계에서는 벡터 데이터베이스를 사용하는 쿼리 구성 요소를 통해 사용자 쿼리를 벡터 데이터베이스에서 가장 유사한 의미론적 일치 항목을 찾습니다. 벡터 데이터베이스에서 선택된 결과는 쿼리와 함께 LLM 모델로 전달되어 사용자 쿼리에 대한 응답을 생성합니다. 이제 워크플로와 다양한 구성 요소가 어떻게 연결되는지 알았으니 다음 단계는 사용할 도구와 프레임워크를 결정하는 것입니다. 이러한 애플리케이션을 구축하는 데 사용할 수 있는 오픈 소스 및 엔터프라이즈 도구가 많이 있습니다. 예를 들어 오케스트레이션에는 Lang Chain, L-Index, DPET, Glenn 등이 있으며, 벡터 데이터베이스에는 Fire, Milver, OpenSearch, Redis 등이 있습니다. 파운데이션 모델의 경우 Hugging Phase, NVIDIA, Olama와 같은 다양한 서비스에서 호스팅하는 모델을 사용할 수 있습니다. 사용자 인터페이스의 경우 Gradio, Streamlet과 같은 프레임워크를 활용할 수 있습니다. 다시 말하지만, 여기에 나열된 것은 몇 가지 프레임워크일 뿐이며 활용할 수 있는 다른 도구와 프레임워크도 많이 있습니다. 이제 다양한 프레임워크에 대해 알았으니 이 애플리케이션에서는 Lang Chain을 주요 오케스트레이션 도구로 사용하겠습니다. 이 프레임워크는 벡터 데이터베이스의 다양한 모델용 플러그인을 제공하며, Hugging PHAS에 호스팅된 임베딩 모델을 사용하는 방법을 보여드리겠습니다. 이러한 모델을 로컬에 다운로드하고 자체 문서용 임베딩을 생성하는 방법을 보여드리겠습니다. 또한 GP 가속 기능이 있는 FS 벡터 데이터베이스를 사용하여 벡터 임베딩을 저장하는 방법도 보여드리겠습니다. 마지막으로, 파운데이션 모델로는 NIM(Nvidia Inference MicroService)에 호스팅된 최적화된 Lama 3 명령어 모델을 사용합니다. 사용자 상호 작용에는 Gradio를 사용합니다. 이제 사용할 모든 구성 요소를 알았으니 코딩 부분으로 넘어가 보겠습니다. 첫 번째 단계는 자체 데이터 세트를 사용하여 벡터 데이터베이스를 생성하는 것입니다. 이를 위해 여러 종속성을 설치해야 합니다. 앞서 언급했듯이 Lang Chain 오케스트레이션 프레임워크를 사용하므로 이러한 라이브러리가 필요합니다. Hugging PHAS에 호스팅된 Hugging Face 또는 임베딩 모델을 사용할 것입니다. 우리는 허깅 페이스 플러그인 도 필요하고, PDF 데이터를 전달하기 위해 P PDF도 필요합니다. 벡터 임베딩을 저장하기 위해 FS 벡터 데이터베이스를 사용하고 있으며, NVIDIA에서 호스팅하는 Lama 3 명령어 모델을 사용하기 때문에 Lenchain NVIDIA AI 가져오기 라이브러리도 필요합니다. 자, 이제 필요한 종속성이 모두 설치되었는지 확인해 보겠습니다. 대부분 이미 설치되어 있을 겁니다. 이제 모든 종속성이 설치되었으니 필요한 라이브러리를 가져오겠습니다. Lenchain의 문서 로더와 텍스트 분할기를 사용할 것입니다. 이 함수는 문서를 다양한 크기 또는 청크로 분할하는 데 도움이 됩니다. 허깅 페이스에서 호스팅하는 임베딩 모델을 사용하기 때문에 Lenchain의 허깅 페이스 플러그인 과 벡터 데이터베이스용 플러그인도 필요합니다. 실행해 보겠습니다. 좋습니다. 이제 모든 가져오기가 완료되었으므로 다음 단계는 임베딩 모델의 경로를 제공하는 것입니다. 이 노트북에서는 Hugging Face에서 호스팅되는 임베딩 모델을 사용합니다. 이 모델은 녹화 당시 MTE 리더보드에서 5위 또는 6위를 차지했습니다. 문서가 있는 경로와 벡터 데이터베이스를 저장할 이름을 지정해야 합니다. 이제 실행해 보겠습니다. 다음 단계는 몇 가지 전역 변수를 초기화하는 것입니다. 벡터 저장소와 임베딩을 초기화합니다. 이 노트북은 예제이므로 몇 가지 전역 변수를 초기화하지만, 실제 운영 환경에서 사용하려면 이러한 변수들을 클래스로 정의해야 합니다. 하지만 여기서는 사용 편의성 또는 코드 설명의 용이성을 위해 전역 변수로 정의하겠습니다. 자, 다음 단계는 Hugging Face에서 임베딩 모델을 다운로드하는 함수를 작성하는 것입니다. 데스크톱이나 노트북에 GPU가 있는지 확인하고, 인코더와 모델에 대한 여러 인수를 제공합니다. 우리는 이 인수들을 허깅 페이스 플러그인에 전달하여 임베딩을 다운로드하고, 다운로드된 임베딩은 'Tings' 변수에 저장됩니다. 자, 이제 두 함수를 모두 실행해 보겠습니다. 네, 잘 됐습니다. 이것도 잘 됐습니다. 이제 임베딩 모델을 다운로드했으니 다음 단계는 PDF를 처리하는 것입니다. 여기서는 pi PDF 함수를 사용하여 PDF를 로드하고, 각 PDF 페이지를 서로 다른 청크 크기로 분할합니다. 여기서 제어할 수 있는 두 가지 변수는 문서 분할 크기( 청크 크기)와 청크 오버랩입니다. 청크는 문서를 몇 개의 토큰으로 분할할지, 그리고 이전 청크와 현재 청크 사이의 오버랩 정도를 나타냅니다. 이 함수는 한 번에 하나의 PDF를 처리하고, PDF를 페이지 단위로 로드한 다음, 해당 페이지를 사용하여 분할 함수를 실행합니다. 자, 이 함수도 실행해 보겠습니다. 이제 가장 중요한 두 가지 함수를 실행했으니 다음 단계는... 두 파일을 함께 처리하기 위해, 여기서는 문서가 있는 경로를 제공합니다. 그런 다음 각 PDF 파일을 처리하고, 정의한 PDF 처리 함수에 따라 여러 청크 크기로 나눕니다. 임베딩 모델을 다운로드하기 위해 함수를 호출하고, 생성된 모든 청크를 임베딩 모델과 함께 전달합니다. 그리고 F 벡터 데이터베이스를 생성합니다. 생성되면 벡터 데이터베이스를 저장하여 나중에 또는 이 전체 애플리케이션을 실시간으로 실행할 때 사용할 수 있도록 합니다. 자, 이제 이 [음악] 함수를 실행해 보겠습니다. 이제 모든 중요한 함수가 정의되었으니 실행하여 PDF 디렉터리에 있는 PDF 문서에 대한 임베딩을 생성해 보겠습니다. 이 함수를 실행하면 임베딩 모델을 다운로드하는 것을 볼 수 있습니다. 샘플 데이터 세트에 있는 두 개의 PDF 문서를 212개의 청크로 나눕니다. 모델이 다운로드되는 데 시간이 좀 걸릴 것입니다. 용량이 26GB 정도 되는 것 같네요. 다운로드가 완료되면 다시 알려드리겠습니다. 이 작업을 할 때 몇 가지 중요한 사항을 말씀드리자면, 어떤 임베딩 모델을 사용할지는 GPU 메모리 용량에 크게 좌우됩니다. 또한, 문서를 나눌 토큰 크기(청크 크기)도 중요한 요소인데, 이 역시 GPU 메모리 용량에 따라 크게 달라집니다. 따라서 애플리케이션을 설계할 때 이러한 사항들을 고려해야 합니다. 저는 이 노트북에서 48GB 메모리를 가진 A6000 GPU를 사용하고 있습니다. 하지만 A3090이나 A490 GPU를 사용한다면 메모리 용량이 크게 달라지기 때문에 그에 맞춰 모델을 선택해야 합니다. 다운로드가 완료될 때까지 기다렸다가 다시 알려드리겠습니다. 네, 이렇게 임베딩 파일 다운로드가 완료되었습니다. 모델, 음, 샘플 데이터 세트에 있는 두 개의 PDF 파일을 212 크기의 청크로 나누고, 각 청크를 처리하여 임베딩을 생성하고 FireSpector 데이터베이스에 저장했습니다. 출력 메시지가 그다지 명확하지 않아서 이 노트북을 게시할 때 수정하겠습니다. 이제 사용자가 쿼리를 통해 벡터 데이터베이스와 상호 작용하고 응답을 생성할 수 있는 온라인 부분인 두 번째 부분으로 넘어가기 전에, 생성된 임베딩이 제대로 작동하는지 테스트하는 것이 좋습니다. 벡터 데이터베이스와 생성된 임베딩을 테스트하는 몇 가지 간단한 함수를 작성해 보겠습니다. 첫 번째는 벡터 데이터베이스 로드 함수입니다. 이 함수는 F 벡터 데이터베이스를 로드한 다음 GPU 메모리로 이동합니다. 이 함수의 역할은 이게 전부입니다. 실행해 보겠습니다. 다음 단계는 테스트 bdb 함수를 정의하는 것입니다. 이 함수는 벡터 데이터베이스를 'Retriever'라는 이름으로 호출하고, 간단한 질문인 'Dora가 무엇인가요?'를 묻습니다. 벡터 데이터베이스를 만들 때 참고했던 논문 중 하나가 ' Dora' 논문이었기 때문에, 'Dora가 무엇인가요?'라는 질문을 하면 정답이 나와야 합니다. 자, 그럼 실행해 보겠습니다. 먼저 ' Load Vector Database' 함수를 실행해서 데이터베이스를 불러오고, 그다음 'Test Vector Database' 함수를 실행해서 결과를 확인해 보겠습니다. 보시다시피 네 개의 문서가 검색되었습니다. ' Dora가 무엇인가요?'라는 질문은 벡터 임베딩을 생성하는 데 사용된 논문 중 하나인 ' Sora Waited'(Decomposed Low Ranking Adaptation, Text to Image Generation, 그림 10, 그림 9)에 대한 것입니다. 현재는 처음 100자만 출력하고 있어서 전체 텍스트가 나오지는 않지만, 100자 이상을 출력하면 정확한 텍스트가 나올 거라고 생각합니다. 잘 작동하고 있습니다. 맞습니다. 좋습니다. 이제 다음 단계로 넘어가서 두 번째 부분인 사용자 상호 작용 부분을 코딩해 보겠습니다. 사용자가 쿼리를 입력하고 벡터 데이터베이스에서 관련 문서를 검색하여 응답을 생성할 수 있도록 하는 부분입니다. 자, 그럼 시작해 보겠습니다. 벡터 데이터베이스를 만들었으니 다음 단계는 온라인 부분을 코딩하는 것입니다. 사용자가 벡터 데이터베이스와 파운데이션 모델 모두와 상호 작용하여 쿼리에 대한 응답을 얻을 수 있도록 하는 부분입니다. 그럼 시작해 보겠습니다. 온라인 부분을 시작하기 전에 먼저 네임 컨테이너를 로컬에 다운로드하고 배포해야 합니다. 빌드로 이동하면 됩니다. nvidia.com에서 Lama 3를 검색하면 N 컨테이너를 얻을 수 있습니다. Docker 탭으로 이동하면 안내에 따라 컨테이너를 로컬에 배포할 수 있습니다. 컨테이너와 상호 작용하려면 로그인 자격 증명과 API 키를 생성해야 합니다. 이 키는 모델을 로컬에 배포하는 데 필요합니다. 또 하나 중요한 점은, 예를 들어 GPU 메모리가 부족한 경우 Prim 데이터 센터(메모리가 충분한 GPU가 있는 곳)에 모델을 배포할 수 있다는 것입니다. 그런 다음 모델과 상호 작용할 때 IP 주소와 나머지 URL을 포함한 올바른 URL을 제공하면 됩니다. 이렇게 하면 모델이 로컬에 배포되었든 Prim 데이터 센터(프라이빗)에 배포되었든 상관없이 모델과 상호 작용할 수 있습니다. 다음 단계는 프롬프트 템플릿 함수를 만드는 것입니다. 이 함수는 기본적으로 벡터 데이터베이스에서 가져온 컨텍스트와 사용자의 질문을 파운데이션 모델에 입력하여 응답을 생성하는 템플릿을 제공합니다. 사용자 이름 U입니다. 좋습니다. 이제 이 함수를 실행해 보겠습니다. 프롬프트 템플릿 함수를 만들었으니 다음 단계는 채팅 응답 함수를 만드는 것입니다. 이 함수는 Gradio를 통해 호출됩니다. 이 함수는 가장 복잡한 함수 중 하나이므로 여러 부분으로 나누어 살펴보겠습니다. 여기에는 많은 작업이 수행되기 때문입니다. 먼저 통신할 LM 모델의 인스턴스를 생성합니다. 이 경우 로컬에 배포된 Lama 38b Instruct 모델을 사용하므로 URL은 Local Host 88000입니다. 앞서 언급했듯이 로컬 데이터 센터의 Prim에 배포된 모델과 상호 작용하는 경우 해당 URL을 사용하면 됩니다. 다음으로 프롬프트 템플릿이 필요하므로 프롬프트 템플릿 객체도 생성합니다. LM과 프롬프트 템플릿은 모두 문서 체인을 생성하는 create 함수에 전달됩니다. 또한 벡터 저장소와 통신하는 retriever 객체도 생성합니다. 이 세 가지가 모두 create 함수에 전달됩니다. 리트리버, llm, 그리고 프롬프트 객체가 리트리벌 체인으로 전달됩니다. 이 객체는 사용자 쿼리와 상호 작용하여 그에 맞는 응답을 생성하는 주요 객체입니다. 반환되는 값은 현재 질문에 대한 응답과 함께 상호 작용 기록입니다. 자, 이제 이 함수를 실행해 보겠습니다. 이제 채팅 애플리케이션 에 필요한 모든 기능을 갖추었습니다. 다음 단계는 Gradio로 래핑하는 것입니다. 여기에는 애플리케이션 제목이 있습니다. 벡터 데이터베이스를 생성할 수 있도록 문서 경로를 제공합니다. 이는 오프라인 프로세스이지만, 이러한 모든 기능을 UI 또는 애플리케이션을 통해 노출합니다. 오프라인 프로세스(이러한 단계)가 완료되면, 이제 채팅 응답 기능을 통합할 수 있습니다. 이전 블록에서 벡터 데이터베이스와 상호 작용하고 쿼리와 파운데이션 모델을 사용하여 올바른 응답을 얻는 기능을 구현했습니다. 이렇게 해서 모든 것이 완성되었습니다. 자, 이제 전부 실행해 봅시다. 디버깅을 좀 해보니, 온라인 부분이나 상호작용 부분을 코딩하기 전에 노트북을 재시작해 버렸더라고요. 그래서 ' 문서 로드' 함수에서 오류가 발생했던 겁니다. 이제 노트북 전체를 재시작하고 모든 셀을 다시 로드했더니 잘 작동하네요. 이제 괜찮은 것 같습니다. 자, 이제 라디오 앱을 완성해 봅시다. 임베딩 모델과 벡터 데이터베이스를 로드하고 앱을 실행해 보겠습니다. 모든 작업을 완료하고 어떻게 되는지 확인해 보죠. 네, 앱이 실행되고 있습니다. URL을 사용해서 어떻게 작동하는지 확인해 보겠습니다. 자, 앱을 테스트해 봅시다. 노트북 전체를 재시작해서 모든 것을 정리했으니, 파일을 로드해 보겠습니다. 여기에 샘플 PDF 파일 두 개를 업로드하고, 이 파일들을 기반으로 벡터 임베딩 또는 벡터 데이터베이스를 생성할 겁니다. 이 버튼을 클릭하면 '문서 로드' 함수가 활성화됩니다. 벡터 데이터베이스를 만들겠습니다. 자, 문서와 벡터 모두에 대해 V개의 회의가 있습니다. 질문을 입력해 볼까요? y는 무엇일까요? 여기 있습니다. 몇 가지 질문을 더 해 보겠습니다. L보다 어떻게 더 나을까요? 네, 여기 있습니다. Checko 애플리케이션이 로컬에 배포되어 있습니다. 문서, 벡터 데이터베이스, 그리고 상호 작용하는 데 사용하는 기본 모델까지 모두 로컬에 배포되어 있습니다. [음악]
