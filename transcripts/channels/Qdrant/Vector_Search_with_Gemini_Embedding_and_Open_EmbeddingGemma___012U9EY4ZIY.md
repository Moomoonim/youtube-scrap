# Vector Search with Gemini Embedding and Open EmbeddingGemma Models | Google DeepMind | Patrick Löber

- 영상 링크: https://www.youtube.com/watch?v=012U9EY4ZIY
- 채널: Qdrant
- 업로드일: 2025-10-28
- 자막 언어: en
- 단어 수: 약 2341개

---

## 스크립트

Thank you for joining this session. I want to talk about uh Gemini embeddings and the open embedding term model. Uh I'm Patrick. I'm a defil engineer at Google DeepMind. I'm actually based here in Berlin. So that's why I'm super excited that we have an event like this um here. Um yeah, that's my talk and here's the overview. So I want to talk about both models. I'm starting with Gemini embedding, give you a quick model overview, how we trained it, and then some of the controls you have with it to make it especially efficient for vector search like controlling the embedding size and specifying task types. And then also I want to show you how to actually build with it, show you some code examples. And then the second model embedding also give you a quick model overview and show you how to use it. So yeah, let's dive in. Uh first of all, this is the Gemini ecosystem. A rough overview, we have of course our main Gemini series. Right now we have the 2.5 series with 2.5 Pro being our most capable model, then 2.5 flash, which is what we call our workhorse model. Really good for everyday tasks. And then Flash Light, which is even more uh costefficient and super fast. And then we have nano for ondevice stuff. And we actually don't talk a lot about embeddings, but we also have a Gemini embedding model. So that's why I'm excited to talk about this one today here. Um before I go into the Gemini embedding model, actually I want to uh tell you about our mission for the embedding model. So the mission is um to create an omnimodel Gemini derived retrieval oriented representation to complement Gemini's capabilities. So omnimodel um this should in the future work across text, images, videos, audios. This is the mission. Right now we're only at uh text embeddings, but in the next iteration I think you can expect to have a multimodal embedding model. And here's the rough overview. Uh, Gemini Gemini embedding is a powerful model for creating high performance text embeddings. It supports over 100 different languages and it features um, Matrioska representation learning for flexible output dimensions starting at 3,72 and then you can go lower. And right now it's uh holding a top spot on the massive text emitting benchmarks which I'm showing in a moment. Here's the paper. This came out in March. So you can find it on ARI for example and read more about it. Uh I want to give you a high level overview of how we trained this uh roughly in three steps. So we have what we call the prefinetuning step. step one where we take one of the Gemini models and adapt the parameters for encoding using a billion scale web corpus. Then step two is the fine-tuning step. So there we fine-tune this on a diverse mix of high quality task specific data sets. And then step three is what we call a model soup. So we take all these multiple fine-tuned models and then average them to create one final more robust more generalized model which is the Gemini embedding model. And what's also interesting it's trained by Gemini. So we use Gemini a lot in the process for the data generation and data prep-processing um specifically for synthetic data generation and then also data filtering where Gemini acts as an what's called an autoator to remove for example lowquality examples and then um a technique that's called hard negative mining where Gemini uh finds tricky examples to make the model um smarter by training it on these samples and performance. Yeah, this is also a slide with some benchmarks. was mostly evaluated on the um massive multilingual text embedding benchmark and ever since we released it in March up until now it's holding the top spot on the multilingual one on the English one on the code one and then uh we also evaluated it on the X or retrieve and extreme up for example so yeah it is a very performant model you can actually also test this this is open openly available um benchmark that you find on hugging face and this was a screenshot taken yesterday. So Gemini embedding currently rank one. Um what I would actually also want to highlight is you also find the embedding Gemma in here in the top 10 which is really really nice to see. Um and what specifically stands out is if you look at the column with the memory and the number of parameters. So basically the first two rows after the um accuracy then it stands out that this is a super small model but still very efficient. So it's nice to see this here as well. Um yeah um it was also trained with this process. I think today uh it should be the standards. I assume you already um are familiar with this, but basically matriarchal representation learning is a technique that trains a single embedding model to produce a set of nested lower dimensional representations within a single highdimensional vector and then you can choose if you want the full vector or just a a subset of this and you can truncate the outputed embedding and adapt this for your specific use case. Um yeah, this is MRL in action. So this is actually very widely used in practice now with this two pass search approach where you do a first pass over the entire database with a lower dimensional embedding size and kind of uh create a short list of your candidates. And then you do a second pass the reranking where you then take the full embedding size and then do this only on the short listed candidates. Um we actually have a a cookbook about this that walks you through this uh step by step and this was uh contributed by a quadrant engineer. So shout out to Chenny if she's around here. Um yeah this is on the you can google for uh Gemini cookbooks then you also land on the cookbooks repository on GitHub or just take the link here. So there you yeah learn step by step how to do this two pass approach and with a vector uh with a quadrant vector search engine. And now I want to show you quickly how to do this from scratch. I'm a developer. Usually I like to do live demos, but today I'm I'm limiting it to the the code screenshots. Uh but this is basically all you need to do to run Gemini embedding via the Gemini API. The starting point uh for all the Gemini models is AI Studio. Who has not yet heard about AI Studio? Oh, okay. Only a few hands. So it's nice to see if I've asked this question maybe six months ago, it would be more than half the room, but I think we're we're doing a good job talking about this. So AI studio is basically the entry point for developers, the playground to try out all the Gemini models, and this is also the place for you to get the API key. You can uh use this for free. Um you can actually not use the Gemini embedding model there in the playground, but yeah, this is the place to get the API key. And then we have the our Google Chen AAI SDK. So the first party SDKs which we have in Python, JavaScript, uh Java and Go. Here's the Python uh code example. So you do pip install Google Chenai. Set up your client and then simply call client models embed content and use the the model string which is for this one Chi embedding 001. Uh yeah, this is all you need. And now I want to show you some of the configurations you can do. So you can control the embedding size simply by adding one line with this configuration and then specifying the embedding size or the output dimensionality. Um by default it's using the highest ones here. I've listed all the available ones. We usually recommend to use the the one highlighted in blue. So 3,72,50036 or 7 768 which is a nice tradeoff of performance versus accuracy. And the second one you can do is uh specify the task type actually because as I said it was trained on lots of different uh tasks. And yeah depending on what what you want to do you can specify this here and then you get more optimized um relationships in the embedding specifically for your use case. So we have these eight eight use cases. Uh I'm not going to go over them right now, but yeah, it's it's nice to give you this control um when working with the embedding model. And then also uh I want to highlight the batch API which now has support for the embedding models just two weeks ago. So not a lot of people uh knew about this uh capability. Uh the batch API basically lets you process the um the embeddings or calculate the embeddings in an asynchronous way where we take up to 24 hours. So you don't immediately get the result but um you will save 50% actually. So here's the comparison of the standard um uh the standard way there. It costs 15 cents per 1 million input tokens and if you use the batch API it's only 7.5 cents per 1 million input tokens. So I think this is one of the cheapest ones uh you will get right now on the market and yeah is a nice way to know. And then yeah this is basically all I want to show about Jonah embedding. I also want to give a yeah highlight to customers that are already building with this. Uh for me as a developer and developer relations engineer, it's always really nice to see when developers and customers already put this in action. So uh this is Evalaw, which is a platform for document analysis for law for firms. So they have a rack system over millions of specialized texts and they um build with Gemini embeddings. And then this one is a pretty cool one. Maybe you know about it. It's root code. So if you're a developer, maybe you know about this. This is an open- source AI coding assistant and they use Gemini embedding for example to index their code base and also do semantic search over their code. And we have a full blog post here about some more uh customer stories if you're interested in that. And with that I want to move on to embedding CHMA. We've I think just already seen it on on some of the slides from base 10. So, so thank you for the shout out. Uh, I'm a big fan of of the CHMA open models. Um, and embedding CHMA is fairly new. Who has already heard about it? Yeah, quite a few people. Nice. Um, and it's a very lightweight model. So only 303 million parameters adapted from the CHMA 3 architecture and yeah really made for efficient ondevice use. I think in the keynote we've heard that the wave three now might be eight ondevice agents on device agent systems and we also recognized that so yeah we focus a lot of our research also on ondevice models and here we have the embedding channel one so it also gives you the flexible embedding dimensions um and yeah it allows a lot of really cool use cases for mobile first rag systems and vector search systems. So also a quick slide about the performance. So it's currently the highest ranking open multilingual text embedding under 500 million parameters also on the uh massive text embedding benchmark. It's also trained on 100 plus languages. This is always a a paradigm we have for the CHMA model series to also have it really focused on being multilingual and it's actually small enough to run on less than 200 megabytes of RAM with the quantized version which is really nice and yeah this is one code example how you can run it. It's available on on all the typical platforms like um Olama, Llama, CPP. This is the example with hugging face and the sentence transformers library. And uh we also have a cookbook uh if you want to see the the code example that walks you through this step by step to build a rack system with embedding chemma. Uh I'll leave it here for a few seconds if anyone wants to scan. And yeah, we also have a paper about this. This actually came out two days ago. So yeah, very new that it's now also available publicly. Um, feel free to check this out. And this is basically um all the summary that you need to know about these two models. So embedding CHM are made for ondevice mobile first offline apps and then Gemini embedding for large scale serverside cloud apps. Um, embedding GHMA is the open weights one at Gemini embedding available via the hosted API and embedding GHMA bestin-class for its size whereas Gemini embedding is really state-of-the-art if you need the absolute performance and then also comparison of the different available sizes and with that these are basically the links you need to get started to start building right now uh AI studio the entry point to play around with all these models to get your API key. You can also generate starter code uh there and then bring this into your own applications. And then we have these two different uh documentation pages under uh a.google.dev either/gemini API or Gemma and then find the docs there. And yeah, that's it. Thank you for your attention. Um I'm also happy to to connect. I'm around later on also for questions. So yeah, thank you for your time.
