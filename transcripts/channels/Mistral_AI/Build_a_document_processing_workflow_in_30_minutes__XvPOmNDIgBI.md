# Build a document processing workflow in 30 minutes

- 영상 링크: https://www.youtube.com/watch?v=XvPOmNDIgBI
- 채널: Mistral AI
- 업로드일: 2026-06-03
- 자막 언어: ko
- 단어 수: 약 2036개

---

## 스크립트

30분 안에 문서 처리 워크플로우를 구축해 봅시다. 다양한 유형의 의료 문서를 입력받아 Mistral OCR을 사용하여 내용을 분석하고, 고객이 포함하고자 하는 세부 정보에 따라 출력 형식을 지정하는 의료 문서 처리 흐름을 개발해 드리겠습니다. 이러한 패턴은 다양한 사용 사례에서 매우 흔하게 나타납니다. 형식이 제각각인 문서들이 여러 개 있는데 , 그중에는 기록해 두고 싶은 이미지나 도표가 포함된 문서도 있을 수 있습니다. 이런 문서들에서 데이터를 추출하고 분류해야 합니다. 그런 다음 결과를 데이터베이스에 입력하거나, 담당자에게 전달하여 조치를 취하도록 하거나, 양식을 작성하는 등의 단계를 추가할 수 있습니다. 먼저 비정형 문서를 데이터로 변환해야 하는 작업이 많이 있습니다. 저희는 Mistral Workflows를 사용하여 워크플로우를 구축할 예정입니다. Python SDK를 통해 워크플로우 단계를 쉽게 추가하고 , 사람의 개입을 위해 일시 ​​중지하고, 이벤트 기록을 사용하여 중단된 부분부터 다시 시작하는 등 중단 상황을 원활하게 처리하는 내구성 있는 워크플로우를 만들 수 있기 때문입니다. 오늘 다룬 내용에 대해 더 자세한 정보가 필요하시면, 설명란에 있는 링크를 통해 코드, 튜토리얼, 문서, AI 스튜디오 등 필요한 모든 자료를 확인하실 수 있습니다. 자, 시작해 볼까요? 먼저, 다음 UVX 명령어를 사용하여 개발 환경을 설정해 보겠습니다. 이 파일은 워크플로 SDK가 구성되고 도우미 명령이 포함된, 바로 실행 가능한 Python 프로젝트의 기본 구조를 제공합니다. 요청이 오면 간편함을 위해 기본 프로젝트 이름을 선택하고, 단계에 따라 Mistral API 키를 생성합니다. 이제 IDE에서 프로젝트를 열어 코드를 검사하고 변경해 보세요. 다음은 비계 프로젝트의 주요 구성 요소입니다 . Mistral 워크플로용 에이전트 스킬이 포함되어 있어 코딩 에이전트를 사용하여 쉽게 개발할 수 있습니다. 워크플로는 소스 워크플로 디렉터리의 별도 파일에 저장됩니다. start.py 파일에는 명령줄에서 워크플로 실행을 시작할 수 있도록 하는 코드가 포함되어 있습니다. hello.py 파일은 workflows 액티비티, workflows 워크플로 정의 및 workflows 워크플로 진입점 데코레이터를 사용하는 방법을 보여주는 최소한의 예제 워크플로를 제공합니다. dev_worker.py 파일에는 로컬 개발을 위한 워커가 포함되어 있으며, 이 워커는 소스 코드의 변경 사항을 감시하고 필요에 따라 워크플로 워커를 자동으로 다시 시작합니다. 워커는 실제로 코드를 실행하는 프로세스입니다. 작업자는 워크플로 플랫폼에 연결하여 자신이 알고 있는 워크플로 및 활동을 등록하고 작업을 불러옵니다. 플랫폼이 워크플로 단계 또는 활동을 예약하면 사용 가능한 작업자가 이를 선택하여 실행합니다. 이 튜토리얼 후반부에서 워커가 실행되는 모습을 보여드리겠습니다. 메이크파일에는 개발 및 테스트 프로세스를 간소화하는 데 도움이 되는 명령어가 포함되어 있습니다 . 예제 디렉토리에는 살펴볼 만한 다른 워크플로도 있으니, 워크플로의 더 많은 활용 사례를 보려면 살펴보시는 것을 추천합니다. 스캐 폴드 프로젝트에 파이썬 파일 5개를 추가할 예정입니다. 추출 필드는 워크플로 에이전트에서 사용될 유용한 문자열과 상수를 포함하고 있으며, 의료 문서 워크플로는 워크플로 의 핵심 기능을 담고 있습니다. app.py는 워크플로에서 실행할 PDF를 읽어들이는 Streamlit 프런트엔드이며, 부하 분산을 확인하기 위해 여러 워크플로를 병렬로 실행하는 기능 부하 테스트도 포함되어 있습니다. 프로젝트에 필요한 종속성을 설치해 보겠습니다. 먼저 이 특정 프로젝트에 필요한 사항을 반영하기 위해 종속성 요구 사항을 일부 변경하겠습니다. pyproject.toml 파일을 열고 다음 종속성을 업데이트하세요 . 이 패키지는 송장 업로드를 위한 기본적인 프런트엔드를 만들기 위해 Streamlit을 종속성으로 추가하고, PDF 분석을 위해 PyPDF를 추가합니다. UV sync를 호출하여 이러한 종속성을 설치하겠습니다. 문서의 종류가 같더라도 형식은 다양할 수 있습니다. 의료 분야에서는 청구서, 진료 결과, 진료 의뢰서 등 다양한 문서들이 서로 다른 필드 이름을 사용하고 동일한 유형의 정보를 구성하는 방식도 제각기 다릅니다 . 문서에서 추출한 정보를 처리하기 위해 필드 이름을 어떻게 지정할지 정의하겠습니다 . 먼저 source 아래에 shared라는 새 디렉토리를 만들고 extraction fields.py라는 파일을 추가하세요. 추출 필드를 열고 모든 문서 유형에서 공유되는 공통 필드 와 문서 유형에 따라 달라지는 특정 필드를 추가해 보겠습니다 . 모든 문서에 공통적으로 포함되는 항목으로는 환자의 성명, 주소 및 사회보장번호가 있습니다. 구체적인 항목에는 처방전의 경우 의사 이름과 주소, 약품, 날짜 등이 포함되고, 의료비 청구서의 경우 청구 금액과 서비스 내역 등이 포함됩니다. 여기서 우리가 하는 일은 채팅 완료 호출에 전달되는 프롬프트에 필요한 정보를 구축하는 것입니다. 이를 통해 상담원은 문서 유형에 따라 데이터의 이름을 지정하고 식별하는 방법을 알 수 있습니다 . 이것이 바로 워크플로의 마지막 단계에서 우리가 원하는 형식을 얻을 수 있게 해주는 요소입니다. 원하는 필드를 구성했으므로 이제 Mistral Workflows SDK를 사용하여 워크플로를 구축할 수 있습니다 . 워크플로는 데코레이터를 사용하여 워크플로의 구성 요소를 정의합니다. 워크플로를 구축하면서 사용 가능한 데코레이터 몇 가지를 살펴보겠습니다 . workflows 폴더에 medical_doc_workflow.py라는 새 파일을 만들어 보겠습니다 . 문서에 필요한 가져오기 및 설정 항목을 추가하세요. 워크플로에서 활동(Activity)은 실제 계산, API 호출 또는 기타 작업을 수행하는 작업 단위입니다. 이것들은 워크플로우 액티비티 데코레이터를 사용하여 설계되었습니다 . 기본적으로 액티비티는 파이썬 함수 이름을 사용하여 등록됩니다. 우리는 활동에 두 개의 매개변수를 전달할 것입니다 . 시작-종료 시간 초과는 액티비티가 실행을 시작한 순간부터 결과를 반환해야 하는 순간까지 실행될 수 있는 최대 시간을 설정합니다. 활동이 이 시간을 초과하면 종료되고 실패로 처리되며 , 재시도가 발생할 수 있습니다. 타임아웃이 없으면 멈춘 액티비티는 무기한으로 차단되는데, 이는 우리가 원하는 바가 아닙니다. 또한, 활동이 실패했을 때 재시도하는 횟수를 지정하는 재시도 정책의 최대 시도 횟수도 추가할 예정입니다. 사용 가능한 모든 워크플로 매개변수는 문서에서 확인할 수 있습니다. 저희 워크플로는 세 가지 활동으로 구성됩니다. 문서 서명 URL을 가져옵니다. 이 URL은 다음 액티비티로 전달할 수 있습니다. 문서 분류 기능은 Mistral 채팅 자동 완성 기능을 사용하여 문서 유형을 분류하고 , 문서 범주 및 파일 정보를 전달합니다. 환자 정보 추출 기능은 Mistral 채팅 자동 완성 기능을 사용하여 주어진 카테고리에 따라 문서에서 원하는 정보를 추출한 다음 JSON 형식으로 반환합니다 . 이 세 가지 활동을 코드에 추가해 보겠습니다 . 먼저, 서명된 URL을 가져오는 활동을 정의해 보겠습니다. 워크플로 활동 데코레이터를 사용하여 시작부터 종료까지의 시간 제한을 5분으로 설정하고 , 최대 재시도 횟수를 2회로 설정합니다. 우리 함수의 정의는 파일 ID를 입력받은 다음, `getMistralclient` 함수를 사용하여 Mistral 클라이언트를 가져옵니다 . 그런 다음 파일 ID를 전달하여 client.files.get_signed_url_async를 사용하여 서명된 URL을 가져옵니다 . 서명된 URL을 반환하므로 이것이 첫 번째 활동의 결과물이 됩니다. 다음으로, 문서 분류 활동을 정의합니다 . 저희는 Mistral의 채팅 자동 완성 기능을 사용하며, 채팅 구문 분석 비동기 함수는 설정 에서 선언한 문서 분류 클래스를 기반으로 응답 형식을 요청합니다 . 우리는 시스템에 지침을 제공하고 문서 URL을 시스템에 전달하여 처리하도록 합니다. 마지막 활동으로, 카테고리를 기준으로 원하는 출력값을 추출해 보겠습니다. 우리는 Mistral 클라이언트의 인스턴스를 가져오고 , 추출할 필드를 가져온 다음, 다시 비동기 파싱을 사용하여 형식화된 결과를 얻습니다. 활동이 정의되었으므로 이제 실제 워크플로로 넘어가서 해당 활동을 호출할 수 있습니다. 워크플로 SDK의 workflows.workflow.define 데코레이터를 사용하여 클래스를 워크플로로 식별합니다. 워크플로에 이름을 전달하는데, 이 이름은 워크플로를 실행하고 AI Studio에서 워크플로 사용 현황을 확인하는 데 사용됩니다. 이는 상호작용적인 워크플로입니다. 왜냐하면 첫 번째 채팅 완료 단계에서 제출된 문서 유형을 파악할 수 없는 경우, 사람이 개입하는 단계가 포함되어 있기 때문입니다 . 우리는 프런트엔드에서 진행 상황을 확인할 수 있도록 몇 가지 단계별 상태를 선언했습니다 . 또한 사용자가 문서 분류를 제공해야 할 때 사용할 수 있는 수동 카테고리 신호가 있습니다 . 워크플로 단계의 상태를 확인하려면 워크플로 쿼리 기능을 사용합니다. 쿼리는 동기식이며 읽기 전용입니다. 그런 다음 워크플로 신호 데코레이터를 사용하여 사용자가 문서 범주를 선택했는지 여부를 나타냅니다 . 신호는 비동기 통신을 사용하며 워크플로 실행 중 언제든지 전송할 수 있습니다. 시그널과 달리 쿼리는 워크플로우 상태를 변경할 수 있습니다. 이 경우 문서의 카테고리가 사용자 입력에 따라 변경됩니다. 마지막으로 워크플로의 진입점 데코레이터는 워크플로 실행을 정의하는 데 사용됩니다 . [코웃음을 치며] 워크플로가 완료될 때까지 각 활동을 순차적으로 진행하며 단계별로 업데이트를 수행합니다 . 우리는 이러한 할 일 목록 항목을 사용하여 워크플로 진행 상황을 표시하고 포함하고자 하는 세부 정보를 제공합니다 . 먼저 OCR을 실행하도록 첫 번째 단계를 업데이트한 다음 클라이언트에서 제공한 파일 ID를 전달하여 서명된 문서 URL을 가져오는 활동 함수를 호출합니다 . 완료되면 상태를 '완료'로 업데이트하고 결과 설명을 추가합니다. 이제 분류 단계로 넘어가므로 상태를 실행 중으로 설정합니다. 서명된 URL과 파일 이름을 전달하여 문서를 분류합니다. 여기서 신호가 중요한 역할을 합니다. 분류 결과의 신뢰도가 낮으면, 모델이 어떤 문서인지 모르기 때문에 사람이 직접 입력할 때까지 분류 단계를 '사람 입력 대기'로 설정합니다. 우리는 신호가 발생하거나 타임아웃이 발생할 때 까지 워크플로 프로세스를 일시 중지하기 위해 대기 조건 기능을 사용합니다 . 신호가 도착하면, 즉 사람이 문서 유형을 결정하면 워크플로는 계속 진행되어 추출 활동으로 넘어갑니다 . 워크플로는 사람이 읽을 수 있는 텍스트와 우리가 구축할 UI에 표시할 구조화된 페이로드를 모두 반환합니다. 이것이 바로 워크플로우의 기본입니다. 액티비티는 실행 작업을 수행하고, 워크플로는 결과에 따라 무엇이 실행될지 구성합니다 . 이 몇 줄의 코드는 내구성을 기반으로 하므로 액티비티에서 오류나 시간 초과가 발생했을 때 재시도 관리나 올바른 지점을 찾는 것에 대해 걱정할 필요가 없습니다 . 이제 메인 함수를 정의하고 실행하여 코드 작성을 마무리해 보겠습니다. 현재 우리는 워크플로우를 실행하는 데 필요한 모든 것을 갖추고 있습니다. 이 작업은 IDE에서 직접 프로그래밍 방식으로 수행하거나 AI Studio에서 액세스할 수 있습니다 . 하지만, 단계를 볼 수 있는 UI가 있으면 좋으니 Streamlit을 사용하여 간단한 프런트엔드를 만들어 보겠습니다. 진입점 폴더에 app.py라는 새 파일을 만들어 보겠습니다. 저희가 개발 중인 Streamlit 앱은 의료 PDF 파일을 처음부터 끝까지 처리하는, 사람이 직접 개입하는 파이프라인입니다. 이 시스템은 문서를 업로드하고, OCR을 실행하고, 문서 유형을 분류하고, 구조화된 환자 정보를 추출합니다. 또한 워크플로 진행 상황을 실시간으로 표시하고 모델의 신뢰도가 너무 낮을 경우 사용자가 수동으로 범주를 확인할 수 있도록 하여 추출된 데이터가 후속 작업에도 안정적으로 사용될 수 있도록 합니다. 먼저 임포트할 항목과 몇 가지 상수부터 살펴보겠습니다. 다음으로, Streamlit과 워크플로 간의 앱 통합 레이어를 추가하겠습니다. .env 파일에 있는 서버 URL과 API 키를 전달하여 새로운 Mistral 클라이언트를 생성하겠습니다. 이 헬퍼는 일반 Streamlit 코드에서 비동기 워크플로 호출을 실행합니다 . 이는 임시 이벤트 루프를 생성하고 비동기 작업이 완료될 때까지 기다린 다음 루프를 깔끔하게 종료하여 백그라운드 비동기 생성기가 메모리 누수를 일으키 거나 보류 중인 작업 경고를 발생시키지 않도록 합니다. 클라이언트 파일 업로드 동기화 기능을 사용하여 PDF 파일을 Mistral에 업로드하는 기능을 개발합니다 . 이 함수는 응답 ID를 반환하며, 이 ID를 사용하여 파일의 다운로드 URL을 가져옵니다 . 다음으로, 워크플로를 트리거하는 함수를 생성하는데 , 이 함수는 파일 ID, 파일 이름, 그리고 문서 분류의 정확도에 대한 확신도를 나타내는 원하는 신뢰도 임계값을 전달합니다 . 쿼리 워크플로 실행 비동기 함수를 사용하여 단계를 폴링하는 함수를 만듭니다 . 그런 다음, 이 헬퍼를 사용하여 주어진 실행 ID에 대한 전체 워크플로 실행 기록을 가져옵니다 . UI는 단계별 폴링만으로는 충분하지 않고 워크플로 상태 또는 최종 결과 페이로드를 전면적으로 수정해야 할 때 이를 사용합니다. 다음으로, 수동 카테고리 신호에 대한 페이로드를 정의하고 해당 신호를 실행 중인 워크플로로 보내는 함수를 추가합니다. 사용자가 문서 범주를 수동으로 선택할 때 사용되며 , 이를 통해 워크플로는 신뢰도가 낮은 모델 대신 사용자가 승인한 값으로 재개될 수 있습니다 . 실행 결과 함수에서 백필 단계를 추가하여 모든 단계 폴링 데이터가 불완전하거나 더 이상 사용 가능하지 않은 경우 워크플로의 최종 결과 페이로드에서 UI 단계 상태를 업데이트합니다. 반환된 구조화된 결과에서 OCR, 분류 및 환자 정보 필드를 추출하고 해당 단계를 완료된 것으로 표시하여 인터페이스에 완성된 출력을 계속 표시할 수 있도록 합니다 . 자, 이것으로 UI의 워크플로우 관련 코드는 모두 끝났으니 나머지는 아주 간단하게 넘어가겠습니다 . 저희는 Streamlit 인터페이스 에서 첫 페이지 미리보기를 표시하기 위해 PyMuPDF를 로컬에서만 사용하고 있으며 , Mistral은 PDF를 직접 가져옵니다. 버튼 클릭 한 번으로 워크플로우를 시작하고, 각 단계가 진행됨에 따라 화면을 업데이트합니다 . 좋습니다, 프런트엔드와 워크 플로우가 준비되었으니, 바로 실행해 보겠습니다 . 실행과 테스트를 쉽게 할 수 있도록 몇 가지 도우미 명령어를 추가하여 makefile을 업데이트하겠습니다 . 미리 살짝 알려드리 자면, 지금 이 파일을 보고 있는 김에 부하 테스트를 위한 명령어를 하나 추가해 보겠습니다 . Streamlit 및 부하 테스트 명령을 추가하도록 makefile을 업데이트하세요. 워크플로를 실행하려면 몇 가지 의료 문서가 필요합니다. 만약 테스트에 사용할 만한 예시 파일이 없다면, 설명란에 있는 링크에서 예시 파일을 다운로드할 수 있습니다 . 프로젝트 내에 'input doc'이라는 디렉토리를 만들고 그 안에 파일을 넣어주세요. 구현이 완료되면 부하 테스트는 해당 디렉토리에서 파일을 찾게 됩니다. 이제 터미널 창에서 makefile 명령어를 사용하여 워커를 시작하십시오. 이 도우미는 모든 워크플로를 자동으로 검색하고 파일 감시 및 자동 새로 고침 기능을 사용하여 변경 사항이 발생할 때 업데이트되도록 작업자를 시작합니다 . 두 번째 터미널 창을 열고 프런트엔드 작업을 시작하겠습니다. 이렇게 하면 브라우저에서 Streamlit 앱이 열립니다 . 예시 PDF 파일 중 하나를 업로드하고 워크플로 단계를 거치는 과정을 살펴보겠습니다. 그리고 여기 문서에서 파싱된 필드들이 포함된 출력 결과가 있습니다. AI 스튜디오로 이동하면 워크플로를 찾고 각 활동의 입력 과 출력을 확인할 수 있습니다. 워크플로우의 유용한 기능 중 하나는 로드 밸런싱입니다. 워크플로가 트리거되면 사용 가능한 작업자가 자동으로 해당 워크플로를 선택합니다. 저희 [Snorts]는 여러 워커를 동시에 실행할 수 있으며, 많은 워크플로우가 병렬로 실행될 경우 사용 가능한 워커에 자동으로 분산됩니다. 이제 직접 부하 테스트 도구를 만들어 실제로 어떻게 작동하는지 살펴보겠습니다. entrypoints 디렉터리에 load_test.py라는 새 파일을 만드세요 . 부하 테스트 도구의 코드에 대해서는 자세히 설명하지 않겠지만, 스트림릿 프런트엔드와 유사한 워크플로우를 실행한다는 점만 말씀드리겠습니다. 다만, 지정된 횟수만큼 동시에 테스트를 실행한다는 점이 다릅니다. 기본값은 10회입니다. 터미널에서 Ctrl+ C를 눌러 스트림릿 앱을 종료할 수 있습니다. 그런 다음, makefile 명령어 `make load_test`를 사용하여 부하 테스트를 실행할 수 있습니다 . 워크플로의 모든 작업은 비동기식으로 이루어지기 때문에 단일 워커가 실제로 여러 동시 실행을 처리할 수 있습니다. 즉, 이전 워크플로가 완료될 때까지 기다린 후 다음 워크플로를 시작하는 것을 차단하지 않습니다. 따라서 이처럼 간단한 부하 테스트에는 작업자 한 명이면 충분합니다. 대규모 부하 테스트를 실행하려면 터미널 창을 두 개 더 열고 각 창에서 `make start worker` 명령을 실행해 보겠습니다. 다음으로, 이번에는 n을 50으로 설정하여 워크플로우를 50번 실행하도록 make load_test를 다시 실행합니다. 우리는 각 작업자가 필요에 따라 워크플로를 어떻게 수행하고 작업을 어떻게 분배하는지 확인할 수 있습니다. 그게 다입니다 . 피자 배달 받는 시간보다 짧은 시간에 의료 문서 처리 워크플로우를 구축하셨습니다 . 저희 워크플로는 Mistral OCR을 사용하여 파일에서 텍스트를 식별하고 Mistral의 채팅 API를 사용하여 파일에서 관련 데이터를 추출하고 형식을 지정합니다. 여기에서 제공된 스킬을 활용하여 워크플로에 추가하거나 다른 워크플로 예시를 확인할 수 있습니다. 시청해 주셔서 감사합니다 .
