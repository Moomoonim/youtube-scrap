# [ifkakao2021] Klaytn 블록체인 데이터 추출하기

- 영상 링크: https://www.youtube.com/watch?v=Psggi4Y-9IQ
- 채널: kakao tech
- 업로드일: 2026-06-11
- 자막 언어: ko
- 단어 수: 약 1625개

---

## 스크립트

안녕하세요. 저는 그라운드덱스 클레이트 팀에서 개발하고 있는 JK입니다. 제가 오늘 발표드릴 내용은 클레이트 블록체인 데이터 추출하기입니다. 순서는 요구 사항, 초기 구현 방식, 그리고 구현하면서 발생했던 문제점들과이를 어떻게 계산했는지 마지막으로 최종 구현 방식을 순서대로 발표하겠습니다. 먼저 요구 사항입니다. 클레이트 API 서비스 카스는 클레이튼 위에서 블록체인 어플리케이션을 쉽고 빠르게 개발하기 위한 API 서비스입니다. 카스에서는 노드를 통해서 직접적으로 읽기 힘든 데이터나 2차 가공이 필요한 데이터들을 추출하고 분석해서 DB에 저장하고 있습니다. 사용자는 이러한 데이터들을 카스를 통해 쉽게 얻을 수 있는데요. 카스에서 사용할 데이터를 클레이트 네트워크로부터 추출해야 하는 요구 사항이 있었습니다. 추출해야 할 클레튼 블록체인 데이터는 크게 두 가지로 나눌 수 있습니다. 블록 번호나 타임스탬프, 트랜잭션, 리시 등의 블록에 직접적으로 기록되는 데이터가 있고요. 블록에 있는 트랜잭션을 실행함으로써 얻을 수 있는 데이터가 있습니다. 전자를 블록 데이터, 후자를 트레이스 데이터라고 부릅니다. 예를 들어 카스에서는 특정 계좌의 클레이 전송 내역을 조회하는 기능이 있습니다. 클레이가 전송되는 방법에는 두 가지가 있는데요. 그림에서 블록 1번에 계좌 장고를 보면 앨리스와 밥은 각각 100 클레이씩 가지고 있습니다. 블록 2번에서는 앨리스가 밥에게 1클레이를 전송하는 트랜잭션이 담겨 있고 이때 계좌 장고를 보면 앨리스 계좌에서 바베 계좌로 1클레이가 전송된 것을 확인할 수 있습니다. 세 번째 블록에는 앨리스가 컨트랙트 함수를 호출하는 트랜잭션이 담겨 있는데요.이 이 컨트랙트 함수에는 바의 1클레이를 전송하는 로직을 포함하고 있습니다. 이때 계좌 잔고를 보면 바위 계좌의 1클레이가 전송된 것을 보실 수 있습니다. 블로간의 트랜잭션만으로는 컨트랙트에서 밥에게 클레이를 전송했다는 사실을 확인할 수 없습니다. 즉 블록내 데이터만으로는 밥 계좌의 클레이 전송 내역을 모두 확인할 수는 없는 것이지요. 이처럼 한 계좌의 클레이 전송 내역을 추적하기 위해서는 블록넷 트랜잭션으로만 확인할 수는 없고 트랜잭션을 실행하면서 만들어지는 데이터를 수집해야 할 필요가 있습니다. 앨리스가 계좌에서 1클레이를 밥의 계좌로 전송하는 트랜잭션은 블록 데이터에 속하고요. 그리고 앨리스가 컨트랙트 함수를 실행하는 트랜잭션도 블록 데이터에 속합니다. 해당 트랜잭션으로 컨트랙트 코드가 실행되면서 내부적으로 발생하는 클레이 전송은 트레이스 데이터에 속하게 됩니다. 외부에서는 발생할 수 없고 컨트랙트를 실행함으로써 발생하는 트랜잭션을 인터널 트랜잭션이라고 부릅니다. 이러한 데이터를 추출하기 위해서 처음으로 구현한 방식에 대해서 소개해 드리겠습니다. 단순히 노드의 API 요청을 통해서 블록 데이터와 트레이스 데이터를 추출할 수 있는데요. 그림은 각각 블록 데이터를 추출하는 방식과 트레이스 데이터를 추출하는 방식을 나타냅니다. 위의 그림을 보면 노드의 API를 통해서 특정 블록에 대한 블록 데이터를 요청합니다. 요청을 받은 노드는 다른 처리 없이 요청한 데이터를 DB로부터 읽어서 반환하고요. 반면에 아래 그림을 보면 노드의 API를 통해서 특정 블록에 대한 트레이스 데이터를 요청합니다. 노드에서는 해당 블록의 트레이스 데이터를 가지고 있지 않기 때문에 요청받은 블록의 트랜잭션들을 다시 실행하고이 과정에서 얻은 데이터를 반환하게 됩니다. 이때 API로 트레이스 데이터를 요청한 후에 노드 내부에서 트랜잭션이 실행되는 과정을 좀 더 자세히 살펴보겠습니다. 트랜잭션을 실행하는 방식은 다음 세 가지 단계로 나니다. 첫째로 트랜잭션이 실행되기 바로 직전 스테이트를 계산합니다. 여기서 스테이트는 특정 시점의 계좌 잔고나 컨트랙트 내부에 저장하고 있는 데이터 등을 의미합니다. 앨리스가 밥에게 클레이를 전송하는 트랜잭션을 실행하려면 실행 직전 앨리스와 밥의 계좌 전고에 얼마가 있는지를 알아야겠죠. 둘째로 해당 스테이트의 트랜잭션을 적용합니다. 셋째로 트랜잭션을 처리하는 과정에서 수신자가 컨트랙트일 경우 컨트랙트 코드를 실행하게 되는데요. 그림에서 KLVM 클레이트 버추얼 머신은 컴파일된 컨트랙트 코드를 실행할 수 있는 가상 머신입니다. KLV 위해서 컨트랙트 코드가 실행이 되고 이때 실행하면서 얻은 결과를 바탕으로 인터널 트랜잭션 데이터를 추출합니다. 이러한 과정으로 트랜잭션을 실행한 결과 스테이트는 변하게 됩니다. 이러한 API를 통해 데이터 추출에는 문제가 있었는데요. 불필요한 스테이트 계산입니다. 해당 트랜잭션을 적용하기 위해서는 앞서 얘기했듯이 트랜잭션이 적용되기 바로 직전의 스테이트가 필요합니다. 스테이트는 매블록 트랜잭션을 실행한 결과로 항상 존재하지만 노드의 디스크 효율을 위해서 일반적으로는 최근 일부 블록의 스테이트와 128블록 주기로 스테이트를 저장하고 있습니다. 그림은 0번부터 128번까지의 블록과 각 블록의 스테이트 유물를 의미합니다. 그림에서는 블록 0번과 128번 블록에는 스테이트가 존재하고 있는 상태이고요. 나머지 블록에는 스테이트가 없는 상태입니다. 노란색으로 표기된 127번 블록의 트레이스 데이터를 요청받은 상태이고요. 127번 블록의 트랜잭션들을 재실행하기 위해서는 바로 직전 126번 블록에 해당하는 스테이트가 필요한데요.이는 존재하지 않습니다. 해당 스테이트를 계산하기 위해서 과거 트랜잭션들을 다시 실행해야 하는데요. 127번 블록보다 과거 블록 중 가장 최근의 스테이트가 있는 블록부터 시작하게 됩니다. 즉 0번 스테이트에다가 1번 블록부터 126번 블록 안에 존재하는 모든 트랜잭션들을 재실행해야 해당 스테이트를 얻을 수 있습니다. 127번 블록의 데이터를 얻기 위해서 126개 블록을 재처리하는 과정은 매우 비유적이고이를 이용해서 데이터를 추출하는데 걸리는 시간은 굉장히 길었습니다. 데이터 추출 시간이 오래 걸리면 그만큼 유저가 필요한 데이터를 얻을 때까지 오래 걸리게 되므로 성능 개선이 필요했습니다. 성능 개선은 구조적인 변경으로 해결했는데요. 위에 그림은 기존 처리 방식을 나타내고 아래 그림은 변경된 처리 방식을 나타냅니다. 기존 처리 방식을 다시 보면 API를 통한 데이터 요청은 이미 처리한 블록에 대한 요청입니다. 즉 과거 블록에 대한 요청이기 때문에 필요에 따라서 과거 스테이트를 다시 계산해야 하고 이미 처리한 트랜잭션도 다시 실행해야 합니다. 이러한 스테이트 계산과 트랜잭션을 재실행하는 것은 사실 블록을 처음 처리할 때 데이터를 추출할 수 있으면 불필요한 일인데요. 그래서 카스에서 필요한 데이터를 노드에 요청하는 방식이 아니라 아래 그림처럼 노드에서 새로운 블록을 처리할 때 데이터를 실시간으로 추출하는 방식으로 변경했습니다. 실시간으로 추출한 데이터를 카스에서 사용할 수 있도록 어딘가에 저장해 놓았어야 했는데요. 그래서 데이터는 메시지 Q에 적지하는 구조로 변경했고요. 메시지 Q로는 카프카를 사용하게 되었습니다. 카스에서는이 카프카를 보고 있고 지속적으로 들어오는 데이터를 처리합니다. 이렇게 하면 실시간으로 블록을 처리하기 때문에 노드에서는 과거 스테이트를 다시 계산할 필요가 없고 트랜잭션 또한 1회 실행해서 데이터를 추출하는 것이 가능합니다. 카프카를 사용하게 되면서 데이터 크기에 따른 성능 저하가 있을 수 있다고 판단했습니다. 아파치 카프카는 실시간으로 데이터 스트림을 생산 및 소비할 수 있는 분산 데이터 스트리밍 플랫폼입니다. 카프카는 작은 메시지에 대해서 낮은 레이턴시 때문에 많은 메시지를 빠르게 처리할 수 있습니다. 하지만 메시지 크기에 따라서 처리량이 급격하게 줄어듭니다. 카프카는 기본적으로 하나의 메시지 크기를 1MB로 제한하고 있고요. 제한을 변경할 수 있지만 하나의 메시지 크기가 10MB 이상이면 카프카 상승 저하가 눈에 띄게 나타납니다. 그래서 메시지 Q에 들어갈 하나의 메시지 최대 크기를 계산해 볼 필요가 있었습니다. 클레이트에서 하나의 트랜잭션 최대 크기는 32k바이고 네트워크 최대 성능이 대략 4000ttps인데요. 이론상으로는 128MB의 블록이 생성할 수 있으므로 노드에서 데이터를 생성할 때 블록 단위로 데이터를 생성하게 되는데 이렇게 크기가 큰 데이터에 대한 고민이 필요했습니다. 데이터를 쪼개서 생산하고 소비하는 쪽에서 재조립해서 사용하는 방식으로 이런 문제를 해결했는데요. 위에 그림을 보면 기존에는 추출한 데이터를 데이터 크기와 상관없이 그대로 생산하고 소비하는 방식이었는데요. 아래 그림과 같이 데이터를 크기에 따라서 일정 크기로 쪼개서 생산하고이를 소비해서 조립하는 방식으로 구조를 업데이트했습니다. 이때 노드에서 메시지를 생산하는 방식과 카스에서 조립하는 방식에 대해서 간단히 살펴보도록 하겠습니다. 우선 카프카 메시지에는 데이터를 다시 조립하기 위한 헤더 정보가 들어가는데요. 데이터를 일정 크기 단위로 잘라서 데이터 조각을 만들고요. 잘라진 데이터 조각과 해당 데이터의 블록번호, 데이터 조각 번호, 그리고 총 데이터 조각의 개수를 메시지 헤더에 함께 담아서 생산합니다. 이때 메시지는 상황에 따라서 다른 블록들이 데이터 조각들과 섞일 수 있는 상황이 발생합니다. 이러한 상황은 카스에서 메시지를 소비할 때 버퍼를 활용해서 데이터를 조립하고 처리하는 방식으로 구현하였습니다. 각각의 버퍼에는 블록번호 기준으로 조각들을 모으고요. 모든 조각들이 모여서 처리할 수 있는 상태가 되면 메시지 조각들을 조립해서 처리하게 됩니다. 예를 들어 노란색 박스을 보시면 158번 블록은 총 세 개의 조각이 존재하는데 1번부터 3번까지 모든 데이터 조각이 있으므로 해당 메시지를 처리할 수 있게 됩니다. 이러한 데이터 분절 및 조합 방식에는 장단점이 있는데요. 먼저 메시지의 최대 크기가 정해져 있기 때문에 메시지 크기에 따른 카프카 성능 저화를 막을 수 있습니다. 그리고 큰 데이터를 처리하기 위해서 데이터를 쪼개지 않고 카프카 성능에 영향을 미치지 않으려면 큰 데이터를 외브 스토리지를 사용해서 전달하는 방식도 있을 수 있는데요. 이때 외부 스토리지에 대한 추가 코스트나 성능이 카프카 이외에 다른 리소스에 영향을 받을 수 있습니다. 하지만 메시지를 분절해서 처리하면 카프카 클러스터 이외에 사용하는 리소스가 없기 때문에 추가 비용이 들지 않고 카프카 성능을 온전히 활용할 수 있다는 장점이 있습니다. 반면 앞서 간단하게 소개하긴 했지만 실제로 메시지를 조립하는 방식은 굉장히 복잡했습니다. 메시지 조각이 중복으로 들어가거나 노드가 재실행되면서 메시지 조각들 중 절반만 들어간다든지 버퍼에 처리하지 못한 메시지가 있을 경우에 어떻게 처리할지 등등 메시지 조립 시나리오에 따라서 구현의 복잡도가 높은 단점이 있었고요. 그리고 메시지를 조립하기 위해서 사용된 버퍼 때문에 컨슈머 쪽 메모리 사용량이 증가하는 단점이 있었습니다. 마지막으로는 느린 자바스크립트 트레이서인데요. 우선 변경된 구조에서도 성능적으로 이슈가 여전히 있었습니다. 개선된 구조에서는 트랜잭션 실행 직전 스테이트를 계산하기 위해서 많은 스테이트를 다시 계산할 필요는 없어졌고 트랜잭션 실행 또한 1회에 이루어지는데요. 마지막 단계인 트레이스 데이터를 추출하는 방식에서 성능 저화가 있었습니다. 트랜잭션의 수신자가 컨트랙트일 경우 클레튼 버추얼 머신 위에서 코드가 실행되고 필요한 데이터를 추출한다고 말씀드렸는데요. 이때 사용되는 것이 트레이서입니다. 우선 트레이서를 이용한 데이터 추출 방식에 대해서 설명드리겠습니다. 기본적으로 KLVM을 통해서 컨트랙트 코드를 실행한다는 것은 코드상 이미 정의되어 있는 1년의 오퍼레이션들을 순서대로 수행하는 것과 같습니다. KVM을 실행할 때 트레이서를 주입할 수 있는데요. 트레이서를 주입하는 경우에는 매 오퍼레이션이 수행될 때마다 VM의 상태를 캡처해서 주입한 트레이서에서 정의된 방식으로 필요한 데이터를 추출합니다. 기존 자바스크립트 트레이서는 KLVM 이외의 데이터 수집을 위한 자바스크립트 버 머신을 추가적으로 사용하고 있고요. 트랜잭션을 처리하는 오퍼레이션 이외에 자바스크립트 버트럴 머신에서 추가적으로 생기는 동작의 오버헤드가 굉장히 컸습니다. 자바스크립트 트레이서를 사용한 방식 대신 고원로 트레이서를 개발하여 주입하는 방식으로 구현되었고요.고 고 트레이서는 기존 자바스크립트 트레이서와 데이터를 추출하는 방식은 같지만 자바스크립트 버추 머신을 사용하지 않으므로 추가적인 오버헤드를 줄여서 데이터를 빠르고 가볍게 처리하도록 변경하였습니다. 그래프는 자바스크립트 기반 트레이서로 트레이스 도중에 CPU 프로파일링을 한 결과를 나타내고 있고요. 루트를 시작으로 함수 호출 수택과 명명된 함수가 실행하데 걸리는 시간을 가로 길이로 상대적으로 나타냅니다. 우리가 관심 있는 부분은 트레이스 TX라는 함수를 실행했을 때인데요. 실제로 트랜잭션을 처리하는 어플라이 메시지 함수 실행 시간보다 자바스크립트 트레이서와 관련된 함수들 겟리 result트나 뉴라는 함수 실행 시간이 매우 큰 것을 보여줍니다.이를 이를 통해서 자바스크립트 트레이서 성능 이슈에 힌트를 얻었고요. 이후고 트레이서를 개발 후 성능에 대한 비교 그래프이고요.고 트레이서와 기존 자바스크립트 트레이서 그리고 트레이스가 없을 때를 나누어서 네트워크 동기와 테스트를 진행했습니다. 그래프는 클레튼 메인 내신 사이프레스 네트워크의 데이터를 동교화하고 있을 때 최신 블록 넘버를 나타내는 그래프입니다. 블록들 중 트랜잭션이 비교적 많은 블록들을 처리할 때를 샘플링했고 이때 평균적으로 블록을 처리하는 속도를 계산해 보았는데요. 트레이스가 없을 때 초당 25개,고 트레이서를 사용할 경우에는 초당 23개, 그리고 자바스크립트 트레이서를 사용할 경우에는 초당 세 개의 블록만 처리하는 것을 확인했습니다. 해당 구간에서는 기존 자바스크립트 트레이서보다고 트레이서가 성능적으로 약 여덟 배 향상된 것을 확인하였습니다. 마지막으로 최종 구현 모습입니다. 블록을 처리할 때 클레튼 버추얼 머신에 자바스크립트 트레이서 대신고 트레이서를 주입하는 것으로 업데이트를 했고요. 구조적으로는 API를 통한 요청 방식이 아닌 블록을 실시간으로 처리하면서 데이터를 추출할 수 있도록 변경했습니다. 그리고 데이터가 클 경우에는 데이터를 쪼개서 카프카에 넣고요. 데이터를 읽을 때 쪼개진 데이터를 조합해서 처리할 수 있도록 변경했습니다. 그래서 현재 이러한 모습에 블록체인 데이터를 추출하고 있습니다. 지금까지 클레이트 블록체인 데이터를 추출하면서 발생했던 문제점들과이를 어떻게 개선했는지에 대해서 저의 경험을 공유드렸는데요. 카스에서는 이러한 데이터를 활용한 API뿐만 아니라 유저의 편의를 생각하는 다른 다양한 기능의 API를 제공하고 있습니다. 혹시 클레이트 블록체인 어플리케이션을 개발하고 계신가요? 그렇다면 카스를 사용해 보시는게 어떨까요? 제 발표는 여기까지이고 시청해 주셔서 감사합니다. 입니다.
