# How to Analyze Graphs of Functions in Precalculus Ft. The Math Sorcerer

- 영상 링크: https://www.youtube.com/watch?v=QjH6vS8TTos
- 채널: Chegg
- 업로드일: 2025-02-13
- 자막 언어: ko
- 단어 수: 약 683개

---

## 스크립트

안녕하세요 여러분, 수학 마법사 Che입니다. 이번 영상에서는 함수의 그래프 분석에 대해 알아보겠습니다. 먼저 함수의 그래프를 보고 그래프에 대한 몇 가지 질문에 답해 보겠습니다. 예를 들어, F( 0)을 구한다고 가정해 봅시다. F(0)은 x가 0일 때의 y값입니다. 그래프를 보면 x가 0일 때 y 값은 2입니다. 따라서 F(0)은 2입니다. 그럼 F(4)는 어떨까요? F( 4)는 x 가 4일 때의 y값입니다. 그래프를 보면 y값이 0인 것을 알 수 있습니다. 마찬가지로 F(-4)도 x가 -4일 때의 y값으로, 역시 0입니다. 그럼 정의역은 어떻게 될까요? 정의역은 함수에 대입할 수 있는 모든 x값의 집합입니다. 이해되시죠? 하지만 우리는 이 그래프가 함수 f(x)의 그래프라는 것만 알고 있고, 공식은 나와 있지 않습니다. 따라서 그래프를 이용하여 정의역을 구해야 합니다. 정의역을 구하려면 모든 x값을 살펴보면 됩니다. x 값을 선택하면 y 값이 나올지 스스로에게 물어보세요. 예를 들어, 여기 빨간 점에 해당하는 x 값을 선택하면 여기에 y 값이 나옵니다. 따라서 x축의 이 빨간 점은 정의역에 속하는 숫자입니다. 마찬가지로, 여기 있는 값을 선택하면 y 값이 나옵니다. 따라서 x축의 이 빨간 점은 정의역에 속하는 숫자입니다. 하지만 오른쪽에 있는 값을 선택하면 y 값이 나오지 않습니다. 따라서 x축의 이 빨간 점은 정의역에 속하지 않는 숫자입니다. 정의역은 y 값을 생성하는 모든 x 값의 집합입니다. 여기를 보면 가장 작은 x 값은 -4이고, 이 값 을 포함해야 합니다. 왜냐하면 이 값에서 y 값이 0이 나오기 때문입니다. 따라서 정의역은 -4에서 4까지의 괄호입니다. 정의역은 y 값을 생성하는 모든 x 값의 집합입니다. 치역은 가능한 모든 y 값의 집합입니다. 이 경우 가장 작은 y 값은 0이므로 괄호를 사용하여 포함합니다. 가장 큰 y 값은 2입니다. 정의역을 정의하려면 괄호를 사용해야 합니다. 정의역은 모든 x 값 중에서 y 값을 결정하는 모든 값이고, 치역은 모든 y 값입니다. 자, 다른 예제를 하나 더 살펴보겠습니다. 여기 함수 f(x)의 그래프가 있습니다. 몇 가지 질문에 답해 보겠습니다. 먼저 함수가 증가하는 구간을 찾아보겠습니다. 함수가 증가하는 구간을 찾는 것이죠. 이 문제를 풀려면 그래프를 왼쪽에서 오른쪽으로 보면 됩니다. 그래프를 왼쪽에서 오른쪽으로 따라가다 보면 x 값이 증가하는 구간이 있고, 여기서는 감소하고, 여기서는 증가하는 구간이 있습니다. 왼쪽 또는 오른쪽으로 생각하면, -∞부터 4까지는 y 값이 증가하므로 괄호를 사용해야 합니다. -∞에서 -4까지는 y 값이 증가하고, -4에서 1까지는 감소하므로 우리가 원하는 결과가 아닙니다. 1부터 무한대까지 Y 값이 커지므로, 1부터 무한대까지 두 집합을 합집합하면 안 됩니다. 왜냐하면 대부분의 문제에서 함수가 증가하는 구간을 묻는 경우가 많은데, 합집합을 하면 구간이 아니게 되기 때문입니다. 따라서 - 무한대에서 4까지는 점점 커지므로 증가하는 구간이고, 1부터 무한대까지는 증가하는 구간입니다. 이것들은 X값입니다. 자, 이것들은 증가를 나타내는 데 사용하는 X값입니다. 이제 감소하는 구간을 살펴보겠습니다. 여기에서 감소하는 구간을 보여드리겠습니다. 다시 말하지만, 그래프를 왼쪽에서 오른쪽으로 봐야 합니다. 점점 커지다가 점점 작아지는 것을 볼 수 있습니다. X값이 -4부터 시작하므로 -4에서 1까지 계속 감소합니다. 여기 파란색으로 표시해 두었으니 -4에서 1까지가 감소 구간입니다. 다시 지우겠습니다. 이제 다른 곳에서는 감소하지 않습니다. 왼쪽에서 감소한다고 생각할 수도 있지만, 그것은 틀린 생각입니다. 그래프를 왼쪽과 오른쪽 관점에서 봐야 합니다. 예를 들어, 어떤 사람이 그래프 위를 걷는다고 생각해 보세요. 위로 올라가고 있으니 증가하는 거죠. 작은 사람이라고 생각하면 이해하기 쉬울 거예요. 위로 올라가면 증가하는 거고, 아래로 내려가면 감소하는 거죠. 자, 이제 극대값 또는 상대적 극대값에 대해 이야기해 볼까요? 극대값은 주변 점들에 비해 가장 큰 y값을 말합니다. 여기 45에서 5가 주변 점들보다 크죠. 그래서 5가 상대적 극대값입니다. 5는 x4에서 발생합니다. 순서쌍으로 나타내면 -4, 5가 되죠. 극대값 문제를 풀 때는 표현에 특히 주의해야 합니다. 어떤 온라인 숙제 시스템은 순서쌍을 요구하고, 어떤 시스템은 x 좌표만 요구합니다. 극대값을 묻는다면 엄밀히 말하면 답은 y값이지만, 극대값이 발생하는 위치를 묻는다면 순서쌍으로 답할 수도 있고, x 좌표(-4)라고 답할 수도 있습니다. 중요한 건 극대값을 찾는 과정에서 순서쌍을 사용해야 한다는 점입니다. 그리고 최솟값은 y값입니다. 극대값은 주변 점들에 비해 가장 큰 y값입니다. 이제 극소값을 찾아보겠습니다. 극소값 또는 상대적 극소값은 주변 점들에 비해 가장 작은 y값입니다. 여기에서 -2는 주변 점들에 비해 가장 작은 y값입니다. -2가 극소값이며, 순서쌍 1-2로 나타낼 수 있는 지점에서 발생합니다. 왼쪽에서 오른쪽으로 갈수록 증가합니다. 다시 한번 보여드리겠습니다. 파란색 영역으로 돌아가서 그래프를 보면 - 무한대에서 4까지, 그리고 1에서 무한대까지 증가하고 있습니다. 화면에서 파란색 영역이 바로 증가하는 부분입니다. 파란색 영역에서는 y값이 커지는 것을 볼 수 있습니다. 이제 감소하는 영역을 빨간색으로 표시해 보겠습니다. 여기에서 y 값이 감소하는 x값을 볼 수 있습니다. 5는 주변 점들에 비해 가장 큰 y값이기 때문에 상대적 최댓값입니다. 그리고 -2는 주변 y값들에 비해 가장 작은 y값이기 때문에 상대적 최솟값입니다. 가장 작은 y 값입니다. 사람들은 이것을 때때로 극대값이라고도 부릅니다. 꽤 흥미로운 문제죠. 이 영상이 도움이 되었기를 바랍니다. 이 영상이 마음에 드셨다면 chag의 다른 영상들도 확인해 보세요. 행운을 빕니다!
