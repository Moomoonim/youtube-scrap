# 1,001 Ways to Accelerate Python with CUDA Kernels | NVIDIA GTC 2025

- 영상 링크: https://www.youtube.com/watch?v=_XW6Yu6VBQE
- 채널: NVIDIA Developer
- 업로드일: 2025-04-16
- 자막 언어: ko
- 단어 수: 약 4043개

---

## 스크립트

모두 감사합니다. 참석해 주셔서 감사합니다. 안녕하세요, 제 이름은 레오 팡이고, CD 파이썬 기술 리더입니다. 오늘은 파이썬으로 좋은 커널을 작성하는 몇 가지 방법을 보여드리겠습니다. 네, 보시다시피 이것은 저희가 올해 GTC에서 진행할 CUDA와 파이썬 관련 강연 시리즈입니다 . 이번 강연은 CUDA와 파이썬에 관한 세 번째 강연으로, 특히 커널 작성에 초점을 맞추고 있습니다 . 음, 그러니까 이제쯤이면 여러분 대부분이 Kodap Python ST의 모든 것을 다룬 이 스택 다이어그램을 보셨을 거라고 생각합니다. 런타임, 컴파일러, 커널 작성 기능부터 장치 라이브러리, 프레임워크, DSL에 이르기까지 모든 것을 보여줍니다. 그러니까 이 말은 우리가 파이썬 생태계 전체를 진정으로 지원하고 싶다는 뜻이고, 특히 파이썬 사용자와 개발자에게 있어서는 이 차트에 있는 모든 것들을 일반적인 파이썬 패키지처럼 자유롭게 조합해서 사용할 수 있어야 한다는 의미입니다. 여러 파이썬 패키지를 가져와서 사용자 코드에서 함께 사용할 수 있어야 한다는 거죠. 그래서 이것은 우리가 구축하고 있는 중요한 임무이며, 특히 이번 발표에서는 커널을 다양한 방식으로 개발하는 방법을 아래에서부터 차근차근 설명해 드리겠습니다. 이를 위해 교육적인 예시로 세그멘테이션 축소를 사용하겠습니다. 그래서 이 문제는, 서로 다른 방식으로 동일한 문제를 해결하는 커널을 작성하는 방법을 보여주는 것입니다. 문제 내용은 다음과 같습니다. 저희는 1차원 버퍼에 다양한 값들을 저장하려고 합니다. 이 버퍼에는 빨강, 파랑, 노랑 등 여러 가지 색깔의 세그먼트가 있습니다. 그리고 각 세그먼트에 대해 값을 줄여나가려고 합니다. 예를 들어 빨강 세그먼트의 값을 모두 더하면 10이 되고, 파랑 세그먼트의 값을 모두 더하면 26이 되는 식입니다 . 이것이 바로 문제 제기입니다. 올해 저희가 진행하는 교육 강연이나 새로운 자료들에 대해 아직 들어보지 못하셨다면, CUDA를 어떻게 사용하는지 궁금하셨을 수도 있습니다. 온라인 자료를 참고하여 C++로 CUDA 커널을 직접 작성해 보려고 하실 수도 있겠지만, 오늘날에는 그것만이 유일한 방법은 아닙니다. 만약 직접 커널을 작성한다면, 먼저 필요한 파일을 포함하고, 입력/출력 벡터를 채우고, 문제 크기를 정의하고, 디바이스에 입력/출력 버퍼를 준비한 다음, 커널을 실행하여 연산을 수행하고, 커널 실행 후 동기화를 거쳐 디바이스에서 호스트로 데이터를 다시 복사하여 후 처리나 검증 등을 수행하게 됩니다. 그러니까 결국 핵심 질문은 이 커널을 어떻게 작성하느냐는 거죠? 음, 그러니까 이 일을 하는 방법은 여러 가지가 있어요 . 음, 이것은 하나의 세그먼트를 블록에 할당하는 방법 중 하나입니다. 따라서 세그먼트가 3개라면 3개의 블록을 실행합니다 . 우리는 백만 개의 세그먼트를 보유하고 백만 개의 블록을 출시하는 등 다양한 작업을 진행합니다. 그리고 최대한 범용적으로 만들기 위해, 저는 커널을 C++ 템플릿 커널로 작성합니다 . 그래서 입력 데이터 유형, 오프셋 유형, 블록 크기, 스레드당 항목 수 등을 템플릿화할 수 있고, 이를 템플릿 매개변수로 인코딩하여 컴파일 시간에 지정하고 컴파일러가 최적화를 지원하는 데 사용할 수 있습니다. 편의상 이 발표 전체에서 세그먼트 크기가 우리가 지정한 블록 크기의 배수라고 가정하겠습니다 . 음, 그래서 우리가 하는 일은 블록 내의 모든 스레드가 서로 통신하고 나중에 함께 작업할 수 있도록 공유 메모리를 할당하는 것입니다. 그런 다음 나중에 필요한 인덱스를 지정하고 계산합니다. 이 인덱스는 이 블록 내에서 스레드로서 내 위치 와 세그먼트 배치 내에서 세그먼트로서 내 위치를 나타냅니다. 음, 이 두 숫자를 이용해서 오프셋을 계산하고 각 스레드에 대해 지정된 데이터를 레지스터에 로드한 다음, 부분 집계를 수행합니다. 세그먼트 크기가 블록 크기보다 훨씬 클 수 있다고 가정하기 때문입니다. 그래서 우리는 이 블록으로 전체 세그먼트를 덮어야 하고, 그 후에 각 스레드는 자체적인 부분 집합체를 갖게 됩니다. 그다음에는 해당 값들을 공유 메모리에 기록하고 블록 내의 모든 스레드에 걸쳐 동기화하려고 합니다. 마지막으로 공유 메모리 데이터를 사용하여 부분 축소를 수행합니다 . 기본적으로 이는 블록의 절반이 자신이 소유한 데이터와 블록의 나머지 절반이 소유한 데이터를 사용하여 계산을 수행하고 결과를 공유 메모리에 기록하고, 블록의 4분의 1이 같은 작업을 수행하고, 블록의 8분의 1이 같은 작업을 수행한다는 의미입니다. 결국 최종적으로 필요한 데이터를 생성하는 스레드는 하나만 남게 되고, 우리는 그 데이터를 전역 메모리에 다시 기록합니다 . 오른쪽? 그럼 이 도표는 나중에 다시 살펴보겠습니다. 이것은 교육적인 예시입니다. 그것은 문제를 해결합니다. 어, 잘 작동하고, 성능 관련해서 얘기하자면 성능도 나쁘지 않아요. 어, 앞서 이 세션에서 조지가 발표했던 내용을 참고할 수 있습니다. 오늘 오전에 말씀드렸듯이, 이러한 커널을 작성하려면 CUDA 프로그래밍 모델, CUDA 스레드 및 메모리 계층 구조, GPU 아키텍처 등 여러 가지를 알아야 합니다. 그래야 성능이 향상되고, 또한 알고리즘을 GPU 아키텍처 위에 구현하여 커널 내부에서 연산을 수행할 수 있어야 합니다. 그리고 마지막으로, 단순한 장난감 코드를 넘어 규모가 커지면 C++ 컴파일 워크플로를 알아야 프로젝트를 빌드 하고 사용자에게 배포하는 방법을 알 수 있습니다 . 배울 게 정말 많네요. 음, 그런데 제가 강조하고 싶은 한 가지는 이러한 것들의 성능 세부 사항에 대해서는 자세히 다루지 않겠다는 것입니다. 왜냐하면 그 부분은 제 동료들이 이미 여러 훌륭한 발표에서 다뤘기 때문입니다. 하지만 CRUD 프로그래밍에서 배우는 것들은 다른 언어에도 적용할 수 있는 것들이 있습니다 . 그러니까 C++ 강연에서 배우든 파이썬 강연에서 배우든 상관없어요. 중요한 건 배우고 작동 방식을 이해하는 거고, 그런 것들은 서로 적용될 수 있다는 거죠, 그렇죠? 그래서 만약 당신이 C++ 개발자이고, 제가 보여드린 것과 같은 C++ 코드를 작성하는 데 익숙하다면 , 정말 좋습니다. 그리고 오늘 오전에 제 동료인 조지와 브라이스가 발표한 내용보다 더 많은 C++ 관련 강연이 준비되어 있습니다. 혹시 참석하지 못하신 분들은 녹화본을 확인해 보셔도 좋습니다. 오른쪽. 그래서 우리가 원하는 것은 생산성을 유지하고 파이썬을 계속 사용하는 것입니다. 그렇다면 파이썬을 계속 사용하는 것이 어떻게 개발 과정을 더 쉽게 만들어 줄까요? 저는 여기서 일반적인 파이썬 프로젝트의 워크플로우를 설명하려고 합니다. 이 프로젝트는 사용자가 GPU에서 커널을 실행할 수 있도록 하는 것으로, 리눅스나 윈도우에 따라 GCC 또는 MCBC와 같은 호스트 컴파일러에 의존하는 MVCC 메커니즘에 의존하지 않기 위해 파이썬 측에서 구현됩니다 . 사용자 코드, 즉 사용자 커널을 GPU에서 실행할 수 있는 기계어로 컴파일하는 다양한 방법이 필요하며, 이러한 블랙 박스들은 모두 그러한 메커니즘을 나타냅니다. 이 블랙박스는 AVN 관련 스택, ML 관련 컴파일러 패스 및 최적화, 또는 NVRTC, NVGLink 및 NVN을 포함한 자체 컴파일러 라이브러리 등 다양한 프로젝트에서 각기 다른 방식으로 결정되고 구현됩니다. 하지만 중요한 점은 이러한 모든 온라인 컴파일이나 G 컴파일 방식 덕분에 더 이상 C++ 호스트 컴파일러가 필요하지 않다는 것입니다 . 그리고 이것이 파이썬 프로젝트를 배포하는 데 중요한 이유입니다. 왜냐하면 사용자에게 GCC나 MCVC를 미리 설치하라고 요구하고 싶지 않을 것이기 때문입니다. 이러한 라이브러리는 pip나 명령어로 설치할 수 없을 뿐더러, 설치하고 작업 환경을 설정하는 데 상당한 시간과 노력이 필요합니다 . 그래서 이 문제를 좀 더 자세히 설명하자면, C++ 측면에서 우리가 방금 작성한 커널이 있고, 라이브러리 개발자로서 여러 데이터 유형을 지원하고 싶다고 가정해 보겠습니다. 방금 말씀드렸듯이 저희 커널은 세그먼트에 저장된 double, float 또는 integer 값을 지원할 수 있을 만큼 범용적입니다. 오른쪽? 그래서 오른쪽에 있는 것처럼 컨테이너 유형에 따라 다른 데이터 유형으로 디스패칭을 시도하는 디스패처를 작성할 수 있습니다 . 하지만 그 의미는 이러한 모든 유형이 프로젝트의 컴파일 시간 또는 빌드 시간에 미리 인스턴스화되어야 한다는 것이죠? 파이썬에서 하는 것과 비교해 보면, 일반적으로 필요한 것만 인스턴스화하고 필요한 것만 컴파일합니다. 음, 하지만 파이썬 세계에서는 필요한 것이 입력 인자에 따라 달라집니다. 만약 커널에 Flow 64용 배열을 제공하면, 우리는 Flow 64용 커널만 컴파일합니다. 그래서 J 컴파일을 사용하면 바이너리 크기를 줄이고 컴파일 시간을 단축할 수 있습니다. 왜냐하면 전체 월드를 한 번에 빌드할 필요가 없기 때문입니다. 필요에 따라 커널을 점진적으로 구축한 다음 최종 출력을 받을 수 있습니다. 이 점을 더 자세히 설명하기 위해 저희의 새로운 프로젝트 중 하나인 CUDA 코어를 예로 들어 설명드리겠습니다. CUDA 코어는 파이썬 내에서 CUDA API와 CUDA 기능을 이용할 수 있는 새로운 방법입니다. 이 라이브러리는 스트림, 이벤트, 장치, 메모리 할당과 같은 일반적인 CUDA 객체를 모두 제공합니다. 또한 MVRTC, MVG Link, 그리고 나중에는 MVVM을 포함한 모든 온라인 컴파일러 툴체인을 지원하므로 CUDA 코어 기능을 사용하여 코드를 작성하고 컴파일할 수 있습니다. 오른쪽에 보이는 것은 방금 헤더에 작성한 C++ 코드를 파이썬 문자열로 로드할 수 있다는 것을 의미합니다. 그런 다음 어떤 커널 특수화 템플릿 특수화를 인스턴스화할지 지정할 수 있습니다. 음, 저는 정수형 변수와 특정 블록 크기, 그리고 스레드당 항목 수를 지정하여 Flow 객체를 생성하고 싶습니다. 그러면 저는 이 특정 커널을 컴파일하고 장치에 로드한 다음 GPU에서 실행할 수 있습니다. 그리고 KPI 에서 제공하는 참조 구현과 비교하여 결과를 확인할 수도 있습니다. 이것은 매우 훌륭한 메커니즘이지만, 임시 매개변수, 상수 등과 같은 모든 C++ 컴파일 시간 정보가 파이썬 런타임 정보가 되어 파이썬 내부에서 런타임에 확인할 수 있게 된다는 것을 의미합니다. 따라서 이 예제의 모든 C++ 템플릿 커널은 잘못된 시점에 인스턴스화됩니다. 음, 그러니까 C++ 개발자이시더라도 C++ 커널을 개발하고 싶으시다면, 파이썬으로 개발해서 빠르게 반복 작업을 진행해 보시길 강력히 추천합니다. 코드가 완성되면 다시 C++ 코드베이스로 옮겨서 작업할 수 있습니다. 말씀드렸다시피, 파이썬은 많은 CUDA 기능을 지원하며, 저희는 이 프로젝트를 계속 발전시켜 나가고 있습니다. 현재 pip와 condolable을 통해 Linux, Windows 등 CUDA가 실행되는 모든 곳에서 지원되며, 최신 CUDA 메이저 버전인 11과 12를 지원합니다 . 자, 이 코드를 보시면 아시겠지만, 저희는 파이썬 개발자들로 구성된 팀입니다. 가능하면 C++는 쓰고 싶지 않아요. 배워야 할 게 너무 많거든요. 가능하면 파이썬 내에서 개발하고 싶습니다. 그래서 어떻게 할까요? 파이썬으로 CUDA 커널을 작성하는 방법이 있습니다. 바로 Numbuda를 사용하는 것입니다. Numbuda는 오픈 소스 프로젝트이며, Anaconda의 NUMBER 팀에서 지원하고 있습니다. 저희는 NUMBER 팀과 긴밀히 협력하여 CUDA 타겟을 NUMBER 프로젝트에서 분리하고 있습니다. 이를 통해 NUMBER의 유지 관리 부담을 줄이고, NUMBER 릴리스 주기에서 분리하여 프로젝트를 더 빠르게 발전시킬 수 있습니다. 여기 간단한 C++ 코드가 있습니다. 입력 배열 의 각 요소를 제곱하여 출력 배열에 저장하는 코드입니다. 하지만 아래에서 보시는 것처럼 CUDA를 사용하면 더 깔끔하고 파이썬스러운 구문 으로 동일한 코드를 작성할 수 있습니다. 예를 들어, 파이썬에서처럼 2의 거듭제곱을 적분하는 데 사용할 수 있습니다. CUDA를 사용하면 이 코드를 다시 살펴볼까요? 이 코드는 작성하기 어렵고 알아야 할 것이 많다고들 합니다. 코드를 작성하고 성능과 오류 등을 확인하려면 CUDA에 대해 알아야 할 것이 많습니다. 그렇다면 CUDA로 다시 작성하면 어떻게 될까요? 바로 이렇습니다. 방금 언급했듯이 이러한 임시 매개변수들은 파이썬에서는 런타임 매개변수가 됩니다. 따라서 블록 크기, 스레드당 항목 수 등을 커널 실행 전에 미리 지정한 다음 커널 내부에서 사용할 수 있습니다 . 하지만 그 외의 논리는 동일합니다. 데이터를 로드 하고 계산을 수행해야 하며 병렬 트리 축소를 작성하는 방법 등을 여전히 알아야 합니다. 그럼 C++를 없앤 건가요? 전적으로. 예. 이것은 이제 완전히 파이썬 예제입니다. 그리고 이 커널을 방금 보여드린 CUDA 코어 예제에 다시 연결할 수 있습니다 . 이건 완전히 파이썬으로 작성되었지만, 쓰기가 더 쉽습니다. 꼭 파이썬을 써야 하는 건 아니죠. 제가 말씀드렸듯이, 깔끔하고 보기 좋게 작성하려면 CUDA나 GPU 아키텍처 등에 대한 지식이 많이 필요합니다. 특히 이 코드에서 제가 정말로 없애고 싶은 부분이 두 군데 있습니다. 누군가 대신 작업을 해 줬으면 좋겠어요. 그러면 제가 성능 향상 방법을 몰라도 될 텐데 말이죠. 저는 병합 방식으로 로드하는 방법 이나 벡터화된 로드를 할 필요도 없고, 명령어 수준 병렬 처리나 축소도 마찬가지로 배우고 싶지 않습니다. 트리 축소에 대해서도 배우고 싶지 않고요. 그럼 어떻게 해야 할까요? 음, 이전에는 이 모든 코드를 디바이스 함수로 작성해서 모든 로직을 추상화하고 디바이스 함수 라이브러리를 제공하는 방식이 있었습니다. 하지만 이전에는 그와 관련하여 문제가 있었습니다. 문제는 파이썬 장치 라이브러리를 지원하는 유일한 방법이 C++를 통하는 것인데, 이는 오버헤드가 매우 크다는 점입니다. 우리는 그것을 파이썬에 다시 연결하고 싶 거나 PTX로 배포하고 싶지만, PTX는 CUDA 마이너 버전 호환성을 지원하지 않습니다. 따라서 사용자 드라이버 버전에 따라 문제가 발생할 수 있습니다 . 로드되지 않을 수도 있습니다. 링크가 불가능할 수도 있습니다. 오른쪽? CUDA 120부터는 MVGLink라는 새로운 라이브러리가 도입되어 마침내 이 문제를 해결해 줍니다. 작동 원리는 다음과 같습니다. C++ 또는 Python으로 작성된 디바이스 라이브러리를 배포하는 경우, 이를 R이라는 형식으로 컴파일해야 합니다. 이 형식은 MVGLink에서 인식하며, 사용자 정의 커널, 사용자 정의 디바이스 함수, 타사 또는 자체 개발 디바이스 라이브러리 등 여러 구성 요소를 MVGLink가 함께 링크할 수 있도록 합니다. 또한 모든 디바이스 함수를 인라인화하여 추가적인 함수 호출 오버헤드를 발생시키지 않습니다. 이를 통해 C++ 버전과 동일한 성능을 가진 커널을 생성할 수 있습니다. 그래서 기본적으로 이것은 Namba 커널의 수명을 연장하는 역할을 하는데, Grey Marco가 Namba CUDA의 내부 파이프라인을 설명하기 위해 훌륭하게 정리해 놓은 자료입니다. 이제 파이썬 디바이스 함수, 즉 디바이스 라이브러리를 제공하는 최종적인 완성본을 갖게 되었습니다. 다음은 제가 보여드릴 두 가지 예제입니다. 첫 번째 예시는 저희가 CUDA 협동 프로젝트라고 부르는 새로운 CUDA 파이썬 프로젝트입니다. 이것은 고성능 C++ 병렬 알고리즘을 제공하는 cub C++ 라이브러리를 기반으로 하며, 우리는 이 메커니즘을 사용하여 연결하고 여러 사용자에게 노출하는 방법을 알아냈습니다. 그러므로 여러분이 해야 할 일은 다시 한번 이러한 모든 임시 매개변수와 작업하려는 유형을 지정하는 것입니다. 그런 다음 이러한 매개변수를 사용하여 로드 장치 함수를 정의하면 내부적으로 생성된 함수가 있습니다. 마찬가지로 감소 연산도 정의해야 하는데, 이 경우에는 단순히 더하기 b입니다. 세그먼트에 대한 성능 합계를 계산하려고 합니다. 이 사용자 정의 파이썬 함수를 사용하면 내부적으로 u kuda corrective를 사용하여 함수를 생성하고 블록 전체 감소를 ​​수행하는 장치 함수를 생성할 수 있습니다. 그러면 원래 한 페이지 분량이었던 사용자 정의 커널이 이제 슬라이드에서 반 페이지 분량으로 줄어들어 매우 깔끔하고 관리하기 쉬워집니다. 데이터를 로컬 레지스터에 로드한 다음 알고리즘이 사용할 공유 메모리를 준비하고 블록 단위 축소를 ​​수행하기 위해 블록 축소 함수를 호출한 후 마지막으로 데이터를 전역 메모리에 다시 기록합니다. 오른쪽? 이 도서관은 아직 건설 중입니다. 아직 베타 버전을 출시하지는 않았지만, 모든 개발 과정이 공개되어 있습니다. 그래서 오늘 여러분은 이미 해당 파일을 복제하고 소스 코드에서 빌드할 수 있습니다. 한번 시도해 보세요. 이 접근 방식의 가장 좋은 점은 단순히 정수, 실수, 배정밀도 등과 같은 데이터 유형을 지원하는 것뿐만 아니라, 다른 여러 가지 데이터 유형도 지원할 수 있다는 것입니다 . 또한 파이썬에서 정의된 사용자 정의 데이터 유형과 사용자 정의 연산자를 지원할 수 있습니다. 그리고 G 컴파일러를 사용하기 때문에 전체 패키지를 패키징하고 휠 버전을 빌드하면 이 Kuda Corive 프로젝트는 매우 작고 오픈 소스입니다. 내부적으로는 C 블록 플러그인 로드와 블록 리듀스를 사용합니다. 피드백을 주시면 패키징 등을 개선하여 설치와 사용을 더 쉽게 만들도록 하겠습니다. 하지만 무엇보다 중요한 것은 이 라이브러리에서 여러분이 무엇을 원하고 필요로 하는지 아는 것입니다 . 그러니 한번 시도해 보세요. 자, 여기서 중요한 점이 있죠? 이것은 제가 아까 보여드렸던 그래프인데, 노란색 화살표 안에 여러 개의 흐름이 함께 작동해서 최종 결과를 만들어내는 것을 보여줍니다. 그리고 이러한 단계들을 분해해 보면 병렬 단계와 각 스레드 에서 수행되는 직렬 단계가 있다는 것을 알 수 있습니다. 모든 스레드는 병렬로 실행되지만, 최종적으로는 모든 스레드가 협력하여 최종 출력을 생성해야 합니다. 이것은 퀴브 합계 부분입니다. 즉, 우리가 CUDA 시뮬레이션 커널( 단일 명령어 다중 데이터)이라고 부르는 것을 작성할 때, 이는 CUDA 프로그래밍 모델에서 사용하는 방식입니다. C++ 측에서는 이러한 시뮬레이션 커널을 작성할 때에도 최종 출력을 생성하고 정렬, 스캔, 축소 또는 하드웨어와 같은 복잡한 작업을 수행하는 데 도움이 되는 핵심 API가 여전히 필요하다는 것을 의미합니다. 그러니까 남성 모드에 대해 이야기하자면, 이것은 같은 문제를 바라보는 또 다른 방식 이고 다른 메커니즘을 탐구하는 것입니다. 따라서 세그먼트 축소는 행렬 m 벡터 곱셈이 됩니다. 만약 여러분이 이 세그먼트 범위를 2차원 행렬로 생각하고 모든 요소가 1인 벡터를 곱한다고 생각한다면 , 이것은 같은 문제이지만 다른 방식으로 표현된 것입니다. 하지만 왜 우리가 이런 식으로 생각해야 할까요 ? 우리가 이런 방식으로 생각하는 이유는 다양한 장치와 라이브러리를 탐색할 수 있기 때문입니다. 이 경우 저는 Nakuda 커널에서 메탈 컴퓨팅을 수행하기 위해 NVMath 장치를 사용하고 있습니다. 그러니까 무슨 일이냐면, 청크 크기, 즉 배열의 데이터 유형을 선언하고 MV device met을 사용하여 메탈 장치 함수를 선언하면 커널 내부에 할당해야 하는 최적의 공유 메모리 크기를 제공해 준다는 것입니다. 그래서 제 커널 내부에서는 이전에 하셨던 것처럼 모든 메탈에 똑같이 할당하면 됩니다. ABC 행렬에 대한 공유 메모리를 할당하고 C를 A * B로 계산한다고 가정해 보겠습니다. 행렬 A의 경우, 여러 세그먼트 로 구성된 2차원 행렬에서 데이터를 로드해야 합니다. 하지만 행렬 B의 경우에는 아무것도 로드할 필요가 없습니다. 모든 데이터가 한 번에 처리된다는 것을 알고 있으므로 공유 메모리를 1로 초기화하기만 하면 됩니다. 전역 메모리에 배열을 생성할 필요도 없습니다. 그런 다음 공유 메모리를 준비하는 동안 블록 내의 모든 스레드를 동기화합니다. 이어서 Metal API를 호출하여 C를 A * B로 계산하고 다시 동기화합니다. 마지막으로 공유 메모리에 저장된 C 값을 출력 배열에 기록합니다. 따라서 Metal API를 사용하면 문제를 다른 방식으로 표현하고 동일한 문제를 계산할 수 있습니다. 가장 큰 장점은 내부적으로 Tensor 코어를 사용하여 계산을 수행한다는 것입니다 . 그래서 우리는 협력 사례가 부패로 이어지는 경우를 많이 봐왔습니다. 즉, 모든 관련자가 참여하여 최종 결과를 도출해야 합니다. 이러한 이해를 바탕으로, 우리는 프로그래밍 모델 자체에 부패성이라는 개념이 내재된 '커파일(coupile)'이라는 새로운 프로그래밍 모델을 소개합니다. 그래서 개별 스레드를 모두 제어하는 ​​대신, 우리가 타일 블록이라고 부르는 것을 제어합니다. 각 타일 블록은 레지스터에 데이터 덩어리를 로드합니다 . 음, 그리고 나서 저희는 넘파이와 같은 연산을 사용하여 로드된 타일에 대한 연산을 수행합니다. a와 b를 더하면 두 타일의 모든 요소에 대한 합계를 계산한 다음 전체 타일을 전역 메모리에 저장합니다. 그래서 이 기능을 사용하면 특정 경우에 대해 단 세 줄의 매우 깔끔한 코드를 작성할 수 있습니다. 데이터를 불러온 다음, 축을 제외한 나머지 축에 대해 합산하고 최종 타일을 다시 저장합니다. 타일 ​​관련해서 더 자세한 내용을 알고 싶으시다면, 제 동료 스티븐 존스가 어제 CUDA의 새로운 기능들을 소개하는 강연에서 다뤘으니 관심 있으신 분들은 꼭 한번 시청해 보세요. 음, 그러니까 이건 새로운 프로그래밍 모델과 기존 프로그래밍 모델의 차이점을 보여주는 겁니다. 저희 둘 다 지원하며, CUDA 타일은 CUDA의 확장 기능으로, 스레드 곱셈 로직과 데이터 로딩 로직을 분리할 수 있게 해줍니다. 따라서 모든 개별 스레드를 제어하거나 포인터 연산, 전역 배열 오프셋 등을 걱정할 필요가 없습니다. 모든 작업은 컴파일러가 처리해주기 때문입니다. 그리고 말씀드렸듯이 협력적인 특성이 중요합니다. 이 타일 프로그래밍 모델은 기본적으로 넘파이와 유사하며, 모든 타일은 타일 배열로 생각할 수 있습니다. 이것들은 커널 내의 배열일 뿐이며, 제가 방금 이야기한 스캔 정렬과 같은 모든 아이디어를 표현할 수 있게 해줍니다. 몇 가지 압축 및 하드웨어 관련 기능도 포함되어 있죠. 그래서 CUDA 커널에서 넘파이와 같은 코드를 작성할 수 있습니다. 올해 말에 CUTILE을 출시할 예정이니 그때 테스트해 보세요. 이전에는 C++로 작성된 스퀘어 커널과 Cinti 프로그래밍 모델에서 CUDA를 보여드렸습니다. 타일 ​​프로그래밍 모델을 사용하면 이제 똑같은 작업을 수행할 수 있습니다. 우리는 전역 메모리에서 데이터를 불러온 다음 제곱근을 계산하고, 아, 죄송합니다. 제곱근이 아니라 제곱을 계산한 다음 그 결과를 다시 전역 메모리에 저장할 수 있습니다. 엔비디아 워프에 대해 잘 아시는 분들을 위해 설명드리자면, 엔비디아 워프는 기하학적 시뮬레이션, 특수 컴퓨팅 및 AI 워크로드용 커널을 작성할 수 있도록 해주는 미분 프레임워크입니다. 그래서 이 새로운 라이브러리인 warp의 핵심은, warp가 커널을 작성할 때 자동으로 미분 가능한 역모드 커널을 합성할 수 있는 기능을 제공한다는 것입니다 . 화면에 이와 같은 커널을 작성하면, 머신 러닝이나 시뮬레이션 워크로드에 바로 연결할 수 있도록 역방향 커널이 합성되어 제공됩니다. 이제 더 이상 상당 부분을 직접 작성할 필요가 없습니다 . 그리고 최근에 Warp는 새로운 타일 프로그래밍 모델을 도입했는데, 이를 통해 Warp 커널 내부에 동일한 타일 코드를 작성하고 방금 설명한 것과 거의 동일한 작업을 수행할 수 있습니다. 음, 그러니까 한번 확인해 보세요. Warp는 이제 라이선스를 Apache 2.0으로 변경했고, GitHub에서 오픈 소스로 제공됩니다. 아, 맞아요. 하지만 제가 아까 텐서 코어에 대해 언급했었죠 ? 그리고 우리는 파이썬을 통해 텐서 코어를 활용할 수 있도록 만들었습니다. 하지만 당신이 진정한 닌자라면 텐서 코어를 완벽하게 제어하고 싶을 겁니다. 음, 올해 Cutless 팀은 파이썬 지원도 발표할 예정입니다. 이를 통해 파이썬 프로세스에서 Cutless DSL을 작성하고, 텐서 코어를 제어하는 ​​모든 옵션을 활용하여 GPU에서 최대 성능을 끌어낼 수 있는 초고속 커널을 작성할 수 있습니다. 그리고 여기서 가장 좋은 점은 더 이상 C++ 기반의 기계적인 작업, 즉 템플릿 파싱 및 컴파일과 관련된 C++ 코드가 필요 없다는 것입니다 . 이를 통해 컴파일 시간을 크게 단축할 수 있으므로 컴파일 시간이 매우 빨라집니다. 이를 통해 파이썬 에서 커널을 매우 빠르고 효율적으로 반복 처리 하면서도 동일한 성능을 얻을 수 있습니다. 그러니 꼭 한번 확인해 보세요. 쿠토 토크는 금요일에 진행되며, 미적분 팀으로부터 더 많은 것을 배우게 될 것입니다. 지금까지 커널을 작성하는 데 사용할 수 있는 여러 도구를 소개해 드렸는데, 이러한 도구는 현재 엔비디아에서 제공하는 것이지만, 커뮤니티에서 만든 도구도 있어 여러분도 같은 아이디어를 표현할 수 있습니다. 예를 들어, Kupi 리덕션 커널을 사용하여 동일한 세그먼트 리덕션을 작성하거나, KIJIT을 사용하여 Numba CUDA와 매우 유사한 Kai 구문을 사용하는 CUDA 구문 커널을 작성하거나, Triton의 블록 기반 프로그래밍 모델을 사용할 수 있습니다. 포인터 오프셋을 수동으로 계산한 다음 이러한 방식으로 커널을 작성할 수도 있으며, Triton을 사용해서도 동일한 작업을 수행할 수 있습니다. 그래서 시중에는 정말 많은 도구들이 있습니다. 자사 제품이든 타사 제품이든 모두 저희 지원 대상입니다. 그래서 결국 가장 중요한 질문은 커널을 작성하는 데 어떤 도구가 가장 적합한가 하는 것입니다. 고를 수 있는 게 너무 많아요. 제가 생각하는 최선의 해결책은 무엇일까요? 물론 답은 '상황에 따라 다르다'입니다. 음, 안타깝게도 명확하고 간단한 답변은 어렵습니다. 고려해야 할 제약 조건이 너무 많기 때문입니다. 혹시 이미 해결해야 할 종속성 h가 있는 건 아닐까요? 기존 패키지를 활용하고 싶다면, 그중 일부는 이미 커널을 작성하는 방법을 제공할 수도 있으니 먼저 그런 패키지를 사용해 보는 것이 좋을 것 같습니다 . 또는 지원 매트릭스가 크고 일부 패키지가 지원 매트릭스를 지원하지 않는 경우, 예를 들어 다양한 Python 버전, 다양한 CUDA 버전, 드라이버 버전, 운영 체제 및 CPU/GPU 아키텍처를 지원해야 할 수도 있습니다. 또한, 파이썬 프로젝트를 어떻게 패키징하고 배포할지, 그리고 프로젝트 의 사용자 경험은 어떤지 등을 고려해야 합니다. 조직이나 회사에서 특정 패키지 사용에 대한 선호도나 허용 목록이 있을 수도 있으므로 이러한 요소들도 의사 결정에 중요한 영향을 미칩니다. 물론, 오늘 다루지는 못했지만 성능 관련 고려 사항도 있습니다. 요점은 최선의 선택은 여기에 있다는 것입니다. 고려해야 할 사항이 너무 많습니다 . 그러니까 가장 좋은 선택은 커널을 아예 작성하지 않는 것이 겠죠? 저희는 시중에 수많은 라이브러리와 패키지가 있고, 그것들이 저희에게 해결책을 제공한다고 말씀드릴 뿐입니다. 어쩌면 다른 누군가가 이미 당신의 문제를 고려하고 성능 향상 솔루션을 개발했을지도 모릅니다. 그러니까 실제로 커뮤니티에서 무슨 일이 일어나고 있는지, 실제 현장에서 어떤 것들이 있는지 살펴보고 다른 사람이 이미 문제를 해결했는지 확인하는 것이 좋습니다. 직접 커널을 작성하는 대신 기존 커널을 ​​활용하면 훨씬 효율적입니다. 커널 작성은 매우 어려운 작업이니까요. 그래서 저희가 CUDA 병렬 처리를 위한 새로운 프로젝트를 만들었습니다. 이 프로젝트를 통해 커널을 직접 작성하지 않고도 C/C++ 코드와 동일한 기능과 성능을 얻을 수 있습니다. 특히 Thrust C++ 라이브러리를 기반으로 하기 때문에 Python에서 Thrust와 유사한 코드를 작성하고 Thrust와 CUDA에서 제공하는 모든 병렬 알고리즘에 접근할 수 있습니다. 이 경우, 반복자를 생성하고 문제를 정의한 다음 이러한 반복자들을 문제 정의에 통합하고 마지막으로 알고리즘을 실행합니다. 네, 호스트에서요. 그러니까 이 라이브러리 전체는 동일한 커널을 작성하고 높은 성능을 얻을 수 있도록 호스트 API를 제공합니다. 내부적으로는 AOTIR 및 MVG 링크를 기반으로 하여 우수한 커널을 제공합니다. 오픈 개발 프로젝트이므로 꼭 확인해 보세요. 곧 패키징 작업을 완료하여 더 쉽게 설치할 수 있도록 출시할 예정입니다. 하지만 그 전에 여러분의 피드백을 기다리고 있습니다. 방금 MVMath와 Python에 대해 이야기했는데, 이들은 디바이스 API뿐만 아니라 호스트 API도 제공합니다. 그러니까 avmeth나 다른 파이썬 패키지를 사용해서도 행렬-벡터 곱셈을 똑같이 할 수 있죠. 다만 이 경우 유일한 단점은 1로 이루어진 벡터를 전역 메모리에 실제로 저장해야 한다는 점입니다. 그래야만 행렬-벡터 곱셈을 할 수 있으니까요. 하지만 아마 이미 작업 과정에서 이 기능을 사용하고 있을 수도 있습니다. 그래서 이 경우에는 아마도 아주 저렴한 수술일 겁니다. 하지만 마지막으로, 파이썬 배열 프로그래밍에 익숙하신 분들은 문제 정의와 제가 코드 조각 중 하나에 보여드린 검증 코드를 살펴보시면 알 수 있을 겁니다 . 이쯤 되면 아마 이미 눈치채셨겠지만, 이것은 2 차원 행렬의 인접한 행들을 더하는 것과 다름없습니다. 네, 맞습니다. 한 줄의 코드로 실행할 수 있는데, 바로 ` xp sum uh with x is equal to -1`입니다. 오른쪽? 그렇다면 왜 멋진 걸까요? 이건 휴대하기에도 편리해서 멋지네요. 배열 API 표준에 따라 이식성이 보장됩니까? 음, 파이썬 배열 라이브러리가 많이 있기 때문입니다. KPI, NumPy, Jax, Desk, PyTorch는 모두 파이썬, 특히 파이썬 배열 API 표준을 따릅니다. 즉, 이 코드를 한 번 작성하면 다양한 배열 라이브러리를 사용해 볼 수 있다는 뜻입니다 . 가져오기 구문을 'import coupy as XP'에서 ' import torch as XP'로 변경한 다음 어떻게 되는지 확인해 보세요 . 그러면 이러한 다양한 배열 라이브러리를 매우 빠르게 탐색할 수 있습니다. 마지막으로 원래의 C++ 솔루션으로 돌아가서, 우리가 겪었던 C++ 문제를 생각해 보면, 이 직접 작성한 커널을 CUP(Device Substitution) 방식의 세그먼트 축소로 대체하고 싶었습니다. 요약하자면, 저희는 여러분이 커널을 작성하고 생산성을 높일 수 있도록 여러 도구를 제공합니다. 파이썬 환경에서 빠르게 반복 작업을 수행하면서도 C++와 동일한 성능을 유지할 수 있습니다. 그리고 이는 MLI나 MVG 링크 같은 최신 컴파일러 기술 없이는 불가능합니다 . 그리하여 마침내 파이썬 디바이스 라이브러리가 등장하게 되었습니다 . 실제로 파이썬 디바이스 라이브러리를 제공할 수 있습니다 . 새로운 CUDA 타일 프로그래밍 모델은 여기서 한 단계 더 나아가 다양한 협력 장치 API를 프로그래밍 모델의 일부로 고려할 수 있도록 합니다. 오른쪽? 하지만 안 좋은 점은 가능하면 커널을 전혀 작성하지 않아도 된다는 것입니다. 이미 어딘가에 해결책이 있다면 , 섣불리 소매를 걷어붙이고 직접 커널을 작성하기 전에 먼저 시도해보고 결과를 확인하는 것이 좋습니다. 음, 그러니까 이러한 도구들을 다양하게 조합하고, 제약 조건, 성능 목표, 개인적인 취향 등을 고려해야 한다는 거죠. 저희는 여러분이 질문하실 수 있도록 여기에 있습니다. 사실, 오늘 오후에는 전문가와의 소통 세션을 마련하여 여러분 과 이야기를 나누고 질문에 대한 답변과 피드백, 지침을 제공할 예정입니다. 또한 Kodap Python 팀에 필요한 사항이 있으시면 언제든지 말씀해 주세요. 올해는 개발자 관련 콘텐츠를 많이 준비하고 다양한 주제를 다루는 여러 강연 과 트랙을 마련했기 때문에 정말 특별합니다 . 프로그램 작성. 그러니 혹시라도 놓친 강연이 있다면 지금 바로 접속해서 확인해 주세요. 음, 녹화된 영상을 나중에 보실 수 있어요. 음, 마지막으로 모든 팀원들에게 감사 인사를 드리고 싶습니다. 저희는 큰 팀을 보유하고 있으며, 여러 팀이 협력하여 이러한 모든 콘텐츠를 생성하고 파이썬 사용자에게 매우 훌륭하고 친절한 파이썬 솔루션을 제공합니다. 그래서 저는 팀원들에게 감사 인사를 전하고 싶습니다. 관심 가져주셔서 감사합니다. [박수] 레오, 고마워요. 질문 시간을 몇 가지 갖겠습니다 . 예. 블랙웰과 함께 FP4라는 확장형 블록 타입을 도입하셨습니다. 예. 예. 파이썬에서 그걸 어떻게 활용할지에 대해 이야기하는 사람을 본 적이 없네요. 네. 어, 당신은 MX FP8 또는 FP4를 말씀하시는 것 같은데, 이는 기본적으로 단일 스케일 팩터입니다. 최근 MVMath Python 팀에서 FP MX FV8에 대한 030 지원을 출시한 것으로 알고 있는데, FP4도 로드맵에 포함되어 있는 것 같으니 곧 지원될 겁니다. 다음 버전에서는 사용 예시를 보여주시면 좋겠습니다. 네, 물론입니다. 예를 들어, Python으로 커널을 작성한 다음 C++이나 다른 애플리케이션에서 미리 컴파일하거나 링크하여 사용하려면 어떻게 해야 할까요 ? 파이썬에서 커널을 어떻게 포착하는지에 대한 질문입니다. 왜냐하면 결국 PTX나 Kin을 생성할 수 있기 때문이죠. 제 추천은 Kin을 직접 생성하는 것이지만, 어떤 커널, 어떤 쿠폰에 대한 쿠폰인지 식별할 수 있는 방법이 필요합니다. C++에서 출력 결과물과 교체하려는 커널을 연관시킬 수 있어야 합니다. 응. 따라서 이러한 식별 방법을 알고 있다면 드라이버 API를 사용하여 다시 로드할 수 있습니다. 음. 오른쪽. 감사합니다. 안녕하세요, 이야기 나눠주셔서 감사합니다. 음, 파이썬에서 매우 인기 있는 또 다른 타일 라이브러리는 Triton입니다. Qile이 Triton과 어떻게 다른지, 또는 Triton이 해결하지 못하는 문제 중 Qile이 해결하려고 하는 문제는 무엇인지 등에 대해 설명해 주실 수 있을까요? 아, 죄송합니다. 질문이 뭐였죠 ? 음, 어떤 도서관을 추천하시나요? 야외 도서관 중에 트리톤이라는 아주 유명한 곳이 있어요. 그건 마치 데이터를 타일 형태로 배열해서 내용을 적는 것과 비슷해요. 그리고 귀여움과 그것이 어떻게 다른지 궁금했어요. 음, 사실 그 도서관에 대해서는 잘 모르겠어요. 그러니까 제가 좀 찾아보고 더 나은 예를 들어봐야겠네요. 아, 트라이던트, 당신은 트라이튼을 말하는 건가요? 아니면 그냥 다른 프로그래밍 모델인 것 같네요. 네, 커널을 작성하는 또 다른 방법이죠 .
