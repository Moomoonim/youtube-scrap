# [ENG] Deep dive into NAVER Cloud Platform 03: Network

- 영상 링크: https://www.youtube.com/watch?v=e0LUx38xDqQ
- 채널: NAVER Cloud
- 업로드일: 2022-12-02
- 자막 언어: en
- 단어 수: 약 1424개

---

## 스크립트

[Music] thank you [Music] hello this is neighbor Cloud today we'll take a look at networking products in Naver Cloud platform the first service we'll look into is the VPC service VPC stands for virtual private cloud and it is a service that enables creation of a logically isolated customer specific Network space in the cloud currently the AP bands that can be created with vpcr 10.0 0 0 8 170 to 0.16 0 0 12 and 190 to 0.1680-16 the subnets for each band are supported from a minimum of 28. it's up to a maximum of 16 bits up to three vpcs can be created per account and the VPC peering feature is provided to enable private communication between different vpcs VPC peering is one-way communication so it creates to networks by specifying the requesting VPC and accepting VPC in Reverse through VPC peering you can enable private communication between vpcs managed by other accounts as well as private communication between vpcs you're managing next we'll take a look at network access control list which is the main component of VPC it is a factor that reinforces the security of VPC network access control list is applied to the subnet and operates when the subnet is accessed from the outside in network access control list you can set allow and block rules for inbound and outbound traffic and rules are processed based on the priority when deciding whether to allow traffic in addition Naver Cloud platform provides a feature called access control group to strengthen security at the VPC level Access Control groups are applied for each server and they are responsible for the communication security of servers in the subnet access control group only supports allow rules and all rules are evaluated before deciding whether to allow traffic this time let's take a look at subnet for subnet the address range is specified in the form of cidr with within the icky ban specified upon VPC creation you can specify a Zone when creating subnets and you can also create multiple subnets in the same zone subnets are largely divided into public subnets that enable external communication and private subnets that only allow internal communication public IPS can only be assigned to servers in public subnets currently up to 200 subnets can be created for each VPC by default external communication is unavailable for private subnets however if you need external communication you can enable external internet communication for servers within private subnets through netgateway after creating Nat Gateway you need to set the path from the root table applied to the private subnet so that it is directed to the net Gateway for external communication next we'll take a look at load balancer one of the representative Services of networking products load balancer is a service that distributes received traffic to multiple servers it performs the role of increasing availability and adjusting system utilization when creating these load balancers in Naver Cloud platform you need to set the target group the target group refers to the group of servers that will actually handle the traffic received by the load balancer you can create Target groups with servers in the same VPC a server that belongs to Target group a may also belong to Target Group B but Target group a can't be connected to multiple load balancers the load balancer performs periodic health checks on bound servers to monitor their status the user can set the health check cycle between 5 to 300 seconds in addition to the health check cycle you can also set thresholds you can set the normal threshold and the failure threshold normal threshold refers to the number of successful health checks required for an abnormal Target to be put back into service failure threshold refers to the number of failed health checks required for a normal server to be switched into the abnormal status and excluded from the load balancing Target the load balancing algorithm uses the round robbing method by default load balancing algorithm method changes sticky session proxy protocol settings Etc can be configured after the load balancer is created currently load balancer in Naver Cloud platform supports TCP proxy TCP HTTP and https protocols according to the supported protocols load balancers are categorized into Network load balancers Network proxy load balancers and application load balancers each load balancer type will be described in further detail in the next slide Naver Cloud platforms load balancers can be selected from small medium and large depending on the load balancing performance please note that supported CPS value varies for each load balancer type for load balancer algorithms in addition to the default round robbing method which distributes traffic to servers one by one upon client request the least connection method which distributes new connections to the server with the fewest client connections and the source sippy hash method which has a hash table for the client AP and distributes new connections to servers mapped to the client IP are supported now let's take a closer look at the characteristics of each load balancer application load balancer supports HTTP and https protocols and it is capable of Branch processing based on host header and Branch processing based on URL since it can operate in the L7 layer in a Edition the application load balancer supports all Branch processing methods including round robin leased connection and Source EP hash next if you look at the network load balancer it supports TCP high performance processing and logs the client IP as it is the network load balancer supports the source Epi has and Round Robin methods the last load balancer type we'll look at is the network proxy load balancer Network proxy load balancer provides proxy based communication so you can use it in tcp-based applications that require session maintenance in addition you can apply the same load balancing algorithms as the application load balancer now let's take a closer look at the features of each load balancer if your system needs a TCP level high performance load balancing feature which type of load balancer would be suitable that will be the network load balancer the network load balancer supports from a minimum of 100 000 to a maximum of 400 000 connections per second it also implements direct server return which responds directly to the customer server so you can use a faster and more efficient service if you need to manage TCP sessions then the network proxy load balancer is suitable if you need SSL authentication and encryption processing then you can use the application load balancer or network proxy load balancer at this time you need to register the SSL certificate to certificate manager in advance if you want to apply various types of load balancing algorithms when using load balancers which type of of load balancer would be suitable you can use the application load balancer and network proxy load balancer please note that both application load balancer and network proxy load balancer support all load balancing algorithms including round robin leased connection and Source EP hash it processes branching on the application Level by checking the packet header for HTTP and https traffic rules such as host header based Branch processing URL path pattern-based Branch processing weighted Branch processing and redirection response processing are supported for the load balancer listener so you can choose the application load balancer if you need a more detailed configuration it provides basic monitoring for the load balancers you've created through this basic monitoring Network load balancer monitoring provides information about five items including concurrent connection connection per second trafficking and available hosts application load balancer monitoring provides information about six items by adding traffic out lastly let's take a look at load balance support settings when setting a load balancer rule you must set the load balance support to be different from ports with other load balancer rules the server Port can be identical to a server port with other load balancer rules the networking service will look at afterload balancer is the global DNS service through the global DNS service you can register and use various records for your domain and you can map Alias records to The Domain configured in global DNS it also provides a monitoring feature enabling you to check the number of requests for the configured domain that's all for our look at Major networking products provided by Naver Cloud platform we'll be back with some interesting content next time thank you
