# Tony Silveti Falls - Training neural networks at any scale

- 영상 링크: https://www.youtube.com/watch?v=__eeLqSlZ0w
- 채널: Cohere
- 업로드일: 2025-07-03
- 자막 언어: en
- 단어 수: 약 9370개

---

## 스크립트

[Music] Um thank you everyone for joining once again on another iteration of ML theory. So today we're very excited to have um Tony Meti Falls um talk to us about training neural networks at any scale. Um so just a bit of a bio about the speaker. So Tony is an associate professor of artificial intelligence artificial intelligence at Saluple in the department of mathematics and a member of the ENRIA um opus team. He works at the intersection of stochastic non smooth non-ucuklidian optimization theory and machine learning with a focus on developing and analyzing algorithms for large scale problems. So Tony I mean you've often time has been a attendee in our community sessions so it's cool a bit and get you you know to present your work. I'm very excited to hear I I I must admit it's above my expertise. So I'll let you take the floor and explain um the lovely things you've been researching. So take it away. Sure. Thanks Andre. So the first thing I'll say is that this is a joint work with many of my collaborators who are all or almost all at EPFL in Switzerland. So I think we all agree that training neural networks amounts to solving stocastic optimization problems roughly of this form. And I want to start the talk with a question to motivate things which is can we design an optimizer or an optimization algorithm uh which respects the natural geometry of neural networks by which I mean uh in such a way that we guarantee feature learning so I'll talk more about this in the slides to come across different model scales. So when I say model, I have in mind deep neural networks with transformer architectures. And when I talk about scaling, I have in mind scaling the width of these layers, which is the modern paradigm for scaling. So let's start by talking about what's been done so far. So if you were to just grab a method off the shelf to solve this problem, you would probably get first SGD, stochastic gradient descent. I think we're all familiar with it. And we can actually write the update of stochastic gradient descent uh as a minimization problem with respect to a certain uklitian geometry. And when I say uklidian geometry, I just mean that we have this ukitian norm squared in red in the update. So this is pretty vanilla and there's two conceptual improvements you might reach for right away in the form of adaptivity. So I will divide these into two camps. In the first we'll have on the-fly adaptation. These are methods that adapt during the training. So things like adrad RMS prop atom atomw and this is really in contrast to what I will call a priori adaptation. So methods that are designed with a problem specific geometry in mind. So you can think of Bregman methods like mirror descent or no lips remmanian optimization if you have some manifold structure or maybe also things like maximal update or muup parameterizations which are specific to training neural networks. So I'll go into detail about atom and and mu just to set the stage. So if you're not familiar this is the atom algorithm. So at each iteration, we're going to draw some stochastic estimate of our gradients. We're going to feed this into a momentum estimator that I show you in teal. And we're also going to keep track of the second second moment of the gradients. And you see this in blue. And this second moment of the gradients is going to form a certain matrix that I'll call HK. And this HK can be used to define a certain mahalenovas norm. And the update for atom can actually be written as an update like it was for SGD but now with respect to a mahalenobus norm. So we've upgraded a bit from the vanilla setting maybe to like a sherbet but it's still somewhat plain because the mahalova norm is after all induced by an inner product. So in some sense it's ukitian. This doesn't mean that Adam acts like a uklitian algorithm because indeed the kernel is changing from iteration to iteration. uh but I think that we can do better than this. So what are some of the shortcomings of Atom? The first thing is that because it's on the fly uh you have to keep track and store those moments that you're going to use to regularize your gradient. And that means that you have to spend some memory to keep track of these. And it's not a big deal when your model's small, but as your model grows to like really huge scales, uh this starts to matter more and more. It's also unaware of the architecture. So Adam doesn't make any distinction about whether a parameter is in the first layer or the last layer of the model. It doesn't make any distinction about the dimensionality or the matrix structure of our problem. And this manifests this manifests itself when we go to look at scaling of atom. So if I start with let's say vanilla atom in the so-called standard parameterization and I go to train uh some model and I'm scaling the width of this model then I'll end up with a plot like you see on the left. So on the x-axis is the learning rate and we see different colored curves with darker curves corresponding to bigger models. And the takeaway from this is that bigger models actually need smaller learning rates when you use the vanilla atom uh standard parameterization. And this is not so great because we would like to be able to search right for the best learning rate with a cheap small model and then use that to train our huge model where searching is expensive. But we can't do that uh with the vanilla setup. Another problem uh when we use optimizers like SGD or atom is that they fail to learn features as width scales. So what I mean by that is with the standard parameterization which corresponds to certain initializations of the weights of our network. So things like the lacun initialization or the kiming initialization. Uh and if we use a global learning rate, we can get stuck in the so-called lazy regime. So it's called the lazy regime because the weights move less and less from their initialization as the width goes to infinity. And so we don't really learn features, right? The weights of our intermediary layers are basically frozen at initialization. And when we train a network in this way, as we scale the width, we basically fit these random features. And we're not really taking advantage anymore of like the hierarchical nature of deep learning. So this manifests itself in practice when you actually go to scale things. And I show you that here with this figure from a Greg Yang Tensor Programs paper. So in the middle you have uh the result of some embeddings that were trained uh using wordtovec. So wordtovec is like a shallow MLP model where you don't really care about the training loss. You're really just training things so that you can later take the embedding matrix and use this to get some nice embeddings of of words into continuous vector space. And if you do this with a finite width model, you get some degree of separation as you see in the middle plot between states and cities represented by blue and orange dots. So if you scale things in a bad way, uh you end up in this lazy or or neural tangent kernel or NTK regime that you see on the left where you've minimized the training loss, but you didn't actually learn any meaningful features that distinguish between states and cities. If we scale things differently, we can hope to have this plot that you see on the right, which is also scaled to infinity uh speaking metaphorically, but has much better separation, right, between the the states in the cities. So, how do we get this plot on the right and ensure that we don't end up with the plot on the left? For that, we can start to look at something called MUP. So it's a parameterization of your your neural network and your optimizer in tandem. And it basically boils down in broad strokes to uh a certain layer-wise step size scaling uh and an initialization scaling uh that takes into account basically the dimensions of the inputs and outputs to each layer. And so if you parameterize things uh using mu I won't go into the details but basically you ensure the correct scaling behavior as the width goes to infinity. And so you don't end up in this NTK lazy regime. Instead, you are sure to end up in the feature learning regime. And as a byproduct of this, you actually also have something called hyperparameter transfer. So now the optimal step size for your models of varying width actually all coincide or roughly coincide as you see in the figure on the right. And so you can find the best hyperparameter on your cheap to train model and then use this on your huge expensive model. And the takeaway from all of this is actually that new P uh is architecture aware, right? It's taking into account the dimensionality of of each layer and scaling things in a certain prescribed way. And we can see this really as a form of a priori adaptation, right? It's a way to make atom aware of the architecture that it's trying to train. And this is really, as we'll see, the right way to go when it comes to things like hyperparameter transfer, even if we're not using atom. Tony, just to interrupt, I believe there was one hand that was up um I think on the previous slide. So maybe um if the Yeah, if if you go back a slide. Yeah, here I was uh wondering since for the NTK and uh the width uh 64 and the width infinite what were the x and y axis of the grass here. Ah yeah so the x and x uh x-axis and y axis are coming from like PCA of the embedding matrix. Oh okay. Yeah that kind of makes sense. So that's the kind of a uh grouping that uh as the width increases that the blue and the orange kind of groups together at uh yeah yeah we hope that they separate so that we have some distinction between the concepts of state and city cool thanks yep okay so I want to talk about feature learning in more detail now that we've I've touched on it kind of ambiguously So for this we need a sort of working model of a neural network and this is what I'm going to use. So without loss of generality I'm not going to take any bias parameters. I'm just going to have weight matrices. I'm going to assume that our input lives in uh RP. So it has P components and that the dimension of all the hidden layers is uniform. It's just equal to M until you get to the final layer where we will output something in R. So I'm going to also assume that the activations are pointwise applied or component-wise applied and that they are non-expansive and that they map zero to zero. So you can relax these but for now I I want to have this just to uh talk about feature learning but it's not a restrictive assumption. So with this model of a neural network, we can define what are like the features of the elf layer uh just by evaluating uh the network until the elf layer. So a partial evaluation will give me the features at the elf layer. You can also think of them as the elf layer's activations. And I'm going to denote this h to the l and I'll also denote the change in the features with delta h after one iteration of our training. And so if we want uh feature learning or we will define feature learning to be holding if the activations the features uh and the changes to them are stable as we scale the width. So when I say stable I mean that the RMS norm is neither vanishing nor exploding to plus infinity as the width goes to plus infinity. So that's what's represented by this big theta means that it's stable as we scale the width. And if you're not familiar, the RMS norm is just a regularized uklitian norm that takes into account the dimension. So you divide by the square root of the dimension of your vector space. So to get an idea of of how we can ensure this, we can look at what happens for like the first layer to begin with. So the first layer's features H1 if I look at their RMS norm just by the definition I can rewrite this as the first weight matrix applied to the input followed by the activation function if it's non-expansive and map zero to zero I can upperbound without the activation function and then I can upperbound this using the RMS to RMS operator norm and so if I want um my features to be stable I want that this operator norm is stable as I scale the width and you can do the same for all the intermediary layers. You get the same result but for a different operator norm right instead of M2P now it's M2M or P2M. Uh the same for the last layer. Right? So we really need to control uh these operator norms and that leads us to the spectral conditions for feature learning. So these are an alternative to the definition of feature learning that I just gave you uh which don't require any assumptions on the activations or the features themselves but rather assumptions directly on the weight matrices which are what we actually have access to. So these conditions are going to uh ensure that these uh features are stable and that their updates are stable. And we can actually rewrite these with respect to the the spectral norm. So that I denote with the the OP. So the spectral norm is the largest uh singular value of a matrix. And asking that the RMS to RMS operator norms are stable is equivalent actually to asking that the spectral norms have a certain scaling behavior. Okay. Based on the dimensions of the input and the output to the layer. And we can extend this to to general rectangular matrices just by uh using the correct scaling. And what we should be taking away from all of this is that if we could control the scaled operator norms layer by layer in the network uh then we could feasibly ensure feature learning as we scale width. To be honest, it's really a heristic uh because these operator norms are just upper bounds and and we don't know what happens like if the previous layers output is in the kernel uh then certainly the operator norm bound is not going to be sharp. But uh it turns out that this is like a super useful heristic and it's pretty close to what happens in practice. So this leads us to our general strategy for how we're going to make an architecture optimizer uh that can train neural networks at any scale. Okay, we're going to cook up uh some non-ucuklitian norms based on layerwise scaled operator norms and then we're going to build our optimization algorithm around this norm and this is what's going to give us a priori adaptation. Right? We're going to encode all the information about the architecture into some norm. And because our algorithm will be using this norm, it will be informed of the architecture. Okay, so that's the background on feature learning and why we're motivated to to do this. But now let's talk a little bit about non-ucuklitian optimization to have an idea of what we can build on. So we saw already that the updates of SGD can be written with respect to this uklidian norm. And you might ask well what happens if you just change the norm here. If you do that you get steepest descent with respect to the norm of your choosing. And the nice thing about steepest descent is that the update that you get from doing this actually has a closed form solution using the dual norm uh of the gradient or of your stoastic gradient estimator. uh and so as long as you can compute the dual norm uh and something called the linear minimization oracle uh you can always do steepest descent with respect to any norm that you want to pick right so this linear minimization oracle is really what gives us the direction that our update is going to be in let's talk a little bit about what this looks like uh geometrically so I'm showing you here the linear minimization oracle for several different LP balls so if If you look uh at the the output for the LMO on the L1 ball, you get this vector in red. Okay, if you take the gradient in black and you put it in the LMO in plain English, it should give you back the least aligned vector that doesn't leave the unit ball. So for the L1 ball, we get this vector in red. And you can also notice that this is a one sparse vector. And this is not by accident, right? This is because the extreal points of the L1 ball are typically one sparse. On the other hand, if we take like the L infinity norm that you see in purple, uh you actually get a dense vector as your output from the LMO. And this is again not an accident because the atoms of the L infinity norm or the extreal points are actually dense. So if you pick uh some norm you're going to induce some unit ball and this is going to severely impact what directions you get from the LMO right and the directions are not always unique by the way so if the set is not smooth then uh the directions won't be unique so some nice things about the LMO is that the output is always on the boundary of the ball and this means that you have a good idea of uh what is the norm of the update you're going to make uh if you use the LMO because you know the norm of the output of the LMO and if you know how to compute the LMO for one ball of some radius then you can actually easily compute it for any radius just by scaling the output. Okay, so for instance if you have it for the unit ball you have it for the ball of any non- negative radius. Okay, that shows uh what the LMO looks like geometrically. But what about analytically? uh we can actually see the LMO as being the sub differential of the dual norm. So if we start with some norm, we can find its dual norm, try to think about what the subgradients look like and that will give us uh outputs for for the LMO. When I say subgradians, I mean in the the classical convex analytic sense, nothing exotic like the Clark or anything like this. So if we start with the L2 norm uh the dual norm to the L2 norm is actually again the L2 norm. So it's self duel and the subgradients uh of the L2 norm. So it's actually differentiable except at zero and the the subgradients are given by G divided by the norm of G right times uh with up to sign. And if we look at steepest descent then we should multiply this by the dual norm. But the dual norm is again just the L2 norm. And so these guys are going to cancel and the direction that we're going to get is just going to be uh the gradient or the stoastic gradient. And so if we do see this descent in the L2 norm, we're going to recover a gradient descent. If we have something non- uklitian like the L infinity norm, then what we get is more interesting. So the dual norm in this case is not again the L infinity norm. It's actually the L1 norm because one and infinity are conjugate exponents. And so the uh subgradients of the L1 norm that we can take are sign vectors. If you take a sign vector, this is always a subgradient of the L1 norm. And if we multiply by the L1 norm of the gradient times the sign direction vector, then what we're going to end up with with steepest descent is actually a sort of sign descent or stoastic sign descent up to this scaling of the step size. And most interesting and most relevant to to training neural networks is actually the spectral norm. Okay, so the L2 to L2 operator norm. Now we can see this guy as the L infinity norm applied to the singular values of our matrix. And in that case uh the dual norm is of course going to be the L1 norm applied to the singular values of our matrix which is more commonly called the nuclear norm which I will denote with this nuke subscript. So just like sign vectors or subgradients of the L1 norm. If we take the analog of this we have semiorthogonal matrices and these are all subgradients of the nuclear norm. So when you go to compute the LMO associated to the spectral norm, you're going to compute the matrix sign, which is really just the vector sign applied to the singular values. So you're going to set all the nonzero singular values equal to one. And you can actually compute this operation without using the full SVD either using Newton Schultz or using randomized linear algebra like sketching. But typically in practice people are using Newton Schultz. So if we have the LMO and we have the dual norm then everything is working. We can do steepest descent. Uh but if we did steepest descent will actually end up multiplying and scaling our direction uh by this dual norm of the gradient. And this dual norm of the gradient we don't know how it scales as the width goes to infinity. So we won't be able to ensure that we will satisfy the spectral conditions for feature learning if we just use steepest descent. Uh but instead of using the norm squared we can replace this by the indicator function uh for the scaled ball in the norm of our choosing. So if you don't remember the indicator function uh it's just a way to enforce constraints. So instead of uh updating by minimizing this quadratic approximation uh we can update our iterates by minimizing a linear approximation to our function uh subject to remaining in the ball. So in plain English it says something like linearize your function minimize it but don't go too far where too far is measured by the norm that you've picked. If we do all of this uh then the type of algorithm that we get where we just directly add the LMO is going to satisfy the spectral conditions for feature learning because we're going to ensure uh that the RMS to RMS operator norm of the updates to our weights uh is stable with respect to scaling in the dimension. So if you're familiar uh by the way with like conditional gradient algorithms and the LMO is is probably familiar to you as well. So conditional gradient algorithms are they're also known as as Frank Wolf algorithms actually are classical. So they come from the 1950s and they're used for solving smooth constrained optimization problems. So if we have some constraint set D that's like convex and compact and we want to minimize some nonlinear function F at each iteration we could linearize F uh and then minimize this linearization over our constraint set and this would exactly amount to computing a linear minimization oracle. uh instead of directly adding this output to our current iterate uh we can instead take a convex combination between the output of the LMO and our current iterate and in this way we're taking a convex combination of elements of the constraint set and we're sure that we're not going to leave it right because the constraint set is compact and so as as long as you have a compact convex constraint set Frank Wolf gives you a way to optimize uh without having to project just using the linear minimization article and So we're going to use the inspiration from both steepest descent uh and the conditional gradient algorithm to come up uh with our contribution. So let's move on to that next. So this is the algorithm uh that we're actually going to propose to use for training neural networks. And at its core, it's really just like the conditional gradient or FrankWolf algorithm. So I'm going to call it the stochastic conditional gradient or sometimes the unconstrained version depending on which type of step we take. But the basic idea is that we first sample uh some gradients using like loss.backward or something. Then we're going to accumulate these uh by taking the average into some momentum estimator. This momentum estimator is going to get fed into the LMO which is going to return us some direction that we're going to step in. And then we either step directly in this direction as was done similar to steepest descent or we're going to take a convex combination with our current iterate which is what was done in the conditional gradient algorithm. So if you're not familiar with Frank Wolf, but you are familiar with deep learning, then you'll probably recognize this as just being weight decay. And indeed that's true. If you just add weight decay, this is another interpretation of what's going on with our our two uh step directions. So in this sense, we can say that the the unconstrained version of this algorithm solves an unconstrained problem. While if we do weight decay, we're going to solve something constrained. And this is actually uh really nice. we finally have uh the feature learning that's going to be satisfied by taking the LMO direction and we're actually also going to get uh an interpretation of weight decay for free on top of this. So if you're familiar with with weight decay, there is this um big distinction between decoupled and coupled weight decay uh which corresponds roughly to seeing it as a tickenov regularized problem or not. And a lot of people argue that it should not simply be seen as a tickenov regularization uh because of how this interacts with like adaptive gradient algorithms. And this is uh also the case in the non-ucuklitian setting. Right? So this is critical because if you just add weight decay to your unconstrained optimizer, you're going to get stoastic conditional gradient uh with a radius that depends on the weight decay and the step size. Whereas if you try to use the unconstrained optimizer on the tickov regularized problem, you're going to get this translation inside the LMO and because it's nonlinear, this is really going to affect uh the direction that you go into. So we get a new interpretation of weight decay in this context that it's taking an unconstrained optimizer and turning it into a constrained optimizer with an implicit radius that's dictated by the chosen combination of the step size and the weight decay. Okay, so this is the algorithm that we're going to use. We just need to to cook up the norm that we're going to plug into it and then I'll show you how it works in practice and what we're able to prove uh for the convergence. So how do we cook up the norm? We first need to think about how to make an LMO that decomposes. So if we're able to assign a norm to each weight matrix in our network, then if we take the max over these norms, the LMO with respect to this new norm is going to be separable. And so when I go to compute the LMO uh of my gradients or my stoastic gradients, I can do it in a separable way. I don't need to know what happens at layer L to compute the LMO for layer 1 for instance. So this is the type of norm that we'll take. We'll try to put some norm on each uh layer and then we'll try to take the max over these guys to get a norm uh over all the parameters. So if we can specify some norm for the input space and the output spaces of each layer then this will induce an operator norm that we can use for that layer's weight matrix and this is coming from the spectral conditions for feature learning. Then we can specify a norm for the whole set of parameters by taking the max and we know what uh norms to put on the input and output spaces of the intermediary layers already. Right? Right. So the spectral conditions for feature learning say we should try to control the RMS norm. So if we put the RMS norm on the input and outputs then what we're going to get uh is a scaled spectral norm. And so we're going to get for the LMO a scaled matrix sign uh with a certain scaling that we know based just on the dimensions of the input and output to the layer. So this works uh for the intermediary layers. Uh but what about the first and the final layers? things are not so obvious there. Uh so for the first layer, we have this issue that the input to the first layer can change a lot depending on what application I'm looking at. So if we're looking at uh images, then the input to the network is probably going to be like a dense vector, right? This is something that's typically not like one hot encoded. and and this is in contrast to language modeling problems where you typically do have a one hot encoded vector as input to your network. So for image domains uh we're typically going to equip the input space with the RMS norm and this is going to give us a scaled operator norm like we've already seen for the the intermediary layers. But for language tasks uh the input is one hot encoded and this complicates things because actually all LP norms are going to be identical. they're going to be equal to one for this one hot vector. And this means that actually all of the induced operator norms are going to be equivalent. But it doesn't mean that the LMO that you get from these different operator norms are going to be the same. Actually, we're going to get very different ones. So if we pick the two to RMS, we're going to get again a scaled operator norm. No surprise there. uh if we pick the one to RMS, we're going to get something called the column norm with a column-wise normalization for the LMO. And if we pick uh the infinity to RMS, we can upperbound this by the one to infinity. And this is going to give us a sign LMO. And using the LMO also informs us how to initialize the network, right? So we want to initialize with a fixed norm given by our our choice of norm on the input space. And so we have not only the LMO but the initialization for all of these guys. And the result of this uh that we can see on like a toy problem where we're just training a three layer GPT on the Shakespeare data set is that we automatically get hyperparameter transfer across width. So I'm showing you the uh output of training several models using unconstrained scion uh with different norms on the input space. So you see it's either sign uh spectral or or column norm on on the first weight matrix but all of them give hyperparameter transfer. Typically the the sign is performing the best or the column norm and the spectral would be performing much worse if this example wasn't uh such a small vocabulary size. So this is kind of impacting what we see here. For the output layers, uh we have no restriction to to bound the output in RMS norm if we have some some vector output. So we don't need to consider bounding uh this. Instead, we can consider bounding the maximal entry by taking the L infinity norm. So this would mean that we want to use the the RMS to infinity norm on the weight matrix in the last layer. But we could also consider upper bounding this uh by something that's going to give us uh the sign for the LMO. And we can see how both of these compare. So I'll summarize this uh in this table. So if you naively took the RMS on the output, of course you get back uh the matrix sign. But if you take the RMS to infinity, then you're going to get this row norm. And if you take one to infinity, you're going to get a scale again the scaled uh vector sign. And we tested this on a very small MLP just trained on on CFAR, not to see if it was effective at getting like a good validation accuracy or anything like this, but just to see that it does hyperparameter transfer. And indeed, we find that no matter what operator norm you take for the last weight matrix, we still have hyperparameter transfer as long as you take the scalings that we prescribe. So I'll summarize all of this, all of our recommendations. Basically, if you're working with images, you should do spectral on on the first layer, spectral for the intermediary layers, and then sign for the output. Whereas, if you're working with one hot encodings, you probably want to use sign or column norm for for the first layer. We're going to infer to this uh instantiation of of that non-ucuklitian algorithm I showed you when we use operator norms as scion or unconstrained scion. And if you want to give scion a try on your problem, you can try it at this GitHub link where we have the code that's available and in my opinion pretty easy to use. What does it look like when you go to actually do this in practice? I want to show you uh the details with a very simple shallow model. So we have just three weight matrices here. We're going to compute our stochastic estimate of the gradient GK. We're going to feed it into our momentum estimator and then we're going to compute uh the the update by taking the LMO of this guy. So this is just the definition of the algorithm so far. Then because we've taken the max norm the LMO is going to be separable and so we can compute the LMO on each block uh corresponding to the different weight matrices separately. If we take uh as an example the sign spectral sign configuration then the scalings and the LMOS are going to give us this update. So we're taking into account the dimension of the input for the first layer and the dimension of the intermediary layers for the last layer. And just uh to mention, you can also scale this with layer-wise radi. So uh this is also something that's possible to do. I see some people confused about this. If if we have to pick one norm for the whole network, does it mean we can't have different radi? No, you can definitely have different radi just by scaling the different norms on each layer. So this is what it looks like to do uh one update of this, let's say mathematically. And there's also a question when it comes to practice about what is the correct uh scaling convention for the output of the spectral LMO because while I have prescribed one thing you will find different prescriptions in for instance the Kimmy paper on muon or in the muon blog post itself. So if we take uh some matrix W which is uh D out by D in then the scion scaling says you should take based on the spectral conditions for feature learning the square root of V out over D in but this is not what you find um in in many other works right so in the Moonlight Kimmy paper which is also looking at spectral LMOS the scaling is given by the max of the square roots and this is actually not leading to to to hyperparameter transfer and this is something that they commented on in the Kimmy paper that uh it's an open question what will give hyperparameter transfer for muon so I think that scion gives a a great answer in this direction and finally there is uh the canonical implementation of muon which is what you find in the modded nano GPT architecture so this speedrun is uh always changing as the record gets broken and so different scalings are getting used so initially they were using the same scaling as what was used in the Kimmy paper, but more recently they've been using uh this alternative scaling which is also different from what's in Scion. And so you have it seems like at least three choices. Uh and it's not always clear which one is better, right? So I'm going to show you some experiments comparing these in a few slides. So let's look uh at the experiments next. The first one that I want to show you has to do with hyperparameter transfer. So in this context we're training uh the nano GPT architecture coming from the speedrun uh with various widths right so different colored curves correspond to models of various widths. On the x- axis we have like the learning rate and on the y- axis we have the validation loss. And so what we see in the top two plots is that for Adam and for Muon with the Kimmy scaling, as we increase the model width, the optimal learning rate is actually shifting, right? It's getting smaller. And in contrast, as we scale the width with scion or with unconstrained scion, uh the the optimal learning rate is actually fixed, right? They're all lying on on a vertical line. And this is actually something that we can prove rigorously under some assumptions. The sort of assumptions you would find in the tensor program series of papers, but nonetheless a proof. So you might ask uh is is the fact that muon's not doing a hyperparameter transfer related uh to the scaling that Kimmy is is using and this is not the case. So we reran the code with uh the the new scaling and you still see that the hyperparameters are are not transferring, right? the step size is not transferring as we scale the width. So it's not a matter simply uh of just using the new scaling. Um it it really has to do also with the fact that uh the typical implementation of muon is using atom on the vector parameters and this is complicating things. So I talked earlier uh when I introduced um the stoastic conditional gradient method about the the constrained problem it would solve. And so we get basically a free bound for the stoastic conditional gradient algorithm on the norm of our parameters because we're never going to leave the constraint set just like in Frank Wolf. For the unconstrained version of the algorithm, we also have a bound, but it's not fixed by the radius. it depends on the sum of the step sizes and so scion the operator norm version of the algorithm inherits these bounds and we see this in practice uh here we're training a convolutional neural network on CR10 and what we see is that the the norms uh for scion are fixed right they're they're not growing past a a certain limit whereas an unconstrained scion they are growing for let's say the first 3,000 steps and they're roughly growing like uh the square root of the number of steps. Okay. And it's only at around iteration 3,600 uh where we see the norm start to flatten out. And this is because we're using a stable decay learning rate. So we have a stable learning rate in the beginning with no warm-up. And this is where you see the norm growing and then once the decay kicks in, the norm starts to flatten out. Okay. So if we had used a constant uh step it would have kept increasing. So following this uh we tried to compare our algorithm to to atom and and to muon for training uh nano GPT architectures let's say and so we did it with a three billion parameter model and the idea of this experiment is that you want to see which optimizer can get below 2.9 validation loss uh the fastest where fastest is measured either in the number of steps or the amount of wall clock time. And what we see is that first all the spectral methods are are beating atom uh by quite a big margin and then that unconstrained scion and scion are both performing better than muon. And so this implementation of muon is taken directly uh from the modded nano GPT speedrun but it was taken when we were writing the paper which is back in November. So it's not the same code that's out there today but it was the state-of-the-art at the time when we ran the experiment. And I should also mention that muon here because it's the older version, it's using the Kimmy scaling that was originally used in the in the speedrun as well. And for scion and unconstrained scion, we're using the sign on the first and the last layers. So we're using the sign spectral sign configuration for the norm. uh another big difference between scion and muon and also uh between atom and spectral methods is the sensitivity to the batch size. So what we see is that actually atom is way more sensitive to the batch size than spectral methods. So in this experiment we have some fixed token budget and we look at what happens to the minimum validation loss as we increase the number of tokens in each batch. So when you have a fixed batch size and you start increasing uh sorry when you have a fixed token budget and you start increasing the batch size you're going to do less steps right less iterations and this heavily impacts the validation loss of atom uh muon in contrast to atom is much less sensitive but actually scion and unconstrained scion are even less sensitive than muon and this is not really a surprise right because muon even though it is a spectral method is using atom on the vector parameter Right? So it's not a pure method in this sense. It's sort of a Frankenstein method that's mixing spectral methods uh and and atom. And so we see at least in this context that Frank Wolf is outperforming Frankenstein. Okay. Moving on from from language tasks, we can look at at image tasks. So we trained an image transformer, something called a data efficient image transformer coming from from Meta or Facebook research. And so we're seeing that we're able to match the accuracy of of atom W with like much fewer number of epochs and also less wall clock time as well, even though that's not shown here. So the the target accuracy here is something like 71 or 72%. And so in case you're wondering like is Adam well tuned in this experiment, uh this is also the results that are in the the Diet repo itself. So we see that they have 72.2 two we're comparing with something a little bit less but for D at base we have 81.8 and we did the experiment also with D at base which has many more parameters and we there are getting to 81.8% 8% uh in much fewer epochs and also less wall clock time and the wall clock time could actually be improved. So this is not the best you could do like some operations in our experiments could be parallelized better and we could see an even bigger difference. So more important than than just beating atom though is the fact that in these experiments we also see hyperparameter transfer. So this is a different domain. We're not looking anymore at text, but we still have uh the optimal learning rate that's transferring across these these DIT models as you see in the plot on the right where the learning rate is on the the x- axis and the test accuracy is on the y- axis. So it works also for for training uh image transformers on imageet. We also did experiments with convolutional neural networks uh looking at them on on CR10 with a spectral spectral sign. Uh maybe this is something I should have mentioned. We took the spectral uh configuration because we're looking at uh image data, right? So we're not using sign spectral sign anymore. We use spectral spectral sign also for the convolutional setup. So here the notion of width is a bit different. It doesn't mean um the same thing. When we scale the width, we're actually adding filters to our our hidden layers. We're not making the filters bigger. This wouldn't really make sense. And we see that the the step size is transferring. Here are some hyperparameter sweeps that are testing the relationship between the momentum and the learning rate. So you see the momentum on the x- axis and the learning rate on the y. And what we can see from this, so so each column corresponds to a certain number of epochs of training. In the top row you have unconstrained scion. In the bottom row you have scion. And basically if you're doing longer training runs in this experiment it benefits you a lot to use a larger momentum. So typically in the text uh experiments that I showed you we were taking a momentum that was like 0.1 and here we're taking a momentum that's much larger closer to like 0.5. Okay. I want to finish with just some theoretical results. um to to talk about theoretical results we have to make a few assumptions about our our framework or our problem that we're going to solve. The main one is that we should have a lipshit smooth function. And so if I would have given this talk five years ago, uh I would have been in the audience saying ah this is a very unrealistic assumption. You have like relu activations which are non smooth. You have max pooling which is non smooth. And this is true. But in the modern paradigm with transformer networks, we typically have continuously differentiable activations. And if we restrict ourselves to a compact set like we do in scion then actually continuous differentiability is sufficient for lipshit smoothness. So we have this assumption automatically if the activations are continuously differentiable. The second assumption I'm going to make is that our stochastic oracle is unbiased and that it has bounded variance. This is also pretty questionable uh for in the sense of realism but it's a very common assumption in in stoastic optimization and we will also adopt it here. So with these assumptions we get uh the following first preliminary convergence results. So for these results we're going to take the step size to be dependent on the horizon the number of iterations that we will run but we will take the momentum to be independent of the horizon. And if you do this then the type of convergence result you get is not convergence to a first order critical point. Actually what you get is convergence only to a noise dominated region. So because uh the momentum doesn't go to zero and doesn't depend on the horizon. We can't kill this noise. So we get a noise dominated region for the unconstrained version uh for the gradient norm and for the constrained version uh if you're familiar with constrained optimization uh the the gradient norm is typically not going to zero right even if we approach uh a critical point. So the correct notion uh for constrained optimization is not the dual norm of the gradient it's rather something called the Frank wolf gap. So that's what you see in the expectation this inner product. This is called the Frankwolf gap and it's equal to zero if and only if we are at a first order critical point. Okay, for our constrained problem. So if we don't let the momentum go to zero, we don't get convergence really to a critical point. We get fast convergence but only to um a noise dominated region. If we allow the momentum actually to go to zero. Uh so if we have vanishing momentum or if we allow it to to be dependent on the horizon there's somehow an equivalence here between vanishing and depending on the horizon then we really get convergence to a first order critical point in expectation right so the expected uh dual norm of the gradient is going to zero uh in unconstrained stochastic conditional gradient and the frank wolf gap is going to zero in the stoastic conditional gradient algorithm with these parameter choices and something I really want to stress is that these uh parameter choices do not rely on any of the assumed parameters. So you don't see the lipshits constant appearing in these choices. You don't see uh sigma the bound from from the variance uh of our gradient estimator. Uh these are really like the best you can do if if you don't know this information. Okay. So several algorithms uh that you might already know are actually special cases uh of this stoastic conditional gradient framework uh if you take different choices of norms and momentum parameters. So if you pick your norm to just be the uklidian norm, we saw already that the LMO in that case is just going to divide by the norm. And so in this uh setting, if we pick the uklidian norm, we're going to get back normalized SGD. And if we take it with momentum that's uh that's not not equal to one then we're going to get back exactly uh normalized SGD with momentum. On the other hand, if we pick like the L infinity norm, we saw that the LMO is going to be the sign and in this case we'll recover sin SGD and sin SGD with momentum or sinum as some people call it. And if we ignore what happens to the vector parameters, right? If we only consider what happens uh to the matrix parameters, then muon is actually also a special case uh of scion where you take the operator norm or or the spectral norm to be uh your your norm for your layers. Okay. So actually we can unify the analysis of all these guys into one framework and some selected related work that I would like to to mention um first is is the stochastic spectral descent. So this is an old paper in deep learning terms. It's 10 years old now. Uh that was published by by my co-author Vulcan Shave in 2015 in AI stats. Uh which was actually coll-located that year with ICLR where Adam was presented. So in the same conference where where Adam was first presented also spectral descent this uh matrix sign algorithm was also first presented. And now 10 years later we're finally coming back around and looking at these methods in more detail. Uh, of course it's related to the FrankWolf algorithm as I already touched on, but also the stochastic frankwolf algorithm. Uh, there's this nice paper by Maktari and co-authors where they first used the momentum estimator with stoastic Frank Wolf. Uh, there's this work on on second order optimizers called shampoo which is actually very related to to spectral LMOS. If you disable the uh accumulation in shampoo then you get back uh the spectral LMO and so in this sense they're they're very related even though if you literally write out the updates of shampoo it's like atom it's an update with respect to some mahalobus norm so uklidian in some sense there's also the muon blog post uh so people ask me all the time what's the difference between scion and muon so the main differences are are how they treat the scaling factors in the spectral LMO the way that muon is doing things doesn't lead to to hyperparameter transfer in contrast let's say to scion and there's also this difference in what they do for the vector parameters so scion is typically taking the the vector sign for vector parameters while muon is doing atom which is not so different from the vector sign but it does use this on the-fly adaptation right so you have to keep track for instance of the second moment of these vector parameter gradients There's also the Kimmy Moonlight paper. Um the difference again with our work is that they're using like this muon with atom and they're also using a different scaling factor and then also these preconditioned SGD works uh and some other works on the duality structure of gradient descent are also related to to what we've done. So actually most of these methods can be uh categorized together in this matrix that you see on the right. So in Scion in the bottom right we are doing the matrix sign on the matrix parameters. So this is why we're in the right column we're not doing any kind of EMA or accumulating preconditioners like in shampoo and we're also doing the vector sign on the vector parameters. So this is why we're in the bottom row. We're not doing atom on the bottom uh sorry on the the vector parameters which would put us in the top row. In contrast, shampoo is doing accumulation on everything. Also, some variants of PSGD are doing something similar to this. And so this is why they're doing EMAs on both the vector sign and the matrix sign while muon is doing the vector sign with EMA. So this is atom, but it's doing the matrix sign without EMA. It's not accumulating left and right preconditioners like in shampoo. It's just computing the matrix sign at each iteration. So I think this is a good way to to organize these methods and how they relate to one another. And if you're interested in reading the paper that that spawns Scion, it's available on archive. It'll also be presented uh in like a week or so at at ITML where it was accepted. As I said before, uh if you want to give it a try, the the GitHub link is there. We have also another paper extending this to to L0 L1 smoothness using clipping uh that you can read if you're interested. But otherwise, I think maybe it's a good place to to stop.
