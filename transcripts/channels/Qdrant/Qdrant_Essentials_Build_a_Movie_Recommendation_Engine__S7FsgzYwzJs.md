# Qdrant Essentials | Build a Movie Recommendation Engine

- 영상 링크: https://www.youtube.com/watch?v=S7FsgzYwzJs
- 채널: Qdrant
- 업로드일: 2025-10-22
- 자막 언어: en
- 단어 수: 약 3290개

---

## 스크립트

All right, it's demo time for day one. And now we're building a simple movie search system for science fiction movies that basically works in a way that we send it a query like Alien Invasion and it retrieves back the most similar movie in my database to it. Um so we're basically also experimenting with the chunking methods that we just discussed in the last video and using um hugging phase for our embedding generation. Let's get started by just installing our dependencies here. And here we're having sentence transformers, which is the library that we're going to use by hugging face for our text embeddings. Those are the embeddings that we're going to use to generate the embeddings for our documents, the ones that will go into our database, right? And then transformers, which we're going to use to analyze the token count of our documents. Quadrant client for the Python client of Quadrant and also a few limit index dependencies that we're going to use for our chunking. So when we get there, I will explain a bit better. But basically, it's going to help us with the chunking methods that we're going to be applying and having giving us the methods that we need um to make things easier for us. Here we're using the sentence splitter and the semantic splitter node parser specifically by lama index for that and also the uh hugging face embeddings which is uh the library that we need to help the semantic splitter to analyze the chunks and split it semantically. Right? So we're importing that and after that we are initializing our text encoder. Basically uh the model that we're going to use by center transformers to create our embeddings. So we run that and the model that we're choosing here is the all mini LML L6V2. This is a very popular model and it produces 384 dimensional embeddings and this is different from the amount of token limit it has which is 256. That actually means that this model can handle up to 256 tokens per input. So it I want to chunk my data if it goes over that limit so I don't lose any meaning because this embedding model is based for 256 tokens. So if we want to check if our data pass that limit, we're going to have to use a tokenizer. And as we can see here below, we have the data set that we're going to be working with. This is our movie data set. It has around 15 science fiction movies. And here we have a few informations like name, description, author, and year. Now, the thing that we're going to want to embed is obviously the description because it has it is a place that has the most information um about our um movie that we want to create our embeddings from. But we can see here that these descriptions some of them are very long. Most of them are very very long. So most likely they are going to exceed the token limit of my embedding model. I specifically did this so we could use chunky methods. Right? So let's see by how much those descriptions actually in exceed the limit of our embedding model by using this auto tokenizer by transformers. Now we are using the same tokenizer that our encoder which is all mini LML L6V2 um is using to accurately generate and predict where should we um split our chunks right and here if we run that we're going to see that basically almost every movie except from Arrival exceeds the token limit. We can see here for example Xmachina uh has 386 total tokens and exceeds the limit by 300 130 tokens. That means that we can create one chunk that's 256 tokens and another chunk that is 130 tokens for example. Now next up here we're going to want to just set up quadrants. Since this is a very small data set we are just using the quadrant in memory mode. Uh this means that the data is only stored temporarily on RAM. We're not going to have to connect to uh anything outside of our system, but also that this memory is not persistent. And this is only good for demo and very small use cases. But this is what we have here. So we're going to be using that. Uh the collection name is my movies that we're going to be using. And after that, we're going to create and defining the collection that we are using here. The first thing that we do is to check if the collection my movies already exist and delete that just to avoid any conflicts. And then we're going to create uh this vector config. That is basically um setting up three named vectors. And you can see here that we are setting uh one named vector for each type of chunking method that we are using. And for example, if I have chunks that I created using fixed size chunking, then I'm going to add this to this named vector right here. Um, this is basically for us a way for us to experiment with named vectors. Um, and we're also using the same distance metric to compare um all of them inside which is cosign similarity. Right? So let's move forward and actually get into the implementing of our text chunks. And here we are setting the token limit that we discussed earlier. 256 is the maximum that we can uh have here. And we are defining three functions for each one of the methods that we're going to be using. So fixed size chunks, sentence splitter, semantic splitter. For sentence splitter and semantic splitter, like we mentioned uh before we are using lima index to help us with that. So sentence splitter we are using the uh the sentence splitter method by limit index setting the chunk size to our limit. We can just do this right here and having a chunk overlap of 40. U for the semantic splitter we are using the hugging face embedding. uh that's going to define the model that we're going to use to define the semantic splitting. And we're using the same model that we're using for the encoder here. If you want to learn more about these parameters and what they do and how to work with these uh methods in lema index, we're going to have the link for them in the description below as well. So, chunking strategies, I'm not going to go into them specifically because we already covered that in the last video, but there is a a quick overview here. um of each one of them and what they do and how they do it. Right after that we are going to get into the chunk embedding and uploading of our data itself. Here in this cell we are um for each one of our movies in our documents right for each doc in documents we are uh getting the description and chunking in different methods right then we're getting those chunks and uploading embedding them and uploading it to our vector database in the specific vector field that we defined for them. Right? We're also using uh a payload of chunking with a method alongside that we use for the chunking as well. And along with it in the payload, we're adding the whole movie information in this doc here which is basically the information that we defined here this whole thing is getting in there. So ear author the entire description of the movie and also the name. Uh we're also adding the chunk itself that was used uh for that embedding and uploading all that to our collection. So let's run that and see how many vectors we have in total. Oh, I got name. Oh, I forgot to run this out right here. Let's do that and try this again. And then let's see how many vectors we have in total after all the chunkings and um all the uploadings. Now this takes a little bit of time mostly because of the embedding generation that can take a little bit of time as well and also because of the semantic splitter that is using also uh an embedding model to generate the semantics and split our chunks. We're also using the method upload points uh to basically upload all of these points to quadrant in a very efficient way. So here you have uploaded 82 vectors to quadrant. So all these methods generated 82 vectors and now it's time to run our semantic search query uh to find the closest um vector in our collection to our query alien invasion like we mentioned earlier. And for that we need to also choose our chunky method. The chunky method that we want to use for that. In this one we're using the fixed method just to try it out with a limit of three. So we want three results. And we can just start by running that. Uh the query points method is going to return us something like this which is a little bit unreadable uh if you're you know if you don't want to keep scrolling. So let's try to put this in a little bit more readable uh way and then we can see first is annihilation with a score of 0.45 then 80 then arrival. Very cool. Now let's try to define a query helper function. So we can um basically search with multiple uh chunking strategies and see what are our results. Right? So let's define that. And now we can see a little bit better and compare better um what are the results that we're getting. We can already see here that uh searching alien invasion with the semantic chunking give us a different top k uh order than uh using the fixed chunking. After that, it's time to inspect. So like since we have different um orders and um our chunking methods are giving me different results, then let's see what are the chunks uh that's being retrieved in each one of these points and let's try to compare if it actually makes a difference uh between splitting in a fixed size or sentence size or uh semantic uh in our chunks. Right? So let's define a function to do that for us and let's try to search through all of our chunking methods using the query alien invasion. Right? So let's get the results for all of that. And now we can see all chunks that are being retrieved uh with our results. Now let's take some time to quickly analyze these chunks and what they um actually look like. We can see here that for fixedsize chunking um the first chunk here is already starting in the middle of a sentence. So it starts with a comma and goes into making the journey a symbolic mirror of their inner decay. This is clearly um myth thought and this is one of the downsides of using fixed size chunking. Even though um that happens, annihilation is still the first result for all three methods which is a good indicator in general. Um then we can see the sentence uh type chunking. Now we can definitely see the difference right here. Now this is definitely full sentences. Things make just a little bit more sense in general. But as our data grows bigger if you're looking for for example a book of 200 pages, you're going to notice this difference much more clearly. After that we have semantic chunking. And we can see it here that the chunk for annihilation is just uh different from uh the one from sentence chunking even though they are just similar in general. We can see here this is the same chunk of sorts right but um still this one for annihilation is a little bit different. So we have here each character is marked by loss of self harm making the journey a symbolic mirror of their inner decay. Now, this is a very interesting sentence and it does make more sense to start with that than to start with the film's nonlinear nar narrative and haunting imagery evoke themes of trauma, memory, and blurring the line between adaptation and annihilation. Even though this is a full sentence, it feels like it's starting in the middle of another thought that was um initiating the sentence, right? and this one um feels just a little bit more like it's the beginning of a sentence itself. Those are subtle things, but they can really improve the precision of your retrieval, especially if you're looking to smaller chunks. For example, if you we wanted to create chunks that are just, I don't know, 40 tokens, we can do that really quickly uh here to see uh if the difference is a little bit higher. So, let's try to change this max tokens here to 40. Create a new collection. Change max tokens to 40. Um, upload the points again. We're going to have more points, right? Now, because we're going to have more chunks, right? And then we can see um how much our queries differ from doing that. We have now 409 vectors with a token size limit of 40. uh it took a little bit more time to run the sound now because of the especially the semantic chunking takes a little bit more time to run and this is one of the downsides even though it's more semantically aligned and has more precision and so on. But here we can see the search um results being performed again and we can now see different orders um for our results. All right. Now, annihilation is second for fixed size chunking. And let's see how this impacted our queries overall. Right. Let's try to run these cells again. See what happens. And we can see that for uh semantic annihilation still the first one. So let's try to see here this result again right for fixed side chunking. Now we have a different um sort of result. We have a chunk that's much smaller here. And we can now clearly see that this is the middle of a thought, right? And it also ends in the middle of a thought as well. Same thing here. And same thing here. We can see this much more clearly. For sentence chunking, we can see here that things is starting to get a little bit weird, but not so much, right? We can see here it also ends at the end of a a full sentence. all three results. Now we can see here that for semantic chunking uh it's basically the same and the reason for that is because our token max token count doesn't really affect our semantic chunking. This is defined um by the semantic splitter uh function itself using the here it is the embedding model hugging face embodying models and transformers all mini LML6V2. This is what defines basically where our chunk will be split so that it also fits the the contact size of this model perfectly and it makes sense semantically. So we have here different results for both of these. ET now appears in the first place and for semantic alien invasion we have here um ET in the third place. If you've watched any of these movies, you're going to see that ET is not really a movie about alien invasion. Even though it's a story of ET um living and and being on Earth, it's not really an invasion per se. And here we can see that Annihilation is the one that makes a little bit more sense to be retrieved first. So, we can clearly see with 40 tokens that uh semenic chunking works the best way. However, if we use the full 256 tokens, we can see that the other chunky methods are actually closer to to having this um first place correct, right? So, it's um a little bit of a nuance there when you are choosing all of the these things, token limits and so on. So, you definitely want to experiment a little bit before you get your um search system out in production and make sure that you are optimizing for the results that you want. Right. Next up here that we're going to try out is to apply a filter. For example, if I only want movies um that are made in years greater or equal to 20,000. And we can apply this filter condition here with the key being year and the range being greater than uh 2000 or equal. The limit that we're going to set here is four. And let's see what we have here. We have four results. Annihilation, arrival, interstellar, and then arrival again. And interesting thing here is that arrival is retrieved back twice. one because one chunk had this score and another chunk had this score. Since I'm retrieving by chunk, quadrant is only taking into consideration the scores of the chunks and retrieving that back to me. But what if I only want one chunk to be considered per movie? For example, the highest scoring one. I only want different movies to be um retrieved back to me here. Uh I don't want arrival to appear twice, for example. So then we can use the method query points groups in quadrant to do that for us. Here we are using group by and selecting the field name and setting the group size to one. And that means that I only want maximum one result per field name. Right? So if I run this, I can see that I now have in fourth place Gravity and Arrival here only appears once with the highest score that it had. So now I have the top four movies that match my query alien invasion instead of the top four chunks that match my query aqu uh alien invasion based on the results retrieved back here which is a very interesting thing to have especially if you're trying to rank um for example into a movie recommendation system. You don't want movies there appearing twice just because it matches more than one thing within that. You only want that to appear once. So this is very useful when you're dealing with those types of systems. To wrap up day one, your pit stop project is to create a simple search engine that applies all these things that we just saw. Pick a data set of your choice, the embedding model from hugging face that you would like. A checking method of your choice. Obviously, you can always use this notebook as a guide. Um, and try to upload that to your database using filtering methods and also grouping by and see the results that you get. This is a very huge step into your search journey. And as we move forward, we're going to see how to scale that beyond simple small data sets into millions and millions of factors. Now, for day two, we're going to get into HNSW indexing and how it really works behind the scenes. Why is it important to set up your payload indexes and also how to tune your HNSW parameters to balance search speed with accuracy and memory usage and a lot more things that you can do uh with your HNSW indexing to improve your indexing speed which is a very very important part to settle the foundation for what's coming next. So see you in day two.
