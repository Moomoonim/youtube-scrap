# What is Multimodal RAG? Unlocking LLMs with Vector Databases

- 영상 링크: https://www.youtube.com/watch?v=anLahYrEFiQ
- 채널: IBM Technology
- 업로드일: 2026-02-16
- 자막 언어: ko
- 단어 수: 약 1296개

---

## 스크립트

최신 문서나 검색 결과와 같은 외부 정보를 가져와서 이를 대규모 언어 모델에 대한 쿼리의 일부로 보내고 싶다면 , 이를 위해 사용할 수 있는 기술이 있죠 ? 그것은 rag 또는 검색 증강 생성입니다. 응. 예를 들어, 제가 Rag를 이용해서 사내 고객 지원 챗봇을 개발한다고 가정해 보겠습니다 . 누군가 LLM에게 "최신 VPN 정책은 무엇인가요?"와 같은 질문을 하면 어떻게 될까요 ? 네, 그건 질의이고, 그 질의는 입력 프롬프트로 전송되어 저희의 대규모 언어 모델인 LLM으로 전달됩니다. 그리고 Rag가 이 부분에서 우리를 도와줄 수 있을 거예요 . 이 기능은 VPN에 대한 최신 정책 문서를 가져와서 해당 정책 문서에서 관련 단락만 입력 프롬프트에 삽입할 수 있습니다. 따라서 LLM 과정 학생은 정확한 답변을 할 수 있는 관련 정보를 가지고 있습니다 . 정확히. 하지만 실제 데이터는 텍스트만 있는 것이 아닙니다. 만약 해당 정책에 네트워크 다이어그램, 스크린샷, 스캔한 PDF 파일 등이 포함되어 있다면, 실제로 필요한 것은 멀티모달 래그(multimodal rag)입니다. 따라서 이 모델은 이미지 와 텍스트를 동시에 이해하고 검색할 수 있습니다. 오른쪽? 멀티모달은 단순히 텍스트와 이미지만을 의미하는 것이 아닙니다. 동영상이 있을 수도 있습니다. 오디오 파일일 수도 있고, 각각의 파일은 서로 다른 방식으로 작동할 것입니다. 텍스트는 덩어리로 묶고 인덱싱하기가 매우 쉽지만, 이미지, 비디오, 오디오는 별도의 전처리, 임베딩, 그리고 원래 질문과 다시 연결하는 방법이 필요합니다 . 그럼 멀티모달 래그를 LLM 솔루션에 통합하는 다양한 방법을 살펴보겠습니다. 자, 그럼 먼저 고전적인 래그(rag) 설정을 그려서 멀티모달이 어떻게 적용되는지 살펴보죠. 여기서부터 시작해 볼게요. 우리는 서류를 가지고 있습니다. 이것들은 VPM 정책 같은 것들뿐만 아니라 지식 기반 문서 같은 것들도 포함합니다. 응. 첫 번째 단계는 오프라인에서 진행될 예정입니다. 그래서 우리는 이 모든 문서를 가져와 임베딩 모델을 통해 처리할 것입니다. 해당 모델은 각 텍스트 덩어리를 벡터로 변환하는데, 이 벡터는 기본적으로 해당 덩어리의 의미를 나타내는 숫자들의 목록입니다. 오른쪽? 그런 다음 해당 벡터들을 데이터베이스, 구체적으로는 벡터 데이터베이스에 저장합니다. 텍스트 문서는 임베딩 모델로 전달되어 벡터로 변환된 다음 벡터 데이터베이스에 저장됩니다. 그게 오프라인 부분이죠, 그렇죠? 그럼 이제 사용자가 질문을 할 때 어떤 일이 발생하는지 살펴보겠습니다 . 사용자가 " 음, 뭐라고 물어보려고 했었지 ?" 같은 말을 입력합니다. 저희는 최신 VPN 정책이 무엇인지 알려드릴 거예요. 좋아요. 괜찮은. 자, 이것이 우리의 쿼리가 될 것이고, 이 쿼리가 입력 프롬프트에 들어갈 것입니다. 응. 그런 다음 해당 입력 프롬프트를 리트리버라는 구성 요소로 보냅니다. 이제 검색기는 동일한 임베딩 모델을 사용하여 해당 질문을 벡터로 변환한 다음 벡터 데이터베이스에서 가장 유사한 벡터를 찾습니다. 그러면 해당 벡터 데이터베이스는 원본 문서에서 가장 관련성이 높은 부분으로 간주되는 가장 일치하는 텍스트 덩어리를 반환합니다 . 네. 그런 다음 이러한 덩어리들을 묶어서 원래 쿼리 옆에 위치하는 컨텍스트 블록을 만듭니다 . 이제 rag의 컨텍스트와 쿼리를 모두 활용하여 입력 프롬프트를 구성했습니다. 그리고 그 결합된 프롬프트를 대규모 언어 모델에 입력하여, 해당 모델이 근거 있는 맥락을 활용해 답변을 생성합니다. 하지만 지금까지 공개된 문서들은 모두 텍스트로만 되어 있지 않나요? 이를 멀티모달로 만들기 위한 몇 가지 옵션이 있지만, 파이프라인을 크게 변경할 필요가 없는 가장 간단한 방법부터 시작해 보겠습니다. 네, 이 접근 방식에 어울리는 좋은 이름이 필요해요. 음, 어디 보자. 여기에 놓겠습니다. 모든 것을 텍스트로 변환하세요. 좋아요. 네, 좋네요. 좋아요. 자, 이 사용 사례에서는 이미지가 몇 장 있습니다. 이것들은 이미지일 수도 있고, 스크린샷일 수도 있으며, 캡션 생성 모델을 거쳐 텍스트 캡션으로 변환됩니다. 이미지를 설명하거나, 비디오 또는 오디오 파일이 있는 경우 음성- 텍스트 변환 서비스를 통해 녹취록을 만들 수 있습니다. 정확히. 일단 변환이 완료되면, 그것들은 사실상 더 많은 문서, 더 많은 텍스트일 뿐입니다. 그들은 완전히 똑같은 과정을 거치게 될 것입니다. 응. 그래서 멀티모달 래그에 대한 첫 번째 접근 방식은 기본적으로 모든 모달리티를 텍스트로 변환한 다음 일반 래그를 사용하는 것입니다. 이는 충분히 간단하지만 한 가지 함정이 있습니다. 그럼 해당 VPN 정책에 대한 네트워크 다이어그램을 생각해 보겠습니다 . 해당 이미지를 캡션 모델에 입력하면 " VPN 게이트웨이 및 이중화 링크가 있는 기업 네트워크 다이어그램"과 같은 결과가 나올 수 있습니다. 기본적으로 현재 상황을 대략적으로 설명하는 글인데 , 기술적으로는 정확한 내용입니다. 하지만 만약 원래 다이어그램에 기본 연결을 나타내는 빨간색 경로가 있고, 장애 조치를 위한 파란색 경로가 있는데, 설명에 그 내용이 명시적으로 나와 있지 않다면 어떨까요? 그러면 그러한 미묘한 뉘앙스가 사라지게 되죠. 자막을 만들면 그 정보 중 일부가 손실될 수 있습니다 . 모든 것을 텍스트화하는 접근 방식은 기본적인 의미론적 텍스트를 얻는 데는 훌륭하지만, 실제로 매우 중요할 수 있는 시각적 맥락과 공간적 관계를 많이 잃게 됩니다. 이것이 바로 우리가 하이브리드 멀티모달 랙이라고 부르는 두 번째 접근 방식으로 이어집니다 . 조쉬, 당신은 이름 짓는 데 정말 탁월하네요 . 그래서 여기서는 텍스트 기반 검색을 유지하지만, 다른 쪽에서는 모델을 업데이트합니다 . 오른쪽? 단순히 텍스트를 읽고 쓰는 일반적인 텍스트 기반 언어 모델(LLM) 대신, 텍스트, 이미지, 오디오, 그리고 어쩌면 비디오까지 입력으로 받아 함께 추론할 수 있는 멀티모달 언어 모델을 사용할 것입니다. 이제 검색 측면에서는 실제로 크게 달라지는 것은 없습니다. 아직 제출해야 할 문서들이 많이 남아있습니다 . 게다가 이미지와 스크린샷에 대한 캡션도 준비해야 합니다. 그리고 영상과 음성에 대한 대본도 있습니다. 그리고 그 모든 것은 텍스트입니다. 이 모든 것은 이전과 마찬가지로 임베딩 모델을 거칩니다. 그리고 그 임베딩 모델은 여러 개의 벡터를 생성하는데, 우리는 이 벡터들을 벡터 데이터베이스에 저장합니다 . 따라서 사용자가 최신 VPN 정책이 무엇인지 문의하면, 여기에 해당 입력란이 있고 검색 엔진은 텍스트 삽입, 단락, 캡션, 스크립트 등을 검색하여 가장 관련성이 높은 일치 항목을 찾습니다. 하지만 중요한 점은 원래의 확장되지 않은 데이터에 대한 포인터도 계속 유지한다는 것입니다. 따라서 캡션을 검색했는데 그 캡션이 다이어그램에서 나온 것이라면 , 그 캡션이 어떤 이미지에 속하는지 정확히 알 수 있습니다. 그리고 컨텍스트를 구축할 때, 단순히 캡션을 모델에 전달하는 것만이 아닙니다. 우리는 입력 프롬프트와 함께 컨텍스트 텍스트와 실제 이미지를 멀티모달 대규모 언어 모델에 전달합니다 . 응. 이제 LLM은 정책 단락을 읽고, 예를 들어 실제 네트워크 다이어그램에서 빨간색 기본 경로와 파란색 장애 조치 경로를 보면서 답변을 생성할 수 있습니다. 하지만 그 대가는 검색 성능이 여전히 텍스트 품질에 달려 있다는 점입니다. 자막과 대본은 애초에 원하는 이미지나 영상을 찾아낼 수 있을 만큼 충분히 좋아야 합니다. 따라서 하이브리드 멀티모달 rag에서는 검색이 텍스트를 기반으로 이루어지지만 , LLM은 답변을 생성할 때 여전히 원본의 텍스트가 아닌 데이터를 보고 추론할 수 있습니다 . 그래서 하이브리드 멀티모달 래그타임이 꽤 괜찮게 들리네요. 왜 이야기가 거기서 끝나지 않는 거죠? 음, 그건 우리가 여전히 텍스트 쪽에 모든 걸 걸고 있기 때문이죠. 캡션이나 녹취록의 품질이 낮으면 검색 엔진은 원하는 자료를 전혀 찾지 못할 수도 있습니다. 이 접근 방식은 중요한 신호가 주로 시각적이거나 텍스트 설명 으로 쉽게 표현할 수 없는 경우 여전히 어려움을 겪습니다 . 네, 시스템이 텍스트와 이미지를 진정으로 아우르는 검색을 하려면, 단순히 텍스트를 프록시로 사용하는 것이 아니라면, 더 많은 것이 필요합니다 . 그리고 바로 여기서 세 번째 접근 방식인 ' 완전 멀티모달 래그(full multimodal rag)'가 등장합니다. 이 이름들에서 뭔가 공통된 느낌이 드네요. 당신 말이 맞습니다. 그럼 이번에는 뭐가 다를까요? 완전한 멀티모달 환경에서는 텍스트 삽입만 있는 것이 아닙니다. 저희는 텍스트, 이미지, 오디오 등을 위한 인코더를 포함하는 멀티모달 임베딩 스택을 사용합니다. 그리고 이러한 모든 인코더는 공통 벡터 공간에 매핑되도록 훈련되거나 정렬되었습니다 . 네, 이거 정말 멋지네요. 자, 여기에 텍스트 문서들이 몇 개 있는데, 이 공유 공간을 사용할 수 있습니다. 이미지 파일이 몇 개 있습니다. 그들은 같은 것을 사용합니다. 그리고 나서, 예를 들어 오디오 같은 게 좀 있을 수도 있겠네요. 이 모든 것은 이 스택을 통해 여기에 들어갈 수 있습니다. 그리고 각각의 요소는 스택을 통과하면서 동일한 공간상의 벡터가 됩니다. 그리고 그 결과는 이전과 마찬가지로 하나의 큰 벡터 데이터베이스에 저장됩니다. 옳은. 응. 그래서 이제 해당 벡터 데이터베이스는 진정한 교차 검사(crossodal)를 갖추게 되었습니다. 일부 벡터는 텍스트 덩어리를 나타내고, 일부는 다이어그램을 나타내며, 일부는 데모 비디오의 프레임을 나타냅니다 . 그런 다음 사용자가 입력 프롬프트로 " 최신 VBM 정책이 무엇입니까?"라고 물으면 , 해당 질문은 리트리버로 전달되고 리트리버는 동일한 멀티모달 임베딩 모델을 사용합니다. 그리고 그 단일 쿼리 벡터를 통해 공유 공간에서 유사성 검색을 이용하여 정책 조항, 네트워크 다이어그램, 심지어 학습 비디오의 핵심 프레임까지 직접 검색할 수 있습니다 . 그래서 자막이나 녹취록의 품질 때문에 병목 현상이 발생하는 일은 더 이상 없다는 점이 좋지만 , 그 대가로 비용과 복잡성이 증가합니다. 강력한 멀티모달 인코더, 더 많은 연산 능력, 그리고 때로는 컨텍스트 창이 너무 커지지 않도록 상당히 영리한 요약 기능이 필요합니다 . 하지만 그 대가로 가장 탄탄한 기초를 얻게 됩니다. 이 시스템은 텍스트, 이미지 및 기타 양식을 훨씬 더 자연스러운 방식으로 검색하고 추론할 수 있습니다. 그러니까 이게 바로 완전한 멀티모달 래그입니다. 요약하자면, Textify Everything Rag는 모든 모달리티를 텍스트로 변환하고, Hybrid Multimodal Rag는 텍스트로 데이터를 가져오지만 멀티모달 LLM은 원본을 볼 수 있도록 합니다 . 응. 그리고 이 녀석처럼 완전한 멀티모달 rag를 사용하면 검색과 생성 모두 완전한 멀티모달이 됩니다.
