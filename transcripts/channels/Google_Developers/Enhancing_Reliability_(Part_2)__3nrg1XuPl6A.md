# Enhancing Reliability (Part 2)

- 영상 링크: https://www.youtube.com/watch?v=3nrg1XuPl6A
- 채널: Google Developers
- 업로드일: 2025-12-04
- 자막 언어: ko
- 단어 수: 약 1600개

---

## 스크립트

[음악] 안녕하세요, 저희가 돌아왔습니다. 안녕하세요, 저는 로버트 크로우입니다. 이번 영상은 두 편으로 구성된 검사 시리즈의 두 번째 편으로, 앞서 다룬 기본 사항을 바탕으로 값과 성능을 디버깅하는 고급 도구를 소개하고, 이 모든 것을 Flax와 Anx에 적용하는 방법을 보여드리겠습니다 . 지난 에피소드에서 우리는 표준 Python 어설션이 실패할 수 있는 Jax의 변형된 환경에서 디버깅의 핵심 과제를 살펴보았습니다. 우리는 검증 기능을 해결책으로 소개하면서, 형태 검증( assert shape) 및 유형 검증(assert type)과 같은 기본적인 검증 기능에 초점을 맞추고, 이러한 도구들이 Jax.jit 및 Jax.vmap, VMAP과 어떻게 원활하게 통합되는지 보여드림으로써 코드 내에서 데이터 유효성 검사의 강력한 기반을 구축할 수 있도록 했습니다 . 이제 이러한 기본 사항을 숙지했으므로 보다 고급 디버깅 시나리오를 다루고 검사 기능을 통해 견고한 Flax NX 모델을 구축하는 방법을 살펴보겠습니다. 자, 시작해 볼까요? 우리는 검사 기능이 도형과 유형을 검증하는 데 어떻게 도움이 되는지 살펴보았으며 , 이는 Jax의 추적 메커니즘과 잘 연동됩니다. 하지만 JIT 컴파일된 함수의 실행 중에 텐서 내부의 실제 값을 확인해야 한다면 어떻게 해야 할까요 ? 텐서 값을 기반으로 하는 표준 Python assert 문은 JIT 내부에서 예상대로 작동하지 않는 경우가 많습니다. 바로 이 부분에서 checks.exify가 중요한 역할을 합니다 . JIT 컴파일러의 핵심 과제는 데이터의 추상적인 표현을 사용하여 함수를 추적하고, 형태와 유형에 초점을 맞춰 최적화된 코드를 생성하는 것입니다. 함수 실행 중에 실제로 어떤 수치 값이 전달될지는 알지 못합니다 . 만약 모든 요소가 양수인지 확인하거나 합계가 조건을 만족하는지 확인하는 등 런타임 값에 의존하는 if 문이나 assert 같은 표준 파이썬 코드를 작성한다면, Jax는 일반적으로 추적 중에 이를 처리하지 못합니다. 실제 데이터를 얻기 전에 어떤 경로를 컴파일해야 하는지 알아야 합니다. 이로 인해 추적 오류가 발생하거나 예상치 못한 동작이 나타나는 경우가 많습니다. checks는 이 문제를 해결하기 위해 checkexify를 제공합니다 . 장식용으로 사용하시면 됩니다. 이는 기본적으로 해당 기능이 두 가지 다른 모드로 실행될 수 있도록 해줍니다. 첫째, Jax는 컴파일 과정에서 함수를 평소처럼 추적할 수 있도록 해주며, 구체적인 값에 의존하는 Python 검사는 대부분 무시합니다. 그리고 무엇보다 중요한 것은 컴파일된 함수가 실제 데이터로 실행될 때 Checksify를 사용하면 검사 시 어설션 문을 사용할 수 있다는 점입니다. 따라서 Jack의 배열 값(예: J&amp;P all 및 mean)을 기반으로 모든 트리가 닫혀 있는지 또는 트리가 유한한지 등을 주장하고, 조건이 충족되지 않으면 실행하고 잠재적으로 오류를 발생시킬 수 있습니다 . 이는 최적화 도구인 Jack의 실행 경로에서 잠시 벗어나 런타임에 Python 값 검사를 수행하는 것으로 생각하면 됩니다. 이는 추적 중에 작동하는 `checks.assert shape`와 같은 어설션과는 다르다는 점을 이해하는 것이 중요합니다. CXify는 값에 대한 검사를 허용하지만, 검증된 값에 대한 추가 작업으로 인해 잠재적인 오버헤드가 발생할 수 있습니다 . 그래서 주로 디버깅 도구로 사용됩니다. 실질적인 예를 들어보겠습니다. JIT 컴파일러가 실행 중인 함수 내부에서 단순 배열 x의 값이 유한한지 확인하고 있습니다. 하지만 새로운 NNx를 활용하면 이것이 얼마나 강력해질지 상상해 보세요 . NNX.module이 이제 파이 트리 구조를 가지므로, 전체 모델 객체를 checkexified 함수에 전달하고 checks.assert treeall finite 명령어를 사용하여 모든 매개변수 와 상태 변수에 대해 NaN 또는 무한대 값이 있는지 한 번에 확인할 수 있습니다. 이는 학습 중에 발생하는 기울기 폭발이나 기울기 소실 문제를 디버깅하는 데 매우 유용한 기능입니다 . checksify 어설션이 완료될 때까지 checks.block을 사용하는 것에 유의하십시오. 이는 일반적으로 필요한 사항입니다. Checkexify는 비동기 디스패치를 ​​지원합니다. 따라서 기본적으로 JIT 컴파일된 함수를 호출하면 실제 출력과 비동기 적으로 계산된 오류가 즉시 반환됩니다. 따라서 함수 호출 직후 오류를 확인하려면 실행이 완료될 때까지 대기해야 합니다. checkexify 검증이 완료될 때까지 블록하지 않으면 , 모든 오류는 이후 checkexified 함수 호출 시에 표시됩니다 . 따라서 Checkify는 특정 수치 값이 잘못되고 있다고 의심되는 변환 함수 내부의 까다로운 문제를 디버깅하는 데 매우 유용합니다 . 이 기능을 사용하면 중간 계산이 특정 기준을 충족하는지 확인하거나, 특정 값 조합으로 인해 발생하는 NaN 값을 검사하거나 , 테스트 중에 복잡한 알고리즘의 불변성을 검증할 수 있습니다. 하지만 이로 인해 런타임에 파이썬 코드가 다시 실행될 수 있으므로 상당한 성능 저하가 발생할 수 있다는 점을 명심하십시오. 최적화된 Jack의 코드를 중단 없이 실행하는 것보다 훨씬 느립니다. 따라서 Cexify는 개발 및 테스트 단계에서 디버깅 도구로만 사용하는 것이 강력히 권장됩니다. 문제가 해결되면 일반적으로 checks.exify 데코레이터와 해당 데코레이터가 활성화했던 특정 값 기반 어설 션 문을 제거하고, 보다 효율적인 검사를 사용하거나 가능하면 로직을 리팩토링해야 합니다. 이는 표준 검사 어설션을 보완하여 디버깅에 절대적으로 필요한 경우 런타임 값을 직접 검사할 수 있도록 해줍니다. 이제 Jax 컴파일과 관련된 성능 및 디버깅에 도움이 되는 다른 유형의 문제로 넘어가 보겠습니다 . Jax를 처음 접하는 사람들이 흔히 저지르는 실수 중 하나는 함수가 너무 자주 재컴파일되도록 의도치 않게 만드는 것입니다. Jax는 이를 감지하기 위해 assert max traces라는 도구를 제공합니다. assert max traces를 이해하려면 Jax.jit의 작동 방식을 간단히 다시 살펴볼 필요가 있습니다 . Jax는 특정 입력 형태 및 유형 집합을 가진 JIT 데코레이터가 적용된 함수를 처음 만날 때 추상적인 플레이스홀더를 사용하여 함수의 작동 방식을 추적합니다. 이 추적 정보는 가속기용으로 고도로 최적화된 코드로 컴파일됩니다 . 이 컴파일 과정은 다소 시간이 걸릴 수 있습니다. 무엇보다 중요한 것은 Jax가 컴파일된 코드를 캐시한다는 점입니다. 입력값의 형태와 유형 구조가 완전히 동일한 경우, 해당 함수를 다시 호출하면 Jax는 캐시된 컴파일된 코드를 재사용하므로 속도가 매우 빠릅니다. 하지만 입력값이 모양이나 유형 등 다른 구조를 가지고 함수를 호출하는 경우, Jax는 해당 새로운 구조에 맞춰 함수를 다시 추적하고 컴파일해야 합니다 . 이러한 유연성은 강력하지만, 의도치 않은 잦은 재컴파일은 주요 성능 병목 현상이 될 수 있습니다. 이는 학습 루프에서 배열 형태를 실수로 변경했거나 Jax가 정적 매개변수로 처리하는 동적 Python 값을 전달하는 등의 문제를 나타내는 경우가 많습니다. Jax.assert max traces는 이러한 문제를 포착하도록 설계되었습니다. 함수가 추적되거나 컴파일될 것으로 예상되는 최대 횟수를 지정합니다. Jax가 해당 제한을 초과하여 추적을 시도하면 검사에서 오류가 발생하여 예기치 않은 재컴파일에 대해 알려줍니다. 주로 개발 및 테스트 중에 함수가 효율적으로 컴파일되는지 확인하기 위해 사용됩니다 . 첫 번째 시나리오에서는 프로세스의 형태가 고정되어 있습니다. 우리는 jacks.jit을 사용하고, `static argums`를 사용하여 shape tupil 인수를 정적으로 표시합니다 . 이는 Jet에게 shape tupil이 변경되면 함수를 다시 컴파일해야 하지만, 배열 x의 값만 변경되고 shape는 그대로 유지되는 경우에는 컴파일하지 않아도 된다는 것을 알려줍니다. 그런 다음, 특정 정적 형태인 tubil을 사용하는 이 함수 구성이 한 번만 다시 컴파일될 것으로 예상한다는 내용을 명시하는 checks.assert max traces를 추가합니다. 동일한 고정된 형태로 두 번 호출해도 제대로 작동합니다. 첫 번째 호출이 컴파일을 시작합니다. 추적 횟수는 1회이고, 두 번째 시도에서는 캐시를 재사용합니다 . 두 번째 시나리오에서는 프로세스 동적 형태를 갖습니다 . 이번에는 정적인 것은 아무것도 표시하지 않습니다 . 먼저 2x2 배열을 사용하여 호출하면 하나의 추적이 시작됩니다. 그다음에는 3x3 배열을 사용하여 다시 호출합니다. 입력 형식이 변경되었기 때문에 Jax는 추적 및 재컴파일을 시도합니다. 하지만 assert max traces는 이 두 번째 트레이스를 감지하고, n이 1이라는 제한을 초과하는 것을 확인한 후 어설션 오류를 발생시킵니다. 이는 입력 형태 변경으로 인해 함수가 예기치 않게 다시 컴파일되고 있음을 즉시 알려줍니다. 그렇다면 `assert max traces`는 언제 사용해야 할까요 ? 네, 주로 개발 및 테스트 단계에서 그렇습니다. 학습 루프 의 주요 단계나 모델의 순방향 전달과 같은 핵심 JIT 컴파일 함수를 래핑하여 컴파일 중에 예상대로 동작하는지 확인하십시오. 또한 자동화된 테스트에서 리팩토링으로 인해 예상치 못한 재컴파일이 발생하지 않았는지 확인하는 데 매우 유용합니다. 어떤 기능이 갑자기 느려지는 이유를 디버깅할 때, 이 방법은 원인을 파악하는 데 도움이 될 수 있습니다 . 주요 이점은 성능 저하를 조기에 발견하고, Jax의 컴파일 모델과 입력의 정적 및 동적 특성을 더 잘 이해하며 , 형태 불안정성과 관련된 버그를 훨씬 더 빨리 찾을 수 있다는 것입니다. 이것은 주로 개발 및 디버깅을 위한 도구라는 점을 기억하세요. 일반적으로 최종 프로덕션 코드에서는 이러한 검사를 제거하거나 비활성화합니다. 약간의 모니터링 오버헤드가 불필요하기 때문입니다. 그리고 때로는 의도적으로 여러 가지 컴파일 경로를 처리하도록 코드를 설계할 수도 있습니다. 이제 체크무늬와 아마가 어떻게 함께 작용하는지 이야기해 보겠습니다. 신경망은 복잡한 데이터 파이프라인 덕분에 검증에 매우 적합한 대상입니다. 레이어 간 도형이 잘못되거나 예상치 못한 입력 형식이 제공되는 것은 유감스럽게도 흔히 발생하는 버그입니다. 검사 기능은 한 레이어의 출력이 다음 레이어로 올바르게 전달되는지, 초기 입력이 모델의 예상과 일치하는지 확인하는 데 도움이 되며, flax 및 anx 모듈 내에서 훈련 중에 활성화 또는 기울기에 NaN 또는 무한대와 같은 수치적 문제가 발생하는 것을 디버깅하는 데 도움이 됩니다. 런타임 어설션 검사를 배치하는 가장 효과적인 위치는 순방향 전달을 정의하는 dunder 호출 메서드 내부입니다. 시작 단계에서 입력값을 확인하고, 중간 계층의 출력을 검증하고, 최종 출력을 반환하기 전에 확인할 수 있습니다. 다음은 다층 퍼셉트론(MLP)을 위한 간단한 NNX 모듈입니다 . 이 예제에서는 dunder 초기화 내부의 검사에 대해 아무런 작업도 수행하지 않습니다 . dunder 호출 메서드 내부에서는 checks.assert rank를 사용하여 입력 X가 2D 배치 및 특징인지 확인합니다. 우리는 ` checks.assert access dimension`을 사용하여 특징 차원 축 1의 크기가 첫 번째 선형 레이어의 예상 입력 특징과 일치하는지 구체적으로 확인합니다 . 우리는 종류도 확인합니다. 순방향 전달 후, 두 번째 선형 레이어의 예상 출력 특징을 사용하여 출력 형태에 대해 유사한 검사를 수행합니다 . 이는 모델이 입력 및 출력 계약과 관련하여 올바르게 작동하도록 보장합니다. 우리는 수표가 제공하는 것의 극히 일부분만을 살펴봤을 뿐입니다. 검증 기능 외에도, 이 검사 기능은 특정 유형의 사용 중단 경고, 테스트 변형, 가짜 객체 추가, 심지어 멀티스레드 CPU를 사용하여 다중 장치 테스트 환경을 가장하는 기능 등 일반적인 경고를 추가할 수 있는 Jax 친화적인 데이터 클래스 구현 유틸리티를 제공합니다. 저희가 제공하는 코드 연습 문제 중 일부에서 멀티 디바이스 페이크를 사용하는 것을 보실 수 있을 겁니다. 결론적으로, check는 Jax를 사용하는 모든 사람, 특히 flax 및 anx를 사용하여 신경망과 같은 복잡한 시스템을 구축할 때 필수적인 라이브러리입니다. 이 프로그램의 검증 함수는 형태, 유형 및 수치적 안정성과 같은 데이터 속성을 검증하는 강력한 방법을 제공합니다. 무엇보다 중요한 것은 이러한 검사가 JIT 컴파일이나 가상 맵 실행이 적용된 함수 내부에서도 안정적으로 작동한다는 점이며, 이는 JAX 생태계의 주요 과제를 해결합니다 . NNX 모듈은 Jax PI 트리를 기반으로 하기 때문에 CEX의 트리 기반 어설션은 모델에서 직접 작동할 수 있어 네트워크의 전체 상태에 대한 정확성과 수치적 안정성을 손쉽게 검증할 수 있습니다. Flax NX 모듈과 학습 루프에 검사 기능을 통합하면 코드의 안정성이 향상되고 명확한 오류 메시지 덕분에 디버깅이 훨씬 쉬워지며 데이터에 대한 가정을 명확히 할 수 있습니다. JAX 애플리케이션을 신뢰할 수 있고 유지보수하기 쉽게 구축하려면 검사 기능을 도입하는 것이 매우 권장되는 모범 사례입니다 . 이 코딩 연습, 빠른 참조 문서 및 슬라이드를 통해 Jax와 전체 Jax AI 스택에 대해 자세히 알아볼 수 있습니다. Learning Jack 시리즈 전체를 시청하려면 YouTube 재생 목록을 확인하세요 . Jax 팬들이 디스코드에서 점점 늘어나고 있습니다. 여기 초대 링크가 있습니다. 다음은 Jax, Flax 및 Jax AI 스택에 대한 문서 링크입니다. 그리고 앞으로 방영될 에피소드에서는 Jax AI 스택의 나머지 부분에 대해 더 자세히 다룰 예정입니다 . 그러니 계속 시청해 주시고, 좋아요와 구독 버튼도 잊지 마세요. 곧 다시 만나요! [음악]
