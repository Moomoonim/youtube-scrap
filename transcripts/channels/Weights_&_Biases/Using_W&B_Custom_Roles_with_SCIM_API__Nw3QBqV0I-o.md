# Using W&B Custom Roles with SCIM API

- 영상 링크: https://www.youtube.com/watch?v=Nw3QBqV0I-o
- 채널: Weights & Biases
- 업로드일: 2025-02-12
- 자막 언어: ko
- 단어 수: 약 1092개

---

## 스크립트

[음악] 안녕하세요 여러분. Weights &amp; Biases의 사용자 지정 역할을 사용하는 방법을 보여주는 간단한 비디오를 녹화하고 싶었습니다. 사용자 지정 역할은 기존 멤버 또는 뷰어와 같은 역할에 추가 권한을 부여할 수 있는 엔터프라이즈급 기능입니다. 추가 권한을 부여한 후 저장하고, 나중에 팀 구성원에게 할당할 수 있는 새 역할로 생성하세요 . 역할 설정에 액세스하려면 오른쪽 상단 모서리에 있는 계정 아래의 설정을 누르세요. 그러면 바로 일반 설정 화면으로 이동하게 되는데, 여기에서 역할을 클릭하고 역할을 편집할 수 있습니다 . Weights &amp; Biases 전용 배포 환경을 사용 중인 경우, 조직 대시보드, 설정, 그리고 역할 순으로 이동하여 접근할 수 있습니다 . 완벽한. 자, 그럼 새로운 역할을 만들어 봅시다. ' 새 역할 생성' 버튼을 누르겠습니다. 우리는 그 역할에 ' 실행 삭제자'라는 이름을 붙여줄 겁니다. 아름다운. 설명: 실행 기록을 삭제할 수 있는 멤버입니다. 엄청난. 앞서 말씀드린 것처럼, 뷰어 권한 또는 역할의 멤버 권한 중 하나를 상속받을 수 있습니다. 이 경우에는 회원으로 표시하겠습니다. 우리는 멤버로부터 모든 권한을 상속받을 것입니다. 그리고 보시다시피, 여기에는 이 멤버가 현재 수행할 수 있는 작업 목록이 나와 있습니다. 즉, 아티팩트 생성, 아티팩트 삭제, 런치 에이전트 생성, 프로젝트 생성, 프로젝트 읽기 등이 있습니다. 하지만 보시다시피, 그들은 프로젝트를 삭제할 수 없습니다 . 그리고 실행 권한의 경우, 실행을 생성하고, 이동하고, 읽을 수는 있지만 삭제는 할 수 없습니다. 권한을 추가하려면 여기 검색창을 누르시면 됩니다. 그리고 삭제된 실행, 아, 죄송합니다. 프로젝트 삭제, 프로젝트 이름 변경 , 실행 중지 및 실행 권한 삭제 기능도 추가해 보겠습니다. 아주 멋진. 이제 이 모든 것들이 추가되었으니, 바로 옆에 쓰레기통이 있는 게 확실히 보이네요. 그런 다음 ' 역할 생성'을 누르시면 됩니다. 시원한. 자, 이제 현재 팀원들에게 이 역할을 할당하려면 팀 메뉴로 들어가서 사용자 목록을 확인하세요. 보시다시피 ' 팀 역할' 옆에 작은 드롭다운 화살표가 있습니다. 여기에서 방금 생성한 새 역할을 실제로 할당할 수 있습니다. 실행 삭제자를 보세요. 실행을 삭제할 수 있는 멤버입니다 . 엄청난. 그리고 지금은 사용자 실행 삭제 도구를 사용합니다. 이제부터 가중치 및 편향 API를 사용하여 이 작업을 자동화하는 방법을 살펴보겠습니다. 이렇게 하면 수동으로 작업할 필요 없이 Python SDK를 사용하여 간편하게 처리할 수 있습니다 . 그럼 이제 SCIM API 관련 문서를 살펴보겠습니다. 아주 멋진. 여기에서 SCIM API를 사용하여 사용자, 그룹 및 역할을 관리할 수 있음을 알 수 있습니다. 음, /scim/users나 /groups 같은 경로를 호출할 수도 있지만, 저희는 모든 역할 목록을 얻고 싶기 때문에 /roles 경로를 사용하겠습니다. 그러니까 API 호출을 하려면 제가 헤더에 넣어드린 것처럼 인증 정보가 필요하겠죠? 이 경우 인증 정보는 사용자 이름, 콜론, 비밀번호를 각각 base64로 인코딩한 다음, 헤더에 ' authentication'이라는 단어를 붙여서 입력하는 방식이 됩니다. 하지만 이 경우에는, 네, 비밀번호는 실제로 저희 Weights &amp; Biases API 키가 될 겁니다. 그럼 wandb.ai로 가보죠 . 자, 그럼 사용자 설정으로 들어가 볼까요? 맨 아래로 쭉 스크롤하세요. API 키를 공개해 주시면 이 데모만을 위한 새 키를 생성해 드리겠습니다. 또 하나 해야 할 일은 SCIM API 조직으로 들어가서 여러 조직에 소속되어 있는 경우 이 부분을 변경해야 한다는 것입니다. 기본 조직을 설정하세요. 이 경우에는 Artem Demos로 설정하겠습니다. 아주 멋진. 자 , 그럼 시작하겠습니다. 음, 먼저 요청 사항을 가져오는 것부터 시작할게요. 요청을 하나 할 거거든요. 저희는 base64 형식도 가져올 것입니다 . 엄청난. 음, 크리스는 벌써 우리가 여기서 뭘 할지 추측하고 있네요. 그는 추측을 꽤 잘한다. 자, 그럼 사용자 이름을 만들어 볼까요? 제 사용자 이름이 Artem이니까, 그 이름으로 'Artem'이라는 변수를 만들겠습니다 . API 키를 생성하겠습니다. 변수 API 키. 음, 그럼 저희 사용자 설정으로 들어가서 이 API 키를 복사하겠습니다. 그리고 여기에 붙여넣으세요. 저희 자격 증명은 방금 만든 사용자 이름, 콜론(:), 그리고 API 키로 구성됩니다. 완벽한. 그리고 나서 위에 있는 base64 라이브러리를 사용하여 이러한 자격 증명을 인코딩할 것입니다 . 음, 우리는 이것을 base64 인코딩이라고 부르겠습니다. 우리는 방금 위에서 생성한 자격 증명을 그렇게 부를 겁니다. 우리는 이것을 인코딩이라고 부르고, 그 다음 전체를 디코딩할 것입니다. 완벽한. 이제 헤더를 만들어 보겠습니다. 헤더는 문서 에 언급된 대로 권한 부여와 같습니다 . 엄청난. 이걸 F현으로 만들겠습니다. 기본 인증, 그리고 공백, 왜냐하면 그들이 원하는 형식이 바로 이거거든요. 그 다음에는 암호화된 비밀번호와 API 키를 입력합니다. 엄청난. 자, 기본적 으로 암호화된 자격 증명을 여기에 입력하겠습니다 . 아주 멋진. 앞서 제안하신 대로 응답 변수를 만들겠습니다. 저는 그냥 동의하겠습니다. 그리고 저희는 라이브러리에 요청을 보내고, 라이브러리가 저희를 위해 요청을 보내면 저희는 GET 요청을 보낼 것입니다. 이 경우 경로는 현재 접속 중인 호스트로 연결됩니다. 따라서 전용 클라우드 환경에서는 https://yourhost/scim/roles 와 같은 경로로 접속하게 됩니다. 이 경우에는 wandb.ai, 즉 wandb의 SaaS 제품에서 테스트 중이므로 api.wandb.ai/scim/roles를 호출합니다. 그다음 헤더를 포함시킵니다. 그럼 이제 반응이 어떤지 봅시다 . 아주 멋진. 이걸로 부르자. 그러니까, 명확하죠. 파이썬 get_roles.py. 완벽한. 삭제자 역할 목록이 출력됐는데, 우리가 만든 삭제자 역할도 포함되어 있죠 ? 실행 기록을 삭제할 수 있는 멤버 맞죠? 보시다시피, 이것이 바로 여기에 있는 설명입니다. 실행 기록을 삭제할 수 있는 멤버입니다. 그리고 현재 이 조직에서 사용 가능한 사용자 지정 역할은 이것 하나뿐이므로, 출력되는 역할은 삭제자 역할뿐입니다. 자 , 그럼 이제 SCIM API를 사용하여 사용자 지정 역할을 만들어 보겠습니다. 완벽한. 자, 그럼 get_roles.py에서 가져온 내용을 거의 다 붙여넣어 보겠습니다. 여전히 요청을 보내는 동일한 작업을 수행할 것이므로 requests 라이브러리를 가져옵니다. 음, 저희는 여전히 인코딩을 할 것이므로 base64 라이브러리를 가져오고, 동일한 사용자 이름, 동일한 API 키, 동일한 자격 증명, 동일한 인코딩을 사용합니다. 그 모든 것은 동일합니다. 하지만 여기서 달라질 점은 API 호출에 추가할 페이로드입니다. GET 요청에 대한 페이로드가 없었기 때문입니다. 자 , 그럼 API 호출을 제대로 하는 방법을 알아보기 위해 문서로 들어가서 사용자 지정 역할을 만드는 방법을 살펴보겠습니다. 엄청난. 여기서는 실제로 API 호출 방법을 보여주는 예시가 이미 나와 있습니다 . 완벽한. 그럼, 복사해서 여기에 붙여넣어 볼게요. 엄청난. 이름을 지어주자. 음, API가 생성한 역할이라고 하겠습니다. 그리고 설명: 이 역할은 API에 의해 생성되었습니다. 권한. 여기서 프로젝트 업데이트를 제공할 수 있습니다. 또한 권한을 부여할 수도 있습니다. 보시다시피 , 권한 목록이 있습니다. 자 , 이것은 목록의 한 항목입니다. 목록의 다음 항목은 다음과 같습니다 . 그러므로 항목 뒤에 쉼표를 넣어야 합니다 . 음, 이름을 지어줘야겠어요. 프로젝트 삭제도 가능하다고 말씀드리겠습니다. 엄청난. 그리고 그것은 또한 우리가 삭제 실행을 할 수도 있다는 것을 시사합니다. 물론이죠, 안 될 이유가 없죠. 좀 더 흥미진진하게 만들어 볼까요? 그럼 우리도 똑같이 해 봅시다. 아주 멋진. 그것이 암시하는 바는 다음과 같습니다. 좋아요. IDE에서 공백을 추가하라고 하는데, 문서에는 여기에 공백을 넣지 않는다고 나와 있어서 혹시 몰라서 공백을 삭제하겠습니다. 그리고 나서 GET 요청을 POST 요청으로 바꾸고 페이로드를 추가하겠습니다. POST 요청을 하려면 페이로드가 필요하기 때문입니다 . 시원한. 그리고 나서 응답을 상태 코드로 변경할 수도 있습니다. 응답 상태 코드. 어떤 결과가 나오는지 확인해 보세요 . 완벽한. 자, 그럼 파일을 실행해 봅시다. 파이썬 post_roles.py. 보세요, 해시할 수 없는 타입 딕셔너리입니다. 그들은 무엇에 대해 불평하고 있는 걸까요? 알겠어요 . 여기에 중괄호가 하나 더 있습니다. 다시 시도해 봅시다. 엄청난. 따라서 여기서 응답은 201이어야 합니다. 응답 예시로 상태 코드 201은 작업이 성공적으로 완료되었음을 의미합니다 . 정말 멋지네요. 자, 그럼 다시 우리 팀으로 돌아가서 가중치와 편향을 재검토하고, 설정과 역할로 들어가 보겠습니다. 그리고 여기에서 API 생성자 역할이 실제로 역할 목록에 나타나는 것을 볼 수 있습니다. 그리고 팀 메뉴의 사용자 목록에서 아유쉬에게 API 생성자 역할을 부여할 수도 있습니다. 더 궁금한 점이 있으시면 언제든지 편하게 문의해 주시고, 저희가 녹음해줬으면 하는 다른 내용이 있다면 알려주세요. [음악]
