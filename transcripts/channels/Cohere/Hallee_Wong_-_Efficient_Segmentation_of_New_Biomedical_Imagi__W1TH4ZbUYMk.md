# Hallee Wong - Efficient Segmentation of New Biomedical Imaging Datasets from Scratch

- 영상 링크: https://www.youtube.com/watch?v=W1TH4ZbUYMk
- 채널: Cohere
- 업로드일: 2025-11-14
- 자막 언어: en
- 단어 수: 약 6161개

---

## 스크립트

[music] All right. Um, hey everybody. We are back with another exciting guest speaker session. Um, for people who have joined us from at Coher community, feel free to check out our channel at ML Healthcare in the Discord channel for the Go AI community. Um, today we have Hi with us. Ali is a PhD candidate at CS sale at MIT advised by Dr. John Kak and Katrina Delka. Her research focuses on developing interactive and human centered AI system for medical imaging analysis. Um she's going to talk about efficient segmentation of new biomedical imaging data sets from scratch. That's an important problem in today's book and she's going to cover from basics to the advanc. Um thank you Hi for taking out time and speaking at today's sessions. We're super excited um to hear about that. &gt;&gt; Yeah, thank you so much for inviting me. Um so I'm excited to talk to you today about efficient segmentation of new biomedical imaging data sets from scratch. So image segmentation is a core part of biomedical image analysis. So specifically this process of analyzing an image and outlining different regions of interest. Here I have a bunch of examples of some of the different common uh modalities in medical imaging um and regions that people might be interested in segmenting. There's tons of different applications for segmentation in medical imaging. Um for example, in clinical workflows like radiotherapy planning and surgery and just about any research that involves imaging will often involve segmentation as a first or an important step in the image analysis pipeline. Um, more specifically, medical researchers are often defining new segmentation tasks. This is because they are either collecting new images, perhaps developing new image um, acquisition methods and modalities or defining new regions of interest. And so many research projects begin with manual segmentation. Unfortunately, this process of manually segmenting images is annotating by hand the boundaries of all these regions is really tedious and timeconuming. Um, and this is particularly true for biomedical images where it requires expert annotators to do the labeling. Um, and users like these researchers often want to segment not just a few images but maybe tens or hundreds or thousands of similar images in a consistent way. We can train deep learning models to automate some segmentation tasks, but that still requires um upfront manual annotation to create that training data. So if we want to segment a new data set, a standard approach might be to first annotate some images and create a small training set and then use that to fine-tune or train a deep learning model and then continue using that task specific model to segment the rest of the data. But this approach requires computational resources and machine learning expertise that's often not available to biomedical researchers. So with multiverse egg we introduce a new framework and system that enables users to efficiently segment new biomedical imaging data sets from scratch. This means without any previously labeled data. To use the system, users begin by specifying a few examples through interaction. The model then adapts based on those examples, learning from experience but without any retraining. And then as the user segments more images, the system gets better at the user's specific task. Um so that with enough examples, the model can then be used to segment the rest of the images automatically. Um to go into more detail about how we achieve this technically. Um, multiverse egg is a system that takes as input an image that you want to segment and then also user interactions like clicks or bounding boxes or scribbles can also be used iteratively. So the previous prediction is also an input. Um, what's new is that multiverse also has an input for a context set. So this set has example images and segmentation mass demonstrating the segmentation task that the user wants to perform. And so Multiverse can predict the segmentation of this new image conditioned on both these user interactions and also a set of example images and segmentation masks. So when applying multiverse there would be three phases to segmenting a new data set. The first part is interactive segmentation where we don't have any previously labeled data. So the user just provides interactions to label that first image. But as we start to um annotate some images and collect these examples um we move into interactive in context segmentation where the model is making predictions based on both interactions and uh context set inputs. Then the final phase of of data set annotation would be incontext segmentation where the model is only receiving context set inputs and the user doesn't provide any interactions and so we can segment images automatically. To put this more concretely, we can consider segmenting this data set of brain MRIs. Um perhaps I want to segment the grain the gray matter in all of these images. you'd start by taking the first image, providing interactions and getting a prediction. Um, but this prediction it's it's not getting the whole region. Um, so the user might also need to provide corrections and iterate with the model. Um, but once they're satisfied with the segmentation, it would then be added to the context set that the system is conditioned on. So for the next image in the data set that you're segmenting, um the model also gets information from the context set and the user doesn't have to provide quite as many interactions to get an accurate segmentation. As more images are segmented and the context set is populated, the model becomes better and better at the specific task. Um and with a large enough context set, we can perhaps even uh segment the rest of the images fully automatically without any user interaction. Um here's one of the results from our paper where we simulated segmenting um many different segmentation tasks and data sets. The top two lines at the top are static interactive segmentation models. So for those models you always have to do the same amount of work for every image in the data set. Um and with multiverse egg instead as you segment more images as we go sequentially through the data set the amount of work to achieve the same level of accuracy declines. So it becomes faster and faster to segment new images. Um so in this talk I'm going to uh cover two different works building up this system. Um first I'm going to talk about uh a previous paper scribble prompt where we focused on improving this interactive segmentation step and then in the later half the talk I'll talk about um our more recent paper multiverse egg where we introduced this whole system and created a model that addresses the second two phases. So within interactive segmentation there's a lot of challenges um and some of them are specific to medical imaging. Um [snorts] the first is that we want this entire system to be very userfriendly. Um and often different tasks uh it's easier to use different types of interactions. For example, if you're segmenting a large area, it may be easier to use a scribble or a bounding box. But if you're segmenting something thin like veins, um clicks are easier to use. And users often have different preferences. Um we also want the system to generalize to new uh t new segmentation tasks um not only uh new types of images but also new segmentation regions. We don't want our models to overfit. And lastly we want to minimize latency because our users may not have access to powerful GPUs to run this model. We ideally want it to be usable on a laptop. There's been a lot of recent work over the past few years in interactive segmentation. In particular, you may have heard of the segment anything model from Meta, which is really excellent on natural images. Unfortunately, it doesn't perform as well on many biomedical imaging tasks, especially ones with more subtle boundaries like the one um on the right side of the slide here. Um, additionally, it uses really powerful vision transformers that are not very computationally efficient. There's also been a lot of works fine-tuning and adapting SAM from medical images, but they often focus on the single prompt or single interaction type scenario. Um, and they don't generalize very well to unseen tasks or new scenarios. You might ask, you know, what what makes biomedical imaging different? Um, I think there's a lot of unique challenges in interactive segmentation of biomedical imaging. um for example the subtle boundaries that I mentioned before um but also in terms of data that's available for training and model development many public medical imaging data sets are small and quite specialized um and there's very diverse image acquisition methods that affects the distribution of images available so in this previous work scribble prompt um which was published at ECCV in 2024 we introduced a new system for interactive segmentation that's works with multiple types of interactions, clicks, bounding boxes, and scribbles. It's very responsive to user feedback. Um, it generalizes to unseen data sets and labels. And it also uses a lightweight CNN, so inference is fast, um, less than a third of a second on a single CPU. Um I'm going to give a brief overview of kind of the interesting parts of training scribble prompt and some key results um so that we can focus more of the talk on on multiverse. So like most interesting models the training strategy um is really key and I think this is you know particularly important for interactive models where we want humans to use them and um interact with them. we need to think about designing our training strategy to help these models learn to behave in a way that um will be user friendly. So with scribble prompt and other interactive segmentation methods at inference time the user would provide the interactions but during training we don't have access to this user. So instead we used algorithms to simulate these different types of interactions. And a key part of the work was coming up with new algorithms for simulating realistic scribbles. There were two main parts to our overall training strategy. The first was simulating diverse interactions which we did through different algorithms for simulating bounding boxes, scribbles and clicks and would also mix them during training. And then the second part was um training on diverse segmentation tasks which we did by building a really large collection of training data sets. This is something we've been working on over the course of several years and projects within our lab. And then very importantly we further augmented our data with synthetic labels. I think the synthetic label strategy is a particularly interesting part of the work. Um in medical imaging often there are small data sets where only one thing is labeled. So maybe um there's a bunch of image retinal images of the eye and they always label the veins. If we train our model on just that that data set uh it's very easy for these models to memorize every time I see a retinal image predict the veins. But that's not the behavior that we want for our downstream users. So during training we also introduced synthetic labels to basically force the model to pay attention to the user interactions as opposed to u memorizing certain segmentation labels for certain types of images. So we would generate synthetic labels by taking an image running a super pixel algorithm which clusters pixels with similar values and then we would consider each of these super pixels as a possible synthetic label and we would sample random labels during training. Um, we found that uh equal mix of real and synthetic labels actually led to the um best performance in scribble prompt. Um, and I think this was really key to making the model responsive to user feedback since I think a common failure case I've seen in SAM and some other models is sometimes for things it recognizes as objects, it's great at segmenting it very quickly, but if you try to segment something that's kind of out of training distribution, sometimes it will ignore your interactions. Here are some example predictions from our final system. Um so this first line is just prompting with scribbles. Uh and scribble prompt produces a much more accurate higher dice segmentation than these baseline methods. We have SAM as well as some medical variants and also some older interactive segmentation work. Um we also see that it performs well in scenarios where we simulate iterative interactions. In the middle we're simulating five clicks and in the bottom we're simulating a couple rounds of scribbling. Um in this work we did a lot of evaluations. We collected a data set of manual scribbles. We also simulate did experiments simulating interactions. Um and we did a user study as well. For the sake of time, I'm just going to highlight the results of the user study. Um so in this user study, we compared Scribble Prompt to SAM. SAM was actually the strongest baseline in our experiments with the manual and simulated interactions. Um this was in terms of generalization performance. We had expert annotators uh perform segmentation uh segmentation tasks from our evaluation data sets. Um and they produced not only more accurate segmentations but also spent less time per image using scroll prompt. Um, and all of our uh participants in the user study reported that it was easier to achieve the desired segmentation when using scribble prompt. So in summary, scribble prompt enables users to segment new regions of individual images. Um, and I think a theme throughout this talk is that uh success relies on set of careful design decisions focused on ease of use and practicality. So now we have a system that covers the first phase of this data set segmentation process. Um multiverse egg the framework captures this um this entire process but we still need um models that can handle these other two scenarios. So now I'm going to talk about how we train that system. So similar to scribble prompt to train the multiverse network we simulate interactive segmentation and multiple rounds of the iterative process. We use the same interaction simulator to generate the scribbles, bounding boxes and clicks during training. But multiverse has this additional input of the context set. [snorts] During training, we sample variable size context sets up to 64. Um and that max size was just based what based on what we could fit on our GPU. Uh when the model is deployed, we won't have the interaction simulator and the user would provide the interactions. So the multiverse egg architecture has a unit-like structure and it takes this input this information about the target image that we're currently trying to segment and also the context set which contains previously segmented images. Um in this unit like structure we have a cross block which shares information between the target image inputs and the context inputs and we do this sharing at multiple depths throughout the network. So at different image scales inside the cross block we have the mechanism that share this. We have the mechanism that shares information between the target features and the context features. So the first step is that we concatenate the target features with each of the entries in the context um the cont the set of context features and then we perform a con convolutions on each pair. Then we average across the context set and concatenate the result with the target features and perform another convolution to produce our updated target features. We also perform another convolution on each of the context set features to get updated context set features. Um as with scribble prompt we also use a large collection of biomedical imaging data sets for training and evaluation. We partitioned our data sets into training data sets which were seen by the model during training and then evaluation data sets which we use in our results. Within the evaluation data sets, there were nine validation data sets which were split into validation and test data. The validation data from the validation data sets was used for things like hyperparameter tuning and model selection um because we wanted to make all of our model development decisions to optimize for performance on data sets not seen during training. But then in our final evaluations, we use unseen data from the test split of this validation these validation data sets as well as test data from these test data sets that were completely held out from the entire research process. Um, building off of our work with semiynthetic labels in Scribble Prompt, we also use semiynthetic data for training multiverse egg. So in scribble prompt we introduced this method for generating a single synthetic label from from an image. Um but the challenge in multiverse egg is that we need sets of images with consistent labels. And if you applied this process to a bunch of chesscts there's no guarantee that you would get super pixels with semantic consistency. So to address this we would take the image in synthetic label and duplicate it and augment aggressively many times. So this way we would get a set of um semi-ynthetic images and synthetic labels that have cons some semantic consistency across them. Okay. So the last part is now how do we we have this entire multiverse system let's evaluate it. I first want to show some qualitative examples that I think help illustrate the advantage of this approach. So the top row here is scribble prompt. Um all of these models we prompted with the same single click and they also have a context set of 10 examples available. So scribble prompt um is only able to get one component of the target segmentation in this CT because it just has one click. Um this is actually this is the aorta we're trying to segment in the slice. It has two parts because it's curving kind of around and going into the heart. Um but and then the second row here we also explored what would happen if you try to combine two off-the-shelf foundation models. So our scribble prompt model and universe is another in context segmentation model also developed by our group. Um so for example we use universe egg to segment based on those 10 context examples and then we use scribble prompt to correct it with that one click. So the segmentation it's better than just the interaction on its own. Um but multiverse is able to segment this region even more accurately because it has learned to integrate information from the context set and the click simultaneously. Um, and we see similar trends across a variety of different image modalities and segmentation tasks. Um, all of these examples as well as all the results I'm presenting here are on our evaluation data sets that were unseen by the model during training. Um, I think an interesting area of analysis with multiverse egg is um what's the relative value of different types of inputs. So here we explored um the value of different context set sizes versus adding more interactions on the dice score of the segmentation. Basically the quality of the segmentation. Um I think the main takeaway here is that doubling the context size from one to two has a much bigger effect versus going from 32 to 64 examples. So there are diminishing returns in scaling up the context size. Um and often to get a very high degree of accuracy providing more user interactions is valuable. I think also another interesting question that we explored here is how do these interactive models compare to fully automatic models? To answer this, we train several hundred fully automatic models on our evaluation data. ly using all of the data that we had available using NNU net which is a popular AutoML pipeline for medical image segmentation. Um and so you can see that very quickly when you add more interactions multiverse is able to produce more accurate segmentations than an automatic model trained on these evaluation tasks. Um and so I think this kind of shows the value of why these interactive systems are important um to enable users to segment at with a high degree of accuracy. Our main evaluation of multiverse egg though was simulating the process of sequentially segmenting unseen data sets. Um, so this is similar, this plot is it's similar to a results plot I showed at the very beginning where the yaxis is the number of clicks per image to get to 90 dice. So lower is better. The blue line is scribble prompt. You have to do the same amount of interaction for every image in the data set. Green is where we combine scribble prompt and universe egg. Um, number of interactions is declining but more slowly. And with multiverse eggg there's a more rapid decline in the number of interactions you have to do to achieve this degree of accuracy. Say these results are averaged over many tasks. So there is a lot of variability um between tasks. For some tasks you may only need two images before you go to zero interactions. Whereas for others that are more challenging it may take many more. [snorts] Um oh seems like my lost my animation there. It's okay. Um, so I think another interesting question um is how does this multiverse egg approach compare to that standard approach I introduced at the beginning of fshot fine-tuning um of your model. [snorts] So we explored this with scribble prompt where we took scribble prompt and we simulate the process of segmenting five images and then we fine-tune scribble prompt on those five images and continued segmenting the rest of the data set. Um in generally this improved uh scribble prompt performance so you had to do fewer interactions in those subsequent images. Um, I think another overall takeaway from this plot is that there's a lot of variability in the number of interactions you have to do to segment different data sets depending on the difficulty of the task. Um, although fine-tune scroll prompt was better than without fine-tuning, it requires um a lot of time training models. Each of those models that we fine-tuned took 20 minutes on an A100. Uh in comparison, multiverse egg in orange here didn't require any training at inference time. Um so on the order of milliseconds to do the forward prediction and in the majority of cases requires fewer interactions to segment the same data sets. So in conclusion with both of these systems um we made a lot of careful design decisions focused on creating uh a very userfriendly and practical system for segmentation of new data sets. Um to build up multiverse, I talked about work from two papers. Scribble prompt um a model that enables segmentation of individual images um and then multiverse egg a more a complete system for segmenting new data sets. Uh I'd like to acknowledge u my co-authors on all of these papers as well as our funding and compute sources. Um that's the end of my talk. Uh if you're interested in learning more about these papers, both of the projects have websites is the name of the model.edu where there's code and demo and model weights that you can explore. Um thank you for listening and I'm happy to take any questions. &gt;&gt; Thank you so much Ali for presenting. Um this is an amazing idea. I think uh um any anyone from our audience if you have any questions please um let us know or I can start. Um I do have a couple of questions. Um so like I like how you have added like the in context images for the segmentation task. Um were you like able to compare it with like Dino V3 or like met Saman to like auto segment like to have like um like compare those results with that like yeah that would be interesting like to um compare with those &gt;&gt; um I guess for something like Dino I think you would still have to you'd still have to train adapters um to fine-tune the models and I guess that would still be kind of similar approach like alternative to automatic segmentation. I think one thing we found with some models like for example like MedSAM where you can only prompt with a bounding box, it just wasn't possible to reach 90 dice in like the majority of cases. Um so I guess our main kind of comparison was to NNUT because like that is kind of the state-of-the-art in many cases for automatic um medical image segmentation. Um I I have not tried we evaluated um a lot of like fine-tuned versions of SAM one for medical images. Um I haven't extensively evaluated the cases where people have fine-tuned SAM 2. Um, we'll also say a limitation. A lot of this work is focused on 2D medical images. And so a lot of the papers on SAM 2, they're they're focused on adapting for 3D medical images. And so if they didn't also try to keep 2D um capabilities or training uh images in their in their training data, I wouldn't expect them to perform well on 2D data. &gt;&gt; Yeah. Also like it would be interesting if if you could add like text for supervision, right? So for example, you have an X-ray, you have a radiology report with that, right? And if you're feeding images along with the text of that reports, uh would it occur in a better supervision or something like that? &gt;&gt; Yeah, I think um text prompted segmentation I guess it's not exactly what you're talking but I guess text prompted segmentation I feel like yeah is an area that a lot of people are interested in. Um, I'm I think there's, you know, I think there's a lot of challenges with doing stuff with text and images in medical imaging due to lack of text data. Um, and I think, yeah, there's a lot of technical challenges in how to augment your text data so you don't just overfit. Um, I feel like a lot of models, they tend to just memorize key words. And so, I'm not sure it's really helping you to memorize like maybe something like liver. We have tons of of images with the liver label. It's probably the most common label in our collection. like just memorizing this association between the the text liver label and the segmentation. I'm not sure it really helps the models generalize. Um, and so I think sometimes training without text data makes it easier for the models to like you can learn to segment things that you can't describe well in text or don't are not not like objects basically. &gt;&gt; Yeah. Even I think like um for like if if we kind of transition or extend this work to like 3D, right? Um, so for example, it could be a really good application like loto CT scans, right? Where you you don't have a clear CT, but you still need to like somehow like segmented out, right? It would be an interesting application there as well. &gt;&gt; Yep. Um, anybody else? Any other questions? Yeah, sure. Dan, go ahead. &gt;&gt; Can you hear me? &gt;&gt; Yeah, we can. Um I have a question about the workflow of using motive vers you know as an end user let's say I have a segmentation mask let's say if I'm dealing with brain images something that I got from free surfer right and let's say I want to extend that and divide an ROI into two right but maybe I want to do this for three four images and not for my entire data set is there a way that I could adapt motive to do this let's say if I have a multiclass segmentation that I already you know existing from free surfer or from some older data set and I just want to kind of interactively fine-tune this. You know the free surfer is not going to give me more ROIs and a new model would still require me to annotate a lot of data manually. &gt;&gt; Yeah. What are your thoughts on on these sorts of problems? Um yeah, I think I think the workflow would still be um kind of [clears throat] the same as what I was talking about where um see if you don't have like if you don't have any data previously segmented I guess um like maybe what you're kind of asking about like oh could you use these examples of like the larger segmentation regions in the context set but I think that's going to &gt;&gt; and now let's say that I've segmented all of gray matter right and the multiverse sag after a couple of examples can segment gray matter And then the next step actually want to segment left hemisphere and right hemisphere. I already have the gray matter segmentation. I want to kind of update that. &gt;&gt; Yeah. Further subdivided. Yeah. I guess this Yeah, this current system Yeah. wouldn't really help you with that particular case. Although you might be able to, you know, if you have you have segmentation of the whole gray matter area, you could use that to like crop the images and that may make your like the performance of the models better by just like you're already limiting the region to predict on. Um but I guess in this current pipeline I don't think it would be helpful to provide a context set of a different segmentation task um that that yeah currently trying to &gt;&gt; would you then like um if I I don't know if I understand this correctly but I if I were to download your demo and get the weights I could use this for some example and then those weights would not be updated. Where is that state that information that improves as I mention that I have I get a new set of weights? &gt;&gt; Yeah. So the weight the weights are not changing at all. Um it's just through this context set is bas it's like it's an input to the model like our prompt to the model. Um yeah so it's kind of like this whole in context terminology. It's I think it comes from like in context prompting of of LLMs like where you provide a text example of something and then ask it to do repeat that on some new thing. And so these models are kind of behaving in a similar way where we're not changing the weights of the model. We're just uh providing different inputs. Um so the oh and I guess actually another thing I didn't mention earlier this architecture is flexible to different context set sizes because like we're using the same com on different inputs in the context set um the architecture can take a variable size context sets the weights are not changing just the input &gt;&gt; but then if you switch from one image to the other how do you carry over the context that you've learned from your previous example or &gt;&gt; oh you would just um add it to the list of inputs going into the model. &gt;&gt; Okay. So, literally like an LLM, you're kind of just vending. Okay, cool. &gt;&gt; Yeah. Um, yeah, basic. Yeah, you would you're segmenting an image and when you're done segmenting it, yeah, maybe download it and add it to some folder that's where the inputs to the model are being loaded. Um, yeah, we have Yeah, we have um uh there's a plugin in 3D Slicer right now that was developed by Kitwear. um for multiverse egg um where yeah basically has this kind of UI that um right now it's just 2D so you're segmenting slices and then you say I'm done with the slice you can add it to the context and keep working on more slices &gt;&gt; interesting &gt;&gt; thank you Daniel thank you any any other questions &gt;&gt; maybe one more question &gt;&gt; sure for tasks where let's say the image doesn't have too much contrast, right? Let's say you're not looking at gray matter, but you're looking at the visual system and you want to segment V1, V2, V3, and those boundaries are not super clear. Maybe you have three, four different data sets, one with a class where you kind of have one region. Is there a way to combine all of that information and and what would you have recommendations of a workflow? where I would just start scribbling like as a user to you know get the maybe there's some learning too as a user that I can minimize the clicks and scribbles by knowing how the algorithm performs best. &gt;&gt; Yeah. Um I guess was the first part that you were mentioning that this data might be multimodal. &gt;&gt; Yeah. Let's say let's say that I um I have you know an age range like you're showing here, right? that I want to segment in the visual cortex v1 and v2 where there's no clear boundary in this image but I have a separate image that maybe I can look at you know on the side of the screen or I don't know if I could load it in somehow as a context but I want to kind of scribble there I was only for these kinds of task where I could see the interaction being extremely valuable right because it may change from really from data set to data set there's no baseline network that would ever &gt;&gt; predict that sufficiently accurately. Um what would be would would you have you guys thought of these problems of Yeah. in any way like combining this multimodto data or &gt;&gt; Yeah. I I think yeah &gt;&gt; scribbling. &gt;&gt; Oh yeah. I I mean I definitely think that's like one future direction is um yeah both of these models just it's a single channel input but you could easily extend that to take multiple channels. I think an interesting technical thing would be like can we make a model that flexible to different numbers of of channels because maybe some data sets people only have one modality others like neuro imaging they often have multiple um yeah I guess right now we've only been evaluating on individual modalities um and I think yeah I think that's scribble prompt one of you know the things about it I think that makes it useful is that it's very responsive to user feedback back because of this training on synthetic labels. These synthetic labels, they're kind of noisy, but they really force them all to not memorize what to segment and it has to try to segment based on the user interaction. [clears throat] Um, so I think that sometimes like scroll prompt doesn't do as well as like meds or the some of the fine-tuned SAMs at doing the thing where it's like you click on the lung and it does the whole lung. But um this leads to more flexibility that it's it's more designed for like iterative refinement um which I think makes it more successful on diverse image modalities. Um but I think we're still kind of in the process of trying to deploy these models like Scribble Prompt is in the dev version of Freeview. Do you use that? Um and yeah, continue to try to refine them based on user feedback. [clears throat] Oh, thank you so much. &gt;&gt; Thank you. &gt;&gt; Um, any other questions we have here? &gt;&gt; All right. Um, if not, um, let's wrap it up. Uh, thank you so much, Hi, for taking our time and giving this wonderful talk. Um, for people who want to reach out to Holly, um, Holly, do do you have any contact information that they can reach out to? Um, oh, I guess you can always send, let's see, I'm trying to get back to the the end slide. Like we have, um, there's project pages for for both of the projects with links to GitHub. You can always open an issue on GitHub and you can also email me directly. My email is just my first name.edu. &gt;&gt; All right. Thank you so much. All right, everybody. Thank you so much. Uh, see you next week with another speaker. Thank you. &gt;&gt; Byebye. &gt;&gt; Thank you. [music] &gt;&gt; [music]
