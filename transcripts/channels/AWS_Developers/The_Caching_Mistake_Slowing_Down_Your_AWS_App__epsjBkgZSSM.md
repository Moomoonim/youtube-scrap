# The Caching Mistake Slowing Down Your AWS App

- 영상 링크: https://www.youtube.com/watch?v=epsjBkgZSSM
- 채널: AWS Developers
- 업로드일: 2026-03-24
- 자막 언어: ko
- 단어 수: 약 2792개

---

## 스크립트

데이터베이스는 상대적으로 속도가 느립니다. 이런 종류의 Rust로 실행되는 데이터베이스는 상대적으로 속도가 느립니다. 메모리 내 데이터베이스는 그렇지 않습니다. 아주 오래전, 몇몇 개발자들이 왜 회전식 Rust 데이터베이스 앞에 인메모리 데이터베이스를 배치하여 한 자릿수 밀리초의 지연 시간이라는 이상적인 결과를 얻을 수 없을까 하는 생각을 했습니다. 데이터베이스가 로드되어 데이터를 가져올 때까지 기다리는 대신, 특정 항목은 훨씬 더 빠른 방식으로 복원하거나 가져올 수 있습니다. [음악] 그게 바로 RAM입니다. 여러분, 캐싱에 대해 이야기해 봅시다. 그게 뭐죠? 이것이 당신에게 어떻게 적용되나요? 어떻게 구현하나요 ? 특정 캐싱 전략. Valkey란 무엇이며 , AWS에서 어떻게 작동하나요 ? 데이터베이스와 캐시라는 비유를 계속 사용해서 이것이 실제로 어떻게 작동하는지 실질적으로 설명해 드리겠습니다. 자, 여러분의 애플리케이션이 사용자에 대한 일부 데이터를 요청해야 한다고 가정해 봅시다. 이것은 어떻게 작동하는 건가요? 애플리케이션은 먼저 캐시를 확인해야 합니다. 데이터가 캐시에 없으면, 즉 캐시 미스가 발생하면 데이터베이스에서 데이터를 가져옵니다. 데이터베이스 내부에서는 이 과정이 좀 더 느리고 시간이 조금 더 걸립니다. 실제로 데이터를 추출한 다음 캐시에 기록하는 과정입니다. 그렇게 하는 이유는 해당 데이터가 다음 읽기 작업에서 사용 가능하도록 하기 위함입니다 . 캐시에 데이터를 기록한 다음 해당 데이터를 애플리케이션으로 반환합니다 . 그러니까, 다시 말하지만 속도는 느려지지만, 다음에 똑같은 데이터를 요청할 때는 캐시에 저장된 데이터를 찾아서 반환받게 되는 거죠. 데이터베이스의 데이터를 캐시에 저장할 때, 데이터의 수명 기간을 설정하기 위해 TTL(Time To Live, 시간 제한)이라는 것도 설정합니다 . 우리는 데이터가 오래된 것이 되는 것을 원하지 않습니다. 온갖 문제가 발생할 수 있으니까요. 따라서 이러한 캐시를 사용하는 다양한 방법, 다양한 전략이 있습니다 . 그럼 지금부터 그들에 대해 이야기해 보겠습니다. 가장 흔하고, 여러분도 저도 사용할 가능성이 높은 첫 번째 캐싱 전략에 대해 이야기해 보겠습니다. 바로 캐시 어 사이드(cache aside) 또는 지연 로딩(lazy loading)이라고 하는 전략입니다. 이 캐싱 전략에서는 기본적으로 애플리케이션이 모든 캐싱을 처리합니다. 즉, 캐시에서 읽는 것부터 캐시에 쓰는 것까지 모든 과정이 애플리케이션에서 처리된다는 뜻입니다. 그리고 이는 제가 처음에 제시했던 예시와 매우 유사합니다 . 기본적으로 애플리케이션이 특정 데이터를 필요로 하면 캐시에 접근하여 해당 데이터가 캐시에 있는지 확인하고 , 없으면 캐시 미스를 반환합니다. 그런 다음 데이터베이스에서 여러 항목을 확인합니다. 데이터베이스에서 데이터를 가져오면 (당연히 그래야 하죠), 지정된 TTL(Time To Live, 보존 시간)을 설정하여 캐시에 저장한 다음, 해당 데이터를 애플리케이션으로 반환하여 애플리케이션이 원하는 작업을 수행할 수 있도록 합니다. 그게 다예요. 아주 간단하죠 . 아주 훌륭한 데이터베이스입니다. 이 제품에는 정말 좋은 점이 몇 가지 있습니다. 첫째, 이 데이터베이스에는 필요한 데이터만 저장합니다. 즉, 이 저장소 에는 지속적으로 접근되는 데이터만 저장된다는 뜻입니다 . 따라서 데이터베이스를 미리 채울 필요가 없습니다. 두 번째로 , 매우 회복력이 뛰어납니다. 캐시가 다운되더라도 괜찮습니다. 여러분의 애플리케이션은 데이터베이스에서 데이터를 읽어오는 역할만 할 것입니다 . 본질적으로 매우 간단한 접근 방식이며, 필요한 기능을 수행하는 데 있어 매우 견고합니다. 그러니 이런 접근 방식이 매우 흔한 것도 전혀 놀라운 일이 아닙니다. 단점도 몇 가지 있습니다. 즉, 문제는 시동이 잘 걸리지 않는다는 것입니다. 저는 거기에 필요한 데이터만 저장된다고 언급했습니다. 하지만 밤새 사용자가 애플리케이션에 접속하지 않고 데이터도 전혀 없는 상황이 되면 어떻게 될까요? 캐시에 있는 모든 데이터가 만료됩니다. 음, 시동을 걸 때 냉정하게 시작하게 되죠. 갑자기 모든 게 엉망이 된 것 같은 기분이 들잖아요, 그렇죠? 모든 데이터가 캐시에 다시 채워져야 정상적으로 작동합니다 . 그래서 아침에는 캐시 속도가 조금 느릴 수도 있습니다. 두 번째로, 천둥처럼 몰아치는 떼라는 게 있잖아요 ? 자주 사용되는 키가 만료되는 경향이 있다면, 많은 요청이 동시에 누락되어 갑자기 데이터베이스에 과부하가 걸릴 수 있습니다 . 영상 후반부에 떼 지어 몰려드는 동물 떼 문제를 해결하는 방법에 대해 이야기할 테니 끝까지 시청해 주세요 . 그리고 세 번째는 오래된 데이터입니다. 데이터는 읽기 작업 시에만 업데이트됩니다. 즉, 쓰기 작업이 발생할 때가 아니라 쓰기 작업이 실패할 때만 데이터베이스가 실제로 업데이트된다는 뜻입니다. 따라서 읽기 작업 중 업데이트가 발생하는 사이에 쓰기 작업이 발생하면 캐시에서 요청하는 데이터가 최신 상태가 아닐 수 있습니다. 그러므로 애플리케이션을 개발하는 동안 TTL 값을 조정할 수 있도록 이 점을 염두에 두어야 합니다 . 이 기능이 실제로 어떻게 작동하는지 Rust 코드로 간단히 보여드리겠습니다 . 이 내용은 여러분이 사용하는 모든 언어에 적용됩니다 . 이건 단지 예시일 뿐이지만, 어떤 역할을 하는지 설명해 드리겠습니다. 그래서 제 사용자 가져오기 함수에는 세 단계가 있습니다. 첫째, 캐시를 확인하고 있습니다. 주어진 ID가 캐시에 있는지 확인하고 , 반환되는 값이 있는지 확인합니다. 만약 아무런 응답이 없으면, 캐시에 없는 거겠죠. 실수했어요. 데이터베이스에서 해당 정보를 가져와야 합니다. 자, 이제 저는 제 SQL ID 데이터베이스에서 일반 SQL 쿼리를 실행합니다. 데이터를 다시 가져오고 있습니다. 데이터를 다시 가져오면 객체로 직렬화한 다음 30초의 TTL(Time To Live)을 설정하여 캐시에 다시 저장합니다 . 그게 다예요. 아주 간단합니다. 캐시를 확인하고 없으면 데이터베이스에서 가져와서 다시 캐시에 저장합니다 . 밥이 네 삼촌이지, 그렇지? 자, 만약 제가 종료하고 바로 실행하면, 여기서 캐시 미스가 발생하는 것을 보실 수 있을 겁니다. 이것이 첫 번째 시도입니다. 효과가 없었어요. 첫 번째는 데이터베이스에서 데이터를 가져왔지만, 두 번째는 캐시 적중입니다. 그래서 제가 다시 실행해 보면, 데이터가 데이터베이스에 있기 때문에 두 번 모두 캐시 적중이 되는 것을 볼 수 있습니다 . 살아있어요. 바로 드실 수 있습니다. 이제 쓰기 관통(write through)이라고 하는 두 번째 캐싱 전략을 살펴보겠습니다. 현재 쓰기 관통(write through)은 앞서 언급한 캐시와 함께 거의 exclusively 사용되지만, 본질적으로 쓰기 관통은 캐시에 대한 쓰기를 처리하는 또 다른 방법입니다 . 따라서 읽기 작업 중 캐시 누락이 발생할 때마다 캐시에 쓰는 대신, 데이터베이스에 쓰기 작업이 발생할 때마다 캐시에 쓰는 것입니다 . 그렇다면 이것은 어떻게 작동하는 걸까요? 애플리케이션이 사용자 정보를 업데이트하거나 데이터베이스의 특정 항목을 업데이트해야 한다고 가정해 보세요 . 그러니까 단순히 데이터베이스를 업데이트하고 누군가가 그 데이터를 요청해서 캐시에 데이터가 업데이트되기를 바라는 대신에 말이죠. 실제로 데이터를 데이터베이스와 캐시에 동시에 동기적으로 기록하는데, 이는 정말 훌륭한 기능입니다. 즉, 클라이언트가 해당 데이터를 요청하기 시작하면 언제든지 캐시에 저장된 최신 버전의 데이터를 사용할 수 있다는 의미입니다. 그렇다면 이 전략은 언제 사용해야 할까요? 네, 데이터의 최신성이 매우 중요한 읽기 작업량이 많은 작업, 즉 데이터를 최대한 최신 상태로 유지해야 하는 경우에 사용해야 합니다. 이 기능을 사용하지 말아야 할 때는 언제일까요 ? 음, 쓰기 작업이 많은 작업 부하의 경우요. 데이터베이스에 자주 쓰기 작업을 해야 하는 애플리케이션이 있다면, 기본적으로 데이터베이스 호출 횟수가 두 배로 늘어나는 셈인데, 이는 바람직하지 않습니다 . 또한, 쓰기 관통 전략의 단점은 캐시 크기가 매우 커진다는 것입니다. 앞으로 훨씬 더 많은 데이터를 작성하게 될 겁니다. 자주 접근하지 않는 데이터라도 캐시에 기록되므로 비용이 다소 증가할 수 있습니다. 음, 또한 읽기 편의성을 위해 지연 시간을 감수해야 합니다 . 즉, 필요한 데이터는 항상 캐시에 저장되어 있다는 뜻입니다. 요컨대, 이것은 작업을 수행하는 데 매우 훌륭한 방법이며, 코드 내부에서 이를 조금 보여드리겠습니다. 자, 여기 있는 것은 Rust 애플리케이션이지만, 이 방법은 Rust 계열 언어라면 어떤 것이든 적용할 수 있습니다. 요컨대, 저는 업데이트 함수, 사용자 업데이트 함수를 가지고 있습니다. 보시다시피, 여기서 하는 일은 먼저 데이터베이스에 쓰는 것이죠 ? 아주 간단하죠. 데이터베이스를 업데이트하는 SQL 쿼리가 있습니다 . 좋아요. 하지만 그런 다음 해당 데이터를 가져와서 캐시에 기록하는데, 이 경우 TTL은 5분 또는 300초입니다 . 그게 다예요. 이게 다입니다. 이런 것을 구현하는 데 필요한 것은 이게 전부입니다 . 물론, 제가 해석한 내용은 몇 분 전에 보여드린 예시와 완전히 동일합니다. 하지만 여기서 바로 실행해 보면, 초기 읽기에서 캐시 미스가 발생하는 것을 확인할 수 있습니다. 두 번째 화면에서 사용자 정보를 업데이트합니다. 데이터베이스에 쓰기 작업이 있고, 캐시에 쓰기 작업이 있습니다. 그리고 다음에 읽기를 시도하면 캐시 적중이 발생하는데, 당연히 해당 사용자가 이제 데이터베이스에 저장되어 있기 때문입니다. 정말 훌륭합니다. 자, 여기서 자세히 다루지는 않겠지만, 쓰기 후 저장(write behind), 읽기 전 저장(read through), 쓰기 우회(write around) 같은 전략들이 몇 가지 더 있습니다. 요컨대, 캐시를 사용하지 않고 쓰기 우회 전략을 사용하는 것이 가장 흔하게 사용되는 전략 중 하나이므로 이 두 가지를 기억해 두세요. 캐싱을 사용하거나 애플리케이션에 캐싱을 추가할 때 개발자들이 고려해야 할 몇 가지 주의 사항이나 함정에 대해 이야기해 보겠습니다 . 첫째, 직렬화 및 역직렬화의 오버헤드를 이해하는 것입니다 . 자, 이건 사용하는 언어에 따라 다르지만, 예를 들어 Rust에서는 객체를 구조체로 저장하고 , 캐시에 뭔가를 쓰려면 다시 JSON으로 직렬화한 다음 캐시에 써야 합니다. 하지만 캐시를 사용할 때마다 JSON 형식의 응답을 받게 되는데, 그 응답을 다시 객체로 역직렬화해야 합니다. 이는 소규모 애플리케이션이나 작은 객체에는 문제가 되지 않을 수 있지만, 더 큰 객체를 다룰 때는 이것이 오버헤드로 작용한다는 점을 이해해야 합니다. 그 점을 명심하세요. 두 번째는 수명(Time to Live, TTL)입니다. 오늘 몇 번 언급했지만, TTL이 무엇을 의미하고 왜 그렇게 중요한지 여러분이 확실히 이해하셨으면 좋겠습니다. 그러니까 TTL 값을 너무 짧게 설정하면 캐시 미스가 많이 발생하겠죠 ? 그건 문제가 될 거예요. TTL 값을 너무 길게 설정하면 데이터가 갑자기 오래된 정보가 되어버립니다. 그러니까, TTL의 정확한 길이를 찾는 것이 중요하죠, 그렇죠? 쓰기 스루(write through) 방식과 같은 것을 사용한다면 이 내용이 크게 중요하지 않을 수 있지만, 단순히 캐시 사이드(cache side)를 사용하는 경우라면 데이터 업데이트의 일반적인 패턴을 파악하여 현실적인 TTL(Time To Live) 값을 설정하는 것이 중요합니다. 이렇게 하면 캐시 미스가 너무 많이 발생하지 않으면 서도 오래된 데이터가 저장되지 않도록 할 수 있습니다. 세 번째 항목인 캐시 무효화에 대해 이야기해 보겠습니다 . 여기서 필 칼튼의 말을 인용하고 싶습니다. " 컴퓨터 과학에서 어려운 것은 단 두 가지뿐입니다." 캐시 무효화 및 명명 규칙. 그래서 캐시 무효화는 반드시 이해해야 할 중요한 개념이며 , 문제가 될 수 있는 부분입니다. 캐시를 무효화하면 오래된 데이터를 방지할 수 있습니다. 물론 TTL이 만료되면 특정 데이터는 무효화되지만, TTL이 너무 길다면 어떻게 될까요? 음, 물론 쓰기 관통(write-through) 방식을 예로 들었지만 , 데이터베이스에 무언가를 쓸 때마다 캐시에도 기록되는 방식입니다. 하지만 데이터베이스와 캐시 모두에 동기적으로 쓰기를 할 때는 추가적인 복잡성이 발생합니다 . 그러니까, 음, 호출은 여전히 ​​두 번이겠지만, 캐시에 다시 쓰는 대신 데이터를 무효화함으로써 캐시 처리 시간을 줄일 수 있다는 거죠. 자 , 예를 하나 들어보겠습니다. 자, 여기 무효화 웰, 즉 ' 사용자 업데이트'라는 함수가 있습니다. 이 함수는 기본적으로 데이터베이스에 기록하는 기능을 합니다. 특별한 건 없고, 여기서 업데이트 SQL 쿼리가 실행되지만, 단순히 캐시 삭제만 실행하는 것입니다. 그게 다예요. 캐시에서 객체를 삭제하는 것일 뿐입니다 . 데이터베이스를 업데이트하지 않고, 방금 업데이트한 데이터를 추가하지 않습니다 . 아니요, 그냥 무효화할 뿐이고 거기서 끝입니다. 그래서 다음에 읽기 작업이 발생하면, 캐시를 활용하는 방식 덕분에 데이터베이스에 쓰기 작업도 함께 발생하게 됩니다. 그러니까, 모든 데이터를 값비싼 대용량 캐시에 저장하는 방식보다 캐시를 ​​최적화하는 조금 더 나은 방법이라고 할 수 있습니다. 네 번째는 보물을 훔치려는 떼의 질주 또는 천둥처럼 몰아치는 무리입니다 . 저는 이 용어를 정말 좋아하는데, 컴퓨터 과학에서 여러 가지 의미로 사용되기 때문입니다. 이 경우에는 많은 요청이 데이터베이스에 캐시 미스를 일으킬 수 있는 상황을 말합니다. 그러니까 특정 객체, 즉 특정 캐시 항목이 만료되고 많은 작업이 동시에 여러 객체에 접근하는 경우입니다. 모든 것을 한꺼번에 요청하는 대규모 요청을 상상해 보세요 . TTL이 완전히 동일하고 만료 시점이 정확히 같은 객체들이 여러 개 생성됩니다 . 이 문제를 해결하려면 지터를 적용해야 합니다. 코드로 보여드리겠습니다. 그러면 훨씬 더 이해하기 쉬울 겁니다. 그러니까, 이것은 함수입니다. 여기서는 실제 캐시가 작동하는 게 아니라, 그냥 무작위로 노이즈를 발생시키는 함수인 거죠, 그렇죠? 그러니까, TTL을 300 초나 30초처럼 원하는 값으로 설정하는 대신, 300초 에서 360초 사이의 값으로 정의하는 겁니다. 그래서 애플리케이션에서 이와 같은 간단한 헬퍼 함수를 ​​사용하여 TTL(Time To Live)을 무작위로 설정하면, 유사하지만 서로 다른 TTL을 가진 객체들이 많아지는 문제를 방지할 수 있습니다. 이렇게 하면 특정 작업은 캐시에, 특정 작업은 데이터베이스에 할당할 수 있기 때문입니다. 자, 여러분 모두에게 아주 유용한 팁 하나 알려드릴게요. 그리고 다섯 번째로, 또 다른 기능에 대해 이야기해 보겠습니다. 바로 실행 중인 프로세스 내부에 데이터를 캐시하는 옵션입니다. 모든 데이터를 외부 캐시에 저장할 필요는 없습니다. 실행 중인 프로세스에 따라 실행 중인 프로세스 내부에 일부 데이터를 캐시할 수 있습니다 . 람다 함수에서도 이 작업을 수행할 수 있지만, 람다 함수의 수명은 매우 짧기 때문에 서버 등에서 실행되는 장기 실행 애플리케이션에 더 적합합니다 . 그래서 이 경우에는 Rust에서 `using`을 사용하고 있는데 , `Moka` 크레이트라는 것을 사용하고 있습니다 . 그리고 모카는 본질적으로 제가 이렇게 할 수 있도록 도와주는 도구일 뿐이지만, 여러분은 원하는 어떤 도구로든 이렇게 할 수 있습니다 . 해시맵일 수도 있습니다. 벡터일 수 있습니다. 배열일 수도 있습니다. 상관없어요. 실행 중인 프로세스 내부에 데이터를 임시로 저장하는 것 . 모카 덕분에 그 작업을 훨씬 더 수월하게 할 수 있게 되었어요. 그래서 이 사용자 가져오기 함수에서는 기본적으로 L1을 확인하거나 Moka를 확인합니다. Moka에 이 사용자가 있나요? 모카에 이런 사용자가 있다면 완벽하겠네요. 나노초 단위로 다시 복구할 수 있습니다. 그건 메모리에서 바로 가져온 거고, 아마 CPU에 저장된 내용일 거예요. 음, CPU 캐시에 있습니다. 거기에 없으면, 저는 직접 발키리에서 확인해 봅니다. 제 데이터베이스에 요청을 보내주세요. 캐시에 있는지 확인해 보고, 있다면 완벽하네요. 반환하겠습니다. 캐시에 없으면, 저는 원본 정보의 출처로 돌아갑니다. 데이터베이스로 돌아가면 그걸로 끝입니다. 아주 간단하죠. 저는 이 장치를 제 L1 캐시, 또는 이 경우에는 제 Moca 캐시에 다시 넣으면 모든 준비가 완료됩니다 . 자, 이것이 이 기능을 사용하는 한 가지 방법입니다. 자, 제가 보여드리겠습니다. Moca를 실행하기만 하면 됩니다. 여기서 L1 미스, L2 미스 같은 걸 보실 수 있을 거예요, 그렇죠? 데이터베이스에 접근했습니다 . 우리는 데이터베이스에 데이터를 입력합니다. 다시 시도해 보겠습니다. 짜잔, 다시 L1에 올라왔네요. 이제 실행 중인 프로세스 내에서 즉시 사용할 수 있습니다 . 이미 과정이 완료되었기 때문에 최상의 예시는 아닙니다. 다시 실행하면 똑같은 결과가 반복될 뿐이지만 , 무슨 말인지 아시겠죠? 이 과정이 오래 걸리는 경우, 해당 특정 데이터는 Moca 또는 실제 애플리케이션의 로컬 메모리 캐시에 저장됩니다. 마지막으로, AWS의 Amazon ElastiCache에서 사용할 수 있는 Redis의 오픈 소스 포크인 Valky에 대해 이야기해 보겠습니다 . 자, Valky 캐시 클러스터는 두 가지 다른 방법으로 실행할 수 있습니다. 자, 여기서 예를 하나 보여드리겠습니다. 서버리스 캐시로 실행할 수 있으며, 시작 속도가 매우 빠릅니다. 원하는 만큼 크기를 조절할 수 있습니다 . 관리해야 할 서비스가 전혀 없어요. 그런 건 전혀 없어요 . 또는 원하는 노드의 종류, 크기 및 개수를 선택하여 자신만의 캐시를 구성할 수도 있습니다. 그러니 좀 더 예측 가능한 업무량을 원한다면 이러한 사항들을 고려해 보는 것이 좋을 것입니다. 음, 여기서 서버리스 캐시를 사용하는 예시를 보여드리겠습니다. "안녕하세요, 유튜브."라고 인사할게요. 어, 캐시 이름으로요. 그리고 저는 이렇게 설명할 겁니다. "안녕하세요, 안녕하세요. 느린 소비 습관은 이제 그만!" 음, 엔진 버전은 최신 버전인 8을 선택하겠습니다. 솔직히 말씀드리면 모든 설정을 기본값으로 사용할 예정이지만, 여기에서 특정 설정을 수정할 수도 있습니다 . 어, 음, 아시다시피, 어떤 가용성 영역 에 속하게 될지, 그리고 그런 모든 것들을요. 그래서 저는 바로 이 기능을 만들어보겠습니다. 순식간에 생성될 겁니다. AWS에서 Valky를 사용할 때 가장 중요한 점은 Valky가 VPC 기반 서비스라는 사실입니다. 즉, VPC 내부에 있는 애플리케이션에서만 작동합니다 . 즉, 인터넷 에 함부로 노출할 수 없다는 뜻입니다 . 음, 가능은 하지만 아주 직접적인 방법은 아닙니다. Valky는 VPC 내부에 있는 애플리케이션, EC2 인스턴스 내부 또는 Lambda 함수와 상호 작용할 것으로 예상합니다. 따라서 Lambda 함수를 사용하려면 Lambda 함수가 VPC에 연결되어 있어야 하며, 이로 인해 지연이 발생할 수 있다는 점을 이해해야 합니다 . 그래서 이 장비가 배치되었습니다. 저것 좀 봐 . 이미 사용 가능한 클러스터가 있습니다. 그래서 제가 여기서 해야 할 일은 이 항목이 생성되는 즉시 컴퓨팅 연결을 설정하는 것입니다. 그래서 제가 잠시 후에 새로고침해도 여전히 생성 중일 겁니다. 그럼, 이 일이 끝나면 바로 다시 오도록 하죠. 좋아요. 사용 가능. 이 장치를 발사하는 데 1분도 채 걸리지 않았으니, 상당히 빠른 편입니다 . 여기서 보시는 것처럼 컴퓨팅 연결을 설정할 수 있습니다 . 이것은 매우 중요합니다. 기본적으로 저는 이 장치를 EC2 인스턴스에 연결하고 싶습니다. 그래서 제가 이렇게 하면, 즉 여기에서 사용 가능한 EC2 인스턴스를 선택하고 설정을 누르면, 제 EC2 인스턴스가 이 특정 클러스터에 연결할 수 있는 권한을 부여받게 됩니다 . 보시다시피 여기서는 여러 가지 일들이 벌어지고 있습니다. 음, 보안 그룹, 온갖 종류의 것들, 예를 들어 출입 규칙 같은 것들이요. 기본적으로, 그들은 여러분의 EC2 인스턴스가 이 장치와 상호 작용할 수 있도록 조정합니다. 이제 EC2 인스턴스로 이동하여 Valky CLI와 같은 도구를 설치하거나, 제가 지금 바로 보여드릴 것처럼 해당 EC2 인스턴스에서 애플리케이션을 실행할 수 있습니다 . 저는 여기로 갈 거고, 이게 바로 EC2 인스턴스에 접속한 제 모습입니다. 저는 별도의 캐시를 가지고 있습니다. 음, 애플리케이션이요. 제가 로컬 환경에서 실행했던 것과 동일한 예제를 이제 EC2 인스턴스에서 실행하고 있습니다. 보시는 바와 같이, 제가 그냥 실행하면 실제로 제 Redis URL에 접속하게 됩니다. 슛이 빗나갔다. 만약 제가 똑같은 방식으로 다시 실행하면 , Redis에 요청이 발생합니다. 자, 이 예시에서도 저는 로컬에서 SQLite를 사용하고 있으므로 , 사실 별 의미가 없겠죠? 하지만 저는 다른 곳에서 데이터베이스를 사용할 수도 있었을 겁니다. 하지만 보시다시피, 이것은 실제로 이러한 일들을 하고 있습니다. 또한, 여기에 몇 가지 명령어가 있습니다. 제가 검사 하나만 하면, 실제로 TTL 값이 표시될 겁니다. 이 경우 TTL은 6입니다. 그러니까 다시 하면 마이너스 2가 된다는 뜻이네요. 따라서 TTL은 이제 음수여야 합니다. 자, 이걸 다시 실행하면 실패할 겁니다. 자, 됐습니다. 해당 객체의 유효기간이 만료되었기 때문입니다. 그리고 다시 inspect 명령어를 실행해 보면, 24라는 결과가 나옵니다. 즉, 제 애플리케이션에서는 기본적으로 30초의 TTL이 설정되어 있는 것입니다. 그리고 TTL 값이 0에 도달할 때까지 계속해서 감소합니다. 0에 도달하는 순간 , 다시 원래대로 돌아갈 겁니다. 음, 이 경우에는 제가 실수를 하게 될 것 같네요 . 자, 이제 아시겠죠? 이 설정 방법은 매우 간단합니다. 별거 아니에요 . 애플리케이션에서 Valkey 클러스터를 가리키도록 설정하기만 하면 됩니다. Valkey는 Redis의 완전한 오픈 소스 포크이기 때문에, 여러분은 기존에 사용하던 Redis 라이브러리를 그대로 사용하면 됩니다. Rust로 구현한 Redis든, Python으로 구현한 Redis든, 무엇이든 원하는 대로 하세요. 설치가 정말 쉬워요 . 서버리스 방식으로 이용 가능합니다. 오늘 시도해보지 않을 이유가 없죠. 그게 다입니다 . 캐싱, 맞죠? Rust 데이터베이스에서 데이터를 더 빠르게 가져오는 방법은, 음, 인메모리 데이터베이스를 이용하는 거죠. 그러니 캐싱을 간과하지 마세요. 처음에는 설정하는 게 굉장히 복잡할 거라고 생각해서 "아니, 필요 없어. 사용자들도 꼭 필요하지는 않을 거야. 괜찮아. 빠른 데이터베이스가 있으니까."라고 생각했어요. 어쩌고저쩌고. 하지만 아시다시피, 데이터베이스에서 데이터를 읽어오는 데 걸리는 지연 시간이 100ms에서 한 자릿수 밀리초로 줄어들면 상당한 영향을 미칠 수 있습니다. 그래서 설정은 정말 간단합니다. 다양한 오픈 소스 옵션이 있는데, 그중에서도 가장 간단한 건 AWS의 Valkyrie 서버리스를 사용하는 거죠. 그렇게 작동합니다. 음, 가서 한번 확인해 보세요. 지금 바로 캐시를 활용한 지연 로딩 전략을 구현해 보세요. 말 그대로 데이터를 가져오는 함수 내부에서 몇 번의 호출만 하면 되는 겁니다. 음, 그렇게 복잡한 건 아니에요. 하지만 몇 가지 주의할 점이 있습니다. 캐시의 크기는 증가할 수 있다는 점을 기억하세요 . 오래된 데이터를 기억하세요. TTL을 이해하세요 . 그 천둥처럼 몰려드는 가축 떼를 기억하세요. 그러니까, 제대로 사용하면 정말 놀라운 도구가 될 수 있으니 사용 시 주의하세요 . 좋아요 버튼 바로 아래에 이 링크들을 여러 개 남겨둘게요. 그러니까, 한번 가서 확인해 보세요. 만약 이와 같은 콘텐츠가 마음에 드시거나 AWS, 클라우드, AI, 코딩 지원 등 관련 주제에 대해 더 배우고 싶으시다면 이 채널을 구독해주세요. 다음 편에서 뵙기를 정말 기대하고 있어요. 안녕.
