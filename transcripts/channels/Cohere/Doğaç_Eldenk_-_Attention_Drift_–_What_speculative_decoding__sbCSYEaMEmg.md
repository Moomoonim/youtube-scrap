# Doğaç Eldenk  - Attention Drift – What speculative decoding models learn

- 영상 링크: https://www.youtube.com/watch?v=sbCSYEaMEmg
- 채널: Cohere
- 업로드일: 2026-06-25
- 자막 언어: en
- 단어 수: 약 7418개

---

## 스크립트

[music] &gt;&gt; It is my utmost pleasure to welcome to this ML systems and theory seminar series Dodge. So Dodge is a master student of the Northwestern University in the US. His research interests are generally around LLM inference and efficiency and has done some great work and today we are very eager to eager to hear about your work on attention drift what speculative coding models learn. I must admit I'm very excited about this because I'm normally in the training regime of machine learning so I'm excited to move away a bit and hear about what we should be doing for inference. So yeah, the floor is yours take it away. &gt;&gt; Thank you. Thanks for the introduction. So this is our work with like with Northwestern Paul and what we do my co-authors Piyush Jain, Hongyang and Steven. So in this talk I will first go over LLMs what is speculative decoding and I will introduce the attention drift the phenomenon we found we will discuss why it happened how can we fix it and some architectural interventions and what we learned through the journey. So to get started LLMs work by predicting token by token. So each forward pass generates one token and it predicts the next possible token and this is perceived as an answer by the LLM to us and we repeat this until it stop. And each forward pass runs through the neural network so it's a heavy computation it takes a lot of compute and we do this for each token. So we don't Yeah. And we we do this repeatedly after we stop. And for the performance characteristics LLMs run on specialized hardware like GPUs or TPUs. And serving them efficiently is tough because we need to paralyze them. And while paralyzing, the first part, the pre-fill, is when the LLM is reading a text for the first time, it's more compute-bounded, so we can utilize the hardware more. But when you do decoding, the step where we start to produce tokens, we are being more memory-bounded, meaning we are constrained by the memory bandwidth of GPU. So, the amount of time we are spending pulling the weights of the neural network from the memory into the GPU is the shared memory is taking the most time, and we are underutilizing our cores. But the pre-fill, the reading part, is easily parallelizable. So, to overcome this limitation, because we are we are under utilizing our hardware, and how we can turn this how we can make it more efficient is the question we are trying to tackle. So, I want to go over speculative decoding and what's the intuition behind it. So, the first one is we are spending the equal compute on hard and easy tokens. Like a preposition or like a very easy answer is still using the same amount of compute. Let's say we are completing the sentence, I would like to and the to is very easy, it's almost like a natural follow-up. But if you want to say something like square root of this big number, then it's much much harder to say, but in reality, the network is spending the equal amount of compute on both. Similarly, like if we do a simple addition, it's really simple. And sometimes it's just a mixture, like in a sentence when you do it, sometimes easy tokens, sometimes hard tokens, but we just spent the same compute. And is there any way that we can do this more flexibly? So, why are we spending the same compute? That's one of the motivation. And the second one is the LLM inference is memory-bandwidth-bounded. So, we are leaving some compute on the table that we are not using. So, is there any way that we can maybe do more compute because we have spare cores to make sure we can generate more tokens in parallel. For example, KV caching is helping us. So, with the KV caching, we don't have to recompute all the past tokens, but we can reuse some part of the computation. Okay. Yeah. So, this animation shows on the left, if you don't use the KV cache, you just do a bunch of computation, but if you use the KV cache on the right, as you can see, you do a lot less. So, you are going from quadratic to linear time. So, which also is partly related to why we we have spare compute. And when we combine those ideas, what if we have a small model that predicts those easy tokens, and a big model that says, "Okay, here you are right, here you are wrong." And sometimes it just doesn't know the answer and gives you the answer. So, we will be having the performance of the big model, like the accuracy of the big model, while spending the amount of compute the small model is actually using. So, we can run it much faster. So, the result is if we can find a way to use both models at the same time, we can have a lossless verification, which we call rejection split. Here, we call rejection sampling. So, we use the smaller model to draft some tokens. We propose some stuff to the big model, and the big model says, "This is right, this is wrong." And it by using this, we can utilize less compute for generating more tokens. And this is the rejection sampling formula. This ensures that we are sampling from the probability distribution of the target model. And I always like to think this as like we have the token probabilities here like I marked them as area. And if drafter said, "Okay, here is a token, and I have sampled this token with this probability. And if it's less likely than the target, we accept it." And if it's not, then we resample it by adjusting the probability. And this ensures the process is lossless. So, in summary, we call this verification. So, given a prediction of tokens, we get the probability that the target model would have outputted them. And instead of doing a forward pass on a single token, instead of predicting one token, we pass many tokens because our compute has some We have some spare compute we can use without slowing down. And the result is that, as you can see here, we give the tokens, it says, "This is right, this is wrong." And we take the longest valid chain. And we always get one bonus token because we can resample. And this ensures that we generate at least one token. And we get the speed up of direct four four times because we basically spent one unit of compute to get five tokens here. So, one of the most popular methods uh used in production is Eagle tree. And this is a post-training method. And in the middle, I show the LLM's internal architecture. And this Eagle tree does What Eagle tree does is let's train a separate transformer layer, a single layer transformer, and let's use the hidden states coming from the target model because those hidden states carry some information which could be harder to learn. And we can just utilize it directly. And let's predict what will be the second token, third token, fourth token by repeatedly reinjecting the token into the drafter model itself. So, here on the middle, we have the fuse layer, which we will talk about this later on. This fuse layer gets some hidden states from the lower, middle, and higher layers and combines it to a single hidden layer and inputs it to another transformer combined with the output token embedding. And it is runs like a regular big transformer model, but it only has one layer, so it's much smaller compute and it predicts the next token. And then we repeat this multiple times. So, another alternative is MTP. This is usually combined with pre-training. So, for example, in Deep Seek 3, we saw this MTP heads are being trained with the model and you can directly use those in your inference to predict multiple tokens ahead. But, if your model doesn't have it, such as Chinchilla 2.6, MinMaxM3, like those models don't have MTP, so we have to fall back to a coarse-grained method where we train a separate model after the base model training. So, oh, before I jump into the attention part, is there any questions regarding speculative decoding or LLM inference? Okay. Um Okay, so I'll move on to the attention sinks. So, attention is the mechanism inside the transformer model that maps the relationship between tokens. And normally the MLP layers don't operate on sequence level, so you don't see the relationship between tokens. Mathematically, they don't interact. But, in attention, they do. And that's why this gives us a unique insight on how the model is thinking and operating. So, in the attention, each token is forms some kind of relationship to other tokens inside the same sequence. So, we display this as a heat map. And in the middle, you can see I made a small visualization of how it would look like for a very simple sequence. And we have each row adds up to one and if the number is bigger it means it attends it to more showing a stronger relationship. So for example if you look at two the decision to generate the token two was mostly influenced by other tokens like one plus and equals and we can take a look at the ratio of how important they were by taking a look at their attention score. And there is a phenomenon in this attention pattern that's been found out recently and like a actually a while ago is sometimes LLMs over attend to certain tokens. And those certain tokens are mostly meaningless like the first token in the sequence or some special marker that tells you this is the beginning of the sequence this is the end of the sequence and so on. So we took a look at this for the speculative decoding models and we found that speculative decoding models are have they have the same attention sync as the target model for the most of the time. So the sync exists in the same place as the drafter. We looked into three different families of models QN Llama and GPT-OSS. And GPT-OSS there is no sync and the drafter also didn't have sync but to for the QN it it was in the second position and was same in the drafter and for the Llama it was in the first and we saw the same thing. And on the right you can see an attention heat map and the values that are brighter closer to white and yellow are the ones that have the most attention. And as you can see the left column is very bright and this is where they think it this is where the most amount of attention is being concentrated in this for this model. And Yeah, we want to take a look at the drafters because the drafters are unique. They are being fed with the hidden space of the target model, then they start to generate their own hidden states. But with the most recent models like Eagle 3, there is no constraint for hidden space to look similar. In previous generations like Eagle, they were constrained to generate hidden states that looks similar to the targets, but that's been abandoned and the Eagle 3 gives the model more freedom so it can have its own hidden states, its own representation, and it outperforms the Eagle 1 and 2. And What's What's interesting is if you look at the heat map, the upper left triangle is just the attention between the hidden states of the target model to the hidden states of the target model. But if you go down to the rectangle on the left bottom left, the speculated tokens are starting to attend to the hidden states of the target model, which are coming from two different models. And the bottom right is where things are more interesting because the drafter model starts to attend to its own tokens. So we want like this might look This might not be so intuitive, but once we look into it in depth, what we see is the attention of the model looks very different in each one of those sections. So from the upper left, we have a strong sync. Here, the black the the colors closer to black are more strong attention. And closer to the white are less, so they're weaker. So the sync here is for the target hidden states. The target hidden states stays like we've seen in target model like the LLMs, but once we start into the speculated regime, like what we see is the sync is starting to weaken. So, as you can see on the left column, it's getting weaker and weaker, while what we see is the speculated part, actually the token itself is getting more and more attention. And this is something we don't observe at the LLM's because, first of all, it just feeds its own output, and it's consistent. But, with the speculative decoding, what we do is we speculated for some tokens. In this case, we speculated for 12 tokens. Then, after 12 tokens, we do a verification, then we reset. Because, after we fetch the new hidden space from the target model, we are refreshing the KV cache like of the We We refresh the state of the drafter. So, this thing only happens when you speculate, not when you prefill. And we observe this consistently for all drafters. Like, we used Llama, Qwen and Qwen 3.5, GPT-OSS. And as you can see on the left, the sink is consistently weakening. And this is the strongest on the Llama, and and we still see it on Qwen 3.5, Qwen 30B. But, the GPT-OSS has a special case. It has a It prevents attention sinks by having a learnable bias. And so, it doesn't have a sink. So, the sink can't go anywhere, and the tokens of attention is more or less stable. But, for the other models, the recent most recent generated token steals the most of the attention from the sink and just keeps increasing with the speculation as the as your speculation gets longer and longer. So, in order to understand what's going on, first thing we want to see is how the magnitudes of those vectors. So, we use the RMS magnitude of this those vectors hidden state to understand is there anything going on because what we figured is like inside the model the residuals from your pre-norm connections, they accumulate the magnitude. So, inside the model if you take the transformer layer one, its magnitude is going to be smaller than transformer layer five, and the five is going to be smaller than 20, and so on. And what we found is we're especially taking a look at the middle column, the FC, which is the component where I showed initially, the where the lower, middle, and higher hidden states are being fused together. And its magnitude is much more different than the drafter's own hidden states. This is a mismatch. Uh this is something we were not expecting, and what it means is we looked at the correlation between accuracy and magnitude, but we wouldn't we didn't we didn't see any. So, it seems like the model is learning to compensate. So, the second layer Secondly, we take a look at the inputed hidden layers from the target, which are fused together. And their magnitudes are also different, but we feed them without any normalization. So, the signal inside the fused layer is dominated by the later hidden states. So, for example, this is much more obvious in GPT- 2 SS family. Like, the lower layer only has three, while the higher level layer is like 500 times bigger than that. And what we proposed later on was while feeding them into the FC layer, we should add normal we should normalize each one of them, so they can contribute equally. Or like they should contribute as much as needed. And the third one is as we speculate more tokens, our hidden states are actually getting bigger and bigger. So here we tested from one to eight, and the magnitude of the hidden state was getting higher as we draft more tokens. This was consistent in all models, even with GPT where there is no attention sink. So what we see here is the drafter is learning depth-specific representations. So it's encoding the magnitude to be it's encoding depth inside the magnitude. So the fix we propose is, okay, normally we take the hidden states of the drafter model right after the transformer layer before the norm that we apply to the head. So instead of this, what if we switch the positioning? So we take the hidden state right after the norm. So our hidden states will be balanced. But doing this requires retraining. So we have to train this model from scratch. If we just apply this during test time, it's failing, its accuracy is bad. And so we have to do this by retraining or fine-tuning the existing drafter. So why magnitude relates to drift? So how do they correlate? One of the interpretations is the query and key similarity because in attention if both your speculated tokens have much higher magnitude than your original pre-filled tokens, they would resemble each other more, meaning the similarity will translate to the higher attention score. And in order to understand whether this is the only reason, we pinned the magnitudes. We have applied the normalization but to the hidden state even though the model wasn't trained for it. And we wanted to see how the attention is changing. And what we see is it eliminates some of the drift after we apply the norm artificially to the hidden state. Uh the what we were seeing is the sink was weakening less, but it wasn't entirely gone. And if we use the post norm architecture, the one we proposed here, taking the hidden state after the norm, it's actually stopping. Basically, the sink is not weakening. There is no drift. So, how we can interpret this as one one one way to see this is like once you have the norm, it allows drafter to be more independent because the magnitude of each prediction resembles each other. But if you don't have it, the pre-norm architecture on the left, I would like to see that as we are training the drafter to act like additional layers on top of the model. So, like the layer M plus one, layer M plus two, because it knows it's the different method, it's a different attention pattern. Uh so, it means the model is learning to act differently for each step. But the second one is it doesn't learn that. There is no different attention pattern. Attention is pretty straight. There is no drift. There is no magnitude issue. So, it just it can just take its input more and more. So, in this is nice, but how can we prove like this is the case? And in order to do that, we should dive deeper into how those models are trained. So, the Eagle tree introduces training time test. So, what it basically does is during the training, it emulates K steps of speculation. And those K steps help model to see what it would do during test time during training to match. And the result is the acceptance rate for the model is becoming flat. So on the left as you can see the eagle its acceptance rate conditional acceptance rate was dropping but the eagle trees staying flat. So the thing is if you train only for two steps you during inference your accuracy still drops heavily. For four or five or eight and in this case we went up to 20 speculation steps. So what people do is usually they train for eight steps. They train as much as their inference time requires. But if you use the pre-normal architecture if our generalization idea holds what it means is it should be it shouldn't fail as bad as the pre-norm. And if you look here we trained the model the pre-norm model for two steps for eight steps and we also trained the post-norm model for two steps. And on the left there is a accuracy conditional acceptance rate per speculation step. And the pre-norm architecture right after the second the TTT2 its accuracy is dropping very sharply because the model is doesn't know how to act as a eighth layer with which has a much much bigger magnitude or 16th layer. It has no idea how to behave as that. And similarly for the TTT8 pre-norm like it's flat until eight but it starts to drop after eight. Maybe not as badly but still drops. But the post-norm model even though we only trained it with two TTT steps its accuracy doesn't collapse. It actually flattens out at some point like it stays below 50% and this means the model can generalize better like it can take its own input and it can output and there is no speculation depth specific thing. It's a model that can repeatedly feed itself. And the result is on the right, the acceptance rate for pre-norm is in total 2.6 and the post-norm is five because it's almost the double because of this generalization ability. And maybe something to look out for is here the K equals 1 and K equals 16 shows acceptance rate at the step one and step 16. So when you train with less TTT steps, you're actually your acceptance rate at the K equals 1 is getting higher because Yeah, like this also means the model is losing some of its accuracy when you train for a longer TTT. So which [clears throat] post-norm might help in some cases. And I think this is a good direction to look into for others if anyone's interested. So another question is, okay, there is the sink. If there is no sink, there is no drift. But will just fixing the sink help with this magnitude issue? Will it help with the accuracy issue? So in order to understand that, we used the gated attention. Uh gated attention allows model The gated attention released last year in NeurIPS 2025. It's a technique that allows model to be sink free attention sink free. And they applied this technique into our models and what we see is it doesn't fix the magnitude problem. And finally, it increases the magnitude problem. The magnitude is much higher. So there is no visible drift, but there is no sink to still the attention problem. So it's hiding the symptom of what's going on. Also, what we found is we trained four models. We just trained a regular pre-norm, a the tension version of the prenorm, a postnorm model where we take the normalized hidden states, and finally the gated attention plus the normalized hidden states. And I think one of the most interesting thing is the gated attention plus postnorm doesn't perform as well. When we apply both together, the entropy of your attention is significantly lower. Like normally it hovers around two. And it drops to 0.6, which means it only attends to a couple tokens. And which we will later discuss, its accuracy is also not as good. But it's fixes the magnitude and sync at the same time. And if you look at postnorm, it fixes the magnitude issue, so it's around 1.2. It's sync is weak, but it's not drifting, 0.1. And its recent token is similarly 0.5, but it's not changing. So, so we want to test like, okay, what else? So we want to take a look at the error accumulation. Is prenorm architecture accumulating the error because we have this residual stream, and is it just carrying over the noise and causing any issues? And what we see is when we have actually injected noise in the magnitude of each one of those models hidden states, for example, alpha equals one is the injected noise 10% of the hidden state magnitude, 0.5 is 5% of the magnitude, and so on. And we observed how much of the accuracy it's actually preserving. And what we found is the postnorm architecture is much more resilient. And in some cases, like when we inject more noise into embeddings, it just preserved the whole accuracy. And but similarly for hidden states, like if we inject 50% of the magnitude, 50% noise of the magnitude, it's holding at 60% of the accuracy, but pre-normal gives zero. Its predictions are very, very bad. Also, like if you take a look here, as you can see, the drafter is benefiting a lot more from the hidden state because it's much more sensitive to the noise being injected to the hidden state rather than embeddings. So, another thing we looked into Eagle series of drafters, but we want to take a look at MTP heads, too, which is getting more and more popular, especially with Qwen 3.5 because they released their models with MTP heads. We wanted to take a look at them. And what we found is there is a there is a drift, and it's not as bad as the drafter models, like separately trained drafter model. But the thing is it also uses a post-norm architecture, so we don't have the magnitude accumulation issue. But we have the drift, which is interesting and it's something we don't see in the drafter models, which hints us it's not only about this pre-norm. It's it's about the learning objective. It's a combination of different things. So, solving one standalone doesn't fix. As we have seen in the artificial experiment of reducing the hidden state magnitude in pre-norm architecture, it did not fix the drift entirely. Similarly, doesn't fix the drift entirely here. But something to notice the sync attention is actually to to me seems like it's oscillating. Like it has a baseline for the pre-fill error, like 11%. It has a baseline of around 4% for drafted tokens. So, I see it as there is a drift, but it seems much more stable. It's stabilizing around different equilibriums for the pre-fill and drafting stages. Uh and there is some additional things, like this model has gated attention. This model has post norm and empty head. So it might be also the case and when we trained the llama with gated attention plus post norm, actually we saw a similar pattern. Like the drift was still there but it had an equilibrium a different equilibrium for the speculative part and critical part which was really interesting and maybe worth taking a look for the future. So going back let's take a look at the architectural change. Uh So what we did is the fused hidden state we added norm right before right after each captured hidden state from the target to bring them to the same magnitude. And the second thing is we changed the positioning of the hidden state captures from the drafter to keep drafting second, third and fourth and so on tokens. And the result is first we can train with less TTT steps most of the time when you do inference on production you don't do TTT you don't serve it with 16 steps or eight steps. Usually it's much less. Uh we can train it with much less and we can use it for longer steps or like more latency oriented use cases like RL rollouts or maybe you need higher interactivity. So it allows us to use less TTT steps which also cuts down training time. So in my case it took around like 30 to 40% less time to train the same model. It's more data efficient. So this is from min max min max at 2.6 I think. And what we see is the FC norm the green line it converges much faster. It's actually also higher accuracy than the pre norm. So it's more data efficient. It requires less TTT steps so the training time is also low. And the result is this is the GPT-OSS 20B. So, the post-norm beats the pre-norm in every setting, in every thinking mode, and in every benchmark. We used a wide set of benchmarks. Like MT-Bench, which is a multi-turn conversation benchmark, which is usually hard for speculative decoding tasks. And also in GSM8K, as which is a math-oriented task. So, there was a difference between around like 5% and 2 to 10% for some other benchmarks. And the post-norm architecture, when trained with the same setting, same dataset, was outperforming the pre-norm. So, actually, are there any questions? I will go over how we found this phenomenon. &gt;&gt; Um I have I have a question. So, um I'm going to preface that I'm not someone who is an expert in um in inference. And you were mentioning this point of like um latency-sensitive applications with like number of steps. So, um I was reading a brief tutorial online that basically was saying that there's like a degradation in the amount of performance you get in um these speculative decoding methods when you have like multiple queries, say on a server if you're trying to serve this as an inference provider like OpenAI or Anthropic. Can you maybe touch on this like practical aspect because I'm I guess I'm clueless in this in this regard. &gt;&gt; Okay. Yeah, sure. So, it goes back to the first point we made in LLM inference. We are memory-bounded when there is one token, but if you do batch serving, you can actually reuse the weights of the neural network. Once you pull it into your shared memory, you can start to reuse that for multiple queries. So, your compute utilization becomes better. So, your GPU is actually better utilized. So, you have less room to do this additional speculation computation. So, dispatching allows you to utilize your hardware better, basically. Yeah. &gt;&gt; Okay, great. Thank you. &gt;&gt; Yeah. Yeah, of course. So, yeah, going back to how we found this, uh So, okay, uh the question is Harsha is asking, "I'm curious if there was some different VET thinking and standard standard generated tokens." Um I'm not sure what VET Okay, difference with respect to thinking and stuff. The thinking tokens, there isn't much difference, but the thinking tokens can be harder to find sometimes, but in math, for example, they're easier, but we didn't take a look at them in depth. Uh what we saw was overall, when you enable thinking, the model just does Like, when we enable thinking here, the accuracy was dropping because we believe like the those tokens are tokens are harder to predict for a smaller one. They're less templated and so on. So, they're just generally harder, but I also heard some people think thinking tokens can be more repetitive, more templated, but we haven't seen that here. Uh yeah, also the other question is if there's a difference in drift for thinking trace. Um it doesn't matter because after each speculation step, we are resetting the KV cache of the drafter model by feeding it the hidden states coming freshly from target. So, the process shouldn't matter for thinking or non-thinking. Yeah. Okay. So, how we found this, this is actually a fun story. Um we were trying to train a drafter with multiple layers, and it was unstable. And what I figured out is, okay, there is this norm inside that applied, and I think the gradients are like they are being they they are not behaving well. Like I maybe I should just try moving this norm to a different place. And the result was it performs 50% better on GSM8K, and I was like, okay, just the norm position. How did it happen? And certainly dig deep, like why is it so good? Why does the norm position change so many things? And in order to understand why it happens, we should take a look at how LLMs process inputs. So, when you ask a question, like what is 2 + 2, basically the LLM has special markers that tell you this is the beginning of the sentence, this is where [clears throat] the user's question starts, and this is where the LLM question starts, and so on. And what was happening is the benchmark that's been publicly available wasn't applying this template which will be actually applied all the time, wasn't applied properly. And the model was performing much better when there is no template. And it was just trying to do plain next token prediction, like without any template. Yeah, it's it's back bench. Um and we took a look at this, and we are taking a look at this, and trying to understand, okay, is it the beginning of the sentence token because it's usually acting as the sync, especially for the Llama, it's the sync token, or is it the other template tokens? So, what we see is the pre-norm is very sensitive. Like first we remove first first column is the baseline, just the regular template, the beginning of sentence token. And second is, what if we remove the sink and the performance is dropping for the pre-norm? And once we remove the template with the beginning of sentence, it's dropping more. And if we remove all, it's just uh a 50% drop in accuracy. But the post-norm architecture is it doesn't see this degradation accuracy degradation. It doesn't experience this as bad. It has some, but it's not as bad. So, the question is like is it a result of attention sinks? Maybe. And the result The answer is no. We took a look at the gated attention version of the drafter. And because this drafter doesn't have a sink, if the sink is causing the sensitivity, the gated attention could have solved it. But the thing is, no. The middle column uh is the gated attention for each one of those. And if you remove the beginning of sentence, yeah, it the accuracy doesn't degrade because it's doesn't have a sink. But once we remove the template itself, the other tokens like the user markers, assistant markers, it still degrades. It still degrades badly. And another thing we see is if we apply both gated and post-norm, the accuracy is also lower than average. Maybe it's less sensitive, but its accuracy was low- lower than baseline. That didn't matter. Like we shouldn't apply both optimizations. And the second thing is what about the system prompt length? Like what if the system prompt is too long, too short, and so on? So, we took a look at this, and we What we saw is the post-norm was consistently better, especially at longer prompts. It had a higher accuracy. And the pre-norm was being affected more by the prompt length. Like the percentage of accuracy it loses was higher. And when we look at the gated attention version, actually the gated attention is much much more sensitive to the prompt length. So, it's requires the prompt to be longer. So, here like on the middle it starts as 1.63, but if you increase your tokens to system prompt length of 256, it goes to 2.6, which is very interesting. But the thing is, probably the sensitivity the system prompt is related to the training regime. Because once we run this experiment with QN and GPT- 2 assets, the post-norm had a slight edge, but the accuracy degradation was not as bad. And we believe this was due to the training data, because LLaMA has a specific prompt with about like 200 tokens, I think, and the others have like a much smaller prompt. And because the LLaMA was always so longer tokens, it was more sensitive to it. So, yeah, I think it's an interesting follow-up, but still like the post-norm was much better on all token lengths for all models. And the third part is like, okay, the long context. What about long context? Because the attention sinks once first introduced the efficient streaming language models with attention sinks paper, it was trying to tackle the problem of efficiently running a language model on a long sequence, especially on a sequence with unrelated questions. So, what they found is like they use a technique called sliding window attention, meaning only the last end tokens are attending. So, the idea is that probably the previous tokens are not related, and this holds for very long chats. Let's say, you keep asking questions, questions, and they start to become unrelated to each other. But the thing is, the model doesn't like it because of this attention sink. It needs to keep the attention sink to preserve its accuracy. So, this technique is called sliding window attention with the attention sink, the streaming yellow line. And in order to see, okay, what about long context with the drafter, we applied three variations of the sliding window attention. The first one is just a regular sliding window attention. We just carried over the last end tokens. The second is the one with the sink. So, we carry the sink plus the last end tokens. And the third one is something more improvised. We said, okay, let's carry over the sink and plus instructions because we want the model to be aligned with the instructions moving forward. Um And the reason why we apply sliding window sliding window attention is because drafters are usually cheaper to train, and people train them with 2,000 tokens, 4,000 tokens, and so on. And what we see is here, we run a model that's trained on 4,000 tokens on 16,000 token context. And the model is model's accuracy crashes. Like the regular here on the left means the full attention. And the pre-norm architecture is basically zero accuracy. And the post-norm still terrible, 0.83, but its degradation is much much much smoother. So, it doesn't is not as bad as the pre-norm. Once we start to apply the sliding window, the pre-norm is actually recovering a lot of accuracy to 2.43, but the post-norm is 2.97. It almost recovers its entire accuracy. So, this is the same benchmark we run here. So, we want to see 2.93 as the baseline for the post-norm and 2.78 as the pre-norm. And but the pre-norm can't catch that. Like it's 10% below the maximum acceptance length it can achieve if this was a short context prompt. And in order to isolate this, we are actually emulating a multi-turn conversation. So, the tokens we filled in the past are not truly related to the final question that's been asked and that's been answered. And what we see here is once we have the sync token, the pre-norm is also slightly increased and post-norm is also slightly increased. And then finally, the system prompt, the pre-norm is crashed. It doesn't accept the system prompt. It doesn't like to see some tokens from with positional encodings that belong to the initial part of the token plus some tokens that has the positional encodings of the final part, like the longer part of the token. It just crashes near zero, which is really interesting. But the post-norm can actually take that as an input and have a slight edge. Uh it's it's very close, but we've consistently saw that it was outperforming. But this is not true context, right? So, the other experiment is, okay, let's use long bench, which is a true long context benchmark, and let's take a look at how it performs. And the result is it's on a 25% overall improvement. And what we are testing here is actually very hard for the model because those tasks are designed for 16K tokens, 30K tokens, and so on, but the model only saw 4K tokens. And we are not applying other techniques such as the arm scaling because we are trying to isolate the effect of post-norm and how it's actually helping in some instances. And here, for example, the government reports, keeping the system prompt helped more a lot helped a lot, like 1.57 to 1.70 1.7 accuracy. And for others, it was a tie, but the post-norm was consistently better in few-shot coding and summarization tasks. So, another question is, what about our window size? Like, do we really need to attend to all tokens? And the probably the answer is no because what we see is around the window size of 1,000, the model is saturated. So, its accuracy is almost the same as the maximum accuracy it can get. And I I I I think this is because the drafter models, they don't have to be very smart. They don't have to be very accurate. The process of speculative decoding is, can you predict those next seven, eight tokens? And those tokens mostly, if they're easy, they're not related to the rest of the context. If they're hard, the drafter already fails. So, there is no need to take a look at the past context, right? The drafter is already not very smart. It's already a dumb small model. So, the answer is probably no. So, the impact is we actually released this as an extension to Eagle tree uh Eagle tree 1.1, and it's been adapted by the community. We saw Kimikate 2.6 uh with MLA release their drafter model by LightSeq board. We saw MinMaxM3, which is released like last week, and in fact, it was using the same architecture. And we released our own GPT2 SS models, which was the fastest on SG Lang. And we also saw some community Deep Seek before flash drafters. Uh yeah, the models being supported in SG Lang, vLLM, and hopefully Llama.cpp and TensorRT in the future. So, it's had some Hopefully, it had some impact in production, especially for MinMax, I've heard that the accuracy gap between the pre-norm and post-norm was significant. Yeah. So, some open questions that's left as our research is, first of all, we looked into the relationship between the acceptance probability and the attention entropy, and we didn't find a clear relationship. Maybe there is something more. Maybe there is something that we can utilize to find if some token is being accepted or not. And the second thing is the training regime. Like we didn't change our training regime. We use the standard Eagle tree training recipe with TTT, and I suspect the way we mask the tokens for loss might have impacted the model's overfitting into template. Like because we we usually mask the special tokens and the user queries in the training, the model might be behaving weird because of that. And maybe the input format, right? And the third one is like why gate's attention is more fragile, uh which was really interesting, and I want to see people look into like why gate's attention is fragile, why it doesn't work out, why is it collapsing, is it because of the target and draft your model architecture mismatch, and so on. And hopefully some of those principles can be extended beyond speculative decoding and influence uh the transformer other models transformer based models, and so on. So, the paper and the GitHub link to our model the paper is here and the GitHub link to our experiments are here. And the conclusion is I think the attention drift is merely a symptom. It's not the issue. So, just trying to fix it doesn't help. And it tells us what to look into, like the what does speculative decoding model learn, how it generalizes, and so on. And the second thing is the norm positioning can change the model's generalization ability, which might impact your production performance because it doesn't overfit as badly into your test data set. Yeah, and that's all. Thank you. &gt;&gt; Great. Thank you. &gt;&gt; [music] [music]
