# Running Qdrant Clusters in Distributed Deployment Mode

- 영상 링크: https://www.youtube.com/watch?v=53qQNUsCx2M
- 채널: Qdrant
- 업로드일: 2022-06-25
- 자막 언어: ko
- 단어 수: 약 1009개

---

## 스크립트

Quadrant의 최신 릴리스는 확장성 측면에서 큰 진전을 이루었습니다. 여러 대의 서로 다른 컴퓨터에서 실행되는 Quadrant 클러스터를 생성할 수 있는 실험적인 분산 모드를 방금 출시했습니다. 이 방법은 로컬 환경에서도 비교적 쉽게 재현할 수 있습니다. Docker와 Docker Compose에 익숙하다면 단 몇 분 만에 자신 만의 클러스터를 실행할 수 있습니다 . 이번 튜토리얼에서는 몇 가지 기본 사항을 다루고 로컬에서 Quadrant 클러스터를 직접 구축하는 방법을 보여드리겠습니다 . 저희는 Docker Compose를 사용할 예정이므로, 프로젝트를 설정 하고 Docker Compose YAML 파일에 몇 가지 구성을 제공하는 것보다 더 쉬운 작업은 없습니다. 그럼 제가 바로 해볼게요. 좋아요. 우선, 버전을 알려드려야 할 것 같습니다. 제 경우에는 3.9가 될 겁니다. 그다음에는 서비스를 선언합니다. 만약 제가 Quadrant 인스턴스를 하나만 사용하고 싶다면, 위와 같은 설정을 제공하면 됩니다. 컨테이너 이름을 Quadrant로 지정하고, 저희가 제공하는 공식 이미지를 사용하겠습니다 . Quadrant의 최신 버전은 0.8.0입니다 . 그게 전부입니다 . 물론, 포트를 노출하지 않았기 때문에 생성된 인스턴스와 통신할 수는 없겠지만, 기본적으로는 작동할 것입니다. 하지만 저는 단일 인스턴스가 아니라 Quadrant 클러스터를 구성하고 싶습니다. 그래서 컨테이너를 더 만들어야 할 것 같습니다. 그럼, 이 첫 번째 것을 사분면 기본이라고 부르도록 하죠. 일반적으로 HTTP API 통신에 사용되는 포트를 노출하는 것이 좋을 것입니다 . 그럼, 그렇게 하겠습니다. 이것은 포트 번호 6과 333입니다. 제 컴퓨터의 동일한 포트에 매핑하겠습니다 . 좋아요. 현재 저는 HTTP API 프로토콜이 포트를 통해 노출된 Quadrant 단일 인스턴스에 대한 완벽하게 작동하는 구성을 갖추고 있습니다. 하지만 이것만으로는 충분하지 않습니다. 저는 동일한 클러스터 내에서 또 다른 인스턴스를 실행하고 싶습니다 . 그래서 해당 설정의 일부를 복사해서 이름을 Quadrant secondary로 바꾸기만 하면 됩니다. 물론 이 보조 인스턴스를 첫 번째 인스턴스와 동일한 포트에 매핑할 수는 없지만, 사실 그것은 큰 문제가 되지 않습니다. 저희의 경우, 소통 창구는 하나면 충분합니다. 하지만, 알겠습니다. 현재 저희는 두 개의 서로 다른 컨테이너를 실행하는 Docker Compose 구성을 가지고 있지만 , 두 컨테이너는 서로의 존재조차 알지 못합니다. 그러면 그들은 서로 소통할 수 없을 겁니다. 우리는 이 문제를 가능한 한 빨리 해결해야 합니다 . 기본적으로 Quadrant는 추가 속성을 허용합니다. 하지만 또 다른 점도 있습니다. Quadrant 측에 이 시스템이 클러스터 모드로 실행될 예정임을 알려야 합니다. 이것은 두 가지 다른 방법으로 할 수 있습니다 . 우선, 설정 항목으로 제공할 수 있습니다 . 기본 설정 파일( 일반적으로 YAML 파일)에서 변경한 사항이 있다면, 그 내용도 수정할 수 있습니다. 하지만 저희의 경우 매우 간단하게 시작하는 단계이므로, 동일한 효과를 내는 환경 변수를 제공하는 두 번째 접근 방식을 사용할 수 있습니다 . 그럼, 파일 의 환경 설정 부분을 제공하는 것부터 바로 시작해 보겠습니다 . 그리고 해당 변수의 이름은 Quadrant double underscore cluster double underscore enabled입니다. 그리고 그 값은 true로 설정되어야 합니다. 좋아요. 하지만 다른 경우에도 마찬가지로 해야 합니다. 그럼, 제가 그대로 복사할게요. 그리고 현재 두 제품 모두 분산 모드로 출시될 것이라는 사실을 알고 있습니다. 하지만 그들은 여전히 서로 소통하는 방법을 모릅니다. 그리고 우리는 그들이 협력해야 한다는 것을 알릴 수 있는 방법을 마련해야 합니다 . Quadrant는 기본 실행 파일에 몇 가지 속성을 제공함으로써 이를 수행합니다 . 명령어를 제공함으로써 기본 동작을 재정의할 수 있습니다. 기본적으로 Quadrant 바이너리가 실행됩니다. 하지만 지금으로서는 이 첫 번째 인스턴스에 대한 URL을 제공해야 합니다. 저는 해당 컨테이너들을 기본 컨테이너와 보조 컨테이너라고 불렀지만, 그 둘 사이에는 마스터-슬레이브와 같은 통신 관계는 없습니다. 그들은 내부적으로 스스로 지도자를 선출할 것입니다. 하지만 첫 번째 인스턴스는 실행 방식이 특정적이기 때문에 먼저 실행되어야 하고, 그 후에 원하는 만큼 보조 인스턴스를 생성할 수 있습니다. 따라서 이 첫 번째 사례는 어떤 면에서 특별하므로 해당 사례 자체에 대한 URL을 제공해야 합니다 . Docker Compose를 사용하고 있으므로 이 특정 컨테이너의 호스트 이름 과 컨테이너 간 통신에 사용하는 프로토콜( HTTP)을 이미 알고 있습니다. 따라서 Quadrant를 기본 포트 인 6335로 설정해야 합니다. 6335는 동일한 클러스터에서 실행되는 모든 Quadrant 인스턴스 간의 기본 통신 포트입니다. 그리고 적어도 첫 번째 사례에 대해서는 이게 전부입니다 . 하지만, 여전히 차선책은 차선책에 어떻게 도달해야 할지 모릅니다. 또한 Quadrant 실행 파일의 기본 매개변수도 변경해야 합니다 . 이것도 비슷한 방식으로 진행되지만, URL 대신 부트스트랩 매개변수를 제공합니다. 이게 전부입니다. 적어도 기본적인 내용은요. 현재 기본 인스턴스와 보조 인스턴스를 실행할 수 있으며, 두 인스턴스는 이미 서로 통신할 수 있는 상태입니다. 그럼 차근차근 해봅시다. 우선 터미널 창을 열고 Quadrant 기본 인스턴스를 생성하겠습니다. 좋아요. 이미 시작됐습니다. 꽤 빠르네요. 지금 당장 저는 Quadrant의 또 다른 인스턴스를 생성할 수 있는데 , 이번에는 Quadrant 보조 인스턴스입니다. 그래서 Docker Compose up 명령어를 사용하면 두 번째 컨테이너를 실행할 수 있습니다. 이것으로 끝입니다. 내부적으로는 이미 서로 소통하고 있었고 지도자를 선출한 상태였습니다. 지금 당장 클러스터의 상태를 확인해 볼 수 있다고 가정해 봅시다. 어, 첫 번째 인스턴스의 포트를 노출시켰다는 것을 기억하세요. 그러니까 지금 당장은 curl 명령어를 사용해서 클러스터 상태를 확인할 수 있을 겁니다. 해당 주소는 localhost에 매핑되어 있으며, 일부 세부 정보를 노출하는 새로운 클러스터 엔드포인트가 있습니다. 보시다시피, 저희는 모든 동료의 명단을 작성했고, 리더도 선출했습니다. 하지만 만약 제가 Quadrant 보조 인스턴스를 하나 더 만들고 싶다면, 그보다 더 쉬운 방법은 없습니다. Docker Compose up 명령어를 사용하면 되고, 이번에는 Quadrant 보조 인스턴스를 두 개로 확장하면 됩니다. 그리고 지금 제 클러스터 내에는 서로 다른 세 개의 인스턴스가 실행 중이어야 합니다. 그럼 Docker 통계를 사용해서 두 프로세스가 실제로 동시에 실행되고 있는지 확인해 보겠습니다 . 좋아요. 저희는 현재 세 개의 컨테이너를 가동 중입니다. 우리는 그것들이 실제로 분산 모드로 작동하는지 아직 알지 못합니다. 그러니 먼저 컬렉션을 만들고 거기에 포인트를 넣는 것부터 시작하는 게 좋겠습니다. 그리고 저는 이미 그와 관련된 유틸리티 스크립트 몇 가지를 준비해 두었습니다. 첫 번째 기능인 ' 컬렉션 생성'은 컬렉션 엔드포인트로 PUT 요청을 보내 JSON 파일로 제공된 구성을 사용하여 컬렉션을 생성합니다. 그리고 주어진 설정으로 컬렉션을 생성합니다 . 좋아요. 터미널 창을 하나 더 열어서 컬렉션이 쉽게 생성되는지 확인해 보겠습니다. 좋아요. 응. 저희가 방금 컬렉션을 만들었습니다. 그리고 지금 당장, 우리는 거기에 몇 가지 포인트를 입력할 수 있습니다. 다른 JSON 파일에서 포인트를 업로드하기 위해 특별히 만들어진 또 다른 스크립트가 있는데, 그 파일은 바로 여기 있습니다. 이 파일은 약 7~8MB 크기 이며 몇 가지 수치 벡터 와 추가 기능을 포함하고 있지만, Quadrant의 이러한 광범위한 필터링 메커니즘은 사용하지 않을 것입니다. 지금으로서는 이것이 우리 클러스터 전체에 어떻게 확산될지만을 지켜보는 데에만 관심이 있습니다. 그럼, 이 스크립트를 하나 더 만들어서 모든 포인트를 업로드하고 Docker 통계와 모든 컨테이너의 메모리 사용량을 확인해 보겠습니다. 보시다시피, 이들 모두 저희가 API에 입력한 포인트 중 일부를 받았습니다. 데이터가 모든 노드에 고르게 분산되는 것은 아니지만, 단일 인스턴스만 있다면 거기에 전송된 모든 데이터를 포함할 것입니다. 지금은 세 가지 모두에 걸쳐 퍼져 있습니다. 그리고 우리는 그것을 가지고 있습니다. Docker Compose를 사용하여 단 몇 분 만에 완벽하게 작동하는 Quadrant 클러스터를 구축했습니다. 이 기능은 아직 실험적인 모드이므로 문제가 발생하면 Discord 커뮤니티나 GitHub 이슈를 통해 알려주세요.
