# [ifkakao2021] UI 테스트 경험기

- 영상 링크: https://www.youtube.com/watch?v=wGrh4LkaiFo
- 채널: kakao tech
- 업로드일: 2026-06-16
- 자막 언어: ko
- 단어 수: 약 1276개

---

## 스크립트

안녕하세요. 카카오에서 카카오톡 iOS 버전 개발에 참여하고 있는 제이라고 합니다.이 시간에는 카카오 톡계의 UI 테스트를 넣은 경험에 대해 이야기해 보겠습니다. 올해 초우토개는 카플레이와 시리가 추가되었습니다. 그로 인해 많은 리팩토링과 테스트가 발생했습니다. 그중에는 가입 테스트도 있었는데요. 개발 과정에서 직접 테스트를 진행하면서 그게 너무 귀찮고 번거로웠습니다. 어떻게 할까 고민하던 중 UI 테스트로 자동화를 구성해 보면 어떨까 하는 생각을 떠올렸습니다. 더구나 접근성과 관련한 많은 기능들을 잘 구현하고 있는 카카오 톡에 접근성을 사용하는 UI 테스트는 아주 잘 맞을 거라고 생각했습니다. 영상과 코드를 보시면 UI 테스트에서 어플리케이션을 실행하고 버튼을 눌러 다음 화면으로 이동하는 코드가 아주 간결하다는 것을 아실 수 있을 겁니다. 이렇게 처음 만난 UI 테스트는 마치 화면에 나온 그림처럼 망치로 못을 막는 것처럼 간단하고 접근하기도 쉬웠습니다. 이번에는 다음 화면인 약관 동의입니다. 몇 개의 필수 체크 버튼을 눌러 버튼의 활성화를 체크한 후 다시 모두 동의 체크 버튼과 동의 버튼을 눌러 다음 화면으로 넘어갑니다. UI 테스트의 API와 동작은 다음과 같이 세 가지로 묶어 볼 수가 있습니다. 첫 번째는 화면의 요소를 조회하고 두 번째는 조회한 요소의 상태를 체크하고 세 번째는 이렇게 가져온 요소의 터치 등의 사용자 이벤트를 전달하는 것입니다.이 예제 화면을 보시면 세 개의 체크박스가 있습니다. 예제와 설명을 통해 두 번째 월드 체크박스를 체크하는 방법을 설명해 보겠습니다. 먼저 화면 요소를 조회하는 방법입니다. 요소에는 여러 가지 타입이 있습니다. 여기서 월드라는 체크박스를 구성하는 요소 중에서 문자율을 표현하는 화면 요소를 나타내는 스태틱 텍스트 요소를 먼저 조회합니다. 스태틱 텍스트 요소들 중에서도 월드라는 문자열을 가진 특정 요소를 줘야 합니다. 그리고 해당 화면 요소에 동작을 전달할 수 있고 요소의 상태를 알려주는 XC UI 엘레먼트 객체를 가져옵니다. 마지막으로 조회한 요소에 사용자의 터치 이벤트를 전달합니다. 드디어 월드 체크박스의 체크가 효시되었습니다. 이제 요소의 상태 체크에 대해 알아보겠습니다. 상태에는 레이블, 밸류 등 여러 가지가 있는데이 중에서 테스트 C에 가장 많이 쓰이는 것은 바로 해당 요소가 화면에 존재하는지 알 수 있는 프로퍼티와 화면에 해당 요소가 나타날 때까지 지정한 타임 인터벌만큼 일정 간격으로 계속 화면을 조회해서 결과를 알려주는 웨이트 ex스턴스 함수가 있습니다. 다른 것들도 당연히 많이 쓰이지만 UI 테스트를 쉽고 편하게 만들어 주는 것은 바로이 피처입니다. 그리고 UI 버튼 등의 접근성 타입에 대응하는 엘리멘트 타입이 있습니다. 엘리멘트 타입은 이으로 되어 있고 총 83개가 존재하는데이 중에서 애니 타입의 경우 엘리멘트 타입과 무관하게 모든 화면 요소를 검색할 수가 있습니다. 이게 필요한 이유는 테스트를 진행할 때 프라이빗 타입을 가진 화면 요소에도 접근해야 할 경우가 있기 때문입니다. UI 타입과 UI 테스트의 엘리먼트 타입에 대해 간략하게 비교해 보겠습니다. UI 기술의 화면 계층을 화면과 같이 구성할 때 UI 테스트에서는 다음과 같이 표현이 됩니다. 조회의 마무리로 아이덴티파이어에 대해 알아보겠습니다. UI 테스트에서 아이덴티파이어란 하나의 값이 아닌 접근성의 여러 문자 요소들을 동시에 가르키고 있습니다. 아래의 세 코드는 모두 동일한데 첫 번째와 두 번째 코드의 내부를 살펴보면 모두 마지막 코드를 생성하게 됩니다.이 화면의 마지막 체크박스는 긴 문자열을 가지고 있습니다. 이것을 어떻게 선택할 수 있을까요? 전체 문자를 아이덴티파이어로 그대로 사용할 수도 있습니다. 다만 서스크립션이나 아이덴티파이어를 인수로 받는 함수를 통해 조회를 할 경우 글자수 제한과 관련한 예외가 발생하고 그대로 테스트가 종료됩니다.이 경우에는 NS 프리디케이트를 통해 직접 조회의 조건식을 만들어 주셔야 합니다. 이렇게 NS 프리드케이트를 사용하게 되면 문자열의 일부 요소 혹은 요소를 구성하는 이인에 등 다른 프로퍼티를 이용하는 다양한 조건으로 조회를 할 수가 있습니다. 이제 가입 테스트의 다음 단계로 넘어가 보겠습니다.이 화면에서는 전화번호를 입력하고 버튼을 클릭해 다음 화면으로 넘어갑니다. 그리고 그 화면은 SMS를 통해 인증 번호를 입력하는 화면입니다. 여기서 저는 그렇게 쉬워 보였던 테스트의 벽을 드디어 마주하게 되었습니다. 시스템 UI라는 벽에 대해서 알아보겠습니다. 화면에는 두 가지 형태의 익숙한 UI가 있습니다. 왼쪽과 같은 시스템 권한 언럿 그리고 오른쪽과 같은 문자나 다른 앱의 푸시를 표시하는 푸시 배너입니다. 자, 그러면이 시스템 UI를 어떻게 다루면 좋을까요? 첫 번째 방법입니다. 애드 UI 인터션 모니터 함수를 통해 테스트와 비동기로 동작하는 핸들러를 추가하는 방법입니다. 핸들러를 등록하게 되면 테스트 도중 의도하지 않게 나타나는 이미 얼러등과 같은 UI 이벤트를 가로치게 되고 인수로 발생한이 UI 요소를 전달받게 되어이를 적절하게 처리할 수가 있습니다. 두 번째 방법입니다.이 이 첫 번째 방법은 비동기로 동작하기 때문에 가로 체진 UI 이벤트와 테스트 중인 화면과의 상호 작용이 필요할 경우 어려움이 많습니다. 그래서이 두 번째 방법인 스프링보드에 접근하는 방법을 사용하면 테스트에서 접근하는 화면과 상호 작용할 수 있는 테스트를 아주 쉽게 작성할 수가 있습니다. 드디어 가입이 다음 화면으로 이동이 가능해졌습니다. 인증 화면에서는 인증 코드를 문자 메시지로 받았습니다.이 이 푸시 배너는 엘리멘트 타입에 존재하지 않는 타입이기 때문에 푸시 배너만의 아이덴티파이어와 애니 타입을 이용하여 조회를 할 수가 있습니다. 조회한 요소에서 문자를 뽑을 수 있고이를 가공하며 인증 코드를 추출할 수가 있습니다. 이렇게 가져온 요소에서 직접 문자열을 가공할 수가 있는데 이런 암호 입력이나 엔진 코드 입력 등의 경우 아이폰의 SMS 암호 자동 체육 피처를 사용하면 좀 더 쉽게 인증 코드를 입력할 수가 있습니다. 자, 제가 만난 두 번째 벽 두 개의 텍스트 필드입니다.이 화면에는 두 개의 텍스트 필드가 있습니다. 텍스트 필드의 키보드의 포커스를 이동하기 위해서는 사용자의 터치 이벤트를 전달하면 되고 단순히 각 텍스트 빌드에 포커스를 이동해서 암호를 입력하면 되는 간단한 테스트 코드입니다. 네, 그렇게 보입니다. 특정 조건에서 두 개 이상의 텍스트 필드가 있을 때 포커스의 이동과 관련한 이슈가 있습니다. 여러분의 테스트 디바이스나 시뮬레이터에서 블루투스 등 하드웨어 키보드가 연결되어 있는 경우 먼저 말씀드리면 단순히 탭으로는 키보드의 포커스가 제대로 이동하지 못합니다. 두 번째 필드에 터치 이벤트를 전달하면 캐럿이 이동하여 화면상으로는 정상적으로 포커스가 이동한 것처럼 보이지만 실제로는 이동하지 않아 타입 텍스트 함수를 통해 문자율를 전달할 경우 에러가 발생하고 테스트가 멈추게 됩니다. 어 이것을 해결하는 방법은 두 가지인데요.이를 이를 실행하는 방법은 세 가지가 있습니다. 먼저 시뮬레이터는 어플리케이션이기 때문에 자기 설정값이 존재합니다. 시뮬레이터가 처음 만들어지면 키보드의 기본 연결 설정이 하드웨어인데이를 설정해서 직접 소프트웨어로 바꿔 주는 방법입니다. 두 번째 방법은 시뮬레이터에서 소프트웨어 키보드로 변경하거나 앱을 실현할 때 아이폰의 키보드 설정과 관련한 UI 텍스트 인풋 모드에 프라이빗 메소드를 사용하여 직접 소프트웨어 키보드를 변경하는 방법입니다.이 이 방법들은 모두 테스트 환경이 아닌 곳에서 작업을 해야 하고 하드웨어 키보드가 필요한 경우 다시 변경해야 한다거나 코드를 직접 수정하는 침습적인 방법을 사용해야 하는 등 불편함을 유발하며 여러분이 언젠가 자동화 테스트를 시도하고 싶을 때 방해가 될 가능성이 큽니다. 마지막 방법에 대한 영상입니다. 간단하게 다른 텍스트 필드로 포커스를 이동하기 전에 잠시 홈 화면을 다녀오는 방법입니다. 테스트 코드만으로 하드웨어 키보드와 텍스트 필드의 이슈를 해결할 수가 있습니다. 자, 고생이 있었지만 이제 어려운 벽은 넘었습니다. 가입의 마지막까지 UI 테스트를 진행하는 모습을 보시겠습니다. 이번에는 제가 테스트를 진행하며 사용한 두 가지 팁을 알려 드리겠습니다. 먼저 앱을 삭제하는 방법인데요. 테스트를 진행한다면 이런 가이 테스트와 같은 경우 앱의 여러 가지 데이터의 초기화가 필요할 수 있습니다. 그럴 때 내부적으로 복잡한 코드를 넣는 것보다 이렇게 간단하게 스프링보드에서 직접 앱을 삭제할 수가 있습니다. 조회 사용하는 API는 모두 재사용이 가능하고 한번 만들어졌다고 해서 가지고 있는 값이나 의도가 고정된 것이 아니기 때문에 이렇게 만들어진 조회를 와일문 등으로 동일한 이름을 가진 앱을 삭제하는 동작을 반복해서 실행할 수가 있습니다. 다음은 디버깅을 사용하여 화면에 접근 가능한 요소를 쉽게 알아내는 방법입니다. 다음과 같은 코드가 있을 때 테스트 실행전에 브레이크 포인트를 적절한 라인에 걸어 주세요. 그리고 디버그 프로포트에서 여러분이 접근해야 하는 어플리케이션 객체를 프린트 오브젝트 명령어를 통해 출력하게 되면 화면에서 접근 가능한 모든 정보를 확인할 수가 있습니다.이 방법은 직접에서 어떤 값으로 테스트를 진행해야 할지 확인하기 어렵거나 화면에 보이는 요소와 접근성에서 확인할 수 있는 요소가 서로 다를 때 이런 방법으로 쉽게 찾아볼 수가 있습니다. 그 외에도 알려 드리고 싶고 아직 제가 부족해서 구현해 보지 못하는 내용이 있는데이는 여러분의 즐거움으로 남겨 드리고 저도 다음 기회에 좀 더 구현해서 알려 드릴 수 있는 기회가 있다면 좋겠습니다. 다음과 같은 경우에 UI 테스트를 넣어 보시기를 추천드립니다. 어떤 서비스에 대한 테스트를 넣고 싶은데 비즈니스의 동선이 길고 테스트를 넣기 위해 많은 수정이 필요해서 당장 시도하기 어렵다 하시면 우선 UI 테스트를 넣어 안정감을 찾으시고 진행하시면 도움이 될 것 같습니다. 그리고 접근성을 잘 지원하고 있다면 UI 테스트를 넣기에는 가장 좋은 환경이 될 것 같습니다. 그리고 접근성을 잘 지원하고 싶다면 UI 테스트를 넣으시면서 접근성의 피처를 잘 적용하시고 접근성에 대한 직접적인 테스트를 하시기 때문에 UI 테스트로부터 시도해 보시는 것을 추천드립니다. 테스트와 관련한 철학적인 이야기는 저도 아직 부족하기 때문에 여기서는 아무 말도 하지 않겠습니다. 다만 테스트를 잘하려는 모든 방법들의 시작은 테스트와 관련한 API 즉 도구에 대해서 잘 알아두는 것이라고 생각합니다. UI 테스트는 유닛 테스트보다 접근하기가 쉽고 API를 배우며 작업하기도 좋은 거 같습니다. 여러분도 이렇게 테스트를 시작하시고 암의 주원인 스트레스도 줄이며 안정적인 서비스와 평온한 개발 생활을 영행하실 수 있다면 좋겠습니다. 여기까지 봐 주셔서 감사합니다.
