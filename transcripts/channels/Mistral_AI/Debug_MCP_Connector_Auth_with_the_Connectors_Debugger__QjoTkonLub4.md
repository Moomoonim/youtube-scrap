# Debug MCP Connector Auth with the Connectors Debugger

- 영상 링크: https://www.youtube.com/watch?v=QjoTkonLub4
- 채널: Mistral AI
- 업로드일: 2026-06-24
- 자막 언어: ko
- 단어 수: 약 675개

---

## 스크립트

연결 디버거는 Studio의 새로운 기능으로, MCP OAuth 인증의 모든 단계를 실행하여 [음악]이 어떤 단계에서 실패했는지, 그리고 실패했을 때 서버에서 실제로 어떤 메시지를 표시했는지 정확하게 보여줍니다 . MCP 커넥터가 인증에 실패하는 경우 , 문제는 여러 단계로 이루어진 OAuth 핸드셰이크 과정 어딘가에 있습니다. 각 단계를 제대로 파악하지 못하면 결국 추측만 하게 됩니다. [음악] 오늘은 디버거가 실제 인증 실패 두 가지를 포착하고, 각각의 의미를 살펴보고, 두 실패가 모두 수정되는 과정을 지켜보겠습니다. 또한 12단계 각각이 실제로 어떤 작업을 수행하는지 자세히 설명하여 서버에서 문제가 발생했을 때 어떤 상황을 확인해야 하는지 알 수 있도록 하겠습니다. [음악] 문서와 스튜디오 링크는 설명란에 있으니 바로 시작해 볼까요? 지금 접속하려는 서버는 제가 만든 데모 MCP 서버라서 디버거가 어떻게 작동하는지 확인할 수 있습니다. 이 시스템에는 특정 단계에서 오류가 발생하도록 설정한 완벽하게 작동하는 OAuth 2.0 구현이 포함되어 있어 디버거가 어떻게 작동하는지 확인할 수 있습니다. [음악] 디버거는 모든 MCP 서버에서 작동합니다. URL을 입력하면 동일한 12단계 검사 과정을 거칩니다. 실행해 봅시다. 서버 연결까지 277밀리초가 걸렸습니다. 인증 챌린지를 분석하고 통과했습니다. WWW 인증 헤더는 형식이 올바르며 디버거는 다음에 무엇을 해야 할지 알고 있습니다 . 보호된 리소스 메타데이터를 가져오는 데 278 밀리초가 걸렸습니다. 해당 서버는 잘 알려진 OAuth로 보호되는 리소스 문서를 가지고 있으며, 올바른 인증 서버를 가리키고 있습니다. 인증 서버 메타데이터를 가져오는 데 410 밀리초가 걸렸습니다. 이제 클라이언트는 토큰 엔드포인트 URL, 등록 엔드포인트 URL, 그리고 지원되는 권한 부여 유형 목록을 갖게 되었습니다. 인증 방법을 감지하고 통과합니다. 서버는 PKCE를 이용한 인증 코드 흐름을 지원합니다. [음악] OAuth 클라이언트를 등록하려고 하는데 실패합니다. 디버거가 서버에서 캡처한 내용은 다음과 같습니다 . 오류 메시지는 "유효하지 않은 클라이언트 메타데이터"이며, 설명에는 서버에서 동적 클라이언트 등록이 허용되지 않는다고 나와 있습니다. 좋은 점은 무엇이 잘못되고 있는지에 대한 명확한 메시지를 가지고 있다는 것입니다 . [음악] 클라이언트가 RFC 7591을 사용하여 자체 등록을 시도했지만 서버가 거부했습니다. 실제로 이런 상황은 두 가지 경우에 발생합니다. [음악] 서버에서 동적 등록이 비활성화되어 있거나(이는 합리적인 보안 선택입니다), 서버 소유자로부터 미리 등록된 클라이언트 ID와 비밀 키가 필요합니다. 또는 등록 엔드포인트가 잘못 구성되어 있어서 반환해서는 안 되는 오류를 반환하고 있을 수 있습니다. 디버거가 캡처한 응답을 통해 어떤 문제인지 알 수 있습니다. 허용되지 않음이란 해당 기능이 의도적으로 제외되었음을 의미합니다. 500 오류 또는 잘못된 형식의 응답은 서버 측 버그를 나타냅니다. 데모 서버에서 이러한 오류는 의도적인 것이었습니다. 저희는 이 정확한 오류를 시연하기 위해 서버에서 회원가입을 차단하도록 설정했습니다. 해결 방법은 등록을 활성화하고 재배포하는 것입니다. [음악] 제가 이미 그렇게 했습니다. 디버거를 다시 실행해 보겠습니다. 1단계부터 5단계까지는 모두 이전과 동일하게 통과했습니다 . OAuth 클라이언트 등록, 136 밀리초, [음악]이 경과했습니다. 서버는 클라이언트 ID와 클라이언트 시크릿을 반환했습니다. 인증 URL을 생성하고 비밀번호를 입력합니다. 사용자 동의를 얻은 후 통과합니다. 인증 코드 교환, 227밀리초, 또 다른 오류가 발생했습니다. 여기서 우리는 우리가 발견한 또 다른 좋은 오류 사례를 발견했습니다 . 토큰 엔드포인트에서 발생하는 오류 본문은 '지원 되지 않는 권한 부여 유형'입니다. [음악] 자, 지금 무슨 일이 벌어지고 있는지 알려드릴게요. 인증 코드 교환 단계는 클라이언트가 액세스 토큰을 받기 위해 토큰 엔드포인트에 인증 코드를 전송하는 단계입니다 . OAuth 핵심 사양인 RFC 6749는 요청을 JSON 폼 인코딩이 아닌 URL 인코딩으로 전송해야 한다고 규정하고 있습니다 . 데모 서버는 JSON 요청 본문만 파싱했습니다. 그래서 MCP 클라이언트가 사양에서 요구하는 대로 폼 인코딩된 요청을 [음악]에 보냈는데, 요청 본문이 완전히 비어 있는 상태로 도착했습니다. [음악] 권한 유형이 정의되지 않았으며 서버에서 지원 되지 않는 권한 유형을 반환했습니다. 해결책은 Express의 URL 인코딩 미들웨어를 등록하는 단 한 줄의 코드였습니다. 이렇게 하면 토큰 엔드포인트가 요청 본문을 실제로 읽을 수 있게 됩니다. 커넥터 디버거가 없었다면 이 문제는 " 토큰 교환 [음악] 실패"라는 모호한 메시지로만 표시되었을 것입니다. 덕분에 정확한 HTTP 상태 코드와 오류 본문을 얻을 수 있었고, 이를 통해 문제를 즉시 파악할 수 있었습니다. 서버가 재배포되었습니다. 한 번 더 실행해 봅시다. 1단계부터 9단계까지 모두 통과했습니다. 토큰 유효성 검사 후 통과. MCP를 초기화합니다. 553 밀리초 소요 후 통과합니다. [음악] MCP 악수가 성공적으로 완료되었습니다. 보고서 개요에서 전체 실행 결과는 정상입니다. 그게 다입니다 . 커넥터 디버거는 MCP OAuth 인증의 12단계 전체를 실행하여 구성 선택 오류 하나와 구현 버그 하나, 총 두 가지 실제 오류를 발견하고 두 오류를 수정하는 데 필요한 정보를 정확하게 제공했습니다 . [음악] 그게 바로 도구의 용도죠. 일반적인 인증 오류 대신 핸드셰이크의 모든 단계에서 실제 HTTP 응답을 받게 되므로 문제를 찾는 데 시간을 허비하는 대신 문제를 해결하는 데 시간을 할애할 수 있습니다 . 설명에 링크된 커넥터 문서를 확인하고, Studio에서 MCP 서버를 대상으로 실행해 보세요 . 시청해 주셔서 감사합니다. [음악]
