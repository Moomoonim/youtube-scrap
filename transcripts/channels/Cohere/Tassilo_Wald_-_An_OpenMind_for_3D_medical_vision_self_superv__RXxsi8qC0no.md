# Tassilo Wald - An OpenMind for 3D medical vision self supervised learning

- 영상 링크: https://www.youtube.com/watch?v=RXxsi8qC0no
- 채널: Cohere
- 업로드일: 2025-11-14
- 자막 언어: en
- 단어 수: 약 8235개

---

## 스크립트

Hey everybody, uh we are back with another guest spe guest speaker session and today we have Tilo Wild with us. Um Tilo is basically a PhD student at German cancer research in the division of medical imaging in hiddenberg. His research focuses on pre-training for 3D medical imaging analysis and he recently had a publication in ICB which he's going to talk about. Um he he is currently interning with Microsoft health futures and he has been working on the human radiomic project as well. Um so he's going to talk about self-supervised 3D learning and 3D imaging today. Um over to you Tilo. Um thank you so much for joining us today. &gt;&gt; Yeah, thank you so much. Uh let me just share my screen. All right. Yes. So, um today is about um a recent project that I've done with um collaborators. It's called um an open mind for 3D medical vision cells supervised learning um which was joint work together with Constantine Olri. You can see him on the bottom left. Let me get my laser pointer here. and with Yonatan Zubriy um a student of ours and um since this is like um a recent project and a follow up basically from a previous um paper we'll also talk a bit about the revisiting MAE pre-training which um started like this whole journey of ours and um to pick you up we were starting this in like 2024 or like late 2023 in December and we were like um in talks with a collaborator and um they wanted to do self-supervised learning and we were wondering um why no one is using 3D medical self-supervised learning at the current stage. I mean we were doing a lot of medical imaging and like a lot of self-supervised learning and we were wondering like supervised learning in segmentation and we were wondering why do we still train like our um 3D medical models from scratch. Um maybe sometimes we use some supervised pre-training but um we were just wondering like where is all these self-supervised models at that you can see in the natural imaging domain where you have your clips and you have you have your um your dinov2s why don't we have that in in the medical field and why does no one use it and um with this question in mind we started a bit of literature research and um went to like scholar and looked for medical supervised learning and found this vocal paper. This was um published in CVPR 2024 and um wanting to like get a feel of the entire domain. We started off like with some questions like what data sets do these papers pre-train on? What common benchmark data sets are used? Um which architectures do they use? um how do they do their downstream evaluations? Do they do segmentation or classification and on which data sets? And how good are these models in general? So like what kind of performance benefits can you expect when you start with medical self-supervised learning in 3D and um given this paper we with these questions in mind we skimmed through it or like read it and um looked for like key phrases describing this and we found like statements about the pre-training data set sizes being like 1.6kct six KCT scans here. Then um regarding implementation architectures, we found like swing unit R as a architecture and then um as downstream data sets stuff like BTCV, lits, MSDS, spleen, Brett's 21 and um given like these keywords, we were like a bit surprised because um if you look at 1.6k CT scans as pre-training, it's like not that much data. If you look at for example total segmentata which was public at the time it came with 1,200 CT scans which are supervised. So the data scale is not really that much higher as you would like hope for. And um same with the architecture we were like um since we are like in the segmentation space not really like that convinced that swing unit R is really like a state-of-the-art architecture. We've just done like a benchmark study recently um n unit revisited where we like revis checked out all these new architectures and these fancy mamba architectures or transformer architectures and swing unit R just didn't perform that well. So um we were not really sure if like performance improvements on swing unit R would translate to um performance improvements once you're at the like performance optimum like or like the performance limits as you are in in unit or metex regimes. So we are not convinced and um also the evaluation that we've seen we in the same study we also like evaluated the like quality of a data set for benchmarking and a lot of the data sets that were used like BTCV breads 21 um you can see them very low here and um these data sets are just not like that good in like yielding consistent results um and allowing one to distinguish if a model actually performs better um than another method. So um we were a bit yeah unsure like if this means the method is good or not and um what performance you can expect. So we like kept searching um and oh yeah one last point I forgot is that uh since we are like the nunit lab um which is like um automated segmentation method that is very prominent in 3D. We were hoping to see it because it's usually self-configuring and um usually um this means that a practitioner doesn't really have to do a lot to get values of it but unfortunately we couldn't really find the values here as like a reference value. So you don't really know um how good the meth the the method and like the pre-training in the end actually performs and um yeah we took this and um found like these issues small pre-training data set sizes non-state-the-art architectures an okayish evaluation that could be improved with some more like robust data sets or Ellen unit maybe as a reference and then we um looked at other papers and we found like a very similar uh um pattern like also some um maybe not state-of-the-art architectures um not that large data set sizes of 5,000 subjects and um looked at additional papers where they for example use a lot of scans like 110,000 which is quite large already and then um unfortunately they propose like new architecture in combination with the pre-training so like the um performance of the pre-training method is not really isolated you don't really like judge how good it performs because usually ablations are lacking and then also you can see down here they evaluate on three data sets um head and neck tumors and with 48 cases and then like signapse of 30 cases so um BTCV again so overall like very very small data sets and questionable um results therefore so you don't really know like how good this method really is. Yeah. And um doing this for a lot of papers, we found that um this is quite consistent across the entire field of 3D medical self-supervised learning. A lot of them train with really small pre-training data sets. Um they use very different architectures also oftent times like transformer architectures which at this point of time were really like um a lot worse than they um have become recently. And um all of them also did different downstream data sets. they use different um um they use very little downstream data sets and sometimes they also pre-train on downstream data sets. So even if they use the same downstream data sets um to evaluate their model, you would not really be able to like make a distinction if one is better than the other because um one might have trained on it and the other might not have trained on it. And lastly um like as a a chip in um in all the evals from our perspective they hardly did comparisons to any unit. So you would never really know if their um training setting is really well well tuned and well built. And um with that we um compiled this whole list and found that basically a lot of different architecture. So um at this point in time as I mentioned transformers are they evaluated really poorly? So you would not really know like do pre-training methods work which one is the best what is the current state was a really difficult question and um down here this is like us where we try to um like address these issues and like use large data sets and so on. Now, yeah, as I already um foreshadowed, we um answered the question that um there's not really a convincing method. There's um the methods usually train on small data sets. They don't train state-of-the-art architectures and they um don't really follow great evaluation practices. So, we were wondering at this point in time, does it even work? So, is self-supervised learning even worth it in 3D medical imaging? And with that we set out to um our first project which was the revisiting MAE pre-training um which preceded um the one that I will talk later about. And in there we tried to answer this question. We tried to answer does pre-training even work for segmentation as downstream task. And um we at the beginning set out to like limit our search space space a bit. So we wanted to of course pick like a state-of-the-art architecture and um this is still CNN's so we picked like a CNN architecture. uh we decided on training paradigms and in the natural imaging domain we found MAE pre-trainings to work really well while a colleague tried contrastive pre-training and um did not really see any improvements back then and um also like more fancy methods like Dinov2 are pretty optimized and then transferring like these 2D methods to 3D imaging is really difficult because you run into a lot of engineering hurdles with your like um VRM constraints so you can't do large batch sizes and um without like having to see like entire images um not being able to see entire images and then also like the data set size in general. So you can't just like take a 2D method and transfer it to 3D imaging. So yeah, we picked MAE and then um as evaluation we chose to like be very rigorous and we um followed like the NNU unit process. We picked a lot of development data sets at the beginning which were not touched at um at um which were not like contaminating our test um evaluation in the end and um yeah had like multiple independent test data sets which were not used during the development phase. So basically we optimize our like pre-training setup on these development data sets and then see if this pre-training um optimized setup transfers to the independent test data sets. Now this um whole project as I like um said in the beginning was with a collaborator which was a German startup called Floyd which provided us with their proprietary data which was about 44,000 3D images of head and neck MRIs with like a lot of diverse spacings and acquisitions. And in order to make this data somewhat usable and not like um shred our model with like sampling bad bad images, we did some like basic data filtering of removing very very um weird images that you would not really see during application. And um with that like data set filtered we went to the development cycle which um uses these development data sets to select some parameters do training and fine-tuning and then like look at the results and iterate and choose like a really good setting and um through this development cycle we basically ablated a lot of MAE parameters which don't really matter too much for this talk and um a lot of fine-tuning settings which we'll come later to as Well, and um however through this like development cycle and through this process, we in the end got a model that showed really like reliable performance and actually also performed better than NNUT which is um these two columns here and um with that we basically answered our question which we wanted does pre-training work yes it does and uh we can actually improve over like from scratch training using for example a state-of-the-art MA E pre-training. Now um this was a bit um with some asterises. So we had the um the finding that pre-training works but we only know that it works for segmentation. And then um we also found that m image modeling is better than contrastive learning given the baselines we selected. However, the baselines were rather limited and um we also found that that the adaptation schedule was very important. And the big butt of this was that um the pre-training data set was still proprietary. So even if we published this work and other people would read it and find it cool and want to follow, they could not reproduce our results and our training setup. Um we also um during this this paper found that the inclusion into NNU unit to provide segmentation results was very like laborious took a lot of hours and like manual work which was really annoying and would make like the adaptation of like SSL really bad and um so this was like an issue and also in this earlier work we only evaluated CNN's and no transformers at all. So we did not really know maybe transformers perform well if you have large data sets and you do good pre-training with it. Um and this this study did not answer that. And lastly of course we only evaluated segmentation. So this was also a big limitation. And with like this um picture of the state of SSL we decided to um tackle a different question. How can we unify like the self-supervised learning domain for 3D medical imaging and how can we enable follow-up research to um be reproducible and to also have impact on the on the broader field. Yeah. And this this um sparked our open mind work that I was invited here to talk about. So um in this paper we tried to um unify and enable SSL research in 3D medical imaging and as um like mentioned there were like a lot of pitfalls that are still blocking SSL to become really prevalent in the domain and in this work we try to a make SSL easier for developers. So we try to like get some large pre-training data set um public that isn't proprietary. So um enable like getting data easily for like a developer then um we found the SSL development to still be difficult because if you have large data sets prep-processing and data handling is not that easy everyone publishes like their own implementation on GitHub but um it's very specific to their own data set and if you transfer like their pre-training to yours it might not work. So we wanted to um like unify the implementations a bit. Um and lastly we also found that like as I said like the NNU unit adaptation so if you have your model pre-trained moving it to a downstream application is really difficult and annoying. We wanted to make like tackle that issue. And from the user side of things if you're just applying and doing some segmentation or imaging work we wanted to make um using sales learning more attractive for users as well. And we identified um a roadblock of um the uncertainty in the space. So no one really knows what kind of method is the best because everyone has their own data, everyone has their own downstream evaluation um evaluates on their own like architecture. No one really knows what was best at that time. And um depending on what kind of um like downstream problem you have, you might be more interested in segmentation and classification. And usually these papers would not evaluate both and like cherry pick like one or the other maybe. And um lastly also like even if there's like a pre-trained checkpoint, it might be difficult to integrate this into your workflow. And we try to like um or we at least we identified this roadblock as well. And um yeah, we try to tackle these in this project. And starting off with these like large data sets um being not available, we um looked in the domain looked like what kind of data is out there and found like these four big um like what what do you call it like a big unified data set that you could just download it if you had access. However, um ABCD is an NIH study of um the adolescent brain cognitive development. So it's 41,000 brain MRI images. However, unfortunately, they are very restricted. So, you need to like sign up on um their portal and submit a project um proposal and then like some um premium will look over it and um check if it's actually like a feasible project and then you might get approved. So, since this like this is like um conditional on having a cool research proposal, this might like limit reproducibility. just imagining that if you propose um a project and get rejected, you can't really use this data set. Then uh we have the NLST data set which is um the national lung screening trial where um there are 72,000 chest cities or those chest cities but um those are dicom and usually um diccom is very laborious to handle and very difficult to convert. Making sure like all the data um is consistent and reproducible and everyone like um is able to convert it is not that easy. So uh we also went on from that and UK bioank which is probably like the largest public semi-publicly available data set um has more than 100,000 full body brain cardiac images um all MRI. However, unfortunately like they they recently um gated their data to be only used in their um research analysis platform um which is basically an online computing instance where you um can buy credits but you can't really download the data and integrate it into your workflow easily. So everything has to happen on their side. also of course making like reproducibility an issue and um honorable mention here is CT rate which came out recently uh but this was too late for our project unfortunately back in the day. So um what we found is that all the different data sets is are not really like publicly available easily or usable easily and um then we stumbled across open Euro which is a um a neuroscience platform where um most of the time fMRIs are imaged so some functional studies of um the human brain but um since every functional image is usually registered to some T1 image um all of these fmis also come with like at least one structural image where where the FMI is usually registered to. So um we were lucky and found a very large database which also was CC0 license so you could do whatever you like with it. And um lastly since this was like um composed of more than 800 different public data sets uh we were able to um get like a large diversity of pre-training data um yeah for our project in this case. And um what we did is that we of course crawled like this platform and um pre-processed whatever data was there. We also took the diffusion images that they had which are um 3D plus time and pre-processed them do through a um normal um diffusion imaging processing pipeline um where we in the end got um T2 um mean diffusion and fractional and isotropy maps which are static 3D images that can be derived from this 3D plus time um diffusion image. Overall um this of course ended up in 114k 3D brain MRIs uh for which we also did like some post-processing trying to identify images where there's for example anonymization in the image. So in case you want to train an M like an MAE objective, you can ignore these regions where there might be like some structure cutaway or some um like phase blurred. And um yeah, here's like a small stat of this data set. Um you can see that 50% of the um entire data is contributed by 12 um data sets whereas um 81 make up 75% and 283 make up 90% of the data with the longtail just contributing the last 10%. And um yeah, this in the end became the open mind data set which you can find on hugging phase and directly download for whatever project you like and use as you will. Now given this pre-training data set, we tick the first box. We have a data set that everyone can use. Great. Now what are the remaining like roadblocks of it? And um first of all we decided to um use this data set then to develop a large uh benchmark and test different methods. So um how to read this plot here is that um all these four first rows are trained from scratch. Um above is the NN unit baseline and down here is the same architecture trained for either a longer um period or for a shorter training schedule. And then we have um yeah highlighted in blue is like the longer schedule here. Then we have the data sets. So we have different data sets since we are all brain that are in the brain region as well. We have some stroke lesions, brain metastases, uh head and neck tumors, whatever's um available. And then we also have some um more out of domain regions and data sets. So for example we have a um heart MRI we have some and some um abdomen CTS with Amos and kits for example and um these indistribution and out of distribution data sets are averaged down here yielding like these columns over there. Yes. And um the first finding that we had of course is that um mast outen coding is is quite strong. So compared to a from scratch um pre-training uh from scratch training the pre-training with mass autoenccoders down here performs really well in particular on average in in domain all of these methods are performing very very strongly and um if we go and compare this to like contrastive methods for example vocal swing unit R pre-training and similar we can see that those perform not that great with some pre-training methods even being worse. than training from scratch. And um it's maybe also to note that the swing r here is not to be confused with the architecture itself. So there's like one architecture and one pre-training paper and um in the pre-training paper they propose a certain method and this swing r here refers to the um this this pre-training method and not the architecture itself because like as you can see up here like all of these methods are using the same architecture. Um yeah comparing to previously we also moved to transformers. So we um trained a Primos M transformer which was um also published rather recently at the point of this paper. And as you can see um these contrastive methods perform really really horribly in this case. We are like substantially worse than training from scratch. Um and again the MAE perform really well when um when using a transformer and um those ones perform quite a bit um stronger than um the from scratch trainings. Now um overall we found the MAE like the basic MAE object objective to be or pre-training method to be the best um quite consistently and um yeah now um having evaluated segmentation we also wanted to be a bit more holistic this time and try to um look at classification which is not really a wellestablished domain. So unfortunately um oh yeah so we have um resangel and primos again. So this time they're not above each other but like left and right. And then we have um these data sets here which are an MR net. It's like a knee tear data set. Then RSNA which um is a classification of um spine fractures. And then uh abide which is an autism detection for um either autistic brains or not autistic brains. And again two different metrics that we evaluate here um average precision and balance accuracy. And um as I was about to say earlier we have the issue in the classification domain that there's no real NN unit in there. So we don't really have like a from scratch autoconfiguring pipeline that allows us to really train something and see how good one would actually get. This is probably because classification is a very very difficult problem in 3D because you have very little data. you have um hardly any supervision signal because like one image is um like paired with one label whereas for segmentation you usually have like these masks and like many different pixels which give you some supervision signal allowing you to train properly and in in classification you don't really have that so you need usually a lot of data and hence um also targets diverge a lot so autism I don't know how one would detect it um I I don't know like like small um it might be a global thing that you can see honestly I don't know but of course this fine fracture thing will be very localized very small so like the automatic configuration might have to be very different for these different data sets. So um long story short, there's no real like baseline we could have easily implemented here and um therefore it's hard to know overall of this table if our configuration is great or not. And uh we just try to do our best and be um consistent in our setup. Now um this being said, we can look again at from scratch training and we can see that CNN's perform a lot better. So over here we get like means of 63 64 whereas over here we get like 57 and 61. So overall um CNN's perform a lot better than transformers um in classification at least which is somewhat surprising given that usually you assume transformers have global reasoning classification might be more of a global task. Transformers should be better but empirically we just found CNN's to still be really really great in this. Now um even further we find that contrastive pre-training is um for the case of classification a lot better which is also not really surprising. It's a finding that is mirrored in the 2D natural imaging domain. You usually um see that contrastive training performs really well there. However, um dense reasoning is is not really like a strong suit of contrastive methods because you just like learn global global um pairings or like you just learn like a global objective. So there's no real dense reasoning happening and um this is also mirrored in our results. So we see also CNN's and transformers to benefit of it with CNN's benefiting a lot more than transformers. And uh finally we also see the same story as usually that um MAE pre-training is not really that good for classification. Instead we just have um like sometimes lower than from scratch results here or at least rather consistently. And for the actually for both CNN's and transformers we see that MAE don't really allow that good of a or don't learn that good of like global embeddings. So um this means that comparing the classification and segmentation results from the two tables that there's no real pre-training that is good at both and uh yeah there's like a lot of research gaps still in this space here now um let's skip that or maybe yeah okay maybe let's talk about it um because the adapt regarding the adaptation there's a lot of different um degrees of freedom as well um we didn't really like highlight this in the previous um or in the in the pitfalls at the beginning. But if you ever like train your model and pre-train your model, there's a lot of different ways that you can adapt it. So for example, for um classification, you can decide to do like some mean pooling to get um global embeddings from some like dense dense encodings or you can um maybe have a transformer that has a classification token. you don't really um have like one way of getting a global embedding and the same also applies for how you do segmentation. In our case, we try to unify this as well. We try to like define two exact ways. How we take pre-train our CNN and how we do adaptation with our with our CNN and how we pre-train our transformer and how we adapt our transformer in the end. And um what we did in the case of um CNN's is that we um have an usually like um encoder decoder unit style architecture and of course we use that however the CNN decoders um were never transferred. We just like after pre-training threw them away because some of the methods like uh contrastive training does not train a decoder at all. So if you do a SIM CLR, you just have your CNN encoder and then you do some um like cooling and get like a global embedding, but you never really train like a decoder that reconstructs like a dense space. So you could not transfer that. And in order to be consistent across pre-training methods, we try to eliminate this factor of variation and said, okay, we only keep the encoder and then always do the same. we initialize like a decoder from scratch either for a CNN or transformer but it has to be consistent and further um we also have the question of okay I have my pre-trained encoder CNN or transformer and I have my decoder how do I train it now the CNN decoders or transformer decoders are initialized from scratch so if you just start training you're going to get like a lot of bad predictions at the beginning and your um loss and your gradient will be very large. And if you um maybe choose a bad um learning rate schedule, you might just um unlearn whatever you learned during pre-training during pre-training. So this aspect of um pre-training is of course not really unified across papers. So we try to um set a few standards for how do we do the um fine-tuning and adaptation of our pre-trained models. Now what you can see here is in in dark blue the learning rate curve of encoder and decoder. In light blue it's like the learning rate when we are um just um training the decoder. So for this case like the encoder remains frozen. Here we have some joint warm-up and then I think it's light blue actually. So like some decoder warm-up and then joint training with a um normal schedule. And here we have a um decoder warm-up and then um joint warm-up and normal training and otherwise we have like this learning rate valley here. Now um since we wanted to um be fair, we tried to evaluate these different learning rate schedules um in segmentation itself, we already found that CNN can usually easily handle like a default training schedule where you just start with a really high learning rate. Whereas if you do transformer training from scratch, you usually want to have like some warm-up and then train your model. Otherwise um you just run into training instability. So you just want to um um yeah know you you know that we knew that for transformers you needed this. So we try to ablate this for both the architectures and all the methods which is um provided in this table here where we um check the different data sets and different um fine-tuning schedules which you can see here in these um rows and then up here you can see the different methods and we try to um average the overall performance of all methods to be somewhat fair to all methods and um then we chose like the best average um value um in overall dice performance and this became the default startup and this this became the default learning rate that we um adapted our um pre-trained encoders with. So um what you can deduce from this table is that having a frozen encoder is really really bad. So whatever kind of embeddings you currently create and get from your self-wise learning methods is not really usable out of the box. You have your frozen encodings here that are like substantially worse. Then uh for the CNN setup, we saw that like the sawtooth pattern here where you go um warming up warming up the decoder and then warming warming up both is the best. Um, and then for the transformer, um, we found that a different kind of warm-up schedule is better where you just, um, warm up everything together and then train the transformer. And, um, overall, if you look at the performance, um, the performance variation back here, you can also see that the, um, the schedules are very important. So compared to the um overall from scratch um oh compared to like um how much performance gains you get which is usually like one or two dice points having a variation of like one point in performance in the end can mean um that either your pre-training works or it doesn't. So we found this to be very important and hence it's um also a signal and um a problem of the domain that if you don't have this unified as a factor it might just be that some um new method that you build and that is published just uses a different um fine-tuning schedule and it is not really better than your baselines because the baselines um just um don't work with this schedule or that this architecture doesn't really like this schedule. So, um this is like an important under underappreciated aspect of the entire pre-training domain. I think now um summarizing the like the findings of the paper, we find that um local tasks are really good with mast image modeling. So, if you do a pre-training with mast outenccoders, you're great. If you want to do um like classification use or some other global task, do contrastive pre-training at the moment. But at the unfortunately there's no really good pre-training that can do both. So we're not really at Dino V3 territory yet. We just need to like push our methods a bit more to get something that is really generalizable. Further we still see even with like training on 100,000 brain Mis CNN's are still superior to um transformers. So at least we found that the data set scale that we train our transformers with is not really the issue. there might be some other underlying problem that is holding the transformer back in medical imaging but I'm quite confident to say that data is not the issue for transformers in medical. Further um in the paper we also found that if you fine-tune for longer schedules than shorter schedules um you also lose performance. So um it's not always worth it to train 250,000 steps. Sometimes it's better to just train 40 or 50 and then you might be better overall than training for the longer period in particular with pre-training. And um also as the last point mentioned, fine-tuning schedules are really important and a key component of pre-training in general. Now um this paper basically ticks two boxes for making SSL more attractive because we were hoping to shine a light which kind of pre-training method to use. um CNN or transformers as architecture um what to use for segmentation and classification. So we are providing a lot of insights and um if you're a user and you know your problem um it's like segmentation or classification you hopefully now can like look in the in the field and see hey these checkpoints exist this one is a contrastive pre-trained method I'll just take it and use it for my for my task but unfortunately a few issues still remain uh which is like the pre-training framework and then um that integrating like methods and checkpoints into N unit is really annoying and um this is why we also used um and published our framework which um is called NNSSL. It's currently on um our DKZ GitHub and um contains all the pre-training methods and the entire development framework that we um use to like train all these encoders and um it also comes with like the features that it can handle the pre-processing of data. So if you download the open mind data set from hugging face, you can use that quite easily to prep-process your data and get a working development environment off the ground really really quickly. Further um this also um yeah also includes the pre-training but it also more more importantly um allows to easily adopt your pre-training checkpoints into nunit. So if you want to do segmentation, I think it's really worth it to check out um our repository because you can just pre-train something and then if you have the pre-train checkpoint um easily migrate it into NNU unit and use it directly directly there actually you can already like enter um a GitHub link in in our NU unit um our NUT branch and then it will automatically download a pre-trained checkpoint. point from hugging phase and pre-process the downstream data set similarly and train your model with a pre-trained checkpoint. So this is um quite a nice resource at least um in my opinion and um yeah this is the downstream framework where you can um if you enter the URL of um of some GitHub check hugging face checkpoint you basically download it automatically and then train your NNU unit with a pre-train checkpoint. So um should be as easy as entering three lines. You just um say nunit v2 plan and pre-process as you would do previously. Then you call pre-process like NNSL with some um like data set identifier name how you want to call your pre-training and the path which can be the hugging phase URL and then um you just call nunit v2 train pre-trained and then everything will be taken care of and you will get your pre-trained nunit segmentation model out and yeah also if you're a developer and if you're interested in contributing to the self-supervised learning space. You can easily um in like upload your own checkpoint onto hugging face. Um it even supports that um you like put a citation in the checkpoint itself in like a dictionary position. So just a key value pair and then um when someone else will use your checkpoint um it will also print out a message like what kind of pre-training was used. So you can get attribution and um if you build something that area every user will use you can get a lot of exposure of course and hopefully like get your pre-training into a lot of hands and um yeah through all these resources we ticked all the boxes that we were um setting out to um to tick at the beginning of the project and um yeah did we also used these resources already a bit um to to test and to do a Mikai challenge recently which um concluded in like October I think and um in case you're interested it's the SSL 3D meets open mind challenge and um yeah we found similarly as a small teaser that um at least in the current domain we have like a lot of methods that are good in segmentation but are not that great in in classification which is over here. So you see segmentation rank, classification rank and you can see that methods that are good in one usually go down and um methods that are bad in segmentation are often times good in classification and um yeah so the the field at least method wise is at the same point as we are and there's no real method is great at everything. And for transformers though we see like a few methods that are not that bad but overall performance of transformers is still really not that high. And um I think there were not that many participants in the transformer track as in the CNN track. And with that I'm also um happy to take any questions and I'm done. Yeah, let's just stay here. &gt;&gt; Thank you so much. That was really nice presentation. Pretty in depth. Um any anybody any has any questions please feel free to type in feel free to like ask you um let me just open this up &gt;&gt; I have a question &gt;&gt; so I see that you've used uh I see that you've used um contrastive methods and uh mass image modeling methods meth, right? Like me, sorry, mass autoenccoders. So, I was wondering, uh, have you had the chance to try out the dino uh methods? &gt;&gt; Um, do you mean dino v something v2 or the normal dino &gt;&gt; dino v2? Um, we did not really have the the um possibility. Um, I think back then I I don't think there was like an open-source repository for 3D V2, Dinov2. Um, and just um, if I mean, we looked at it a lot and we were like thinking about it as well. However, Dinov2 is like heavily engineered usually. So, you have like a lot of different moving parts that don't translate super well to potentially small batch sizes. And then um transferring all of these things over into 3D I think is really hard and I think it's really difficult to make it work really well. &gt;&gt; I would be surprised if it works &gt;&gt; super great for down dense downstream tasks at the moment. Um and I have not seen any like super convincing results from a paper that does claim they do Dino V2. Mhm. Yeah. Yeah. Thanks. &gt;&gt; Great though. I think it would be great to have more like um general purpose pre-training methods actually. &gt;&gt; Yeah, I agree. That's true. &gt;&gt; Yeah, thank you very much. Um maybe one question would be like did you like try out Metsam? &gt;&gt; Met Sam as a pre-training or &gt;&gt; uh no no we did not. Metsam I mean we um had a benchmark on it for um for interactive segmentation. &gt;&gt; Mhm. &gt;&gt; But it's a bit problematic because metsam one was two is a is a 2D method. &gt;&gt; Yeah. So um we try to keep it to 3D encoders and if we introduced like a 2D method I think it would have been very confusing and also very unfair to compare because usually in our results we always see that methods which are native 2D perform like at least three four points worse than anything in 3D. &gt;&gt; So it will be really hard for this method uh to outperform whatever is in 3D. Um yeah &gt;&gt; yeah I think that there's a a 3D version of metam as well like give it &gt;&gt; I think so I think there are quite a bit of um recent interactive methods &gt;&gt; um yeah but we have not really looked into how we can like use those directly for segmentation or like some adaptation in this case &gt;&gt; I feel like a lot of methods exist and it's really hard to adapt them to anything which is a &gt;&gt; I think the the biggest downside of of this domain because we don't really have like everything is not everything is like 2D images and RGB so it's really hard to just take an encoder and drop it in somewhere. &gt;&gt; Yeah. Um anybody has any other questions? &gt;&gt; Um I have a question. So you mentioned that the benchmark show uh shows like for reconstruction methods it excels for dense task and contrast for global task. So I was thinking is do you believe that uh a multitask loss with some explicit planning component like this there's a method called gappy which is uh kind of a natural uh evolution of MA. So do you think having uh following Cappy's approach could be a key to achieve a better generalization across segmentation and classification task on this data set. So to repeat because I'm not quite sure I c caught everything. You were asking if like um comp combining different objectives during pre-training can maybe get something that works on both. &gt;&gt; Is that is that &gt;&gt; Yeah. So basically there's a uh another SSL approach uh kind of similar to MA which is called Cappy with basically &gt;&gt; capping. Yes. &gt;&gt; Yeah. &gt;&gt; Yeah. was wondering if um uh you know Kathy uses like a planned augmentation strategy and then also combines multiple pre uh you know pretext tasks. So I was wondering if that could help in generalizing between classifications and notation. &gt;&gt; Yeah, I think copy is cool and I've seen it come out like I think in February or March this year, right? So it's quite new and a simple version of Dinov2 according to the authors. So I think it's it's a worthwhile um um like option to to investigate. I think it's also um designed in a way that it's basically very similar to IBOT with some compute efficiency gains and like some additional like clustering. So I think it's really cool. I think it might be really good in 3D if you find a well tuned configuration of it. I'm not really sure how well like everything translates. um we've tried like to hack like uh a simple version of that together. Um back when I was um at doing my internship um but of course like I was working on something else and didn't really have the time but I think if you spend some time and work on on copy I think it's a has great potential. I think it's a lot easier to get going than Dinov2. um and iterating from there would be like the next step of course but I think it would yield embeddings which are much better for um global reasoning and and local reasoning. So I think it would be like maybe one of the first where you have both like boxes ticked. &gt;&gt; Yeah. Yeah. I think it's it's more robust in terms of capturing the uh the functioning networks. That's what they claim. So I think that might uh be I don't know it might be more robust and it will be interesting to see how that will work. &gt;&gt; Yeah. Yeah. It would be great to see a bit more interest in the entire domain. I think uh currently it's a bit bit slow. not a lot of people are working in it and um I think there's a lot of progress to still be had in particular the classification side of things is really difficult to train in medical and there's not really a lot of established frameworks so if you have a good pre-training method and a good like base embedding that you can iterate from I think you can make all these small classification data sets and and problems a lot better and um I hope that this gets a bit more attention because it would be great to have like a cooler classification stack. Um where segmentation it feels really hard to edge out performance. Sometimes you're in noise regime where you're probably like just trying to overfitit better to what kind of um like pattern the radiologist follows than actually being more sensitive to like structures. So I think um I think classification might be like um a cool domain where one can also do like SSL research into and then hopefully get better methods out of it. &gt;&gt; Yep. Do do we have any other questions? Um &gt;&gt; all right. Um I think let's let's wrap it up. Thank you so much for taking our time and giving this amazing talk. Um, if anybody has any questions, feel free to follow up on our discord and we will be sure to forward those question to you and we can get back to you. Thank you so much. Um, let's let's see you all next week with another guest speaker session. &gt;&gt; Thank you so much. &gt;&gt; Thank you. &gt;&gt; Bye.
