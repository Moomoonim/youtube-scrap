# Building an Automated Amazon Bedrock Batch Inference Pipeline

- 영상 링크: https://www.youtube.com/watch?v=gCGAMfewNug
- 채널: AWS Developers
- 업로드일: 2024-11-21
- 자막 언어: ko
- 단어 수: 약 581개

---

## 스크립트

오늘은 Amazon Bedrock 배치 인터페이스 파이프라인 아키텍처를 살펴보겠습니다. 이 솔루션은 매우 큰 양 의 데이터를 매우 효율적으로 처리하도록 설계되었습니다. 안녕하세요, 저는 Stuart입니다. 이 자동화된 워크플로의 각 단계를 설명드리겠습니다. 여기에서 소개하는 솔루션은 Amazon Bedrock의 배치 추론을 사용하여 여러 요청을 효율적으로 처리합니다. 다음 솔루션 아키텍처를 통해 이를 구현합니다. 먼저 S3 버킷에 파일을 업로드합니다. 이 파일은 두 개의 프로세스 폴더에 업로드되며, 첫 번째 Lambda 함수( 2단계)가 실행됩니다. 이 Lambda 함수는 Dynamo DB에 새 항목을 생성하고 대기 상태로 표시합니다. Dynamo DB는 작업 ID부터 현재 상태, 생성 시간까지 모든 것을 추적하는 중앙 명령 센터라고 생각하시면 됩니다. 세 번째 단계에서는 Event Bridge가 실행됩니다. Event Bridge는 15분마다 실행되며 Lambda 함수의 배치 작업 프로세스를 트리거합니다. 이 함수는 워크플로 관리자 역할을 합니다. 네 번째 단계에서는 Dynamo DB에서 모든 활성 작업과 진행 중, 제출됨, 유효성 검사 중 또는 예약됨으로 표시된 작업을 검색합니다. 다섯 번째 단계에서는 Amazon 내부에서 검사를 수행합니다. 모델 작업 API를 통해 각 작업의 최신 상태를 가져옵니다. 6단계에서는 상태가 변경되면 함수가 DynamoDB 레코드를 업데이트합니다. 이를 통해 시스템 내 모든 작업에 대한 매우 정확한 정보를 항상 확보할 수 있습니다. 다음으로 7단계로 이동합니다. 새 작업을 제출하기 전에 함수는 중요한 검사를 수행합니다. 서비스 할당량 제한 내에서 사용 가능한 슬롯 수를 계산하는 것입니다. 8단계에서는 사용 가능한 슬롯을 찾으면 작업을 시작합니다. 이제 API를 통해 생성한 모델 작업을 사용하여 보류 중인 요청에 대한 새 배치 처리 작업을 제출합니다. 9단계에서는 새 작업이 제출될 때마다 DynamoDB 레코드가 업데이트됩니다. 이를 통해 명확한 감사 추적을 유지할 수 있습니다. 이것이 전체 9 단계 프로세스입니다. 마지막으로 모든 작업이 완료되면 프로세스 파일이 S3 폴더의 프로세스 폴더에 깔끔하게 저장되어 사용할 수 있게 됩니다. 이 솔루션을 구현하기 전에 필요한 모든 것을 준비해야 합니다. 활성 AWS 계정과 배치 처리에 지원되는 AWS 리전에 대한 액세스 권한이 필요합니다. Amazon Bedrock에서 선택한 모델을 활성화했고 적절한 권한이 설정되어 있으므로 이제 배포 프로세스를 살펴보겠습니다. 먼저 AWS CloudFormation 콘솔에서 " 스택 생성" 버튼을 클릭합니다. 이 버튼을 클릭하면 자동 배포 프로세스가 시작됩니다. 모든 스택은 YAML 또는 JSON 템플릿을 기반으로 구축됩니다. 이 예에서는 기존 템플릿 사용을 선택하겠습니다. 다음으로 템플릿 소스를 지정해야 합니다. 여기서는 Amazon S3 URL 사용을 선택합니다. 이 URL은 템플릿을 가리킵니다. Amazon S3 URL(YAML 템플릿 링크)을 복사하여 붙여넣을 수 있습니다. 스택을 구성할 때 지역에 고유한 이름을 지정해야 합니다. 이 이름은 관련 리소스를 식별하는 데 도움이 됩니다. 다음으로 데이터를 처리할 Bedrock 모델을 지정합니다. 선택한 모델 ID를 입력합니다. 보안 설정의 경우 기본값인 "commissions"를 사용하거나 사용자 지정 " Iroll" 또는 "IARN"을 입력할 수 있습니다. 보안 구성을 지정해야 하는 경우 스택 옵션을 검토해 보세요. 여기에서 필요한 태그 또는 사용자 지정 설정을 추가할 수 있습니다. 필요할 수도 있지만 이 단계는 선택 사항 이며 반드시 수행해야 하는 것은 아닙니다. 시작하기 전에 IIM 리소스를 승인해야 하며, 스택 배포를 제출하면 이러한 리소스가 생성됩니다. 이제 본격적인 작업이 시작됩니다. 제출 버튼을 클릭하면 AWS 계정 내에 파이프라인 배포가 시작됩니다. 배포 후 S3 버킷에 두 개의 프로세스 폴더를 생성합니다. 이 폴더에 처리할 모든 데이터를 업로드합니다. 가장 가까운 파일은 모든 작업이 완료되면 자동으로 forward slpress 폴더에 나타납니다. 시스템에서 파일 이동 및 처리를 모두 처리하므로 모든 것이 깔끔하게 정리됩니다. DynamoDB 테이블을 통해 모든 작업 상태와 메타데이터를 모니터링할 수 있습니다. 이제 이 파이프라인을 사용하려면 Amazon Bedrock 의 배치 추론을 위해 생성할 수 있는 JSM 파일을 업로드합니다. 이 단계를 완료하면 파이프라인 배포가 성공적으로 완료됩니다. Amazon Bedrock 콘솔에서 배치 작업 상태를 확인할 수 있습니다. 이 솔루션은 워크플로에 여러 가지 주요 이점을 제공합니다. 자동화된 Q 관리로 프로세스 용량을 최적화하고 배치 추론 가격 책정을 통해 효율성을 높일 수 있습니다. 비용이 최소 절반으로 절감됩니다. 시스템이 필요에 따라 확장되며, 중앙 집중식 추적을 통해 모든 것을 한눈에 파악하고 관리할 수 있습니다. 시작하는 데 필요한 모든 문서와 자료는 링크 설명에 있습니다. 이 영상이 도움이 되었다면 좋아요를 누르고 AWS Developers YouTube 채널을 구독해 주세요. 시청해 주셔서 감사합니다.
