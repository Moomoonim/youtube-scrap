# Evaluation Comparisons in W&B Weave

- 영상 링크: https://www.youtube.com/watch?v=oBVzAuYijqw
- 채널: Weights & Biases
- 업로드일: 2024-07-15
- 자막 언어: en
- 단어 수: 약 1469개

---

## 스크립트

[Music] hey folks um I wanted to give an overview of the evaluation comparison tool so uh here I'm in a project called human eval 6 if I click on the evaluations tab you'll notice that I have checkboxes on the left hand side if I select a few evaluations the compare evaluations button becomes live when I click that behind the scenes we're building a comprehensive report comparing both evaluations in terms of performance latency token usage Etc here we can see uh the compare evaluations report at the top we can see us we can see the two uh evaluations that we're comparing uh and have links to their underlying uh ground truth so if I click on evaluation I can actually go and browse my evaluation page um if I click on the model I can view my underlying model Etc the first section that you see is a summary metric section it contains a radar plot and bar plots uh that compare the summary metrics for each evaluation as of this writing summary metrics are defined as the user defined scores that they pass to the evaluation as well as additional weights and biases derived metrics the two derived metrics that come out of the box are model latency and total tokens but more can easily be added as we scroll down we see what uh the model scorecard so uh we see the left hand side is the Baseline model and the right hand side is the comparison model um or the Challenger model um here we highlight the differences in the properties of the models in the first section so here we can see that the uh user has defined a property called Model name and this one is uh gbt 4 Turbo and this uh and the right one is gbt 40 um I of course can expand all properties if I want to see for example The Prompt template or temperature or other userdefined properties uh but it's often useful just to look at the differences in terms of comparison below I see a numeric summary of what the plots above are showing where we can see the score scoring functions as well as the derived metrics for convenience uh pill values are shown uh uh highlighting the differences so here we can see that uh we did use uh more tokens on the uh Challenger model but it was uh actually faster and had better performance scrolling down I now am uh in the example filter section and the output comparison section so the area below we can see the 164 common examples that both models were evaluated on and if I scroll all the way to the bottom I can uh page through my different examples and then see my two models uh see what my models uh uh output for each respective example now if you're using the evaluation framework you might also have a number of Trials per example so here the user has defined three trials per example so I can quickly jump between trials by clicking uh the view icon I can see their summary metrics below as well as model latency and then the overall aggregate metrics if I want I can click on uh this little button to view the definition of the scoring function so that I can remember what code generated that score and I can also click on the individual scoring metrics to see the exact call that produced that score furthermore I can see the model predict call uh here in line as well as a link to the underlying example if it contains additional information so uh this is sort of the uh one view that shows uh an example the all of the models that were are selected there outputs all of the metrics the aggregate scores and links to all of the supporting data underneath now often times what I want to do is find examples that are either particularly challenging hard or where there's a big difference between my evaluations and that's where the example filter comes into play so I automatically we've selected uh two different metrics uh you're free to choose whatever metrics are available to select from uh so let's say I want to look at this region in the the upper left so uh actually first off um what this plot is showing on the left hand side is each example is a DOT and the x axis is the score that the Baseline uh model got and the y- axis is the score that the Challenger model got so everything on the green uh the green 45 degree line are examples where both the Challenger model and the Baseline model performed the same for that particular score as you get farther away from that Center Line you get more variation in performance so for example this upper left region are all of the examples where the Challenger model performed 100% on this particular score but the Baseline model actually uh performed 0% so if I highlight that region I now I'm filtered down to the six examples that fit that particular qualification and here I can see those particular examples and browse through them page through them as I please furthermore however uh you can see that some of these dots were uh uh became outlined and some of them are still um there so in this particular item I'm looking at model latency so model latency for this example I want to look at items where model latency was quite similar and performance was quite different so let's zoom into this sub region below now I'm looking at the single example where uh the model latency Falls within this range and the uh scoring function Falls within this range and here it goes we can see models that had similar performance but highly differentiated and maybe I can learn something novel about those about the particular models um in this particular case double clicking clears the selection and we can go back to our 164 total examples uh one last feature uh to note is that you can uh click this rotation icon which will change the Baseline and Challenger model respectively updating the data below now this view is also uh sort of dynamic to um mobile size uh please ignore all these other ones I'll talk about them momentarily but just keeping your eye on the Le hand side uh if we jump down to mobile it it does work quite nicely and this grid below does scale and scroll accordingly um there we go even going farther down to a very very small view we can still see most of the data I think this is something we should improve but um you know notably I tried to make it so that at least a basic mobile view would work um also the plots themselves sort of become stacked rather than uh rather than side by side okay now let's close this and take a look at a couple different variations so if I decided to select one compar one evaluation we get an evaluation report for a single evaluation which is quite nice I can scroll through my examples and uh see how my model performed Etc now if I select many evaluations you'll see that we naturally scale to support all of them as well which is really great for identifying uh High performing models when you're doing many many evaluations notably the filter sections excluded when you don't have two evaluations this is something we can improve in the future but it's sometimes easier to compare two thing uh two dimensional plots are much easier than three and four dimensional plots uh but here it's really nice you can see all of the different model outputs for this particular example and all of their individual trials and rollouts so that was an example that had many trials but one met one userdefined metric in this particular case we have many userdefined metrics so you can see that uh the radio plot becomes a little bit more interesting we get many more metrics and the footer section here has a lot more data again this is a single example with many metrics here I'm showing a single example with uh excuse me two examples with many metrics we get the comparison charts as well um and we can see uh all of the summaries and uh lastly we have uh the many comparison and many metrics which sounds like a lot but is actually quite digestible and um I found this to be quite useful when exploring other people's evaluations so that's it [Music]
