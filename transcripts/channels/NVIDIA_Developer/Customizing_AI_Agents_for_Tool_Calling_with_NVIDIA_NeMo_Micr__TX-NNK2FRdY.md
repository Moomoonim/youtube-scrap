# Customizing AI Agents for Tool Calling with NVIDIA NeMo Microservices

- 영상 링크: https://www.youtube.com/watch?v=TX-NNK2FRdY
- 채널: NVIDIA Developer
- 업로드일: 2025-04-23
- 자막 언어: en
- 단어 수: 약 2610개

---

## 스크립트

Hi, I'm Chris Alexiuk, a deep learning&nbsp; developer advocate at NVIDIA. Today,&nbsp;&nbsp; I'll be walking you through an example&nbsp; of making AI agents star performers using&nbsp;&nbsp; NVIDIA NeMo microservices. AI agents rely on&nbsp; tools to execute complex tasks effectively. As&nbsp;&nbsp; the number of available tools expands from&nbsp; 10 to hundreds, customizing the underlying&nbsp;&nbsp; models for tool calling becomes increasingly&nbsp; critical to maintain efficiency and accuracy.&nbsp;&nbsp; To continuously improve AI agents and&nbsp; better provide excellent user experience,&nbsp;&nbsp; organizations need to set up AI data flywheels,&nbsp; which are self-reinforcing feedback loops. As we&nbsp;&nbsp; see here, the data from user interactions is&nbsp; captured and then used to refine AI models,&nbsp;&nbsp; leading to better outcomes and even more&nbsp; valuable data for further customization. Today, we're going to work through an end-to-end&nbsp; example demonstrating how to customize AI agents&nbsp;&nbsp; for precise function calling using&nbsp; the XLAM data set. In this tutorial,&nbsp;&nbsp; we're going to work through four stages.&nbsp; The first stage, as you can see here,&nbsp;&nbsp; is preparing data for fine-tuning and evaluation.&nbsp; The second stage is customizing the model with&nbsp;&nbsp; LoRA fine-tuning. The third stage is evaluating&nbsp; the accuracy of the customized model, and finally,&nbsp;&nbsp; the fourth stage is adding some guardrails to&nbsp; make sure that our LLM exhibits safe behavior. For this tutorial, there are a few prerequisites.&nbsp; The first is that we need to make sure our NeMo&nbsp;&nbsp; microservices are running. We can do this&nbsp; by following this link to our deployment&nbsp;&nbsp; guide. Basically, all you need to do is&nbsp; run the microservices through the parent&nbsp;&nbsp; Helm chart. The parent Helm chart simplifies&nbsp; setup, providing all necessary microservices&nbsp;&nbsp; conveniently in one step. To help streamline&nbsp; our development workflow for the demo today,&nbsp;&nbsp; we're going to need to, after setting up our&nbsp; actual NeMo microservices, add one additional&nbsp;&nbsp; item, which is going to be deploying&nbsp; our LLaMA 3.21B instruct model. This&nbsp;&nbsp; can be deployed with this simple command. All&nbsp; this is going to do is make sure that we have&nbsp;&nbsp; the model we'd like to actually work with&nbsp; deployed in our microservice environment. NIM is our inference microservice, which is&nbsp; easy to use, basically designed for secure,&nbsp;&nbsp; reliable deployment of high-performance&nbsp; AI models, whether you're using cloud,&nbsp;&nbsp; data centers, workstations, or whatever you're&nbsp; using. So that's why we're selecting the NIM. Before moving on to the rest of the tutorial,&nbsp; you'll need to make sure that you set up your&nbsp;&nbsp; `config.py`. These values are going to be provided&nbsp; to you during the setup process. Your Hugging Face&nbsp;&nbsp; token will need to be something that you provide&nbsp; from your Hugging Face account. Optionally,&nbsp;&nbsp; you can use your WandB API key, and the rest&nbsp; of these I recommend leaving as their defaults. To set up our data, first, we need to make sure&nbsp; that we have the actual data downloaded, and then&nbsp;&nbsp; we're going to prepare it for both customization&nbsp; and evaluation. After some boilerplate in the&nbsp;&nbsp; notebook, you're going to be asked to go ahead and&nbsp; provide your Hugging Face token. This token will&nbsp;&nbsp; need to be associated with an account that does&nbsp; have access to this particular data set. It's&nbsp;&nbsp; just a simple form submission on the Hugging&nbsp; Face website, but once you have that token,&nbsp;&nbsp; you're free to download this data set, which&nbsp; is going to be in this following format. Taking a little bit of a look at the XLAM data&nbsp; set, we can see that the data set is comprised&nbsp;&nbsp; of a number of rows where each row is going&nbsp; to be associated with a query, some answers,&nbsp;&nbsp; and some tools. The idea here is that&nbsp; the XLAM data set is going to provide&nbsp;&nbsp; our LLM with a query as well as some potential&nbsp; tools, and then the responsibility of the LLM&nbsp;&nbsp; is going to be to generate the correct tool&nbsp; call given the list of tools and query. So,&nbsp;&nbsp; in this case, our answers result is&nbsp; the ground truth that we're going to&nbsp;&nbsp; use to determine whether or not our LLM&nbsp; correctly identified which tool to use. We need to do a little bit of work to make sure&nbsp; this format is compatible with both our NeMo&nbsp;&nbsp; Customizer microservice as well as our NeMo&nbsp; Evaluator microservice, which is what we're&nbsp;&nbsp; going to look at next. To prepare the data&nbsp; for customization is fairly straightforward.&nbsp;&nbsp; What we're going to do is make sure that we've&nbsp; provided the data in this following format. Now,&nbsp;&nbsp; this is specific to the Customizer. In order&nbsp; to make sure that it runs well with this job,&nbsp;&nbsp; it's going to be important that it's in this&nbsp; format. So, what we've done is we've gone ahead&nbsp;&nbsp; and provided you with an actual helper&nbsp; function to help you get this done. Now,&nbsp;&nbsp; the helper function has a lot of code, but don't&nbsp; worry about it. All you need to do is run this&nbsp;&nbsp; `convert_example` and then we're going to run it&nbsp; across the entire data set. Once you've done that,&nbsp;&nbsp; your data is now in a format that's ready&nbsp; for the NeMo Customizer microservice. Before we move to the next step, however, we're&nbsp; going to go ahead and split this data set so that&nbsp;&nbsp; we have our classic train, validation, and test.&nbsp; Next, we're going to prepare some data so that&nbsp;&nbsp; it's compatible with the evaluator microservice,&nbsp; which we'll get to in just a second. As you can&nbsp;&nbsp; see, we have the same kind of thing here where&nbsp; we need to make sure that our data is in a very&nbsp;&nbsp; specific format. What we're going to do is use,&nbsp; again, the helper function to help us get into&nbsp;&nbsp; this format. You'll notice that all you have&nbsp; to do once again is run the `convert_data_set`,&nbsp;&nbsp; and that's going to help you get&nbsp; that data into the desired format. Now that we have data in our desired format, we're&nbsp; good to move on to our actual fine-tuning and then&nbsp;&nbsp; evaluation. After we've done that, we're going&nbsp; to implement some guardrails and then we'll have&nbsp;&nbsp; seen the whole end-to-end example. This whole&nbsp; time, we've actually been using just Python,&nbsp;&nbsp; and we're about to get into those&nbsp; microservices in the next step. For customization, it's pretty straightforward.&nbsp; We just have to do some checks, then we're going&nbsp;&nbsp; to go ahead and upload our data to the&nbsp; NeMo data store. We're going to provide&nbsp;&nbsp; the configuration needed to do LoRA&nbsp; customization through NeMo Customizer,&nbsp;&nbsp; and then we're just going to do some inference&nbsp; gut checks to make sure things are working.&nbsp;&nbsp; The first thing we're going to set up is a&nbsp; namespace for data store and entity store&nbsp;&nbsp; microservices, which will be spun up as part of&nbsp; the parent Helm chart that you ran in order to&nbsp;&nbsp; start this tutorial. You'll notice that what&nbsp; we're going to do is create a namespace for&nbsp;&nbsp; both our data store and our entity store so that&nbsp; we can keep track of things a little bit easier. In this next step, what we're going to do is&nbsp; upload our data to the NeMo data store. So,&nbsp;&nbsp; what we're going to do is create a repository&nbsp; ID, then we're going to go ahead and use the&nbsp;&nbsp; Hugging Face API to create our repo. Once we've&nbsp; done that, we're going to upload our actual data&nbsp;&nbsp; set files to the NeMo data store, and then&nbsp; finally, we're going to register that data&nbsp;&nbsp; set with the NeMo entity store so that it can&nbsp; be seen by our NeMo Customizer microservice. Starting the training job is an incredibly&nbsp; straightforward process. All you have to do&nbsp;&nbsp; is submit it to the actual jobs endpoint. You'll&nbsp; notice here that we've provided this code snippet.&nbsp;&nbsp; This is the whole code to get the training job&nbsp; started through the NeMo Customizer microservice.&nbsp;&nbsp; You'll notice that what we need to provide is a&nbsp; name for the thing that we're going to customize,&nbsp;&nbsp; we need to provide an output including namespace&nbsp; for our model, we're also going to point at our&nbsp;&nbsp; base model, which is the model we're starting&nbsp; with, we're going to provide a way to get to&nbsp;&nbsp; our data set, and then we're going to set up&nbsp; the actual hyperparameters for our training&nbsp;&nbsp; run. You'll notice that we're going to&nbsp; do supervised fine-tuning with our LoRA.&nbsp;&nbsp; We're going to train for two epochs with a batch&nbsp; size of 16. We're going to go ahead and use a very&nbsp;&nbsp; small learning rate, and we're going to set up&nbsp; some LoRA configs here as well. NeMo Customizer&nbsp;&nbsp; leverages efficient fine-tuning techniques such as&nbsp; LoRA for SFT, optimizing large language models for&nbsp;&nbsp; various use cases. The big idea here is that&nbsp; it's this API call to get things fired off,&nbsp;&nbsp; and that's it. All we have to do now is run some&nbsp; code to verify that our endpoint is up and active,&nbsp;&nbsp; and we're going to just check back every&nbsp; so often. You could put this in a loop&nbsp;&nbsp; until we see that our status has been&nbsp; completed. Once the status is completed,&nbsp;&nbsp; we're ready to do that gut check&nbsp; to make sure everything worked out. As you can see, we take a sample of our&nbsp; data, and we're going to go ahead and&nbsp;&nbsp; send that to the newly customized&nbsp; NIM. In the newly customized NIM,&nbsp;&nbsp; you see that the response we get is a function&nbsp; that has the argument `city="New York City"`.&nbsp;&nbsp; `zip_code_by_city` is the function name,&nbsp; and you'll see that in the ground truth,&nbsp;&nbsp; we have `function=zip_code_by_city` and&nbsp; we call it with `city="New York City"`,&nbsp;&nbsp; exactly what we'd expect. Now, we're going to&nbsp; go ahead and take note of our custom model name&nbsp;&nbsp; in order to move to the next step, which is&nbsp; going to leverage the evaluator microservice. The evaluation microservice is again very&nbsp; straightforward. Basically, what we're going&nbsp;&nbsp; to do is establish a baseline accuracy. So, we're&nbsp; going to evaluate the base model and then we're&nbsp;&nbsp; going to evaluate our LoRA customized model. What&nbsp; this means is that we're going to have both the&nbsp;&nbsp; accuracy from our original model and then the&nbsp; improvement that we got from that customization&nbsp;&nbsp; process. There are two main entities that we need&nbsp; to care about with NeMo evaluation microservice.&nbsp;&nbsp; The first is an evaluation config option.&nbsp; Basically, what this is is how we wish to evaluate&nbsp;&nbsp; the model or system that we want to evaluate.&nbsp; You'll notice that we're going to use a custom&nbsp;&nbsp; eval type called `custom_tool_calling`. It's going&nbsp; to use a custom data set, which is the data set we&nbsp;&nbsp; configured earlier in this process when we did our&nbsp; data preparation, and then we're going to look for&nbsp;&nbsp; exactly this template in order to evaluate whether&nbsp; we got the correct tools and the correct function. The next component of our evaluation job is going&nbsp; to be our target. So, if our target, our config&nbsp;&nbsp; is how we're going to evaluate, the target is&nbsp; what we're going to evaluate. In this case,&nbsp;&nbsp; you can see we're going to point our target at&nbsp; our base model. This is the one we set up again&nbsp;&nbsp; when we're doing the initial setup steps here.&nbsp; The idea is we just want to see how well this&nbsp;&nbsp; NIM does on its own before the customization.&nbsp; So, all that's left to do is fire off this job,&nbsp;&nbsp; which will kick off our evaluation job.&nbsp; We're going to go ahead and just wait for&nbsp;&nbsp; our job using this helper function that you see&nbsp; here. Once we're done waiting for 235 seconds,&nbsp;&nbsp; we're good to go ahead and see how it did. It&nbsp; did not so good, right? So, you can see here&nbsp;&nbsp; that we get some stats back, and this tells&nbsp; us that, in fact, it did pretty bad on this&nbsp;&nbsp; task. Function name accuracy was quite low, and&nbsp; function name and args accuracy was even lower. So, let's go ahead and see what&nbsp; the customized model can do.&nbsp;&nbsp; You'll notice that for the customized model, we&nbsp; don't have to recreate that config. Right since&nbsp;&nbsp; that config was created initially, we can now just&nbsp; swap our target since we're trying to evaluate&nbsp;&nbsp; exactly the same way. The how hasn't changed&nbsp; for our evaluation; it's just the what. So,&nbsp;&nbsp; we point now at our customized model and&nbsp; again kick off the job. We wait 230 seconds&nbsp;&nbsp; later, and we get a much better response. Function&nbsp; name accuracy is now up to 0.92, and our function&nbsp;&nbsp; name and args accuracy is now up to 0.72,&nbsp; a massive increase over the baseline model. All that's left to do is apply some guardrails&nbsp; so that we can make sure this model is used in a&nbsp;&nbsp; safe and effective manner. For guardrails, again,&nbsp; very few steps. We're going to add the guardrails&nbsp;&nbsp; configuration and then evaluate the safety of&nbsp; guardrails. This is going to be by eyeball,&nbsp;&nbsp; so we're just going to make sure that&nbsp; it's working the way that we'd expect&nbsp;&nbsp; it to work. What we're going to do is&nbsp; actually use the content safety NIM. Now,&nbsp;&nbsp; the content safety NIM is going to help us&nbsp; ensure that our interactions remain safe and&nbsp;&nbsp; compliant. The way that it does this is by&nbsp; having a NIM essentially act as a guardrail,&nbsp;&nbsp; verifying that user input is safe, making&nbsp; sure that nothing inappropriate squeaks&nbsp;&nbsp; through when customers are using this&nbsp; particular service that you're offering. With that particular content safety NIM,&nbsp; we're going to go ahead and add the guardrails&nbsp;&nbsp; configuration to the microservice again. This is&nbsp; done pretty straightforwardly; it's just a simple&nbsp;&nbsp; API call. We're going to go ahead and apply it&nbsp; for this tool calling input. What we're going&nbsp;&nbsp; to do is add a rail, and it's going to be an&nbsp; input that checks the content safety check,&nbsp;&nbsp; right? So, just to make sure that incoming&nbsp; content is safe. The prompt here is basically&nbsp;&nbsp; just to see if there is unsafe content in user&nbsp; or agent messages in the below conversation in&nbsp;&nbsp; accordance with our safety policy. With the below&nbsp; categories, provide a safe or unsafe rating,&nbsp;&nbsp; and you can see it goes on and on to&nbsp; describe in detail what is happening here,&nbsp;&nbsp; but this is the idea. It's just going to check&nbsp; and make sure that input is safe. If it's safe,&nbsp;&nbsp; it's going to let it through. If it's&nbsp; not safe, it won't let it through. Finally, we're going to set up some safe and&nbsp; unsafe messages, and we're just going to see&nbsp;&nbsp; how this works. So, let's scroll down and check&nbsp; it out. We have this class that helps us implement&nbsp;&nbsp; the actual guardrail with tool calling. Once&nbsp; we've got this set up, we can go ahead and&nbsp;&nbsp; just activate this with our NIM. You can see&nbsp; here that for this guardrails off situation,&nbsp;&nbsp; we can pass in our unsafe message and&nbsp; get a response. This is not ideal. So,&nbsp;&nbsp; let's turn our guardrails on and see&nbsp; that if we pass in our unsafe message,&nbsp;&nbsp; we're going to go ahead and get the response that&nbsp; it was blocked due to that safety check. Then,&nbsp;&nbsp; with our user query that's safe, this is our&nbsp; safe message. If we run this with guardrails on,&nbsp;&nbsp; you can see that we get the actual&nbsp; response the way that we would hope to. In this tutorial, we've customized a model&nbsp; specifically for function calling using the&nbsp;&nbsp; XLAM data set, verified its accuracy using NeMo&nbsp; Evaluator, safeguarded model deployment with&nbsp;&nbsp; NeMo guardrails, and used the whole NVIDIA&nbsp; NeMo microservices pipeline to streamline&nbsp;&nbsp; this workflow with simple APIs, making it&nbsp; nice and straightforward for development&nbsp;&nbsp; and deployment of precise function calling&nbsp; AI applications. To learn more about NVIDIA&nbsp;&nbsp; NeMo microservices and try this tutorial,&nbsp; visit the links provided in the description.
