# Pinecone #2 - Managing Indexes

- 영상 링크: https://www.youtube.com/watch?v=DCQrrnFbLt8
- 채널: Pinecone
- 업로드일: 2022-01-03
- 자막 언어: en
- 단어 수: 약 571개

---

## 스크립트

in this video we're going to have a look at how we can manage indexes in pinecone now an index in pinecone is the highest organizational unit that pinecone uses it's where we saw our vectors and serves queries over the vectors that it contains we're going to have a look at how we can get a list of our indexes create an index describe an index and also delete an index so the first thing if you haven't already you will need to pip install the pinecone client and then you'll need to import pinecone so first we need to initialize our connections pinecone we just head over to apple.pinecone.io and that will open up your pinecone console so i'm going to take my quick start api key and i'm just going to place it here now that i've done that i can have a look at my indexes so i run this and i i get an empty list because i don't currently have any indexes so what i'm going to do is create one so i will write pinecone dot create index and in here we first need to pass the index name so i'm going to write example index and you also need to pass the dimension of your index so every index will contain vectors and all those vectors must have the same number of dimensions so i'm going to write 128. now those are the only two parameters that we need to define so we can we can run that and we will see in a moment that we will now have an example index index so if we go back to list indices up here we see that we now have a one single index so what i'm going to do now is create another one so second index and what we can do is also define a few other parameters so we can say which index type would like to use now by default this is just an approximated index so using approximate nearest neighbor search you can also use exact for exact nearest neighbors search now we pretty much always want to get going to want to use approximate nearest neighbors search so i'm going to i'm going to leave it as it is by default we can also define our metric so by default that is cosine we could write euclidean we can also say how many shards we'd like to allocate to our index by default that is just one and if we would like replicas as well so we'll go ahead and create that index and if i list my indexes again we'll now see that i have two so what we can now do is go ahead and describe our index so i'm going to write describe index and i want to describe our example index okay and we see we have the name the index type that is using so by default it's approximated the metric time replicas dimensions shards and index config now if we decide we don't want to use an index anymore all we need to do is write pinecone dot delete index and pass the name of that index so right sample index and now if i list my indexes again we'll see that we only have the the two end apes left so that's it for this this video on managing indexes
