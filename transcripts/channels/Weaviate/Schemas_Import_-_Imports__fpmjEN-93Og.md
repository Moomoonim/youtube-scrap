# Schemas Import - Imports

- 영상 링크: https://www.youtube.com/watch?v=fpmjEN-93Og
- 채널: Weaviate
- 업로드일: 2023-09-27
- 자막 언어: ko
- 단어 수: 약 340개

---

## 스크립트

안녕하세요, 이번 영상에서는 Aviate에 데이터를 가져오는 구체적인 방법에 대해 알아보겠습니다. 지금까지 WebA에서 데이터는 클래스에 속하고 속성 집합을 가진 객체로 표현된다는 것을 배웠습니다. 이전 스키마 섹션에서는 이러한 구조에 대한 프레임워크를 만드는 방법을 배웠고, 이번 섹션에서는 권장 모범 사례와 몇 가지 주요 고려 사항을 포함하여 Aviate에 데이터를 가져오는 방법에 대해 설명하겠습니다. 이 섹션을 마치면 스키마와 가져오기에 대해 배운 내용을 종합하여 실제 데이터 세트를 Aviate에 가져올 준비가 됩니다. WebA 객체를 생성하려면 소스 데이터를 로드하고, 소스 데이터를 원하는 VPX 객체 속성에 매핑하여 데이터 객체를 구축한 다음, WebA에서 원하는 클래스에 추가해야 합니다. 선택적으로 객체 ID와 벡터를 수동으로 지정할 수 있습니다. 객체 ID가 지정되지 않은 경우 WebA에서 자동으로 생성할 수도 있습니다. 중복 객체 생성을 방지하려면 결정론적 객체 ID 생성 프로세스를 사용할 수 있다는 점에 유의해야 합니다. 벡터가 지정되지 않은 경우 WebA는 해당 클래스에 벡터라이저가 설정되어 있는지 확인하고, 설정되어 있으면 해당 클래스에 벡터라이저가 있는 경우 객체 ID를 생성합니다. 벡터라이저가 설정되면 벡터라이저를 사용하여 벡터를 생성하여 가져오기 속도를 극대화합니다. 일괄 가져 오기 프로세스를 제공 하는데, 이 프로세스는 요청당 여러 객체를 처리하여 네트워크 지연의 영향을 줄입니다. 또한 Python 클라이언트와 같은 클라이언트는 프로세스를 병렬로 수행하여 가져오기 속도를 더욱 향상시킵니다. 데이터 수집 속도를 크게 향상시키려면 일괄 가져오기를 사용하는 것이 좋습니다. 일괄 가져오기를 통해 배치 크기, 실패 시 재시도 횟수 등의 배치 매개변수를 설정할 수 있습니다. 특별한 이유가 없는 한 대량 객체 생성에는 일괄 가져오기를 사용하는 것이 좋습니다. 일괄 가져오기 프로세스는 배치 수준 및 개별 객체 수준에서 오류를 처리하도록 구성되어 있습니다. 배치 수준 오류는 배치 요청에 대한 HTTP 응답 코드로 표시되고, 객체 수준 오류는 배치 생성 요청에 대한 응답의 일부로 반환됩니다. 따라서 배치가 성공적인 200 응답을 생성하더라도 객체 수준에서 개별 오류가 발생했을 수 있다는 점에 유의해야 합니다. 아래 코드 예제에서 콜백 함수를 통해 새 오류를 확인하는 모범 사례를 보여드리겠습니다. 요약하자면, Waviate 객체를 생성하려면 속성을 가진 데이터 객체를 만들고 추가하면 됩니다. 원하는 클래스를 가져올 때는 특별한 이유가 없는 한 일괄 가져오기를 사용하는 것이 좋습니다. 일괄 가져오기는 가져오기 속도에 큰 영향을 미칠 수 있습니다. 가져오기 중 오류 처리는 일괄 수준 또는 개별 객체 수준에서 수행할 수 있습니다. Waviate에서는 각 객체에 대해 객체 ID와 벡터를 수동으로 지정할 수 있습니다. 여기까지입니다. 시청해 주셔서 감사합니다. 곧 다시 뵙겠습니다.
