# Speeding Up AI Quantization Techniques for Models and Vector DBs

- 영상 링크: https://www.youtube.com/watch?v=yVlHscjOQKc
- 채널: Weaviate
- 업로드일: 2025-03-26
- 자막 언어: en
- 단어 수: 약 1121개

---

## 스크립트

[Music] so hello um today I will be talking about quantization how one can use quantization techniques for AI models and Vector DBS um my name is Martin I'm a weit COR engineer and I've been in weit for over first four years right now um so I will be first telling you about uh large language models and U their quantization techniques then I will move on to the embedding models how we can quantize them and how Al I will be talking also about how the quanti quantization techniques can be used in Vector databases so let's start first with large language models quantization techniques so um modern large language models are created uh using Transformers model and Transformer model is uh an algorithm that uses uh mathematical formulas uh and works with vectors and parameters models parameters that are being obtained during the training process to then uh find out the and or to predict you the next token and um there are two parts or two main uh types of parameters uh that the model consists of uh there attention layer parameters and Feit forward layer parameters 30% of those parameters are the attention layer and Feit forward layer is the rest Feit forward layer parameters are the ones that the whole knowledge of the model that was acquired during the training process is encoded in and um so what we can do with those parameters so if you have those parameters uh on your device on your uh computer you can just run them and you will be able to interact with a with a large language model those parameters are floating Point numbers uh of a Precision 16 bit or 8 8 bit so um they are quite a big numbers and the quantization process is a process of downgrading or down SC scaling the uh lowering the Precision of those numbers so for example going from floating point 16bit or 8bit to 4bit which then in total makes the parameters smaller uh more memory friendly more GPU friendly there are many um quantization Frameworks out there in the on the market I will just highlight the ones that are most popular ones but really the the one that is the the facto the standard now is the ggf format the ggf format is the format uh that is being used uh with Lama CPP and uh you can this Lama CPP uh framework uh comes with already predefined levels of quantization uh people are using quantization quantization levels um starting from uh Q4 level because Q2 and Q3 are degrading the parameters that much that then the model is unusable it loses all of its reasoning capabilities and also when people are quantizing the models they are choosing the km uh um quantization level where we are able to quantize more the attention layer uh parameters than the fit forward layer parameters because fit forward layer parameters are the ones that the whole knowledge of the um large language model is encoded in and they are very sensitive to the degradation process uh let's talk about the embedding models uh embedding models are the ones that are um that that have the only their only task is to create a vector out of an input sentence and that Vector is um U an array of floating Point numbers uh floating Point numbers of 32bit uh over a 32bit Precision those text embedding models they or in general and M they can be also quantized for qu to in order to quantize the model the embedding model we use either Onyx or open Vino here at V we are using Onyx with Onyx you are able to convert a a model uh precisely to a given CPU architecture that will use that given uh CPUs instruction sets that are actually available then you can actually more easily and um uh and faster run those quanti mod models on a given um CPU uh on a given CPUs on a given CPU architecture uh let's talk about the vectors and the vector representations of data uh when we are talking about uh normal relational databases we are talking about uh columns and their specific types we can also uh save data uh or uh represent data as vectors and then that vectors we can take and save them and create a vector index and save them in a vector database such as a we it and of course as I mentioned before those vectors are floating Point numbers of 3 to2 bit Precision so if one would choose an hnsw index which is an inmemory index and have millions of data objects index then it would require lots of memory so in order to reduce the memory footprint or the uh requirements uh one can pick uh quantization one can quantize the uh vectors that are which the vector index consists of there are three types of um Quant quantization that one can pick a product quantization is a two-step process uh and it's a law uh lossless uh way of uh compressing the vectors because it preserves the original distances but with some types of vectors we can even uh go up to a 90% of memory compression but we can also choose a binary quantization binary quantization is a lossy compression technique which uh reduces the floating Point number to either zero or ones G that gives us a 32 reduction in storage requirements but unfortunately U that comes with a cost um the distance calculations may not be as accurate as the original embeddings as the original floating Point numbers vectors but maybe something in between we can use scolar quantization when we go uh from the floating Point numbers Precision to an integer integer uh 4bit or 8bit Precision where we are able still to grasp a lot of the knowledge encoded or a lot of the numbers encoded in floating Point number and represent them represent them with integer numbers and then save it so but yeah cool but what why why does it matter it matters uh because you can then take all of those techniques and run for example we it or any other Vector database uh Define binary quantization you use uh Onis um uh Vector index and run the vector database on a Raspberry Pi 5 which is a really small and a very constrained device you can also run the vector embedding models on as um on such a small devices like like Nvidia Jetson or in Nano uh and create a really cool site that will run on both of those devices thank you oh
