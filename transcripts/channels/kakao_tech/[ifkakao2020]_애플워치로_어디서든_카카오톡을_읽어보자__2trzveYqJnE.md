# [ifkakao2020] 애플워치로 어디서든 카카오톡을 읽어보자

- 영상 링크: https://www.youtube.com/watch?v=2trzveYqJnE
- 채널: kakao tech
- 업로드일: 2026-06-10
- 자막 언어: ko
- 단어 수: 약 653개

---

## 스크립트

안녕하세요. 카카오톡 iOS 파트에서 톡 iOS앱 개발을 하고 있는 타일러라고 합니다. 올해 초에 카카오 톡은 애플워치 셀룰러 모델의 사용성을 향상시키기 위해서 애플워치만으로도 카카오톡을 사용할 수 있도록 개발했고 이후에는 컴플리케이션도 지원했습니다. 오늘은이 개발 과정에 대해서 간략하게 공유해 드리도록 하겠습니다. 순서는 워치앱 데이터를 최신하는 방법 그리고 워치앱 내부 구조 마지막으로 컴플리케이션에 대해 말씀드리겠습니다. 먼저 워치앱 데이터를 최신하는 방법입니다. 크게 세 가지가 있는데요. 첫째로 연결된 아이폰과 데이터를 공유하는 것, 두 번째는 서버에서 직접 데이터를 가져오는 것, 그리고 클라우드 킷을 이용하는 방법이 있습니다. 카카오톡은 위을 이용했기 때문에 클라우드 킷은 다음에 알아보도록 하겠습니다. 네. 아이폰과 데이터를 공유하기 위해선 워치 커넥티비티 프레임워크를 사용해야 합니다. 데이터를 주고받을 땐 즉각적으로 주고받거나 백그라운드 작업을 통해 주고받을 수 있습니다. 정보를 즉각적으로 주고받기 위해서는 iOS, 워치 앱이 모두 러닝 상태이고 리치브라에 사용 가능하며 아래 코드와 같이 정보를 딕셔너리 혹은 데이터 형태로 주고받을 수 있습니다. 카카오 톡에서는 주로 채팅 리스트, 메시지 리스트, 답장 리스트 등을 주고받고 있으며 주고받는 형태는 딕셔너리를 선택했습니다. 백그라운드 작업을 통해 주거받을 땐 카운터 파트앱이 러닝일 필요가 없고 OS가 적절한 타이밍을 잡아 실행합니다. 하지만 아래 코드와 같이 두 종류가 있는데요. 먼저 업데이트 어플리케이션 컨텍스트는 항상 최신 테스크만 관리하고 트랜스퍼 유저 인포 파일은 테스크가 내부 Q에 대기하게 됩니다.이 이 때문에 카카오톡은 인증 정보같이 최신화가 되어야 되는 정보는 전자로 로인 같이 누적되어도 상관없는 정보는 후자의 함수를 이용했습니다. 카카오톡은 전체적으로 딕셔널 형태로 정보를 주고받고 있습니다. 이유는 주고받는 타입이 다양하고이 타입을 체크하기에 용이하다고 생각했기 때문입니다. 그래서 요청 메소드 타입과 통신에 용되는 딕셔너리를 가진 프로토콜을 만들었고 요청과 응답 객체에이 프로토콜을 준수하도록 했습니다.이 이 프로토콜을 준수함으로써 요청 객체를 만들 때와 어떤 요청인지를 판단할 때 용이하게 사용하고 있습니다. 다음은 서버에 직접 접근하는 방법입니다.이 방법은 다들 익숙하신 URS 세션, 알람모 파이어 등을 이용하실 수 있습니다. 카카오톡은 네트워킹에 필요한 정보를 가지고 있는 프로토콜과 서버 API 타입을 조합하여 알람보 파이어를 이용해 API를 호출하고 있습니다. 다음으로는 워치앱 내부 아키텍처입니다. 앞서 말씀드린 데이터를 관장하는 서비스 클래스들이 있고 워치앱 각 화면을 나타내는 뷰 클래스들이 있습니다.이 사이에 폰과 서버로부터 받아온 데이터를 각 뷰에 맞게 가공해 주는 뷰모델 클래스들이 있습니다. 먼저 서비스 클래스들에 대해서 알아보겠습니다. iOSA의 데이터를 요청할 때 앞서 말씀드린 요청 타입을 통해 요청 객체를 만들고 워치 커넥티비티 프레임워크를 이용해 데이터를 요청합니다. 정상적인 응답이 왔을 경우 핸들러를 이용해 적절한 행동을 취해 주고 있습니다. 다음은 서버에 요청하는 서비스 클래스입니다.이 부분도 비슷합니다. 요청 타입에 따라 API를 호출하고 응답에 적절한 행동을 취해 주고 있습니다. 이번에는 뷰에 대해서 알아보겠습니다. 워치에서 뷰 전환은 기본적으로 스토리보드 베이스인데요. 코드로 뷰 전환을 하고 싶다면 아래와 같이 스토리보드에 인터페이스 컨트롤러 아이덴티파이어와 필요한 객체를 애니 오브젝트 형태로 넘겨 주어야 합니다. 저는이 부분이 실수를 유발할 가능성이 높고 불편하다고 생각되어 워치신과 컨텍스트 객체들을 만들었습니다. 워치시는 각 뷰들에 대한 아이덴티파이어와 컨텍스트들을 가지고 있으며 컨텍스트는 베이스 클래스를 상속받아 구현되어집니다.이 이 워치신을 유연하게 사용할 수 있도록 워치신 프레젠터블 프로토콜을 만들었고이를 채택한 베이스 인터페이스 컨트롤러를 만들었습니다. 실제 사용하는 인터페이스 컨트롤러는이를 상속받도록 합니다. 마지막으로 뷰모델입니다. 뷰모델은 뷰에 대해 추상화한 프로퍼티들을 가지고 있고 이들이 변경될 경우 뷰에서 인자하여 반영합니다. 채팅 리스트를 가져오는 것으로 예를 들어 보겠습니다. 먼저 카카오톡은 iOSF 데이터가 우선 순위가 높습니다. 그래서 iOS가 연결되었을 경우 폰의 데이터를 요청하여 워치앱에 반영합니다. 하지만 연결되 있지 않다면 서버의 데이터를 요청하고 현재 서버로부터 받아온 상태라는 걸 표시해 주고 있습니다. 마지막으로 컴플리케이션입니다. 기본적으로 컴플리케이션은 클라킷 프레임크를 사용해야 합니다. 그리고 컴플리케이션을 띄워 주기 위해선 패밀리와 템플릿을 사용하는데요. 이때 리프레시하는 방법에 대해서 알아보겠습니다. 카카오톡은 안 읽은 메시지가 있을 경우 없을 경우 두 가지 상태를 제공 중이며 주기적으로 리프레시하고 있습니다. 먼저 리프레시하기 위해서는 컴플리케이션 서버에 리로드 타임라인을 호출합니다. 이후 서버는 겟 커런트 타임라인 엔트리를 호출하고 앱에서 적절한 템플릿을 핸들러로 넘겨주어 컴플리케이션을 갱신하고 있습니다. 예시는 그래픽 서큘러와 그래픽 코너 두 가지 패밀리입니다. 각 패밀리에서 적절한 템플릿을 생성하고 안읽금 메시지 유무에 따라 이미지와 스트링을 조합해 반환하게 됩니다. 지금은 안 읽은 메시지가 없을 경우이고 안 읽은 메시지가 있을 경우 컴플리케이션이 다르게 바뀝니다. 컴플리케이션을 백그라운드에서 갱신하고 싶다면 스케줄 백그라운드 리프레시로 등록하시면 됩니다. 익스텐션 딜리게이트에 핸들로 등록된 백그라운드 테스트가 넘어올 경우 컴플리케이션을 갱신하고 상황에 따라 리스케줄 하시면 됩니다. 애플 개발자 공식 문서와 WWDC에 더 자세한 내용들이 나와 있으니 해당 부분도 같이 봐 주시면 더 도움이 될 것 같습니다. 발표를 드려 주셔서 정말 감사드리고 계속해서 좋은 사용성으로 보답하도록 노력하겠습니다. 이상 카카오톡 애플워치 세션 발표를 마치겠습니다. 감사합니다.
