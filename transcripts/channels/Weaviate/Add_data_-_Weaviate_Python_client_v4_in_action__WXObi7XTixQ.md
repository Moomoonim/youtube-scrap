# Add data - Weaviate Python client v4 in action

- 영상 링크: https://www.youtube.com/watch?v=WXObi7XTixQ
- 채널: Weaviate
- 업로드일: 2024-02-06
- 자막 언어: ko
- 단어 수: 약 1149개

---

## 스크립트

안녕하세요, 저는 Wave8의 교육자 JP입니다. 이전 영상에서는 새로운 V4 파이썬 클라이언트를 사용하여 컬렉션을 만드는 방법을 보여드렸습니다. 이번 영상에서는 그 내용을 바탕으로 새로운 클라이언트를 사용하여 Wave8에 데이터를 가져오는 방법을 살펴보겠습니다. 먼저 간단하게 스크립트를 처음부터 작성해 보겠습니다. Wave8을 임포트하고, 이전과 마찬가지로 내장 클라이언트에 연결합니다. 데이터를 가져올 것이므로 API 키도 헤더에 전달하겠습니다. 벡터라이저로 CoH를 정의했으므로 CoH 키를 사용하겠습니다. [Music]이라는 CoH 키는 환경 변수로 저장되어 있으므로 여기에 불러오겠습니다. 좋습니다. ' reviews'라는 컬렉션이 있으므로 ` clientcollections` 함수를 호출하여 불러오겠습니다 . 컬렉션 이름을 가져와서 검토해 보겠습니다. 이렇게 하면 상호 작용할 수 있는 Python 객체가 생성됩니다. 이제 영화 컬렉션도 같은 방식으로 해보겠습니다. BAS 임포트를 보여드리겠습니다. 단일 삽입을 하려면 컬렉션 객체를 사용하면 됩니다. 데이터 하위 모듈 또는 네임스페이스를 살펴보면 `insert`라는 메서드가 있습니다. 이름에서 알 수 있듯이 `insert`는 단일 삽입용입니다. 메서드를 로드하고 툴팁을 보면 속성, 참조, UUID 등과 같은 매개변수가 있다고 나와 있습니다. 보시다시피 제공해야 하는 유일한 것은 속성입니다. 예를 들어 보겠습니다. `username`이라는 속성이 있다고 가정해 보겠습니다. 이 속성을 제공하면 됩니다. 예를 들어 ` username`을 입력해 보겠습니다. 이것이 데이터 행 또는 객체를 생성하는 데 필요한 전부입니다. 올바른 속성이 삽입되었고 스키마와 일치하며 올바른 객체에 삽입되었다면 데이터 행을 생성할 수 있습니다. 나머지는 제가 알아서 처리해 드리겠습니다. SP를 사용하시면 되고, 앞서 말씀드린 것처럼 ID 벡터 등 다른 객체나 속성을 지정할 수도 있습니다. 그럼 영화 삽입을 예로 들어보겠습니다. '모던 타임즈' 라는 영화를 삽입하고 싶다고 가정해 보겠습니다. 그리고 참조 매개변수를 통해 참조를 확인할 수 있도록 교차 참조를 생성하고 싶습니다. 이렇게 하려면 참조 속성 이름(예: ' review')을 사용해야 합니다. 그리고 대상 UU ID를 제공하고 싶은데, 아직 없습니다. 어디서 얻을 수 있을까요? 먼저 속성 이름 이나 변수 이름을 정해 보겠습니다. 이 삽입 메서드는 UU ID를 반환하므로, 이 ID를 변수 에 할당하면 됩니다. 이 UU ID를 사용하여 참조를 다시 생성할 수 있습니다. 물론 이것은 실제 예시는 아닙니다. 제 속성이나 'review' 에는 내용이 없기 때문입니다. 하지만 ' review' 또는 대상 객체를 먼저 생성하면 UU ID를 캡처하여 나중에 사용할 수 있습니다. 참조를 생성하는 간단한 예시를 살펴보았습니다. 이제 여러 객체를 삽입하는 방법을 살펴보겠습니다. 이 방법도 매우 유사하게 작동하지만, 먼저 사용할 객체 목록을 만들어 보겠습니다. 이 목록을 ' review props'라고 하겠습니다. 리스트 컴프리헨션을 사용하여 다음과 같이 정의하겠습니다. ' username'이라는 열이 있다고 가정하면, 문자열 'user'를 사용하고 인덱스를 지정하겠습니다. (오류: for I in range) 이제 다섯 개의 객체를 생성해 보겠습니다. 이렇게 하면 각기 다른 인덱스를 가진 다섯 개의 사용자 객체가 생성됩니다. 이제 'reviews data insert manyu to add review props'라고 입력하면 목록에 지정된 모든 객체가 생성됩니다. 이처럼 목록을 리뷰 함수에 전달할 수 있게 된 것은 저에게 있어 큰 편의성 개선 사항입니다. 만약 참조, 속성, UID 또는 벡터와 같은 추가 메타데이터를 포함하여 여러 객체를 전달하고 싶다면, 이 편리한 객체를 사용할 수 있습니다. Data 객체라는 클래스가 있지만, 당연히 먼저 임포트해야 하죠. 그래서 `import data object`라고 해야겠네요. 아, 맞다. ` from weate classes data`였군요. 제가 설정하려는 작업이 바로 그거니까요. ` data` 서브모듈에서 ` import data object`를 찾을 수 있겠네요. 제가 원하는 건 바로 이거예요. 이제 데이터 객체들을 만들어 볼까요? 우선 하나를 만들어 보죠. 속성으로 인스턴스를 생성할 수 있겠네요. 예를 들어 ` title`은 간단하게 ` movie`라고 할게요. 일단은 그냥 `1`이라고 하죠. 아, 실수했네요. 참조는 이전과 비슷한 형식일 테니 복사해서 붙여넣을게요. 이게 단일 데이터 객체의 모습입니다. 이제 이런 데이터 객체들을 모아서 목록을 만들어 보고 싶네요. 코드는 대략 ` for I in range five` 정도일 거예요. 여기서 ` F` 문자열을 실제 인덱스로 바꿔주면, 이렇게 하면 영화 데이터 객체 5개가 담긴 목록이 생성됩니다. 이 목록을 ` movie objects`라고 부를게요. 이를 활용하여 영화 데이터에 여러 영화 객체를 삽입하면 이제 5개의 객체가 삽입됩니다. 각 객체는 속성과 참조를 가지며, 앞서 언급했듯이 UID와 같은 항목을 추가하고 사용자 지정 UID를 생성할 수 있습니다. 삽입 및 여러 객체 삽입은 매우 유용한 함수입니다. 특히 여러 객체 삽입은 목록을 직접 처리할 수 있기 때문에 더욱 그렇습니다. Weate에 업로드할 목록이 하나만 있는 경우 이 방법을 사용하지만, 대규모 데이터 세트를 처리해야 하는 경우도 많습니다. 이러한 경우에는 여전히 유용한 배치 함수를 사용할 수 있습니다. 이제 배치 함수를 간단히 살펴보겠습니다. V4 클라이언트의 배치 함수는 두 가지 유형이 있습니다. 클라이언트 객체에서 배치를 찾을 수 있으며, 동적, 고정 크기, 속도 제한과 같은 다양한 메서드를 볼 수 있습니다. 동적 배치는 아마도 가장 익숙한 유형일 것입니다. 이 기능을 사용하는 방법은 이전에 했던 것처럼 컨텍스트 관리자를 사용하는 것입니다. 배치를 사용하면 다음과 같은 작업을 시작할 수 있습니다. 배치 처리에 필요한 모든 기능은 객체를 배치로 추가하는 방식입니다. 예를 들어 영화를 추가한다고 가정하면 제목은 ' 아바타'가 됩니다. 클라이언트 측에서는 해당 객체가 어떤 컬렉션에 속해야 하는지 알 수 없으므로 컬렉션을 지정해야 합니다. 즉, ' 컬렉션: 영화'라고 지정하면 됩니다. 이 정도면 충분합니다. 필요에 따라 UUID나 벡터와 같은 정보도 선택적으로 제공할 수 있습니다. 배치 처리에 사용할 수 있는 다른 함수나 메서드로는 ' fix size'가 있습니다. 이 메서드를 사용하면 배치 크기를 지정할 수 있습니다. 예를 들어 ' batch size'를 50으로 설정하면 배치당 50개의 객체가 we8로 전송됩니다. 또 다른 유용한 메서드는 ' rate limit'입니다. 이 메서드는 weate로 데이터를 전송하는 속도를 제한합니다. 이 기능이 유용한 이유는 API 추론 기능을 사용할 때, 예를 들어 제가 사용하는 H와 같은 API를 사용할 때 계정 에 특정 전송 속도 제한이 있는 경우, 이 기능을 사용하여 제한에 걸리지 않도록 할 수 있기 때문입니다. 그 숫자로 설정되어 있습니다. 일반적으로는 실제 속도 제한보다 약간 낮은 값으로 설정하는 것을 권장합니다. 왜냐하면 이러한 것들은 변동될 수 있기 때문에 정확한 제한값에 맞추는 것은 좋지 않기 때문입니다. 따라서 약간 낮은 값으로 설정하는 것이 좋습니다. 또 다른 유용한 기능은 컬렉션 객체를 사용하여 배치 처리를 시작하는 것입니다. 예를 들어, 영화 객체를 배치 동적(Batch Dynamic)으로 처리하고 컨텍스트 관리자를 배치로 사용한다고 가정해 보겠습니다. 이제 배치 객체를 사용하여 객체나 참조 등을 추가할 수 있습니다. 객체를 추가하고 속성만 제공하면 됩니다. 예를 들어, "This Time" 제목은 다음과 같습니다. 오, 여기 제목이 있습니다. 이것이 제가 해야 할 전부입니다. 매개변수 목록을 보면 컬렉션 매개변수가 없는 것을 볼 수 있습니다. 이는 해당 정보가 컬렉션을 기반으로 하는 영화 객체에 포함되어 있기 때문입니다. 정말 편리하죠. 마지막으로 오류 처리에 대해 이야기해 보겠습니다. 전체 배치 삽입이 성공했더 라도 객체 수준에서 문제가 발생할 수 있다는 점을 알아두세요. 객체 형식이 잘못되었거나, 일시적으로 API 오류가 발생하는 등의 문제가 있을 수 있습니다. 이러한 문제를 처리하는 방법은 여러 가지가 있으며, 이 기능은 정말 유용합니다. 예를 들어, 배치를 실행한 후 오류 횟수를 지정할 수 있습니다. 컨텍스트 관리자 내에서 배치를 실행하는 동안 이 횟수가 업데이트되므로, 오류 횟수가 100 이상이면 중단하도록 설정할 수 있습니다. 그런 다음 무슨 일이 일어나고 있는지 확인할 수 있습니다. 물론 이는 루프 내에서 for 루프를 사용하여 100,000과 같은 조건을 설정할 수 있습니다. 또한 배치 실행이 완료된 후 배치 결과를 확인할 수도 있습니다. 예를 들어, "이제 영화들을 볼 차례입니다."와 같이 확인할 수 있습니다. 배치 작업을 수행하고 실패한 객체 또는 참조를 개별적으로 확인해 보겠습니다. 그러면 최종적으로 다음과 같은 작업을 수행할 수 있습니다. 예를 들어, 길이가 0보다 크면 실패 처리 하거나, 모든 영화 배치의 실패한 참조 길이가 0보다 크면 실패 처리하도록 설정할 수 있습니다. 지금은 일단 통과로 처리하겠지만, 이렇게 하면 배치 실행의 전체 결과를 확인하고 무슨 일이 발생했는지 알 수 있습니다. 참고로, 이러한 컨텍스트 관리자를 사용하는 경우, 다른 데이터 하위 집합으로 이 프로세스를 다시 실행하면 실패한 객체 및 참조와 같은 속성이 덮어쓰여집니다. 따라서 배치 실행 사이에 오류 처리를 수행해야 합니다. 이것으로 V4 클라이언트의 객체 가져오기 프로세스에 대한 간략한 소개를 마치겠습니다. 많은 편의성 개선 사항이 있으며, 이전에 언급하지 않았지만 GPC를 내부적으로 사용하여 가져오기 프로세스가 훨씬 빨라졌습니다. 저희가 개발하면서 즐거웠던 만큼 여러분도 즐겁게 사용하시길 바랍니다. 감사합니다. 곧 다시 뵙겠습니다.
