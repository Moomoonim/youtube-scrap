# CUDA 13.0—New Features and Beyond | NVIDIA GTC D.C.

- 영상 링크: https://www.youtube.com/watch?v=_vL8uz7kf0M
- 채널: NVIDIA Developer
- 업로드일: 2025-12-02
- 자막 언어: en
- 단어 수: 약 8510개

---

## 스크립트

Hey, good afternoon everybody. My name is Rob like&nbsp; mentioned and I work with the CUDA team here at&nbsp;&nbsp; NVIDIA. And today I have kind of the daunting task&nbsp; of condensing all of the work that's happening&nbsp;&nbsp; in CUDA into about a 30-minute presentation for&nbsp; you. So if you remember anything is that I love&nbsp;&nbsp; talking about CUDA and you can always grab me or&nbsp; my co-workers in the hall. We'll talk your ear off&nbsp;&nbsp; about this stuff for hours. Otherwise, if there's&nbsp; one or two things that pique your interest here,&nbsp;&nbsp; my mission is done. So, CUDA 13 is our latest&nbsp; release. It came out in August of this year, and&nbsp;&nbsp; this is really the first release that's full bore&nbsp; on Blackwell. We've got full support for all of&nbsp;&nbsp; the architectures and so on. But many of you know&nbsp; that CUDA follows semantic versioning. And so,&nbsp;&nbsp; for us, going to a new major version is a really&nbsp; major event and it's a significant milestone.&nbsp;&nbsp; It lets us make changes to the API, changes that&nbsp; really set the stage and lay the foundation for&nbsp;&nbsp; all the development work that we're going to&nbsp; do over the rest of the CUDA release. Generally&nbsp;&nbsp; speaking, a CUDA release tends to last you around&nbsp; 18 months or so. And so CUDA 13 for us is more&nbsp;&nbsp; than just the specific set of new things that&nbsp; are in the box. It's really about how we carry&nbsp;&nbsp; forward the CUDA programming model and some of&nbsp; the really exciting things that we're doing over&nbsp;&nbsp; the next six to 12 months even. Before I get into&nbsp; all the really exciting stuff though, let me kind&nbsp;&nbsp; of paint a picture for you for what's in the box&nbsp; with CUDA 13 itself. Memory management is always&nbsp;&nbsp; a really significant part of optimization&nbsp; for any algorithm. And this is independent&nbsp;&nbsp; of architecture. This could be a traditional ARM&nbsp; server topology. It could be Grace. It could be&nbsp;&nbsp; x86 PCIe. could be an embedded system. Doesn't&nbsp; really matter. The point is that in any system,&nbsp;&nbsp; where your memory is, when it's moving, and when&nbsp; it's accessed is one of the most critical pieces&nbsp;&nbsp; of information for you as a developer to be aware&nbsp; of. To access it on the GPU, it needs to be in GPU&nbsp;&nbsp; memory. To access it on the CPU, it needs to be in&nbsp; CPU memory. Otherwise, you're moving it back and&nbsp;&nbsp; forth. And so, we're putting a lot of effort into&nbsp; extending and augmenting CUDA's memory management&nbsp;&nbsp; APIs. In CUDA 13, we've given you support for&nbsp; things like manage memory discard and UVM.&nbsp;&nbsp; We're also giving you things like the ability to&nbsp; pass in parameters we call memory allocators. You&nbsp;&nbsp; can tell the runtime, I want this memory on the&nbsp; CPU. I want it on the GPU. I want it moved here or&nbsp;&nbsp; there. All kinds of additional hinting to control&nbsp; where things are moving in your system and how&nbsp;&nbsp; they're accessed. We've also added a new driver&nbsp; mode along the same lines called CDMM or compute&nbsp;&nbsp; device memory manager. What this does, if you're&nbsp; familiar with Grace systems, the memory layouts&nbsp;&nbsp; of the Grace system were done as Newman nodes.&nbsp; Each GPU represented a specific NUMA node. All&nbsp;&nbsp; the system was available in a large NUMA pool. And&nbsp; this is great for some types of applications. It's&nbsp;&nbsp; not as great for other kinds of applications. In&nbsp; particular, some of the things like Kubernetes had&nbsp;&nbsp; a little bit of a challenging time understanding&nbsp; NUMA and understanding where to allocate memory&nbsp;&nbsp; and where to move it around. So ultimately what&nbsp; we're doing with CDMM is giving you the option to&nbsp;&nbsp; use NUMA or not. By enabling CDMM, you still can&nbsp; take advantage of all the coherency in the Grace&nbsp;&nbsp; platforms. you can still use ATS and so on, but&nbsp; you don't have a NUMA style topology. You have a&nbsp;&nbsp; much more traditional memory style topology. So,&nbsp; if you're taking legacy applications and bringing&nbsp;&nbsp; them in or if you have other things that are&nbsp; not familiar with Numa, this can be a great way.&nbsp;&nbsp; Ultimately, we're just giving you more flexibility&nbsp; in how you configure these systems and how you use&nbsp;&nbsp; the memory that's available to you. For Windows&nbsp; users and Windows developers, we're now signing&nbsp;&nbsp; all of the executables and libraries that we&nbsp; distribute, which is great if you're trying to&nbsp;&nbsp; comply with particular security requirements or if&nbsp; you're trying to sell through particular OEMs and&nbsp;&nbsp; things like this. We've augmented our checkpoint&nbsp; and restore. Now you can migrate to different GPUs&nbsp;&nbsp; and different platforms. Now, some of you may&nbsp; not be familiar with checkpoint and restore.&nbsp;&nbsp; There are two real important use cases for this.&nbsp; One is if you have a really long running job,&nbsp;&nbsp; let's say you're doing a m training or something&nbsp; like this that can take many hours or days,&nbsp;&nbsp; it's nice to be able to periodically checkpoint&nbsp; it. So if something happens to the system, you get&nbsp;&nbsp; a single event upstead or get some unreoverable&nbsp; error, you need to resume, you can do that. The&nbsp;&nbsp; other really nice thing about checkpointing and&nbsp; restoring is inference. And what makes it nice&nbsp;&nbsp; for inference is that if you've ever tried to cold&nbsp; start an inference system, you can understand that&nbsp;&nbsp; doing all the initial setup and configuration and&nbsp; so on can take a non-negligible amount of time.&nbsp;&nbsp; And there are actually some recent publications&nbsp; where people have been using GPU checkpointing&nbsp;&nbsp; and restore or warm start of inference systems.&nbsp; And you can get some really substantial speed&nbsp;&nbsp; ups at system configuration or initialization&nbsp; time by restoring from a checkpoint as opposed&nbsp;&nbsp; to launching a new cold application and having&nbsp; that application go through all of its initial&nbsp;&nbsp; setup. There are some other kind of nerdy things&nbsp; in here that I I'm really fond of. for example,&nbsp;&nbsp; we can spill registers out of individual kernels&nbsp; to shared memory. This is representative of a long&nbsp;&nbsp; list of things that you can do at the optimization&nbsp; level. It's really kind of a micro optimization,&nbsp;&nbsp; but the nice thing here is that you can get about&nbsp; an order of magnitude lower latency by spilling&nbsp;&nbsp; to shared memory than you can with traditional&nbsp; register spilling. So again, this is like I'm&nbsp;&nbsp; not going to go into a lot of detail about this&nbsp; because it's a very niche thing and it's a very&nbsp;&nbsp; ninja level optimization, but we have a lot of&nbsp; stuff in the box that like this. And then another&nbsp;&nbsp; big one that I'll come back to is a unification&nbsp; of the programming model and of the development&nbsp;&nbsp; architecture between our embedded SOC targets,&nbsp; the Jetson family and our server topologies in&nbsp;&nbsp; the data center. Historically, NVIDIA has&nbsp; had these treated as two separate entities&nbsp;&nbsp; effectively for software developers. And now with&nbsp; updates to the driver and updates to the toolkit,&nbsp;&nbsp; these are really one and the same. You can write&nbsp; one application, run it anywhere, scale it all&nbsp;&nbsp; the way from massive cluster scale data center&nbsp; down to a robot. So really cool things and in&nbsp;&nbsp; terms of our long-term development directions,&nbsp; these are probably about the same as when I had&nbsp;&nbsp; come and spoken to you last year. In terms of&nbsp; languages, our goal within CUDA is always to&nbsp;&nbsp; be present wherever you want to be developing. So&nbsp; CUDA has a long pedigree in C++. It grew up there.&nbsp;&nbsp; It's been used there for a long time. Python, of&nbsp; course, extremely popular and relevant in many&nbsp;&nbsp; different domains, HPC, deep learning, and so&nbsp; on. We've put a lot of effort and continue to&nbsp;&nbsp; put a lot of effort into migrating the stack to&nbsp; Python. Making Python a first class language for&nbsp;&nbsp; CUDA toolkit. And that doesn't mean writing C++&nbsp; and Python. It really means reinterpreting and&nbsp;&nbsp; reimagining how you write and build applications&nbsp; to take advantage of Python natively. We're also&nbsp;&nbsp; looking at additional languages that are becoming&nbsp; more prevalent. Things like Rust, C, other areas.&nbsp;&nbsp; Ultimately, again, this is something where we want&nbsp; to be present and available. We don't want you to&nbsp;&nbsp; have to take a C++.Oo O and put a wrapper around&nbsp; integrate it with some toolkit or framework. We&nbsp;&nbsp; want you to just be able to use CUDA the way&nbsp; mother nature intended effectively. Now NVIDIA&nbsp;&nbsp; talks a lot about scaling, right? Scale up, scale&nbsp; down, scale out, scale everywhere. And it's great&nbsp;&nbsp; to build large scale clusters. If anyone has ever&nbsp; done programming for large scale clusters, it can&nbsp;&nbsp; be a little bit more challenging. The technologies&nbsp; to do this aren't really new, right? NPI is not&nbsp;&nbsp; new. It's been around for a long time. There are&nbsp; lots of things that make it easier and better,&nbsp;&nbsp; but we're always putting a lot of effort and&nbsp; energy into trying to understand how we can do&nbsp;&nbsp; this more effectively. How can we give you tools&nbsp; that allow you to build at cluster scale, run&nbsp;&nbsp; inference at cluster scale, calculate workloads&nbsp; at cluster scale without having to understand the&nbsp;&nbsp; nuances and the mechanics of working in a cluster.&nbsp; We have things that we've already come out with&nbsp;&nbsp; like coupon numeric that allow you to do this kind&nbsp; of scaling. Take numpy like code and broadcast it&nbsp;&nbsp; across a wide variety of machines. We also are&nbsp; working on new programming models where you can&nbsp;&nbsp; take things like a CUDA graph and then selectively&nbsp; distribute memory allocations, workload elements&nbsp;&nbsp; and so on across the entirety of a cluster.&nbsp; So in general, we're putting a lot of work&nbsp;&nbsp; into trying to make cluster scale orchestration&nbsp; and application management much easier for you.&nbsp;&nbsp; The flip side of that is hierarchy and locality.&nbsp; It's kind of turtles all the way down. If you're&nbsp;&nbsp; looking at cluster scale, if you're looking&nbsp; at a single server system with multiple GPUs,&nbsp;&nbsp; if you're looking at a single GPU, the concepts&nbsp; repeat. Moving memory is expensive. It's always&nbsp;&nbsp; expensive. What changes is the cost, but it's&nbsp; always orders of magnitude slower to access&nbsp;&nbsp; global memory than it is to access on chip memory.&nbsp; It's orders of magnitude slower to copy from a CPU&nbsp;&nbsp; to a GPU. Doesn't matter what hardware topology&nbsp; you're on. That's just fundamental limitations&nbsp;&nbsp; of the architectures that exist in the world&nbsp; today. And so I mentioned earlier some of the&nbsp;&nbsp; kind of small scale improvements, but this is in&nbsp; aggregate a big push from us to help make memory&nbsp;&nbsp; management easier and to make the migration&nbsp; of memory more transparent, more seamless,&nbsp;&nbsp; and more predictable. You want to understand&nbsp; where your data is moving and when. You want to&nbsp;&nbsp; understand why. And when you're doing system level&nbsp; optimization, this is one of the real pieces of&nbsp;&nbsp; lowhanging fruit. Like when is my data moving and&nbsp; what am I spending in terms of energy? what am I&nbsp;&nbsp; spending in terms of time? How can I reduce it and&nbsp; orchestrate? Now, on a completely different note,&nbsp;&nbsp; we have a topology that's becoming much more&nbsp; prevalent. If you saw the keynote yesterday,&nbsp;&nbsp; you heard Jensen talking about aerial, heard&nbsp; Jensen talking about quantum computing. You've&nbsp;&nbsp; probably seen some sessions about this, too.&nbsp; All of these domains have something in common,&nbsp;&nbsp; which is that we have to re we have to respond&nbsp; to real world events very, very quickly. We're&nbsp;&nbsp; talking about brand applications, for instance.&nbsp; you have windows of data processing on the order&nbsp;&nbsp; of hundreds of microsconds. If you're talking&nbsp; about quantum computing, you have windows on&nbsp;&nbsp; the order of tens of microsconds. And if your GPU&nbsp; or your system is not able to respond in that time&nbsp;&nbsp; frame, tough luck. You've lost it. And so being&nbsp; able to schedule things on the GPU, being able&nbsp;&nbsp; to predictably schedule things on the GPU with&nbsp; deterministic latency in response to real world&nbsp;&nbsp; events outside of the GPU system is critically&nbsp; important. And the GPUs and the GPU software&nbsp;&nbsp; wasn't, you know, if you think back to like 30&nbsp; years ago when we were doing virtual fighter,&nbsp;&nbsp; it's it wasn't designed for that. If it takes&nbsp; a couple extra milliseconds to render a frame,&nbsp;&nbsp; like that's a bummer, but it doesn't it doesn't&nbsp; cost you anything really in the real world. If&nbsp;&nbsp; you're doing any of the domain applications I&nbsp; mentioned and a bunch of other ones besides, it&nbsp;&nbsp; really does. And so there's a lot that we're doing&nbsp; in terms of how we can schedule these workloads,&nbsp;&nbsp; how we can orchestrate, and just in general&nbsp; how we can be responsive to external events.&nbsp;&nbsp; I'm not going to talk about it much today,&nbsp; but if this is something that interests you,&nbsp;&nbsp; find me afterwards. I'm happy to talk about this&nbsp; because it's really exciting. GPU scheduling is&nbsp;&nbsp; one of those things that I get very excited&nbsp; about. I'm probably unique, but anyway,&nbsp;&nbsp; it's it's it's very very important for a lot of&nbsp; different domains and the way that we control and&nbsp;&nbsp; the way we orchestrate is super. And finally, the&nbsp; last one for us before I really get into the meat&nbsp;&nbsp; of this is just generally how we educate people&nbsp; about the platform. All of this is complex. We try&nbsp;&nbsp; to make it as easy as possible. One of the things&nbsp; we always like to say is that we try to write a&nbsp;&nbsp; million lines of code so you only have to write&nbsp; one. But we all know that's a lot, right? You have&nbsp;&nbsp; to write a lot of code and understanding how to&nbsp; write that code and what the system is doing under&nbsp;&nbsp; the hood and how to use the hardware that you have&nbsp; to maximal advantage is hard. UDA as a platform&nbsp;&nbsp; has grown really organically over the last 20&nbsp; years and the documentation and the examples and&nbsp;&nbsp; everything have kind of grown organically&nbsp; with it and coming to it fresh. You know,&nbsp;&nbsp; I like to tell a story. Many of you probably know&nbsp; Stephen Jones or at least you've seen him speak&nbsp;&nbsp; at other GTC events or on YouTube or what have&nbsp; you. And before I joined NVIDIA, I had a couple&nbsp;&nbsp; of weeks to myself. And so I sat down and I did&nbsp; a bunch of programming exercises on the GPU. I&nbsp;&nbsp; thought I'm gonna do a reduction or a scan or,&nbsp; you know, pick any kind of like basic algorithm,&nbsp;&nbsp; right? So, I did it and I got my performance to a&nbsp; pretty good point and I was really happy with it.&nbsp;&nbsp; And then I went and joined NVIDIA and I said,&nbsp; "Hey, Stephen Jones's desk is right across the&nbsp;&nbsp; hall. I'm going to go ask him how well I did." And&nbsp; he took a look at my code and he said, "No, this&nbsp;&nbsp; is terrible. This is wrong. This is wrong. This&nbsp; is wrong. You should have used that over here."&nbsp;&nbsp; And I was thinking to myself, how would I learn&nbsp; that outside of NVIDIA? I, you know, everyone in&nbsp;&nbsp; this room can't go and stand on Steven's desk and&nbsp; annoy him until he looks at your code. So, how do&nbsp;&nbsp; you pick this up? And so, long story short, we're&nbsp; doing a lot to revamp the onboarding material,&nbsp;&nbsp; completely rewriting the samples. We have an&nbsp; update to the programming guide where it's being&nbsp;&nbsp; completely rewritten, hopefully much easier. We&nbsp; have the accelerated computing hub now, which is&nbsp;&nbsp; a great GitHub resource. You can, one click of a&nbsp; button, you're in Google Collab, you can play with&nbsp;&nbsp; things and evaluate. We're working with the DLI&nbsp; team to improve the courses. And my goal anyway&nbsp;&nbsp; is to make it so that if you as a developer want&nbsp; to learn CUDA, you want to teach CUDA to yourself,&nbsp;&nbsp; if you want to pick up a new technique or a new&nbsp; technology, whatever it is, we want to make that&nbsp;&nbsp; available and accessible to you. And so we've&nbsp; got a whole team of people now who are focused&nbsp;&nbsp; on this. They've actually been focused on it for&nbsp; about a year. The initial fruits of their labor&nbsp;&nbsp; are going to be coming public in about a month.&nbsp; And then beyond that, with every new release,&nbsp;&nbsp; you'll see more and more of this from us. But in&nbsp; general, we just need to make this much easier.&nbsp;&nbsp; Speaking of making things easier, let me get into&nbsp; some of the exciting things. And this is actually&nbsp;&nbsp; a project that I've been working on for about&nbsp; two years now, and I'm really, really excited&nbsp;&nbsp; about it. Because with CUDA, one of the kind of&nbsp; traditionally challenging things is just wrapping&nbsp;&nbsp; your head around how the GPU works. Parallel&nbsp; programming is inherently a little bit like&nbsp;&nbsp; seeing into the fourth dimension. And you have&nbsp; to understand how to segment work across the SM,&nbsp;&nbsp; how to get thousands and thousands of threads&nbsp; cooperating with one another in parallel. And&nbsp;&nbsp; for many many domains, that's not really what you&nbsp; want to do. You want to do the dotproduct of a&nbsp;&nbsp; tensor with another tensor or do a block multiply&nbsp; by a scaler or add or whatever. You're just trying&nbsp;&nbsp; to do some math basically. And so the programming&nbsp; model that allows you to do that most effectively&nbsp;&nbsp; is probably something that looks a lot like numpy.&nbsp; I can have an array of data. I can take the dot&nbsp;&nbsp; productduct with another array of data and I don't&nbsp; necessarily want to understand or know how to deep&nbsp;&nbsp; dive into the internal minations of the GPU to&nbsp; make all that happen. It's much more natural to&nbsp;&nbsp; just express the math at the array or the tensor&nbsp; level. There's another problem that kind of crops&nbsp;&nbsp; up in parallel with this which is that as we go&nbsp; from GPU generation to GPU generation, tensor&nbsp;&nbsp; cores become much more capable and much more&nbsp; complex. As a result, we now have many different&nbsp;&nbsp; data types. We have many different possible&nbsp; combinations of memory accesses and data types&nbsp;&nbsp; and tile sizes and so on. And that did a couple of&nbsp; things. First, it made a lot of the libraries very&nbsp;&nbsp; very large. If you look at Kublas, KUDNN, the&nbsp; historical way of doing these things has been&nbsp;&nbsp; to have tens or hundreds of thousands of discrete&nbsp; kernels for any possible combination of data type,&nbsp;&nbsp; problem shape, what have you. The other thing it&nbsp; did is make things architecture specific. If you&nbsp;&nbsp; think back to the early days of GPUs and up until&nbsp; really about Ampair, we had a technology that many&nbsp;&nbsp; of you have probably heard about called PTX. PTX&nbsp; is a virtual ISA assembly language. And the way&nbsp;&nbsp; that we always handled portability was that when&nbsp; a new GPU generation came out, we have compilers&nbsp;&nbsp; that can take that virtual ISA assembly language,&nbsp; translate it into the specific operations that run&nbsp;&nbsp; on that GPU, and then get them on the hardware.&nbsp; That works really well for JIT compiling one&nbsp;&nbsp; instruction to another instruction or a set of&nbsp; instructions to another set. It doesn't really&nbsp;&nbsp; work well if your application has to fundamentally&nbsp; change. And that's what happens with this domain&nbsp;&nbsp; specific hardware. Going from an ampair era tensor&nbsp; core for instance, it required a single warp of 32&nbsp;&nbsp; threads to fully drive the tensor. On Hopper, that&nbsp; same tensor core now requires 128 threads across&nbsp;&nbsp; four warps to drive it effectively. And we can jet&nbsp; compile instructions to other instructions, but we&nbsp;&nbsp; can't jet compile new threads into existence. We&nbsp; can't jit compile a new memory layout for you. And&nbsp;&nbsp; so we started to think about how far do you need&nbsp; to zoom out to be able to regain that independence&nbsp;&nbsp; and that portability between GPU generations. And&nbsp; it turns out that you pretty much have to zoom out&nbsp;&nbsp; at the same level I was just talking about. So we&nbsp; can kind of kill two birds with one stone. We can&nbsp;&nbsp; give you as developers a much more performant and&nbsp; productive programming model that's easier to use,&nbsp;&nbsp; easier to teach people, easier to understand, and&nbsp; that reintroduces portability to different GPU&nbsp;&nbsp; architectures and generations by rel by leaning&nbsp; into and really relying more on runtime code&nbsp;&nbsp; generation. It's not thousands and thousands and&nbsp; thousands of pre-ompiled pre-built kernels. It's&nbsp;&nbsp; the compiler understanding the underlying hardware&nbsp; and mapping abstracted algorithmic operations onto&nbsp;&nbsp; that hardware for maximal effect. And so the&nbsp; programming model looks a lot like this. If you&nbsp;&nbsp; think about the way that we usually subdivide work&nbsp; on GPUs, you have the grid of data that represents&nbsp;&nbsp; the entirety of your problem space. You subdivided&nbsp; that into blocks. Each block represented something&nbsp;&nbsp; that was going to run on an individual SM.&nbsp; And then in the traditional SIMY model,&nbsp;&nbsp; you're further subdividing them into threads. And&nbsp; those threads have to communicate with one another&nbsp;&nbsp; and handshake and do all the things threads do. In&nbsp; the tile model, you're not doing that last step.&nbsp;&nbsp; You're just subdividing your work into individual&nbsp; blocks and then describing things that are going&nbsp;&nbsp; to happen to those blocks. Each unit of data is&nbsp; a tile, not a specific element. And you're doing&nbsp;&nbsp; things at the array level of granularity. Now, of&nbsp; course, not your entire application is built this&nbsp;&nbsp; way. You have things that are element wise.&nbsp; have things that are not array or block level&nbsp;&nbsp; operations. And so we expect that all of this&nbsp; fits into a much larger ecosystem. I've got this&nbsp;&nbsp; one circled here on the slide. Cut tile is our&nbsp; userfacing programming model expression of this.&nbsp;&nbsp; And I that's an asterisk I'll come back to in a&nbsp; second. But ultimately this sits side by side with&nbsp;&nbsp; SIMT programming with some of the more low-level&nbsp; optimizations like Cutless. And it's really&nbsp;&nbsp; a pick the right tool for the right job. All of&nbsp; these things compose well into the same program.&nbsp;&nbsp; You have SIMT kernels that are doing traditional&nbsp; things. We're not saying you need to rewrite your&nbsp;&nbsp; whole application in tile. But for things that&nbsp; can take advantage of tile or especially things&nbsp;&nbsp; that are more linear algebra based or would make&nbsp; good use of the tensor course, write them in tile.&nbsp;&nbsp; If you really want to get into the low levels&nbsp; and kind of hand tweak and optimize things,&nbsp;&nbsp; we have lower level expressions of the same thing&nbsp; in cutless. The big difference between tile and&nbsp;&nbsp; cutless, and I'll mention this quickly because I&nbsp; get this question a lot, is you know what's the&nbsp;&nbsp; difference? They're both kind of solving the&nbsp; same problem. How do I drive the tensor cores&nbsp;&nbsp; from a higher level language? Cutless is all&nbsp; about exposing the low-level internals of the&nbsp;&nbsp; GPU at architecture specific granularity. Tile&nbsp; is about getting good performance on a GPU with&nbsp;&nbsp; architecture agnostic granularity. So what that&nbsp; means is that if you really want to do something&nbsp;&nbsp; that's non-portable, Atlas exposes it to you and&nbsp; Tile doesn't. Now the good thing about this is&nbsp;&nbsp; that for most use cases architecture independence&nbsp; isn't something that you have to give up. You can&nbsp;&nbsp; get very very close to the speed of light on&nbsp; a GPU architecture with coup. If you want to&nbsp;&nbsp; go the extra mile and really optimize it beyond&nbsp; that cutless is available to you. The trade-off&nbsp;&nbsp; is that it's not going to be portable. So you&nbsp; kind of want to pick the smallest subset of your&nbsp;&nbsp; application to write in there as you possibly can.&nbsp; But in general, all of these things are routes to&nbsp;&nbsp; the same place, which is maximum performance&nbsp; on the just to give you a kind of quick feel&nbsp;&nbsp; for how this looks. I've got three algorithms&nbsp; here on the screen, or rather I should say&nbsp;&nbsp; three different variants of the same algorithm.&nbsp; On the left, I'm showing a simp matrix multiply&nbsp;&nbsp; written in Python and using Numba to compile it.&nbsp; And for anyone who's done matrix multiplication,&nbsp;&nbsp; this is like the worst possible implementation of&nbsp; matrix multiplication. It's not driving the GPU to&nbsp;&nbsp; maximal efficiency at all. But it is kind of long&nbsp; and it's fairly complex, right? We have to take&nbsp;&nbsp; the matrix multiplication operation. We have to&nbsp; divide it into individual threads, figure out how&nbsp;&nbsp; they communicate with one another, all that fun&nbsp; stuff. The two green boxes show the same matrix&nbsp;&nbsp; multiplication, making full use of the tensor&nbsp; cores, driving the GPU in top gear with the pedal&nbsp;&nbsp; all the way down in significantly less code. And&nbsp; that's the tile programming model in a nutshell.&nbsp;&nbsp; We're able to do very very high performance&nbsp; operations at an abstracted level of granularity&nbsp;&nbsp; in a really intuitive and productive programming&nbsp; world. And this is especially intuitive for anyone&nbsp;&nbsp; who's worked with numpy in in the Python space&nbsp; especially. We've really intentionally designed&nbsp;&nbsp; the API to look and feel very intuitive to&nbsp; people who have done array and tensor level&nbsp;&nbsp; programming before. The C++ version of it looks&nbsp; very similar. Obviously C++ has different syntax&nbsp;&nbsp; and different semantics, but by and large the way&nbsp; that you express tile operations, the way that you&nbsp;&nbsp; express these linear algebra operations in C++ is&nbsp; pretty much the same. We also have full support&nbsp;&nbsp; for profiling and debugging and so on in inside&nbsp; systems. And I'll just show you this very quickly.&nbsp;&nbsp; We have statistics available for tile kernels. You&nbsp; can kind of get good summaries of how the compiler&nbsp;&nbsp; is implementing things, how the GPU is running&nbsp; things and so on. We also in 13.1, I should have&nbsp;&nbsp; mentioned by the way that this is all coming&nbsp; in 13.1 in about a month. We have source code&nbsp;&nbsp; mappings of performance metrics for tile kernel.&nbsp; So you can see just as you can see for C++ today,&nbsp;&nbsp; you can see how things are breaking down, how&nbsp; individual instructions map to particular GPU&nbsp;&nbsp; operations and you basically get enough insight&nbsp; into how things are running in the system to&nbsp;&nbsp; be able to make intelligent decisions about&nbsp; further optimizations or just general coding.&nbsp;&nbsp; So the kind of TLDDR here is that all of this&nbsp; stuff is expressed in Insight the way that you&nbsp;&nbsp; would expect it to be expressed in Insight. And&nbsp; the goal is to make it as easy to use as possible.&nbsp;&nbsp; This is all fully part of the CUDA programming&nbsp; model. IEL kernels or CUDA kernels. You can do&nbsp;&nbsp; all the things with file kernels that you can&nbsp; do with regular kernels. You can put them in a&nbsp;&nbsp; graph. You can call them from other kernels. You&nbsp; know, anything you want to do with a CUDA kernel,&nbsp;&nbsp; you can do it with a tile kernels. The other kind&nbsp; of interesting correlary to this is that when we&nbsp;&nbsp; look at compilation stacks, compilation doesn't&nbsp; just happen at the lowest level anymore. As more&nbsp;&nbsp; and more frameworks are leaning into runtime code&nbsp; generation and code reuse, you get a lot of things&nbsp;&nbsp; like Jax or PyTorch Inductor or, you know, these&nbsp; different technologies that are generating code&nbsp;&nbsp; all the way down. And so you don't just have one&nbsp; compiler anymore. In a real application today,&nbsp;&nbsp; especially a deep learning application, it may not&nbsp; be uncommon to have five or 10 different compilers&nbsp;&nbsp; doing different things. And so the the big open&nbsp; question really is how do they all work together&nbsp;&nbsp; and how do we orchestrate all this the backend&nbsp; for tile programming is a model that we're calling&nbsp;&nbsp; CUDA tile IR and effectively it's a parallel&nbsp; programming path or a parallel compilation pathway&nbsp;&nbsp; what you had in SIMY up at the top here you can&nbsp; see all the kind of higher level exposures and&nbsp;&nbsp; expressions but as we get to tile we have a little&nbsp; bit of a bifurcation on the synt path. You're&nbsp;&nbsp; continuing down the way the compilation has always&nbsp; worked. You're going through NVCC and you're&nbsp;&nbsp; getting into the back end of NBCC which is NVBM&nbsp; or kind of a variant of LLVM that generates PTX.&nbsp;&nbsp; That PTX is compiled and so on. On the right, we&nbsp; have a a kind of conceptually equivalent parallel&nbsp;&nbsp; path where you have a tile application. That&nbsp; tile application goes down its own programming&nbsp;&nbsp; and compilation path. The end result of that is&nbsp; tile IR which is a kind of sister to PTX if you&nbsp;&nbsp; like and then that at runtime is compiled down&nbsp; to something actually runs on the GPU and the&nbsp;&nbsp; way that works is through MLIR. It's a series of&nbsp; lowering passes if you're familiar with how MLI&nbsp;&nbsp; works. We have a intermediate representation&nbsp; of what your algorithm is supposed to be and&nbsp;&nbsp; supposed to look like that's compiled at runtime&nbsp; down to the specific sequence of instructions that&nbsp;&nbsp; runs on the GPU. And of course this all kind of&nbsp; mixes and matches and plays nicely together. So&nbsp;&nbsp; every level of the stack is calling this. Many&nbsp; levels of the stack are generating many different&nbsp;&nbsp; things. And the end result is a binary object that&nbsp; is portable that runs at performance on the GPU.&nbsp;&nbsp; Really this part is probably only interesting&nbsp; if you're a compiler developer except it gives&nbsp;&nbsp; us a really nice party trick. If you look at the&nbsp; way that code generation is done in the ecosystem&nbsp;&nbsp; today and I'll use openAI Triton as an example&nbsp; here. If you write a Triton application or if&nbsp;&nbsp; you have a higher level DSL that generates Triton&nbsp; for instance you know PyTorch will generate Triton&nbsp;&nbsp; which generates other things and so on as you go&nbsp; down the stack the compilation path looks very&nbsp;&nbsp; similar. Triton generates something called Triton&nbsp; Tile IR which is an MLR dialect. It goes through&nbsp;&nbsp; a series of lowering passes and eventually winds&nbsp; up at the open source NVIDIA GPU back end. This is&nbsp;&nbsp; something where NVIDIA works with the OpenAI&nbsp; team. We contribute knowledge about the GPU&nbsp;&nbsp; architecture specific recipes for doing different&nbsp; operations on that GPU and so on. A lot of the&nbsp;&nbsp; the programming and integration and coding work&nbsp; comes from outside of this. But end of the day,&nbsp;&nbsp; you still wind up down here. you wind up in the&nbsp; backend LLVM stack and tile works very similarly,&nbsp;&nbsp; right? If you if you think about how this&nbsp; comes together, you can have that same PyTorch&nbsp;&nbsp; application generating coutile instead of Triton&nbsp; going through the own parallel compilation path&nbsp;&nbsp; and you know getting onto the GPU which sounds&nbsp; like we're we're looking at a situation where&nbsp;&nbsp; you're choosing one or the other but actually&nbsp; much much nicer than that because what you do&nbsp;&nbsp; instead because this is all ML and ML is kind of&nbsp; like playing with Lego for compiler people. We&nbsp;&nbsp; can take Triton Tile IR go through a dialect that&nbsp; translates it to NVIDIA's backend go through the&nbsp;&nbsp; Tile IR backend layers go through the compilation&nbsp; flow and get onto the GPU and this way NVIDIA can&nbsp;&nbsp; raise all the boats right if you're using Triton&nbsp; remain in Triton you can take advantage of the&nbsp;&nbsp; backend compiler you can get the performance&nbsp; associated with that you can get day zero support&nbsp;&nbsp; for new GPU architectures as they come out get the&nbsp; portability get your name on a walt wait that's&nbsp;&nbsp; different but you know you get all kinds of stuff&nbsp; as part of this for free effectively with back-end&nbsp;&nbsp; plugins to not just Triton but lots of different&nbsp; compilation frameworks and different tools. Our&nbsp;&nbsp; goal generally is to make the backend IR something&nbsp; that NVIDIA maintains and then the front end and&nbsp;&nbsp; the front-facing pieces of this can be consumed&nbsp; by really anyone to integrate into whatever you&nbsp;&nbsp; want. And so you can imagine all kinds of DSLs for&nbsp; different applications that are taking advantage&nbsp;&nbsp; of optimized linear algebra for GPUs. So I'm&nbsp; really excited about that. Beyond that, we've&nbsp;&nbsp; got a lot of things happening on the library side.&nbsp; FB64 and FP32 and larger data types are really&nbsp;&nbsp; really important for lots of different domains.&nbsp; Finance, high performance computing, you name it.&nbsp;&nbsp; GPUs are not historically super good at very&nbsp; very wide computations, at least not compared&nbsp;&nbsp; to the lower precision computations that&nbsp; we're doing on modern GPUs. Deep learning&nbsp;&nbsp; in particular is pushing tensor cores to lower&nbsp; and lower precisions as opposed to more bits.&nbsp;&nbsp; That's led to a lot of research in the space and&nbsp; researchers have identified techniques for doing&nbsp;&nbsp; higher precision operations with lower precision&nbsp; intermediate products recovering the full accuracy&nbsp;&nbsp; and getting higher performance at the same time.&nbsp; And you'll sometimes heard this referred to as the&nbsp;&nbsp; Ozaki technique. It goes by other names as well.&nbsp; There are a number of papers on this that have&nbsp;&nbsp; come out over the last couple of years. I have&nbsp; some links on the bottom of the slide here. But&nbsp;&nbsp; the TLDDR is that if you look at this chart using&nbsp; emulation techniques on modern GPUs and this is on&nbsp;&nbsp; GB200 where the numbers actually came from, you're&nbsp; seeing very very substantial speed ups versus just&nbsp;&nbsp; out of the box naive FP32, FB64 implementations&nbsp; and even on lower precisions as well by using&nbsp;&nbsp; the same kind of decomposition techniques. And&nbsp; so this is something that we're really leaning&nbsp;&nbsp; into at the programming model layer both for the&nbsp; math libraries where you'll see this available&nbsp;&nbsp; as opt-in right now and we're probably going to&nbsp; transition to it as default pretty soon in the&nbsp;&nbsp; future as well as just for general SM programming.&nbsp; you can imagine Ozaki kind of data types in CCTL&nbsp;&nbsp; and things like this where you can take advantage&nbsp; of emulation techniques without going through the&nbsp;&nbsp; tensor cores as well. It was released for the&nbsp; first time on SP or excuse me FP32 back in 129&nbsp;&nbsp; which is in May of this year. Double precision&nbsp; came out with CUDA 13 update 2 and like I said&nbsp;&nbsp; it's optin right now. we do see pretty substantial&nbsp; benefit from this though. So once we've given a&nbsp;&nbsp; little bit of soap time and kind of let it sink&nbsp; in a little bit future releases are going to&nbsp;&nbsp; invert this. It'll become optin by default and&nbsp; you can opt out of it if you so choose for any&nbsp;&nbsp; reason. Moving on to the compilers. I've talked&nbsp; a lot about runtime compilation and runtime code&nbsp;&nbsp; generation. How are we going to generate&nbsp; all this code? How are we going to runtime&nbsp;&nbsp; u compile and finalize the GPU architectures? And&nbsp; ultimately that kind of shifts the problem around&nbsp;&nbsp; a little bit, right? We're not giving you these&nbsp; really really massive libraries of pre-ompiled&nbsp;&nbsp; kernels anymore, but now we're runtime compiling&nbsp; something that's going to run on the GPU. And&nbsp;&nbsp; so we're putting a lot of effort into how we can&nbsp; make these things faster and more efficient. We've&nbsp;&nbsp; added a tool called compile time advisor that&nbsp; analyzes your compilation flow and you can see a&nbsp;&nbsp; nice flame chart of where the compiler is spending&nbsp; its time. Gives you some feedback about how and&nbsp;&nbsp; where and why you can optimize things for faster&nbsp; compilation. We also give you more knobs you can&nbsp;&nbsp; tune to get faster compile time. So we actually&nbsp; have compile time focused optimizations as opposed&nbsp;&nbsp; to code efficiency optim op imp implementations.&nbsp; this is the fast compile flag and you can sort&nbsp;&nbsp; of choose different set points along this curve.&nbsp; We've also added better support and we'll continue&nbsp;&nbsp; to improve support for runtime compilation caching&nbsp; of intermediate products. Right? You were always&nbsp;&nbsp; able to do this with just general JIT compilation.&nbsp; Now we've added caching to NVRTC as well.&nbsp;&nbsp; We're also continuing to improve and expand on&nbsp; the insight tools and my colleague Jackson is&nbsp;&nbsp; around here somewhere from the insight team.&nbsp; He would love to talk to you all day about&nbsp;&nbsp; developer tools and what's going on. But in&nbsp; addition to the stuff we're doing for tile,&nbsp;&nbsp; we're giving you much better Python support&nbsp; generally with the developer tools. And then&nbsp;&nbsp; this is a really exciting one for us. We're really&nbsp; leaning into LLM based assistance here because if&nbsp;&nbsp; you've ever used something like Insight Compute,&nbsp; you've probably seen that it gives you just page&nbsp;&nbsp; after page after page of statistics. And unless&nbsp; you've been doing GPU programming for a long time,&nbsp;&nbsp; you kind of look at it and go like, I don't&nbsp; know what any of this means, right? I've got&nbsp;&nbsp; all these numbers, but how do I actually fix my&nbsp; kernel? Well, our LLM assistant here is there&nbsp;&nbsp; to do exactly that. It'll look at your results.&nbsp; You can ask questions about it. You can ask it,&nbsp;&nbsp; how do I better interpret things and how can&nbsp; I just generally get better performance out of&nbsp;&nbsp; the kernels based on the feedback that I've got&nbsp; right now. Calling back to the beginning of the&nbsp;&nbsp; presentation, I mentioned that we're unifying the&nbsp; compilation pathway between the data center and&nbsp;&nbsp; the embedded devices. And this is a really&nbsp; exciting one for us because if you've ever&nbsp;&nbsp; done embedded development for NVIDIA, there&nbsp; was always a disconnect in the Delta. The&nbsp;&nbsp; CUDA version might be slightly behind. You were&nbsp; missing some functionality. There was a different&nbsp;&nbsp; driver with different models and you know, it&nbsp; just kind of got a little bit weird. Even in&nbsp;&nbsp; the cases where it wasn't, if you were trying&nbsp; to write one application and run it anywhere,&nbsp;&nbsp; we just didn't give you the ability to do that.&nbsp; If you were building for a ARM data center system,&nbsp;&nbsp; you had a different set of libraries, you had a&nbsp; different set of compilers, you had a different&nbsp;&nbsp; set of everything. Even though the compilers&nbsp; for the embedded space were largely the same,&nbsp;&nbsp; it's the same CPU architecture. They had the same&nbsp; instructions for the most part, you know, there's&nbsp;&nbsp; really not a substantial technical underlying&nbsp; reason that they should be different. And so, long&nbsp;&nbsp; story short is that they're not. And that gives us&nbsp; a lot of really great stuff. I'm not going to go&nbsp;&nbsp; into detail on the hardware architecture. We have&nbsp; other sessions here at GTC where you can learn&nbsp;&nbsp; more about this and find out more about what's&nbsp; going on in the embedded space and with some&nbsp;&nbsp; of the embedded products. But the key takeaway for&nbsp; me is that it's always been really kind of painful&nbsp;&nbsp; for me that CUDA was different in the embedded&nbsp; space and now it's not. And so with 13, you get&nbsp;&nbsp; all kinds of things in the embedded space that&nbsp; you never had available before. In particular,&nbsp;&nbsp; scheduling assistance. And I talked about how&nbsp; important it is to be able to process and capture&nbsp;&nbsp; and run on real world data without losing it.&nbsp; Where is that more true than in a robot? And so&nbsp;&nbsp; we give you all the scheduling tools that exist&nbsp; in the data center space on embedded. Now you&nbsp;&nbsp; have multi- instance GPU or MIG that allows you&nbsp; to subdivide your GPU into physically discrete&nbsp;&nbsp; instances that don't share memory bandwidth.&nbsp; They can't step on one another. You get much&nbsp;&nbsp; better safety that way and more determinism,&nbsp; better QoS, all these good things. We give you&nbsp;&nbsp; multipprocess server, MPS, which allows you to put&nbsp; more applications onto the GPU at the same time,&nbsp;&nbsp; get better resources utilized, really just&nbsp; squeeze more juice out of the thing. And then a&nbsp;&nbsp; new technology that you may have heard a little&nbsp; bit about, we've been talking about this for&nbsp;&nbsp; a little while now, called green context. Green&nbsp; context takes the conceptual GPU sharing of MPS,&nbsp;&nbsp; but it does it within a particular CUDA context.&nbsp; It squeezes things down, gives you better spatial&nbsp;&nbsp; partitioning on the GPU. And just in general,&nbsp; this is where the really cool box of tricks&nbsp;&nbsp; is for when you're trying to squeeze that last&nbsp; little bit of performance out of the GPU or where&nbsp;&nbsp; you're trying to hit those really tight latency&nbsp; targets. But the important thing is that it's now&nbsp;&nbsp; one platform everywhere. We have one GPU driver&nbsp; that spans across the entire suite of products. It&nbsp;&nbsp; gives you a productivity boost. You can build on&nbsp; Spark, deploy on a robot, deploy on a supercomput.&nbsp;&nbsp; It doesn't really matter. Just build wherever&nbsp; you want to. Run it wherever you want to. It's&nbsp;&nbsp; the same now. And for me, that's that's very very&nbsp; exciting. Confidential computing, I'll talk about&nbsp;&nbsp; it very briefly. confidential computing is not new&nbsp; for us, but we do continue to improve and expand&nbsp;&nbsp; upon it. Basically what confidential computing&nbsp; is, if you're not familiar with it or if you&nbsp;&nbsp; haven't heard about it, it's a system technique&nbsp; for protecting your data while it's in use. There&nbsp;&nbsp; are really good techniques for protecting data&nbsp; while it's in transit across the network. There&nbsp;&nbsp; are really good techniques for protecting the data&nbsp; while it's sitting on your hard drive. When it's&nbsp;&nbsp; in flight to the GPU and is being operated&nbsp; on within the GPU, how do we protect it? How&nbsp;&nbsp; do we make sure that it's not intercepted?&nbsp; How do we make sure that you can trust the&nbsp;&nbsp; environment that you're running in? And that's&nbsp; effectively what confidential computing is for.&nbsp;&nbsp; It's a virtualization based solution. You have a&nbsp; trusted execution environment. You can see kind of&nbsp;&nbsp; the the TLDDR of it just from the diagram here.&nbsp; The big news for CUDA 13 is that it's available&nbsp;&nbsp; for Blackwell. We have support for HDXP200. We&nbsp; have support for RTX Pro 6000. It's single GPU&nbsp;&nbsp; pass through per for now. So it's one GPU per&nbsp; trusted execution environment. that's expanding&nbsp;&nbsp; to multiGPU pass through and just a broader set&nbsp; of techniques and technologies in the future.&nbsp;&nbsp; This is really important for inference especially&nbsp; it's not the full AGPU but many times the full&nbsp;&nbsp; AGPU or you know the full cluster scale system is&nbsp; not required for inference workloads especially&nbsp;&nbsp; edge inference workloads and so this is this is&nbsp; pretty big finally I mentioned Python back at&nbsp;&nbsp; the beginning we've been putting a lot of effort&nbsp; into optimizing and building Python and a lot of&nbsp;&nbsp; this is kind of along two axes the first axis is&nbsp; how do we expose ose all the richness of the CUDA&nbsp;&nbsp; ecosystem to Python without having you write C++&nbsp; and Python. Nobody wants to do that. It's kind&nbsp;&nbsp; of counterintuitive or counterproductive. So what&nbsp; we're creating are multiple layers of exposure for&nbsp;&nbsp; the CUDA runtime for CUDA accelerated algorithms.&nbsp; You know, you can think about things like doing a&nbsp;&nbsp; reduction or a scan or element wise operations,&nbsp; things like this in CCCL. You've always had&nbsp;&nbsp; optimized libraries on the C++ side. These layers&nbsp; bring you the richness of the driver API, the&nbsp;&nbsp; richness of the runtime API, and the accelerated&nbsp; algorithm libraries to Python and give you a&nbsp;&nbsp; really nice way to program in Python directly. I'm&nbsp; not going to really go into depth about what all&nbsp;&nbsp; these things are. There are other sessions here at&nbsp; GTC I'll show you in a second where you can learn&nbsp;&nbsp; more about these, but if you go to the accelerated&nbsp; computing hub, if you go to the documentation,&nbsp;&nbsp; if you go to our GitHub, you can find out all&nbsp; kinds of information about how these things work.&nbsp;&nbsp; A really big pain point for people too has been&nbsp; just Python packaging in general. there are lots&nbsp;&nbsp; of inefficiencies in the way that we were doing&nbsp; things. It was always kind of painful to install&nbsp;&nbsp; and do version management and so on. And some&nbsp; of that's really low hanging fruit to clean&nbsp;&nbsp; up. And so the long story short is that we are&nbsp; cleaning it up. We've simplified the wheel name&nbsp;&nbsp; substantially. We took off the CUDA version tags&nbsp; and now you just have one package that's versioned&nbsp;&nbsp; the way that the package managers expect them&nbsp; to be versioned effectively. We've also removed&nbsp;&nbsp; a lot of redundancy with the packaging in terms&nbsp; of removing and refactoring how we're installing&nbsp;&nbsp; things. So the shared files are actually shared.&nbsp; Instead of having a a single copy for each wheel&nbsp;&nbsp; that you install, now they're they're shared&nbsp; as they should be. And we have a really nice&nbsp;&nbsp; and easy to use meta package that you can use&nbsp; with pip where you can just pip install a CUDA&nbsp;&nbsp; toolkit and you can select specific libraries,&nbsp; specific dependencies, a subset of the libraries,&nbsp;&nbsp; you can just request particular versions. And so&nbsp; our hope is that this makes installing things like&nbsp;&nbsp; PyTorch or other very complex applications much&nbsp; easier to work with. We're starting to roll this&nbsp;&nbsp; out and we'd love any feedback that you have as&nbsp; you start using this just to help us continue&nbsp;&nbsp; to refine this and continue to make this easier&nbsp; to use. So with that, I talked fast. I apologize&nbsp;&nbsp; for that. I did wind up with a couple minutes at&nbsp; the end though for questions. And we have other&nbsp;&nbsp; sessions on CUDA at GDC here. In particular,&nbsp; we have a session on coupon numeric, which is&nbsp;&nbsp; our large scale cluster scaling out solution&nbsp; for Python. We have another session on scaling&nbsp;&nbsp; Python analytics with coupon numeric and legate&nbsp; boost. We have training labs. We have connect&nbsp;&nbsp; with the experts. And a number of my colleagues&nbsp; are walking around both on the demo floor and&nbsp;&nbsp; you know just grab anybody with a green badge&nbsp; and ask them about CUDA and we'll we'll happily&nbsp;&nbsp; talk to you about it all day. So with that, I do&nbsp; think I have a couple of minutes for questions.&nbsp;&nbsp; Thank you for the talk. I'm very excited&nbsp; about cash spill over into shared memory. So,&nbsp;&nbsp; thanks for that. my question is complex data types&nbsp; in nickel. Is there any way we could get complex&nbsp;&nbsp; data types in the nickel library? there's nothing&nbsp; that inherently prevents you from having complex&nbsp;&nbsp; data types in the nickel library. So that's I&nbsp; that's not on the road map right now. off the&nbsp;&nbsp; top of my head, but I can certainly take that&nbsp; back and we can work something in there. Okay,&nbsp;&nbsp; thank you. Thanks for the presentation. I work&nbsp; with Jetsons that don't have FP64 units. So,&nbsp;&nbsp; I'm very interested in your extended floatingpoint&nbsp; capabilities. but the jetpacks versions always&nbsp;&nbsp; seem to be several versions behind the latest&nbsp; CUDA compilation. So, does will this thing work&nbsp;&nbsp; on Jetson AGX Orange? Is there a jetack for that&nbsp; or will the full 13.1 be on jets and Thors? Yeah,&nbsp;&nbsp; that's a good question. I didn't get into a&nbsp; lot of the details about this. Orin and Thor&nbsp;&nbsp; have fundamentally different architectures and so&nbsp; there are different drivers. In terms of library&nbsp;&nbsp; support and enablement, everything that we're&nbsp; talking about will be enabled on Orin. The trick&nbsp;&nbsp; is that when I talk about unifying the driver&nbsp; architecture, that's for Thor and newer. so some&nbsp;&nbsp; of the things don't don't go back to Orin and&nbsp; Orurin will still have a little bit of a delay,&nbsp;&nbsp; but it's kind of one of my pet projects to&nbsp; make that delay as minimal as possible. Okay.&nbsp;&nbsp; Thank you. For new CUDA C++ programmers, the&nbsp; tutorials mentioned that you've been working on&nbsp;&nbsp; for a year and you're updating for 13.1 with tile&nbsp; specifically. So that's interesting portability.&nbsp;&nbsp; How do we find those? There are two places you can&nbsp; find them. the first is the the programming guide.&nbsp;&nbsp; If you just search for the Google, excuse me,&nbsp; if you do a Google search or something for the&nbsp;&nbsp; CUDA programming guide, that's being totally&nbsp; rewritten from the ground up. And so that's&nbsp;&nbsp; launching alongside 131. We also have two places&nbsp; you can look for CUDA learning material. One is a&nbsp;&nbsp; GitHub repository called the accelerated computing&nbsp; hub. Okay, that's got a lot of the Python stuff&nbsp;&nbsp; in it. We're growing it out and that's really&nbsp; where you'd go to find like tutorial material,&nbsp;&nbsp; right? Or how do I learn and just how do I get&nbsp; my my bearings? The CUDA samples repository is&nbsp;&nbsp; equivalently being built up. that's much more&nbsp; reference material. How do I use this API? How to&nbsp;&nbsp; use that library? That sort of thing. So, between&nbsp; those three places, that should probably cover&nbsp;&nbsp; you. And if there's anything that would help that&nbsp; we don't have, let me know and I' I'll I'd love to&nbsp;&nbsp; build it. All right. Thank you. Quick question.&nbsp; You mentioned in the beginning talking about the&nbsp;&nbsp; programming languages that you're looking into C&nbsp; as well. How deep will that go in the stack? Is&nbsp;&nbsp; it really for the SDK usage or will that go beyond&nbsp; that? Yeah, it's a good question. So, in general,&nbsp;&nbsp; one of the things that I tend to believe, and&nbsp; you can all correct me if I'm wrong about this,&nbsp;&nbsp; I feel like if we're going to enable a programming&nbsp; language for CUDA, you should be able to do&nbsp;&nbsp; everything in that programming language. You&nbsp; should be able to write a CUDA. You should,&nbsp;&nbsp; excuse me, you should be able to write a kernel.&nbsp; You should have full access to all the libraries.&nbsp;&nbsp; You should have full access to, you know, things&nbsp; like Cub and Thrust and so on. It should just be&nbsp;&nbsp; everything in the box expressed in that ecosystem.&nbsp; So, we wouldn't do something peacemail. If we're&nbsp;&nbsp; going to lean into a language, we're going to&nbsp; really lean into it and fully enable everything.&nbsp;&nbsp; There may be cases where that gets a little&nbsp; challenging at first. And I'll I'll use Rust&nbsp;&nbsp; as an example here. Rust has as part of its ethos&nbsp; a real focus on memory safety. That memory safety&nbsp;&nbsp; and the memory model that Rust espouses doesn't&nbsp; really work at all with shared memory on a GPU.&nbsp;&nbsp; And so we're looking at things like, okay, if we&nbsp; did Rust, exposing tile programming in Rust is a&nbsp;&nbsp; is a relatively straightforward thing. And in&nbsp; fact, we'll have tile programming in Rust very&nbsp;&nbsp; soon after launch. If I look at how we're going&nbsp; to do GPU side, general programming in Rust,&nbsp;&nbsp; do you want to write everything as an unsafe&nbsp; region? Do you need GPU safe or memory safe&nbsp;&nbsp; exposures of things on the GPU in addition to&nbsp; unsafe? how does that play out exactly? So you&nbsp;&nbsp; may have situations with different languages where&nbsp; for kind of rubber meets the road reasons you get&nbsp;&nbsp; things exposed at different rates, but our our&nbsp; goal wouldn't be to just do a little bit. Like&nbsp;&nbsp; if we're going to do Rust, we want to do all of&nbsp; Rust. If we're going to do C#, we're going to&nbsp;&nbsp; do all of C# and so on. All right, I'm being told&nbsp; that they're about to get the hook. So anyone who&nbsp;&nbsp; has any more questions for me, I'll be out in the&nbsp; hallway here for a few more minutes or otherwise&nbsp;&nbsp; I'll look forward to talking to you on the floor.&nbsp; But thank you for coming. We really appreciate it.
