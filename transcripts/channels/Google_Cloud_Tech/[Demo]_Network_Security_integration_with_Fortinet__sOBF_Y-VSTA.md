# [Demo] Network Security integration with Fortinet

- 영상 링크: https://www.youtube.com/watch?v=sOBF_Y-VSTA
- 채널: Google Cloud Tech
- 업로드일: 2026-01-29
- 자막 언어: ko
- 단어 수: 약 1168개

---

## 스크립트

여러분, 안녕하세요. 오늘 영상에서는 네트워크 보안 통합에 대한 자세하고 심층적인 분석을 이어가겠습니다. 이번에는 포리넷과 함께합니다. 제 이름은 하리카이고, 오늘 저와 함께 해주신 분은 포리넷 팀의 프라빈입니다. 진행 과정에서 Fortunate 방화벽, 차세대 방화벽 엔터프라이즈, GenF 터널과 같은 네트워크 가상 어플라이언스에 대한 이해가 도움이 될 것입니다. Foret 방화벽과 같은 타사 보안 어플라이언스를 사용하는 기업 고객은 기존 애플리케이션 네트워크 아키텍처를 방해하지 않고 이러한 장치를 투명하게 통합하는 데 상당한 어려움에 직면합니다. 이러한 고객들에게 가장 중요한 것은 네트워크 경로 에 네트워크 보안을 투명하게 통합하는 능력입니다 . 기존 구축 아키텍처는 트래픽 흐름을 위해 광범위한 네트워크 주소 변환을 필요로 합니다 . 이 필수 NAT 프로세스는 특정 경우에 애플리케이션 호환성을 깨뜨려 타사 보안 제품을 적용하기 어렵게 만듭니다 . 고객은 애플리케이션 배포에 네트워크 변경을 강요받지 않고 네트워크 보안을 통합하기를 원합니다 . Google Cloud 가상 프라이빗 클라우드 트래픽에 대한 포괄적인 가시성을 확보하고 워크로드 보안을 강화하려면 독립 소프트웨어 공급업체의 특수 네트워크 어플라이언스를 통합해야 합니다. 심층 패킷 검사 기능을 포함하는 이러한 전용 솔루션은 네트워크 패킷에 대한 완벽한 분석을 가능하게 합니다 . 표준 헤더뿐만 아니라 실제 페이로드까지 검사합니다. 이러한 장비를 와이어 연결 방식으로 설치하면 현재 네트워크 라우팅을 변경하지 않고도 탁월한 네트워크 분석 기능과 고급 네트워크 보안 기능을 즉시 활용할 수 있습니다 . 또한 제네바 캡슐화를 사용하여 원래 출발지 및 목적지 주소를 변경하지 않고 트래픽을 어플라이언스로 안전하게 전송합니다. 타사 방화벽을 설치하면 심층 패킷 검사를 수행하고 무단 액세스를 실시간으로 차단할 수 있습니다. 어플라이언스는 여러 VPC, 프로젝트, 테넌트의 트래픽을 검사할 수 있어 확장성에 도움이 됩니다. 이러한 네트워크 보안 통합 검사는 기본적으로 두 가지 방식으로 분류됩니다. 첫 번째는 대역 외 방식으로 트래픽 미러링에 사용되고, 두 번째는 패킷을 직접 가로채서 처리하는 데 사용됩니다. 네트워크 보안 통합 솔루션은 생산자-소비자 모델을 기반으로 트래픽 검사를 처리합니다. 프로듀서 VPC 네트워크는 타사 네트워크 어플라이언스가 있는 서비스 제공업체이며, 전체 설정을 네트워크 서비스 프로듀서 배포라고 합니다. 이는 내부 통과 네트워크 로드 밸런서의 진입점 역할을 하는 내부 포워딩 규칙을 사용합니다. 로드 밸런서는 배포된 보안 어플라이언스가 포함된 관리형 또는 비관리형 인스턴스 그룹을 통해 백엔드로 들어오는 트래픽을 분산합니다. 이 경우에는 포레 방화벽입니다. 네트워크 서비스 배포를 생성할 때, 연결된 전달 규칙의 특정 이름을 참조해야 하며, 네트워크 보안 통합은 이 이름을 사용하여 검사 대상 트래픽을 프로듀서 배포로 라우팅합니다. 컨슈머 VPC 네트워크는 Google Cloud 환경에서 실제 워크로드를 호스팅하는 곳입니다 . 가상 머신이든 GKE 노드든 무엇이든 될 수 있습니다. 방화벽 정책 내에서 규칙을 설정함으로써 어떤 워크로드 트래픽을 검사 대상으로 보낼지 정확하게 제어할 수 있습니다 . 이러한 정책 규칙을 사용하면 출발지/도착지 IP 주소 또는 범위, 네트워크 태그 또는 서비스 계정을 포함한 다양한 속성을 사용하여 트래픽을 지정할 수 있습니다. 대역 외 서비스는 사전 정의된 규칙에 따라 미러링을 지원하며, 패킷 미러링은 방화벽 정책의 미러링 규칙에 지정된 필터링 기준에 따라 네트워크 트래픽을 복제하여 워크로드 네트워크 트래픽을 대규모로 분석할 수 있도록 지원합니다 . 이 미러링 트래픽은 심층 패킷 검사를 위해 타사 가상 어플라이언스 배포 환경으로 전송됩니다 . 반면, 인밴드 통합을 사용하면 타사 방화벽을 배치하여 심층 패킷 검사, 패킷 가로채기 및 무단 액세스 차단을 실시간으로 수행할 수 있습니다. 앞서 언급했듯이, 여기서의 해결책은 서비스 중심적 접근 방식입니다. 즉, 생산자는 확장 가능한 타사 네트워크 어플라이언스 세트를 인터셉트 배포로 게시할 수 있고, 소비자는 차세대 방화벽 정책을 사용하여 트래픽을 검사를 위해 로컬 인터셉트 엔드포인트로 정확하게 리디렉션할 수 있습니다. 주요 이점으로는 방화벽과 침입 탐지 시스템을 트래픽 경로에 직접 배치하여 실시간으로 검사할 수 있다는 점이 있습니다. Geneive 헤더를 사용하여 라우팅 정책을 변경하지 않고 트래픽을 투명하게 리디렉션하면 원래 패킷 정보가 보존됩니다 . 어플라이언스는 여러 VPC, 프로젝트 및 테넌트의 트래픽을 검사할 수 있어 확장성을 향상시킵니다. 이 워크플로는 소비자가 생산자 보안 서비스의 심층 패킷 검사를 위해 트래픽을 리디렉션하는 방법을 설명합니다 . 소비자는 특정 규칙을 사용하여 네트워크 방화벽 정책을 구성합니다 . 이 규칙들은 일치하는 트래픽을 프로듀서에게 리디렉션하도록 구성되어 있습니다 . 소비자 VPC 내의 패킷이 지정된 방화벽 정책 규칙과 일치하면 리디렉션 프로세스가 시작됩니다. 패킷은 원래 출발지 및 목적지 IP 주소를 유지하는 데이터를 보존함으로써 제네바 캡슐화를 사용하여 즉시 캡슐화됩니다. 이는 라우팅을 위해 프로듀서 서비스에 대한 메타데이터를 추가합니다 . 방화벽 정책에 연결된 보안 프로필을 기반으로 캡슐화된 패킷은 소비자의 전용 가로채기 엔드포인트 그룹으로 전송됩니다. 소비자 측의 인터셉트 엔드포인트 그룹은 캡슐화된 패킷을 생산자 측 VPC 네트워크의 해당 인터셉트 배포 그룹으로 전송합니다 . 생산자 네트워크의 내부 패스스루 네트워크 로드 밸런서는 캡슐화된 패킷을 수신하여 검사 장비를 호스팅하는 백엔드 가상 머신으로 분배합니다. VM의 어플라이언스는 딥 패킷 검사를 수행합니다. 완료되면 장치는 생산자 가로채기 배포 그룹을 통해 패킷을 소비자의 네트워크로 다시 보냅니다. 소비자 네트워크의 가로채기 엔드포인트 그룹은 캡슐화된 패킷을 수신하고 Genev 헤더를 제거하여 패킷을 효과적으로 디캡슐화하고 원래 형태로 복원합니다 . 그러면 원래 패킷은 소비자 VPC 내의 원래 목적지로 전달됩니다. 오늘은 SSL 복호화가 네트워크 보안 통합을 통해 어떻게 작동하여 애플리케이션에 대한 더욱 세밀한 제어를 가능하게 하는지에 대해 논의하겠습니다. 프라빈 씨, Forinate 방화벽을 이용한 SSL 복호화 데모를 보여주실 수 있을까요 ? 고마워, 하라. 이 데모에서는 인 밴드 배포 모델에서 Forigate가 Google Cloud의 네트워크 보안 통합과 어떻게 연동되는지 살펴보겠습니다 . Forinet이 제공하는 다양한 보안 기능 중에서 이 데모는 주로 애플리케이션 검사, 특히 TLS로 암호화된 트래픽 검사에 중점을 둡니다. 전체 솔루션은 Terraform을 사용하여 배포되므로 일관되고 반복 가능한 배포 모델을 구현할 수 있습니다. 호라가 앞서 언급했듯이, 아키텍처는 생산자 측과 소비자 측 모두의 리소스로 구성됩니다. 소비자 측에서는 엔드포인트 그룹 및 엔드포인트 그룹 연관 관계와 같은 엔티티를 정의합니다. 여기에 표시된 것처럼 엔드포인트 그룹과 엔드포인트 그룹 연결은 함께 작동하여 트래픽 소스를 식별하고 보안 검사를 위해 트래픽이 올바르게 리디렉션되도록 합니다. 프로듀서 측에서는 게이트용 로드 밸런서와 몇 가지 네트워킹 구성 요소로 이루어진 인터셉트 배포와 같은 엔티티가 있습니다. 여러 개의 차단 시스템을 결합하여 확장성과 고가용성을 제공하는 차단 시스템 그룹을 구성할 수 있습니다 . 여기에 표시된 것처럼, 감청 배포 그룹은 감청 배포를 통합하여 중앙 집중식으로 안정적인 트래픽 검사를 가능하게 합니다. 다음으로 Google Cloud 콘솔에 로그인하여 프로듀서 측의 주요 요소를 살펴보겠습니다. 구체적으로, 여러 지역에 걸쳐 백엔드 인스턴스 역할을 하는 48개의 인스턴스를 사용하는 Jenny 프로토콜인 6081 포트에서 수신 대기하도록 구성된 로드 밸런서를 살펴보겠습니다 . 다음으로, Google Cloud Platform에 구성된 방화벽 정책을 살펴보고, 특히 수신 및 송신 트래픽 모두에 대한 검사를 보장하는 규칙에 초점을 맞추겠습니다 . 진입 규칙과 진출 규칙 모두 이전에 생성한 동일한 보안 프로필 그룹을 참조합니다. 이는 양방향 트래픽이 Forigate 와 NSI 통합 시스템에 의해 검사된다는 것을 확인시켜 줍니다. 이제 Forigate 방화벽에 로그인하는 것으로 시작하겠습니다 . 방화벽의 Geneive 구성 설정을 살펴보겠습니다. Geneive는 1번 포트에 구성되어 있습니다. 여기에는 제네바 인터페이스로 들어오고 나가는 트래픽을 검사하도록 구성된 방화벽 정책이 있습니다 . 이 정책에는 애플리케이션 제어, 웹 필터링 및 SSL 검사를 포함한 여러 보안 서비스가 활성화되어 있습니다. 다음으로, 이 방화벽 정책에 연결된 애플리케이션 제어 프로필을 자세히 살펴보겠습니다. 보시는 바와 같이, 해당 프로필에는 Forigate의 상세 애플리케이션 시그니처를 사용하여 스트리밍 및 다운로드 활동과 관련된 특정 YouTube 시그니처를 정확하게 감지하고 차단하는 애플리케이션 필터가 포함되어 있습니다 . SSL 검사는 TLS로 암호화된 트래픽을 복호화하고 검사하여 애플리케이션 수준에서 보안 정책을 적용할 수 있도록 합니다. 이 프로필은 방화벽 정책에서 참조됩니다 . 자, 이제 윈도우 클라이언트로 전환해서 몇몇 웹사이트에 접속해 보겠습니다. 먼저 google.com으로 이동해 보겠습니다 . 보시다시피, 구글 접속은 허용되어 있으며 정상적으로 작동합니다 . 자, 이제 유튜브닷컴에 접속해 보겠습니다. YouTube.com 접속은 잘 됩니다. 하지만 동영상을 재생하려고 하면 트래픽이 차단됩니다. 이 트래픽은 앞서 살펴본 4gate 방화벽의 애플리케이션 제어 정책에 의해 차단되고 있습니다. 마지막으로 방화벽을 살펴보겠습니다. [목을 가다듬으며] 로그를 보면 특정 YouTube 재생 기능에 대한 접근이 차단되었으며, 해당 이벤트는 애플리케이션 제어 기능과 관련된 것으로 명확하게 나타납니다. 이 데모는 Forigate의 네트워크 보안 통합이 클라이언트 측 변경 없이도 인바운드 및 아웃바운드 트래픽 모두에 대해 원활하고 투명한 검사 및 적용을 제공하는 방법을 보여줍니다 . 매우 감사합니다 . 대역 내 네트워크 보안 통합에 대한 자세한 내용은 설명란의 문서 링크를 참조하십시오 . 시청해 주셔서 정말 감사합니다 . [음악]
