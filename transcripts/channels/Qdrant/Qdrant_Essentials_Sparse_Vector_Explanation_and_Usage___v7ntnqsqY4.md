# Qdrant Essentials | Sparse Vector Explanation and Usage

- 영상 링크: https://www.youtube.com/watch?v=_v7ntnqsqY4
- 채널: Qdrant
- 업로드일: 2025-10-22
- 자막 언어: ko
- 단어 수: 약 1443개

---

## 스크립트

안녕하세요, 저는 쿼드런트 개발자 옹호팀의 제니입니다. 저희 팀 도 녹음 환경이 그다지 좋지는 않지만 , 너그러이 이해해 주시길 바랍니다 . 벡터 검색에 대한 첫 번째 영상에서 기억하시겠지만, Quadrant에서는 밀집 벡터가 주요 역할을 하지만 다양한 종류의 벡터를 사용할 수 있습니다 . 오늘은 희소 벡터와 그 활용법에 대해 이야기해 보겠습니다 . 희소 벡터는 몇몇 차원을 제외한 나머지 차원이 모두 0으로 채워진 고차원 벡터입니다. 희소 벡터의 각 차원은 특정 객체를 나타내고, 그 값은 이 특정 희소 표현에서 해당 객체의 역할을 나타냅니다 . 예를 들어 영화 추천 시스템을 생각해 보세요. 이 시스템에서 각 희소 벡터는 특정 사용자의 영화에 대한 의견 , 희소 벡터의 각 차원 , 영화, 그리고 이 차원의 각 값을 설명할 수 있습니다 . 특정 사용자가 이 특정 영화에 대해 남긴 평점입니다. 두 개의 희소 벡터 또는 두 개의 희소 표현을 비교할 때는 유사한 객체를 비교하는 것이 타당합니다. 예를 들어, 같은 영화에 대한 평점을 비교할 때, 우리는 그에 맞는 완벽한 측정 기준을 가지고 있습니다. 둘째 날에 우리는 내적에 대해 이야기했었는데 , 내적은 두 벡터를 비교할 때 해당 차원을 곱한 다음 그 값을 모두 더하는 측정 방법입니다. 음, 아주 잘 어울리는 것 같네요. 자, 이제 현실적으로 생각해 봅시다. 0과 0을 비교하는 것이 의미가 있을까요? 이렇게 많은 영감이 있다는 점을 고려할 때, 이 모든 0을 저장하는 것이 과연 의미가 있을까요? 글쎄요, 확장성에 최적화된 시스템에서는 확실히 그렇지 않겠죠 . 이 0들을 없애버립시다. 그럼 이제 객체와 차원을 나열하고 각각에 인덱스를 부여해 보겠습니다. 이제 우리는 0이 아닌 값을 가진 객체의 인덱스와 해당 값만 저장할 수 있습니다. 이제 문제는 이러한 표현들을 어떻게 비교하느냐입니다. 왜냐하면 들어오는 벡터를 이러한 모든 표현과 비교할 때마다 일치하는 인덱스를 모두 찾아야 하고, 그것도 빠르게 해야 하기 때문입니다. 네, 데이터베이스 내에서 희소 벡터 요소를 구성하는 데이터 구조 덕분에 가능합니다 . 둘째 날 영상에서 보셨겠지만, HNSW라는 것이 있는데, 이는 밀집 벡터 지수입니다. 희소 벡터도 역 인덱스라고 불리는 고유한 인덱스를 가지고 있습니다. 그리고 걱정하지 마세요. HMSSW보다 훨씬 간단합니다. 여기서는 정확히 일치하는 항목만 다루기 때문입니다. 역 지수를 예시를 통해 살펴보겠습니다. 희소 벡터가 세 개 있다고 가정해 봅시다. 각각의 객체는 ID, 0이 아닌 차원의 객체 인덱스, 그리고 해당 객체의 값과 가중치를 가지고 있습니다 . 역인덱스는 각 객체를 해당 객체의 영향이 알려지지 않은 모든 희소 표현에 매핑합니다. 그러니까 기본적으로 이 인덱스는 표현을 반전시킵니다 . 이는 0이 아닌 모든 희소 벡터 차원을 희소 벡터 자체로 매핑합니다. 이제 비교를 위한 새로운 희소 벡터가 들어왔습니다. 그리고 우리는 역인덱스를 즉시 살펴보면 6번째와 7번째 차원의 값이 모두 0이 아닌 벡터들을 모두 찾아낼 수 있습니다 . 이제 벡터 간의 유사도 점수를 계산하려면 각 벡터 ID 1, ID 2, ID 3에 대해 0이 아닌 일치하는 차원의 값을 곱한 다음 내적처럼 모두 더하면 됩니다. 이것이 바로 역인덱스가 대량의 벡터 내에서 빠른 검색과 비교를 가능하게 하는 방식입니다 . 이제 희소 벡터가 대규모로 저장, 표현 및 비교되는 방식을 이해하셨을 것입니다. 사분면에서 실제로 어떻게 활용되는지 살펴보겠습니다. 이 강좌에서 사용할 설정은 모든 영상에서 동일하게 적용됩니다 . 우리는 쿼드런트 클라우드를 사용할 것이고, 웹 UI에서 벡터를 확인할 것입니다. 왜냐하면 이것이 훨씬 편리하기 때문입니다. 클라이언트를 쿼드런트 클라우드에 연결하는 방법( 제가 지금 보여드리는 방법)이나 클라우드 대시보드를 여는 방법을 잊어버리셨다면, 초기 영상들을 참고해 주세요. 모든 권장 사항과 지침을 확인하실 수 있습니다. 저는 초기와 동일한 방식으로 모든 것을 사용하고 있습니다 . 그럼 이제 사분면의 희소 벡터를 다루는 것부터 시작해 보겠습니다. 희소 벡터는 밀집 벡터와는 사분면 구성 방식이 약간 다릅니다. 여기서는 크기나 거리 측정 기준을 정의할 필요가 없는 희소 벡터 구성을 사용합니다 . 희소 벡터의 크기는 각 희소 벡터에 있는 0이 아닌 요소의 개수에 따라 달라집니다. 따라서 컬렉션 내의 벡터마다 다를 수 있습니다. 그리고 사분면에서 희소 벡터를 비교하는 데 사용하는 거리 측정법은 항상 내적입니다. 전에 얘기했던 그거요. 저희 커뮤니티에서 자주 받는 질문들입니다. 특정 사분면에 있는 희소 벡터가 가질 수 있는 최대 0이 아닌 요소의 개수는 몇 개입니까? 그리고 이 수치는 UN32 유형에 의해 제한되는데, 이는 40억을 훨씬 넘을 수 있음을 의미합니다. 희소 벡터를 사용하여 컬렉션을 생성하기 전에 알아두어야 할 마지막 사항은 희소 벡터가 기본값이 아니라는 점입니다. 그렇기 때문에 희소 벡터로 컬렉션을 구성할 때는 항상 벡터에 이름을 지정해야 합니다. 기본적으로 모든 희소 벡터는 벡터라는 이름으로 명명되어야 합니다. 하지만 그건 아주 간단해요. 어떤 이름이든 사용하셔도 됩니다. 예를 들어, 저는 여기서 매우 독창적인 사고방식으로 ' 희소 벡터'라는 이름을 사용하고 있습니다. 그게 바로 합격 방법이에요. 이렇게 사용하는 겁니다. 이것이 바로 컬렉션에서 희소 벡터를 구성하는 방법입니다. UI에서 어떻게 보이는지 한번 살펴볼까요? 여기 있습니다. 희소 벡터 모음. 저희 예시에서는 설정할 매개변수가 많지 않았지만, 그래도 설정할 수 있는 매개변수는 몇 가지 있습니다. 희소 벡터의 이러한 매개변수는 데이터베이스에서 희소 벡터를 구성하는 데 사용되는 역인덱스와 관련이 있습니다. 그리고 우리에게는 세 가지 매개변수가 있습니다. 하지만 이에 대해 설명하기 전에, 기본 매개변수들이 실제로 많은 사용 사례에서 상당히 유용하다는 점을 말씀드리고 싶습니다. 따라서 역인덱스 매개변수를 변경해야 할 특별한 이유가 없다면 기본값을 사용하는 것이 좋습니다. 꽤 괜찮고 효과가 있을 거예요 . 첫 번째 경우는 데이터베이스에 특정 개수의 희소 벡터가 저장될 때까지 희소 벡터를 검색하고 비교할 때 역인덱스를 사용하지 않으려는 상황을 말합니다. 참고로, 이 매개변수를 사용하더라도 희소 벡터의 경우 삽입된 점의 개수나 이 매개변수의 설정값과 관계없이 사분면의 역인덱스는 항상 생성됩니다. 디스크에 저장된다는 이 매개변수는 역 인덱스가 저장되는 위치에 대한 것입니다. 기본적으로 RAM에 저장되지만, 디스크에 저장하도록 설정할 수도 있습니다. 데이터 유형이라는 이 매개변수는 값의 유형을 나타냅니다. 따라서 희소 벡터 차원의 0이 아닌 가중치는 인덱스에 저장됩니다. 어떤 데이터 형식을 선택하든 원래 값, 즉 0이 아닌 비트는 디스크의 해당 영역에 그대로 저장된다는 점을 기억해야 합니다. 다음은 역 인덱스 구성을 가진 컬렉션의 예입니다. 다음은 여러분이 원하는 경우 변경할 수 있는 세 가지 매개변수입니다. 제가 여기서 선택한 매개변수들은 단지 예시일 뿐이며, 어떠한 경우에도 권장사항이 아닙니다. 하지만 이 코드를 실행하면 컬렉션이 UI에 어떻게 표시되는지 확인할 수 있습니다 . 더 나아가 방금 전달한 사용자 지정 인덱스의 매개변수를 확인할 수도 있습니다 . 자, 이제 다시 돌아가서 희소 벡터를 컬렉션에 삽입하고 검색하는 방법을 살펴보겠습니다. 컬렉션에 희소 벡터를 삽입하려면 사분면의 희소 벡터를 제외한 형식으로 표현해야 합니다. 그리고 이는 제가 첫 부분에서 설명했던 이론적인 내용과 거의 정확히 일치합니다. 우리는 이 부분의 표현에서 중요한 객체의 0이 아닌 인덱스 와 그 값을 가지고 있습니다. 인덱스는 UIN 32 값으로 표현됩니다. 따라서 그 값은 0에서 40억 이상까지 다양할 수 있으며, 부동 소수점 값으로 표현됩니다. 사분면에서 희소 벡터를 생성 하고 사용할 때 중요한 규칙 두 가지가 있습니다. 첫째, 인덱스는 고유해야 하며 , 둘째, 인덱스 배열의 길이는 값 배열의 길이와 동일해야 합니다. 이제 희소 벡터 두 개를 컬렉션에 삽입하는 방법을 살펴보겠습니다 . 다음은 희소 벡터를 가진 첫 번째 지점과 ID가 1과 2인 두 번째 지점입니다. 그리고 여기, 이름부터 아주 독창적인 희소 벡터가 있습니다. 길이가 3인 값 하나와 길이가 2인 값 하나, 그리고 각각에 해당하는 0이 아닌 값 하나. 아주 간단합니다. 이제 그것들을 컬렉션으로 분류하고 UI에서 다시 한번 확인해 보겠습니다. 여기 있습니다. 0.1 포인트 2 길이 3 길이 2이며, 여기에서 유사한 제품들을 확인할 수도 있습니다. 선택의 여지가 많지는 않지만, 대규모 컬렉션에서는 중요할 것이고, 또한 복사해서 컬렉션에 어떤 벡터들이 있는지 확인할 수도 있습니다. 이제 두 점 컬렉션에 대해 유사도 검색을 실행하고 쿼리 벡터와 가장 유사한 희소 벡터를 어떻게 찾을 수 있는지 살펴보겠습니다. 우리가 사용할 인터페이스는 댄스 벡터를 처음 소개했을 때 보셨던 것과 거의 비슷합니다. 유일한 차이점은 쿼리를 전달하는 방식입니다 . 다음은 0이 아닌 두 개의 요소를 가진 희소 벡터이며, 우리는 명명된 벡터를 사용하고 있다는 것을 나타내는 매개변수를 사용하고 있습니다. (동어반복적인 표현을 사용해서 죄송합니다.) 그래서 우리는 이 특정 희소 벡터를 기준으로 결과를 평가하고 있습니다 . 쿼리를 실행하고 결과를 확인해 보겠습니다. ID 1로 포인트를 획득했고 점수는 0.4입니다. 점수가 0.4인 이유와 우리가 얻은 점수가 왜 0.4점인지 자세히 살펴보고, 희소 벡터 비교에서 내적이 어떻게 작용하는지도 알아보겠습니다. 그럼, 우리가 수집한 데이터에서 0.1이라는 답이 나온 이유를 예시를 통해 살펴보겠습니다. 두 가지 요점이 있습니다. 첫 번째 지점은 인덱스가 1, 2, 3인 0이 아닌 값을 세 개 가지고 있습니다 . 그리고 2번 지점은 지수가 1과 5인 두 개의 0이 아닌 값을 가지고 있었습니다. 저희 쿼리에는 0이 아닌 값을 갖는 인덱스 1과 3이 있습니다 . 희소 벡터의 유사도 점수는 쿼리 벡터와 비교 대상 벡터 간에 공유되는 일치하는 인덱스만을 비교하여 계산됩니다 . 예를 들어 첫 번째 지점의 경우, 여기 인덱스는 1, 여기 인덱스는 1이고, 여기 인덱스는 3, 여기 인덱스는 3이 됩니다. 그리고 두 번째 요점은 여기에 하나, 여기에 하나만 있다는 것입니다. 5 와 3은 일치하지 않습니다. 그리고 우리는 단순히 대응하는 값들을 곱하고 더하기만 하면 됩니다. 한번 확인해 봅시다. 예를 들어 0.1의 경우 인덱스 1을 보고 해당 값 1을 확인합니다. 여기서 인덱스 1을 살펴보면 해당 값은 0.2임을 알 수 있습니다. 여기 1 x 0.2가 있습니다. 다음으로 인덱스 3으로 이동해 보겠습니다. 여기에 해당하는 값은 1입니다. 그리고 여기서는 3으로 이동하여 해당하는 값 0.2를 찾았습니다. 다시 한번 1 대 0.2입니다. 여기서 이들을 모두 더하면 0.4가 나옵니다. 마찬가지로 쿼리와 2를 비교했을 때 결과 점수는 0.1로 0.4보다 작습니다. 그래서 저희가 가장 잘 맞춘 결과는 .1이고, ID1 점수 0.4라는 것을 기억하시겠지만, 저희 답변에서 정확히 그 결과를 얻었습니다. 물론 희소 벡터를 이용하면 훨씬 더 많은 작업을 수행할 수 있습니다 . 인덱스를 두 개만 사용하는 것이 아니라 필터와 함께 조합하여 사용할 수도 있습니다. 밀집 벡터와 결합하여 사용할 수도 있고, 이후 영상에서 보여드릴 검색에도 사용할 수 있습니다. 이제 여러분은 쿼드런트에서 희소 벡터를 저장, 비교 및 ​​인덱싱하는 방법을 알게 되었습니다 . 다음 영상에서는 희소 벡터를 이용한 텍스트 검색 방법과 이 방법이 밀집 벡터에 비해, 또는 밀집 벡터와 함께 사용할 때 어떤 장점을 가지는지 설명드리겠습니다. 거기서 만나요.
