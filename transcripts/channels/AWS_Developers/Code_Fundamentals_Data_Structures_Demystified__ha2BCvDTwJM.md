# Code Fundamentals: Data Structures Demystified

- 영상 링크: https://www.youtube.com/watch?v=ha2BCvDTwJM
- 채널: AWS Developers
- 업로드일: 2024-11-29
- 자막 언어: ko
- 단어 수: 약 1379개

---

## 스크립트

코딩을 배우면서 두 번째로 배우는 것은 무엇일까요? 바로 PR " Hello World" 바로 그 다음이죠. 안녕하세요, 저는 여러분께 코딩의 기본을 보여드리겠습니다. 데이터를 저장하고 변수를 사용하는 방법을 배우게 될 거예요. 그런데 변수에 문자열과 숫자뿐만 아니라 다른 데이터 유형도 저장할 수 있다는 사실을 알고 계셨나요? 파이썬의 데이터 구조에 대해 이야기해 보겠습니다. 데이터 구조란 무엇이고, 어떻게 사용되며, 언제 사용되는지 알아보겠습니다. 여기서는 리스트, 딕셔너리 , 세트를 살펴보겠습니다. 시작해 볼까요? 먼저 리스트부터 시작해 보겠습니다. 예를 들어, 슈퍼히어로 모임을 주최한다고 가정해 봅시다. 정말 멋진 아이디어죠! 파이썬의 리스트는 참석자 명단과 라인업을 관리하는 데 완벽합니다. 파이썬의 리스트는 모두 요소 의 모음이기 때문에 다양한 데이터 유형의 요소를 저장할 수 있어 여러 작업에 활용도가 높습니다. 간단한 것부터 시작해서 조금 더 복잡한 작업들을 살펴보겠습니다. 먼저 리스트를 만들어 보겠습니다. 그리고 이제 참석자를 리스트에 추가해 보겠습니다. 여기서는 append 메서드를 사용하여 스파이더맨을 참석자 리스트에 추가합니다. append 메서드는 항상 기존 요소를 추가합니다. 리스트의 끝에 새 항목을 추가하는 것은 이 예시에서처럼 새 항목의 특정 위치에 신경 쓰지 않고 단순히 리스트 에 포함시키기만 하면 되는 경우에 적합합니다. 스파이더맨을 추가하면 이 작업이 완료된 후 리스트의 마지막 요소가 됩니다. 하지만 특정 위치에 삽입할 수도 있습니다. 때로는 새 항목을 추가하는 위치를 더 세밀하게 제어하고 싶을 때가 있기 때문입니다. 이때 ` insert` 메서드가 매우 유용합니다. 첫 번째 인수( 이 예시에서는 0)는 새 항목을 추가할 인덱스를 지정합니다. 0을 사용하면 리스트의 맨 처음에 삽입하라는 의미이며, 다른 모든 항목은 오른쪽으로 완전히 이동합니다. 따라서 리스트의 순서가 중요한 경우, 예를 들어 손님을 도착 시간 이나 중요도 순으로 정렬할 때 매우 유용합니다. 손님을 제거할 수도 있습니다. 블랙 팬서가 파티에 참석할 수 없다고 가정해 보겠습니다. 여기서는 ` remove` 메서드를 사용하여 블랙 팬서를 손님 목록에서 제거합니다. `remove` 메서드는 지정된 항목이 처음 나타나는 위치를 검색하고 목록에서 해당 항목을 제거합니다. 항목이 여러 번 나타나는 경우 첫 번째 항목만 제거되므로 이 점을 유의해야 합니다. 리스트에 없는 항목을 제거하면 파이썬은 `value` 오류를 발생시키므로, 항목을 제거하기 전에 리스트에 있는지 확인하는 것이 안전합니다. 또한 게스트 목록을 정렬할 수도 있습니다. 이 예제에서는 게스트 목록을 알파벳순으로 정렬해 보겠습니다. `sort` 메서드는 문자열의 경우 오름차순, 숫자의 경우 오름차순으로 정렬합니다. `sort` 메서드는 기존 리스트를 수정하는 제자리 작업으로, 새 리스트를 생성하지 않습니다. 기존 순서를 유지 하고 정렬된 새 리스트를 만들려면 ` sorted` 함수를 사용하면 됩니다. 이제 기본 사항을 익혔으므로 리스트 컴프리헨션을 사용하여 새로운 리스트를 생성하는 고급 작업을 살펴보겠습니다. 다음은 리스트 컴프리헨션입니다. 리스트 컴프리헨션은 새로운 리스트를 생성하는 강력하고 간결한 방법으로, 간결한 `for` 루프와 같습니다. 여기서는 각 요소가 해당 게스트 이름의 길이인 새로운 리스트를 생성합니다. 리스트 컴프리헨션은 데이터를 변환하거나 리스트를 필터링하는 데 매우 유용하며, 가독성을 높여줍니다. 예를 들어 루프와 같은 다른 방식보다 더 빠릅니다. 다음으로 리스트 컴프리헨션을 살펴보겠습니다. 좋은 예로 좌석 배치도를 생각해 보겠습니다. 이것은 중첩 리스트 컴프리헨션입니다. 마치 루프 안에 또 다른 루프가 있는 것처럼, 같은 영웅끼리 앉지 않는 모든 가능한 영웅 쌍을 만들어 좌석 배치를 계획합니다. 두 개 이상의 리스트에서 항목 조합을 생성해야 할 때 매우 유용합니다. 이 경우, 어떤 영웅도 자기 옆에 앉지 않도록 좌석 배치를 계획하는 데 도움이 될 수 있습니다. map과 Lambda를 사용하면 빠른 변환이 가능합니다. 여기서는 강력한 파이썬 기능인 map과 Lambda 함수를 결합했습니다. map은 리스트의 모든 항목에 함수를 적용하고, Lambda 함수는 익명 함수로, 이 예시에서는 문자열을 대문자로 변환합니다. 이 조합은 데이터에 중요한 변환을 적용하는 데 매우 유용하며, 특히 리스트 의 모든 항목에 간단한 연산을 적용해야 할 때 유용합니다. 리스트는 매우 다재다능하며 데이터를 정렬하고 복잡하고 간단한 연산을 처리하는 데 탁월합니다. 이제 딕셔너리로 ​​넘어가 보겠습니다. 파이썬의 딕셔너리는 키-값 쌍으로 이루어진 순서 없는 컬렉션입니다. 각 키는 고유해야 하며, 해당 값에 접근하는 데 사용됩니다. 딕셔너리를 사용하면 키와 값을 연결할 수 있습니다. 각 히어로의 초능력과 추가 정보를 추적하는 데 사용해 보겠습니다. 이것은 중첩 딕셔너리입니다. 바깥쪽 키인 각 히어로는 해당 히어로의 초능력과 강도 수준을 담은 안쪽 딕셔너리와 연결되어 있습니다. 이러한 구조는 복잡하고 관련된 데이터를 매우 논리적인 방식으로 구성할 수 있도록 해줍니다. 이제 딕셔너리의 다른 연산들을 살펴보겠습니다. 먼저 중첩된 데이터에 접근하는 방법을 알아보겠습니다. 딕셔너리에서 중첩된 데이터에 접근하려면 대괄호를 여러 개 사용합니다. 첫 번째 대괄호는 ' Spider-Man' 키를 가진 바깥쪽 딕셔너리에 접근하고, 두 번째 대괄호는 'Strength' 키를 가진 안쪽 딕셔너리에 접근합니다. 이렇게 하면 데이터 구조를 탐색하여 특정 정보를 추출할 수 있습니다. 이제 새로운 히어로를 추가해 보겠습니다. 딕셔너리에 새로운 키-값 쌍을 추가하는 것은 매우 간단합니다. 새 키에 값을 할당하기만 하면 됩니다. 만약 해당 키가 이미 존재한다면 기존 값을 업데이트합니다. 이러한 유연성 덕분에 데이터 세트를 확장하여 더 많은 정보를 얻을 수 있습니다. 새로운 영웅들을 추가했지만, 여기서 정보를 업데이트하고 싶다고 가정해 보겠습니다. 중첩된 딕셔너리의 값을 업데이트하려면 먼저 아이언맨 딕셔너리에 접근한 다음, 그 안에 있는 '힘' 키에 접근하여 값을 1만큼 증가시킵니다. 이렇게 하면 깊이 중첩된 데이터를 수정할 수 있다는 것을 알 수 있으며, 복잡한 데이터 구조에서 특정 속성을 업데이트할 때 유용합니다. 자, 이제 좀 더 고급 딕셔너리 연산을 살펴보겠습니다. 먼저 딕셔너리 컴프리헨션부터 시작해 보겠습니다. 딕셔너리 컴프리헨션은 리스트 컴프리헨션과 유사하게 간결 하게 새로운 딕셔너리를 생성하는 데 매우 효과적입니다. 여기서는 영웅들의 이름과 힘 레벨만 포함하는 새로운 딕셔너리를 생성했습니다. 이는 복잡한 데이터 구조 에서 특정 데이터를 추출할 때 매우 유용합니다. 또한 딕셔너리를 병합할 수도 있습니다. ` update` 메서드를 사용하면 두 딕셔너리를 병합할 수 있습니다. 여기서는 새로 추가된 두 영웅에 대한 정보를 기존의 영웅 정보 딕셔너리에 추가할 것입니다. 새 영웅 딕셔너리의 키가 기존 영웅 정보 딕셔너리에 이미 존재하는 경우, 해당 키는 새 값으로 업데이트됩니다. 이처럼 매우 빠르고 효율적인 방법입니다. 이제 여러 소스의 데이터를 결합해 보겠습니다. 기본 딕셔너리를 살펴보면 정보 자동화에 도움이 됩니다. 기본 딕셔너리는 누락된 값을 자동으로 채워주는 팩토리를 사용하는 딕셔너리의 하위 집합입니다. 여기서는 특정 키에 접근할 때 해당 키가 존재하지 않으면 자동으로 항목 목록을 생성하는 함수를 사용합니다. 그렇다면 이 함수는 언제 유용할까요? 데이터를 그룹화해야 하는데 모든 그룹을 미리 알 수 없는 경우에 매우 유용합니다. 키가 목록에 추가하기 전에 해당 키가 존재하는지 확인하는 번거로움을 줄여줍니다. 보시다시피 딕셔너리는 복잡하고 관련된 데이터를 정리하는 데 매우 강력하며, 고유 키를 기반으로 정보를 빠르게 찾아야 할 때 정말 유용합니다. 파이썬의 세트는 고유한 요소들의 순서 없는 모음입니다. 각 요소가 한 번만 나타날 수 있는 수학적 집합과 유사합니다. 따라서 목록 에서 고유 값을 찾 거나 합집합, 교집합, 차집합과 같은 집합 연산을 수행하는 데 매우 편리합니다. 다시 슈퍼히어로 파티 이야기로 돌아가서, 파티에서 가장 좋아하는 특별한 선물을 주고 싶습니다. 누가 파티 선물을 싫어하겠어요? 바로 이럴 때 세트가 빛을 발합니다. 각 항목이 한 번만 나타나도록 보장해 주기 때문입니다. 여기서는 파티 용품 세트를 만들어 보겠습니다. 중괄호(#)를 사용하여 세트를 정의하는 것을 기억하세요. 세트는 키-값 쌍이 없다는 점을 제외하면 딕셔너리와 매우 유사합니다. 세트는 순서가 지정되지 않고 중복된 요소를 포함하지 않습니다. 먼저 항목을 추가하는 연산을 살펴보겠습니다. 중복된 항목은 무시됩니다. `add` 메서드는 세트에 항목을 추가합니다. 만약 '망토'를 다시 추가하려고 하면 무시됩니다. 세트는 고유한 항목만 저장하기 때문입니다. 따라서 컬렉션에 중복된 항목이 없도록 하는 것이 매우 유용합니다. 또한 '부츠'가 성공적으로 추가된 것을 볼 수 있습니다. 이는 새 항목이기 때문입니다. 항목을 제거할 수도 있습니다. `remove` 메서드를 사용하면 세트에서 특정 항목을 제거할 수 있습니다. 만약 해당 항목이 존재하지 않으면 키 오류가 발생합니다. ` discard` 메서드도 있습니다. 이 메서드는 항목을 찾을 수 없더라도 오류를 발생시키지 않습니다. 하지만 해당 항목이 세트에 존재하는지 확실하지 않을 때 유용합니다. 이 점을 기억해 두면 좋습니다. 자, 이제 시작해 보겠습니다. 좀 더 고급 집합 연산을 살펴보겠습니다. 먼저 여러 집합을 사용하는 연산부터 시작해 보겠습니다. 여기서는 '고유 항목 찾기'라는 기능을 살펴보고, 특히 이 핵심 코드 부분을 살펴보겠습니다. 여러 집합 연산 옵션을 시연해 보이겠습니다. 첫 번째는 합집합( Union)입니다. 합집합은 여러 집합에서 고유한 항목만 결합합니다. 다음으로 교집합(Intersection)을 살펴보겠습니다. 교집합은 두 집합 모두에 공통되는 항목을 찾고, 차집합( Difference)은 한 집합에는 있지만 다른 집합에는 없는 항목을 찾습니다. 이처럼 집합 연산은 매우 유용하며, 서로 다른 항목 모음을 비교하거나 결합하는 데 탁월합니다. 또한 집합을 효율적인 검색에 사용할 수 있습니다. 이 코드는 집합을 사용하여 효율적인 멤버십 검사를 수행하는 방법을 보여줍니다. 여기서는 항목이 집합에 있는지 확인합니다. 특히 큰 모음의 경우, 리스트에 있는지 확인하는 것보다 훨씬 빠릅니다. 여기서는 리스트를 사용하겠습니다. 집합 멤버십을 테스트하는 컴프리헨션을 사용하여 게스트 목록과 어벤져스에 어떤 히어로가 있는지 빠르게 확인할 수 있습니다. 이제 집합 컴프리헨션을 살펴보겠습니다. 이는 리스트 컴프리헨션 및 딕셔너리 컴프리헨션과 매우 유사합니다. 집합 컴프리헨션을 사용하면 간결한 방식으로 집합을 생성할 수 있습니다. 여기서는 이름이 자 이하인 히어로들의 집합을 만들 것입니다. 이는 단 한 단계만으로 데이터를 필터링하고 고유성을 보장하는 훌륭한 방법입니다. 집합은 고유성을 보장하고 효율적인 멤버십 테스트 및 수학적 집합 연산을 수행하는 데 매우 유용합니다. 지금까지 파이썬 데이터 구조의 기초부터 고급 개념까지 살펴보았습니다. 여기서 핵심은 다음과 같습니다. 리스트는 순서가 있는 데이터와 순차적 연산에 적합하고, 딕셔너리는 관련 데이터를 정리하여 빠르게 검색하는 데 탁월하며, 집합은 고유성을 보장 하고 효율적인 집합 연산을 수행합니다. 각 구조는 마치 슈퍼히어로처럼 고유한 강점을 가지고 있으며, 적절한 구조를 선택하면 코드를 더욱 효율적이고 읽기 쉽게 만들 수 있습니다. 이제 단순히 이름을 출력하는 것에서 그치지 않고 복잡한 데이터 구조를 사용하여 애플리케이션의 효율성을 높일 수 있습니다. 올바른 데이터를 올바른 방식으로 사용하는 것은 코딩 실력 향상에 매우 중요합니다. 이러한 아이디어를 연습하면 전반적인 코딩 실력을 향상시킬 수 있습니다. 이 영상이 도움이 되었거나 더 많은 실습 위주의 기술 콘텐츠를 보고 싶으시다면 좋아요를 누르고 AWS Developers YouTube 채널을 구독해주세요. 시청해주셔서 감사합니다.
