# Andrei Panferov  - QuEST: Stable Training of LLMs with 1 Bit Weights and Activations

- 영상 링크: https://www.youtube.com/watch?v=e3ClKTyVrkI
- 채널: Cohere
- 업로드일: 2025-03-05
- 자막 언어: en
- 단어 수: 약 8300개

---

## 스크립트

[Music] so hi everyone my name is Andre I'm a PG student at Institute of Science and Technology Austria where at the group of Professor alar we work mostly on efficiency for machine learning and specifically for on efficiency for large language models and today I'm going to present our latest work that's called quest stable training of LMS with one bit weights and activations where we targeted a very specific thing that is llm pre-training where we actually want to uptain models that are efficient from the start and utilize efficient kernels when they're done training and they use during inance we did this work together with J Chen sish Roberto Lopez k m d and Professor Alistar himself and to start with the L pre trining we need to First understand what is the training itself and how it's usually done with larish language models and in general when we say that we want to train a model what we mean is that we have some sort of parameterized function here for example we can see that it's a function L that is parameterized by some set of parameters fee and what we want to do is we want to find such parameters fee that minimize some sort of Target function this function L on some set of data represented by X and Y here and for per for and to do this we usually use methods that take into account gradient information of this function meaning that if we can find the gradient of this function with respect to parameters Fe what we can do is do updates some parameters Fe step by step performing So-Cal gradient descent moving in the direction in which this function has this test Decline and such arrive into a point where into a Minima of this function and to do this this function naturally has to be differentiable with respect to those parameters and when we think about differential functions we usually imply that those functions are functions of real numbers and as such parameters of machine learning models by default are perceived as real numbers but the thing is in computer there is no such thing as a real numbers because this is a set of infinite cardinality basically so you cannot encode the selection from set of infinite size and what you can do at best is use some approximations for real numbers and in computers those approximations are usually floating Point numbers where we have some sort of grid a finite grid where we as basically can enumerate every point in this grit and uh encode somehow a selection from this grit with some finite number thus representing a number on the real axis with some finite bitwidth and the thing about floating Point numbers in the way they are usually used is that they are good approximation in a sense that they provide very huge range meaning they can represent both very large numbers and small numbers and they provide consistent relative accuracy on the entirety of this range meaning that with usual operations such as addition and multiplication you get some sort of predictable error with this approximation of floating point over real numbers but the thing is that those numbers are quite heavy in a sense that you need a lot of information to use code floating Point numbers the representation the people use is somewhat complicated that you need to store this sign and the so-call exponent and mantisa separately to represent floating Point numbers and in general floating Point numbers usually take from 16 up to 64 but usually it's either 16 or 302 bit for every single real number approximation of a floting point number and that is might be a lot and also because the representation is somewhat convoluted with this s exponent and montisa it might be hard to perform operations on them because also processors were designed with support for those operations in mind they are still they still might not be very energy efficient and for example as one and just in general it might be hard to support more complicated operations with them without M Define the hardware level and in general if we talk about Energy Efficiency and in general efficiency of computations maybe there are certain D data types that are more efficient than floating Point numbers and if we actually look at the Modern accelerators for computations for example at Nvidia B100 GPU it actually has a variety of data types that are not high Precision floating Point numbers but these are low Precision floating Point numbers like fp4 which is has only four bit per parameter or fp8 fp6 or integer numbers which basically represent the yes integer numbers that are that is a uniform G on top of real numbers and for which it's easier to implement operation such as addition because when adding to integer numbers together is quite obvious that you get an integer number back and the bitwise operations needed to perform the operations on them are simpler than real numbers and that's why actually those for those data types like either low Precision floating Point numbers or integer numbers you can make kernels that perform operations on top of them much more efficiently than in high Precision floting point numers for example here we can see speed UPS of Nvidia a100 GPU operations on top of different data types when compared to same operations on floating 16 numbers so here specifically we have Matrix Matrix M lication speed up and we can see that for low Precision numbers it's basically proportional to how much memory is save compared to floating 16 so going from floating point1 16 to integer four you reduce the size of your numbers in memory by four but you also speed up all of the computations with those numbers by a factor of four meaning that you can actually run your models if if your models Were Somehow represented in those l formats you get practically proportional speed UPS to how much you actually compress them in terms of those specific data types compared to fp16 and that's why it might be very beneficial to train models that can be stored in those representations because not only you get models that are smaller because of low Precision but because you can actually utilize kernels that are simply faster than floating Point metrix paration kernels to get practically proportional speed ups when running those models so this is an additional incentive why low Precision models are important and why it might be beneficial to train moduls in low precision and but there is a problem with training low Precision namely that when you use floating point it's a very dense cre representation of real numbers meaning that any small perturbation to those numbers will still L on some points of this grid and as such you can actually perform gradient descent on top of them as I mentioned because applying a gradient step on top of your model will actually change its representation precisely as it would do with real numbers or at least close enough but for example if we look at the integer four numbers here we see that on this plot sort of you can see a representation of if you had the floting point numbers and let's say you had some gradient step that you wanted to apply during gradient Des Center those numbers to FL in point numbers you can just add this step and this point here would just move by a little but for integer numbers if you wanted to take the same step you would actually see no change in your representation because the step is smaller than your GD size and than your GD step and as you decrease your bit with your G step get relatively larger and in general your representations becomes sort of insensitive to small perturbations and as such you can no longer just apply gradient steps to those parameters so you kind of lose differentiability of your parameters with respect to your L function and you can no longer just perform gradient descent on top of those parameters and there are a few ways to fix this and uh let us start by how people usually do this and the idea is that you actually do not store your model in those low Precision representations inherently but rather have a high Precision copy of your model to which you know how to apply gradients and when you actually perform operations on this model you quantize it for example every single forward pass of your model to obtain quantize representations perform operations on top of those but then when you come after you've computed the gradient you apply those gradients to this High Precision copy which can be done in a straightforward way because those representations are inher differentiable and as such you solve the problem of nondifferentiability of your parameters because Al you perform all of the computations with your low Precision kind of copy of the model the gradients are applied to the high Precision copy and a such you don't lose any gradients because they really do get applied every single time you perform forward backward pass however there is a problem with this approach so this approach of keeping a full Precision copy of the model is standard but the problem is that this operation highlighted here is the quantization of your model parameters that you have to do every single forward path to actually obtain parameters that your perform operations with from your H Precision copy this operation is non differentiable by itself in a sense that if you want to perform gradient descent we need to be able to propagate the gradient of our loss through the entire model but this oparation itself is non differentiable meaning we cannot propagate loss through it and as such we actually cannot apply gradients to apply to those High Precision parameters to the high Precision copy that we have inher so what we actually have to do instead is oh actually oh sorry I think I messed up the order a little because uh yes we'll get to the problem of no differentiability later but right now let's talk about the separation of quantization itself that we have to do every single horror test and basically what we are doing here is we have our weight matrices for a model and what we have to do every single Flor pass is we have to find Quant representations of those matrices and naturally we have uh this sort of discrepancy that we have kind of real parameters High Precision parameters but we don't use them in operations because we use quantized parameters in operations and as such there is discrepancy between what we store and apply updates to and what we use in operations and naturally this discrepancy becomes too large the training process doesn't make sense anymore because what we update becomes sort of decoupled from what we're actually using when using our model and as such it makes sense to sort of try and minimize this discrepancy and that would be just minimize some sort of difference between the quantized parameters X hat that we obtain every single forward pass for the computations and the high Precision copy of the parameters X that we actually gets updated after we do the backward pass when we do the step of our optimizer and to minimize this error what we propose we do is we optimize the quantization procedure itself so here we can see that we're doing usually quantization is some sort of round into a GD as I mentioned and before people often did this rounding in a simple manner that the GD was fitted on two weights in a mean Max fashion meaning that it it was fitted to for its sort of left hand to be exactly the minimum value of your weights and right hand to be the maximum value of your weight this way the grid would cover the entire range of values that you're trying to quantise but we argue that this is actually not the optimal way because it's very sensitive to this to outliers in your weights and what we're proposing here we do instead is that we fit the grid to the distribution in an MSC optimal way so minimizing this discrepancy between x and x hat in the form of mean square error between those two variables and to do this we make an additional subsumption based on observations that for large language models for already pre-trained ones people sort of know that weights are almost normally distributed and as such if we wanted to optimize a grd for distribution of all n Reg model we would optimize it for something that very closely resembles a normal distribution and we extend this idea by saying that we will actually fit the grid to our weight distribution in a way that is MSC optimal minimizes mean quadratic error of rounding our weights to this grid so mean quadratic error between x and x head uh that would be optimal for a ga distribution and it's formalized here in the sense that we have this rounding operator with some sort of scaling that determines how exactly we fit the weights onto the grid and this scaling factor is chosen as to minimize the expectation of quadratic error of rounding a normal distribution to a GED meaning that if our weights were indeed normally distributed this rounding procedure would produce an MSC optimal rounding of the weights to the grid that we're using so any questions about this idea of how we optimize the forward pass by better matching the W distribution to a g so I guess we'll return to this later and if any questions come up I'll be happy to answer them but for now yes let's just remember that for the forward pass we do smart smart mapping between the weights and the grid that we're quantizing to using the idea of normality of normality of the distribution of the weight so we do it in an MSE optimal fashion assuming normally distributed weights so this is how we optimize the forward pass and for the backward as I already mentioned before there is a problem that this rounding operator that is inherent to to uh projection is non not differentiable and people have known about this problem for a while and the standard thing the standard way to address this problem is to use the straight through estimation and what it is is that if we try to draw the plot of this rounding function it would be a step function in a sense that it would have zero gradient anywhere except the places where you can you actually jump from the one notation point to the next one and there it has an infinite gradient and a jump in your function and it sort for step and what the stra straight through estimation does it says that if your steps are small enough then this function is in general very close to Simply a linear function with linear efficient of one and what we can do is say that the gradient of this step function we can approximate it with the gradient of a linear function with a coefficient of one which is just one everywhere so basically we can approximate the gradient with respect to the unrounded values as the gradient with respect to the quantized values when doing the backward pass so on the for we have X and we obtain X by rounding this operation is not differentiable but the approximation says that we can say that the gradient with respect to X can be obtained by just reusing the gradient with respect to X head that we have already somehow obtained through back propagation so we sort of learn can approximate back propagation through this round in operation by just saying that the gradient is one is propagated as is through it but uh there a problem with this approach is that if we use this smart distribution fitting for our GD on the forward path and we look at the normal distribution and how a GD is fitted to it we will see that there are some portions of the distributions that fall outside of the GD which naturally happens because those are sort of outliers in our distributions some taals that happen very rarely and a such effect our mean square error by a very small margin and it's better to just leave them outside and try to accommodate so it the straight of results in the fact that tails of our distribution fall outside of the grid and naturally we have a very large quantization error for those stales and in practice if we wanted to do straight through estimations through points that happen to be in those tailes it would not make much sense because when quantizing those values simply get round get clipped get rounded to the nearest Tre point that might be very far away from them and when we apply gradients to the underlying High Precision representations they will switch left they will sort of move left and right but it wouldn't change anything with respect to what we how we actually do computations because in the next four they will still get rounded in exactly the same way to the exactly the same gr point so in general the gradients for points that fall outside of our grid are unreliable because of how far away the updated weight is from the quantized weight that is actually used in operations and extending this idea what we might want to say what we did in the paper is that when doing the backward passest the thing that we want to minimize is we have this Hyperion copy of the weights and the weights that we're actually using operations and on the forward PA we minimize discrepancy between those two by minimizing the quadratic error between them as we did with distribution fitting but on the backward path we also want to ensure that the gradients that we apply to them actually makes sense and we can formulate in this form of quadratic eror between the gradient taken in this point between the gradient in the point where it's applied and the gradient in the point where it was sort of calculated because we actually used this value for this parameter on the forward fast so this is sort of again the discrepancy between the full Precision weights that we update and the low Precision weights that we used in computations but now in the of gradients that we apply to them and then we calculate and while we don't have explicit values for uh this gr because we never actually do unquantized forward passes we still can use this idea that if maybe this this quantity might be very large if the difference between W and W head is large and this again reiterates this idea so if the point Chang it too much during the quantization on the for pass like it gets rounded way too hard then probably the gradient that we estimated for it will not be a good approximation for the real gradient that uh this point line way over here has and what we can do with this untrustworthy gradient is we say that instead of applying this gradient that had huge Distortion because of this rounds on the four the better thing to do is simply not to apply to completely zero out zero it out and we formalize in this idea of masking on top of our gradient estimation so what we had initially for straight through estimation is we just had this differentiable this gradient with respect to x equal to the gradient with respect to X head but what we're saying is that we can extend it with a binary mask that is chosen on how much X change it due to quantization on the forward path so this is somehow a measure of discrepancy between the weights that we update and the weights that we actually using computations and what we say is that we can build this B mask by using some sort of threshold on this error that would result in this sort of pattern where weights that are closer to quantization grid points are sort of deemed trustworthy because they didn't change too much during the forest pass but values further away from grid points like the red area here actually we say we say that it changed too much during the forward pass and the discrepancy between those two values is too large and it's better to simply not apply any gradients to them instead of applying this untrust Force gradient so what we do here is we apply this binary mask during back propagation through this sort of round into grid operation and to choose this threshold we actually say that it makes a lot of it makes little sense to leave gaps in in our cotization GD because uh we observed it both in practice and logically it would make little sense for a point here not to get gradient because if it does get gradient it actually moves left or right and can change its value so it might it makes more sense to apply gradients to every single point inside the Greek so we chose this sort of factor for Trust of values for this threshold for binary Mass to be half the grid interval such that it covers all of the inner points of the grid but also sticks out outside a little so is this idea about zero out the gradients to points that fall way outside of the grid uh clear to you other any questions uh I think there two questions by pav in the chat uh I could read them out uh so first please do I don't have it open yeah all good um the first question is can performing in trining pruning or lasso regularization improve the problem with the outliers in this context and the second part is um does it make sense to penalize weights for numerically overflowing the target quantized range by introduction of an additional loss term uh so I think I'll answer the second question first so what we did in practice as I said we fit AG to distribution in This MC optimal weight but what we actually also have here is that we normalize the weights but it L2 Norm practically meaning that uh some even even if this assumption about normality of the weight was right no matter how you scale your GD weights will still fall outside and actually this amount of Weights that fall outside is can be computed even in theory by just Computing the mass of those tails that fall outside for thec optimal Feit so it doesn't make sense to penalize them additionally and regarding General penalizing the scale we use L2 Norm or scaling for the weights meaning that the actual scaling for them can be way different but we sort of normalize it to a 01 normal distribution so even if x was a normal distribution with very large variance normalizing by itself to Norm would produce a 01 normal distribution for which we have those optimal coefficients and for which it is sort of expected that some values will fall outside of the gek because this is a trade-off of G being denser in the Middle where there are more values to be expected and the some values falling outside which is sort of inevitable when you're dealing when you're dealing with distributions that have sort of infinite tales and the first question could you please remind me what it was again yeah uh uh I think the first question was can performing in training pruning or lasso regularization improve the problem with the outliers in this in the context that you were mentioned okay so in general we didn't look into pruning on any level Beyond a very small level because later I'll show that we actually have combination of our method with sparcity which can be thought of as some very local pruning but on a level higher than just some adjacent values and structur sparcity within looking to pruning okay thank you so the reiterate this is the basic formulation of the West algorithm so what we do on the forward is we quantize our weights in a way uh that would produce MSC optimal quantization if they were distributed normally and on the back word we back propagate through this separation that is not inherently differentiable by using straight through estimation with an additional binary mask that was chosen via analyzing quantization errors of weight on the forward pass so this mask is not build using the gradients themselves but rather what happened on the forward past and up until now I mostly mentioned that we quantize the weights but that's not the truth because actually we quantize both weights and activations and it's important because the speed UPS I mentioned they those are speed ups for fully quantized matrix multiplication operations meaning that both ters that you're multiplying together are quantized meaning that both your weights and activations have to be quantized and this sort of quantization that happens that has to happen during forward path of the training we can use exactly the same quantization scheme to quantize activations too and thus perform matrix multiplication between those in the low Precision format which would be which would mean that the resulting model that we obtain would be optimized for both for quantizing both weights and activations and as such for for using those specific low Precision metrics multiplication Kel later when we will want to deployed but we'll talk about this later and now uh let's show let's talk about this additional things so this is basically the idea behind Quest but we do one additional thing on top of this that makes our training some produce even better results and become in a way a little more stable is that as I said we kind of know that large language models with distribution often are close to normal distribution but actually there is a way to ensure that it almost perfectly matches the normal distribution and that is applying the hadamar transform to whatever you're quantizing by doing that by applying a Hadar transform to the matrices that you're quantizing is the results in distribution of values in those transport matrices is guaranteed to be normal and when rounding it to the optimized GD we can actually say with certainity that this rounding would be performed in an MSC optimal way when using this idea together with West that optimizes the MSE feed for quantization under this Assumption of normal of normality so what I'm trying to say here is that instead of just assuming the normality we can actually sort of enforce it with the by inserting the homart transform inside the quantization scheme and more specifically the way we would do it is our our model is mostly comprised of linear layers and here we have two algorithms for a forward pass of quest and for a backward PA of quest of the full Quest algorithm that actually uses the hardar transform and on the forward PA we have full Precision weights and activations and first thing we do is we apply the Hadar transform to both of them then we quantize both of them here the quantization is this projection operator to some sort of grid with an optimal scale and then we perform metrix multiplication in this slow Precision format and what we have to store from this matrix multiplication is actually those two low Precision weights and activations uh and those aformentioned masks here for masks that depends on the difference between the quanti and the unquantized tensors I don't have the notation but the those masks m alpha star are actually just those binary masks that will use the backward PA during the backward pass and on the backward pass we reuse those quantized weights and activations those masks and the new arrived gradient from the following layers to perform raing computations for the weight and uh for the inputs to and the thing we should note here is this is that those gradients are not quantized and even if we have to perform matrix multiplication between those unquantized gradients and quantized weights we'll have to do it with higher Precision matrix multiplication kernels because this is still a full Precision operation because the gradients are not quantied and as such we don't get speed up during the backward pass and in general this means that we don't get speed up during the training at all and it means that this algorithm doesn't allow us to train faster what it does it allows us to produce models that use both quanti weights and activations and use more efficient kernel for for passes and for inference but the training itself is not sped up and one additional thing why this Hadar transform that we inserted so the first idea why it's useful is because it improves the fit between the values that weing and the grid that is chosen to be optimal for normal distribution because hmar transforms normalizes your distribution and makes it normal but another thing is that on the backward path we naturally have to back propagate through this hardar Transformer and this means we have to apply the inverse hmar transform to whatever gradients we're Computing and this shows additional interesting point because if you remember the masks that we applied during the backward test that were based on the cotization error are binary meaning that some portion in this setup some portion of the weights doesn't get any gradients it gets fully zeroed out and the problem with it is that if you look at this the weights that don't get gradients fall outside of the distribution meaning that if they don't get updated they will still lie outside of the distribution meaning that they will never get gred and they will practically always remain stale during training and unused one thing to cter it naturally is using weight Decay such that those values even if they don't get any gr they would slowly drift towards the center mean slowly D towards the clation GD but a better way is to actually use this hardar transform because as you can see when we apply this binary mask to the gradient we actually then to obtain the actual gradients with respect to our full Precision white copy we have to perform the inverse hardar transform over this mask gradient and doing this makes it such there are no fully zero entries so some entries of this tensor are zero but when you perform this inverse cabar transform it reshuffles all of the entries basically perform some random some sort of rotation on top of this vector and now all of the weights of the original weight copy that we have here get the gradient and we get rid of this problem of stale weight and we actually have ablation showing that this Kar Transformer reduces the number of stale weights during training and this is one another reason why hadamar transform is useful so firstly it improves the distribution feed and secondly it reduces the number of stale weights during training so uh now that we've discussed the scheme let's discuss the results that we obtained and later interpret how we can interpret them but firstly let us look at this plot so this plot shows the C4 validation loss of models of Lama family from 30 million parameters to 800 million parameters trained on the C4 trading set for the amount of tokens that is 100 times the number of parameters for example this 30 million parameter model here was trained on 3 billion tokens and this 800 million parameter model was trained on 80 billion tokens and we trained those models of different sizes namely 30500 200 400 and 800 million parameters on the corresponding numbers of tokens and we did it for with and without Quest ization for Quest we used quantization to one bit two bit 3 bit and four bit for both weights and activations and what we can see here is first an interesting thing is that one bit training meaning that all of the forward path operations all of the metrix multiplications were performed on binary weights and binary activations this still converges and provides a stable training and stable training law and on its own it's already interesting how this setup where for every practically tenser in your network have only one bit of information it can still stably convert and provide results that are noticeably better than other quantization precisions but are still in a sense somewhat competitive but what is more interesting what we can see here is that the optimality in terms of model size to model error tradeoff seems to be around 4bit quantization meaning that if we train with quest for both weights and activations quanti to 4bit we get par optimal models on this size to accuracy tradeoff size to validation loss tradeoff but there is a caveat to interpreting those results because as I said the number of beta we used is proportional to the number of parameters and at the fixed model size megabytes models that work bized harder have more parameters and as such they have seen more data and a more careful analysis of what's Happening Here is needed to actually make any claims about optimality and this is what we do next in this section of the paper where we interpret the quantization scale in laws to find the real optimal Precision so maybe any questions up to this point about the method itself uh you can unmute well uh first of all thank you for answering prior to questions and uh quick question did you try to experiment with adding random noise to the masks and or drop out like how does that influence the training convergence plots um actually no we didn't try to add random noise we tried it we tried a few things where we would try to do something with those clipped values like we try to accumulate them or apply with smaller learning rates but none of those experiments showed any improvements and we decided that simple binary clipping is the best thing we can do here we didn't try to insert an additional noise but this might be an interesting direction for a future work for example one other thing we might try to do is make it a smooth function instead of a binary mask but iner noise is also an interesting proposition we might consider it for a future work thanks thanks and while I'm still here is uh like con concept of the lo lottery ticket uh applicable here do think like when you have an AAL structure which is could be applicable to larger scale models for instance uh so let's say you trained a smaller scale model and you kind of have a topology of your mask or you know your weights and then you can just extrapolate that onto the bigger model without okay I'm I'm not sure if that happens because as I mentioned one of the things the hardar transform does is that it shuffles the mask and the way we verified it in the paper is we had this study where we looked at masks during training of a single model at different steps and we saw how consistent it is and we saw that when we actually use the hardar transforms The Masks kind of shuffles a lot during training meaning that weights adapt a lot and as such I don't think that the masks that we learn on its own is that interesting because it might shift during the training of one model itself and it's probably not transferable to other models thank you okay so if there are any other questions I'll go wors this scal and law discussion part of this paper and in general when we talk about scaling loss what we mean is that we want to try and predict or describe the final loss of our model as a function of its parameters and for lar language model specifically the parameters of the models are usually dictated by the literal number of parameters of this model and the amount of data it was trained on there are other things that are very important for the optimization procedure like how the exact architecture of your model like the trade-off between its depth and width and the learning rate the optimizer but assuming all of those things are either fixed or chosen optimally you have two variables that you can easily change in your training processes like the general size of your model dictated by the number of parameters in it and the amount of data it was trained on usually described in the amount of tokens it has seen during training and the way people have been describing this for quite a while now is in form of this scaling law uh initially proposed by Hoffman and cters in their paper on optimality of pre-training from a compute standpoint where we will see three distinct terms of loss of a model one is error that is recoverable on your training set that your loss function has some innate error inside of it another term that describes how your loss scales with respect to your number of parameters and the a third term that describes how it scales with the amount of data that your model has seen and differ since part about this scal low on its own is that it sort of decouples data from the number of parameters and numerous papers reproduced this idea that those two terms are indeed sort of decoupled and you can print them separately and make some and infer some ideas about optimality from this scaling law and as you can see this scaling law on its own originally didn't include any idea about quantization or training precision and what we do which is not a Noel idea of of its own because it was used by a few other papers trying to describe scaling loss of precision is we introduced some sort of efficiency Factor like an effective parameter count multiplier that depends on the Precision that you're trading in and also the quantization method itself but L just denoted by Precision that multiplies your parameter account and doesn't affect the data and the inat parts of your scaling law so basically what we're saying is is that quantization during pre-train only has effects on the parameter count of your model in the form of an effective multiplier and by setting this effective multiplier of precision of full Precision to be one will recover the original scaling law and sort of normalize this effective scale for all other precisions and to actually somehow use this scaling law for to make predictions what we need to first to do is to fit this scaling law in the sense that we need to fit parameters a b e Alpha Beta And also effective multiplier for every single Precision that we want to analyze by training a number of models and fitting this the difference between the predicted performance and the actual performance with some error function and here we follow the setup of Hof in cters where they used I think uh reach regression on top of logarithm of those L values to fit all of those parameters and what we obtain is that this effective parameter count we fixed it to 60 to one for 16 bit and we say that 8 bit produced somehow effective parameter count that is larger but we theorize that this is just noise because it probably cannot be larger and what it means is that 8bit pre-training with Quest is practically in distinguishable from 16bit pre-training in the sense that it has exactly the same effective parameter count and you don't lose any per any of the performance because you quantize so the predict the performance and by extension the actual performance of mods quantized to 8bit will be exactly the same however for hard cotization we see that it starts to drop off rapidly for example for 4bit this effective parameter count is7 for 3 bit this multiplier is4 and for one bit like the binary quantization it's actually just 2% meaning that from this point of view of scaling loss a model that has a billion parameters for example quantized to one bit actually is practically the same as the model that only has 20 million parameters of full precision and we after feing those effective parameter counts we can try and analyze of what it exactly says about quantization and if we it would make sense for this effective parameter account to only get lower as you incre decrease your bitwidth what is actually optimal and to do this we first of all Analyze This C law in a very specific regime and this regime is based on the idea that in the shinilla paper it was determined that from a compute from the training compute standpoint the ratio between D to n for optimal pre-training was around 20 but nowadays mod meaning that for every parameter you had 20 tokens in your training set but nowadays models are usually trained with this ratio often larger than 1,000 meaning that for every single parameter in your model you have more than a thousand tokens in your data set and this is dictated by the idea of that uh runtime compute of your model is not free it's not negligible and it makes sense to train smaller models for way longer Beyond some compute optimal bound to then have a performance model that is actually cheap to run because it's so small and we can take this idea to extreme by just taking the limit of d to end to Infinity to arrive at this scale in L the over training regime where we will simply practically discard the second term of this scaling Lo the data dependent term by saying that we trained our model to saturation to full saturation with data and obtained the best possible loss possible for this model size because again what interest as is not the optimal training but how cheap we can run the model afterwards so this says that this would mean that we TR can train our model to saturation and have the best performance at a certain runtime costs during and what we can remember is that runtime costs during inference can actually be described conation has huge effect on brand time cost during inference in the sense that lower Precision have proportionally cheaper inference in the sense that the maximum throughput of your model and latency is actually inversely proportional to your Precision like if one bit quation is 16 times faster than full Precision 16 bit numbers and what we can say is that we can formulate this in a sense that we will describe our runtime costs F to be proportional to precision and to the number of parameters in our model and this would indeed describe for example the scut of our model and then what we might want to do considering that we're interested in INF R time is we might want to ask a question such as what's the optimal n NP such that the runtime cost is below some threshold that we put ourselves regarding the inference cost of our model and then we can write we can write this minimization problem where we want to minimize the loss of our model with respect to the number of parameters of our model and precision such that we respect this maximum runtime cost that's proportional to both number of parameters and procedure and when writing this scaling law uh we see that first of all it has an obvious solution where you just set F to F Max and then choose this number to be as large as possible so this ratio of effective parameter multiplier divided by precision is actually what determines what Precision you should choose in this overtraining regime and if we plot this quantity like effective P divided by P we see that it picks around four bit quantization so first it grows almost linearly because effective p is practically one for very high precisions and one divided by P increases but then at some point effective P starts to drop off rapidly and even the additional benefit of faster inference doesn't save you from the fact that your loss is pretty bad and as such you have some sort of Peck a maximum somewhere and we show that this Peck for Quest is at four bit per parameter meaning that our four our method is optimal for overtrained models around four bit per parameter and this is what we mean when we say the that we TR our method is optimal for pre-training at 4bit it means that it's optimal in terms of this runtime cost of overtrained models during inference so any questions about how we came up to this conclusion came to this conclusion Okay so if not I'll continue because we don't even have that much time so what you can also see in this plot is that we have quest for integer grids but we also have quest for fp4 what I mean by that is that quantization is just rounding to a grid and we have a way to optimize uh how we actually round to this grid for for practically any any arbitrary G by saying that we optimize it in a sense of quadratic errow rounding the normal distribution to that grid and as such we can also optimize rounding normal distribution to a floating point4 grid and we can train models that would be inferenced in the fp4 format and we can also fit the effective P for those models and on this plot on this plot you can also see this effective p and an interesting thing we observe is that effective P for fp4 seems to be worse than for integer 4 meaning that the in4 is actually somewhat better for pre-training than fp4 and we have another idea why that might be happening because if we'll actually uh we had an idea about that quadratic error of rounding during forward path being important we explicitly minimize it with the scale optimization foration and if we actually take a look at how this quadratic error relates to the result in Fe effective multiplier we can see that there is some sort of dependency between those two values and fp4 appears to be simply worse both in terms of this quadratic error so it simply has higher quantization error of normal values normally distributed values to this grid and as a result it also has worse effective parameter multiplier but our method is still stable for fp4 and still produce models that are definitely par optimal to 8bit and 16bit in the same regard as we just discussed for integer GDs and one other thing we can do for our method is we can also combine it with sparcity more specifically we're interested in the structured sparsity of that are supported on the latest generation of Nvidia gpus where out of every four consecutive values in The Matrix exactly two are zero and this allows us to store those matrices in a very highly compressed format and also run matrix multiplication with them very fast because of a specific colel support and what we find that it's also produces stable trading but it's sometimes but it's somewhat worse than integer form in terms of this optimality we discussed before and one last thing that I mentioned about our results is that our method can naturally be used for weight only quantization we simply don't have to quantize activations we won't have as good of a kernal support for the inference but there are ideas of why it's it would still make sense to quantize weights because for example when running decoding like generating one token at a time with llms you're actually more interested in how much your weights are compressed in terms of memory then this SC will support like the maximum throughput and that's why it makes sense to analyze weight on lization as well and if we plot this effective p over P for weight on lization we'll see that we find that 3 bit is actually optimal for this setup and yes it can be analyzed in exactly the same way we analyzed full quantization and we get that for weight on quantization quantizing a little harder makes more sense which is sort of expectable and in general that's about it with the results that we have and if you have any questions I'll be happy to answer them
