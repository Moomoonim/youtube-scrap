# Efficient Inference with Command A: Optimizing Speed and Cost for Enterprise AI

- 영상 링크: https://www.youtube.com/watch?v=H57yyseFLs8
- 채널: Weights & Biases
- 업로드일: 2025-06-27
- 자막 언어: en
- 단어 수: 약 3675개

---

## 스크립트

Hi everyone. Uh I'm Ya. I lead the efficiency research team at cohhere. Today together with Richard uh we're representing our team to present some of the optimizations we did for run inference for our command a model. Okay for those of you who don't know command A. It's our most upto-date model for cohhere. Some artifacts about it. It's 111b parameters and the contact lens with we support is up to 256k. And on coher API uh we actually reach 156 tokens per second. Later on we're going to talk about how we actually do it. If you don't care about that speed uh we can make it to run only on two H100s or two A100s minimum. So that's really cheap considering for a model this size. And what's even better uh it's already open source on hugging face. So if you guys are interested feel free to go try it out and see if it's the best uh one for your use case. Okay. only uh okay let's start with the model architecture uh I'm going to I'm going to talk about why we can able to make it so cheap so here on the left side you can see the full and square attention that means every token will attend to all the tokens before it including itself what we're employing here is the sliding window attention it means for every token it only attends to a fixed size of token so you can see here um say this is the window size then every token is only attending to up to let's say if the window size is 4k then every token is only attending to 4k tokens before it and since we are doing causal model here so I'm drawing this uh right triangle to indicate only the lower half of the window is actually being intended that's how we make the attention sparse and for the whole model what we do is interleaf sliding window tension what we do with command a is at one to three ratio which means when we have one full attention layer it comes with three sliding window attention layers and repeating like that. One full, three sliding window attention, another full and another three more uh sliding window attention. So together with this whole structure, it makes our model very sparse and cheaper and faster to run. But to achieve the 156 tokens per second that was mentioning, this certainly is not enough. To improve this throughput and latency, we need to do more. Here I'm going to talk about speculative decoding. That's one of the major optimizations we do for our inference. Let's start with what speculative decoding is. So speculative decoding speeds up generation from auto reggressive models by computing several tokens in parallel. That's the key advantage of speculative decoding and without affecting output quality. There are three major ways to do it. Uh the first is using a separate draft model. How it does it is usually you train a separate draft model that is very small and you use it to propose draft tokens and then later on use the big model to verify those tokens. So for example, if you have a 111B model like command A, you can treat a much smaller one could be even one B or 2B billion tokens and you can propose just use that 2B billion tok sorry 2B billion sorry 2B uh parameters. You can use that small model with only 2B parameters to propose some tokens and then verify by the large model. The second way is self speculation. Some major ways to do it one is Medusa and or like a multi-token prediction or eagle. So the key difference is instead of training a separate draft model here, what you do is you modify the base model to add some other components to run together with this single one forward path with your big model and to predict multiple tokens at the same time. After that's the same story you need to verify with the large model to make sure of the quality. And the third way is called rule based or heristic. Uh one of uh some of the popular ways are prom lookup decoding or you can call it ungram or gramma con constrain the decoding. The main idea is you're going to use some rule or some huristic to predefine what are the possible next tokens from your current context and then same story you're going to need to verify with your large model. So one typical use case is for code editing because when you editing code the majority of the context still remains the same. So it's very easy for you to find the very possible next tokens by the existing context and then you verify with the larger model. In this way it speeds up the u inference much much more. Okay. Uh today uh I'm going to talk about Medusa. That's one of the ways we use for our specular decoding. I'm going to share some insights from what we see from our training and the final gains. Here is a quick demo for us with the Medusa. You can see on the left side is the case that without with Medusa and the right side is without Medusa. You're going to see the speed difference very huge. This is actually a very simple prompt just asking the model to count even with such a simple prompt you can see a huge difference in the speed. Before we dive into more, I want to talk about what is Medusa. So here's the structure. You can see on the left side, that's the standard typical large language model. You start with the input and you convert it to embeddings and then after the embeddings, you let it go through some transformer layers and after the transformer layers, you have the hidden state, you're going to feed it into the LM hat to predict the next token. The key point for Medusa is after you get the last uh layers hidden states you fit it into some Medusa has at the same time which means in one forward pass you're going to have in this particular example you're going to have four hats during prediction at the same time. So the original LM you can see the prediction it has uh is 8 I s and the three Medusa has going to predict after that one. So the original LM hat going to predict T+1 and the Medusa had one going to predict T+2 and so on. So the Medusa had three going to predict T+4 which means for this one single forward pass we're going to have four draft tokens at the same time. And then after that we're going to feed all those four tokens to be verified by our large B base model. If we got lucky all four are correct which means for one forward pass we're predicting four tokens instead of one. Okay. Now uh I want to say thank you to wisd and biases. So all our training and all the ablation jobs that we are actually running with wisdom biases. It makes it super easy for us to compare all different runs do ablation analysis. And on this particular screenshot you can see we have so many different jobs but it's still very very easy for us to compare the performance on different metrics at the same time. Okay. Now I'm going to share some insights we have from the data. So one key question people have when training speculations especially self speculations is should we use synthetic data or should we use original data. What we see is for large models like command a 111b since data is helping but very minimum especially for us since we have the original training data we use for 111b. So for large models like this, it's actually not too much difference and you can better stay with the original data since generating synthetic data for a model at this size for the amount of data you needed is very expensive. However, for much smaller model, uh we do have command R sevenB for those of you who don't know. Um it's the same generation of models with command A, but it's much smaller one. It's only seven billion parameters. For models smaller at that size, synthetic data going to help a lot more. So one idea we have for this is because for the smaller model they actually fit to the ground truth uh less in this case the purpose of speculation is actually to align the speculations logistics together with the main model since the original model is not well aligned to the ground truth data using synthetic data going to help a lot more here and the draft quality actually scales with the data set but of course only to a certain extent. So on the charts on the left side you can see uh the dots are a little bit uh not clear on the uh axis but the few dots we have is from 10% to 25% to 50% to 100%. So you can see for all four different kinds of tasks it's showing the same pattern that on 10% to 25% you see a big increase but from 25 to 50 is slowing down and from 50 to 100 that's very minimum. So for our particular setup for the 7B model uh if you want to train on the synthetic data up to 50% you're going to get the best performance but it doesn't make much more sense to uh go more beyond that and here the percentage means how much of the synthetic data is generated for the original training data prompts that's it for data and now I'm going to show you about the final gates how much did we achieve with Medusa so we actually were able to achieve 2.17 times speed up with a TP2 setup batch size one on coding but the gain is actually going to decrease if say the batch size increases or the temperature increases or the TP which is the tensor parallelism we use for inference if that increases or uh the tasks become less deterministic like creative writing the idea is for tasks like coding because it's uh very much deterministic for so it's much easier for the speculation a head or a draft model itself to predict the next tokens. But for creative writing since it's a much harder task and it's more um much much less deterministic. So it's going to be harder for the speculation. And here on the left side you can see um this is a comparison for different type of tasks. From code you see we can get to 2.17. However, as the task gets less deterministic from code um to reasoning and to translation to creative writing, when it gets to creative writing, it it's only 1.6 times. And on the right side, we you can see that's the effect of temperature. So all of them are actually using rejection sampling, which means if the speculation is not exactly matching the main model's prediction, we will discard it. So the quality remains the same for all different setup. But as the temperature increases you can see the speed up increase is actually uh dropping. That's uh our main idea behind this is because high temperature is actually making it less deterministic which increases the re rejection possibility. Okay. So Medusa actually leaves the memory bound bottleneck because it's doing parallel decoding. However, uh as we said before, if the batch size increases, it actually leads it to more of computebound. And in this case, the speed up going to drop a lot. So, you can see here with our setup with command A, uh we can actually got to batch size eight, we're still seeing very very good speed up, but if it gets to batch size 16, it's dropping a lot. And when we get to batch 632, the speed up is so much less. And the difference from the previous charts is here the uh y-axis is actually the percentage of gain. it's not the absolute number anymore. And on the right side, uh you can see for the code data set across the code data set, if we increase TP for inference because when TP goes up, there's a lot more communication need to be done and that's actually adding a much much more computation cost which means the actual gain going to drop too. Okay. Um so one of the main features we offer by cohhere is actually guided generation. Now Rishi going to talk about more on how do we actually integrate specular decoding together with GT generation. Thank you. Yeah. Yeah. That was a nice talk. Yeah. Am I audible? Are you on? Yeah. So we'll start with speculative decoding and guided generations. But before we get into how we include speculative decoding with guided generations, let's just go back and see what actually is guided generation or guided decoding. So guided generation is one way by which we can guide a particular model to output a particular structure or in a particular format. So it doesn't hallucinate or deviate from certain sort of formats that we want say like JSON, your Python, your SQL. So you want your model to adhere to a certain sort of a schema so that you use a technique called as guided generation. So let's start off with an example. So say we have an LLM that generates the following an open braces and the fruit ID. Okay. Now without gadget generations what would happen is that you have all the tokens in your OK cap size that are valid. You have apple, orange, tomato, you have serial numbers as well as 1 2 3 4 all of these. The model decides that okay the best token that I need is an apple. But now the issue comes here is that it's a fruit ID which means you require its intention to be either a number or it should be a certain sort of serial code. Now what we do is we add in a sort of a schema. The schema over here actually helps us understand and make the model you know follow a particular way by which it can actually an output based on a particular sort of format for a particular application like JSON. So over here you can see in the schema that the fruit ID is said that it should be of the type integer. The model now understands that okay I need the I need to generate an output such that my fruit ID that I have as my key has an output as an integer over here. So again now the model starts off with a guided decoding method where the previous time step it did output uh open braces and a fruit ID. Now with guided decoding over here we only the model has an option only to select from specific number of valid tokens rather than all the tokens that are present in the vocabulary for you. So using the guided technique we are actually able to mask some of the tokens for the particular format. So over here you can see that now your valid tokens are serial numbers like 1 2 3 4 and so on. So this way uh using a guided decoding method you are able to get the model to adhere to a particular schema and a format or structure that you would want. Now moving on to something called as dynamic guided decoding. So over here we have different flavors of uh guided generations. They have the JSON which is a standard JSON format that would be provided a JSON schema where the users can actually provide a schema for your model to adhere to and you have tools and the way tools is used is some sort of like a backend you could say to agentic workflows where you want your model to actually use grounded answers by contacting some sort of API services in order to get responses and not hallucinate. So with with respect to tools what happens is your model would actually intelligently understand what sort of schema needs to be formatted for what sort of a tool which means that if I say that what is a weather in Canada the model intelligently understands that it requires the weather app and using the prompt details it's able to create a structured JSON payload for you for say the location and Canada and which goes as service to the weather app. Now in this case we know that we had to use the structured format just for the portion of the JSON that was generated but the model even has to do some amount of generations before it which means you have generations which are non-guided initially after which you need to detect that okay I'm going to output a JSON format for which I need guidance and then I need to stop my uh guidance and continue back in my non-guided format. So this is where dynamic GG comes into play where you are able to tell the model on when to start start a guidance and when to end its guidance. So let's take an example over here. Uh let's take that the TK of one at the time step K minus one it's at a non-guided method at the moment and the token generated is at non-guided method. At TK we end up generating something called as a part of a trigger token. So you have the action which is generated as well in a non-guided method. At time stamp TK +1 you have it generated as back ticks and at time TK plus2 you have it generated as JSON. Now these trigger tokens are used to enable guided generations for you guys. And this way you're able to tell the model you know what once on seeing these trigger tokens you should start off your guided generations. The way this works is we pass these tokens output tokens into something called as like a trigger token matcher. This trigger token matcher matches that is the tokens that are generated previously to some K steps equal to the same trigger action that I have mentioned. If yes, the next time step starts off with guided decoding. So a tk + 3 which is at the time step after generating a trigger tokens would end up generating a token that has been guided. So this brings us to the point where how uh how guided decoding and dynamic guided decoding was used before speculative decoding because this was still an auto reggressive method by generating one token at a time. Now we go on to how dynamic decoding is used with uh speculative decoding. Over here at time k minus one we had some tokens that were generated in the non-generative mode. At tk + one you now instead of having just one token have four tokens generated as you all can see. So you have the action, you have your back ticks, you have your JSON and then you have your open braces and these are all your speculative draft tokens at the moment. The one issue here you guys would see is that you have the open braces generated in a non-guided mode. The thing with guided generations is you require a certain sort of a finite state machine or a certain sort of a node transitions in order to understand how do I transition from one particular token generated in a guided mode to another token over here since we have the open braces being generated in a non-guided mode the guided generation techniques FSM or I would say a sort of a graph does not recognize the fact that we have already started our guided generations. Okay. So what would happen in this case is that we go under the trigger matcher over here and at the time of trigger matching we see that action back ticks and JSON are generated rightly and that's a good thing for us and we know that at the next step we need to start off with a guided decoding method. So in order to make sure that we don't uh screw up or we don't mess around with our finite state machine at that point what we do is we end up dropping out our open braces at that time dropping that out when we go on to the next time step what happens is we can start off and enable our guided decoding and a tapus one step you would end up generating your uh speculated tokens based on the guided mode over here and you guys need to know that the G the guided generation will only be enabled when the previous draft tokens are accepted. If say any of those draft tokens on the next step are not accepted say for example action or for example JSON you do not activate your guided generations at that moment because you need to satisfy the rule of making sure that you have your trigger tokens uh present in your guided uh mode and sorry you have your trigger tokens present in the non-guided mode in order to activate the guided generations. So your open braces, your open quotes, label and close quotes all now are in the guided mode and have been generated based on the guided technique. Yeah. So this is it with respect to um guided decoding. Uh yeah, this was something that was different because there are many corner cases as well with respect to it. Uh yeah and so thank you for this uh session. Yeah.
