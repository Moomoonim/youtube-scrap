# Transform Edge AI Applications with Generative AI Using NVIDIA Metropolis Microservices for Jetson

- 영상 링크: https://www.youtube.com/watch?v=DTVYAND5X8Q
- 채널: NVIDIA Developer
- 업로드일: 2024-01-23
- 자막 언어: en
- 단어 수: 약 316개

---

## 스크립트

in this gen demo what you'll see here is the Nano&nbsp; all model a zero Shan detection model being able&nbsp;&nbsp; to detect based on prompts so here we provide&nbsp; a prompt of person via simple Cur commands so&nbsp;&nbsp; simple rest APR it's able to detect a person now&nbsp; let's add a hardhead class and it's a simple API&nbsp;&nbsp; request and now it's able to detect a hard ad&nbsp; now we can add the classes let's add West and&nbsp;&nbsp; now it's able to detect the West now let's add&nbsp; all three of these objects a person a hard hat&nbsp;&nbsp; and a vest and it's able to detect all three of&nbsp; these objects now let's add items on this floor&nbsp;&nbsp; so let's add a pallet let's add this boxes and&nbsp; let's even be specific let's add this magenta&nbsp;&nbsp; color box and now it's able to detect all of these&nbsp; items so that's the power of this model the Z shot&nbsp;&nbsp; detection model you can change new objects you can&nbsp; detect new objects and it's extremely powerful the&nbsp;&nbsp; other thing that we have added is we are able&nbsp; to collect all of this meta metadata and we're&nbsp;&nbsp; storing it in a redest database so here you're&nbsp; seeing an output of all the metadata that's&nbsp;&nbsp; been generated which get stored into a database&nbsp; the other thing that we add it is we're able to&nbsp;&nbsp; also visualize F our dashboard we're able to&nbsp; see system performance system metrics like GP&nbsp;&nbsp; utilization CP utilization and more and lastly we&nbsp; I've also connected this to a mobile application&nbsp;&nbsp; this is a reference application that we have&nbsp; provided to kind of help you get started to&nbsp;&nbsp; learn how to use the rest apis to be able&nbsp; to build your own application here we are&nbsp;&nbsp; getting the data from our vstd we're just doing&nbsp; a web RTC stream and playing it on this mobile application
