# Accelerated Time Series Analysis with Polars on NVIDIA GPUs | Accelerated Data Science Series

- 영상 링크: https://www.youtube.com/watch?v=KH5ZCQhmsnU
- 채널: NVIDIA Developer
- 업로드일: 2025-02-12
- 자막 언어: ko
- 단어 수: 약 1781개

---

## 스크립트

안녕하세요, 저는 Matt Harrison입니다. 코드 변경 없이 시계열 분석 코드 속도를 향상시키는 방법을 보여드리게 되어 기쁩니다. 저는 Effective Poers라는 책을 썼는데, 이 책에 있는 예제를 활용하여 NVIDIA 가속기를 사용해 코드 속도를 높이는 방법을 보여드리겠습니다. 설치 방법부터 시작해서 CPU를 사용한 시계열 분석과 GPU를 사용한 분석을 차례로 보여드리겠습니다. 설치는 매우 간단합니다. 가장 중요한 것은 GPU가 있는지 확인하는 것입니다. Google Colab을 사용 중이라면 여기에서 런타임 유형이 올바르게 설정되어 있는지 확인하세요. 저는 L4를 사용하고 있는데, L4는 GPU를 지원합니다. 이제 준비가 완료되었습니다. 다음 명령어를 실행하면 제대로 설정되어 있는지 확인할 수 있습니다. 보시다시피 NVIDIA L4가 설치되어 있으므로 문제없이 진행할 수 있습니다. 하드웨어 관련해서는 몇 가지 소프트웨어를 설치해야 하는데, 다행히 설치는 매우 간단합니다. 저는 요즘 속도가 매우 빠른 UV를 선호합니다. 추가 인덱스 URL과 pullers, 그리고 대괄호 안에 GPU를 입력하고 qdf pullers cu 12도 입력해야 합니다. 보시다시피 설치가 매우 빠르게 완료되었습니다. NVIDIA가 UV와 호환되도록 신경 써준 것에 정말 만족합니다. 이전에는 이런 종류의 프로그램을 설치하는 것이 꽤 까다로웠는데, 이번에는 정말 쉽고 몇 초밖에 걸리지 않습니다. 자, 이제 데이터를 로드해 보겠습니다. 주식 데이터 세트를 로드하겠습니다. 그리고 pullers도 로드해 보겠습니다. 보시다시피 pullers 114를 사용하고 있습니다. 데이터를 로드하기 위해 scan 함수를 사용하겠습니다. pullers의 scan 함수에 익숙하지 않으신 분들을 위해 설명드리자면, scan 함수는 지연 실행 함수입니다. 가능한 한 지연 실행 함수를 사용하는 것이 좋습니다. 이렇게 하면 속도를 최대한 향상시킬 수 있습니다. 데이터 프레임을 보면 데이터 프레임이 아니라 쿼리 계획이 표시됩니다. 지연 실행 데이터 프레임을 사용하면 이렇게 됩니다. 데이터에 접근하려면 collect 함수를 실행해야 합니다. 실행해 보겠습니다. 데이터 크기를 확인해 보니 3,600만 행의 데이터가 있네요. 다음으로 할 일은 데이터 구조를 살펴보고 어떤 데이터가 있는지 확인하는 것입니다. 먼저 처음 10행을 가져와서 확인해 보겠습니다. 날짜, 시가, 고가, 저가, 종가, 거래량, 그리고 티커 심볼이 있네요. 주식 데이터인 것 같습니다. 그리고 여기에 인덱스 레벨도 보이는데, 아마도 pandas에서 내보낸 데이터인 것 같습니다. pullers에는 인덱스가 없지만 데이터에 포함되어 있으니 제대로 읽어온 거겠죠. 데이터 유형도 확인해 보겠습니다. pandas와 달리 pullers에서는 데이터 유형이 여기에 나열되어 있습니다. 명시적으로 확인하려면 `d types`라고 입력하면 됩니다. 정수 두 개, 날짜/시간, 부동 소수점 몇 개, 그리고 문자열 하나가 있네요. 데이터가 잘 정리된 것 같습니다. 이제 집계를 시작해 보겠습니다. 첫 번째 집계는 바로 이것입니다. 제가 일련의 집계를 작성했습니다. 여기서 작업들을 설명할 때, 체이닝이라는 개념이 익숙하지 않으시다면 Poers를 사용할 때 매우 중요하다는 점을 알아두세요. Poers에는 쿼리 엔진이 내장되어 있어, 지연 실행(lazy execution)을 통해 작업 속도를 높일 수 있습니다. 예를 들어, 아래에 필터와 같은 작업이 있다면, 파케 파일에서 행이나 열을 읽어 필터링해야 할 시점을 미리 파악하여 매우 빠르게 처리할 수 있습니다. 자, 여기 체이닝 과정을 보여드리겠습니다. 지연 실행 데이터프레임을 가져와서, 이 줄에서는 날짜/시간 열을 연도 열로 변환하고, 연도별로 그룹화합니다. 그런 다음, 셀렉터 모듈의 숫자 선택기를 사용하여 모든 숫자 열을 집계하고, 평균을 계산합니다. 마지막으로 `collect` 함수를 호출하면 쿼리 플래너가 최적의 방법을 선택하여 결과를 보여줍니다. 여기 결과가 있습니다. 위에서 셀 매직 타임을 보면 밀리초가 걸렸다는 것을 알 수 있습니다. 또한 이 작업은 CPU에서 실행되었습니다. 지금은 CPU에서 실행되는 일반적인 pollers 코드입니다. 나중에는 GPU에서 실행할 것입니다. 또 하나 주목할 점은 여기에서 연도별로 그룹화했지만 연도가 정렬되어 있지 않다는 것입니다. 왜 정렬되어 있지 않은지 헷갈릴 수 있는데, pollers가 pandas와 같은 라이브러리에 비해 좋은 점 중 하나는 병렬 처리가 가능하다는 것입니다. pollers는 사용자가 연도별로 그룹화하려는 것을 알고 있으므로 모든 집계 작업을 병렬로 실행하고 결과를 데이터프레임에 저장합니다. pandas는 이처럼 똑똑하지 않아서 이러한 작업을 순차적으로 처리하고 시간이 훨씬 오래 걸립니다. 하지만 pandas의 단점은 연도가 정렬되어야 한다는 것을 알지 못하고, 완료된 순서대로 연도 데이터프레임을 반환한다는 것입니다. 실제로 다시 실행하면 결과 순서가 달라지는 것을 볼 수 있습니다. 자, 이제 다음 집계를 해보겠습니다. 제 데이터를 다시 생성한 다음, 두 개의 기준(티커와 연도)으로 그룹화하겠습니다. 그리고 각 연도별로 모든 티커에 대한 평균값을 계산하겠습니다. 실행해 보겠습니다. 결과는 다음과 같습니다. 이 작업을 여러 번 실행하면 행 순서가 달라질 수 있습니다. 이는 pullers를 사용하여 속도를 높이는 데 따르는 단점 중 하나라는 점을 알아두세요. 이 문제를 해결하기 위해 여기에 정렬을 추가하겠습니다. 실행해 보면, 정렬하는 데 걸리는 시간이 약간 더 길지만 정렬에는 시간이 더 걸립니다. 정렬로 인한 성능 저하가 있지만, 데이터를 이 순서대로 얻고 싶기 때문에 감수하겠습니다. 다음 셀을 실행해 보겠습니다. pullers 의 기능 중 하나인 동적 그룹화( Group by Dynamic)를 사용하겠습니다. pandas에 익숙하신 분들은 아시겠지만, pandas와 pullers의 정말 좋은 기능 중 하나는 날짜 열을 기준으로 그룹화하되 다른 열은 제외하고 그룹화할 수 있다는 것입니다. 연도별로만 그룹화하고 싶습니다. 예를 들어 3개월마다, 2주마다, 또는 3일마다 그룹화하고 싶습니다. pandas와 pollers 모두 이러한 기능을 제공하며, pollers에서는 `group_myDynamic`을 사용합니다. 여기서 `datetime` 열을 `sorted`로 설정하면 pollers가 정렬 순서를 유지하도록 자동으로 인식합니다. 따라서 정렬 문제를 해결하는 좋은 방법입니다. 이제 `group_buy`를 살펴보겠습니다. ` group_buy`는 날짜/시간을 인덱스 열로 사용한다고 명시합니다. pollers에는 인덱스 열 개념이 없는데, 이는 pandas에서 인덱스 열을 사용하는 것을 보고 알게 된 사실입니다. pandas에는 인덱스 열이 있지만, 이 인덱스 열은 그룹화할 대상을 나타냅니다. 이 코드를 실행하면 인덱스 열이 왼쪽에 표시됩니다. 또한 `every` 옵션이 있는데, 여기서 핵심적인 역할을 합니다. 연도별 열을 따로 만들 필요 없이 1년마다 그룹화할 수 있습니다. 2년마다 그룹화하도록 변경하면 2년 단위의 집계 결과를 얻을 수 있습니다. 매우 강력하고 유용한 기능입니다. 이제 ` Group`을 선택하겠습니다. 티커별로도 그룹화할 수 있으므로 기본적으로 이중 그룹화를 할 수 있습니다. 연도별로 그룹화하고 티커별로 그룹화할 것입니다. 왼쪽에는 티커가 있고 그 안에 각 연도가 있습니다. 평균을 계산하고 이를 수집합니다. 이 코드는 실행하는 데 약 1.3초가 걸립니다. 자, 이제 두 주식의 성과를 비교하고 싶다고 가정해 보겠습니다. 다음으로 그 과정을 살펴보겠습니다. 자, 여기 코드가 있습니다. 코드를 살펴보면 '연도' 라는 열과 ' 월'이라는 열을 만들고, 티커, 연도, 월별로 그룹화한 다음, 최댓값을 모두 추출합니다. 그런 다음 티커, 연도, 월 순으로 정렬합니다. 이 코드는 실행하는 데 2.28초가 걸리고 13만 개의 행이 반환됩니다. 이제 모든 티커에 대해 연도와 월별 집계 데이터가 있습니다. 좋습니다. 데이터가 집계되었지만, 저는 다른 작업을 하고 싶습니다. 일반적으로 두 개의 다른 주식을 비교할 때 제가 하는 방법은 피벗(pivot)이라고 하는 작업을 하는 것입니다. 비교하려는 두 주식을 가져와서 여기 열에 표시되도록 한 다음, 두 주식을 별도의 열에 넣고, 각 주식의 연도별 및 월별 값을 다른 열에 넣습니다. 이렇게 하면 두 주식을 쉽게 비교할 수 있습니다. Polar에서도 이 작업을 할 수 있습니다. 집계를 하려면 위에서 보여드린 것처럼 하면 됩니다. 그런 다음 다시 피벗을 선택합니다. 여기서 ' 인덱스'라는 용어를 사용하는데, Polar에는 인덱스가 없습니다. 하지만 가장 왼쪽 열을 기준으로 피벗을 적용하고, 날짜는 가장 왼쪽 열에 유지합니다. 그런 다음 '티커' 열을 기준으로 피벗합니다. 티커 열을 기준으로 피벗하면 해당 열의 값을 가져와서 열 헤더로 만들고, 그 다음에는 종가 값을 넣습니다. 이 프로그램을 실행하면 왼쪽에 ' 인덱스'라는 항목이 있고, 날짜가 다시 표시되는데, 이건 인덱스가 아니라 일반 열입니다. Polar는 인덱스 기능을 지원하지 않거든요. 그리고 위쪽에는 모든 티커 심볼과 해당 종목의 종가가 표시됩니다. 두 종목을 비교하고 싶다면 아주 간단합니다. 비교하고 싶은 두 종목 열을 선택하기만 하면 됩니다. 이렇게 각 열이 티커를 나타내는 형식으로 데이터가 정리되어 있는데, 비교하기에는 좋지만 사실 최선의 방법은 아닙니다. 저는 시각화를 선호합니다. 시각화를 통해 훨씬 빠르게 정보를 파악할 수 있기 때문입니다. 그래서 두 종목을 선택해서 시각화해 보겠습니다. 일반적으로 Pism M Plot 같은 도구를 사용할 때는 데이터를 이 형식으로 유지하지만, Polar는 Altar라는 플로팅 도구를 지원합니다. Altar는 Altar 내에서 피벗 기능을 제공하므로, 여기서 피벗을 제거하고 x축에는 날짜를, y축에는 y축에는 날짜를 나타내는 선을 그리면 됩니다. Y 값은 종가를 원하고, 티커별로 색상을 지정하고 싶습니다. 여기서는 아마존과 애플로 필터링하겠습니다. 다시 말하지만, 지연 평가 데이터 프레임을 사용하는 장점은 이 방대한 파킹 파일을 스캔할 때 (3천만 행이나 되죠) 모든 행을 읽어올 필요 없이 특정 티커로 필터링하기 때문에 필요한 행만 필터링할 수 있다는 것입니다. 자, 실행해서 결과를 확인해 보겠습니다. 보시다시피, 이 기간 동안 애플과 아마존의 주가를 비교하는 그래프가 나타납니다. 이것은 제 책에서 영감을 받은 예시이며, 시계열 분석에서 흔히 사용하는 작업입니다. 이제 가속기를 사용하여 이 코드의 속도를 얼마나 쉽게 향상시킬 수 있는지 보여드리겠습니다. 다시 한번 GPU 런타임이 활성화되어 있는지 확인하세요. 보시다시피 L4가 활성화되어 있고 GPU라고 표시되어 있습니다. 이제 collect 함수에서 engine을 GPU로 변경하기만 하면 됩니다. 그러면 결과는 동일하지만 훨씬 빠르게 얻을 수 있습니다. 위에서 확인해 보면, ta1 코드가 CPU에서 실행되는 데 689 밀리초가 걸렸습니다. GPU에서는 절반의 시간밖에 걸리지 않았죠. 600밀리초 대 300 밀리초, 큰 차이가 아닌 것처럼 보일 수도 있지만, 데이터셋이 커지면 훨씬 더 큰 차이를 확인할 수 있습니다. 참고로, 이 코드를 pandas에서 실행하면 몇 초 정도 걸릴 겁니다. 따라서 GPU를 사용하면 거의 즉각적인 피드백을 얻을 수 있습니다. 코드는 동일하지만, ` collect` 부분을 GPU에서 실행하도록 변경한 것뿐입니다. 자, 다음 코드는 두 개로 그룹화하는 코드입니다. 티커 와 연도를 볼 수 있죠. 여기서도 GPU에서 실행하도록 변경한 것만 다릅니다. 보시다시피 GPU에서 두 배 더 빠르게 실행됩니다. 다음은 정렬 기능을 추가한 코드입니다. 실행해 보면 역시 매우 빠르게 실행되는 것을 알 수 있습니다. 이제 동적 그룹화를 실행해 보겠습니다. 실행 시간은 CPU에서 실행했을 때와 거의 비슷합니다. CPU에서 실행되는 것을 보면 무슨 일이 벌어지고 있는지 궁금할 수 있습니다. 현재 가속기가 베타 버전이기 때문에 NVIDIA에서 열심히 개발 중이지만 Polar의 모든 기능을 구현하지는 못했습니다. 예를 들어 동적 그룹화 기능은 아직 구현되지 않았습니다. 하지만 이 코드는 여전히 실행되지만 GPU 대신 CPU에서 실행됩니다. 코드가 오류를 일으키지는 않지만, 속도가 향상되지 않더라도 GPU를 사용할 때와 동일한 속도로 실행될 것입니다. 참고로, 이 코드를 Pandas에서 비슷한 작업을 수행하는 데 약 24초가 걸렸지만, Polar에서는 CPU에서 약 1.3초가 걸립니다. 많은 사람들이 Pandas에서 Polar로 옮겨야 하는지 묻습니다. 이는 복잡한 질문이지만, 일반적으로 그룹 구매를 많이 하는 경우 Polar로 옮기면 속도 향상을 가장 크게 체감할 수 있습니다. GPU에서 실행되는 부분과 CPU에서 실행되는 부분을 확인하려면 ` collect` 호출을 다음과 같이 변경할 수 있습니다. 이 기능은 코드가 GPU에서 실행되지 않으면 오류를 발생시킵니다. 보시다시피 " 동적 그룹화는 아직 GPU에서 구현되지 않았습니다."라는 메시지가 표시됩니다. 자, 다음 쿼리를 실행해 보겠습니다. TA5 쿼리인데, 티커, 연도, 월별로 그룹화하는 쿼리입니다. 이 쿼리를 실행하면 약 7배 정도 속도가 빨라집니다. GPU에서 정말 빠르네요. 이제 TA6 쿼리를 실행해 보겠습니다. TA6은 피벗을 수행하는 부분입니다. 피벗에 대해 알아야 할 점은 지연 평가 데이터 프레임에서는 피벗이 작동하지 않는다는 것입니다. 따라서 가속하기가 조금 더 어렵습니다. 피벗은 기존 열의 값을 알아야 새 열을 만들 수 있기 때문입니다. 지연 평가 데이터 프레임에서는 열의 값은 알 수 없습니다. 열이 있다는 것만 알 뿐 값은 알 수 없죠. 따라서 특정 연산은 지연 평가 데이터 프레임에서 실행되지 않으며, 피벗이 그중 하나입니다. 해결 방법은 피벗을 수행하기 전에 `collect` 함수를 호출하여 데이터를 비정규화된 데이터 프레임으로 변환하는 것입니다. laaz 데이터 프레임을 만들고 나서 데이터를 피벗할 수 있습니다. 실행해 보면 코드가 여기서도 잘 작동합니다. 이제 비교 준비가 된 코드입니다. 열에는 티커 기호가 있습니다. 마지막으로 ta7, 즉 플로팅을 실행해 보겠습니다. 여기서 유일하게 변경된 사항은 GPU를 사용하는 것입니다. 플롯이 여기서도 잘 작동합니다. 코드를 가속화하는 것이 얼마나 쉬운지 알 수 있을 것입니다. 설치 지침을 따르기만 하면 되는데, UV Super Fast를 사용하면 정말 쉽습니다. 그런 다음 collect를 GPU에서 실행하도록 변경하면 됩니다. 다른 변경 사항은 전혀 없습니다. 아주 간단합니다. 제 경우에는 이렇게 함으로써 최대 7배의 속도 향상을 확인했습니다. 이것은 작은 데이터 세트이지만, 더 큰 데이터 세트를 사용하면 속도 향상 폭이 훨씬 더 클 것입니다. 결과를 데이터 프레임에 수집했습니다. 여기 CPU와 GPU에서의 실행 시간을 보여주는 플롯이 있습니다. 거의 모든 작업에서 상당한 속도 향상을 볼 수 있습니다. 속도 향상이 없는 두 가지 작업이 있는데, 하나는 동적 그룹화이고 다른 하나는 피벗팅입니다. 여기 속도 향상을 보여주는 또 다른 차트가 있습니다. 여기서는 7배 속도 향상을 볼 수 있지만, 대부분은 2~3배 속도 향상을 보여줍니다. 시청해 주셔서 감사합니다. 이 영상이 유용했기를 바랍니다. 결론적으로 Polar는 빠릅니다. NVIDIA의 Polar Accelerator는 속도를 더욱 향상시켜 줍니다. 설치와 사용이 간편합니다. 저는 Polar Accelerator를 사용하면서 빠른 속도 향상을 경험했고, 더 큰 데이터 세트를 사용한다면 훨씬 더 큰 속도 향상을 볼 수 있을 것입니다. 저에 대해 더 알고 싶거나 Polar에 대해 더 자세히 알고 싶으시면 제 책을 읽어보시거나 소셜 미디어에서 저를 팔로우해 주세요. 좋은 하루 보내세요.
