# LLM Fast Model Loader Using SageMaker Python SDK

- 영상 링크: https://www.youtube.com/watch?v=lkxExSoglXM
- 채널: AWS Developers
- 업로드일: 2025-02-20
- 자막 언어: en
- 단어 수: 약 1709개

---

## 스크립트

Large language models are incredible, but loading them can be really painfully slow. I'm Stuart, and today I'm gonna show you how Amazon SageMaker Fast Loader can dramatically reduce your model deployment times. So the problem with traditional model loading is kind of like moving into your new house, and you're carrying every single box individually. So for massive 70-byte parameters like Llama 3.1, this process could take hours, and it's very consuming, significantly slow in computational resources. It takes up a lot of stuff. So this is why Fast Model Loader solves this by completely redesigning how we load models. Think of this way. Instead of sequential time-consuming steps, it uses a two-key technique. The first is weight streaming. So what is this? This is directly loading model weights from, say, S3 to GPU memory. And the second is model sharding. This is pre-dividing the model into uniform chunks, which helps for optimal loading. So I wanna show you a demo on this, but before we begin, you're gonna have to have these few prerequisites set up. First of all, you're gonna have to have SageMaker as an executional role, and this is gonna need the appropriate permissions. You will need access to a GPU instance. In the demo that I'm doing today, this is an ml.p4d.24xlarge. This is recommended for this example which I'll be showing. You'll also have to install the SageMaker Python SDK. So let's take a look at this Jupyter Notebook. The Jupyter Notebook demonstrates how to leverage Fast Model Loader, and this will improve our model loading time significantly. In this notebook, we'll see how the feature works. We're gonna be streaming model weights directly from Amazon S3 to GPU accelerators, bypassing the typical sequential loading steps that contribute to deployment latency. We'll walk through deploying this using Llama 3.1 70B model, and we'll use Fast Loader. This will show us a few things. It will show us how to optimize the model for streaming using model builder configuration or configure tensor parallelism for distributed inference. Then we're gonna look at deploying the optimized model to a SageMaker endpoint. And then finally, at the end of the demo, we'll test this deployment with inference requests. So first, in our AWS console, we navigate to the Amazon SageMaker AI. I've set up a SageMaker Studio here already just in advance, and here I've got a JupyterLab. I've already imported all of this code just to save us some time, and so here is the notebook. So let's see what this does. Let's start with our initial setup. First, we'll need to install the SageMaker SDK. This command here installs the 2.237.3 version of SageMaker SDK. I would always recommend to check the latest version in the documentation when you're implementing yourself. But as of this recording, this is the latest version. The next thing we're gonna do is set up our SageMaker environment. So this code block establishes our SageMaker session and retrieves the necessary role permissions. The get_execution_role function obtains the IAM role that SageMaker is going to use for all of our operations. We're gonna also set up the region, and we're gonna create a session object for managing the SageMaker interactions. Next, we are gonna configure the model builder. This configuration here sets up the model builder for the Llama 3.1 70B model. The schema builder defines how inputs and outputs should be formatted, which is really essential here for proper model interaction. The output path here specifies where our shard model will be stored. In this example, we're gonna store it in S3. This code sets up model builder to reuse previously optimized model shards instead of creating new ones. It specifically uses the Llama 3.1 70B model and points to an existing optimized shard, which was stored in S3 through the CUSTOM_MODEL_PATH. You'll see that this configuration includes basic input-output formats through the schema builder, necessary AWS missions via the role_ran, and specific deployment on a G5.48xlarge GPU instance. This is a really great approach, and it saves us time by skipping the optimization step by maintaining fast model performance benefits. However, we are running this code for the first time, so I'm going to leave this in. If I run this for a second time, all the shards would be in the S3 bucket, and I could just comment this code out. Now we've optimized a model using Fast Loader. This optimization step is very crucial because it configures the model for tensor parallelism, and we're doing this across eight GPUs. And we're preparing the model weights for efficient streaming. The ml.p4d.24xlarge instance type provides the computational power needed for this large model. The sharding configuration tells Fast Model Loader to split the model into eight parallel sections. This enables efficient distributed processing. The deployment process here begins by building the final model. After optimization, we build the final model artifact and deploy them to SageMaker endpoint. Our key configuration here are the instant type, memory requests, and the required number of accelerators. This line here builds the final optimization model using the model_builder object. The model_builder encapsulates the entire model-building process, which includes loading the model architecture, loading the weights, and also applying necessary optimizations. When we look at the next section of code, this handles two very important settings for SageMaker. This part here checks if the model is not currently sharded, and if it's not sharded, it enables sharding by setting the is_sharded_model to true. When we think about model sharding, think about it like it splitting a large book into smaller chapters, which could be distributed across multiple readers. The second part of this is the network isolation setting. Network isolation is like putting the model into a secure room with a restricted access to the outside world. This checks if our network isolation is currently enabled. If it is enabled, it disables this setting in the enable_network_isolation, and it sets this here to false. This part is important because for large language models like Llama 3.1 70B, which require substantial computational resources to run effectively, this creates a resource requirement here which has a specification of 200 gigabytes of memory and eight GPU accelerators. The way I like to think about this is like placing an order for a computer and its resources. It's similar to specifying requirements for our powerful computer. Like, the memory here is like ordering 200 gigabits of RAM. And the num_accelerators, well, that's like requesting eight GPUs. Here we use this type of configuration when we are gonna be deploying large language models that need significant memory running across distributed training access multiple GPUs. And this ensures the model always has enough computational power to run all of the resources effectively. And now finally, we have our model deployment. We start with our method call. You can see here this is method_model.deploy. This deployment are models to create SageMaker endpoints for realtime inference. Our instance type is specified as the ml.p4d.24xlarge. And then next we need to acknowledge the end user or end user license agreement. Sometimes this is referred to as EULA. And then after that, we pass the previously defined resource requirements. As you can see here, this resource or this deployment will create our HTTPS endpoint that can be used to get predictions from our model, and it will do this in real time. What I'm gonna do now is I'm gonna run this code, and then we'll head to the next section and test our endpoints. This takes a couple of minutes to run, so I'll pause, and we'll see the results in a second. What's happened here is this code has created a predictor object to interact with our endpoint. The payload here includes our prompt and also our generation parameters. We've got a few things to look at, so let's break this down. We have the max_new_tokens. This limits the response length. We have top_p, which controls the diversity in the output. And then we have the temperature. Now this affects how random and deterministic our responses are. So I'm gonna pause again while I run this. It will take a few minutes. We'll come back, and then we'll take a look at the result. So I ran the endpoint code, and we have the model's response. It's in the generated_text field. The response seems to be repeating itself, though, and this is due to the relatively low temperature. The model potentially here is getting into a loop, and the max token is actually cutting off our response. So here we're just gonna improve this code a little bit and help with our output. To improve the output, we can update the fields. We will update the max_new_tokens to 128 for a longer response. Next, we will update the top_p to 92, which is a slight increase, but it will give us a little more diversity. We'll also increase the temperature here, and we're gonna increase this to 0.7. And this will increase the creativity, and we can add then a new feature called repetition_penalty. What this does is it will prevent repetition. Now we've added these. We will just run this code again. It will just take a few seconds in this instance to come back again. So now we can see our output is longer, with absolutely no repetition either. Once you've run this code, it's always a good idea to clean this up, and you can clean this up by using these commands. These commands remove the endpoint and predictor to prevent unnecessary charges. This was a great demo, and Fast Model Loader represents a significant advancement in how you could deploy and scale your LLMs on SageMaker. In this video, we walked through the step-by-step process of implementing this feature with both the SageMaker Python SDK and SageMaker Studio. By using weight streaming and model-sharding techniques, you can dramatically increase model loading times. This is enabling a more responsive response for your LLMs, and it also helps scale your LLM-based applications. Remember, if you like this video, be sure to like and subscribe (bubbles popping) to the AWS Developers YouTube channel. And as always, happy coding. (bell chimes)
