# Continual Learning for Long-Running Agents: Agents That Keep Getting Better

- 영상 링크: https://www.youtube.com/watch?v=SVWmuJx0hHM
- 채널: NVIDIA Developer
- 업로드일: 2026-07-01
- 자막 언어: en
- 단어 수: 약 4564개

---

## 스크립트

Uh hello everyone. Yeah, I'm Jack Manong. I'm a founding research engineer at Prime Intellect. So, I did I think we've like learned a lot about agents today and like all the remarkable things that they do. And I actually find it quite crazy that like you could think back to like back in 2024. 2024 was probably the year that most people discovered agents were a thing. 2024 is when Cursor Agent came out. And before Cursor Agent, people were already using LLMs for coding. But you would your mental model of the LLMs was very much like a auto-complete of like oh when I write a comment for a function, sometimes it completes my function. Sometimes I'm halfway through some boilerplate implementation and it just finishes my boilerplate. But then like Cursor Agent came out and people were like oh you can actually just like prompt something in the English language and then you could just like you you can modify your code in this agentic way. But like even back in 2024, long-running agents weren't a thing. And like if your agent was running for more than 5 minutes, you would not expect it to still be doing anything useful. But then like here we are in like 2026 and you hear people talk about how they're like spending millions and millions of tokens. They're doing things like oh I'm about to go to sleep. So I like put Claude code on dangerously skip permissions and then when I woke up it solved my bug. And so yeah, the the agents run like significantly longer than I think like most people would have imagined back in 2024. So I think like anyone in the crowd who's a coder today, I think every time I'm like looking at people's screens, I almost never see people typing in like the IDE fashion. You're always seeing people like with multiple terminal sessions doing some sort of agentic thing. And so I would say that most coders in the room would not argue that like the models are actually really really useful. And so a lot of enterprises have also discovered this and so they're building a lot of agentic workflows. And um question now becomes more economic kind of questions. So, things like uh how can I make the models more reliable for my task? Or like uh how do I make the models more efficient uh like cost-efficient at performing my task? Or uh things like uh how can I make the model be able to perform my task fast enough that I can deliver the kind of user experience that I want for my product? So, today I'm going to be making the case that the answer for all of the above uh is that you want to be training your own models uh and in particular doing like continual learning for your agents. Oh. Okay. So, first I'll start by like what is the issue with uh long-running agents? And so, the issue with long-running agents is that yeah, if you have an agent uh going for very long, it's going to create a lot of context. And like you might have like used Oh. Oh. Can't. I'm trying to switch. Uh Where am I supposed to point to? Cuz I feel like I can't switch the slide. I point there. I point here. Oh, okay. Ah, okay. Yeah. So, like um if it the the this is the open claw model list. So, you will see that like a lot of a lot of the models I think these days already advertise like a 1 million token context. And first there's like one argument that okay, yeah, I actually need more than 1 million context. But even if in the 1 million uh uh context, uh anyone who has tried to uh do any sort of like long context task, you will know that like just because the model Oh, you changed figure? Okay. Let's hope this one responds better. Yes, okay. This one responds instantly. Okay. So, like uh so so any of you who've tried to do any of these agentic tasks, you know that like just because the model accepts a 1 million token context, it doesn't mean that it can reason across the whole 1 million context. So, you get this like behavior that some people call context rot. And so, you can kind of see this in the benchmarks. So, anytime there's like a big model release, basically now there's always like a long context section. And in the long context section, there will always be two benchmarks. So, the first benchmark is MRCR. So, MRCR is a needle in the haystack task. So, here we are basically trying to test like the model's ability to retrieve a particular piece of information in a very long series of text. So, if you ever try to read any of the MRCR tasks, it's basically they prompt the model for multiple like essays or like poems about certain topics, and then you ask it a question at the end to retrieve some information from what it generated. And you can see that as the context length gets longer, the performance gets worse. So, you can see that for like GPT-5.4, when your context is only up to 256K, you can do almost 80% on this information retrieval. But once you go like beyond to like in half a million to a million token context, GPT-5.4 basically halved the performance to somewhere around like 36%. And this is something that they fixed somewhat in 5.5. So, you can see 5.5 it still happens. So, it drops from like 87 74, but it's not as bad as it was for GPT-5.4. Yeah. So, so, so, so, that's for like information retrieval ability in the context. But like anyone who like tries to like code any agentic task, you know that like this like information retrieval thing is nice to measure, but it's not exactly what you want to be measuring. You actually want to be measuring the agent's ability to reason across the context. And so, one popular benchmark uh I started to be reporting, I think like starting last year, uh is GraphWalks. So, GraphWalks is basically a benchmark that is testing the agent's ability to do graph tasks. So, I think like all the computer scientist people, uh you probably learned about graphs at some point. Uh the basic idea is that we pass like a a list of nodes and a list of edges into the prompt. And uh we basically ask the model to perform graph uh questions. So, we ask things like, "Oh, uh what are all the parents of X?" Or like, uh "If you started at node Y and you did like a breadth-first search at depth two, uh uh list all the uh nodes that would be in the frontier." And you can see it's basically the same story that like um when you are at uh 256K uh token context, the models are able to perform this task decently well. But once you go close to a million token context, uh the performance just drops significantly. And so, okay, the the models are kind of uh bad at long context, right? And so, like uh uh what can we do about it? What can we do to still make our long-running agents work? Um And so, one fix that you can uh go about is to not pass the entire context into the agent. But instead of passing the the context into uh the the prompt at once, uh you basically pass references into the context and then allow the agent to slowly access the references and access the context this way. And I think like if you've ever been a data scientist before and like you've used uh like uh you've done any sort of exploratory data analysis, this should be uh pretty common to you, right? Cuz like um imagine you're trying to do exploratory data analysis in like a Jupyter notebook, you don't pass your entire CSV into your your code. You basically like, okay, you start off by doing your normal data science imports and then you create a data frame and then you're like doing these code snippets to slowly filter through your data. Look through like the first five rows, do some transformation, look at some histogram, slowly understand what's inside your data before you try and figure out what you can do with this data. And so our argument is that like when you're building these like long-running agents, this is what you want to be doing, too. Like instead of passing like the entire context into the agent and hoping that just because it says it accepts 1 million token context, that it can somehow reason across the 1 million uh token context, you want to be giving it references and then allowing it to slowly explore the context as it goes about the task. And so if you think about designing agents this way, then a lot of other things become easier. So uh context chunking becomes very easy because uh you can basically just like uh chunk the context and then pass them around as variable. Uh a tool calling also becomes very easy because you can basically uh do these like tool call functions and instead uh uh like one of the issues with uh tool calling, uh if any of you have seen like the agents fail on tool call, is often they need to be able to auto-regressively generate the thing that they want to pass into the tool call. So if they're like trying to pass like a context that's like 2,000 uh tokens that's somewhere earlier inside uh its prompt, uh you are basically counting on its ability to be able to auto-regressively generate that correctly. Whereas if you just uh think about it as like passing a variable, uh then this is uh more likely to be generated correctly. Uh sub-agent delegation also becomes very easy if you just do this in like a programmatic tool call way. Uh you can even do it in like a batched way. And so like why stop at just doing variables, right? Why not have like the entire grab bag of like uh a programming structures? And so uh say for example, you have like a task that requires you to uh process 10,000 documents. So if you were doing this in like the uh like normal language model way, you would basically need your model to do like And like 10,000 sequential tool calls along with the outputs, it's very likely you're going to hit like multiple compactions here. And you are basically hoping that every time it does the compaction, that it somehow remembers all the tool calls it's already made. It needs to remember the result of the tool calls. It needs to remember like which documents it already processed. So, this is very unreliable. But if you just did this in like a as a recursive language model, um then you can just do like a a for loop and basically do like a for loop and like programmatically uh do subqueries to process your documents. Okay. And so, yeah, that that's basically our proposal in terms of like what people should be doing for long-running agents. Our proposal is like doing recursive language models. And I I think this figure basically uh covers what I just talked about. That you want to be making your uh agents instead of passing the prompts explicitly, you pass them as a variable, and then you basically make them code to interact with the the context. And also, you want to be doing this like subagent delegation thing. And the main idea here is that like you don't want to be doing compaction because every time you uh end up with a compaction, uh often the agent just kind of gets lost and doesn't really know where it is anymore. And so, you want like a main orchestration agent that is always uh delegating out any sort of messy task into the subagent. Okay. And so, uh well, this isn't entirely new. So, if any of you have like used any of the agents you have today, uh they kind of already do uh these agentic patterns. So, for example, uh basically any chat interface that you can use. So, like uh Cloud Code, Gemini, or if you're doing like ChatGPT, when you try to paste a really long text into the chat window, they'll basically always convert it into the this like pasted thing. And this is basically the idea of like yeah, they they also know that you don't want to be like passing these like giant walls of text into the prompt. And so you always convert it as a handle and a reference. And so then the agent can like access it slowly. Perplexity, I think this was like two days ago. This was like June 2nd also announced that they were doing like programmatic tool calls for search. And so the basic idea for those of you don't know Perplexity, they're a search engine and their main thing is doing deep research. And so deep research you often have need to be like doing a lot of these like parallel searches. Previously, they were just doing these sub-agent calls in the normal instruction tool call way, but now they've like basically swapped to doing it programmatically. Basically, write a for loop, have a few primitives and use those function primitives to do the sub-agent delegation. Because like anyone who like uses the agents like they kind of know that like once you like see it like doing the compaction, like it it's basically over. Like it's not going to be able to remember what it did after the compaction. And so this pattern called recursive language models has been pretty popular. So they've been used for all sorts of things. There's recursive language models for math. There's recursive language models for coding. There's recursive language models for video understanding. Basically, anything that requires long context understanding. There was even like a recursive language model for Epstein files at some point. Yeah, that's an interesting one. And like like anything that requires you to process like either a lot of documents. In the video case, it's usually because they're trying to find something inside the video. And then they need to be able to chunk the video and do parallel processing. Yeah. And so there's this blog post by the first author of recursive language model called the mismanaged genius hypothesis. And so basically, this is the idea that like the language models are already capable to do a lot of the tasks that we want them to do. But the main issue is that they don't orchestrate themselves very well right now. Uh and like you can uh say that a lot of this year will be be people building out better harnesses to better orchestrate the model. But our argument is like uh a lot of these harnesses like uh uh Claude code uh open claw, uh they're already like kind of wiped coded anyway. So, since the models can already code out these harnesses, uh why don't you just make them code out their own custom harness while they're inferencing, right? Um Yeah. Uh the issue right now is that if you do try to do this pattern and like try to do this recursive language model thing, you'll find that they don't actually work that well out of the box. And so, the main issue here is that uh the models are very specific to the harness that you train them on. And so, um Uh right now, if you just use the recursive language model prompt, uh it won't do very well. Uh but if you like read the blog post, they basically have this experiment where they were like trying to do long COT, and if you use the uh original RLM prompt, uh then it doesn't perform very well. But uh if you do a little bit of prompt engineering, then you can basically get it to uh perform better than the base model. And so, our idea here is that like if you're able to perform better just by doing uh prompt engineering, uh then you would do even better if you were actually trained uh on this particular harness, uh right? And so, the the way that we're thinking about it is that like uh RLM and this like particular agentic pattern is like the next thinking. And so, the idea was like um back in 2022, uh the language models were very Q&amp;A, uh and like uh the these people uh at OpenAI basically had the idea of like chain of thought of like test time scaling of uh what if we allow the model to uh spend a bunch of tokens doing things that weren't exactly the output? Could we uh scale the capabilities of the model without scaling the parameter count? And that's uh back in 2022, this was kind of a weird thing. Uh a lot of the chain of thoughts weren't super good. Uh but then at some point uh they just got trained into the model. And you can say that like uh somewhere beyond uh 2025, uh no one questions thinking anymore. Like you basically never ask uh a model and expect it not to think. Uh there are I guess there are certain tasks that you do that, but like most of the tasks that we care about, uh we we kind of wanted it to think before it does its output. And so we see this this will be the same thing for like recursive language models and like harnesses of like instead of them thinking, uh they will be writing code to do like the tool calls and subagent delegation. Okay. And so a lot of enterprises uh already uh train their models for their particular harness. And so uh they have this continual learning loop. And so the basic idea is that once you have an agent deployed, then you can harvest the the traces and the trajectories that the agent does. And so this consists of like all the model calls, the tool calls, the the user messages, and the memories that the agent tried to access. Uh you can get the feedback either through like the users' rating of the particular output or um some sort of rule-based thing like just like uh figuring out if the uh user was able to complete their task or if they were like frustrated in some way. You can also do like uh message judge and basically have an LLM go through the trajectory and like uh check whether or not it thinks that uh this was a successful trajectory or um if uh the the trajectory was efficient. And then basically you can use this feedback signal to to go back and like train the model. Uh and yeah. Uh and and that's what we're doing at Prime Intellect. So uh Prime Intellect, we want to be the platform uh for people who are uh deploying their own models and training their own models. So we provide a uh model training as a service stack. So uh there's a CLI for you to do this. And uh we find actually most people end up using the CLI cuz you see there uh they are sending their agents to do the training. But if you prefer a GUI, then yeah, you can also go to IGUI and basically we support many of the open source models. So we have support for like GPD OSS. We support the llama models. We support the the Nemo trial models, of course, and also many of like the current models for multi-model. Um Yeah, and we provide you metrics for experiment management. So you basically get like a weights and biases kind of thing, but there's like some specific things that are for reinforcement learning in terms of like configs and like buffers and evaluations. Yep. Yeah, and we also provide a rollout viewer. So this is very useful for like debugging what's wrong with your agent. So you can basically your agent does some a few rollouts during the training and you can basically like click into the traces and like see what your model tried to do, what tool calls it tried to try to make, and also any sort of metrics that your environment logs. Okay. So other than the model training, we also provide a lot of other stuff. So what I talked about just now was the lab section hosted training. We also provide an environments hub. So environments hub is 5,000 open source RL environments that we've sourced from the community. It also contains all the environments that you would see in all the popular benchmarks. We also have evaluations. So this is allows you to run your evaluations consistently and you're able to get the same rollout viewer that you got before where you can look at the traces that succeeded than the traces that failed and try and figure out where your model is performing well or not. We also have sandboxes and so this is the idea of like people who are doing reinforcement learning. You I I guess in in the keynote this was mentioned that like the agents use a lot of CPU. So yeah, they're either running tests inside the CPU or they're running some sort of environment that they're interacting with inside the CPU. And so we also offer sandboxes for people who are doing that for reinforcement learning and their evals. We also offer compute so you can get on-demand GPU through us and also multi-node clusters and also for those teams that are training for multiple months at a time, you can get reserved instances. And so all of this is built on top of uh NVIDIA's uh stack. Uh yeah, um a lot of like the hosted training stuff and uh the offering we have are on Hopper and and Blackwell. Uh I don't think we currently offer any Vera Rubin. Uh they're they're still in production. I don't think they're widely available yet. But uh when these uh become early we work really closely with NVIDIA. I'm sure we'll be one of the first ones to support it. Uh we do our inference with uh NVIDIA Dynamo. So um I'll just give like a brief intro to Dynamo. Uh so so Dynamo is this idea of like I think most of you here who have like served uh inference before, you're probably familiar with like a vIRM and SG Lang or like some of you may be TensorRT. Uh but like when you try to scale your inference, you realize that like the bottleneck isn't really like the engine and the the kernel implementation there. The bottleneck becomes like the whole orchestration layer around that. Of like if you're multi if you're doing like a data parallel thing where you have like multiple uh engines running, then uh then you have like first issue of like being I like cache aware. Of if I'm doing like a multi-turn thing where my my agent is doing multiple tool calls and then coming back into the inference, uh I want to make sure that it hits the same engine and is able to hit that prefix cache. And also uh PD disaggregation concerns where you might have a prefill node that runs a config that is better for throughput, but then you have like a a decode node that runs better for latency. And then how do I uh uh do the KB cache transfer between that? Uh and all the orchestration, auto scaling kind of stuff. And so that's what we use uh NVIDIA Dynamo for. Uh we also have uh GPU and CPU sandboxes. Uh I think GPU sandboxes right now is in beta. Our research team is trying them out, but I think we're going to release them in a few weeks. Uh for Vera CPU sandboxes, we've tried them out in our workloads and they perform pretty well. But again, I think this is not widely available. So I think we don't offer it right now. Uh and a lot of our stack and like the environments uh are are well integrated with the Nvidia stack. So I think Nvidia Nemo Gym has a integration with our verifiers. So all the 5,000 environments that we have on the environments hub, you can basically run them in Nemo Gym. We also have them on Nemo Cloud and we also support all like the Nemo Cloud models. Uh So we have some pretty interesting users for our model training as a service. So this is one case study that was done with us by Ramp Labs. So Ramp Labs is this fintech based in the US and they have this Agentech Excel agent. And so they wanted to optimize their Agentech Excel agent and here they were able to use our stack to train a much smaller model like a Quen 30B that is able to beat Opus 4.6 at this particular task. And also you can see that it's much more efficient than Opus 4.6 in terms of latency. And so they were able to like perform the task faster and deliver a better user experience. We also have data vendors using us. And so the basic idea here, I think this is like a recent development starting this year. So I think like last year the new labs were a bit too cash rich and a bit too data poor. And so they were just like buying anyone who said that they were selling data. But like now that there's been a lot of data vendors coming into the space, they're starting to be a bit more picky. And often they're asking the data vendors to prove that their data is worth training on, that the environments actually improve the benchmarks. And so a lot of data vendors are using us to basically be able to validate the data. So if you can train the model and like get a nice reward curve on your data or like prove that it makes the the agent perform things more efficiently, that's a very definitive way for you to be able to tell the new labs that your data is worth training on. And there's this tweet that basically says that like these labs which have training capability are able to sell their data at like a two to three x markup of like their commodity peers. And so if all that sounds very interesting to you, yeah, please check us out We're primeintellect.ai. Um Oh, I didn't have the second slide. Okay. How do I go back? Ah. Okay. Yeah. So, that's it for me. Guys have been a great audience. Enjoy the rest of GTC and enjoy Computex.
