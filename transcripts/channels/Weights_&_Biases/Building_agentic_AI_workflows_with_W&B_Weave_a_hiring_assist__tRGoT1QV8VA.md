# Building agentic AI workflows with W&B Weave: a hiring assistant case study

- 영상 링크: https://www.youtube.com/watch?v=tRGoT1QV8VA
- 채널: Weights & Biases
- 업로드일: 2025-07-10
- 자막 언어: en
- 단어 수: 약 7453개

---

## 스크립트

[Music] Hello everyone. This is Nico and Karan and today we'll be walking you through a state-of-the-art agent AI application focused on evaluation, guardrails, and auditability. For the purposes of this demo, we'll use a hiring agent. So here's what the overall higher level workflow looks like. So as you can see there's two main inputs. One is the resume and other is the job opening. So these would be the two inputs. In our scenarios these are two input PDFs. And then this will pass through our AI system which will then generate an output and it will determine whether it thinks we should move ahead interviewing that candidate or not and the reasoning for the same. But now what happens within this AI system is there are multiple steps in here. So first and foremost as the PDFs are being passed we'll have an extraction model that will extract those details. For our scenario, we are using GPT4 mini, but this can be any LLM. And then we're doing a comparison here. And now what we would want to do is we would want some guardrail on that output of the comparison. So this is where we run those guardrails twice. So let's say the first time a comparison happens, we run a hallucination guardrail. This is something that Weights and Biases Viv provides by default. And let's say on this overall scenario there's three possible outcomes. So the first scenario is there is no hallucination and then a final output is uh shown. The second scenario is where a hallucination occurs in which case the loop selfiterates the agent again will come back and then try to give out another response where the hallucination guardrail will be run for the second time. In that case, let's say if there is no hallucination, the output is generated. And in the third scenario where there is hallucination that happens in both of those cases, this is when an expert will be invoked and that is where a human in the loop or an expert annotator comes in and then adds in their own feedback. So that's what this overall project talks about. So now let's jump into the example. So in here I have this streamlit UI. So in this case I'll upload the job offer again uh we are hiring for this open position we have right now for a gen AI engineer and then for our applications what we have done is for the purposes of this demo we have generated some synthetic applications so I'll just upload them but this is where a manager and HR can add in the own applications and now I'll hit the evaluate application button What's now happening behind the hood is now it's trying to process do comparison and then generate an output and then determining if there's a hallucination or not. While this is happening as you can see on this left hand section there's some settings that you can have. So for the extraction model you can determine what LLM you would want to use. Similarly for the comparison model we are using GPT4 mini for both of these scenarios. Similarly, if you want to use your own custom LLM, let's say you have an internal built LLM or a fine-tuned version of an LLM, you can easily add that and you can now play around and now start interacting with that custom LLM. And then for a guardrail, you can also set a guardrail model. In this case, we have used GPT40 mini throughout. So now my application has actually come back with a response where I can see an expert wasn't asked which means one of the first two scenarios actually occurred and in this case it determines that it would want to recommend this interview or this candidate for an interview and here's the corresponding reasoning for the same. So now let's jump into the weights and biases Viv UI and take a look at what this looks like and what went behind the scenes when it generated this particular result. So now I am in the weights and biases viv workspace and once I hit refresh you can now see uh my calls just ran which is literally what I had uh logged 2 minutes ago. And here you can notice there's two extractions. Uh so we are again uploaded PDFs and then there was a prediction that actually happened. Now you can see there's a bunch of columns in here. So again what we are looking at right now is a general trace view which will showcase each and every trace that has been logged to this application. Now let's say in my scenario I am most interested in my output which is the output of the predict function. What I can do is instead of having a generic view I can now filter out and create a new view I can only filter out let's say on the predict function. This is where weights and biases viv has a concept called a saved view. So here if you notice on the top right we have this option where you can now save this view or save this as a new view. What I've done is I've already created my own view here which is called the guardrail traces. So now once I am in my guardrail traces view now I can see the call that I just made. And here you can again see the tokens the cost the latency at an higher level. But here you'll also notice the two applications or the two PDFs that I had uploaded. I can also look at those. So here's my application. Here's my job offer. Again goes on to show that weights and biases can support rich media which not just includes images, it can also include things like audio files, video files. So again if you have any um agentic application or any genai application dealing with multimodel data uh reads and biases can log and allow you to visualize them. Now for this predict function you can see it did it mentioned that there was no hallucination and then it recommended for an interview which is what we saw in our streamllet part. Now you'll also notice in the feedback column I have this robot emoji. So this is something that I have used or implemented in this project. So now what we have done here is anytime a robot meaning our agentic AI bot generates or gives any response we would want to determine that using the robot emoji. And let's say if a human or an expert annotator is being intervened or asked in this process, that is where we'll add an I emoji determining that. So now let's dig into what our predict function does. So here again you can now look at the predict function. Here again the standard inputs which is the offer the uh the second input which is the application and the final output that we just saw. But now here you can see there is a sequence of events that we've automatically captured. Again you can see we use the langraph uh like API here. So now let's take a look at how this workflow has been generated and what does that entry point look like in terms of capturing and recording traces with weights and biass viv. So along with this calls tab there is this code tab. So if I click on this code tab this is where you can see uh I have used the viv python SDK. Now when it comes to interacting with Viv there's different ways on how you can do that. One is through the Python SDK which is what we have done in our application. The other is through our TypeScript JavaScript SDK and you can also interact with Viv with our service API and we have also added support for hotel. So if you have any application working with the open telemetry standards you can use that. You can also use our log to weave with MCP as well. So again different ways whatever or however your application is built you can log capture traces to weights and biases fe. So now in case of our Python SDK once you import the SDK all you need is a oneline decorator. So in this case you can see we have our predict function and all I had to do was decorate this function with this oneline decorator called weave.ob and that was all that was required in terms of capturing in this entire set of traces. Again in this case we're using langraph we have or weights and biases we has its own built-in integration with langraph as well. So now I can look at all of these traces where I can see there's two extractions that were happened. One for the job offer, one for the application and then there was a comparison that happened in this case. And now if you notice when I was mentioning the either of the two scenarios that happened, this is the case where a hallucination happened for the first time. The agent went back, recalculated, did the comparison again, and then it checked for a hallucination guardrail. And for the second time, there was no hallucination. And we can again click on this node actually expand all the way to the hallucination guard. And in this case, you can see this says there was a hallucination. And then if I do the same on the second hallucination guard rate, this now has an output as a false. So as you can again see that's one scenario on how that can happen. Now what you can also do is you can look at details on each and every sequence of events. So here you can see there's a bunch of things which you can look at a parent level. You can break it down into individual sub calls which we did here for the hallucination guardrails. At the bottom you'll also notice we have this nice little timeline view with a scrubber. So you can easily scrub through things look at what happened look at the output and then determine it accordingly. Along with that for each and every call V will also capture all the information around the input tokens the output tokens and then give you the cost for the same and then you also have the latency that's also being captured. So again this is one form of visualization. You can also render this view in a code composition format. This is something a lot of developers like where you can now have a higher level predict function but now you can see the sub calls that were made in this case what the calls that langraph made what happened in each of these different scenarios and then we also have a flame view in which case again let's say if I would want to look at the distribution of the time or the latency this gives a nice little view where I can see my extraction uh extraction for the job offer and then extraction for the application took the bulk load of my time literally I would say like more than half of it whereas my comparison hallucination and in this case the second iteration of it took a comparatively less amount of time so what this tells me is let's say in the next iterations if I would want to improve the latency maybe I might want to look into the extraction and then figure out a way on how I can improve the efficiency and the latency there. So that's just another way on how you can render or use these different visualizations. Again, one more thing, right now I'm using weights and biases weave in a light mode. I know lots of developers prefer this in a dark mode as well. You can also do the same. Um there is a command that you can do or you can also simply toggle that from the user settings in your profile as well. So as you can see I just switched to a dark mode and again if I hit the option and M on my keyboard I can switch back to the light mode as well. So this is again super relevant and then here you can see a distribution of all of these calls. So now let's switch over to another scenario. So in this case where the hallucination happened for the first time but let's say it did not happen again. Now let's move on to the scenario where a hallucination happened for both of these cases and then an expert was now being called. So now I'll switch over to another project or another view where I can now see in this particular trace there is an oh sorry in this case there is a robot as well as an I emoji which again tells me a human was actually invoked. Now let's look at that predict function again. Again you can see it's pretty similar to what we saw before where um like there was a comparison with a hallucination guardrail. The same thing happened again but now an expert was now being called. So this is again another scenario where Vivve can also trace and give you quickly in terms of how many times was an expert being involved. How many times was the bot able to self-reflect and then reduce or eliminate hallucinations. And here for calculating hallucination we have used our own inbuilt hallucination scorer that Viv provides out of the box. Again, you're free to use that or you can build in your own custom hallucination or any guardrail that you want. So now let's dig deeper into this hallucination guardrail and then see why exactly is the hallucination happening. So I can now start to dive deeper and deeper all the way to the actual call to the LLM. So in this case we asked OpenAI through light LLM and now on this section here I can see the exact system prompt the user prompt that openai received. In this case you can see it actually got two inputs. One was the input data and the output and then it had to determine if there was a hallucination or not. And the thing we care the most about here is in the response you can see there is a hallucination. So the hallucination is true here. So now let's say a developer built this application tested it out as an expert annotator or as a PM. I'm now looking at this application and I feel the hallucination shouldn't happen. Maybe this is a scenario where I can now start to improve my application so I can reduce the time where an actual human has to come in and review these things. Typically what you would have to do is go back in the code, make those changes, rerun that, look at the traces back again, which requires a lot of back and forth, a lot of manual intervention and also requires some technical capability in terms of actually going back making those changes, reshipping it. What we would want to do is we want to make these things a lot lot easier. So it can be useful for any technical or a less technical persona as well. So to that aspect we have a built-in playground. So on this UI you can see there is an option to try this prompt in a playground. All I have to do is click that button and what it will do is retain that exact state. And now this will give me this playground view where first and foremost you can notice on the right I have these different options where I can now change the number of tokens, play around with the temperatures, make any of these changes. Additionally, I can also add a different comparison model where I can add as many views as I want. And now what I can do is maybe let's say swap out my model. So in this case I might want to use OpenAI. In the other scenario I might want to use a different model. You can also use your own custom LLM. As you can see in this case we fine-tuned a Lama model and published that through O Lama. I can interact with that model as well. Or you can quickly or easily add in your own custom provider. So it's very easy. All you need to do is enter in those credentials, the base URLs, and you're pretty much good to go. You can now start interacting with your own custom models. In this case, uh let's say for the purposes of this demo, I am fine with GPT41 mini. And now I want to see if I can make any change in my user prompt that will actually change the output of the hallucination from true to false. So let's say let me try editing this. First and foremost let's say I would want my response in JSON. So I can do that and then also ask my LLM uh to respond in JSON. Please output in JSON. And now let's see what's happening in this scenario. So in this case we are hiring for a strategic account executive role in the US. And here's are some of the key responsibilities some details around it. And now if we look at the job application the person that applied for this job actually has more experience as an account executive but in the federal space. And that's where you can see the preferred work location is Washington DC. But now let's see what the final output is. I can also see this person has done some good work, closed some good deals. So let's look at the final output where the output was we should not move ahead with an interview. And if I look at the reasoning, it does say that there is a misalignment in the job expectation where this person has more experience for the federal space whereas the role that we are hiring for is more in the commercial space. And then it also mentions uh there is a difference or the preferred work location does not match where I think it was more preferred in Washington DC whereas there was an expectation in San Francisco which does make sense but then if I look at this next explanation that actually does not seem to make a lot of sense where it's talking about expected salary and misalignment. Maybe I might want to remove this and instead add or stress on the difference between these two roles. So I can mention that the candidate has more experience in the federal space and not much experience in the commercial space. Cool. So let's see if making this change actually changes our LLM response. So once I've made these edits, what I can do is just hit retry. So now this again goes back to my GPT41 model and I can see it's already starting to generate its own response and the thing I care the most about is the final column on hallucination and that actually did it. So making that change actually changed our hallucination from true to false. So this is again a easier way for any annotator any PM to come in make those changes and now start to play around uh improve that particular application. So now what I can do is I can go back make that as a preferred change and then rerun my next set of iterations. And in doing so, I can actually eliminate the number of times a human is actually involved and make my AI agent a lot more robust. So now with weights and biases we've it was very easy to see exactly what was happening to understand why there was a hallucination the exact root cause of it and then how you can make changes to avoid that. Typically this is where weights and biases or an observability tool can help you. Without weights and biases vaf it's very very difficult to actually know what's happening happening in this particular dynamic agentic workflow. And again this was a very simple few steps workflow like as your applications start to get more and more complex without weights and biases weave it becomes very very difficult or almost impossible to know what's happening and why that is happening whereas here with a simple oneline integration with the decorator or with uh like hotel MCP you have complete visibility into your entire chain of thoughts at not just an input and output level but also at sub levels. So you can now play around with this. Now in order to extend it further, what I can also do is maybe have a new data set. And so let's say if I go back to my traces view, all of those scenarios where I actually see an eye icon, I can again filter it down. I can select those scenarios and now I can add them to my data set and create a new data set based on that. So in this case let's say I had my evaluation data set. I can simply replace my application or uh oops or my output interview with the expert annotator decision and then the output reason with the expert annotator reasoning and that's that can be a new data set on which my next iteration can happen and this is where you can continuously run uh like an iterate again and again and have those robust evaluations. So this is something my colleague Nico will talk into more details on how you can extend this tracing and run more rigorous evaluations to actually get um like or actually have the confidence to ship your application in production. So with that over to you Nico. Yes. So as Karen said, now that we have tracing and monitoring of our Agentic applications done, one of the key challenges that we have is how do we evaluate the agent? And I think that's one of the key challenges that most of our customers and prospects have when it comes to bringing agents into production. So the first question is if we go back to a report here, we can see that this was our system and the key question, the first question of course is what parts of it should I evaluate? So the first thing here is that I have is the system. So I could evaluate the whole system in terms of there is my resume and job openings that come in as PDFs and then I have my output here binary decision yes or no and then a reason for it. So I could evaluate that as a system. Then again I could also evaluate my guart rail model my comparison model or the extraction of the text from the PDFs. Uh now there are a bunch of things and ideally I do all of this. Today we're going to focus on how can I create a system evaluation here for the whole and then specifically focusing on the decision- making part of it, the comparison model that we also fine-tuned. Um, and so now this is what we're going to to focus on for the second part of of the tutorial. So if we if we click here now back into the project, we can first of all see you know back into traces here what we've seen before that we have another tab here. Uh and in Evals first of all looks pretty similar to traces. We have different views that allow me to save and persist specific filters or um column selections and expose them to specific stakeholders. For now, we just have a new EELS overview here that filters down on a specific last version of our evaluation. And why that's important, I'm going to explain to you at the end as well. Now what as I said what the focus of this evaluation is is we have a GBD40 mini baseline and we want to compare a prompt engineered version and a fine-tuned and prompt engineered version of the comparison model with each other and basically just see how the system how the decision-m changes and if it actually improves. Now this is a pretty yeah standard view here. It's similar to tracing. you have inputs and you have outputs only that in this case the inputs are different versions of the model of the system that I'm evaluating and also different versions of the evaluation itself. Uh and then as an output I get whatever kind of columns I want to specify what metrics I want to specify. So I have two different categories here the decision itself which is a binary classification. So in this case something like precision recall and then for the reason which is uh the judge which is really the text where we have in this case five different specific uh metrics that an LLM judge compares the predicted reason with the target reason and tries to score the predicted reason based on these um specific uh categories. And then of course for each of these you can see who uh locked this and when it was the number of tokens the cost of of all of it and then uh the latency. Now in this case I was really interested in three main uh metrics that I just pinned here on the right hand side being the F1 for the decision and then for the reason score the pass rate that means how many of the reasons were generally viewed as as valid and then the average score. So from these five different um dimensions what's the the average score that um the specific reasoning hiring agent gives here. Cool. Um so now with that set up the the first thing that is important to understand is what do you need to actually create this evaluation. So we have as I said two different aspects here. The first one is the model. If we click on it, everything is automatically versioned as Karen said and we've we can see different parameters here that we have yeah saved in our code. So whenever I change anything in my code, this will be automatically created as a new version. In this case, I have three main models. The extraction comparison guardrail model uh and then a couple of prompts here. Uh and now what I can do is I first of all I can see where was this model used. So I could go ahead and actually look at all the the predict calls to my specific um uh version of hiring agent. So this is this is actually the code. But you know if we go back here and and actually look at the calls here we can see these are all the calls where my hiring agent predict function was called. Uh now what we really want to see now is we want to maybe for example compare it to different versions. Right? So we have 21 versions here. In this case, let's have a look at for example these these three ones here. Um, and this first of all just gives me a sideby-side comparison of all the different arguments that that were saved. And if I diff only, we we can see that what we're looking at here really is two different attributes being um worked on. So there's one one the comparison model itself that's being changed and then the prompt. And so the prompt in this case changes from 16 to 17. We can see that there's a bunch more details being added here to the prompt. And then from 17 to 18, the prompt stays the same, but the model changes changed from a baseline model to a fine-tuned one. Um, now if we go back, uh, the other part of the evaluation here is is the object itself, right? The evaluation object. And that really consists of, you know, two main components. There's a data set and scores. A data set really is in this case consists of uh a PDF, a local path in this case, the extracted target extracted text, the same thing for the application and then the target um decision and the target reason. Uh there's different versions of this. I'll go into this in more detail at the end of this this section. But the other piece for an evaluation is are the scores. And that's really what calculates the scores at the end. So you have the decision decision score and the reasoning score which again I I'll go into more detail uh at the end of this this section. Now the first thing that we really want to do is we really just want to compare these right and so looking at these numbers is great but ideally I could have some kind of visual report and that's exactly what happens if I compare for example a couple of these and now you can see more visually systematically compared how different versions of my hiring agent perform on these metrics. So I have a lot of these. Let's actually only look at uh the F1 as I said the uh pass rate generally of the reason the score of the reason and then maybe latency and tokens. And now you can see on the first hand here you can see how the surface area of these different versions actually increases which is generally good right in blue the lowest one and then kind of increases with the versions. I can look into more detail for each of these categories here. Generally speaking for this you can see that the F1 is pretty much the same. So we might need to work a bit on the the data set make it more challenging maybe or balance it more but they're all pretty good. But when we when we come to the reason itself to the generation of the reason you can see that the difference is quite big actually right so there's only like 0.017 017 performance here uh from my baseline model but then it goes up to to 36% and then 83% when I'm prompt engineering and finetuning and so why is that we can have a look at that going down you can first of all see the differences uh so if we un toggle this you can see as I said before these are all the different parameters right arguments of each version and then to see the difference I can just diff and now I see that There's the prompt that changes from zero to three and then there's the model that changes from mini to this fine-tuned version of the mini one. uh I can go down further and just see first of all you know just the numbers quantitatively all right how does the decision compare how does the you know different metrics compare that I've selected here uh and then at some point once I have an understanding of quantitatively how they compare I want to drill down and understand how does it really look like qualitatively on specific examples and that's where it's really handy to click here on the data set results where you can actually see the data set itself right? The the specific offer, PDF, and and text and application, and then target, interview, and reason. And then I can also just look at specific examples here, right? I could go through all of these different uh rows here and see what exactly was the input, uh what exactly was the decision, how exactly did the reason look like, and what were the scores. Again, if I go ahead and expand this for a second, you can see that, you know, this is really a great view to to drill down into all my samples here, my examples, and really just understand why am I seeing the the quantitative evaluation results that I'm seeing. And now what we did here, for example, is we actually because we only have 20 samples here, we ran three trials to make it, you know, statistically more significant and just ran evaluations three times. So that's you know 60 examples for each of these and now you can see each of these runs here and then the aggregated performance uh here on the left hand side. Now this is great but what if you have a big data set right big evaluations hundreds thousands of different samples and you just really want to pin down and look at the ones the samples that have high signal and tell you what you want to change. And you can do that if for example we go back here and let's say it's clear that the baselon is not the best but we really want to find out why is the finetune model better than my prompt engineer one because that's an expensive thing to do. Maybe we can just do prompt engineering. So let's remove this from our comparison. Same view as before but now we have since we only have a sidebyside comparison we have these additional selectors. And so now what we can do is we can actually look at all of our metrics here and decide for example let's have a look at uh the samples where the the reason wasn't good because the experience analysis of it wasn't the the score was low right so we can click on it so this is basically the metric where the the reason uh of the hiring agent lacks exper lacks a good analysis of the experience of a candidate and so we You can see that there's a challenger model here. That's the finetuned one and the prompt engineered one. And then there's the baseline here which is only prompt engineered. And so if you select for example these two here, we they have scored a four and only a two two and a half for the the base model. And now furthermore, we can even narrow it down even more and say we want to have a look at uh the examples where the experience an analysis wasn't good because there wasn't enough evidence. Right? And so this is another score that we added which is a score that tells us how good how much evidence was provided by uh the hiring agent. And so now we can go ahead and select this single example here where you see that the the challenger model has much more evidence apparently than my baseline for the analysis of the experience of the candidate. Now there's there's a single one left here. So we could go ahead and select this one. expanded and now we can actually go down and understand you know why why is that what evidence lacked there how was the uh analysis of the experience not good so first of all this is a customer support engineer Tokyo um that that's the that's the offer then we can see both of them said we should definitely invite this person to the interview and first of all you can see that the reason here is actually much shorter so it's this is you know the first baseline and then for the finetune one I nice categories here, you know, nicely structured. But, you know, first of all, nicely structured doesn't have to mean it's it's much better. But now, if we go ahead and look at this in more detail, we can see this mentions, okay, so it's a good fit because the person has skills in in Python and TypeScript. So, it names specific languages here. If we go down to the experience analysis here though, we can see that you know it mentions an internship um and then even tensorflow and and carers and pytorch as specific frameworks right not only python or um or typescript but also specific frameworks in python. So clearly you can see that this has just much more detail, more structure and and really sightspecific experience that this candidate has in in in different uh yeah different experiences the person did in the past. So you can kind of understand okay so you know the the fine to one clearly gets the structure and more detail better than the just the prompt engineer one. And so this would be then you know where you could decide okay let's actually fine-tune on a bigger data set or improve the the model further. Generally speaking, I think this um workflow where you start off looking at quantitative comparisons here, quantitative metrics and drilling down into high signal examples to qualitatively assess is really something that we've seen people do very often. And we've really helps you to make this process very easy. you know, you don't have to search for logs, go somewhere and try to look through print statements or, you know, other other tools that make this quite cumbersome. Um, now if we go back to EVELs, what I said before, um, there's always two main things we have to focus on when creating evals. The first one, of course, is to improve the main agent. The second one is to improve the actual evaluation. And that's because it has to be representative of course for my user group and for my use case. Uh and that's that's important because ultimately that is what gives me confidence to deploy my my agent. And so typically oftent times people just struggle creating these evaluations uh or updating them. And so uh I just want to show you how you know you can do these things in in weave. So as I said an evaluation generally consists of a data set and scores. So the first thing I have to do is I have to create a data set. Now if we go here into our data set, we have a bunch of, you know, different tabs here. We have one that's called data sets. And if we click on it, you can see all of my different data sets and their versions. So now I can do a lot of things to just either I just upload a data set as a CSV or any kind of other file as a baseline. Then experts or you know specific people could come in view this data set decide to actually edit this data set go into here maybe update the reason for example make it more specific publish it as a new version. You know this would be automatically saved as a new version in here or what we see quite often is once you have a baseline and you have something in production or in some testing environment you want to update it continuously with production data. And so that's something that's very easy to do too in in in Weave is you can go into traces here. Uh and then you can for example just you know in this case select the the trace view for expert annotation. And that could be something where you set a filter on whether an annotation has been given or not. And so in this case we can see there's uh these six traces here where we had you know a specific case where at the end an expert actually gave a review uh and so this was through the stream application in this case could come either you know through the application itself or people could come in and actually manually annotate this through through weave and that's where these annotation views are specifically also especially uh important. But once you have these annotations, what you can do is you can, you know, select, for example, a couple of these where self-reflection didn't work. It couldn't selfheal and people had to annotate. And then you add them here to the data set. You can select add them to the data set. Let's say the evaluation data set in this case. And then you see these are the columns. And this is where I'm mapping parts of the production trace and annotation to my new data set. So in this case, you know, I I want to have the expert uh annotation for the decision and I also want to have the uh expert reason. And so the cool thing here is I can directly feed back production traces and annotations into a data set to just update it regularly and then run it again as a new version for example. The second piece of an evaluation that you have to update is the scores. So if we go back into our version here, we're clear on the data set now. And this is good because it has to be representative for my user group. Now scores are important to give me signal right about what how is my my model performing, what use case am I looking at and so what metrics should I be looking at. Now in this case the decision score is quite easy. It's just comparing binary classification true equals true or false equals true false and so on. Um, and so there's no change that I did here, but the reason score is much more complicated. So in this case, you can see I have six versions of this. If I click on it, uh, you can see the same thing as before because everything is versioned in in weave, I have, you know, different versions of my score. And for example, the if we compare the last two ones here, since this is a judge, um, it was actually quite hard to make it um, very specific, right? very specific in judging how good the reason is. And so what what what you see here is for example I had to update the prompt of the judge quite quite a lot to make it much more specific here like explain exactly how it should analyze whether the position fit was correctly analyzed whether the experience was correctly analyzed and then I had to make some some adjustments to the actual scoring function as well and this is important because for example in the first case here uh my all of the different models I've shown you before I actually just scored 100%. So that means even the baseline was already saturating uh this benchmark and so it's hard for me to actually improve my model and make and gain confidence in its performance in production. Uh and so this is just showing you uh both of these components right that ultimately uh come together in this evaluation object uh are very important to to iterate on both the data set uh and the scores the scoring functions and uh both of these is is actually quite easy to do and we've as I've hopefully shown you now and with that I would say yeah we covered a lot of ground here we had a look at system evaluation evaluation of the the decision um component of the of the hiring agent. We compared different version of these. We had a look at the difference in the version. We had a look at quantitative evaluation. Drill down into qualitative samples. And then in the second part of this uh of the session, we actually had a look at how to improve and update the evaluation object, the data set and the scores themselves. With that, I would say thanks a lot for listening. I would greatly encourage you to actually have a look at this report. Everything that I'm sh that we have shown you here is public including um the code here. Uh and we have actually also have a detailed white paper here that explains also from a compliance perspective but also productivity point of view how to actually create such an hiring agent with weights and biases models the fine-tuning and weights and biases weave the agent building and evaluation piece. And with that happy tracing and evaluation reach out if you have any questions and have a good day. [Music]
