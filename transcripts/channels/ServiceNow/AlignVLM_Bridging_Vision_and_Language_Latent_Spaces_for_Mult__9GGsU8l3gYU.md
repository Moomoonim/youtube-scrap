# AlignVLM: Bridging Vision and Language Latent Spaces for Multimodal Document Understanding

- 영상 링크: https://www.youtube.com/watch?v=9GGsU8l3gYU
- 채널: ServiceNow
- 업로드일: 2025-06-02
- 자막 언어: en
- 단어 수: 약 2205개

---

## 스크립트

Welcome everyone to the AI research bites. Uh this series as always is uh short and informative talks showcase showcasing cutting edge research work from service now AI research uh and other AI uh practitioners in service now. The AI research bite are open to everybody especially those interested in keeping up with the fast-pac AI research community. So today's session feature a 15 minutes talk from Ahmed Masri on the latest advancements of aligning VL VLM and will be followed by a 10-minute Q&amp;A. So Ahmed is a PhD student in computer science at York University in Toronto and a visiting researcher at service now research. His research focuses on developing multimodel vision language models and benchmarks for chart table and document comprehension. His work has been published in top tier conferences like ACL and AMNLP accumulating over 1,199 citations uh on Google Scholar. He is also an active open source contributor providing models and data sets with tens of thousands of downloads on hugging face. So uh thank you Ahmed and up to you now. Thank you. Thank you so much. So hi everyone. Um so today I will be presenting align BLM uh which is about bridging vision and language latent spaces for multimodal document understanding. So for your information this is just a short disclaimer that the upcoming statements are to the best of our knowledge. So our agenda for today um first I'm going to walk you through the motivation and some background and then I will walk you through our solution the align connector followed by the experimental setup the results and finally the key takeaways. So let's start. So the first question uh we ask is why does this topic matter? Why multimodel vision and language models? So in enterprise applications or setup you will often encounter textrich visuals such as like charts, infographics, PDFs or tables. And in order to analyze these visuals or extract data from them, you need an AI model which can understand both the visuals and the language. So how do these vision language models work? So over here you can see the architecture of a vision language model, a simple architecture. So um the uh the architecture has three main components. The first component is a vision encoder which was pre-trained to understand images. The other component is a large language model which was pre-trained to understand text. And finally you have this connector component over here which basically maps the vision feature from the vision encoder to the LM input space. So you can think of this connector as a translator. It translates visual features into textual features. So there has been a lot of work on the vision encoder side and the LM side but there has been very little work on the connector itself. So the key question we ask in this project is how can we design a good connector to effectively align the visual features with the LM input space. So before we answer that question let's have a look at this example over here. So imagine you have an image and you encoded it and right now you have some visual features in the vision space. So and on the other side you have the text space of the LLM. So over here you can see let's say the points representing the LM tokens or words basically words which the LM can effectively understand. So what you want to do with your connector is you want to map the vision features from the vision space into the text space while preserving the semantics. So for example, if you have some vision features coming from an image of a tree in the vision space, you want to map them to the text space so that they can land as close as possible to the word let's say tree. So ideally what you is system connectors out there when they do this mapping they don't enforce any hard constraint on this mapping process and this can become problematic when you have some noisy inputs because if you have some noisy inputs in this vision space when and you map them they can land anywhere but ideally you want to keep them as close as possible to this region. So how can we design a connector to do that? We present the align connector. So over here you can see the architecture of our align connector and the align VLM model. So uh for the basically we have two kinds of inputs a text input. So so for the text input it gets processed as normal. First the text gets tokenized and then the relevant LM text embeddings are selected and then they are giving to the LM as text inputs. For the vision inputs it's a little bit different. So first we encode the vision features and then we convert the vision features into some probability distribution or probabilistic weights over the LLM text embeddings. So basically we convert the image into probability weights which we use them to combine the text embeddings together using a weighted average sum operation. So mathematically this operation over here will force all the vision features to be in the convex hole of DM text embeddings and these will become the vision inputs to our model. Another way to look at it is that the vision inputs and the text inputs they are coming from the same source in this model design. Right? So they are coming from the LM text embedding matrix in both sides. Uh the image we only use the image to produce weights or probabilities which we just used to combine those LM text embeddings over here. So now I'm going to walk you through our experimental setup and how we trained and evaluated our models. So we train our model in three stages. The first stage is uh natural image understanding. The second stage is document understanding where we train the model on our big docs data set. And finally in the third stage we train the model on our dog big docs dog downstream uh data set which enables the model to follow the user queries and prompts and questions. And for the different model components for the LLM side we use the llama 3.2 family of 1 billion 3 billion and 8 billion parameter models. So we experiment with three different model sizes. For the vision encoder, we use the SIL 400 mill model. And for evaluation, we evaluate our model on nine diverse benchmarks spanning documents, infographics, charts, tables. All right, let's talk about the exciting part which are the results. So for the results we ask uh we want to answer two questions. How effective is the align connector and how effective is the align VLM models the whole model together. So in order to do that we have conducted two key comparisons. The first one we compare the align connector against the previous connectors out there. So we designed an experimental setup where we controlled uh the the the setup like we use the same LM we use the same vision encoder same training data. So we train the models on the different models under same configurations while we only change the connector. So the main purpose is we want to isolate the true impact of the connector itself on the performance while keeping all the other variables in the experiment as fixed as possible. The other key comparison is we compare our line BLM model the model we trained with the align connector against the other vision language models out there. And we have three categories of models to answer different questions. For example, we want to tell you why we want first we want to convince you why should you choose align VLM if you already have data for finetuning and when should you choose align VLM if you don't have data for finetuning and when align VLM is a better choice over black boss models. So let's answer all these questions in the upcoming slides. So first in this slide you can see we compare the align connector against the previous connectors out there such as OB's MLP perceiver sampler and in this figure you can notice that we have nine different diverse benchmarks and it's pretty obvious that the align connector the orange circle is actually outperforming all of them in all the benchmarks in fact on some benchmarks the accuracy is way much higher so such as the W2Q benchmark so this proves that our align connector is much better alternative than the previous ones. Um, now let's um compare our aligned VLM model against open source base VLMs. By base VLMs, we mean that we took some base VLMs out there and we trained them on the same data sets as our models using the same similar configuration setup. So, we do this to ensure a fair comparison. And over here you can see the different results. So we have two categories of models. The small model category um of 1 to 3 billion parameter models and the larger model categories of 8 billion to 11 billion parameter models. And it's pretty obvious that on both model categories, our align VLM models are actually outperforming the other ones. For example, our align VLM 8 billion is outperforming llama 3.2. And over here you can see align VLM 3 billion and even the 1 billion is outperforming 2VL. Now let's compare our align VLA model against the open source instruct vision language models. So by instruct vision language models we are referring to models which were just trained on undisclosed data sets by their vendors and we don't perform any additional fine-tuning on these models. So uh this the comparison over here may not be entirely fair because models were trained on different data sets. But hey let's put our model to the test and see how well it performs against those open source ones. So on the small model category uh you can notice that align vm3 billion is actually outperforming uh very famous models such as quint 22 billion in turn v2.5 and so on. on the larger model category you can even though align VLM 8 billion doesn't have the highest performance it's still it's performing very competitively with open source models um finally let's compare align VLM against closed source uh VLMs so closed source VLMs are basically trained on undisclosed data sets we have very little knowledge about the architecture or training data or training details. So these are models such as GPT4, Gemini and plot. So as you can notice over here in this uh in the results you can notice that our align VLM model of 8 billion it's outperforming Gemini Pro 1.5 CLET however it's slightly lower than the GPT4 model. So Align VLM is performing very competitively against those closed source models. In addition to the previous results, we have also conducted an interesting experiment to uh validate our hypothesis that a line connector is more robust to noise. So recall these images from the previous slides. Um basically here you have the vision space and some visual features and over here you have the text space with the LM text embeddings. And remember we were mentioning that for uh ideally you want all your ideally you want all your visual features to be land to be mapped to this region over here in the text space. However, imagine if you have some noisy inputs I mean your connector can map them anywhere but we argued that the align connector will force all the vision features to land in this region over here which is the convex hole. So let's see how well is this hypothesis or how true is it. So we conducted an experiment where we add some random noise to the inputs to our connector. And then we basically measured the performance before adding the noise and after adding the noise and we compare two connectors our line connector and the MLP connector which is the most widely used connector out there. And as you can notice for our connector we only noticed a 1.6 drop in performance. However, for the MLP connector we noticed a massive 25% drop in performance. So these results over here validate our hypothesis that the align connector is much more robust to noise. So to summarize um in this talk uh basically multimodal document understanding are critical for AI agents that can handle complex data such as like charts, tables, PDFs and the align connector is enhances the vision language models for for doin understanding tasks and as you have noticed from our results the align VLM is a state-of-the-art and efficient model for document understanding. For the upcoming weeks, we plan to open source the model and the codebase and also we are working on improving other architecture components in VMs. So if you are interested to read more or uh learn more about this work, feel free to check out our paper on archive. And uh yeah, that's pretty much it. Thank you so much for listening.
