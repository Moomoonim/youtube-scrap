# Amazon MemoryDB for Valkey: Fast In-Memory Database Setup in 10 Clicks!

- 영상 링크: https://www.youtube.com/watch?v=wUdNboQsYdQ
- 채널: AWS Developers
- 업로드일: 2024-11-04
- 자막 언어: ko
- 단어 수: 약 1410개

---

## 스크립트

인메모리 데이터베이스는 RAM(랜덤 액세스 메모리)에 데이터를 직접 저장하고 처리하여 애플리케이션 성능을 향상시켜 줍니다. 특히 캐싱 용도로는 지금 당장 필요한 기술입니다. 이제 Amazon Memory DB for Valky를 소개합니다. Amazon Memory DB for Redus OSS를 실행하는 것과 비슷 하지만 가격은 30% 더 저렴합니다. Linux Foundation에서 관리하는 오픈 소스 기반의 강력한 도구 이며, Valky는 Redus OSS를 완벽하게 대체할 수 있습니다. 오늘은 몇 가지를 보여드리겠습니다. 첫째, 새로운 Memory DB for VY 클러스터를 생성하고, 워크스테이션에서 Valky CLI를 사용하여 연결해 보겠습니다. 둘째, 이 인메모리 데이터베이스를 읽고 쓰는 Python 웹 애플리케이션을 구축해 보겠습니다. 자, 시작해 볼까요? Amazon Memory DB for VY 클러스터를 실행해 보겠습니다. Amazon Memory DB 콘솔에서 왼쪽의 '클러스터'를 클릭하고 주황색 ' 클러스터 생성' 버튼을 클릭합니다. 이제 시작해 보겠습니다. 간편 생성 과정을 따라가겠지만, 다른 방법도 있습니다. RDS 스냅샷에서 복원하는 등 다양한 작업을 할 수 있지만, 가장 간단한 방법인 간편 생성 기능을 사용하겠습니다. 데모 구성, 즉 가장 작은 구성을 사용할 것입니다. 너무 오래 걸리지 않도록 하기 위해서입니다. 데모를 선택하고 이름을 지정하겠습니다. 저는 GNA say hello YouTube you YouTube YouTube cluster라고 하겠습니다. 좋습니다. 선택적으로 설명을 추가할 수 있습니다. 중요한 것은 엔진을 Valky로 선택하는 것입니다. 여기서는 RDS와 Valky 중에서 선택할 수 있으므로 Valky를 선택합니다. 또한 새 서브넷 그룹을 생성할 것입니다. 이 그룹은 클러스터를 포함할 서브넷들의 모음입니다. 이름을 지정하겠습니다. 저는 hello yoube Valkykey라고 하겠습니다. 설명은 R로 지정할 수 있습니다. 이제 특정 VPC를 선택하겠습니다. 저는 Valky 워크스테이션을 실행할 특정 VPC를 정의해 두었습니다. 그리고 나중에 이 옵션을 선택하고 서브넷을 선택합니다. 저는 이 PPC에 서브넷이 두 개밖에 없으므로 두 개 모두 선택하겠습니다. 서브넷이 더 많다면 모두 선택하거나 일부만 선택할 수 있습니다. 이는 여러분의 선택입니다. 벡터 검색을 활성화할 수도 있습니다. 벡터 검색 테스트는 다음 영상에서 다룰 예정이지만, 여기서는 자세히 다루지 않겠습니다. 벡터 검색 활성화는 시작 시에만 가능합니다. 벡터 데이터베이스가 필요한 경우, 주황색 "생성" 버튼을 클릭하면 됩니다. 잠시 기다려야 할 수도 있습니다. 잠시 후 다시 뵙겠습니다. 보시다시피 완료되었습니다. 시작하는 데 시간이 좀 걸렸지만 이제 다음 단계로 넘어가 워크스테이션과 Valky CLI를 사용하여 연결할 준비가 되었습니다. 자, 이제 워크스테이션을 Valky 데이터베이스에 연결할 차례입니다. 워크스테이션이나 애플리케이션은 동일한 VPC 내에서만 연결할 수 있다는 점에 유의하세요. VPC 외부, 예를 들어 노트북에서 연결하는 것은 권장되지 않습니다. 물론 이론적으로는 가능하지만요. VPN을 사용할 수도 있지만 지금은 사용하지 않을 겁니다. 저는 Guntu가 실행 중인 EC2 인스턴스를 가지고 있고, Valky CLI 도구를 사용하여 연결할 것입니다. 연결하기 전에 Valky CLI를 설치해 보겠습니다. ` valky tools install` 명령어를 입력하면 Valky에 연결하는 데 필요한 모든 것이 설치됩니다. `install`을 누르니 이미 설치되어 있네요. 어라? 좋습니다. 이제 Valky CLI를 실행해 보겠습니다. 로컬 호스트에 연결하려고 하는데, 우리가 원하는 것은 실제 데이터베이스에 연결하는 것이기 때문에 Ctrl+C를 누릅니다. 이제 연결할 엔드포인트를 찾아보겠습니다. Valky 클러스터 엔드포인트로 돌아가서 이쪽을 보세요. 이 부분을 전부 복사해 두세요. 이것이 클러스터에 연결하는 데 사용할 엔드포인트입니다. 다시 여기로 돌아가서 `valky valky c-h as host`를 입력합니다. 그리고 포트 번호(Das P)는 제거해야 합니다. 이것이 실제 연결에 사용할 포트입니다. 대부분의 경우 이 포트를 찾을 수 있을 겁니다. 시간은 변경하지 않았다면 6379일 겁니다. 클러스터에 연결하기 위해 A-c를 플라이로 추가하여 클러스터 연결을 처리하는 방법을 알 수 있도록 하고, TLS를 활성화하여 SSL을 사용할 수 있도록 합니다. 이 모든 것이 잘 작동하면 연결 완료입니다. 좋습니다. 만약 제가 키를 모른다면, 보세요. 이미 키가 있습니다. 어떻게 된 걸까요? 잠시 후에 설명드리겠습니다. 어쨌든 데이터베이스에 연결되었습니다. 다음 단계에서는 파이썬 애플리케이션을 사용하여 이 데이터베이스에 실제로 데이터를 쓸 것입니다. 이 애플리케이션은 파이썬 Redis 라이브러리 등을 사용하는 멋진 웹 애플리케이션이 될 것입니다. 자, 그럼 시작해 볼까요? Amazon Memory DB for Valky에 웹 애플리케이션을 연결하는 것이 얼마나 쉬운지 보여주기 위해 파이썬 Flask 애플리케이션을 만들고 데이터를 읽고 쓰도록 하겠습니다. 새 파이썬 프로젝트를 시작하겠습니다. 프로젝트 이름은 Flask라고 하겠습니다. V key도 괜찮겠죠? 디렉토리도 변경하고 Flask 파일을 생성해 보겠습니다. 가상 환경은 우리가 필요한 모든 것을 설치할 수 있는 공간입니다. 파이썬 3, V, V 명령어를 사용하면 적절한 패키지 버전 등을 사용하여 작업할 수 있는 가상 환경을 만들 수 있습니다. 가상 환경에 접속하려면 ` source`와 ` bin activate` 명령어를 실행합니다. 만약 이 명령어를 실행할 수 없거나 "명령을 찾을 수 없음" 오류가 발생하면, 필요한 패키지를 설치하기 위해 다음 명령어를 실행해야 합니다. 운영체제에 따라 설치 방법이 다를 수 있지만, 여기서는 Auntu를 예로 들어 설명하겠습니다. 좋습니다. 이제 필요한 패키지 몇 가지를 설치해 보겠습니다. 먼저 웹 프레임워크인 Flask를 `pip install flask`로 설치하고, Reddis도 설치합니다. Reddis 라이브러리는 Valky와 연동하는 데 사용됩니다. Valky는 Reddis와 호환되므로 바로 사용할 수 있습니다. 이제 디렉토리를 생성해 보겠습니다. 음, 템플릿을 만들 거예요. 나중에 인덱스 HTML 파일들을 만들 건데, 작성 방법은 보여드리지 않고 그냥 복사해서 붙여넣을게요. 템플릿 디렉토리를 만들고 `app P1 y`라고 입력할 거예요. 그러니까 ` app py`에 내용을 쓸 거라는 거죠. 자, 지금 바로 해볼게요. 먼저 필요한 라이브러리들을 가져오고, 플래시 메모리를 설정한 다음, 레드디스(Reddis) 부분, 즉 발키리( Valky) 부분으로 넘어갈게요. 아주 중요한 이 줄이 있어요. 이 줄은 레드디스 라이브러리의 특정 부분을 가져오는 거예요. `reddis cluster as reddis`라고 입력하는 거죠. 메모리 DB에서 클러스터를 실행했기 때문에 이 부분을 사용하는 게 중요해요. 단일 인스턴스가 아니니까요. 발키리에 성공적으로 연결하려면 이 부분을 사용해야 해요. 자, 이제 애플리케이션의 기본 요소들이 실행되고 있어요. 실행은 되는데, 아마 실패하거나 여러 가지 오류가 발생할 거예요. 음, 경로 설정에 대해 이야기해 볼까요? 연결에 필요한 기본 설정을 몇 가지 해 보겠습니다. 먼저 Valky 데이터베이스에 연결하도록 설정해 보죠. 이를 위해 두 개의 변수와 연결 문자열을 정의해야 합니다. 먼저 VY 엔드포인트 변수와 Valky 포트 변수를 정의할 건데요, 이 포트들은 연결 문자열 안에서 사용될 겁니다. 포트는 특별히 다르게 설정하지 않는 한 항상 6379입니다. Valky 엔드포인트를 얻으려면 ADS 콘솔에서 복사해 왔죠? 다시 복사해서 이 변수 ​​안에 붙여넣어 보겠습니다. 포트는 이미 port 변수에 정의되어 있으므로 필요 없으니 삭제합니다. 자, 이제 Redis 클라이언트를 만들어 보겠습니다. 이 연결 문자열이 바로 Redis에 대한 연결 문자열입니다. 저는 이 연결 문자열을 Redd라고 부르겠습니다. 왜냐하면 실제로 Redis를 사용하고 있기 때문이죠. Redis를 사용하는 이유는 다음과 같이 연결해야 하기 때문입니다. Reddis 클러스터 연결을 설정하고, 호스트를 V 키 엔드포인트로, 포트를 V 키 포트로 정의합니다. 두 가지 옵션도 설정하는데, 하나는 응답 코드이고 다른 하나는 SSL입니다. 이제 연결하여 애플리케이션을 사용할 준비가 되었습니다. 시작하기 전에 몇 가지를 생성해 보겠습니다. 이 애플리케이션은 쇼핑 카트 애플리케이션입니다. 인메모리 데이터베이스의 가장 일반적인 사용 사례 중 하나는 캐싱입니다. 이 경우 사용자의 쇼핑 카트 내용을 캐싱할 것입니다. 이렇게 하면 사용자가 데이터에 훨씬 빠르고 쉽게 액세스할 수 있습니다. 쇼핑 카트에 필요한 모든 기능을 구현하는 클래스를 만들고 잠시 후에 다시 돌아오겠습니다. 자, 스피드 코딩을 즐기세요! [음악] [음악 ] [음악] 코딩 과 문제 해결 몽타주를 즐겁게 시청하셨기를 바랍니다. 하지만 제가 여기서 한 일은 이렇습니다. 쇼핑 카트용 클래스를 만들었는데, 아주 간단하지도 않고 아주 복잡하지도 않습니다. 그냥 Valky에서 데이터를 저장하고 검색하는 데 사용하는 쇼핑 카트입니다. 몇 가지 핵심 요소가 있는데, 바로 이 줄입니다. getred. `cin.get`은 Redis 또는 Valky에서 데이터를 가져오는 것이고, `set`은 Valky에 데이터를 설정하는 것입니다. Valky에서 데이터를 가져오고 설정하는 방법은 간단합니다. 먼저 키를 정의하는데, Valky나 Redis의 키는 보통 이름, 열, 숫자 등으로 구성됩니다. 그리고 데이터를 추가하는데, 이 경우에는 특정 요소에 대한 모든 데이터가 담긴 JSON 객체를 전달합니다. 키는 어떻게 설정할까요? ` index` 함수에서 임의의 사용자 ID를 생성하고, 이를 특정 객체의 이름으로 사용합니다. 실제로는 실제 사용자 ID나 이와 유사한 것을 사용하겠지만, 이 데모에서는 이 정도면 충분합니다. 참고로, 모든 코드는 설명란에 추가해 두었으니 확인해 보세요. 추가적으로 ` index.html` 템플릿 파일도 첨부했습니다. Tailwind 템플릿 파일입니다. CSS로 구동되는 HTML 파일인데, 플래시 템플릿일 뿐입니다. 자세히 설명하진 않겠지 만, 이 파일은 여러분의 애플리케이션에 멋진 디자인을 더해줄 겁니다. 자, 이제 이 파이썬 앱을 실행해 보겠습니다. py, 보세요. 모든 주소에서 실행되고 있습니다. 이 경우에는 포트 5000을 사용하고 있지만, 실제 운영 환경에서는 다른 포트로 변경하는 것이 좋습니다. EC2 인스턴스로 돌아가서, 제 EC2 인스턴스의 DNS를 복사해서 여기에 붙여넣겠습니다. 포트 5000, Valky 쇼핑 카트에 담으시겠습니까? 네, 담을 수 있습니다. 사과 5개를 담겠습니다. 짠! 장바구니에 담겼네요. 오렌지도 담을 수 있습니다. 128개, 코모도르 64개도 담았습니다. 모두 장바구니에 담겼네요. 이제 하나씩 빼보면 Valky에서 모두 삭제될 겁니다. 마지막으로 한 가지 더 보여드리겠습니다. 여기에서 Ctrl+ Z를 누르면... 멈췄네요. 제 애플리케이션이 중지된 것 같아요. 네, 사실 별로 상관없지만, 제 V 키, 그러니까 Valk 키에 연결해서 예전에 했던 명령어를 찾아보면, 여기 있네요. 네, 좋아요. 이제 데이터베이스에 연결해 볼게요. 이렇게 Keys라고 입력하면 Valky에서 사용 가능한 모든 Keys를 볼 수 있어요. 멋지네요. 그럼, 여기서 특정 데이터를 어떻게 가져올 수 있을까요? 여기 있는 Keys를 복사해서 getkey라고 입력하면 Commodore의 데이터가 나와요. 예를 들어, Commodore의 데이터가요. 다른 것도 할 수 있어요. 이게 더 큰 데이터인 것 같은데, 이걸 가져오면 돼요. 보세요! 사과, 오렌지, Commodore. 이 모든 데이터가 이제 메모리 데이터베이스에 저장되었어요. 애플리케이션에서 아주 빠르게 접근할 수 있죠. 데이터를 삭제하려면 flushDB라고 입력하면 아주 간단해요. Keys star, 아무것도 없어요. 자, 이렇게 메모리 데이터베이스를 사용하는 간단하고 기본적인 방법을 알아봤습니다. 기억하시죠? 데이터와 클라우드 환경에서는 속도가 가장 중요합니다. 애플리케이션이 특정 데이터를 매우 빠르게 읽고 쓸 수 있어야 하므로 인 메모리 데이터베이스를 간과하지 마세요. 지금 바로 인메모리 데이터베이스를 시작하는 가장 좋은 방법은 Amazon Memory DB for Valky를 살펴보는 것입니다. 현재 이용 가능하며 Amazon DB for Redus OSS보다 30% 저렴합니다. 보시다시피 클러스터를 실행하는 데 단 10번의 클릭밖에 걸리지 않습니다. 한번 사용해 보세요. 더 많은 기술 콘텐츠에 관심이 있다면 이 채널을 구독해 주세요. 이 영상이 마음에 드셨다면 좋아요 버튼을 눌러주세요. 배울 내용이 훨씬 더 많으니 다른 영상들도 확인해 보세요. 다음 영상에서 뵙겠습니다.
