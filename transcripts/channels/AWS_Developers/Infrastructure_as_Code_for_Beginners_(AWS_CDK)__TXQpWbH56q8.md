# Infrastructure as Code for Beginners (AWS CDK)

- 영상 링크: https://www.youtube.com/watch?v=TXQpWbH56q8
- 채널: AWS Developers
- 업로드일: 2026-05-27
- 자막 언어: ko
- 단어 수: 약 6029개

---

## 스크립트

클릭을 멈추자 모든 게 이해되기 시작했어요. 이유는 다음과 같습니다. 아, 드디어 끝났네요. 세 가지 AWS 서비스, DynamoDB 테이블, Lambda 함수, API Gateway를 모두 구성하고 연결하고 배포했습니다. 클릭과 스크롤을 엄청나게 많이 해야 했지만 , 적어도 다시는 이 작업을 반복할 필요가 없다는 게 다행입니다. 롭, 클라이언트가 스테이징 환경 승인을 방금 마쳤어. 미국 서부 2 서버에서도 동일한 API 스택을 구축할 수 있습니까 ? 그러니까, 동일한 스택, 동일한 구성, 동일한 권한인데, 단지 하루가 끝날 때쯤에만 그런다는 거죠. 감사해요. 와, 그럼 제가 처음에 뭘 클릭했는지조차 적어두지 않았던 기억을 떠올리며 다른 지역에서 그 모든 걸 다시 해야 한다는 거네요. 예전에는 미래의 제가 다시는 그럴 필요가 없을 거라고 생각했고, 사진처럼 모든 걸 기억한다고 믿었거든요. 음, 과거의 나는 이 점을 포함해서 많은 것들에 대해 틀렸어. 아시 다시피, 이 인턴은 AWS 콘솔을 통해 DynamoDB, Lambda, API Gateway 및 이들 간의 IAM 연결을 사용하여 서버리스 REST API를 설정하고 있었습니다. 클릭을 많이 해야 했고, 한 번은 아주 잘 작동했지만, 그 후 다시 해야 할 때 콘솔은 도구가 아니라 오히려 벌칙이 되기 시작했습니다. 하지만 그는 인프라스트럭처를 코드로 관리하는 법을 배우 면서 비로소 모든 것이 이해되기 시작했습니다. 클릭하는 것에 대한 여담입니다. 업계에서는 이를 클릭 운영(click ops)이라고 부릅니다. 알면 알 수록 좋다. 다시 본론으로 돌아가서. 롭, 클라이언트가 스테이징 환경을 승인했는데, 동일한 구성과 권한 으로 미국 서부 2 서버에 동일한 API 스택을 구축해 줄 수 있을까 ? 그들은 오늘 하루가 끝나기 전에 그것이 필요해요. 아주 아주 쉬워요. 엄청난. 어떻게 된 거죠? 새로운 지역에서 동일한 API를 사용하며, 결국에는 문제가 발생합니다. CDK 배포, 그것만 있으면 돼요. 완료. 아, 더 많은 지역이나 다른 구성이 필요하신가요? 저를 시험해 보세요. 저는 아직 존재하지도 않는 곳에 배포하고 있습니다. 농담이에요. 저는 모든 곳에 배포합니다. 미스터 인터내셔널, 네. 인프라스트럭처를 코드로 관리하자, 시작해 보자 . 좋아요, 인턴이 이제 좀 더 생산적이고 약간 과하게 신이 났지만 괜찮아요. 이 영상에서는 인프라스트럭처를 코드로 관리하는 방법을 배울 거니까요. 이걸로 클라우드 환경을 한 번의 명령어로 배포하고, 여러 지역에 복제하고, 문제가 생기면 (어차피 언젠가는 문제가 생길 테니까요) 롤백하고 , 마지막으로 AWS에서 월말에 깜짝 감사 편지를 보내지 않도록 모든 걸 완전히 삭제할 수 있게 될 거예요 . 이 사실을 알게 되면, 당신은 단순히 API를 구축하는 사람이 아니라, 문제가 발생했을 때 모든 사람이 떠넘기는 사람이 됩니다. 듣기에는 저주처럼 들리지만, 이력서에는 승진처럼 보일 겁니다 . 자 , 여기 우리가 만들고 있는 지도가 있습니다. 서버리스 REST API, 각각 하나의 작업을 수행하는 세 가지 AWS 서비스. API Gateway는 HTTP 요청을 수신하고 이를 Lambda 함수로 라우팅합니다. 앱의 첫 화면입니다. Lambda는 서버를 관리할 필요 없이 파이썬 코드를 실행해 주는, 즉 서버리스 서비스입니다. 시간당 요금이 아닌 요청당 요금이 부과되며, DynamoDB는 용량 설정을 따로 하지 않아도 확장 가능한 NoSQL 데이터베이스이므로 데이터를 저장하는 데 편리합니다 . 각 구성 요소를 하나씩 배포하고, 제대로 작동하는지 확인한 다음 다음 구성 요소를 추가합니다. 터미널에서 바로 사용할 수 있는 CRUD API가 제공되며, 설명란에 링크된 블로그에서 제가 사용하는 모든 명령어를 확인할 수 있습니다. 이를 통해 여러분도 제가 사용하는 코드와 명령어를 재사용하여 따라할 수 있습니다. 콘솔을 통해 모든 단계, 모든 클릭을 거쳐 API를 구축할 겁니다. 당신이 이렇게 하길 바라서가 아니라, 문제를 해결하기 전에 제가 느끼는 고통을 당신도 느껴보길 바라기 때문입니다 . 그리고 제 체면이 실시간으로 떨어지는 걸 볼 수 있도록 구석 어딘가에 클릭 카운터를 넣어둘 거예요. 저는 AWS 콘솔에 접속해 있습니다. DynamoDB를 검색해 보겠습니다. 그러니까, 그건 한 사람에게 한 번은 통하는 거죠. 자 , 이제 월요일 아침 버전을 상상해 보세요. 팀원에게도 동일한 API가 필요합니다. 슬랙 메시지를 보내고, 다이나모DB에 접속해서 테이블 생성 버튼을 클릭하고, 파티션 키를 ID로 설정한 다음, 람다에 접속하는 등등의 과정을 거치면 됩니다. 슬랙 메시지가 꽤 길네요. 세 번째 문단쯤 되면 읽기를 멈춘다. 그들은 ID라는 대문자로 된 파티션 키를 사용하여 테이블을 생성하고 람다가 왜 빈 결과를 반환하는지 한 시간 동안 고민합니다. DynamoDB는 대소문자를 구분하지만 친구 관계는 대소문자를 구분하지 않기 때문입니다. 스테이징 환경이 필요합니다. 동일한 단계를 따라 클릭 운영을 진행하세요. 대부분의 설정을 제대로 하셨네요. 잘못된 부분은 일주일 후에야 드러나는데, 그때는 스테이징 환경이 프로덕션 환경과 다르게 동작하고 아무도 그 이유를 파악할 수 없기 때문입니다. 그런데 누군가가 API 게이트웨이를 삭제해 버립니다. Lambda 함수의 IAM 역할에는 3주 전에 작성된 사용자 지정 정책이 있습니다. 당신은 과거의 당신이 미래의 당신이 기억할 거라고 생각했기 때문에 아무데도 저장해 두지 않았습니다. 과거의 당신은 많은 것에 대해 틀렸어요. 한 관리자가 "어떤 환경에서 암호화가 활성화되어 있나요?"라고 묻습니다. 세 가지 환경, 각 환경당 네 가지 서비스, 그리고 예/아니오 질문에 답하는 데 20분을 허비했습니다. 매번 똑같은 패턴이에요. 콘솔은 두 번째 사람이나 다른 환경이 화면에 나타나기 전까지는 정상적으로 작동합니다. 그러니까, 그 클릭 소리는 파일일 수도 있다는 거죠. 오류 발생 후 가져오기, 팀원에게 전달 또는 재실행을 위해 커밋하는 파일입니다 . 그것이 바로 코드형 인프라입니다. AWS 리소스를 설명하는 파일을 작성합니다 . 해당 파일을 읽어들이는 도구가 있고, 그러면 모든 것이 자동으로 생성됩니다. 같은 파일인데도 매번 같은 결과가 나옵니다. 팀원도 같은 API가 필요하신가요? 그들은 저장소를 복제하고 하나의 명령어를 실행합니다. 동일한 테이블, 동일한 이름 대소문자, 동일한 타임아웃, 동일한 권한. 스테이징 환경이 필요하신가요 ? 스택 이름만 다르게 하여 동일한 코드를 배포합니다 . 누군가 API 게이트웨이를 삭제했나요? 배포 명령을 다시 실행하세요 . 이 도구는 누락된 리소스를 감지하고 다시 생성합니다. 3주 전에 작성하신 사용자 지정 IAM 정책은 스택 파일에 있으며, 누가 왜 작성했는지 보여주는 Git 기록이 있는 버전 관리 시스템을 통해 관리되고 있습니다 . 회복에는 오후 내내 걸리는 것이 아니라 단 몇 분이면 충분합니다 . 비용 관리. 환경 설정이 완료되면 단 하나의 명령으로 모든 것을 올바른 순서대로 삭제할 수 있습니다. 잊어버린 DynamoDB 테이블로 인해 몇 달 동안 조용히 요금이 청구되는 일이 없습니다 . 2023년에 했던 튜토리얼에서 나온 표를 하나 발견했는데, 항목은 하나도 없었지만 가격은 4.73달러나 나왔어요. 그걸 보고 제 예산 관리 능력이 부족한 건 아닌지 의문이 들었죠. 인프라스트럭처 애즈 코드(Infrastructure as Code)는 이러한 문제들을 해결합니다 . 다음 질문은 어떤 도구를 사용할 것인가입니다. AWS에는 세 가지 주요 옵션이 있으며, 각각의 장단점을 명확히 설명하고자 합니다. 하지만 자세한 비교는 별도의 영상에서 다룰 예정입니다. 그러니까 CloudFormation이 기반입니다. YAML 또는 JSON 형식으로 파일을 작성하여 AWS에 전달하면 AWS가 모든 것을 생성합니다. 우리가 사용할 것은 CDK인데 , 이는 클라우드 개발자 키트의 약자입니다. 이 프로그램은 내부적으로 CloudFormation 템플릿을 생성하는데 , CloudFormation은 강력 하지만 출력 내용이 매우 장황합니다. 각 리소스가 어떤 모습일지에 대한 모든 세부 사항을 직접 지정해야 하지만, CDK는 이러한 빈칸을 채워줍니다. 또 다른 옵션은 Terraform입니다. Terraform은 HCL이라는 자체 언어를 사용하며 여러 클라우드 제공업체에서 작동합니다. 이 코드는 CloudFormation을 통하지 않고 AWS API를 직접 호출합니다. AWS, Azure, GCP에서 동시에 개발하는 경우 Terraform은 훌륭한 멀티 클라우드 솔루션입니다. AWS만 사용하는 경우, CDK(클라우드 개발자 키트)를 사용하면 이러한 모든 번거로움이 사라집니다 . AWS CDK를 사용하면 Python, TypeScript, JavaScript, Java, C#, 또는 Go로 인프라를 작성할 수 있습니다. 이 도구는 사용자의 코드를 CloudFormation 템플릿으로 변환하고 배포합니다. 반복문, 조건문, 자동 완성 등 프로그래밍에서 익숙한 것들을 접할 수 있고, 단순히 YAML 문서를 수동으로 작성하는 것이 아니라 실제 프로그래밍을 할 수 있습니다. 이 비디오는 Python CDK를 사용합니다. 아, 그리고 공사 시작하기 전에 비용 좀 간단히 확인해 볼까요? Lambda의 상시 무료 요금제는 월 100만 건의 요청을 지원합니다. DynamoDB의 상시 무료 티어는 프로비저닝 모드에서 25GB 의 스토리지와 25 읽기/쓰기 용량 단위를 제공합니다 . 기본적으로 이 튜토리얼은 무료 계정에서도 비용 부담 없이 따라할 수 있습니다 . 마지막에 모든 걸 완전히 분해해서 아무것도 작동하지 않도록 하는 방법을 보여드리겠습니다. 참고로, 이 과정을 따라하려면 AWS 무료 계정과 AWS CLI 설정이 필요합니다. 무료 티어 계정이 없으시다면, AWS 무료 티어 계정을 올바르게 설정하는 방법에 대한 이 영상을 시청하시고 , AWS CLI를 구성하는 방법에 대한 이 영상도 시청해 보세요. 그리고 마지막으로 한 가지 더 말씀드릴 게 있습니다. 본격적인 개발을 시작하기 전에, 이 프로젝트에는 만약 빼먹으면 모든 API 호출이 접근 거부 예외와 함께 실패하는 코드가 딱 한 줄 있습니다. 그 부분이 나오면 어떤 대사인지 보여드릴게요 . 첫 번째 프로젝트에서 그걸 깜빡해서 오류 로그를 2시간 동안 쳐다봤어요. 2시간. 그 시간에 저글링이라도 배울 수 있었을 텐데, 아니면 뭐든 말이야. 그랬다면 훨씬 더 생산적이었을 거예요, 그건 확실해요. 이제 프로젝트를 설정할 시간입니다. 자, 시작해 볼까요? 시작하기 전에 몇 가지 사전 준비가 필요합니다. 터미널을 열겠지만, 무료 계정이 있고 루트 사용자가 아닌 관리자 사용자로 로그인했는지 확인해 주세요 . 자, 그럼 시작하겠습니다. 자, 터미널 창이 열려 있는데, 시작하기 전에 몇 가지 필수 조건을 확인하고 싶습니다. 그러니까 파이썬 3, 즉 Dash 버전이 필요합니다. 파이썬 버전이 출력될 것입니다. 또 하나 필요한 것은 노드가 있는지 확인하는 것입니다. Node.js는 향후 클라우드 개발 키트용 NPM 패키지를 설치할 수 있게 해주는 핵심 요소이기 때문입니다 . 마지막으로, 리소스를 프로비저닝할 AWS 계정에 노트북이 연결되어 있는지 확인해야 합니다 . 자, 그럼 AWS STS를 살펴보겠습니다. 이것은 AWS CLI인데, AWS 명령어를 입력할 수 있게 해줍니다. STS는 발신자 정보를 가져오며, 출력 결과에 발신자가 누구인지 표시될 것입니다. 그럼 지금 터미널을 정리하겠습니다. 그러니까 그게 필수 조건 확인 사항입니다. 괜찮은. 먼저 CDK 프로젝트의 스캐폴딩부터 시작하겠습니다. 그렇다면 제 프로젝트를 어디에 두고 싶을까요? 저는 바탕화면에 설치하는 걸 좋아해서 CD 키를 눌러 바탕화면으로 이동한 다음, 음... 시작하기 전에 한 가지 필요한 게 있는데, AWS CDK라는 패키지입니다. ` NPM install dash G AWS CDK`를 입력하면 클라우드 개발 키트(CDK) 패키지가 설치됩니다. 이 패키지를 사용하면 CDK를 이용하여 클라우드에 리소스를 프로비저닝할 수 있습니다. 저는 이미 설치했기 때문에 출력 결과가 당신과 약간 다를 수 있습니다. 괜찮은. CDK dash dash version 명령어를 입력하면 버전 정보가 출력되는 것을 확인할 수 있습니다. 이제 프로젝트 디렉토리를 생성하고 Python을 사용하여 CDK 앱을 초기화해 보겠습니다. 지금 저는 데스크톱을 사용하고 있습니다. 바탕화면에 CRUD API라는 디렉토리를 만들겠습니다. 즉, MKDIR CRUD API를 입력하세요. 방금 생성한 CRUD API 디렉토리로 이동합니다. 지금쯤이면 CRUD API라는 폴더에 있어야 할 겁니다. 그리고 여기서 CDK 프로젝트를 초기화하겠습니다. 그렇게 하려면 다음 명령어를 사용하면 됩니다. CDK 초기화 앱 {dash} {dash} 언어는 Python입니다. 그리고 지금은 우리가 시작하는 데 필요한 모든 기본 파일 구조, 기본 코드 및 구성 요구 사항을 생성하고 있습니다 . 그게 어떤 모습일지 한번 봅시다. 실제로 어떻게 보이는지 확인하려면 IDE를 사용하는 게 좋습니다. 제가 선호하는 IDE는 Kiro이므로, 이 폴더를 Kiro에서 열겠습니다. 자 , 여기 제 Kiro IDE가 보이실 겁니다. 가장 최근 프로젝트인 CRUD API를 열기 위해 '프로젝트 열기'를 클릭하겠습니다. 완벽한. 이제 많은 파일과 폴더가 보일 겁니다. 화면에는 이렇게 보일 겁니다. 테스트 폴더가 있어야 합니다. CRUD API 폴더를 만들어야 합니다. 즉, 상위 폴더와 동일한 이름의 폴더를 하나 더 만들어야 합니다. 자, 이것이 우리가 앞으로 작업할 기본 틀입니다. CDK가 프로젝트 구조를 생성해 주었습니다 . 이제 파이썬에서 가상 환경을 활성화하고 필요한 패키지를 설치해 보겠습니다. 그리고 이전에 사용하던 터미널 대신, 키로에 내장된 터미널을 사용하겠습니다. 터미널, 새 터미널. 저는 여기서부터 작업을 시작할게요. 시원한. 자, 이 터미널에 source라고 입력하겠습니다. 가상 환경을 활성화하기 위해 이 명령어를 입력하겠습니다. 그런 다음 requirements.txt 파일에 있는 모든 필수 구성 요소를 설치하겠습니다 . 그래서 이 프로그램에는 몇 가지 필수 조건, 즉 가상 환경에 설치해야 할 파이썬 패키지들이 포함되어 있습니다. requirements.txt 파일에 나열된 모든 필수 패키지를 pip를 사용하여 설치하겠습니다 . 완벽한. 이제 필요한 모든 패키지가 설치된 파이썬 가상 환경이 구축되었으므로 개발을 시작할 수 있습니다. 그럼 다음 단계로 넘어가겠습니다. 따라서 여기서 중요한 파일은 CRUD API 스택인 stack.py 파일입니다. CRUD API 폴더 안에 위치해 있습니다. 네, 바로 그곳에서 AWS 리소스를 정의합니다. 디렉토리 이름 이 CRUD API인 것을 알 수 있을 겁니다. 네, 폴더 이름과 똑같이 CRUD API 스택이라고 이름 붙여진 것을 알 수 있을 겁니다. 그러니까 스택이란 앱을 위해 클라우드에 함께 배포될 관련 리소스들의 집합이라고 할 수 있습니다 . 자, 이제 클릭하는 대신 여기서부터 리소스를 정의하기 시작할 겁니다. 그러니까, 우리가 클릭해서 수행한 모든 작업을 파이썬 코드로 여기에 표현할 겁니다. 이게 바로 작동 방식입니다. 이것은 훌륭한 기본 템플릿이지만, 우리가 만들 각 람다 함수마다 모든 람다 함수를 정리할 폴더를 만들어야 합니다 . 그럼, 여기에 lambda라는 디렉토리를 하나 만들겠습니다 . 그리고 바로 이곳에서 람다 함수를 정의할 것입니다. 이 경우에는 람다 함수가 하나뿐이므로 여기에 넣겠습니다. 사람들을 사로잡는 일련의 단계. CDK는 일회성 부트스트랩이 필요합니다. 이렇게 하면 CDK가 배포에 사용하는 일부 IAM 역할이 포함된 S3 버킷이 AWS 계정에 생성됩니다. 이 경우, 패키징된 람다 함수는 S3에 zip 파일 형태로 저장됩니다 . 계정당, 지역당 한 번씩 실행하면 됩니다 . 이미 완료했다는 사실을 잊어버리고 6개월 후에 다시 실행하면, 이미 부트스트랩이 완료되었다는 메시지를 보게 되고, 짧지만 강렬한 기쁨을 느낄 것입니다. 좋습니다, 그럼 이 명령어를 실행해 보겠습니다. 그래서 여기에 CDK 부트 스트랩이라고 입력하겠습니다. 좋습니다, 이제 기본 설정이 완료되어 바로 사용할 수 있을 것 같네요. 이제 정의할 리소스를 제대로 배포하기 위해 필요한 S3 버킷 및 기타 리소스를 확보했습니다 . 프로젝트의 뼈대는 완성되었고, 초기 설정도 끝났습니다. 이제 본격적인 구축에 들어갑니다. 먼저 데이터베이스인 DynamoDB부터 살펴보겠습니다. CDK에서는 몇 줄의 파이썬 코드로 정의하면 나머지는 CDK가 처리합니다 . 코드를 작성하시면 저희가 AWS에 배포하고 정상적으로 작동하는지 확인합니다. 빌드, 배포, 검증. 빌드, 배포, 검증. 이 영상의 나머지 부분은 이와 같은 워크플로를 따릅니다 . 좋습니다. 먼저 CRUD API 폴더를 열겠습니다. 그 안에 CRUD API 스택이라는 파일이 있는데 , 제가 필요한 파일입니다. 지금은 터미널 창을 최소화해 두세요. 그리고 바로 이 함수 안에서 인프라를 코드로 정의하게 될 것입니다 . 파이썬에서는 ` def` 키워드를 사용하여 이렇게 작성합니다. 그래서 여기에는 SQS 리소스를 배포하는 예시도 나와 있습니다 . 음 , 그럼 다이나모DB를 이용해서 해볼까요? 자, 이제 이 파일의 모든 내용을 아래 링크된 블로그에 있는 코드로 바꿀 겁니다. 링크된 블로그 에서 코드를 찾아서 복사해서 붙여넣기만 하면 됩니다. 자, 그럼 이 코드를 살펴보죠. 그래서 저희는 AWS CDK에서 여러 클래스를 가져오고 있습니다. 그래서 스택은 AWS 리소스를 그룹화할 수 있도록 해줍니다. 어, 삭제 정책은 스택을 삭제할 때 발생하는 상황을 제어합니다. 음, 지금은 CFN 출력에 대해 걱정하지 않으셔도 됩니다. 나중에 살펴보겠지만, 리소스를 배포한 후에는 배포된 리소스의 값을 출력하는 데 사용됩니다 . 그리고 여기서는 DynamoDB 모듈도 가져와서 DynamoDB라는 이름으로 별칭을 지정하고 있습니다 . 그래서 타이핑할 양이 조금 줄어듭니다. 그리고 이 기본 클래스, 즉 CRUD API 스택에서, 바로 이곳에서 데이터베이스를 정의할 것입니다. 자, 여기서 우리는 DynamoDB 테이블을 정의하고 일반 변수로 이름을 지정했습니다. 이 변수의 이름은 원하는 대로 지정할 수 있지만, 여기서는 DynamoDB 키워드를 사용하고 있습니다. 이 키워드는 여기 에서 가져온 것입니다. 그리고 도트 테이블이라는 방식을 사용합니다. 따라서 DynamoDB.table을 사용하고 괄호 안에 생성할 테이블에 대한 정보를 입력할 수 있습니다. 자, 바로 이 줄이 DynamoDB 테이블을 생성합니다. 여기서는 테이블 이름 , 파티션 키( 고유 ID), 테이블의 기본 키, 청구 모드, 삭제 정책을 지정합니다. 그래서 우리가 클릭을 할 때, 즉 클릭 작업을 수행할 때 설정하고 싶은 모든 정보들을, 바로 여기서 코드를 사용해서 설정할 수 있습니다. 그래서 이것이 바로 DynamoDB 테이블을 구축하고 배포하는 방법에 대한 정의입니다. 그리고 바로 이 부분, 출력 결과를 볼 수 있는 곳에서 CFN 출력을 확인할 수 있는데, 이는 바로 이 가져오기(import)에서 나온 것으로, 생성된 리소스의 이름을 출력할 수 있게 해줍니다. 따라서 이 경우, 이 함수는 데이터베이스 테이블과 관련되어 있으므로 배포 후 테이블 이름과 테이블 설명을 출력하는 print 문을 할당합니다. 배포하기 전에 항상 해야 할 가장 좋은 방법은 현재 저희가 가지고 있는 코드와 AWS 계정에 실제로 있는 코드의 차이점을 확인하는 것입니다. 이렇게 하면 클라우드 계정에 추가, 삽입 또는 제거될 내용의 차이를 파악할 수 있습니다. 자, 그럼 터미널을 먼저 정리하고 CDK uh CDK diff라고 입력해 볼게요. 그리고 이 기능을 배포했을 때 클라우드 계정에 어떤 변화가 있을지 확인해 볼 것입니다 . 네 , 지금은 읽기 좀 어려운 부분이 많지만, 가장 중요한 건 ' 리소스'라고 쓰여 있고 더하기 기호가 있는 부분입니다. 그래서 우리는 DynamoDB라는 새로운 리소스를 추가하고, 테이블 이름은 items 테이블로 지정할 것입니다. 네 , 맞습니다. 바로 그거예요. 자, 이제 배포할 준비가 되었습니다. 이것은 CDK 배포라는 하나의 명령어이며 , 모든 것이 제대로 작동하는지 확인하기 위해 권한을 요청한 후 최종 버튼을 누릅니다. 하지만 최종 버튼은 누르지 않아도 자동으로 눌러줍니다. 아, 사실 이번엔 내 허락을 구하지 않았어 . 흥미로운. 좋아, 좋아. 현재 배포가 진행 중이며, 배포가 완료되면 콘솔에서 확인할 수 있습니다. 네, 배포가 완료되었다고 나오네요. 25초 맞죠? 자, 그럼 콘솔을 살펴보겠습니다. 그래서 저는 다시 게임 콘솔로 돌아왔습니다. DynamoDB에서 확인해 보겠습니다. 제가 테이블로 가면 거기에 있어요. 그리고 이 테이블의 이름은 스택의 이름에 따라 정해지기 때문에, 여기에 무작위로 생성된 고유한 문자들이 포함될 것입니다. 이것이 바로 우리 테이블의 이름입니다. 그래서 우리는 일일이 클릭하며 설정을 진행하지 않고도 이 모든 것을 구성했습니다 . 그래서 우리는 코드로 배포하는 첫 번째 인프라를 구축했습니다. 저희는 현재 인프라스트럭처 코드(Infrastructure as Code)를 사용하여 클라우드에 배포하는 데이터베이스를 보유하고 있습니다 . 이제 우리는 해당 데이터베이스와 상호 작용하여 생성, 읽기, 업데이트, 삭제 작업을 수행하는 파이썬 코드가 필요합니다 . Lambda 함수는 API 요청을 처리하는 Python 코드입니다. 그리고 우리는 여기에 그 내용을 적어두고 람다를 코드형 인프라로 정의할 것입니다. 자, 그럼 이제 람다 함수를 만들어 보겠습니다. 그 파일은 Lambda라는 폴더에 저장될 겁니다. Lambda에서 index.py라는 새 파일을 만들겠습니다. 그리고 제가 미리 준비해 둔 코드를 붙여넣겠습니다. 다시 한번 말씀드리지만 , 이 모든 코드는 아래 설명란에 링크된 블로그에서 확인하실 수 있습니다 . 다음은 DynamoDB와 제대로 통신하기 위해 필요한 패키지를 가져오는 Lambda 함수용 Python 스크립트입니다. 이 스크립트는 고유 ID를 생성하고 환경 변수에서 테이블 이름을 읽어오는 등의 작업을 수행합니다. 이 경우에는 항목 테이블이었습니다. 테이블 이름을 하드코딩하면 안 되겠죠? 왜냐하면 이 Lambda 함수를 다른 DynamoDB 테이블에도 재사용하고 싶을 때, 함수가 어떤 테이블을 사용하고 있는지 동적으로 인식하도록 하고 싶으니까요. boto3는 파이썬에서 다양한 리소스와 프로그래밍 방식으로 통신할 수 있도록 해주는 패키지입니다. 이 경우 boto3를 사용하여 DynamoDB에 연결하고, 해당 테이블 변수에 DynamoDB 테이블을 할당합니다. 이때 환경 변수에 저장된 테이블을 사용하게 됩니다. 그리고 이것은 람다 함수의 이름을 지정하는 기본적인 파이썬 코드입니다. 람다 함수는 이 핸들러를 호출하고, 데이터베이스 에서 항목을 가져오거나, 게시하거나, 저장하거나, 삭제하려는지에 따라 작업을 시도합니다 . 데이터베이스에서 관련 작업을 수행합니다. 다음은 DynamoDB 테이블에서 모든 항목을 가져오는 코드입니다 . 테이블.스캔 오른쪽. 그러니까 기본적으로 모든 CRUD 작업이 여기에 파이썬으로 작성되어 있습니다 . 하지만 이것은 코드형 인프라(Infrastructure as Code)는 아닙니다. 이것은 람다 함수의 파이썬 코드일 뿐입니다. Flask 앱이나 서버리스 앱처럼 데이터베이스에서 쿼리를 실행하는 데 사용되는 일반적인 코드입니다. 그러니까, 분명히 말씀드리자면, 여기 있는 이 람다 코드는 인프라스트럭처 코드(Infrastructure as Code)가 아닙니다. 인프라스트럭처 애즈 코드(Infrastructure as Code)란 CRUD API 스택 파일에 있습니다. 즉 , 람다 함수의 내용, 즉 람다 함수가 포함할 코드 등을 정의했더라도, 클라우드 환경에서 이 코드를 포함하는 람다 함수를 프로비저닝하도록 CDK에 명시적으로 알리지 않았다는 뜻입니다. 자, 이제 CRUD API 스택으로 이동한 다음, 람다 함수를 나타내기 위해 이 파일에 인프라스트럭처 코드를 추가해야 합니다 . 이제 CDK에서 Lambda 함수를 정의하고 이미 배포한 데이터베이스에 연결합니다. 이 영상 초반에 언급했던 코드 한 줄 기억하시나요? 만약 잊어버리면 모든 게 엉망이 되는 단 한 가지는 무엇일까요 ? 응. 이제 막 쓰기 시작했어요. 괜찮은. 자, 다시 이 파일, 즉 리소스를 정의하는 CRUD API 스택 부분으로 돌아가서, 여기에 있는 모든 내용을 이 새로운 코드 블록으로 교체하겠습니다. 이 코드는 블로그의 해당 단계에서도 찾을 수 있으며, 유일한 차이점은 ` create` 문을 추가하고 Lambda 타임아웃을 위한 `duration`과 같은 몇 가지 새로운 모듈을 임포트했다는 것입니다. Lambda는 서버리스 방식이므로 Lambda 함수의 실행 시간을 제한하는 타임아웃을 설정할 수 있다는 점을 기억하세요 . 또한 람다 리소스를 정의할 수 있도록 여기에 람다 리소스를 가져옵니다 . 그리고 제가 언더스코어 람다라는 별칭을 사용하는 이유는 파이썬 에 실제로 람다라는 이름의 충돌하는 키워드가 있기 때문입니다 . 네, 재밌는 사실 하나 알려드릴게요. 그래서 저는 AWS 람다의 별칭으로 언더스코어 람다(underscore lambda)를 사용하고 있습니다. 아니면 아예 별칭을 지정하지 않고 AWS Lambda로 그대로 두어도 됩니다. 하지만 저를 따라오고 싶으시다면 , 제가 여기서 사용하는 별명을 사용하시면 됩니다. 그래서, 여기서 새로운 소식은 무엇인가요? 여기서 람다 함수가 정의됩니다. 저게 바로 저희 API 핸들러죠, 그렇죠? API에 요청을 보낼 때 실행되는 함수이며, 이 람다 함수를 트리거합니다. 그러니까, 이 점(.) 키워드가 람다 함수를 생성하는 역할을 할 겁니다. DynamoDB.table과 유사하게, 이 dot table 메서드를 사용하면 몇 가지 매개변수를 입력하여 DynamoDB 테이블을 정의할 수 있습니다. 저도 마찬가지예요. 여기서도 같은 개념입니다. 여기서 점 함수 메서드를 사용하면 람다에 몇 가지 매개변수를 지정할 수 있습니다. 이제 람다 함수 API 핸들러를 호출하겠습니다. 파이썬 3.12 버전을 사용할 예정입니다. 그리고 해당 코드는 lambda라는 폴더 에 있는 lambda라는 파일의 에셋에서 코드를 사용하게 될 것입니다 . 그러니까 람다 폴더를 압축해서 업로드하는 겁니다. 각 람다 함수마다 폴더가 필요합니다 . 이 경우에는 테이블이 하나뿐이므로, 어떤 DynamoDB 테이블을 쿼리해야 하는지 알 수 있도록 index.py 파일이 있는 폴더를 하나만 만듭니다. 만약 DynamoDB 테이블이 여러 개 있다면 어떻게 해야 할까요? 네, 이 함수는 ' 테이블 이름'이라는 환경 변수를 사용할 겁니다. 그리고 이 변수, 즉 ' items 테이블'에 이미 배포된 테이블의 이름을 지정해서, 이 람다 함수가 DynamoDB 테이블에 연결해야 할 때 사용할 수 있도록 할 겁니다. 람다의 기본 설정 단계인 타임아웃 시간과 메모리 크기를 설정하고 있습니다. 다행히 이 템플릿은 재사용할 수 있습니다. 처음에는 양이 많아 보일 수 있지만, 그 이후로는 매번 이 모든 것을 다시 쓸 필요가 없습니다. 파이썬 3.14로 변경해야 할 경우 , 저는 그냥 이렇게 하면 됩니다. 오른쪽? 콘솔에 들어가서 이것저것 클릭해 볼 필요가 없어요 . 그게 바로 클릭옵스(ClickOps)입니다. 권한 설정은 이 한 줄이면 충분합니다. 매우 중요합니다. 이게 바로 한 줄짜리 문장입니다. 매우 중요합니다. 항목 테이블은 API 핸들러에 읽기/쓰기 데이터 권한을 부여합니다 . 그게 무슨 뜻인가요? 그래서, 변수 이름이 items인 이 DynamoDB 테이블이 여기에 들어갈 겁니다. 어떤 서비스가 해당 데이터베이스에서 데이터를 조회할 수 있는지에 대해 매우 엄격한 기준이 적용될 것입니다 . 따라서 기본적으로 어떤 서비스도 해당 저장소에서 데이터를 조회할 수 없습니다. 보안은 시작일입니다. 그리고 이 경우에는 API 핸들러에게 권한을 부여하도록 허용하고 있습니다. 그렇다면 API 핸들러란 무엇일까요? 바로 이 변수 ​​때문입니다. 이 변수는 무엇입니까 ? 이 변수는 우리가 만들 람다 함수를 나타냅니다 . 그래서 우리는 DynamoDB 테이블에 우리 API 핸들러가 데이터에 대한 읽기 및 쓰기 접근 권한을 갖도록 허용한다는 것을 알리고 있습니다. 자, 이 읽기/쓰기 데이터 권한 부여는 CDK에 내장된 메서드로, 한 서비스가 다른 서비스에 접근할 수 있도록 권한을 추가할 수 있게 해줍니다. 그래서 출력 결과는 여기에서 그대로 유지됩니다. 파일이 저장되었는지 확인하시고 계속 진행하겠습니다 . 터미널을 정리하겠습니다. 그리고 제가 클라우드에 배포하려는 버전과 현재 버전의 차이점을 확인하기 위해 CDK diff를 입력하겠습니다 . 기본적으로 이 파일과 제 클라우드 환경에 실제로 있는 파일의 차이점입니다. 그러니까 제 클라우드 환경에는 실제로 람다 함수가 없다는 점에서 차이가 있어야 한다는 거죠 . 어디 보자. 좋아요 , 됐습니다. 여기서 우리에게 몇 가지 정보를 주고 있네요. 여기 있는 정보를 한번 살펴보죠. 그래서 신용 API 스택의 경우, 이를 배포하면 여기에 몇 가지 새로운 항목이 생성될 것입니다. 람다 함수가 DynamoDB에 접근할 수 있도록 역할을 업데이트합니다 . 또한, 자원 측면에서 보면 새로운 역할과 새로운 정책이 추가되는 것입니다. 그러니까 람다 함수의 경우, 새로운 역할과 정책을 추가하는 작업이 될 겁니다. 해당 람다 함수에는 정책도 포함되어야 하는데, 이는 DynamoDB와 통신하고 데이터를 생성하거나 데이터를 쿼리할 수 있는 권한을 의미합니다. 저희는 API 핸들러라는 람다 함수인 새로운 리소스를 추가할 예정입니다 . 그것이 우리가 정의한 함수입니다. 기본적으로 람다 함수를 생성하면 CloudWatch용 로그 그룹도 함께 생성되어 람다 함수의 상태를 모니터링할 수 있습니다. 좋아요, 알겠습니다. 자, 이제 배포를 진행해 보겠습니다. 좋아, 이번에는 권한을 요청할 거야. 승인 필요 기능이 활성화되어 있고 스택에는 보안에 민감한 업데이트가 포함되어 있습니다. 그렇다면 그 이유는 무엇일까요 ? 네, 보안상 민감한 문제이기 때문입니다. 제가 람다 함수라는 한 서비스가 다른 서비스의 데이터에 접근하고 통신할 수 있도록 허용하고 있거든요 . 그러니까 보안 정책이 변경된 겁니다. 그러니까, " 이러한 변경 사항을 배포하시겠습니까?"라고 묻는 겁니다. 저는 람다에 더 많은 접근 권한을 주고 싶기 때문에 '예'라고 답하겠습니다. 이 경우에는 우리가 생성한 DynamoDB 테이블에 대한 접근 권한만 필요합니다. 배포가 완료되면 콘솔에서 확인할 수 있습니다. 좋습니다, 지금 까지는 모든 것이 배포된 것 같네요. 다시 한번 확인 하고 검증해 봅시다. 자, 다시 콘솔로 돌아와서 Lambda를 검색해 보겠습니다. 자, 어떤 결과가 나왔는지 봅시다. 자, 됐습니다. 이것이 바로 저희 람다 함수입니다. 그리고 제가 그걸 클릭하면 방금 보여줬던 모든 코드가 보일 겁니다 . 자, 됐습니다 . 다음은 해당 코드 전체입니다. 그리고 이 Lambda 함수에 대한 환경 변수를 구성합니다. 환경 변수 구성에서 테이블 이름을 확인할 수 있습니다. 터미널로 돌아가면 확인할 수 있듯이, 테이블 이름으로 된 환경 변수를 생성했습니다. 그 이름은 DynamoDB 테이블의 테이블 이름으로 지정될 것입니다. 이 경우, 그것은 테이블의 이름입니다 . 시원한. 그래서 우리는 단 몇 줄의 코드로 그것을 해낼 수 있었습니다. 두 개의 리소스가 배포되었습니다. 데이터베이스와 함수는 서로 통신할 수 있도록 올바른 권한으로 연결되어 있습니다 . 이제 마지막 단계인 API 게이트웨이만 남았습니다. 이를 통해 전체 애플리케이션에 실제로 접근할 수 있게 됩니다. API 게이트웨이는 인터넷에서 HTTP 요청을 수신하여 Lambda 함수로 전달합니다. 배포가 완료되면 터미널 에서 해당 API에 접속하여 데이터베이스의 데이터를 수정할 수 있는 활성 URL이 생성됩니다. 저는 지금 crud api의 stack.py 파일에 있습니다. 그리고 우리에게는 자원이 하나 더 필요합니다 . 바로 이전 리소스에 Lambda 함수를 사용했던 것처럼 , 여기에도 또 다른 Lambda 함수가 필요합니다 . 제가 할 일은 기존 코드를 모두 업데이트된 코드로 교체하여 차이점을 보여드리는 것입니다. 괜찮은. 그래서 제가 방금 전에 있던 모든 코드를 이 새로운 코드로 바꿔 붙여넣었습니다. 이 코드는 블로그의 해당 단계에서도 확인하실 수 있습니다. 그리고 여기에 새롭게 추가된 것들은 AWS API Gateway와 같은 몇 가지 가져오기 기능입니다 . 그게 바로 그 임포트이고, 편의상 API GW라고 줄여서 부르는 겁니다. 여기서는 아무것도 변하지 않았습니다. 하지만 여기에서 새로운 자료를 보실 수 있습니다. 그래서 우리는 API라는 변수를 만들 거예요. 이름은 원하는 대로 지어도 되지만, API GW라는 가져오기 별칭을 사용합니다. 여기서 API라는 변수를 볼 수 있는데, 이는 API 게이트웨이 리소스의 .rest API 메서드 호출과 같습니다. 그러니까, 이 메서드 호출은, 여기 있는 메서드 호출과 비슷하게, Lambda에 function.function 메서드 호출이 있거나 DynamoDB에 .table 메서드 호출이 있는 것처럼, 리소스에 대한 구성 매개변수를 지정하는 곳입니다 . 이 경우에는 테이블의 경우, 테이블 이름이겠죠? 그런 것들 말이죠. 따라서 람다 함수에 대해서는 다음과 같은 정보를 가지고 있습니다. 이 경우, REST API에 대해 우리 API의 이름이 items API라고 알려주는 것입니다. 그리고 ' item service'는 REST API 이름이고, 여기에 설명을 추가할 겁니다. 그리고 API 게이트웨이는 모든 요청을 저희 람다 함수로 전달해야 합니다 . 예를 들어 http:// APIGateway URL/items로 curl 요청을 보내면, 해당 요청이 호출해야 할 Lambda 함수가 클라우드 환경에 있다는 것을 알아야 합니다 . 자, 여기서 우리는 그 사실을 알려줍니다 . 저희는 'items integration'이라는 변수를 만들고 있는데, 현재 이 변수는 API 게이트웨이( 바로 이 별칭입니다)를 통합하는 역할만 하고 있습니다. 즉, API 게이트웨이에서 Lambda와 API 핸들러를 통합하는 것입니다. 저것이 바로 저희가 연동하려는 람다 함수입니다. 그러니 모든 요청을 저희 람다 함수로 전달해 주세요. 그게 바로 그 기능입니다. 그리고 저희 람다 함수 의 이름은 API 핸들러입니다. 이제 우리 API에는 몇 가지 경로가 있습니다. URL에 사용되는 기본 이름입니다. 그러니까 URL이 http://blahblahblahblahblahblah.com/items 같은 형식이라면 , 맞죠? 이 {슬래시} 항목, 즉 경로의 첫 번째 부분이 바로 우리가 여기서 정의하는 것입니다. URL을 좀 더 체계적으로 정리하기 위해서입니다. 그리고 이 URL에서 GET, POST, PUT 또는 DELETE 요청을 할 수 있습니다. 자, 여기 있는 건 이게 전부입니다. 메서드를 추가하고 있습니다. 그래서 이 URL {slash} 항목에 대해 GET 및 POST 메서드를 추가하는 것 외에도, PUT 및 DELETE 요청에 리소스를 추가합니다. 왜냐하면 API에서 특정 항목을 업데이트하거나, 테이블에서 특정 항목을 가져오거나, 특정 항목을 삭제하도록 지정해야 하는 경우가 있기 때문입니다. 그러니까, 여기 있는 이 리소스 ID에는 여러분이 가져오거나, 업데이트하거나, 삭제하려는 리소스의 ID가 저장됩니다. 어, 이것들은 add 메서드라고 하는 내장 메서드입니다. 단일 품목은 어디에서 오는 건가요? 단일 항목은 {슬래시} ID가 붙게 될 API 게이트웨이 리소스의 이름일 뿐입니다. 그러니까 http:// blah blah blah blah 와 같은 형식이 될 겁니다. 이게 바로 저희 API 게이트웨이 URL입니다. 닷컴/항목. 이것이 바로 그 이유입니다 . 그건 하나의 자원입니다. 그리고 우리의 또 다른 자원은 {슬래시} ID입니다. 그래서 이 경우, 제가 데이터베이스에서 항목 {슬래시}와 같은 특정 항목 의 ID를 조회하면 , 해당 ID를 가진 기본 키를 데이터베이스에서 찾아 해당 항목을 업데이트하거나 삭제하거나, 또는 단순히 정보를 조회하는 작업을 수행합니다. 자, 그럼 여기로 가보죠. 음, 출력 결과를 보시면 배포 후 API 게이트웨이 엔드포인트 URL을 지정하는 새로운 줄이 추가된 것을 확인할 수 있습니다 . 그러니까 출력이란 CDK를 터미널에 배포한 후 CDK가 사용자에게 표시하는 정보들을 말합니다 . 그래서 배포 후 API 게이트웨이 URL을 바로 확인하고 테스트하여 제대로 작동하는지 검증할 수 있으므로 유용합니다. 그럼 바로 시작해 봅시다 . 자, 배포하기 전에 가장 좋은 방법은 먼저 화면을 지우고 CDK 차이점을 확인하는 것입니다. 그리고 여기 터미널 화면을 좀 더 크게 만들어 볼게요. 따라서 로컬 환경의 스택과 실제 클라우드 환경의 스택 간의 차이를 확인할 수 있습니다. 좋습니다, 여기 우리가 볼 수 있는 출력 결과가 있습니다. 그러니까, 이 기능은 Lambda 함수가 API Gateway에 의해 호출될 수 있도록 해주는 것입니다. 그래서 몇 가지 정책 변화가 일어나고 있습니다. 그리고 앞으로 구축될 자원들도 있습니다 . 그래서 저희 REST API의 단계별 구성, 즉 이 경우에는 프로덕션 단계에 몇 가지 새로운 권한이 배포될 예정입니다. 아마도 API 게이트웨이가 람다 함수를 트리거할 수 있도록 허용하는 것과 관련된 권한일 것입니다. 그래서 CDK diff에서 볼 수 있는 것들이 바로 이런 종류입니다. 전반적인 출력 결과를 보면 API 게이트웨이 URL도 표시하기 위해 더 많은 정보를 출력하고 있음을 알 수 있습니다. 그래서 이제 CDK 배포를 실행하면 이러한 리소스들이 배포될 것입니다. 네, 이 변경 사항을 배포하려면 권한을 요청하는 중입니다. 이번 업데이트는 보안에 민감한 내용과 관련이 있기 때문입니다. API 게이트웨이와 람다 함수가 서로 통신할 수 있도록 IAM 정책을 수정하는 작업입니다 . 그럼 Y를 누르겠습니다. API 게이트웨이가 생성되는 것 같네요. 모든 작업이 완료되면 테스트할 준비가 될 것입니다. 괜찮은. API가 이제 활성화되었습니다. 여기에서 엔드포인트를 확인할 수 있습니다. 음, 둘 중 하나를 클릭하시면 됩니다. 둘 다 똑같아요. 자 , 복사하세요. 마우스 오른쪽 버튼을 클릭하고 복사할게요. 그리고 우리는 실제로 여기에 curl 요청을 보낼 수 있습니다 . 그럼 한번 살펴보겠습니다. 실제로 작동하는지 테스트하기 위해, 먼저 환경 변수를 생성하여 해당 변수를 특정 URL과 같도록 설정하겠습니다. 그래서 저는 API라고 입력하고, 셸 환경에 이 URL과 동일한 API URL이라는 변수를 생성합니다. 이제 ` echo URL` 명령어를 실행하면 해당 URL이 표시될 것입니다. 완벽한. 그래서 이 변수가 우리가 배포한 URL과 같다는 것을 알게 됩니다 . 이제 curl 명령어를 사용하여 테스트해 볼 수 있습니다 . 자, 우선 저희 데이터베이스는 방금 생성했기 때문에 비어 있습니다 . 자, 그럼 설명란에 링크된 블로그에도 나와 있는 이 curl 명령어를 사용해서 POST 요청을 보내 보겠습니다 . 그래서 저는 curl 명령어를 사용해서 해당 API URL({slash} items)로 POST 요청을 보내고 있습니다. 왜냐하면, ' items'는 우리가 엔드포인트 URL 리소스 중 하나라고 부르는 것이기 때문입니다. 그래서, {슬래시}개 항목입니다. 바로 그곳에서 모든 항목을 가져오거나 데이터베이스에 항목을 추가하는 GET 및 POST 요청을 수행할 수 있습니다. 시원한. 그러니까, 제가 이 코드를 실행하면, JSON 데이터를 전달하게 되고, 이 데이터는 DynamoDB 테이블에 삽입됩니다. 이 경우, 해당 제품의 이름은 '귀리 우유'입니다. 설명은 고급 바리스타 버전입니다. 시원한. 그래서 삽입이 성공적으로 된 것 같고 , 이를 확인할 수 있습니다. 어, 잠깐 다른 항목을 추가할게요 . 농구 유니폼으로 하나 해볼까요? 좋아요. 그래서 두 번의 curl POST 요청을 보내 데이터베이스에 두 개의 항목을 삽입했습니다. 이제 GET 요청을 보내서 모든 항목을 제대로 가져왔는지 확인해 보겠습니다. API URL /items에 대한 curl 명령어를 복사해서 붙여넣겠습니다. 이 URL은 여러분도 아시다시피 저희가 가지고 있는 URL입니다. 네, 방금 DynamoDB 테이블에 두 개의 항목을 삽입했습니다. 그래서 API 게이트웨이에서 저희 람다 핸들러로 요청을 보내 어떤 CRUD 작업을 사용할지 결정할 수 있었고, 이 경우에는 GET 요청이었으며, DynamoDB 테이블의 모든 항목, 즉 모든 정보를 가져올 수 있었습니다. 또한 ID로 특정 항목 하나만 가져오는 일반적인 명령도 실행할 수 있습니다. 이 경우, DynamoDB에서 자동으로 생성된 이러한 ID를 볼 수 있습니다. 자, 그럼 이 ID 중 하나를 예로 들어 보겠습니다. 제가 할 수 있는 것은 ` curl -s API URL /items /DynamoDB 테이블에 있는 항목 중 하나의 고유 항목 이름`이라는 명령어를 입력하는 것입니다 . 바로 이 URL에 있습니다. 그래서 제가 curl 요청을 보내면, 이 ID를 가진 특정 항목에 대한 데이터를 반환받게 됩니다 . 이 내용은 저희 API 게이트웨이에서 나오는 겁니다, 그렇죠? 그러니까 /items가 유일한 경로는 아니었던 거죠. 또한 /items / ID라는 동적 변수에 대한 URL 경로가 있었습니다 . 이 경우에는 데이터베이스 테이블 항목 중 하나의 고유 ID 이름을 전달하여 해당 특정 항목에 대한 모든 정보를 얻을 수 있었습니다. 이 경우에는 농구 유니폼이 해당 품목이었습니다. 이제 모든 것이 제대로 작동하는지 확인할 수 있으며, 완전한 CRUD API를 갖추게 되었습니다. 자, 여기 있습니다. 그럼 정리해 보겠습니다 . 자, 이렇게 해서 콘솔 클릭 한 번 없이 코드 로 배포하여 AWS에서 실행되는 완벽한 CRUD API를 구축했습니다 . 이 방법이 처음 효과를 봤을 때, 저는 단말기를 30초 동안 빤히 쳐다봤습니다. 정말 믿기지 않아서 혹시나 우연이 아닐까 싶어 다시 한번 배포해 봤습니다. 그게 바로 API가 제대로 작동하는 모습입니다. 정리하기 전에 커튼 뒤를 살짝 들여다보고 싶어요. CDK는 내부적으로 CloudFormation을 생성한다는 점을 기억하세요. CloudFormation이 우리를 위해 얼마나 많은 파일을 작성했습니까 ? 자, 여기 있는 이 명령어를 살펴보면 확인할 수 있습니다. CDK synth 명령어를 사용하면 CDK 파일로부터 생성된 CloudFormation 템플릿을 확인할 수 있습니다. 자, 이게 바로 클라우드폼의 모습입니다. 바로 여기 보이시죠. 터미널 화면을 좀 더 크게 만들어 볼게요. 그럼 위에서부터 시작해 볼까요? 이건 또 뭐죠? 그러니까 YAML 설정 파일인데, 기본적으로 키-값 쌍과 같습니다. 그리고 그 문서에는 우리가 지금까지 설정한 모든 리소스 와 모든 구성이 한 줄 한 줄 자세히 설명되어 있습니다 . 그래서 items 테이블은 DynamoDB 테이블이고, 몇 가지 속성을 가지고 있습니다 . 이것은 다음과 같은 속성과 구성을 가지고 있습니다. 음, API 핸들러, 그게 바로 저희 람다 함수입니다. 음 , 사실 이게 바로 저희 람다의 역할입니다. 그러니까, 이건 람다 함수가 특정 주체에 의해 실행되고 호출될 수 있도록 권한을 부여하는 IAM 정책 역할입니다. 그리고 이 경우에는 DynamoDB 정책 문서도 있습니다. 그래서, 양이 꽤 많아요. 여기 정보가 정말 많네요, 그렇죠? 그리고 클라우드에 배포한 모든 리소스에 대한 모든 정보를 일일이 수동으로 입력하고 구성할 필요가 없습니다. CDK를 사용하면 상대적으로 아주 적은 양의 타이핑만으로도 그 작업을 수행할 수 있습니다. 그러면 나머지 세부 사항도 알아서 채워줄 겁니다. 그래서 이 파이썬 CDK가 바로 여기에 보이는 YAML 템플릿, 즉 CloudFormation 템플릿을 생성했습니다. 그리고 이것이 바로 AWS가 궁극적으로 리소스를 직접 생성하는 데 사용할 것입니다 . 그래서 CDK는 IAM 역할, API 게이트웨이용 람다 권한, 배포 단계 구성 및 기타 여러 세부 사항을 처리해 주었는데, 그렇지 않았다면 여러분은 문서 에서 정보를 짜맞추고 마지막으로 문서를 확인한 이후 변경 사항이 없기를 바라면서 직접 처리해야 했을 것입니다 . 자 , 마무리하기 전에 마지막으로 한 가지만 더 말씀드리겠습니다. 바로 비용 관리입니다. 추가 비용이 발생하지 않도록 지금까지 만들어 놓은 모든 자료를 철거해야 합니다. 이건 데모 목적으로만 제작된 것이니, 데모가 끝나면 이런 기능들은 더 이상 실행할 필요가 없습니다. 그리고 당신도 블로그를 따라 배우면서 튜토리얼을 진행하는 동안 마찬가지로 하시길 바랍니다. 자, 이제 이 작업을 수행할 명령어를 알려드리겠습니다 . 콘솔을 열고 람다, 다이나모DB, 삭제 순으로 이동한 후 확인하는 대신, CDK destroy라고 입력하고 엔터 키만 누르면 됩니다. 그러면 "정말로 삭제하시겠습니까?"라고 물어볼 거예요. 예. 이제 이 스택을 사용하여 프로비저닝한 모든 리소스가 완전히 사라질 것이라고 확신할 수 있습니다. 그리고 우리는 그로 인해 어떠한 비용 이나 책임도 지지 않을 것입니다. 네 , 됐습니다. 클라우드 환경에서 모든 것이 삭제되었습니다. CDK는 API 게이트웨이, 람다 함수, DynamoDB 테이블, 모든 IAM 역할 및 정책을 올바른 순서로 삭제하고 모든 종속성을 처리합니다. 어떤 것을 먼저 삭제해야 할지 고민할 필요도 없고, 예상치 못한 청구서가 날아올 일도 없으며, 2023년부터 당신을 괴롭히는 4.73달러 상당의 DynamoDB 테이블도 없습니다. 하지만 한 가지 알아둘 점은 CDK destroy 명령은 모든 것을 삭제한다는 것입니다. 따라서, 만약 잘못된 변경을 했다고 생각되어 전체 스택을 해체하지 않고 되돌리고 싶다면, 버전 관리를 하고 있는지 확인하세요. Git을 사용하여 코드를 버전 관리하면 마지막 커밋으로 되돌린 다음, 문제가 발생했던 부분을 수정하고 CDK 배포를 다시 실행할 수 있습니다 . CDK는 사용자의 코드 와 배포된 코드를 비교하여 변경 사항을 되돌립니다. 삭제는 작업이 끝났을 때 하는 겁니다. 되돌리기 후 배포는 실수를 했을 때 사용하는 기능입니다. CDK 삭제 명령이 완료되면 항상 클라우드폼 콘솔에서 스택이 완전히 사라졌는지 확인합니다. 5초만 투자하면 다음 달 청구서에 예상치 못한 금액이 청구되는 것을 막을 수 있습니다. 사실 AWS CLI를 통해 바로 그렇게 할 수 있습니다. 자, 그럼 어떻게 하는지 보여드리겠습니다. 그럼, 우리 스택의 이름은 이거 맞죠? CRUD API 스택. 그래서 저는 AWS CloudFormation describe stacks, stack name CRUD API stack이라는 명령어를 입력하겠습니다. 그러니까, 설정을 해야 하고 지역을 지정해야 한다는 뜻이네요. 좋아요. 그렇게 하죠 . AWS 설정을 할 수 있습니다. 여기 있는 모든 걸 그대로 받아들이세요. 기본 재지역 이름입니다. 자, 이 경우에는 미국 동부 1위입니다. 알겠습니다. 자, 이제 AWS에서 필요한 지역 설정을 완료했으니 이 명령어를 다시 실행할 수 있습니다. 네, 해당 스택은 존재하지 않습니다. 그러므로 이 경우에는 오류가 오히려 좋은 것입니다. 오류. 해당 스택은 우리가 제거했기 때문에 더 이상 존재하지 않습니다 . 그래서 우리는 그것이 존재하기를 원하지 않습니다 . 네 , 그래서 좋은 오류라고 할 수 있죠. 괜찮은. 자, 마무리하기 전에 CDK를 사용하여 인프라스트럭처를 코드로 구현할 때 초보자들이 흔히 겪는 몇 가지 일반적인 오류에 대해 이야기해 보겠습니다. 디버깅 시간을 절약해 드리고 싶습니다. CDK 초보자들이 흔히 저지르는 네 가지 실수가 있습니다. 제가 네 가지 모두 경험해 봤기 때문에 알아요. 난 10개 다 맞췄어. 비록 우리가 지금 4개에 대해 얘기하고 있지만 말이야. 제 첫 번째 프로젝트에서, 그중 일부는 여러 번 접하게 되었어요. 그러니 제가 이런 오류를 몇 번이나 겪었는지에 대해서는 굳이 이야기할 필요가 없겠죠 . 여러분이 오류라고 생각할 만한 것들에 대해 이야기해 봅시다. 예를 들어 "이 스택은 에셋을 사용하므로 툴킷 스택을 배포해야 합니다."라는 오류 메시지가 나타날 수 있습니다. 그런 메시지가 보인다면, 부트 스트랩을 실행하는 것을 잊었다는 뜻입니다. CDK 부트스트랩을 한 번 실행하면 문제가 해결됩니다. 이는 CDK에서 "껐다가 다시 켜보셨나요?"와 같은 의미입니다. 다음 오류는 사용자에게 권한이 없다는 것입니다. ' 사용자에게 권한이 없습니다'라는 메시지가 표시되면 AWS CLI 사용자에게 CDK에 필요한 권한이 없다는 의미입니다. 개인 학습 계정의 경우 관리자 권한을 사용할 수 있습니다. 실제 운영 환경에서는 절대로 이렇게 하지 마세요. 당신을 보고 있어요. 당신이 누군지 알잖아요. 또 다른 오류는 index 모듈을 가져올 수 없다는 것입니다 . 따라서 이 경우 람다 코드가 CDK가 예상하는 위치에 있지 않습니다. 람다 폴더에 index.py라는 이름의 파일이 있는지 확인하세요. handler.py, mycode demo.py 또는 tutorial.py의 index 파일을 복사한 것이 아니어야 합니다. 말 그대로, index.py입니다. 폴더 구조에서 보셨듯이 말이죠. 마지막으로 정말 흔한 오류는 액세스 거부 예외입니다. 예를 들어, DynamoDB에서 항목 접근 거부 예외가 발생합니다. DynamoDB에 쓰고 읽을 수 있도록 람다 함수, 즉 람다 핸들러에 읽기/쓰기 권한을 부여하는 코드 줄을 기억하시나요 ? 만약 그 단계를 잊어버렸다면 , 이런 일이 발생합니다. 그러니 그 단계를 꼭 포함시키세요. 이게 바로 프로젝트의 전부입니다. 빈 터미널에서 시작하여 CDK 프로젝트를 생성하고 , Dynamo DB 테이블을 배포한 후 정상적으로 작동하는지 확인했습니다. Lambda 함수를 작성하고 올바른 권한으로 배포했습니다 . API 게이트웨이를 추가하고 배포한 다음 , 터미널에서 전체 CRUD API를 테스트하고 단 하나의 명령으로 모든 것을 제거했습니다 . 그리고 누군가 우리에게 해당 환경을 다시 프로비저닝해달라고 요청하면, 우리는 CDK 배포만 하면 됩니다. 명령 하나. 아주 쉬워요. 몇 가지 유의할 점이 있습니다. 인공지능 시대에는 AI 코딩 도우미를 활용할 수 있습니다. 제 경우에는 Kiro를 사용하여 필요한 함수와 CDK 코드, 즉 인프라스트럭처를 코드로 작성할 수 있도록 지원받고 있습니다. 에이전트에게 원하는 바를 일반적인 영어로 설명하기만 하면 필요한 코드를 실제로 생성해 줍니다. 따라서 이러한 기능을 활용하면 특정 작업을 수행하는 데 필요한 변수 이름, 임포트 이름 또는 메서드 이름을 일일이 추측할 필요가 없습니다 . 인공지능에게 요청하기만 하면, 인공지능이 필요한 인프라를 코드로 생성해 주고 , 사용자는 그 코드를 검토한 후 배포할 수 있습니다. 또 하나 주목할 점은 제가 각 구성 요소를 직접 배포하고 검증한 후에 다음 구성 요소를 추가했다는 것입니다. 보시다시피, 저는 모든 인프라를 한 번에 구축하지 않았습니다. 나는 내가 올바른 방향으로 가고 있는지 확인하기 위해 단계별로 진행했다. 그건 초보자가 하기에 좋아요. 자 , 이제 여러분이 더 깊이 탐구하고 기능을 추가하고 싶을 때 이 위에 구축할 수 있는 세 가지에 대해 이야기해 보겠습니다. 따라서 Amazon Cognito를 사용하여 인증을 추가하면 로그인한 사용자만 항목을 생성, 업데이트 또는 삭제할 수 있습니다. 또는 모든 것을 다시 작성할 필요 없이 동일한 코드베이스에서 개발, 스테이징 및 프로덕션용으로 별도의 스택을 배포할 수도 있습니다 . Lambda 오류가 급증할 때 알림을 받도록 CloudWatch 알람을 추가할 수도 있습니다. 설명란에 링크된 블로그 게시물에는 전체 코드, 모든 명령어, 아키텍처 다이어그램이 포함되어 있습니다. 유용했다면 구독해 주세요. 저는 여러분이 클라우드 및 소프트웨어 개발의 기초를 빠르게 익힐 수 있도록 이와 같은 유형의 영상을 더 많이 제작하고 있습니다. 즐거운 코딩, 즐거운 제작 되시고, 시청해 주셔서 감사합니다.
