# Optimizing query performance with Pinecone's Rerank API and Fleak

- 영상 링크: https://www.youtube.com/watch?v=-sKMoUec8E8
- 채널: Pinecone
- 업로드일: 2024-08-23
- 자막 언어: ko
- 단어 수: 약 3776개

---

## 스크립트

안녕하세요 여러분, Pine Con Fleek에 참여해 주셔서 감사합니다. 오늘은 쿼리 품질 최적화에 대해 이야기해 보겠습니다. 오늘 저와 함께 Fleek의 파트너들이 참여하고 있습니다. 보, 슬라이드 넘기시겠어요? 아, 죄송합니다. 슬라이드 넘기세요. 네, 저는 코리 와팅 햄이고, Pine Cone의 스태프 파트너 엔지니어입니다. 하지만 오늘 발표는 베이, 엔, 젠이 맡아주실 예정이니, 먼저 자기소개를 부탁드리겠습니다. 보, 안녕하세요 여러분. 저는 베이이고 Fleek의 공동 창립자이자 CEO입니다. 엔, 안녕하세요 여러분. 저는 엔이고 Fleek의 CEO이자 공동 창립자입니다. 자, 보, 시작해 주세요. 네, 감사합니다 코리. 먼저 벡터 데이터베이스가 무엇인지 간단하게 소개하겠습니다. 벡터 데이터베이스는 벡터들의 저장소라는 것을 모두 알고 계시죠? 문서를 입력하면, 그 문서를 변환합니다. 매력적인 모델을 사용하여 벡터로 변환하고 이러한 벡터를 p와 같은 벡터 데이터베이스에 저장합니다. 이제 쿼리가 있다면 동일한 프로세스를 따릅니다. 쿼리를 벡터로 변환한 다음 이 쿼리를 실행합니다. 그러면 Python은 벡터 유사도 알고리즘을 실행하여 쿼리 벡터와 가장 유사한 벡터를 검색 결과로 반환합니다. 하지만 한 가지 주의할 점은 벡터 데이터베이스 검색이 항상 좋은 결과를 반환하는 것은 아니라는 것입니다. 이 과정에서 문서를 벡터로 변환하고 벡터 유사도를 계산하는데, 단순히 이러한 벡터를 비교하는 것만으로도 벡터 손실이 발생합니다. 즉, 문서를 벡터로 변환할 때 정보 손실이 발생합니다. 다시 말해, 벡터 유사도가 항상 문서의 의미적 관련성을 나타내는 것은 아니며, 실제와 다른 결과를 제공할 수 있습니다. 예를 들어, Apple 회사와 과일로서의 사과에 대한 여러 문서가 있다고 가정해 보겠습니다. 이 모든 문서를 벡터 데이터베이스에 저장한 다음, "정보를 찾고 싶다"라는 쿼리를 실행합니다. 애플이라는 회사에 대한 쿼리를 실행하면 벡터 데이터베이스는 실제로는 사과 과일의 영양 정보와 같은 결과를 많이 반환할 수 있습니다. 이것이 바로 최적화되지 않은 결과입니다. 반면에 두 문서를 벡터로 변환하지 않고 직접 유사성을 비교할 수 있는 알고리즘들이 있습니다. 이러한 알고리즘들을 실행 알고리즘이라고 합니다. 보시다시피, 이 알고리즘들은 문서를 직접 비교하며, 문서를 벡터로 변환하는 단계가 없습니다. 따라서 정보 손실이 없기 때문에 더 정확한 결과를 제공하는 경우가 많습니다. 하지만 이러한 알고리즘은 계산량이 많고 속도가 훨씬 느리다는 단점이 있습니다. 이러한 문제를 개선하기 위해 일반적으로 사용하는 기술은 2단계 검색입니다. 단계 검색에서는 사용자가 벡터 데이터베이스에 쿼리를 실행하고, 벡터 데이터베이스는 문서 목록을 반환합니다. 그런 다음 반환된 값에 대해 실행 알고리즘을 실행합니다. 가장 관련성이 높은 문서들이 검색 목록의 맨 위에 오도록 하기 위해 이 기법은 RAG 애플리케이션을 구축하고 개선하는 데 매우 유용하고 실용적입니다. RAG 애플리케이션에서는 쿼리를 실행하여 컨텍스트로 사용할 문서들을 가져온 다음, 원래 질문과 함께 컨텍스트를 L에 전달합니다. L은 컨텍스트를 요약하여 질문에 대한 답을 제공합니다. 모든 검색 결과를 L에 그대로 전달하면 유용한 정보가 중간에 손실될 가능성이 있습니다. L은 방대한 컨텍스트에서 적절한 정보를 선별하는 데 능숙하지 않기 때문입니다. 반면에 컨텍스트 윈도우 크기를 줄이면 정말 유용한 문서가 컨텍스트 외부에 있을 수 있고, 그러면 L은 질문에 정확하게 답할 수 없게 됩니다. 이 경우 순위 지정을 적용하면 모든 관련 문서가 목록의 맨 위에 오게 되므로 컨텍스트 크기를 줄이는 동시에 유용한 컨텍스트를 L에 전달할 수 있습니다. 그래서 질문에 훨씬 더 잘 답변할 수 있을 것 같습니다. 자, 이제 공동 창업자인 이찬에게 마이크를 넘기겠습니다. 이찬이 Fleek이 무엇인지, 그리고 Fleek이 2단계 검색 시스템 구축에 어떻게 도움이 되는지 간단히 소개하겠습니다. 감사합니다. 실습 부분으로 넘어가기 전에, 누군가 Pineo 관련 지식이 모두 있어야 하는지 질문하는 것을 봤습니다. 그래서 잠깐 화면을 전환하겠습니다. 혹시 Pineo 인덱스를 만들어 본 경험이 있는 분이 계시면 화면 공유를 잠시 멈춰주시겠습니까? 네, 죄송합니다. 오늘 여기 계신 분들 중 Pineo 인덱스를 만들어 본 경험이 있는 분이 계신가요? 그래야 실습 과정에서 무엇을 해야 할지 알 수 있으니까요. 경험이 있으신 분은 손을 들어주시겠습니까? 아니면 반대로, Pineo 인덱스를 만들어 본 적이 없는 분이 계시면 손을 들어주세요. 먼저 인덱스를 만들어 본 경험이 있으신 분은 손을 들어주세요. 좋습니다. 10명 정도 되는 것 같네요. 좋습니다. 그럼, 음... Fleek은 서버리스 API 빌더입니다. 오늘은 Pine Cone의 기능을 소개하기 위해 이 자리에 섰습니다. Fleek을 사용하여 Pine Cone에 인덱스를 매우 빠르게 작성하는 방법과 방금 설명한 것처럼 간단한 인터페이스를 통해 재순위 API 및 시맨틱 검색 API를 구축하는 방법을 보여드리겠습니다. Pine Cone의 데이터 수집, 재 순위, 임베딩 API와 함께 사용하고 싶은 모든 기능을 이용할 수 있습니다. Fleek의 특징은 데이터를 변환하든, 시맨틱 검색을 호출하든, 임베딩을 가져오든 바로 사용할 수 있다는 점입니다. 죄송합니다. 화면 공유를 하려고 하셨죠? 슬라이드에서 벗어난 것 같네요. 화면을 공유하겠습니다. [음악] 네, 오른쪽이 오늘 워크샵 튜토리얼에서 사용할 인터페이스입니다. Fleek API는 다음과 같습니다. 서버리스 방식이라 인프라를 구축할 필요가 없고, 저희 엔드포인트는 바로 운영 환경에 배포할 수 있습니다. 사용자가 아무것도 하지 않아도 자동으로 확장 및 축소가 가능합니다. Pine이나 Snowflake 같은 타사 데이터베이스에 데이터를 입력하든, 프런트엔드 앱을 위한 백엔드 API 엔드포인트를 생성하든, Fleck을 사용하여 필요한 모든 것을 구축할 수 있습니다. 자, 그럼 바로 시작해 볼까요? 제 Fleck 인터페이스가 잘 보이는지 확인해 보겠습니다. 화면이 보이시죠? 네, 좋습니다. 여기가 Fleck 인터페이스입니다. 먼저 방금 설명드린 튜토리얼을 바탕으로 PineCO에 데이터를 수집하는 방법을 배워보겠습니다. 여기 계신 분들 중 아직 첫 PineCO 인덱스를 생성하지 못하신 분들이 몇 분 계신 것 같은데요. 아주 간단합니다. Gmail 계정으로 로그인해서 첫 PineCO 인덱스를 빠르게 생성할 수 있습니다. 데모를 보여드리겠습니다. PineCO 인터페이스가 보이시나요? 네, 처음 오신 분들은 로그인하시면 됩니다. 여기 위쪽에, 음, 저는 신규 사용자가 아니어서 바로 제 Pyon 인덱스에 가입했습니다. 인덱스를 만드는 방법은 간단합니다. ' 생성'을 클릭하고 인덱스 이름을 지정하면 됩니다. 한 가지 강조하고 싶은 점은, 많은 분들이 약간 어려움을 겪는 부분인데, 인덱스 차원이 선택한 임베딩 모델과 일치하는지 확인해야 한다는 것입니다. 오늘 제가 사용할 PineCO 임베딩 모델은 1024차원입니다. PineCO 인덱스를 만들 때 주의해야 할 유일한 부분인 것 같습니다. 자, Corey에게 질문 있으신가요? 네, 없는 것 같네요. 좋습니다. 오늘은 이 PineCO 인덱스를 사용할 건데요, 보시다시피 테스트 환경을 만들 겁니다. 보시다시피 아직 벡터가 없죠. 이제 이 인덱스에 벡터를 추가해 보겠습니다. 다시 Fleck 인터페이스로 돌아가서, 먼저 새 워크플로를 만들겠습니다. 이름은 '데이터 수집 및 업데이트'로 하겠습니다. 먼저, 로직 테스트를 위해 몇 가지 데이터 샘플을 사용해 보겠습니다. 이것은 HTTP 데이터 입력인데, API를 구축하는 과정이며, 나중에 이 API를 호출하여 데이터를 가져오는 데 사용할 수 있는 샘플 페이로드입니다. 여기서는 전자 카탈로그 항목을 모든 필드와 함께 복사하여 붙여넣겠습니다. SQL이나 PostgreSQL처럼 표 형식 데이터에 익숙하신 분들은 이 보기 방식이 페이로드를 더 쉽게 볼 수 있도록 도와줄 것입니다. 잠시 이 부분을 닫겠습니다. 첫 번째 단계에서는 이 데이터를 간단히 정리해야 합니다. 의미 있는 검색을 위해서는 제품 설명이 필요하기 때문입니다. 제목과 기능 필드를 연결하여 이 배터리에 대한 자세한 설명을 만들어 보겠습니다. [ 단계]를 클릭하면 초기 페이로드 위에 새 설명 필드가 생성된 것을 볼 수 있습니다. 표 형식 보기 또는 JSON 보기 중에서 원하는 형식으로 확인할 수 있습니다. 다음 단계에서는 그냥 넘어가겠습니다. 여기서 임베딩을 생성하기 위해 임베딩 제공자로 Pine을 선택 하고 제 인덱스 U를 선택하겠습니다. 하지만 Pine API 키를 복사해서 임베딩 모델에 붙여넣어 새 인덱스를 생성할 수도 있습니다. Pine은 JSON 형식의 임베딩 모델을 제공합니다. 여기 JSON에서 pass는 선택자입니다. 이 경우 설명 필드에 대한 벡터를 생성할 것입니다. 오른쪽에서 볼 수 있듯이 설명은 JSON 필드의 일부이므로 JSON 형식을 사용하기만 하면 자동 완성되어 설명이라고 알려줍니다. 나머지 필드는 모두 유지하려면 입력 이벤트에 벡터를 연결하기만 하면 됩니다. 이제 임베딩을 생성해 보겠습니다. SQL 노드의 입력이 임베딩 노드로 전달되는 것을 볼 수 있습니다. 벡터를 축소하면 모든 초기 필드가 입력에 연결된 것을 확인할 수 있습니다. 이제 임베딩 벡터가 생성되었습니다. 마지막 단계는 매우 간단합니다. 우리가 원하는 건 이 새로운 벡터를 Pineo 인덱스에 추가하는 것뿐이죠. 다시 Fleek 베타 버전을 선택하고, 인덱스는 ' Electronics catalog'를 사용할 겁니다. 벡터 필드는 방금 임베딩 노드를 사용해서 생성한 이 벡터입니다. JSON 패스 검색기를 사용하면 되고, 임베딩은 임베딩 객체 안에 있다는 걸 알고 있죠. 확인해 볼게요. 네, 임베딩 만 포함하는 올바른 필드입니다. 메타 필드는 벡터 생성에 사용되지 않는 다른 필드를 저장할 수 있는 곳입니다. 여기에는 'title'이 있고, JSON 패스는 'do title'입니다. 원하는 만큼 키 필드를 생성할 수 있습니다. 예를 들어 ' price', ' PR' 등이 있죠. ' price'의 JSON 경로는 대문자로 써야 할 것 같은데, 그냥 셀렉터를 사용하는 게 더 안전하겠네요. 셀렉터가 저보다 더 잘 알고 있으니까요. 셀렉터는 정말 편리하죠. 네, 이 항목에는 할인이 적용되지 않습니다. 또 뭐가 빠졌죠? 하위 카테고리로 가볼게요. 'description'도 있고요. 네, 잘 된 것 같네요. 음, 네, 이 데이터를 파인콘에 저장하고 라운드 탭 버튼을 클릭해 보겠습니다. 샘플 데이터에는 오늘 항목이 하나만 포함되어 있기 때문입니다. "Su가 파인콘에 성공적으로 기록되었습니다"라고 표시되어 있지만, 나중에 이 API를 사용할 때는, 즉 이 데이터를 게시하는 방법을 보여드린 후에는 벡터를 수집할 때 여러 항목을 포함할 수 있습니다. 이제 파인콘 인덱스를 확인해 보겠습니다. 여기... 그런데 죄송합니다. 기록할 때 네임스페이스를 지정해야 하는 것 같습니다. 파인콘 동기화 노드에서 고급 설정으로 이동하여 네임스페이스를 지정해야 합니다. 그렇지 않으면 기본 네임 스페이스가 사용됩니다. 좋은 지적입니다. 오늘은 테스트를 위해 테스트 네임스페이스를 사용할 것입니다. 다시 실행해 보겠습니다. 네, 네임스페이스로 이동해 보겠습니다. 이제 테스트를 사용할 수 있습니다. 여기 있습니다. 이제 배터리 항목의 벡터가 파인콘에 기록되었고 여기에서 확인할 수 있습니다. 네, 이제 워크플로로 돌아가겠습니다. 워크 플로 테스트가 완료되고 만족스러우면 다시 시작하겠습니다. 수집 결과를 API 엔드포인트에 게시할 수 있습니다. 게시 버튼을 클릭하면 스테이징 엔드 포인트와 프로덕션 엔드포인트 중에서 선택할 수 있습니다. 오늘은 프로덕션 엔드포인트를 선택 하고 게시 버튼을 클릭하겠습니다. API 엔드포인트가 생성되는 데 1분도 채 걸리지 않습니다. 이 엔드포인트를 복사하여 로컬 HTTP API 클라이언트에 붙여넣고 일반 API 호출처럼 사용하면 됩니다. 시맨틱 검색 워크플로를 위해 터미널에서 이 과정을 보여드리겠지만, 이 API를 사용하여 PinecCon에 데이터를 수집하는 방법은 간단합니다. API 키만 Fleek API 키로 교체하면 됩니다. 문서에서 API 키를 생성할 수 있습니다. 지금까지 질문이나 의견이 있으시면 말씀해 주세요. 이제 재 순위화 및 검색을 위한 API 생성으로 넘어가겠습니다. 메타데이터를 ThreeR 알고리즘과 함께 또는 없이 사용하는 방법에 대한 질문이 있는데, 바로 다음 부분에서 자세히 다루겠습니다. 음, 그 질문은 잠시 생각해 보고 여러분의 답변이 나오는지 확인해 보겠습니다. 데모의 나머지 부분은 괜찮은 것 같네요. 이제 주입 파이프라인을 게시하고 워크플로로 돌아가면 주입 파이프라인이 게시된 것을 볼 수 있습니다. 이제 새로운 워크플로, 즉 검색, 검색 및 재 순위 지정을 수행할 수 있는 새로운 API를 생성할 것입니다. 이 워크플로의 이름을 '이름 검색(name retrieval)'으로 지정하고 여기에서 이름을 다시 업데이트하겠습니다. 이 검색 프로세스를 생성하려면 샘플 데이터가 필요합니다. 오늘 사용하는 데이터 세트는 전자 제품 카탈로그이므로 카메라나 TV에 대한 몇 가지 질문을 드리겠습니다. 다른 질문이 있으면 QA 섹션에 댓글을 남겨주세요. 잘 작동하는지 확인해 보겠습니다. 청중의 제안에 따라 질문을 변경할 수도 있습니다. 아마 여러분 중 일부는 RAKG를 많이 사용해 보셨겠지만, RAKG를 생성할 때와 비슷한 단계를 반복할 것입니다. 검색의 첫 번째 단계는 질문을 여기에 다시 삽입하는 것입니다. Pine API 추론 API를 사용하여 조정한 데이터를 알고 있으므로 동일한 임베딩 모델인 JSON 패스를 사용할 것입니다. 매우 간단합니다. 필드가 하나뿐이므로 질문만 입력하면 됩니다. 그런 다음 ' 임베딩 생성' 단계를 클릭하면 임베딩이 생성됩니다. 이제 만족합니다. 다음 단계는 Pine 지식 검색을 사용하는 것입니다. Pine에서 이미 생성한 연결을 선택하고 오늘의 인덱스인 전자 제품 카탈로그를 선택합니다. 벡터 패스는 질문입니다. 아, 질문이 아니라 벡터입니다. 다시 JSON 선택기를 사용할 것입니다. 저보다 똑똑하니까요. 상위 결과 개수를 10개로 설정해 보겠습니다. 질문과 관련된 결과를 가져올 수 있는지 확인해 보겠습니다. 매우 방대한 JSON입니다. 다행히 언제든지 표 형식으로 전환할 수 있습니다. 아직 정리를 하지는 않았지만, 일부 결과를 가져온 것을 볼 수 있습니다. 관련 카메라 정보입니다. 여기서는 저희 자체 개발 혁신 기술인 매우 간단한 SQL 노드를 소개하겠습니다. 이 노드는 페이로드에서 직접 실행됩니다. 여기서는 JSON 선택자 SQL을 소개할 텐데, SQL 구문에 익숙하지 않으신 분들을 위해 설명드리겠습니다. SQL 구문에서 사용되는 다른 JSON 선택자와 유사합니다. 필드 이름을 참조할 수 있고, 중첩된 경우 여러 레이어를 사용하여 방금 생성한 JSON 데이터를 참조할 수 있습니다. 이제 이 JSON 요소 패서를 실행하고 데이터를 정리할 수 있습니다. 가격 필드 와 제목을 기준으로 상위 10개 검색 결과를 확인할 수 있습니다. 이것이 기본적인 첫 번째 단계 검색입니다. 첫 번째 단계는 완료되었습니다. 괜찮은 결과가 나왔네요. 일부는 관련성이 있고 일부는 실내외 공간으로 이동하라는 내용이었습니다. 다른 질문으로 변경할 수도 있습니다. 첫 번째 단계 검색에 대한 다른 질문이 있습니까? 앞서 언급했듯이 첫 번째 단계는... 검색은 말 그대로 질문을 삽입하는 것과 같습니다. 키워드 매칭을 통해 결과를 도출하는데, 질문에 카메라를 언급했기 때문에 대부분의 경우 검색 결과는 카메라와 관련성이 높아 문제없이 작동합니다. 다른 유형의 질문도 가능합니다. 예를 들어, 드론 목록 에 카메라가 포함된 여러 대의 드론이 있다고 가정해 보겠습니다. 만약 " 좋은 비행 카메라는 무엇인가요?"라고 묻는다면, "이런 비행 카메라가 좋은가요?"라는 답변이 나옵니다. 비행 카메라가 아니라, 공중을 날아다니는 카메라를 말하는 거죠. 음, 어느 정도 이해가 되는 것 같네요. 실내외 보안 카메라, 비행 카메라 등 몇 가지 결과가 더 나올 수도 있겠네요. 이제 고급 3차 랭크 API를 활용해 보겠습니다. 카메라를 다루고 있으니, 드론은 제외하고 "카메라가 SK(카메라 이름)를 위해 어떤 카메라가 있나요?"라고 질문해 보겠습니다. ( 아직 여름이라 SK가 생각나지 않네요.) 미리 생각해 두는 거죠. 좋습니다. 그럼 첫 번째 검색에서는 랭크를 사용하지 않고, 이번에는 랭크 없이 검색해 보겠습니다. 그들은 보안 카메라를 정말 좋아하는 것 같네요. 어, 왜 이것도 또 다른 보안 카메라죠? 제가 기억하기로는... 네, 이제 결과를 재순위화해 보겠습니다. 재순위화 결과, 이상한 문자열들이 보이네요. 트럭, 그게 문제입니다. 우리는 우선순위 정렬 목록을 최적화하고 싶습니다. 결과 50을 확장하면 상위 10개 결과만 가져올 수 있습니다. 네, 여기서 덧붙이자면, 이 50은 Pine에서 반환된 상위 결과이고, 이 10은 두 번째 단계 재실행 알고리즘에서 반환된 상위 결과입니다. 먼저 Pine에서 50개의 문서를 가져온 다음, 가장 관련성이 높은 상위 10개의 알고리즘 문서를 선택합니다. 그런 다음 다시 실행합니다. 네, 기본적으로 한 번의 호출로 두 단계 검색을 수행하는 것입니다. 그리고 순위 필드를 설명으로 지정해야 할 것 같습니다. 아, 네, 설명은 다시 한번 말씀드리지만 메타데이터를 저장하는 곳입니다. 따라서 재순위화는 재순위화를 수행하기 위해 메타데이터 필드를 참조합니다. 다시 시도해 보겠습니다. 닫겠습니다. 이제 됐습니다. 상위 3개 추천 항목 모두 실외 카메라입니다. 감시 카메라는 순위가 낮습니다. 이 2단계 검색을 기반으로 다른 예를 들어 보겠습니다. 흥미로운 예시인데, 주로 게임 디자이너를 위한 것입니다. 순위 결과를 선택 해제하고 1단계 검색만 사용해 보겠습니다. 예상대로 상위 항목들은 모두 고성능 게이밍 노트북입니다. 다른 재순위 지정과 함께 키워드를 사용했기 때문이라고 생각합니다. 이제 재순위 지정 결과를 도입하고 같은 방식으로 상위 10개만 표시하면 다시 Mac 제품군이 상위에 더 많이 나타납니다. 대부분의 디자이너는 Mac에서만 지원되는 3D 소프트웨어를 사용합니다. 따라서 순위가 ​​이러한 점을 충분히 파악하고 있다고 생각합니다. 네, 이것이 테스트 단계입니다. 이 순위 노드에 만족하면, 보시다시피 단 4단계만으로 API 엔드포인트를 생성할 수 있습니다. 이 경우 애플리케이션 레이어에 연결하여 고객이 사용할 수 있도록 하려고 합니다. CES는 실시간으로 작동하므로 동기화된 HTTP 응답을 출력으로 선택하고 다시 이 API를 게시합니다. [음악] 이번에는 새 프로덕션을 보여줍니다. H 게시, 좋습니다. 이제 여기에 API 호출이 있습니다. 이 curl 명령어를 복사해서 터미널에 붙여넣겠습니다. 터미널 보기로 전환해야 하는데, 모두 보이시나요? 터미널 네, 좋습니다. 제 API 키를 추가하고 질문을 바꾸기만 하면 이 API 엔드포인트가 이제 작동합니다. 다른 질문으로 바꾸면 해당 질문을 기반으로 제품 추천을 제공해 줍니다. 보시다시피 챗봇을 구축하든 기존 웹 앱이나 전자상거래 플랫폼에 제품 추천 기능을 통합하든 동일하게 작동합니다. 쿼리 임베딩 생성, 쿼리 실행, 재순위 지정 실행, 결과 반환까지 단 하나의 API 호출로 모든 것이 가능합니다. 정말 멋지네요. 어떤 질문이든 입력하면 바로 작동합니다. 잠시 멈추고 순위 또는 Pine의 2단계 검색에 대해 질문이 있는지 확인해 보겠습니다. 존, 질문에 답변하기 위해 '실시간 답변'을 클릭하겠습니다. 이 데모가 메타데이터와 재순위 지정 알고리즘을 사용하는 것에 대한 질문에 답변이 되었나요? 존, 만약 답변이 되지 않았다면 다시 질문해 주세요. 다른 질문이 있으신가요? 음, 필립이 달러가 무엇을 의미하는지 묻고 있는 것 같네요 . 이 청크에는 Fleek UI에서 사용한 JSON 경로가 포함되어 있습니다. 텍스트인가요, 숫자인가요? 점수는 내부적으로 어떻게 변경되나요? 음, P 리서치 노드로 이동하면 실행 결과를 다시 보여주시겠어요? 네, Jon 뷰에서 생성된 결과를 보여드리겠습니다. 달러 청크는 실제로 여기를 가리킵니다. 네, 이것이 핵심 문자열입니다. 알고리즘을 실행하면 두 개의 매개변수가 필요합니다. 사실 여러 개의 매개변수가 있지만, 중요한 매개변수 하나는 쿼리 문자열입니다. 즉, 원래 쿼리, 이 청크 필드의 값입니다. 이 쿼리를 사용하여 여러 문서와 비교합니다. 여기서 사용하는 문서는 결과 의 설명 필드입니다. 음, 다시 바 UI로 돌아가시겠어요? 네, P 리서치 단계를 실행하거나 전체 파이프라인을 실행할 수 있습니다. 전체 파이프라인을 실행해 보겠습니다. 네, 보시다시피 팬 결과는 이 일치 목록과 그 안에 있는 모든 요소를 ​​반환합니다. 재순위 지정을 하지 않으면... 이건 Pankong Vector 유사도 점수로 순위가 매겨진 것일 뿐이고, 이 리드의 모든 요소에는 메타데이터 필드가 있습니다. 그리고 메타데이터 필드에는 다양한 필드가 포함되어 있죠. 우리가 이 원본 문서와 비교하는 문서는 바로 이 설명 필드입니다. 왜냐하면 우리가 이 Pankong Cory 노드에서 순위 필드를 지정했기 때문입니다. 그러니까 달러인 거죠. chunk는 원래 쿼리를 가리키고 , rank 필드 설명은 메타데이터 목록, 그러니까 매치 목록의 메타데이터 필드에 있는 필드 이름을 가리킵니다. 그리고 이 값을 가져와서 원래 쿼리와 설명을 비교하고 다른 점수를 계산한 다음, 그 점수를 사용하여 매치 목록을 다시 실행합니다. 제 설명이 명확한지, 아니면 더 자세한 설명이 필요한지 알려주세요. R 순위 지정 방식을 조정하는 것에 대해 Cory, 의견을 추가하시겠습니까? 오늘은 다양한 재순위 지정 알고리즘을 사용하고 있습니다. Pinerank API를 사용하는 경우, 다양한 최적화 메타데이터 필드를 변경할 수 있습니다. 예를 들어 설명을 기반으로 최적화하고 싶지 않고 최적화할 더 나은 필드가 있다면 다른 메타데이터 필드를 선택할 수 있습니다. 이 컨텍스트 외부에서는 Co 도 재순위 지정 API를 제공하는 것으로 알고 있으며, 일부 특정 전자상거래 제공업체는 자체 API를 사용하는 것으로 알고 있습니다. 그들만의 재순위화, 그러니까 달성하고자 하는 관련성 최적화 지표를 알고 있다면, 자신만의 순위 알고리즘을 작성할 수도 있습니다. 또 다른 질문이 있는데요, 그래프 데이터베이스를 사용한 RAG 응답의 포괄성을 기준 RAG 클래스 순위와 어떻게 비교하시겠습니까? 아니면 스크리어 데이터 세트와는 다른 맥락적 시나리오에서 사용되나요? 음, 어떻게 답해야 할지 모르겠네요. 그래프 데이터베이스는 그냥 데이터베이스이고, 문서를 검색할 수 있는 핵심 인터페이스를 제공하지만, 그 문서들이 관련성이 있는지는 모두 그래프 데이터베이스에 쿼리를 어떻게 작성하는지에 달려 있습니다. 그래프 데이터베이스는 머신 러닝 알고리즘을 사용하지 않습니다. 아니, 내부적으로는 대부분의 그래프 데이터베이스가 머신 러닝 알고리즘을 사용하는 것은 아니고, 규칙 기반 방식입니다. 다른 RDBMS처럼 CLE를 작성하는 것과 같고, 검색 품질은 쿼리 순서에 따라 달라집니다. 네, 그리고 덧붙이자면, 만약 만약 소스 데이터의 종류에 따라 검색 방식이 달라집니다. 대부분의 데이터가 비정형 데이터인 경우, 벡터 유사도 검색이 그래프 검색이나 지식 그래프 검색보다 훨씬 효과적입니다. 지식 그래프 검색은 객체 간의 관계가 매우 명확한 정형 데이터에 적합 하지만, 그렇지 않은 경우에는 벡터 유사도 검색이 훨씬 효율적입니다. 예를 들어, 오늘 사용한 소스 데이터는 완전히 비정형 데이터였습니다. 카메라 간의 관계가 명확하게 정의되어 있지 않고, 단지 유사도에 기반한 검색이었습니다. 바로 이런 경우에 벡터 검색이 진가를 발휘합니다. 다른 질문에서 이전 답변이 더 명확하게 설명되었다고 했고, 상위 K 개수를 늘리면 검색 속도가 어떻게 향상되는지, 그리고 적절한 상위 K 개수는 얼마인지, 좋은 점수는 얼마인지 등을 질문했습니다. 실제로 많은 질문이 있는데, 검색 지연 시간은 상위 K 개수와 약간의 상관관계만 있습니다. 일반적으로 상위 K 개수는 1050개 또는 100개 정도이지만, 더 적은 개수로도 검색이 가능합니다. Top K 값이 클수록 읽기 지연 시간이 증가하지만, 쿼리 크기 때문이라기보다는 PineCone 내부적으로 Top K 값이 클수록 더 많은 양의 원시 코퍼스를 처리하게 되기 때문입니다. 즉, 더 큰 테이블을 스캔하게 되어 속도가 느려질 수 있습니다. 하지만 이 경우에도 수십 밀리초 정도에 불과하며 수백 또는 수천 밀리초까지 차이가 ​​나는 것은 아닙니다. 적절한 Top K 값은 애플리케이션의 용도에 따라 다릅니다. 예를 들어 100개의 항목만 보여주는 추천 엔진이라면 Top K 값을 100 이하로 설정하는 것이 적절할 것입니다. 반대로, 많은 컨텍스트 정보를 활용하여 추천을 수행하고, 가장 중요한 정보를 LLM이나 다른 모델에 입력해야 하는 경우에는 Top K 값을 더 크게 설정할 수 있습니다. 하지만 일반적으로 Top K 값은 10, 50, 100 정도를 사용하며, 그보다 큰 경우는 드뭅니다. 그 외에도 좋은 점수가 무엇인지에 대해서는 애플리케이션에 따라 매우 다릅니다. 추천 엔진이라면 관련성 점수가 매우 높아야 하지만, 이상치를 찾는 경우에는 관련성 점수가 낮을수록 더 좋습니다. 왜냐하면 해당 벡터로 확인하는 쿼리가 이상치, 즉 알려진 예시와 일치하지 않는다는 것을 강조하기 때문입니다. 따라서 좋은 점수의 정의는 매우 주관적입니다. 아, 필립이 추가 질문을 하나 했는데요. 선형적으로 증가하는지 아니면 지수적으로 증가하는지 궁금합니다. 제가 틀렸다면 필립, 수정해 주세요. 제 생각에는 상위 K개 증가에 따라 지연 시간이 선형적으로 증가하는지 묻는 것 같은데, 정확히 예측하기는 어렵습니다. 네, 감사합니다. 필립이 바로 그 질문을 했습니다. 정확하게 예측하기 어려운 이유는 앞서 말했듯이 기본 코퍼스와 관련이 많기 때문입니다. 백만 개의 벡터가 있고 상위 K개가 100개인 경우와 상위 K개가 1,000개인 경우를 비교해 보면 차이가 있습니다. 벡터가 10억 개 있는 경우 상위 K 값이 100개인 것과 1,000개인 것 사이에 큰 차이가 없을 겁니다. 하지만 상위 K 값이 1,000개인 경우에는 더 큰 차이가 발생할 수 있습니다. 또한 방금 실행한 것과 같은 쿼리를 얼마나 자주 실행하는지에 따라 달라집니다. 매우 특이한 쿼리가 많이 실행된다면 캐시를 자주 받지 못할 수도 있습니다. 따라서 고려해야 할 사항이 많습니다. 하지만 일반적으로 상위 K 값이 클수록 지연 시간에 영향을 미치지만, 전체적인 관점에서 보면 그 영향은 매우 작습니다. 실제로 매우 유용한 방법은 최상위 수준 세분화를 기반으로 데이터를 더 효율적으로 저장하는 것입니다. 이렇게 하면 각 인덱스의 검색 크기를 훨씬 줄일 수 있습니다. 또는 벡터 매칭 외에도 네임 스페이스나 메타데이터 필드를 추가하여 더 정확한 선택을 할 수 있습니다. 이러한 모든 기술은 지연 시간을 단축하는 데 매우 유용할 수 있습니다. 좋은 지적입니다. 스티븐이 여러 필드를 벡터로 저장할 수 있는지 묻습니다. 이 예제에서는 제목에 설명이 있습니다. 기술 통계 필드와 추가 예시(예: 설명)가 있다면 좋을 것 같습니다. 질문을 제대로 이해했는지 모르겠네요. 스티븐, 여러 필드를 벡터로 저장한다는 게 정확히 무슨 뜻인지 좀 더 자세히 설명해 주시겠어요? Fleek 파이프라인에서 원시 데이터로부터 임베딩을 생성할 때 여러 필드를 선택해서 임베딩을 만들 수 있는지, 아니면 필드를 연결해야 하는지 묻는 건가요? 네, 말씀해 주세요. Pine의 경우, 한 번에 하나의 벡터만 생성하는 것으로 알고 있습니다. 문서에서 벡터를 생성해야 할 때, 문서에 여러 필드가 있을 수 있는데, 이 경우 하나의 필드를 선택해서 벡터를 생성하거나, 먼저 필드를 조작해서 새 필드를 생성하고 '새 필드 설정'을 사용해서 벡터를 생성한 다음, 메타데이터와 함께 벡터를 저장해야 합니다. 메타데이터에는 여러 필드를 저장할 수 있죠. 네, 맞습니다. 벡터만 생성하는 것이 맞습니다. 하나의 필드에 대해서만 벡터를 생성할 수 있습니다. 여러 필드를 임베딩 생성에 포함시키려면 먼저 데이터 정리 과정에서 필드들을 하나로 합쳐야 합니다. 임베딩을 생성하기 전에, 또는 여러 필드에 대한 벡터를 생성하려면 동일한 문서에 여러 벡터를 저장해야 합니다. 여러 벡터를 생성한 다음 저장하는 것이죠. 두 번째 질문은 흔히 발생하는 질문인 것 같은데, 재순위 API를 사용하는 대신(제 이해가 맞다면), 비정형 필드를 기준으로 순위를 매기는 대신 메타데이터와 같은 통계 필드를 사용하여 결과를 재순위화할 수 있는지에 대한 것입니다. 텍스트 임베딩 대신 통계 필드를 기준으로 결과를 재순위화할 수 있을까요? 제 생각에는 충분히 가능합니다. 일반적으로는 상위 50개 또는 상위 20개를 가져온 후에 이러한 작업을 수행할 수 있습니다. 또한 상위 K개 벡터와 관련된 메타데이터를 얻을 수 있으므로, Fleck의 경우처럼 정렬 순서를 쉽게 추가할 수 있습니다. SQL 노드를 사용하여 필드 x를 기준으로 정렬하면 됩니다. 해당 필드가 숫자형이면 내림차순 또는 오름차순으로 정렬할 수 있지만, 통계 필드가 더 복잡한 경우에는 결과를 얻은 후 원하는 방식으로 순위를 매겨야 할 수도 있습니다. 제 설명이 이해가 되셨는지, 아니면 질문에 대한 답변이 되었는지 모르겠습니다. 다시 말씀드리지만, 메타데이터는 검색과 함께 제공되므로 이를 활용하여 어떤 조치를 취할지 결정할 수 있습니다. 또는 재순위 필드를 사용하여 테스트 필드에 대해 ALM 매칭을 다시 수행할 수도 있습니다. 또한 통계가 단일 숫자이고 서로 가까운 값을 찾고 있다면 중간 필터링을 사용하는 것도 좋은 방법입니다. 특정 범위(예: 보다 큼, 보다 작음) 내의 통계를 찾고 있다면 중간 필터링을 사용할 수 있습니다. Post St, 제 생각엔 그게 최신 버전인 것 같아요. 네, 스티븐의 마지막 게시물이었던 것 같은데, 다른 게시물 있나요? 네, 좋습니다. 스티븐, 좋은 거래였어요. 저희가 이렇게 하는 이유가 바로 이거죠. 많은 분들에게는 모든 게 처음이라 때로는 좀 부담스러울 수 있거든요. 그래서 저희는 고객분들이 Pine Count와 Fleek을 잘 활용하실 수 있도록 교육하고, 성공에 필요한 모든 도구를 갖추도록 도와드리고 싶습니다. 그리고 또 다른 게시물이 있는데요, 수강생 들의 입력 정보를 바탕으로 한 강좌 순위도 있습니다. 완료율(수강 완료 학생 수), 등록률(수강 등록 학생 수), 1점에서 5점까지의 평점, 그리고 강좌 출시일도 있죠. 저희는 완료율, 등록률, 평점, 그리고 출시일을 기준으로 강좌 순위를 다시 매기려고 합니다. 당연히 완료율, 등록률, 평점이 가장 높은 강좌를 추천하고 싶고, 최근에 출시된 강좌도 추천하고 싶습니다. 흥미롭네요. 음, 스티븐님의 경우에는 이미 결과 정렬 규칙이 있는 것 같네요. 그렇다면 쿼리 결과를 가져온 다음, 이 규칙을 적용해서 순위 재조정 알고리즘을 실행하면 될 것 같습니다. 오늘 소개해 드릴 내용은 의미론적 재순위 지정과 유사합니다. 자연어 및 구조화된 데이터의 경우 재순위 지정이 가능하지만, 이미 모든 속성이 포함된 코스가 있다면 규칙에 따라 재순위를 지정하는 스크립트를 작성할 수 있습니다. 재순위 지정은 ORDER BY 절이 아니라는 점을 명확히 해야 합니다. 즉, 특정 필드를 기준으로 순위를 매기는 것이 아니라 원래 질문과의 유사성을 기준으로 순위를 매기는 것입니다. 이것이 바로 랭커가 하는 일입니다. 호세, 질문하신 내용은 애플리케이션 자체에 구현되는 비즈니스 로직으로, 메타데이터 필터를 기반으로 필터링 및 정렬하는 기능을 의미합니다. 팸은 "감사합니다. 천만에요."라고 답했습니다. 네, 이제 분 정도 시간이 흘렀습니다. 추가 질문이 있으시면 충분한 시간을 드리겠습니다. 질문이 없으시면 일찍 마무리하고 참석자분들께 10분 정도 시간을 드리겠습니다. 질문하실 시간을 1 ~2분 드리겠습니다. 질문이나 의견을 자유롭게 해주세요. 모든 분들이 필요한 답변을 얻으실 수 있도록 하겠습니다. 네, 이 정도면 된 것 같네요. 보 이안, 오늘 함께해 주셔서 정말 감사합니다. 자세한 설명 감사드립니다. 코리, 정말 훌륭한 제품이고 저도 제 업무 에 활용할 수 있기를 기대합니다. 감사합니다. 감사합니다. 감사합니다. P, 감사합니다. 이 데모를 사용해 보고 싶거나 Pineco에 데이터를 쉽게 입력해 보고 싶은 분들은 F에서 무료 계정을 만드시면 됩니다. 네, 뵙기를 기대하겠습니다. 감사합니다. 좋은 하루 보내세요. 안녕히 계세요.
