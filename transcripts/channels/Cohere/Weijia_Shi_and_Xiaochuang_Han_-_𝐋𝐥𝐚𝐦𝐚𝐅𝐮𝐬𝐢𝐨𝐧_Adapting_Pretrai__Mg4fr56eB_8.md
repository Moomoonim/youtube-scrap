# Weijia Shi and Xiaochuang Han - 𝐋𝐥𝐚𝐦𝐚𝐅𝐮𝐬𝐢𝐨𝐧  Adapting Pretrained Language Models for Mult

- 영상 링크: https://www.youtube.com/watch?v=Mg4fr56eB_8
- 채널: Cohere
- 업로드일: 2025-11-14
- 자막 언어: en
- 단어 수: 약 7844개

---

## 스크립트

[music] Hi everyone, thank you so much for joining us for a special guest speaker session uh at the original regional Asia. We have with us Vijay Shi and Han. Vijaya is a PhD student at University of Washington. Her research focuses on language model pre-training and digital augmented models. She also studies multimodel reasoning and investigates copyright and privacy risk associated with language models. and he's a research scientist at Metafare. He recently completed his PhD at University of Washington and his work focuses on multimodel generative way. Thank you so much Vijhan for taking out time and I'll hand it over to you. &gt;&gt; Thanks so much for the introduction and thanks thanks everyone for being here. So in today's talk I will give a broader overview and how I think about um what kind of language model we should build in the future. So instead of just focusing on like one project, the llama fusion, I will give a broader um broader picture covering several projects. Um so and in general um I will dive into some of the challenges we're facing with current language models and I want to convince you that simply scaling up the current paradigm is going to hit a wall and I will propose an alternative way to build this model. So this may not be the only way to go but just want to offer a like different perspective and please please feel free to um post any question online. I will check it. Um yeah I I will check it. Yeah I will check it during the talk and we also have a QA session at the end. Okay cool. Let's get started. So a default paradigm for today's language models looks something like this. So we train one very large model end to end on all data and this one model is deployed for every use case. We've seen a huge success just by scaling up this monolithic model. As we increase the model size and amount of training data, they just keep getting better and better and the compute required for training this models grows exponentially. But at the same time, we're also seeing a lot of limitations with this monolithic design. For example, once a model is trained, it becomes very difficult to update the model with new knowledge because all the data and knowledge are deeply intertwined within a single set of parameters. But there are many reasons we may want to update the model. For example, in our previous work, we found um it's possible to detect pre-training data from current blackbox language models. We apply our proposed approach to detect the GBT3 pre-training data and we found that model is likely trained on books listed here and a lot of books are copyrighted from the book three um the pirated books collection and at that time the book three data set was posted online. So, it's possible that the model developers just scraped the entire internet and accidentally included in pre-training. But including this kind of data in pre-training could lead to a lot of problems like many book authors filed lawsuits against the major AI companies requesting their data to be deleted from the train models. This research question is it possible to actually remove the data from a trained monolithic model. Researchers have proposed different oning methods with a goal to remove specific training data from a train model without the without hurting the model utility. Can any of these methods work in case like this? In our subsequent subsequent work, we found it's actually very difficult to this unlearning method to to to use this unlearning method to make a model forget specific data. So looking at this plot, ideally we want the model to uh fall in the top up top left corner. So it will preserves the model utility but make the model forget the target data. But for a lot of unlearning methods we evaluate um they either remove the data completely but at the cost of utility or they cannot really make the model forget the data. So for the points lies in the middle. So this shows it's very difficult to remove unwanted data from a monolithic model once the model is trained and not just hard to update the current monolithic model. The current scaling paradigm is also going to hit a wall. data has been very important for driving a lot of progress in model scaling but we are soon running out of the we are soon running out of data so here I'm showing a projection of total stock of human generated public text versus the data versus the data set sizes used to train the major models you can see if we If we keep following the current development trend, we will soon use up all data. So we're about to become data bound. We cannot just keep scaling the current paradigm and hope it will lead to the next breakthrough. So how can we do things differently? In this talk, I want to discuss a different principle for building language models. Modularity not monolith building language models from composable components. I will start by giving an overview of what modularity means and explain why why I think this is a promising direction forward. There are two types of modularity I will focus on today. The model modularity. Instead of having one model do everything, we can augment it with external modules like retrievers or specialized models. Each bring new capabilities at inference time. And a key example of this is a retrieval augmented language model. The direction I've been studying for a long time which has also been widely adopted across many real world applications. So I know many of you are probably familiar with this but just a quick recap the way it works is unlike the standard pre-training models that makes prediction solely based on the input context retrieval augmented models would look up an external database retrieve a few related documents and combine the both the input context and retrieve information to make the final prediction. The data store is very flexible. So you can change it at any time to update the model with new knowledge. I work on different pipelines um at different stage in this pipeline from building a better retriever model to pre-train language model to use retrievers. I will dive into the details um in the later section. And augmenting language model with retrievers is just one way to add new capability the model does not have during inference. And more recently I've been exploring how this principle of modularity and augmentation can ex can extend beyond text to the visual domain. Um in our recent work we show by augmenting um language models with multimodel tools to sketch and draw we can make them reason visually much better. Let me give an example. So we have this geometry question like the angle uh fee k is 105° angle bcj is 105 degrees. Can we find the angle eic? So I give this question to the previous GP4 model and this is the response the model generates. You can see the model completely relies on text to generate it chain of thought and doesn't get the answer correct. But now I think how would I approach this problem? So when we solve a geometry question like this uh we will naturally sketch it out on paper. We may ask ourselves um if a supporting line is needed to help solve the problem. Then we will draw the line on the paper to guide our reasoning process. So this way of drawing and sketching are very fundamental to how we solve the problem, communicate or even generate ideas. Now we let language model do the same thing. Drawing inspiration from this, we recently proposed a visual sketch pad is a framework that augments the multimodel language models with a visual canvas and a set of drawing tools. For example, when we given this geometry question, the the model may think okay I probably need to draw an auxiliary line. Then each use code generation to draw this line. And this intermediate sketch could guide the model to reason through this problem. And we found this ability to think through drawing can greatly improves the model performance on reasoning intensive tasks. Um so we're so model modularity can address a lot of problems um in current monolithic models but where are we going next? I think we're just at the beginning of this change towards modularity and we should push it even further. So in this talk I also want to convince you that the next frontier of modularity lies in data. We should develop language models that are distributed and data modular. So we often say we're running out of data, but to be more precise, it's really the public data that is running out. There's actually a lot of high quality but high-risisk data currently cannot be used in training language models due to privacy or regulatory issues. So I think one promising path going forward is to find ways to use this private data through new training algorithms and architectures that preserves data privacy. So this brings me to my next proposal data modularity different from standard monolithic models that centralize all data and train all parameters indiscriminately on all of them. I think future models should categorize data based on its prominence. So where the data comes from and who creates it. Then we can train different model components locally on different data sources preserving data privacy. Then during inference we can mix mix and match these different components based on different applications and user requirements making the model highly customizable. And one example is we can also we can have more fine grain data access control using this new methods. Let's say if a user have access to a Washington Post and Disney content in this case we can turn on the module that train on these two data sets. But at the same time if another user doesn't have the permission to use any of this data we can simply turn this components off. Um I prototyped this idea uh in our recent work faxo which trains the first mixture of expert language model via data collaboration letting multiple parties to independently contribute decentralized train experts. And now um this paradigm is being applied within the cancer AI alliance to model private clinical records. So this about flexo but what are some other benefits of data modularity. The standard multimodel um understanding and generative models are usually trained end to end from scratch on the joint tax image corpora. So this requires significant computation resources to achieve good performance across both modality. But this method overlooks language models and diffusion models that already exist. So can we comp and so in our so in our recent work lava fusion we look into the question can we compose a multimodal generative models from existing pre-trained modality specific components. So instead of training every everything from scratch, can we take existing language model like llama or existing image generation models like flux and we compose them and into into unified models with a light fine-tuning. There are many benefits of doing this. So first compared with the standard model train end to end from scratch it would be much cheaper to train such unified models because we can reuse the compute of these specialized components and another benefit is the optimal model size may vary across different modalities. If we just have one monolithic model um train on both modalities then this means during test generation and image generation um both modality input will go through same same parameters but different modality may require different um size of model parameters. So for example for text generation maybe we need a larger model size but for image generation and a small model will be enough. So if we can compose different size models together then we can also achieve inference efficiency. And the last benefit is because the because the model architecture is modular we can easily add another modality component into the final model with cheap fine-tuning. And so this overview of what I'm going to talk about in this today's talk. Um but in a high level um both like the model and data modularity connect to my research goal of developing modular architectures and training algorithms to make language models more controllable, factual and um collaboratively developed. Um okay let's jump right in and talk about um model modularity. So in this part I will take a deep dive into how to build the retrieval augmented language models. Um the retriever is probably like the earliest type of external modules used with language models. So many of the lessons and design principles we learned from building retrieval augmented models can generalize to other types of modules. Um so I so I study uh how to adapt as where I study how to build the better retrievers and how to um pre-trend language model to use retrievers better. Um because of time constraint I will skip the first part. Um I will talk about how we pre-train language models to use retrievers better. Um so the standard way to put a retriever together with a language model is to directly concatenate um the retrieve documents with the input context um and fit it into language models. This works pretty well, but sometimes um it still has some errors. the language model has never seen the cont concatenation of related documents. So often times it will make a lot of errors. So for example, you will ignore retrieval results or rely too much on incorrect retrieval results. So the question I try to ask is can we pre-train a language model to make better use of retriever? So how would that actually work? Um let's think through it together. So suppose we have a large collection of documents in our pre-training corpus and we pick one of them. So let's say document zero here and we will feed this document into the language model and train it to predict the next token just like the standard language modeling. Now with a retriever in loop we can take this document zero and fit it into retriever. Then this retriever would return the top k related documents. So let's say document five and document three are the top k retrieve results for document zero and then we can let the language model condition on the both document 53 at zero just like how the standard retrieval augmented language model work at test time. But this brings a design question. So where should we apply this loss? So one option is to apply the loss to only document zero because it's our original target document. But this setup is bit inefficient. We're fitting in a large amount of context like both document five and document three here. But the model is only learning from tokens in document zero. So the training signal is not very rich even though the input context is long. And the alternative is to apply the loss to all the documents here. Specifically, we can apply loss to document five, three, and zero. This setup is much better. It allows a model to learn from the range of context and generalize to conditioning on the ver varying number of retrieved documents just like the model would encounter during inference. But here's the problem. So some documents naturally show up more frequently as a top k retrieval results across a corpus like in this case document five um appears many times as nearest neighbor documents to any other query document. So this will lead to a duplication issue. the model would end up seeing and training on the same document five over and over again which can hurt generalization. So to make training more efficient we really need to satisfy two constraints. The language model needs to condition on the set of related documents and each document should appear exactly once preserving its original frequency. And how can we achieve that? We ged this problem into a document ordering problem. So given a pre-training corpus consisting of many documents, we can construct a graph where each node represents each document and two nodes are connected if they're nearest neighbors of each other and then we can convert this graph into a sequence of documents by traversing it. So in this case we can obtain the following sequences of dots. In the last step we just need to divide them into different buckets. So let's say our language models max sequence length 20. Then we can directly group the document zero document document 9 together document 135 together and document 7. But once we have that the training is very straightforward. We can concatenate all the documents in the same bucket and fit it into language models and apply loss to all of them. Now you can see this really satisfy all the constraint we need to meet. So first related documents are put in the same context. Second each document appears exactly once and this method is very simple. All the things I've discussed can be done in the data prep-processing step. So any of the shelf training codebase can be used for actual training. So how well it works? We train models with up to 7 billion parameters from scratch on a total of like 300 billion parameter 300 billion tokens. And we can see our proposed method income pre-training is much better than the standard pre-training method. Beyond reading comprehension, we also tested how well the model performs when augmented with retrieval at inference time. So here we compare against both standard pre-training and a K nearest neighbor baseline which is a version I discussed earlier that is similar to ours but some documents can appear many times during training. You can see uh the K method is not very good likely due to the um duplication problem. And now here's our model. It outperforms all the baseline showing that training with retrieval can actually help the model read retrieve documents better during inference leading to around 12.3% improvements. And there are some other benefits our proposed methods can bring uh like income learning. We also find our methods perform much better um on the like 32 shot in learning tasks. So why does our method also help with increased learning? My explanation is our training setup helps bridge the gap between how we train the language model and how we use the language models. So in retrieval augmented generation setup the model needs to read concatenation of related documents and in incomes learning the model also needs to read related example and generalize to a test example. By training the model to condition on the retrieved related inputs during pre-training, we bridge this gap between language model training and use. Yeah. And this method uh has been adopted as like some long as a training recipe for loan model because there are not that many loan documents out there in the standard pre-training data. So for example like in common crawl um only about um 80% uh so almost about 80% of documents are pretty short typically under 1,000 tokens as shown in this figure. So inquence pre-training provides a way to construct long and semantically related context helping the model to learn to reason across multiple pieces of information within a single long context window. So this wrap ups um the first part on model modularity. Um is there any question in the chat? Oh yes, one question um posing the chat is at this point we will have to assume that retrievers have to be extremely good otherwise the pre-trained models will come out bad. Is this correct? Um it doesn't need to I don't think it I don't think needs to be extremely good. As long as the documents are sort of uh related, we you then putting all these documents together could provide some meaningful signals compared with the um standard pre-training methods. Uh and I'm not sure if this answers the question. Um but by examining the um the long context documents we um we we retrieved using the using the inhouse pre-training we have we can see sometimes the documents are very related. So you will provide meaningful signal for the um next document to to to conditional and in another question is in your model is a retriever trained alongside the model? Uh no in our case um we just construct uh we do a one-time retrieval and to build this document graph and the retriever is not trained along with the model but I think it would be an interesting direction to explore that but I'm not sure if this will work at the pre-training scale because here we're doing the pre-training from scratch if we want to train both things end to end um probably we can apply it to the finetuning Another question is what were some useful signals that showed you that the model's retrieving abilities improved? Um do you mean the training signal or like the evaluation metrics? I think uh if it's evaluation we observe like in the long context tasks our our train model is better um likely because uh the model hasn't seen a lot of long documents of using the standard pre-training methods. Uh okay the next question is why is not just fine-tune the language model with your technique and then retriever language model could both be fine-tuned for better retriever and enhance learning. Yeah and that's a really great point. Um we can definitely apply our method to continue pre-training but for our experimental setup we did pre-training from scratch to do a control comparison and I think uh do I answer all the question? Uh is there any more? Okay. Uh if there's no more question um let me move to the next section. Um the data modularity. Um so so cancer is probably one of the most serious challenges we faced today. Um can we use AI to help advance cancer research? For example, um can we train a language model on cancer data then use it to assist researchers and clinicians in generating uh new insights or guiding treatment decisions. This is exciting idea but there's one critical bottleneck. The cancer data needed to unlock AI potentials already exist, but they are siloed across 57 national cancer institute due to patient privacy and um intellectual property concerns. So we cannot just centralize all the data and train all parameters on them like in the standard monolithic language model training. Um so this brings me to the idea of data modularity and how about we just train different components locally on different sources of data and then mix match these components depending on the downstream use cases. There are two key benefits to this approach. So first we don't need to centralize all data together um allowing the model to be completely trained in a decentralized way to preserve data privacy and second whenever we need to update the model we can simply update the related experts instead of train the entire model from scratch. So how can we build this model? There are different design choices like um what each component is and how to combine them. But in today's talk, I will focus on how to train such model using the modern mixture of aspert architecture. So let me introduce flexo is a new type of mixture of aspert model where different experts are trained on different sources of data. Um before diving into how we designed the training algorithm um let me walk you through how the standard mist of expert work. So here's a head-to-head comparison from our previous OMO e paper. So unlike the dense model, the mixture of asper model replaces the fif layer in each transformer block with a router and a small set and set of smaller f4 networks called asper modules like E1 to um E6 here and for each input token the router decides which expert to route the token to. So in standard both a router and experts are trained end to end on entire data set just like in the standard dense model. So even so even though the model architecture is modular there's no control over how specific data influences the model parameter. Um so we proposed flexo to um to allow a two different to allows for two features in trainings. So it's a new type of industry of asper model but with one key difference. The different aspers are trained on different data sources and our model supports two advantages to two key features. So first flexible training. It allows each data owner to train their expert locally ensuring that raw data remains private and never has to be shared with central entity. It also supports flexible inference. So at inference time we can activate different experts along with their corresponding data sets based on the end application or user access policy. For for example uh if the user doesn't have access to Washington Post data, we can simply exclude that train aspert from the final model. So how do we train this model? One straightforward way is to treat each F4 layers and attention layers as a standard dense model and train each dense model independently on different data source. But this comes with two major downsides. So first since all the experts the dense model are trained completely separately so no router is learned during this stage. So this means we need an additional merging step to combine all the experts and train the router and this merging step is not ideal because we need to take all the experts together and do a centralized training and not just this centralized training because all the experts are trained completely in isolation. they can diverge significantly from each other making the final merging very difficult. So to to address this issue we introduced two key training methods. So first we train the experts to coordinate with each other and then we design a domain inform router. Uh let me explain how this works step by step. So first we train a public a shared public model in the first stage and then this public model is shared with each data owner who use it to uh train their own expert. Uh more specifically each data owner would initialize a mixture of experts with two expert. So one is this um public model that is kept frozen during training and then they train their priv private experts on their local data. We also reuse attention layer from the public model and keep it frozen during training. So this setup ensures that all local experts are aligned with the same public experts. So by extension they align with each other. So during inference we can simply combine all the trend experts into one final model. But how does this merging work? And to answer this let's take a closer look at how the router is trained. Um in standard the router is a linear classifier that is trained end to end along with all the experts. But in our case we cannot train the router end to end because um the aspers are trained independently. So what we do is we decompose this router weight matrix W into asper specific embeddings. So each row here corresponds to an embedding specific to that expert. And instead of initializing the entire weight matrix randomly like in the standard, we use domain specific embeddings to initialize them. So let's say our expert one correspond to the news domain. So the way we initialize a router embedding one is we take a lot of news article we feed them into a retriever and this retriever will give um average document embedding and we use that to initialize uh R1 vector. The advantage of using as using the domain embedding for initialization is that this kind of domain specific embedding can provide meaningful signals about what type of contents each expert handle serve as a much stronger starting point. So during local expert training the public router embedding our pup is shared across all different data owners and kept frozen during training and each data owner then continually train their own expert specific router embedding. So R1, R2 to R and here. Then during inference we can simply concatenate all the expert routing embedding together in the same um in the same embedding same embedding t embedding table. But one thing you may notice here is there's difference between like how we train the model and how we use it during inference. So during training each experts only learns to route between the public expert and the one local expert but during inference the model has to route among all different local experts. So how do we bridge this gap? Um so we can view this routing as a classification problem. So essentially during each local expert training we are learning n pairwise binary classification deciding between the public expert and the local expert based on the local data. But during inference we're doing a multiclass classification. We need to recombine all this binary classifier into a single multicloud classifier that can choose among all experts. So the key to making this work is train is to train each binary classifier in a way that decide decision boundary uh in a way that decision boundary is pushed as far as possible towards the local class. As to achieve this we introduce a negative bias during expert training to push this decision boundary closer to the local expert. So during the training the router makes a decision by comparing the score of the local and public experts with the negative bias. So B1 to BN included. Um yeah this is how the algorithm works. Uh let's look at how how well it works in practice and uh some experimental results. So for training our public anchor model, we use one trillion tokens from common crawl. And to simulate high-risk or private data sets, we selected sources um from historically sensitive or have recently become in inaccessible data u due due to the change in data access policy for example like radio data. So and we train eight experts with a total of 37 um parameters and out of which u 20 billion parameters are active during inference. We evaluate the model on both general domain and domain specific tasks. So let's first look at how each individual expert performs. Uh we find like each expert like from public to textbook they are good at in specialized domain. For example like the mass expert performs pretty well on mass tasks. Um general domain benchmark most asperts perform similarly but with the exception of the coding aspert which may be overfitted to the um code coding domain data. Next, we combine all the experts into unified model using flexo and we find it if it can outperform the best individual experts on their respective tasks. And this is likely because the model can effectively combine the complimentary strength of different experts selecting the most relevant components for each example in the data set through the router leading to a stronger performance. We also compare our flexo approach to other model merging methods like model soup which average model weights or other like output ensembling methods like BTM. So across the board we found the flexo um consistently outperforms on all of them. Lastly, we compare flexo with a standard model trained on the full combination of data entry end. And surprisingly, we found that flexible could still perform better in this case. And compared with the standard, uh, one major advantage of flexo is its ability to out specific data source at inference time. For example, when we remove the news expert, the performance on the news related evaluation drops a lot, but other tasks remain unaffected. And this is what we want. It means we can selectively remove a data source without disrupting the rest of the model's behavior. And um so now flex ovo is being applied within the cancer AI alliance to help model uh sensitive clinical records across um different cancer centers. So each hospital will start from a shared public model. Then they train their own experts on patient data and then this independently trained experts can be merged into unified models and the data and task is like this. So given the patient history written in natural language, we use a model to predict next clinical diagnosis, future lab values and miss medications. So here you can see a comparison between flexo federated learning and other open source models and flexo perform the best and isn't just better than flexo in terms isn't just better than federated learning in terms of performance. It also brings um several practical advantages. So first training flexo happen completely in isolation but federated learning typically requires synchronization of the gradients across institution and when we build this federated learning baseline we actually run into a lot of issues for um the synchronization and unlike federated learning that eventually trains uh one dense monolithic model the flexo architecture could allow for modular upbase So whenever data changes or center changes we can simply add or update the corresponding aspect without the need to retrain the entire model. Um any so this about the flexible any question up to this point. Oh um Abraham um thank you so much for the presentation. I was just wondering how do you train the initial tokenization that's used across all the models the tokenizer that's used across all the models. &gt;&gt; Oh you mean the public model here and how do we train the public model &gt;&gt; the tokenizer particularly that tokenizes this the inputs like the data sets into the model. &gt;&gt; Oh yeah that's a great question. So here we just start from a public open model uh existing models and we um give the same pre-processing headline to each hospital to make sure the data is pre-processed in the same way. So although the data may have slightly different um distribution because different hospital may have different expertise but I think the overall data format is as similar. So we can um directly uh combine the train experts. We didn't like &gt;&gt; do you think you so why I particularly asked was about the distribution shift uh because every hospital don't have different data sets. So do you think there's anything that could be done to assess how much performance drop you get from the distribution shift? Oh, whether the can still &gt;&gt; like if if there's a way you guys are currently evaluating the drug performance that's coming from the distribution ships across different hospitals. &gt;&gt; So basically what we need to compare is like if we just train one in-house model on one hospital data does it perform as well as like the combined model train across all the all the hospital data and then And we found training the combined model across all the hospital data is better than the one than training one train 21 model only. &gt;&gt; Ideally maybe what I would how I think a better comparison for me would be if you get a subset of data set that's not from the hospitals. So that's going to be like um not clinical data set now because that's wouldn't be available and then you train on all of those &gt;&gt; and then you train individual and you train your experts with subsets of that data set. That seems like a better comparison compared to just training one model um with just one hospital's data set. &gt;&gt; Oh, I see your point. Yeah, we haven't tried that in our setup. I think that's a really really great point. &gt;&gt; Yeah. Yeah, we can definitely add it for &gt;&gt; Okay. Yeah, because there are a couple of works that have shown that you can you can get back the training data distribution by just looking at the tokenizer. &gt;&gt; Yeah. So, I understand that if you train a whole tokenizer across other hospitals, there could be leakage of information. But again, there's going to be a better performance doing that. &gt;&gt; Yeah. Yeah. That's really good point. Yeah. &gt;&gt; Yeah. Thanks so much for suggestion. Okay. Cool. Uh is there any other question for this part? Yeah. If not, uh yeah, let me talk about the the M fusion. So besides merging different X only components, we could also try to merge models trained on different modality data. So in our recent work um llama fusion we try to merge models across modality. So unlike the prior work that prior multim prior multimodel models that is trained uh enterend on combination of text image data from scratch and we propose to uh reuse a existing pre-trend um language model like lava and add a image module on top. So the way it works is uh we use an llama model pre-trained for language and we added a dedicated transformer module for visual understanding and image generation using the modat specific fif layers to process text and image input separately. So basically if we have a text input we fit it into the uh the text language models and um you go go through this the attention layer the fifth layers and if I have an image input we will fit it into the the the image module it also has separate like QV and FFN and the way the two modules interact with each other is through um the self attention so basically um the image tokens processed from the image tower and the text tokens processed from the text tower can attend to each other on in every layer. So the merging is kind across every layers in the um transformer model. Um oh uh is there any question? Oh, &gt;&gt; yeah. I have a question. Yeah. Shall I ask now or? &gt;&gt; Yeah. Yeah, sure. Sure. Please feel free to ask. &gt;&gt; So, when you Yeah. So, when you merge two models, does this means that uh after pre-training whatever the weight matrix you get and uh merging means I need two components. So, one is one weight matrix and another models another weight matrix. So then you what is the technicalities? Can you tell me to actually do the merging part? Means you are merging the two matrices and the the weights component and then you get a resultant and that you called as a merged model because those that m the resultant matrix have values from the first model and second model to the what is the core technicalities uh to do the merging? Oh uh just to make sure I understand the question. Um so basically how do we do this kind of merging if we have separate modules? &gt;&gt; Yeah. So does the merging means weight merging or something else to merge? &gt;&gt; Yeah that's a great question. Um we are not doing way merging here. So the way we're merging two modules is by cross attention. So basically any the text token can attend to every all the image token here across every layer and all the image token could attend back to the text module. Sorry the text tokens output from the text module. So we are not adding additional parameters. So basically we have let's say we have one transformer for image one transformer for language models and the way it interact with each other is they can attend to each other's to token in every layer. Oh is this clear? Yeah, I got it. Got it. &gt;&gt; Yeah. Um, yeah, I think for Yeah, definitely there prior work proposing like way merging for merging different like text only language model but I'm not sure it will work in like cross modality merging because the two models are too different. &gt;&gt; Correct. Yeah. Yeah. Maybe I'll read this and if I have any question I'll email you. Uh, &gt;&gt; yeah. Yeah, definitely. Please feel free to email me. &gt;&gt; Yeah. Thanks for the question. &gt;&gt; Yeah. So, so here's how well it works. Um, compared with like transfusion which is a prior model that's trained end to end on combination of all data and our proposed approach to LA fusion could achieve um good performance with much much uh less compute. Um okay so uh this actually wraps and the end of part three. So let's recap what we discussed today. So in this talk I describe some of the fundamental limitations of current monolithic language models uh which motivates my research on modularity trying to build models from composible components to overcome these challenges. So first I talk about how uh the model modularity how we can augment existing language models with external tools like retrievers or um visual modules to enhance its factuality or reasoning and um in the last part I covered data modularity. Um, I think we should build future language models that are inherently modular from decentralized train components that can be co-developed to ensure data privacy and control and we can also merge models um from different modality to um to build them more efficiently. Okay, thank you. Uh there any question for Oh, Omar &gt;&gt; just a question. Uh I mean usually what I know is when we're trying to fuse like for example model that has been trained on images and language model we try to uh project the tokens that come from the images uh using a projection layer so that we have uh the tok the image tokens uh projected into the language embedding space. But here it feels like uh the combination only comes at the self attention layer. Uh which you said it's a cross attention layer. Uh so I don't know how both like methods compare or if even what I'm saying is accurate or not. I'm I'm not sure. &gt;&gt; Oh uh what do you mean by like projecting the image tokens into the text input embeddings? Oh, I think we here we use a standard um standard tokenization method to tokenize all the image &gt;&gt; image. Yeah, I mean probably I'm wrong but what I thought is done is uh there is like a linear projection layer uh spec specifically for the tokens of the image and in a way the idea is we want those tokens to be projected into the embedding space or the space of the language uh the language embeddings &gt;&gt; and then the fusing happens like naturally like this. Are you referring to like image understanding like clip clip features because they train this &gt;&gt; yeah yeah &gt;&gt; features align with the text. So here we're actually focusing on image generation. So for image generation people usually use this VA tokenizer. So it's not projected to the um to the text space and we're following the previous um previous model to like the transfusion. We use exactly same setup. &gt;&gt; Okay. Okay. So, yeah. So, my Yeah, I thought it was about image understanding more than &gt;&gt; Yeah, I think people usually use different tokenization for image understanding and generation. So, for understanding they have this click feature which is um which aligns the text and input embedding space. But for the image generation um yeah, I don't think the embedding space is aligned. &gt;&gt; Yeah. Yeah. Yeah, it makes sense. probably it would be like aligned in like um not into the language subspace but into the image subspace I mean differently if you know what I mean so it's because the output would be an image &gt;&gt; yeah that that makes sense yeah I'm not sure people look into that on image generation setup but I think it will be very interesting if we can align that to make it better &gt;&gt; I can add I can add a little bit to that as well I think um if we don't have the modatic specific design. If we only have one tower, one single tower for video and uh sorry for image and text then I think is u basically the situation Omar described because the input embedding of text and the unit sampler of image like the VAE would all project to one tower instead of two towers. So that will be like in a single space. here like we do modality specific so it can be considered as two separate space but we find that the modality specific design is um uh like has a better performance than a single tower. &gt;&gt; Okay. Okay. Thank you. &gt;&gt; Oh uh Ahmad &gt;&gt; yeah so I I had a question as well. So in standard uh multimodel models um it's it has been shown in few papers that they are highly biased towards the language model because that is like very highly trained as compared to the vision component. So do you think that if we go towards this uh uh direction where we have every expert separately trained this issue will still occur? Uh bias you mean like a lot of parameters are actually used for modeling language instead of modeling image. &gt;&gt; Yeah. Yeah. And they also use lots of text huristics as compared to focus on vision. &gt;&gt; Oh I see. So here I think we have like separate mod separate um power for different modality data. So it kind of makes sure like different powers specialize in its own modality. um probably this problem will be mitigated but I think um like I mentioned in the talk like the one downside for the standard approach is like even though for example if we just have one set of parameters then both modality data has to go through all of them so basically um even for the image for the image tokens maybe the parameters responsible for for image token only a is only small set but they still need to go through the entire parameters but here if we know okay we don't need that many parameters for image component. We can make it much smaller compared with the language model. Uh &gt;&gt; yeah, makes sense. Makes sense. &gt;&gt; Yeah. And thanks for the question. Uh I think we're almost the time and thanks everyone for um all the great questions. &gt;&gt; Thank you so much. &gt;&gt; And thanks so much for the invite. &gt;&gt; Yeah, thank you so much. Thank you. [music] Heat. Heat. &gt;&gt; [music]
