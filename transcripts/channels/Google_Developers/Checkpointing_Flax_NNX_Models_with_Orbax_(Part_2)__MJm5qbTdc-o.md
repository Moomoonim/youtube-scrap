# Checkpointing Flax NNX Models with Orbax (Part 2)

- 영상 링크: https://www.youtube.com/watch?v=MJm5qbTdc-o
- 채널: Google Developers
- 업로드일: 2025-12-04
- 자막 언어: ko
- 단어 수: 약 1137개

---

## 스크립트

[음악] 안녕하세요, 저희가 돌아왔습니다. 저는 로버트 크로우입니다. Orbax를 사용한 체크포인팅에 대한 2부작 시리즈의 2부에서는 지금까지 배운 내용을 바탕으로 옵티 마이저 상태 저장 및 분산 샤딩 모델 처리와 같은 더 복잡하지만 필수적인 주제를 다룹니다. 자, 시작해 볼까요? 지금까지는 모델 매개변수에 초점을 맞췄 지만, Atom이나 SGD와 같은 확률적 경사 하강법을 사용하는 옵티마이저를 훈련하는 동안 옵티마이저 자체도 상태를 유지합니다. 모멘텀 벡터, 적응형 학습률 또는 현재 걸음 수 등을 생각해 보세요. Flaxx는 모델, 옵티마이저 정의 및 옵티마이저 상태를 하나로 묶는 편리한 nx.optimizer 옵티마이저 클래스를 제공합니다. 옵탁스에 대해서는 추후 토론에서 자세히 다루겠습니다. 중요한 점은 NX 옵티마이저가 우리 모델과 마찬가지로 NX.variables를 사용하여 내부 상태를 관리한다는 것입니다 . 즉, NNX 상태 최적화 도구를 사용하여 상태를 추출 하고 동일한 Orbax 메커니즘을 사용하여 저장할 수 있습니다. 일반적으로 학습을 재개할 때 일관성을 유지하려면 모델 매개변수와 옵티마이저 상태를 동시에 저장하는 것이 좋습니다. Orbex는 모델 매개변수와 옵티마이저 상태와 같은 여러 항목을 함께 저장하기 위해 OCP.orgs.composite를 제공합니다. 이 기능을 사용하면 여러 저장 항목을 서로 다른 이름으로 하나의 체크포인트 디렉터리에 묶어서 관리할 수 있습니다. 먼저 모델의 매개변수를 추출합니다. 배치 통계와 같은 다른 모델 상태를 별도로 저장할 필요가 없는 경우, nx.split을 필터 nx 매개변수와 함께 사용하여 매개변수만 가져옵니다 . 둘째, nx.state를 사용하여 옵티마이저 객체의 전체 상태를 추출합니다. 이것들을 어떻게 구할지 알아볼게요. 먼저 디렉토리를 설정한 다음 OCP.orgs.composite를 사용하여 모델 매개변수와 옵티마이저 상태와 같은 여러 항목을 함께 저장합니다. 그런 다음 nx.split을 사용하여 모델의 매개변수를 추출합니다. 이때 ninx pram에 필터를 적용하여 배치 통계와 같은 다른 모델 상태를 별도로 저장할 필요가 없는 경우 매개변수만 추출합니다. 다음으로 NX.state를 사용하여 옵티마이저 객체의 전체 상태를 추출합니다. 현재 단계는 일반적으로 NNX 최적화 프로그램 자체에 저장되어 있습니다. 그런 다음 저장 항목을 나타내는 딕셔너리를 생성합니다. 이 딕셔너리의 키는 원하는 매개변수 이름과 이 경우 최적화 프로그램의 이름이며, 값은 해당 최적화 프로그램 저장 인수입니다. 어, ocp.orgs.standards 표준은 상태 파이 트리를 감싸는 것을 저장합니다 . 우리는 manager.save 호출 내에서 이 딕셔너리를 압축 해제된 ocp.orgs.composite에 전달합니다 . 이는 Orbach에게 지정된 단계에 대한 체크포인트 디렉터리 내에 param 상태를 params라는 이름으로 저장하고, 옵티마이저 상태 py 트리를 optimizer라는 이름으로 저장하도록 지시합니다 . 우리는 기다렸다가 평소처럼 문을 닫습니다. 복합 체크포인트를 복원하는 것은 프로 저장 프로세스와 유사하지만 추상적인 구조를 사용합니다. 먼저 모델과 최적화 도구 모두의 추상적인 버전이 필요합니다 . 우리는 ninx.evalshape를 사용하여 optex 최적화 프로그램의 유형과 같은 구조가 이러한 추상 객체 에서 저장된 내용과 일치하는지 확인합니다 . 이 경우, 복원 abs 매개변수 상태 및 abs 최적화기 상태를 위한 템플릿 역할을 할 추상 모델과 추상 상태 pi 트리로부터 필요한 그래프 defaf를 얻습니다 . 먼저 모델과 옵티마이저의 추상화된 버전을 얻기 위한 헬퍼 함수를 ​​만듭니다 . 저희는 모델과 옵티마이저 모두에 nx.eval 형태를 사용합니다. 그리고 이러한 추상적인 객체들로부터 그래프 defaf와 추상 상태 pi 트리를 얻게 되는데, 이것들은 복원을 위한 템플릿 역할을 할 것입니다. 즉, ABS 파라미터 상태와 ABS 최적화 프로그램 상태입니다. 우리는 최신 단계를 찾은 다음 저장과 유사하게 OCP.org의 복합 항목에 대한 사전을 생성하지만 이번에는 추상 상태 템플릿을 대상으로 OCP 인자 표준 복원을 사용합니다. manager.restoretore를 호출하면 로드된 매개변수와 옵티마이저 상태 pi 트리를 포함하는 복원된 항목의 딕셔너리가 반환됩니다. 마지막으로, 우리는 모델과 최적화 도구의 새로운 구체적인 인스턴스를 생성합니다. 가장 중요한 것은, 그 다음 nx.update를 사용하여 복원된 항목 사전에서 복원된 데이터로 이러한 새 인스턴스를 채운다는 것입니다 . 이제 모델과 최적화 도구 모두 저장된 상태로 복원되었습니다. 최신 대규모 학습은 거의 항상 수천 대에 달하는 여러 가속기에 연산 및 데이터를 분산시키는 방식으로 이루어집니다. Jax는 SPMD(단일 프로그램, 다중 데이터)라는 패러다임을 사용하여 이를 처리합니다. SPMD는 동일한 프로그램이 서로 다른 데이터 덩어리를 처리하는 여러 장치에서 실행되는 방식입니다. 우리는 장치들의 논리적 메쉬를 정의하고, 파티션 사양을 사용하여 이 메쉬 전체에 걸쳐 어레이를 어떻게 분할 또는 샤딩할지 지정합니다. Flex는 이와 잘 통합됩니다 . 모델을 정의할 때 이러한 차트 작성 지침을 메타데이터로 NNX 변수에 직접 첨부하여 JAX에 각 매개변수 또는 상태를 어떻게 분배해야 하는지 알려줄 수 있습니다. Orbax는 이러한 분할된 JAX 배열을 처리하도록 설계되었습니다 . 저장할 때, 분포를 이해하고 개별 항목을 효율적으로 저장합니다. 하지만 단순히 조각들을 보존하는 것만으로는 복원이 충분하지 않을 때도 있습니다. 토폴로지가 변경된 경우, Orbex는 복구 시점에 사용 가능한 장치에서 구성 요소를 다시 조합하는 방법을 알아야 합니다 . 변경 사항이 없고 표준 복원 방식을 사용하는 경우, 샤딩을 지정하지 않고도 체크포인트를 복원할 수 있습니다 . 하지만 복원 시에는 항상 샤딩을 지정하는 것이 좋습니다. 반면에 Pyree 복원 기능을 사용하는 경우, OCP 체크포인트 유틸리티의 복원 인수 생성 기능을 사용하여 복원 인수를 생성해야 합니다. 분할된 상태를 저장하는 방법은 기본적인 경우와 매우 유사합니다. 일반적으로 Jack이 디바이스 메시 컨텍스트 내에서 수행하는 작업의 결과로 생성되는 상태에는 실제 분산 배열이 포함됩니다. Orbax는 내부적으로 각 어레이 샤드를 저장하는 방법을 결정합니다 . 배열 트리 구조만 전달하면 됩니다 . 분할된 데이터를 복원하는 것이 가장 까다로운 부분입니다. 추상 상태 템플릿이 필요하지만, 그 템플릿에는 대상 샤딩 정보가 포함되어 있어야 합니다. 일반적으로 이를 위해서는 먼저 nx.evvel 형태의 추상 모델을 생성해야 합니다 . 둘째, 이를 분할하여 단순한 추상 상태를 얻습니다. 셋째, nx.get partition spec을 사용하여 원하는 샤딩 사양을 추출할 수 있습니다. 넷째, 이러한 사양을 추상 상태에 적용하면 일반적으로 jack.jit 함수와 mesh 컨텍스트 내에서 jacks.laxwithsharding 제약 조건을 사용하게 됩니다. 이는 형태 dtype 구조가 샤딩도 인코딩하는 추상적인 상태를 생성합니다 . 이렇게 샤딩을 인식하는 추상 상태는 orbbacks의 restore 함수로 직접 전달되며, restore 함수는 제공된 차트 정보를 사용하여 기기에서 분산 배열을 올바르게 재구성합니다. 마지막으로 복원된 상태를 모델에 다시 병합합니다. 코드는 다음과 같을 수 있습니다. 대상 샤딩 정보를 포함하는 추상 상태 템플릿을 가져오는 헬퍼 함수를 ​​만들겠습니다. nx.eval의 형태를 사용하겠습니다. 우리는 이를 분할하여 단순한 추상 상태를 얻고 샤딩 사양을 추출할 것입니다. nx.getartition 스펙을 사용하면 될 것 같습니다 . 우리는 jacks.lax.wischarting 제약 조건을 사용하여 이러한 것들을 추상 상태 리프에 적용할 것입니다 . 이는 형태 dtype 구조가 단락도 인코딩하는 추상적인 상태를 생성합니다. 마지막으로 복원된 상태를 모델에 다시 병합하겠습니다. Orbax는 핵심 워크플로우 외에도 효율성과 안정성을 위한 다양한 기능을 제공합니다. 저장 작업이 백그라운드 스레드에서 수행되는 비동기 체크포인트 기능을 활성화하면 메인 학습 루프가 차단되는 것을 방지할 수 있습니다. 나중에 완료될 때까지 기다리는 것을 잊지 마세요. 체크포인트 관리자는 원자적 저장을 보장합니다. 즉, 체크포인트 상태 디렉터리는 모든 파일이 기록된 후에만 최종 확정되므로 상태 손상을 방지합니다 . 또한 복합 체크포인트 내의 JSON 저장 기능을 사용하여 데이터 세트 반복자와 같은 사전 준비되지 않은 데이터를 모델 상태와 함께 JSON 형식으로 저장할 수 있습니다 . 마지막으로, 내부적으로 Orbex는 특히 클라우드 스토리지의 경우 대규모 분산 배열의 읽기 및 쓰기를 최적화하기 위해 Tensor Store를 자주 사용합니다. 결론적으로 Flax NX는 Jack의 모델 정의에 친숙한 객체 지향적 느낌을 제공합니다 . Orbex는 이러한 모델에서 추출한 상태와 X 상태 조각을 체크포인트하는 데 필요한 강력한 도구를 제공합니다 . 핵심 워크플로를 기억하세요. 모델을 분할하여 저장할 상태를 얻습니다. ninx.evalshape를 사용하여 복원을 위한 추상 템플릿을 생성한 다음 병합 또는 업데이트하십시오. 체크포인트 관리자는 일반적인 학습 루프에 유용하며, 복합 인수를 사용하면 여러 조각을 한꺼번에 저장할 수 있습니다. 분산 학습의 경우, 샤딩이 올바르게 처리되도록 Orbax 유틸리티 헬퍼를 올바른 실시간 또는 추상 상태 및 메시 컨텍스트와 함께 사용하는 데 특히 주의해야 합니다. 이러한 도구를 사용하면 코딩 연습, 빠른 참조 문서 및 슬라이드를 통해 Flax 및 Anxi 스택을 안정적으로 저장하고 다시 시작할 수 있습니다. Learning Jack 시리즈 전체를 시청하려면 YouTube 재생 목록을 확인하세요. Jax 팬들이 디스코드에서 점점 늘어나고 있습니다. 여기 초대 링크가 있습니다. 다음은 Jax Flax 및 Jax AAI 스택에 대한 문서 링크입니다. 그리고 앞으로 방영될 에피소드에서는 Jax AI 스택의 나머지 부분에 대해 더 자세히 다룰 예정입니다. 그러니 계속 시청해 주시고, 좋아요 와 구독 버튼도 잊지 말고 눌러주세요. 곧 다시 만나요! [음악]
