# Vision-Language Models: A New Architecture for Embedding Models | Jina AI | Michael Günther

- 영상 링크: https://www.youtube.com/watch?v=gV9x7HQw7DQ
- 채널: Qdrant
- 업로드일: 2025-10-28
- 자막 언어: en
- 단어 수: 약 2656개

---

## 스크립트

Thank you. Welcome. Um I will talk about uh vision language models and what makes them interesting for training embedding models and specifically uh how we trained our most recent embedding model Gina uh embeddings version 4. Um okay. Uh so just uh I'm I assume that you're all a bit familiar with embedding models at least. Um I want to start with a short introduction about like how embedding models are typically trained. So the typical architecture of an embedding model is you have some language models like um in the early days people use bird now more like sometimes large language models and uh what uh they basically doing is they're getting some text input which is tokenized and then for each uh of these tokens uh those um um those language models basically producing a token embedding representation and then Those token embedding representations are convoluted into a single uh vector representation which is done by some pooling operations which is typically mean pooling or uh last token pooling. And uh then we have vector representation and we can do this for um multiple text inputs like two sentences or like a question and an answer. And uh then use the co similarity um to calculate the vector similarity to get quantization uh um get basically a similarity value how related those text values are to each other. And yeah, if you look at training pipelines in uh the last years um basically some we can see that most um uh training pipelines uh uh consists of three steps um three phases of training. So first there's a pre-training which is done to train the actual language model which is bird or as I said a large language model maybe and it's trained on plain text. So here usually you do some mask language uh modeling or you get do a next token prediction and then you have your pre-trained language model and then the actual model is converted into embedding model by uh usually doing some small architectural change like adding this pooling operation and here um then pair wise data is used to train a model. So that means that uh you have some text which are related to each other and the model is trained to assign them to uh similar embedding representations and all data which is not paired to dissimilar embedding representations. And then in the last phase um this model is um then trained towards specific tasks to uh by doing some task specific fine-tuning for example to optimize the model to perform better specifically for retrieval or for clustering or for classification tasks or also some uh domain specific um uh applications. Yeah. And now vision language models um like instead of using a traditional model you can also use um vision language models. And the interesting thing about this is that it combines um some vision encoder um often taken from a clip model um if you notice and uh of course a large language model into one model which is then trained together. Um like if you look a little bit more in detail it works like that. So the vision encoder is basically taking images and um um dividing them into patches and then uh producing for each of these patch some output embedding representation and then these are feed into the large language model together with also um input embedding representations for the tokens. So at the end we have one uh language model which is processing both um embeddings for tokens and embeddings for image patches. And um what we specifically use here was this QN 2.5 uh V uh VL model. And what's good about this, it's of course can process images as well as uh text. It's multilingual. It's very flexible regarding the resolution. So you can actually encode images with more than 10 megapixels. And it's trained on a very diverse set of um like image types. So it's trained with pictures, illustrations, but also like screenshots of documents and things like that. And on a diverse set of tasks, for example, for question answering tasks or for um like um answering questions about images and for OCR related tasks and things like that. Um yeah to um after we taking this model basically in the second phase we also did some modifications to um train it as embedding model and what we basically did is we used um Laura so um parameter efficient fine-tuning for um uh training specific adapters and here we equipped the actual just the language model part with these Laura adapters And another thing uh which uh we did is we wanted to support a dense retrieval and uh late interaction retrieval. So dense retrieval is basically what we have seen before. Um late interaction retrieval um uh is another form of vector search uh where you basically um not have only one output embedding per uh per input but you basically take the actual token embeddings or the image patch embeddings. Um as result uh embeddings which you store in your database and then you can do some matching uh which basically is more accurate on the cost of having more storage like yeah and yeah that's like yeah one interesting aspect about this model and what people generally have started to do with vision language model based embeddings is visual document retrieval. Um so what's here happening is that usually if you have some uh like illustrations um like uh some or pages of PDFs or something like that uh what people have usually done to make them searchable is uh you can uh do some layout detection and um apply some OCR model to extract uh text from uh images of um these document pages and then you can see which based on which layout elements are in this document and which texts correspond to them um you can basically extract text chunks and then you can uh create an embedding for each text chunk. Um however then you lose a lot of information. So for example the image um information cannot be um processed um basically getting lost but also you uh use basically the relations and uh which are um encoded in the layout representation and um so the idea instead is basically to um uh render the PDF pages as images and then you can directly use a VM uh to encode this all at once into an embedding representation. and it performs fairly good if the model is trained for doing this. Yeah, for training such a model, we basically use three kinds of paired data in our second pair training phase. And that's um basically traditionally pairs of texts which are related to each other. Then we have um document pages and some text snippets which could be questions about these documents but also facts uh which are explained in the documents and uh then we have for traditional image search some pictures with some descriptions of uh what's um what's the content of these pictures for the training itself. Um, as I said, we want our model to support dense retrieval and plate interaction retrieval. And we want to support um textto text search as well as text to image search, which leads basically to four combinations. And um so that the model can do all these things well together. We basically have four different contrastive loss functions which are uh using the info NC loss which um as I said basically tries to optimize the model to encode um the related pairs uh into similar embeddings and unrelated pairs in the same batch um into dissimilar embeddings. And yeah in addition to this uh basically um info NC loss for each combination we also have a distillation component. Um the idea here is that the late interaction retrieval is usually um better performant uh in terms of accuracy as the dense retrieval. So we can um basically distill um the scores from the late interaction um to um the dense scores. And this is basically the pair stage of the training. And in the task specific training, we then have like a little bit more specific training. For example, for symmetric retrieval, we use some specific loss functions. And for um uh codes uh related tasks, we also use a little bit more specific training. Yeah. Um then we wanted to benchmark our model and what we were specifically interested as I said is this visual document retrieval. However, um here we notice that the existing benchmarks are a little bit limited. So what's typically used is a benchmark called Vidori. And um this for example is not multilingual. It um only covers um one specific query type which is um natural language questions and um it mostly contains documents which are PDF pages but there are a lot of other vis visual visually rich documents. So um we constructed a new benchmark which supports which contains more languages and and basically um also contains query types which are more keyword based or which are descriptions or um fact for fact retrieval and um also more document types like for example maps or markdown rendered markdown pages and things like that. And um it also covers a lot of domains. Yeah. If you um uh here are some benchmark results of our model. So um I it basically reports some average scores of different benchmarks of our model and um the uh other common um common models. And I categorized this a bit to make it a bit more um understandable in like there are basically these two like our visual uh document retrieval benchmarks and the vidori benchmark um where our model performs pretty good uh giving like basically the best results on uh these tasks uh in the late interaction mode. Then there is as uh the typical image retrieval. Um herefore we used the clip benchmark and there it also performs quite well and on the more text related tasks we can see that um our model there are other models which perform better which are text only uh but uh we still um have a close to state-of-the-art performance. Yeah. Um yeah, one another aspect about um multimodel retrieval in general is that um you might have the use case that you have a text query and you want to retrive some text and some images. However, um uh the typical problem about this is it doesn't really work. Um as we can see here some distribution of coen similarity values that for um specifically clip models like the openi model or which is however actually in general not not specifically good for text retrieval but also the gen clip model which is good for text retrieval and we can see that um the text um the text similarity scores they are textto text scores they are having much higher similarity values than the text to image scores because um they have different modalities and so if you have a text query you basically only get back text results if you do the vector search and no images. So what you can uh with division language models that's um a little bit better so significantly better you still have a tendency to get texts for text but um you don't get um unrelated texts preferred over related images and this is actually the reason behind this is the architecture. So you can see that in the traditional clip models you have a completely separate uh vision encoder and the language model encoder which producing the embeddings. But in the vision language model as we have seen in one of the earlier slides um the um images and the texts are basically passed inputs are passed through a shared encoder like yeah. Um another thing uh we took a look are runtime optimizations which are of course for many um applications in practice very important. Um here we can uh see there are basically two type you basically we focus on quantization and there are two types of quantization. One is you can do model quantization where you basically apply quantization to the model weights. So the objectives here are to uh reduce the memory the model takes or the VRAMm usage if you hosted it on a GPU and that's especially important if you have like large models and you have only like a smaller GPU where you want to host it and it also gives you better usually better inference um uh times by reducing latency and increasing throughput and the other thing you can do is um uh call it vector quantization where which is basically quantization applied only on the outputs. So the inference part is more or less the same um and unaffected but uh you only quantize the output embeddings which reduces um the size of the vectors and thereby the size of the vector database. Um and it also usually increases the uh efficiency of the vector search. So during the search you have less latency and better throughput. Um yeah for model quantization we um use GGUF. So we exported our model into the GGF format which is a binary format uh which is used by many um uh inference engines like uh llama cpp and lama and also supports multiple different quantization schemes. So the weights are quantized into two bits, four bits and so on. What we did here specifically was that GGF doesn't have a good support for uh image inputs. So we built a fork where this is supported and uh the other thing is then just the normal calibration you usually do for quantization. So you have some quantization parameters uh which you have to train for each specific model. And uh if you look at the um this table we can basically see that the model size could be reduced from 7 GB to uh 700 megabytes. And also the peak VRAM usage is much lower like yeah um for vector quantization um you basically yeah as I said um represent uh the um outputs by a lower amount of bits. So each dimension in the binary case has only like um one bit. So it has two states zero and one which then correspond to a logical representation of either minus one or one. And in the four or eight bit quantization um you have more bit codes. So 16 or 500 um 256 uh different bit combinations and therefore each combination can then cover a specific range in a vector space. Um um yeah and uh that's what quantization does. Um you can either apply um vector quantization just after you have trained the model um uh that we call post uh training quantization and um however you can also train the model to produce better quantized weights by basically training integrating the quantization into the training process doing a little bit of fine-tuning with the existing training data and hereby to make that work the problem is usually that the quantization process itself is not differentiable. But here you then can use a skip through estimator which basically um just doing the back propagation you pass the gradients over the quantization uh step to make that work. And yeah what we see here is basically that even post training quantization works quite well and uh with um quantization error training we can even often get a little bit better performance um compared to the original model. Okay. And yeah, that's basically it. Um, if you're interested in the models or want to know more about quantization or the benchmark, you can just make a photo of this this slide and then there all the links. I Yeah, thank you for your attention. [snorts]
