# How to Route AI Agent Workloads Across Models with NVIDIA NeMo Switchyard

- 영상 링크: https://www.youtube.com/watch?v=2Tskr8an1oE
- 채널: NVIDIA Developer
- 업로드일: 2026-08-11
- 자막 언어: en
- 단어 수: 약 1100개

---

## 스크립트

One model is no longer enough for agentic&nbsp; workflows. Today, AI agents need multiple models&nbsp;&nbsp; working together to get stuff done with the right&nbsp; balance of accuracy, efficiency, cost, and control&nbsp;&nbsp; across all tasks. A system of models. Now, a&nbsp; system of models works best when each task reaches&nbsp;&nbsp; the right model. And most teams kind of already&nbsp; do this, right? They assign models to specialized&nbsp;&nbsp; sub-agents when they're building the models. When&nbsp; you're building a workflow out using LLMs, you're&nbsp;&nbsp; going to have the models that you prefer for&nbsp; different tasks. You know, the planner is going to&nbsp;&nbsp; get like a frontier-class model. The search agent&nbsp; might get a specialized open model and maybe some&nbsp;&nbsp; you know very sensitive data accessing model is&nbsp; going to run on prem on a local device. This is a&nbsp;&nbsp; a great idea. It helps. But in real workflows kind&nbsp; of demands are going to shift turn by turn, right?&nbsp;&nbsp; A search agent might hit something a little bit&nbsp; ambiguous. A normal step that's not supposed to&nbsp;&nbsp; be difficult might suddenly need deep reasoning.&nbsp; Static assignments can't really help us with that.&nbsp;&nbsp; That's the difference between assigning a model&nbsp; and then orchestrating models across a workflow.&nbsp;&nbsp; NeMo Switchyard helps agents do exactly that. It&nbsp; routes dynamically. It's going to read the live&nbsp;&nbsp; context, the state of the agent, tool results,&nbsp; progress, difficulty to pick the right model at&nbsp;&nbsp; every step. Under the hood, Switchyard takes a&nbsp; few components. It's going to take a model pool,&nbsp;&nbsp; some signals, a decision policy, more on that in&nbsp; a second, an execution layer, and a feedback loop.&nbsp;&nbsp; Let's take that down step by step. So, first of&nbsp; all, the pool, right? Routing only works if we&nbsp;&nbsp; have a pool of models that are genuinely different&nbsp; from each other. Frontier models for planning and&nbsp;&nbsp; open-ended reasoning, specialized open models for&nbsp; targeted specific work, execution style tasks,&nbsp;&nbsp; and small local models where control is&nbsp; of most importance. Each target carries&nbsp;&nbsp; a capability profile like what it's good at,&nbsp; what it costs, how fast it responds, all of&nbsp;&nbsp; that stuff. And the profile is what the router&nbsp; is actually going to choose between. Secondly,&nbsp;&nbsp; signals. What can the router actually see when it&nbsp; decides? Right? We have the request. So this is&nbsp;&nbsp; like what kind of task is this? How difficult&nbsp; could it be? The conversation itself. This is&nbsp;&nbsp; things like looking back through the turn history,&nbsp; looking at tool results, looking at how far we're,&nbsp;&nbsp; you know, moving down the to-do list. And finally,&nbsp; of course, the models themselves can emit signals,&nbsp;&nbsp; things like confidence that we read directly&nbsp; from a forward pass. Better signals mean better&nbsp;&nbsp; decisions. Some signals are costly to extract. And&nbsp; that trade-off is kind of, you know, the big deal.&nbsp;&nbsp; Next, we're going to talk a little bit about&nbsp; execution. This is the idea that we need some&nbsp;&nbsp; kind of layer to actually execute the request.&nbsp; So, the router is going to make a decision and&nbsp;&nbsp; then that decision needs to be sent somewhere to&nbsp; do work. This is something like in the case of&nbsp;&nbsp; uh NVIDIA NeMo Switchyard, right? We're going to&nbsp; pass that to an LLM gateway or the agent harness,&nbsp;&nbsp; which is then going to send that request to&nbsp; the correct model via its typical API. Lastly,&nbsp;&nbsp; feedback. You know, we need kind of telemetry to&nbsp; understand, you know, are we getting better? Are&nbsp;&nbsp; we doing well? Are we actually saving costs? We&nbsp; can benchmark routers and understand that they&nbsp;&nbsp; should perform better, but without feedback, we're&nbsp; kind of in the dark when we're actually running&nbsp;&nbsp; these in production. Lastly, the thing I didn't&nbsp; talk about is the routing or decision policies.&nbsp;&nbsp; And this is kind of like the whole thing. These&nbsp; are the algorithms, the beating heart of something&nbsp;&nbsp; like NeMo Switchyard which tells the router how to&nbsp; choose between models in a pool based on signals.&nbsp;&nbsp; So let's go through a few examples of them.&nbsp; First of all, we have the LLM classifier. LLM&nbsp;&nbsp; classifier is going to put a tiny classifier in&nbsp; front of the pool of models. It's going to read&nbsp;&nbsp; incoming requests and issue a verdict. Does this&nbsp; need to go to the strong tier? You know, does this&nbsp;&nbsp; need to go to a specialized model? Those kinds of&nbsp; decisions. The idea here is that we can set how,&nbsp;&nbsp; you know, aggressive we want that policy to be&nbsp; with our classifier. So we can kind of have a&nbsp;&nbsp; knob to tune when it comes to how much we want to&nbsp; prefer one model versus the other. In the case of&nbsp;&nbsp; like a strong weak classifier, we might want the&nbsp; model to lean towards strong for use cases where&nbsp;&nbsp; being wrong is costly or lean towards weak for&nbsp; use cases where being wrong is not very costly&nbsp;&nbsp; and efficiency is extremely important. Next,&nbsp; we have cascade routing and it gets its signals&nbsp;&nbsp; kind of for free. In an agent loop, a evidence is&nbsp; already flowing. Tool calls succeeding or failing,&nbsp;&nbsp; retries, progress towards the goal. The cascade&nbsp; reads that evidence and holds most turns on the&nbsp;&nbsp; efficient tier. It only escalates when the signals&nbsp; say the model is struggling. So this is like we're&nbsp;&nbsp; slamming tool calls over and over again. We're&nbsp; we're banging our head against the wall. Hey,&nbsp;&nbsp; go up to the next tier, my guy. This is&nbsp; something like cascade routing. Lastly,&nbsp;&nbsp; we have something like the prefill router.&nbsp; This goes one level deeper and it considers a&nbsp;&nbsp; lot of signals including signals from the model&nbsp; itself. Before the model can generate anything,&nbsp;&nbsp; it has to prefill. So we have to load things up&nbsp; and do a full forward pass over the prompt. That&nbsp;&nbsp; pass is dense with signal token level confidence&nbsp; and internal states. We read it and with a bunch&nbsp;&nbsp; of other factors that we consider like how fast&nbsp; the models are. Again, all of these signals we&nbsp;&nbsp; have uh we can determine with pretty decent&nbsp; confidence whether or not to go to the model&nbsp;&nbsp; that was selected. Now this is a trainable router&nbsp; which means that we have to do some work to make&nbsp;&nbsp; it work. Great. But that work makes it work really&nbsp; great. So finally, let's just recap, right? We had&nbsp;&nbsp; pools, signals, we have the decision policy, we&nbsp; have execution, we have feedback. The most capable&nbsp;&nbsp; AI systems aren't one model anymore. They're many.&nbsp; And routing is how many models become one system&nbsp;&nbsp; with something like NeMo Switchyard. Thanks so&nbsp; much for watching. We'll see you the next time.
