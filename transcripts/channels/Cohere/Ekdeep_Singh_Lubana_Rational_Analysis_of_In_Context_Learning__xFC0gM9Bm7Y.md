# Ekdeep Singh Lubana   Rational Analysis of In Context Learning Elicits a Loss Complexity Tradeoff

- 영상 링크: https://www.youtube.com/watch?v=xFC0gM9Bm7Y
- 채널: Cohere
- 업로드일: 2025-07-10
- 자막 언어: en
- 단어 수: 약 11842개

---

## 스크립트

Cool. Uh so today we're going to be hosting uh Akib Singh Lubana. He's a research fellow at Harvard and he's going to be presenting his paper uh rational analysis of in context learning elicits a loss complexity trait trade-off. Um so before he starts I'll give a quick introduction as to who he is. So EDP is a research fellow in the physics of intelligence program at Harvard University's center for brain science. His main interests uh research areas of interest are AI alignment, the science of deep learning and interpretability. Uh he graduated with a bachelor's degree in electrical and computer engineering from IIT ROI in 2019. Since then he's interned at various industry and academic labs including Bell Labs in Cambridge and Qualcomm AI in Amsterdam. He completed his PhD at University of Michigan in 2024 and has published some really interesting papers that I highly recommend reading in on learning dynamics and interpretability in NERUPS, ICML and I clear. So we're super excited to be hosting him and learning from a true superstar in the field. And without further ado, I'll give the floor to ADE to give an overview of what he'll be sharing today. &gt;&gt; Uh, awesome. Yeah, thank you very much for that very generous introduction. Uh okay. Uh uh are the slides looking? Okay. &gt;&gt; Yeah, looks great. &gt;&gt; Awesome. Uh cool. Okay. So, yeah, thank you so much for having me. Really excited to be here. Uh so what I'm going to present today is actually three papers and uh try to encompass them into like this broad uh sort of series of papers or agenda that we've been pursuing over the last few months uh on trying to understand a very specific phenomenon around incontext learning. So uh just to establish some common ground and a motivation. So incarnate learning is this really cool uh behavior or capability that started to emerge uh around GP3 models. Uh this was in the this is a figure from the Gibbly 3 paper from OpenAI by Brown at all where what they show is that if you simply put a few exemplers exemplifying some behavior some task and just give a model a novel query following that task a model can solve that by just in context inferring what the structure of the task is. uh this is often what you might think of as few short variant of incarnate learning or the few short formulation of incard learning uh there's also what you can think of as the in uh the belief update formulation of incarning where I might have some prior over concepts that exist out there and as in the context I'm being offered uh novel ways that those concepts are being used I can infer novel mappings for those concepts and start to produce uh in accordance with them. So the best example I like over here or the example I find to be the best over here is this uh example about learning in context this language called Kalamak from the Gemini team uh in their in the report on Gemini 1.5 where what they showed was if you simply feed in a grammar book for this language which only has about 100 speakers in the world and the dictionary uh for translation you can find that a model if you give it uh sentences to translate from English to calamang it is pretty accurate at doing uh it's not perfect but uh it is highly accurate. Uh okay so this is encodics learning uh recently we've been in this paradigm of scaling everything that exists out there any control variable about the learning pipeline we can find we want to scale it so people started scaling in context learning basically what you do now is uh in a let's say in a few short and context learning uh formulation you take a task which you wanted a model which you wanted to like see how well a model performs and you just provided a few examplers and then you know the model performs somewhat decently. So this is a state-of-the-art performance that existed uh from some benchmark uh and some method that that was out there on a machine translation task but then you just keep on adding more and more examples and you see that actually the performance just keeps on rising up. So like you really beat state-of-the-art by by just like talking more essentially to the model. Uh and you can do this for other tasks as well. uh this is pretty cool because often people used to think of incarnate learning as a mechanism or a capability that we have that models offer us so that we can elicit uh their behaviors and then hence uh evaluate them on concrete tasks. Now the weird thing that happened uh at least what I found to be a fascinating result was uh this paper many shot breaking that came out from enthropic last year with Gemin but what they show is that for certain behaviors let's say uh over here what what they're plotting is a bunch of decently well fine-tuned safety tuned models uh and they're evaluating whether they engage in harmful behaviors. Now if you truly have a generally intelligent model, I'm not saying these are generally intelligent, but like assume that you had a generally intelligent model. If I just actually in context talk to it and gave it information about a new task, if it is truly intelligent, ideally it should just like figure out what that structure of the task is and just start performing in accordance with that. So even if you have a very well safely fine-tuned model, if in in context I just provided a bunch of examplers about harmful behavior. So what I'm going to for now call harmful. So like violent or hateful speech whether the model engages in violent or hateful speech deception and so on. Uh you find that once you hit some critical context before that the model keeps on refusing the queries but after that it does start to engage in. So this is a percentage of harmful responses what are the uh given 100 queries whether what are the odds that the model will engage in this harmful response. uh this is a cool measure but like what I like to look at more so is this negative log likelihood measure that how likely the model deems these undesirable uh behaviors and what you find is there's this uh so if you look at the llama 2 results you find this really fascinating uh phenomenon that occurs where to begin with the model is deeming these uh responses to be not very likely but then suddenly there's a transition of sorts there's a point where there's a certain break this is more or less around the same point where the uh where we were seeing that the models are starting to engage in these undesirable behaviors and there's a bump and the model starts to find these sentence these these generations very very likely these behaviors very likely. So this specific bump this specific kind of uh phenomenon is a bit of a challenge when it comes to something like evaluating model behaviors. uh currently well if not necessarily anymore but like at least until a few months ago and still for smaller models uh benchmarking them we need them to be able to understand our instructions and often one way that we get them to understand our instructions is by providing in context exemplers of the behavior we want to evaluate them on. So to that end like in context learning is one of our uh standard tools that we use for illicitating model capabilities. But if you have these sudden bumps, if you have these uh sudden uh uh in context learning curves, then there emerges an unreliability of using this tool uh for eliciting model capabilities because as it may have uh it may so happen that you did not offer enough context, just enough context to elicit that capability. Perhaps you needed 32 examples but you only provide 30 examples and hence you could not see you did not see that the model was engaging in that behavior. had you provided one more example that the model would have done that. Uh so what I want to do today is go through a series of papers that we've done uh driven by this question uh on understanding how these certain learning curves are emerging in context. Uh I'm going to call this a learning curve because essentially that's what's happening. You're providing data in context and you are the model is engaging or learning that behavior in context. Uh okay. So on that note uh we will talk through these three papers. Uh the first paper is going to be about creating an abstraction uh creating a possibly one of the most toy scenarios that we can think about with a very specific hypothesis that captures this phenomenon that we're talking about the certain learning in context learning curves. In the second paper, we'll talk about how how to further simplify this task that we're we're we're working with in a simplified setting with with models such that we can provide uh a much more precise behavioral characterization. And in the third paper, we'll talk about a theoretical characterization where as it so happens, we can not only explain all this phenomenology that we'll observe, but also create a highly predictive account where before even you train a transformer, I can tell you uh what kind of a solution the model will will learn and how these incontext learning curves emerge in this uh scenario. Okay, so let's get started. The first paper in context learning of representations. uh these are my co-conspirators on the paper. It was a really fun collaboration with all these first three guys uh who were the co-pass. Okay. So the hypothesis that we started off with here is that if you train a language model on some pre-training data uh on some pre-trained corpus there's a bunch of concepts that are involved in there and the model will eventually learn some prior over these concepts. So something like let's say the word apple. Apple it if I were to ask you what an apple is you might think of the fruit or the company but I could just start calling anything apple and start using that as a novel referent for that concept uh for some novel concept. So like uh when I'm doing that you'll need some data. You'll need to like have certain observations which help you realize that okay we're talking about Apple with some novel semantics and I need to update my prior to think about Apple in a different way. Let's see how the how a model how a language model responds to this. So what I'm going to do is I'm going to define the simple grid over here a simple grid graph and associate to the nodes of this graph specific concepts that I'm pretty sure the model has seen during it pre-raining. So, apple, bird, milk, and so on. I'm going to do a random walk on this graph. So, I go through apple, bird, milk, sand, and so on, and like just keep going through just some random walk. Uh, I'm going to then string this random walk into a sentence and feed it into a pre-trained language model. So, it'll be a llama 8 3.1 model. Um, I don't tell the model that there's a random walk happening on some grid structure. I literally just feed in apple, bird, milk, and so on. And what you see is uh just this very simple task already starts to reproduce the phenomenon we were talking about before. There's a critical amount of context you need to reach. So a critical amount of length of the walk that you need to reach such that before that uh the model is basically behaving randomly. But then once you reach that r once you reach that critical context once you reach that critical length of the walk the model suddenly starts to make next token predictions which are in accordance with this graph structure. So here accuracy it's defined as let's say my walk were to end at opera. I'm going to look at the next token predictions of the valid neighbors. So egg plane and mango I'm going to add up the probabilities and report that over here. So it's a pretty soft measure of performance and it pretty clearly shows that depending on the size of the graph there's a critical context and this context is just increasing as you increase the complexity of the task or the structure of the graph. Okay. So seems like we're able to reproduce this phenomenon very simply using this uh uh the simple task of random walk on a grid. Now what I started off with the model might have some prior over these concepts and hence what's happening possibly what's happening is that in context it has to learn novel semantics about what it means to be an apple. In this task, an apple is just a node of a graph and hence its representation ideally if the model understands uh the semantics of apple its representation would be very similar to let's say the representation of bird, house and milk and less similar to let's say the representation of app of mango which is also a fruit. So according to the pre-training prior they may have similar representations but according to this in context task they should not. So let's see if that's the case. Uh what I'm going to do now is I'm going to uh as I'm going through this random walk, I'm going to compute the model's representations from layer 26 uh of the llama model. Uh so let's say my walk was of length 100. I look and it ends at apple. I compute the representation of apple at at layer 26 and then I take some window behind it. So let's say window of 20 tokens and I pull out the representation for all these nodes that exist in that window. If they do not exist in that window, I might extend the window. But like basically all I want is I need to have at least one representation for each of these facets. Once I have that I just average out all the apple representations I get and just compute a 2D PC of that. So I have representation for apple, bird, car, egg and so on. Uh you get a matrix and do a 2D PC. So I'm going to plot that 2D PC over here. I'm also going to compute what we'll call a ders energy measure. So here uh AIG is the ground truth adjacency of uh uh is the A is the ground truth adjacency matrix and AIJ is reporting whether the IT node and the J node are connected to each other. So let's say apple and bird if we're talking about these two nodes then AI is one but if we're saying apple and car then AI is zero and hil and hjl these are the representations we extract from layer L for node and J. So ideally if the model's representations for node I and J are very similar then this whole product will be very low because the uh there will be a similarity between the ground truth adjacency matrix and the equidian distances. So let's see if this actually works. So we're going through this walk and we're plotting both the energy and the PCAS on the right hand side and you see that when the energy hits its uh uh minimum the PCA starts to exhibit the structure of the graph. So indeed uh what we're finding is the model representations are changing in context. Uh like if for any cognitive scientists in the room uh people often think of uh people often talk about this framework of conceptual role semantics where the idea is that the semantics of a concept are input by the way it is the role that it plays in some set of uh in some other set of concepts. So like here apple is playing the role of a neighbor to concepts of bird, house and milk. So ideally the representation of apple should reflect that and you see that uh apple is very similar over here it it is closest to the concepts of house bird milk uh but like if we look at when we were to if you were to look at this at pre-training uh so basically if I go back a slide uh which I'm unable to apparently uh okay apparently that does not work okay yeah that works uh yeah so if we go back a slide you can see that uh The representations of apple and mango are very similar to begin with. But then as we were going through the walk, the representations start to reflect the structure of the graph. And eventually we arrive at the point where apple and mango. So apple is over here. Mango is flying around over here. They have uh they respect the structure of the graph and have very dissimilar representations. uh and to emphasize this uh whole study, it's very similar to this whole uh line of literature that exists in in neuroscience and cognitive science on cognitive maps where you can actually do a similar experiment with humans uh where you have some visual concepts and you're just doing a random walk. You're just flashing images to a human subject one by one and uh taking fMRI recordings of their brain as they're watching these images and then you simply do a lowdimensional visualization of their brain scans and find that you can extract out the graph structure that you use for performing this random walk from their uh brain scans themselves. Uh you can easily think like how how our task came to be. You can just do this with a language model where rather than showing it the model images, you're showing it words. Uh so rather than showing an image of a banana you say the word banana. Uh so yeah the aim here was in this first paper to show that even a very simple task can uh capture this notion of uh sudden learning in context. So the model has to in context infer a novel set of semantics about some concepts that it may have some existing prior over. Uh now this is still a hypothesis. we are just seeing like substant we saw substantial evidence with like a real model on this task. But let's see if we can make it further more concrete. So uh to that end what I'm going to argue is that if you think about this first task uh this random walk task this is basically just a marovian random walk on some graph structure right. So I could take this to heart and now intentionally design a model which which is meant to perform random walks on some well- definfined state space. So that's where the second paper algorithmic phases of incontext learning comes in. Uh again my co-sponsors it's a really fun collaboration co- first offered with cor. Okay. So in this paper what we're doing is uh as I was saying we have uh I'm going to think about doing a marovian walk on some state space. So I'm going to define end transition matrices which have some prior over them. So uh during training what happens is I uh I'm now going to train a model from scratch. Previously we were not doing that. We were just taking a llama model off the shelf. Now we're training a model from scratch. So I uh based on some prior I just randomly sample a transition matrix. Uh so let's say I sample the nth transition matrix. I sample a sequence from that transition matrix and then I simply train a transformer model via next prediction on it. So basically what I'm trying to do here is I'm trying to simulate or learn a finite mixture of markup chains. At inference time what I can do is uh I can define I can either take a transition matrix T star which was sampled from this seen chains uh seen set of transition matrices or I can just define a novel transition matrix. So I can do an in distribution evaluation or a completely out distribution evaluation and here what's going to happen is that like if you take this transition matrix you just sample a bunch of sequences. So let's say the sequence ends at two and you compute the next token probabilities given this context uh that the model makes uh so the predicted next token probabilities from the model and you know the ground truth next token properties which come from your tar. So what you can do is you can just like take a bunch of these sequences which end at different states take a k between them and just average it out. So that gives you basically an estimate of whether the predicted next token properties which can be thought of as like the predicted transition matrix whether that matches the ground truth transition matrix. So this is going to be our evaluation measure. And now what we see is just to give away the main result the model is actually transitioning between a lot of very different kinds of behaviors. So here uh in this 3D phase diagram that we're plotting uh each color corresponds to a different algorithmic strategy. So what I mean by an algorithmic strategy which are uh for now they're listed out over here. I'll describe in detail what these are. Uh but each strategy has a color associated with it. And it's algorithmic in the sense that I can procedurally write down in Python uh what these strategies are supposed to do. So like given a sequence what a next token prediction according to this algorithmic strategy should be and then I can compare that next token prediction with the model's next token predictions. And what you'll find is that like in these regions where this specific color dominates uh the given strategy that almost perfectly explains the next token predictions that I write on Python versus what my model makes. So each each of these axes which is the amount of training uh so the number of training steps on the z-axis we have the data diversity what I'm going to call data diversity. number of markoff uh uh transition matrices that you see uh during training and an inference time access which is how much context I offer the model to make its next token predictions and what we were seeing before which is that the model as a function of context was making transitions between its behavior. So it uh it transitions from like performing one way to performing another way. For example, it was performing making random predictions to it starts to make predictions according to the grid structure of the graph. uh similar behaviors will will emerge in this task and that's via context but in actuality there's like even other axes which lead to like very algorithmic behaviors and transitions between okay uh also just to mention this task is turns out to be very rich despite the simplicity basically all known results on incarn learning can be simulated in this task so it's a very rich and portal ground for studying in learning okay so I've given a big spiel about what this phase diagram is let's see what these solutions in fact Okay. So if you think of a markup chain, there are two characteristic statistics that define a markup chain. Uh it's unig statistics as in its stationary distribution and its biogram statistics which is the transition matrix that was used to generate the chain in the first place. So what I'm going to do now is uh I'm going to speculate some solutions that I can come up with based upon that. So let's say you have the some transition matrix based upon which you sampled some sequence. Now what you do is uh you compute a histogram of the unique uh tokens that exist. So let's say if the state space is 10 then I compute the unigram distribution of each token in that sequence versus I compute the biogram statistics which is uh the transition probabilities between two random states. Uh and what I can do is once I have the in context input unigram and bagram statistics I can compare them with the ground truth stationary distribution. the ground truth inagram statistics versus the ground truth transition matrices of the chains of the transition matrices that I see during training. So what I'm doing is I'm taking an incontext sequence and I'm comparing it with uh the with the generative process that generates the training data. So let's say uh if I take the histogram for for for a second uh the incontext histogram and I compare it with the stationary distributions of the transition matrices that are used to generate the training data. I can simply compute the distances between these and use that as kind of like a likelihood that how likely these uh this specific transition matrix is to generate this sequence with the given specific uh set of unigram distribution and then use that distance uh as a likelihood function. I have some prior over these which I can assume my model has learned and then use that to define essentially a basian averaging solution. So you can compute a posterior probability and then use that to define a posterior predictive. That's essentially what we're going to do over here. So this first solution is what we'll call unigs based retrieval. So I have the unig statistics uh of the sequence. I'm comparing them to the unig statistics of my transition matrices seen during training and whichever transition matrix I'm closest to basically I do use that distance as a weighted average to perform uh to perform weighted average and get a retrieval based solution. So you're interpolating between the transition matrices seen during training based on unagram statistics based on distance and unagram statistics. You can similarly do it based on diagram statistics. So that's what we'll call a bagram based retrieval solution. Meanwhile, you can also have a situation possibly where the model has just truly learned the generative process of a markup chain, which is that it understands that given a a sequence, all I need to do is I need to infer the transition matrix and need to make next token properties according to that transition matrix. So, I have no dependence on the training data anymore. I can genuinely simulate any markup chain. And if that's the case, we'll we'll call it a biogram based inference approach. Or just for for now completeness, we'll call it unagram based inference if it uses unagram statistics. Uh this is possibly the the weirdest solution. Unig based inference. One might argue why would a model make next prediction according to unagrams and we'll see there's a pretty good reason why it does that and it will do that. Okay. So um these are the four solutions that we had. So previously what I was showing that 3D face diagram it corresponds to these four solutions. And what we can already speculate is that uh technically background based retrieval is the best thing you can do. It's the base optimal thing to do if you were to get as low an error possible on the training data. So in distribution background based retrieval will generalize really really well. But outer distribution it it just cannot it cannot go out of distribution. It can only interpolate between the transition matrices in the training. Meanwhile, the background based inference approach which understands the generative process of markup chains. It does not care distinguish between in distribution and out of distribution. So it should generalize equally well and similarly you can make statements about the other or predict uh make predictions about other solutions. So let's see if this comes to pass. So uh to check whether a model is using unagrams versus biograms uh we're going to define this test that we call bagram utilization. Uh so here we make uh we build on the argument that technically if you have a sequence and you just shuffle the sequence the unigram statistics of that sequence remain the same because unigrams are order and variant but uh the biogram statistics can can be complete will completely be obliterated. So what I can do is I can just like offer a model a sequence make next token prediction shuffle the sequence again make next token predictions and just compute the k between them. If the KL is zero then what that means is the model only cares about unagram statistics versus if the KL is non zero then at the very least the model does model is trying to capture statistics beyond unagrams. Uh we'll see eventually that it is indeed capturing by statistics. Okay. So here is what the results look like for this uh test. Uh on the x axis we have the amount of training. On the y axis we have the data diversity. What you see is that as a function of training there's this like critical amount of training you need to reach where suddenly the model starts to use statistics. Uh again I'll clarify that it is indeed using bagram statist but like for now it's it goes from unigs to not unigram statistics which will be and what's happening here is this vertical line this corresponds to the model learning and induction. Now if you need to compute diagram statistics uh if you need to like make in context a statistics perform in context statistics computation uh you just need an induction head you need to be able to see which token comes after which token which is the role of an induction head. So that's why this is this vertical line exists. Uh there's a critical amount of training you need to go through. It's an optimization bottleneck phenomenon and hence until you have reached this stage the model will have to rely on unicorn statistics. Uh the second thing we'll do is we'll compute this experiment. Uh we'll perform this experiment that we call proximity to retrieval. So I have the predicted transition matrix. The transition matrix that I predict after offering a model a bunch of sequences from some transition matrix. And then I can check whether the next token probabilities they are for a novel transition matrix that the model never saw during training whether it is close to a batch of seen transition matrices. So as the set of transition matrices that the model saw during training versus a randomly defined set a novel set of transition matrices that the model never saw during training. Now if the distance between these two set of transition matrices is similar then that means the model does not have a bias towards performing versus for performing in next making next token predictions in accordance with the transition matrices that were seen. It can easily uh it can easily perform in accordance with it is more or less performing in accordance with the unseen transition matrices as well. Meanwhile, if the distance was uh closer to the seen set of transition matrices, then what that would mean is the model has a bias towards performing in accordance with the scene set of transition matrices. Uh so that's what we see over here in this test as a function of training. If the data diversity is low, if the number of transition matrices you see during training are low, you see that the model uh has a preference towards performing a retrieval-l like approach. So its distance to transseen transition matrices of the predicted transition matrix is lower compared to an unseen batch of transition matrices. Uh it doesn't exactly matter how this thing is computed but like we rescale it to 0 to one so that we can uh easily see whether it's closer to retrieval or whether it's closer to inference. Uh but yeah as you increase data diversity as you have too many transition matrices to memorize you see that the model eventually starts to perform closer to an inference based approach. with stars who uh have the ability to generate uh in make a prediction according to a normal transition matrix eventually. Um so another thing worth emphasizing here is that as you if you look at any given set of uh transition matrices so that there's something like 2 to the^ 6 as a function of training there is kind of like this frontier that exists and this frontier what's happening over here is that like eventually the model will still memorize even though it starts off with a preference towards like performing like inference eventually it will still memorize the relevant transition matrices and start to perform like uh like a retrieval based approach. So even though it may seem like if you just simply increase the data diversity the model can will perform like inference uh maybe it's a capacity constraint or something in actuality what happens is that like the frontier does not exactly have a uh an exhausting trend like it's not saturating yet. So there's a possibility maybe this just keeps on going if you were to train further and uh we'll see how this whole thing transpires in a few slides but just want to flag this for now. Okay. So we have the bagram test statistics we have the proximity to retrieval which is saying whether the model is performing retrieval or inference. Now what you can do is you can simply do a cartisian product of this the in context statistics of your model whether you have a reliance on training data versus not and you get these four solutions that we were talking about uh uni retrieval by retrieval uh uni inference by inference uh these are still speculative so we're saying that this is the region where the model will perform closest to based inference this is the region where unagramram based inference approach will best explain model behavior and so on and when you actually programmatically write down these algorithms in Python. You see that indeed this is true. So here I'm reporting the KL between the next token predictions according to these algorithms versus the actual model that we train for a given amount of time/data diversity. And you see that indeed the next token predictions according to these algorithms versus a transformer is almost approaching zero. Uh the KL is approximately approaching zero uh in the regions that we speculated these uh these algorithms to explain model behavior. So we have a pretty solid match between our predictions and what the model actually does. Uh on that note, I quickly want to emphasize that like often when you read papers about incontext learning which try to explain how incontext learning works, I I would argue that those papers are possibly confounded by which phase or which algorithmic behavior you're in. Like it's possible that uh early on we used to think in context learning let's say around 2022 or something we used to think in context learning is like simply task retrieval and that's indeed true if you let's say train too much or if your data diversity is too low but if you just train uh if you just train on a very diverse data set uh for not necessarily the longest amount of time uh let's say in like an online fashion you probably will get something like an inferencebased approach and hence your model's phenomenology it's behaving will be very different. Uh so that might help us reconcile some of the existing results that are out there on a constant. Okay. So just to summarize what we did in this paper, we started off with this idea that model behavior is changing by ch scaling up context. Then we realized actually it's not just scaling up context. Even scaling up the training parameters uh the training variables is uh changing model behavior often very very suddenly. So we saw what these behaviors are. We defined algorithmically what and procedurally what these behaviors are. We saw when these behaviors emerge. So specifically what regions best explain the model behavior by these algorithms. Uh in the papers we also have how these algorithms are implemented. So let's say for based infrance you need an induction head. So that's where uh we have a characterization of like how this very vertical line transition emerges. And in retrieval based approach we actually find that we can literally reconstruct the transition matrices from the weights of the MLPS in a transformer. So you can uh actually show the model has memorized the transition matrices uh for performing like a retrieval based approach. Uh and we're not the first ones to be doing such a thing. Uh we have this arguably this rich phase diagram is uh we were the first ones to show it but people in the past have taken like similar tasks. So for example there was this task on in context linear regression where you just offer the model some in context uh in context offer a bunch of uh input output queries. So some input vector it's linear regression mapping to some uh scalar and have the model in context infer uh a novel uh linear regression mapping and what you see is that as a function of data diversity or as a function of training time the model behavior changes. uh so here uh DMMSE is like one behavior these authors also are all defined versus rigression which is kind of like the base optimal thing you can do for performing linear regression similarly there's another set of tasks called incontext classification that has been also very popular and similar results have been shown on incontext classification as well so all these papers what we're all of us are together coming along to show here is that depending on your training conditions and your inference conditions broadly depending on on your experimental conditions. Different algorithmic behaviors explain model behavior. Different algorithmic strategies explain model behavior. What is unclear is why this is happening in the first place. This is arguably the question we started off with. We started off with the fact that as you're scaling up the context, model behavior changes very drastically. And what we've shown at this point is that like very simplified, very controlled setups can very nicely replicate that behavior. So going back to the question uh why is this happening? Let's see if we can explain that. So this is where the third paper comes in. Uh in context learning strategies emerge rationally. This was uh a really fun collaboration with Daniel Noah and Gotham. Uh Daniel is this young prodigy of a cognitive scientist. So if you're looking to recruit somebody, recruit him. Okay. So here uh what we're going to do is we're going to take a step back and think about what were these tasks that we were characterizing in context running on arguably. So if you think about the markup chain task I was saying we're trying to learn a finite mixture of markup chains a finite mixture of transition matrices with similar markup chains. So you could do the same thing uh for the incontest linear regression task for example like where what people have done is they just have a bunch of linear regression task and you're just like trying to learn that finite mixture of linear regression task. So here I would argue that like a general formal formalism is starts to kind of like emerge which is all in context learning papers in the past what what we've been doing is we've been taking a bunch of tasks where I'm going to define a task as like some functional which takes in some parameters w uh these parameters are sampled from some true data distribution. So there's some true prior over the parameter space. You sample a set of par parameter vector from that and just feed it into some functional f of w to define a function. And this function is what we'll call a task. Uh I define a prior over these individual functions. And we'll call this basically our training distribution which is uh which is essentially like a a a distribution which has a discrete prior over these d functions. And uh when you sample from this uh training distribution, you sample first a function and then you sample a sequence from that function as shown over here. So as I was saying before, you can map on previously existing tasks. So linear regression and classification uh which are relatively popularly studied task in this language. Uh and you can also simplify uh so in in context linear regression and in context classification people have shown that there's only two kinds of uh algorithmic strategies which explain model behavior uh just to have a coherent uh set of results with those uh papers. We're also going to simplify our markup chain setting and call it now balls and nerds. So basically what's happening in this setting is we have a mixture of earns or bins. This kind of like a standard probability theory question where you have some ball uh some some earned with a bunch of different types of balls in it and I'm going to sample a ball one by one with replacement and show it to you. I get a sequence of these balls uh you as an observer I get a sequence of these balls and I have to predict on the next trial when a next ball is sampled what is the probability that that ball will be of a certain type. uh you can basically see that like the optimal thing to do here is just keep track of the frequency at which a ball is shown ball is seen in context in that obs in that sequence that you've observed and make a prediction according to that. So it's just a frequency computation or unigram statistic computation task. Uh now as we were saying there's general formalism here. So for all these tasks that we've seen in the past uh you can think of them as just trying to learn a finite mixture of some tasks. So to begin with uh what we did with the markup chain paper chains paper we simply speculated what are optimal solutions that a model might learn if it is trying to simulate these markup chains. Let's see therefore what are optimal solutions you might learn if you're just simply trying to learn a finite mixture of tasks which themselves have a shared structure in the sense that they are the same functional whose parameters are sampled from some true distribution. So there's two distributions we're talking about a t a tow train and a tow group. So what I would argue is that if you sample a sequence from a given function in this mixture of tasks uh one optimal thing uh optimal solution that exists which will be the optimal for the dist the training distribution the tow train is you define a posterior predictive over drain. So you assume there's a discrete prior that there's only these D tasks that I care about and given that sequence given the incontinent sequence I'm going to compute the likelihood that a given one of these tasks generated that sequence once I have the likelihood and I have some prior over these tasks a discrete prior over these tasks I simply do a product and that tells me uh that allows me to do a weighted average in accordance with the prior and the likelihood to compute next to probabilities according to the posterior probability which will be just the product of the prior that I have on this task and the likelihood that one of these tasks generated the sequence. I'm going to call this a memorizing predictor. I call it a memorizing predictor because you need to know precisely what these tasks were. Uh so you have a very specific reliance on your training data. The the task you s meanwhile I'm going to also argue that technically because of the shared structure it it is plausible that the model actually just suddenly starts to to know what uh starts to input what the true data distribution here is, what the true generative process is. And in that case the model will just have a prior which is continuous in nature. So it it just has a prior u that matches the true prior from which the parameters are sampled. It knows the generative process of the function itself and just uh given some incontext sequence it just tries to see from the possible support of this distribution which parameters best explain the incontext sequence. So we'll call this a generalizing predictor. This predictor can generalize to any novel sequence for an any novel task. So there is no reliance on training data. You just actually have learned the generative process. What you can do is you can take these two predictors uh these two general formulations and show that for all the three tasks that we were talking about before you can write down closed form solutions for them. So for example for in context linear regression uh the two solutions that people have talked about before what's called the DMSSE or the regression solution you can show that actually they are just specific instantiations of these two general predictors. So for example, ridge regression is just the optimal base optimal general thing to do given a novel task and the memorizing predictor is uh if you just need to perform b optimally on the training distribution uh and we can do the same thing for other tasks as well. So we have this uh similar to the mark of change paper where we had like speculated solutions we have that now for these tasks. Uh now what I'm going to do is I'm going to simply take my model train on train it on these uh take a transformer model train it from scratch on these uh mixtures of task and see what its next token predictions are. I'm going to look at the next token predictions I make uh using my algorithmically defined generalizing and memorizing predictors and just compute a relative distance between them. So like is the model closer to performing like the generalizing predictor or is it closer to performing like the memorizing predictor. I'm going to add a half here so that it uh uh I'm going to rescale this so that it's between zero and one. But this current measure goes between minus one. Okay. So this is what the results look like as a function of training time and as a function of task diversity. You see that the model starts off with for low diversity to perform something like a memorizing predictor. Uh so it has a pretty strong reliance on the training data. But as you keep on increasing the diversity uh if the model just cannot memorize enough task fast enough you see that the model starts to perform more like a generalizing predictor. So what I'm plotting here is a relative distance and this is what the numbers look like or the scale of the numbers. If you take any specific value of the diversity so let's say 2 to the^ 6 over here and you just keep on training further you see what we were seeing before in the markup setting. There's kind of like what people call transients where a model has the ability to begin with to to generalize. It can perform very well out of distribution but then it suddenly starts to have a reliance on the training data and it can only perform well in distribution. And similarly if you take only a given value of training uh training steps you see that as you keep on going above uh if you keep on increasing task diversity uh the model starts to uh have the tendency to behave like a generalized predictor on the marginalss we're reporting the absolute distances. So like it's not just that the relative distance is going down the model is truly starting to behave like the distance absolute distance starts to go to zero as a function of training or as a function of data diversity. Okay, so this was for balls and earns for all three settings. You see this phenomenology uh start to hold. The model is transitioning between these two behaviors as a function of the two controls we're talking about task diversity and training steps. So this what this shows us is that the model really just wants to perform well according to the two distributions that define the generative process. The training distribution or the true distribution. The generalizing pred the memorizing predictor or the generalizing predictor. This for uh cognitive scientists is very provocative or evocative of the notion of rational analysis. Basically, if you have a learner who is just trying to perform optimally on the distribution that is offered to it, often uh John Anderson back in 1990 argued that like one way to explain the learner's behavior then is to assume that it is trying to perform optimally adapt to the data under its computational constraints. We're going to take this approach to see if we can like make a predictive account for what's happening. So as I was saying the model is trying to learn these two predictors uh generalizing predictor which performs very well out of distribution memorizing predictor which performs very well in distribution. What I'm going to do now is uh I'll argue that basin inferences tells us that if this is your hypothesis space if these are the only two behaviors you'll ever uh ever be perform like what you can do is that as a function of observation as a function of your training data you can rewe these hypotheses optimally. So this is just a posterior predictive according to that. But now what rational analysis says is that we need to model the computational constraints. We need to model if I can in fact even implement these behaviors. So to that end we're going to talk about two standard computational constraints that neural networks exhibit. Uh basically what we want to do is we want to compute this posterior probability probability of some predictor f given the training data. So one uh standard computational constraint that neural networks exhibit is scaling laws. they uh the rate at which the loss reduces the rate at which the evidence or the rate at which I accumulate evidence or I absorb the data to make next predictions uh follows a power loss scaling. Another is simplicity bias. Neural networks often have a bias towards performing uh towards uh learning simpler solutions first. So this form over here this is simply saying the uh or the form of universal prior algorithmic information theory which is saying the model have a has a preference towards learning simpler solutions where the simplicity or the complexity is defined as k of f the colograph complexity of that predictor s. Now colograph complexity is not actually something you can compute. So what we'll do is we'll literally just write down the python files for the different predictors that we were talking about before and just run them through a compression algorithm. Uh this sounds insane but you'll see that this works. Uh so the alpha beta gamma here are free parameters. We need to do some printing for that and uh that will be explained in a second but yeah so what we want to do is we have these two predictors we're computing the posterior priorities over them and we want to compare which uh which specific predictor the model behaves according to so we have the what we want to really compute is the log posterior odds whether I prefer to perform like the memorizing predator or the generalizing predictor. So this come divides down into this factor into the prior odds or the likelihood odds like how well a given predictor explains the training data. And if you take those assumptions I was talking about before do some math it turns out you can write down this uh you can write down a close form for this uh where basically the difference in the loss of the predictors and the difference in their complexity dictates which solution a model will learn. Uh so let's see uh so if you have that uh log post procedure or you can turn it now into a predictor. So you can simply run it through a sigmoid and that will give you the posterior probability of the post predictor the memorizing predictor versus one minus sigmoid which will be the generalizing predictor's posterior property. So this just now gives us what I was talking about before in the base optimal way of reweating these hypothesis. we now have uh if if this uh this theory holds then we now have a close form solution to how a model a trained model's behavior should look like and that is indeed what happens. So here in this plot over here we're showing the transformers relative distance. what we were plotting before and you can see uh our equation over here which is defined in a closed form way and there is no reliance on any uh any transformer parameters there like none of this has any parameters in there. Uh we find that we can actually perfectly predict the model behavior uh using just this equation itself. Uh so we see these nice contours emerging in this setting itself and the same holds for other settings too. We can use this equation to perfectly predict model behavior across a bunch of settings. Uh as I was saying, so we titerate along possibly all hyperparameters that made sense to titrate along for the three settings we were working with. And in pretty much all settings, our equation continues to hold and we are able to explain model behavior very well. Uh I'll maybe skip the slide in the interest of time, but just to offer some intuition and wrap things up. uh what we were saying before is that models transition between different behaviors as a function of experimental conditions. Uh so essentially models according to this equation this functional form that we're talking about. What we're saying is that the models have a simplicity bias. They have a prior which prefers simpler solutions. When data diversity is low you can just implement a generalizing predator or you don't really have a big big preference whether between generalizing or memorizing predictor. So you can just memorize the data. But as you keep on increasing the diversity uh you implementing the memorizing predictor will require learning or memorizing a lot more parameters. So you have a preference towards implementing the generalizing predictor. But as a function of training you start to see that actually the memorizing predictor explains the data better. So it'll have a better loss. And a product of this is what gives you a phase diagram that looks like this. That as a function of diversity you you have a tendency to perform proclivity towards performing like a generalized competitor. But as a function of training time you have have a proclivity towards performing like the memoriz and this is what we were seeing before uh in the previous slide we had the probabilities but if you simply take a log you should get uh an add factor. So here we have the log prior odds uh that we empirically computed uh the log base factor the likelihoods and if you simply do an addition of that you should get a phase diagram that looks like this and indeed that is what we were observing before the phase diagrams we were looking at before they emerge out of a consequence of this. So what we're saying is that models uh depending on how well a hypothesis explains a data versus the simplicity or the computational constraint that like do I can I even implement this from the get- go? Can I even implement this behavior from the get-go or do I need to like do a lot of learning to be able to implement that behavior dictates uh leads to transitions where once I see there's a certain hypothesis which explains the data better I that uh the likelihood overtakes the prior and has the model behave in accordance with a different algorithmic behavior. uh my co-author Daniel in accordance with our pitch made this uh cool chart DBD image of William of AAN holding a razor because essentially what we're talking about is simplicity bias I a razor uh thank you &gt;&gt; okay thank you so much that was an awesome awesome talk I think we have like five minutes for questions so if anyone has like some really pressing questions uh to ask please uh Feel free. Okay, we have a question in the chat. It looks like uh Q&amp;A. See if I can open that up. Sorry. Would you mind posting your question in the chat? I'm just not uh seeing the Q&amp;A anymore. Okay. So, Angela asks uh thank you for the talk. In the first section regarding in context learning of representation, you showed that the model learned the graph by 2D PTA projection. So, she's asking in layer 26, how did you select this layer and what was this consistent to all the layers? &gt;&gt; Yeah. Uh yeah, great question. So, let's look at backup slides. Uh so, this is what the results look like across layers. uh what you see is that in the earlier layers there's like a pre-training prior bas bias. So like the models uh representations are very much like what we would expect according to the pre-training pair. But then as you keep on going through the different layers uh as you keep on going deeper into the layers. The later layers behave in accordance with our graph structure. So layer 26 is over here. And if anything it only gets better uh deeper uh there's 32 layers. So eventually the unmbeding layer will look very weird because it has to but uh deeper layers show more so the in context structure and the earlier layer show more so the pre-training structure. &gt;&gt; That's really cool. I want to ask also um do you think there's any overlap between your work and uh the physics of language models from uh I believe it was professor Zen Allen Zoo at Meta. So he has like a fewers on like knowledge storage and diversity of training data. Do you see any overlap with that? &gt;&gt; Yeah, I yeah this is kind of like a provocative question. Uh but so I I think like so our group is called the physics of intelligence group. Uh so in right in there you see that there's a pretty strong similarity but thinking about things in a very similar way. Uh I would argue that like physics as a topic as a field it's motivation is to like build predictive accounts. Uh so it's not just about explanations. It's to take phenomenon that like a system exhibits a natural system exhibits and then start off with like phenomenological theories. So essentially identify what control variables uh control the control the systems behavior and then go further ahead and then develop mechanistic accounts which bottom up explain the behavior how the behavior emerges in the first place. Uh I think like from that point of view what we have been trying to do uh for example the in context learning of representations paper is the phenomenon the algorithmic phases paper is trying to like create a phenomenological account. uh so create a predictive account and then in the incontest learning strategy emerge rationally paper we're trying to like actually perform a theoretical account uh which has a phological theory itself but we don't have a mechanistic theory yet so in that regard I think like whereas I see the physics of language models paper it's still in like maybe stage one or stage two but it's not exactly gotten into like a predictive account or predictive theory design yet uh so it's cool but I I think maybe the physics world is overloaded a little bit. &gt;&gt; Yeah, true. Uh I had a query regarding one of the limitations that you have shown in uh the in context learning specifically with the basian model stuff. So like uh here you like one limitation that that you use of the uh settings where the model behavior is largely explained by only the two uh uh predictors that that you had. &gt;&gt; Uh uh yeah. So like other works uh have identified four predictors to explain their uh uh task uh like to produce the bagram versus unic outputs. &gt;&gt; So yeah what what do you think like can it be extended to that use case too? &gt;&gt; Y yeah totally. Uh so currently the way we did the modeling uh we just focused on uh computing log procedure odds uh like we're doing a model comparison kind of perspective right like there are two predictors and we're just computing like what has a higher procedure probability but you can easily just like not do a comparison uh so you just compute the absolute posterior probability in an unnormalized manner. So there's some partition function that will throw up and you just ignore the partition function for now. But when you will when you have to compute the next token probabilities, you have to do a comparison anyway. So let's say there were four predictors, all of them will be over here in the in the in the denominator and that partition function factor it'll just cancel out. So you can easily extend it to multiple predictors and in fact like uh we're currently working towards doing that uh just to demonstrate to people because this question often comes up that it's actually pretty easy to do that. &gt;&gt; Yeah. Yeah true unless like uh this uh kind this this work this last last paper specifically identifies ICL as a beasian but I think so like under assumptions like uh that that you made initially so it's like behaving more or less like that so like can we uh like change the assumptions a bit to come up to like a different conclusion to the Yeah. Uh &gt;&gt; yeah. So basically ICL is definitely basian is what we are saying but like basian in what manner? Uh so &gt;&gt; like the retrieval solutions I was talking about before. Uh they are basian solutions. They're basian averaging predictors. Uh over here this is unagram retrieval. Bagram retrieval it's just basian averaging. But unprints diagram infrance are also basian solutions. It's just like the prior you assume are different. Uh so like basian with respect to what uh like what is the distribution you're assuming? &gt;&gt; Yeah. uh and then when we go into the second paper what we're talking about is the computational constraints part where uh when we talk about the computational constraints we're saying uh what is the prior this prior dictates like even if these predictors explain the data very well like what do I prefer to implement given that both of them are going to perform pretty well what what is my preference between them so this is why we actually in the paper we said this is a hierarchical basian model the these predictors themselves are basian But then we're also putting another basian pri on top of that. Uh so &gt;&gt; yeah yeah that's kind of cool like this work uh like builds upon uh like top of many works that have previously come come across. So you can like I can specifically say like looking at this paper it sh it stands on the shoulders of joints and specifically with the mark of processes that are involved in this and the pre previous papers as well that you have worked on. So like that kind of intuitive so very uh good like uh explanation of what the work is currently representing. So yeah kudos to that. Yeah. &gt;&gt; Thank you so much. Uh I see there was another set of questions. Uh I can if you guys want I can I can respond to them now. &gt;&gt; Yeah. Uh I don't want to take up too much of your time. If you if you have to go but I think we have two last questions if you if you have time. &gt;&gt; Yeah sure. Uh so Hershel asks how would you say these results extrapolate to more real world tasks like images and how would you approach directly analyzing on complex data and task? Yeah we're actively working on that. Uh I'm personally pretty confident everything I'm saying holds on real settings. uh so for example we started off with if you recall uh many jailbreaking that was a phenomenon we were trying to explain and we designed a synthetic task which captures that then we designed a synthetic toy model which captures that and then we designed a predictive theory which provides us a precise intuition for how to explain the behavior. But now if the be if the theory is correct, we should be able to go back to the the phenomenon we started off with many shot jailbreaking and make some statement about it. So that's what we're working on right now. I currently believe we can actually like preemptively predict uh before you even run this experiment, I can tell you how many examples you will need for getting the model to behave let's say in accordance with like generate violent or hateful speech. Uh so we're actually trying to create a predictive account of many chart jailbreaking with a pre-trained language model. Now uh the theory is exactly the same what I was showing before. Uh just the thing what that we need to do is we need to model the prior we need to infer the prior in cognitive science is a proper uh pretty common thing to do uh inferring prior from data. So uh we're just like reappropriating those tools to see if we can make a predictive statement about this. Uh we have some decent results already. So I am uh quite hopeful where where where this is going. But also uh we have a bunch of other work uh where we show a lot of these theories if you like start off with these synthetic toy systems to capture some phenomenon and then like make predictive theories about that toy phenomenon then like just make predictions about the real data whether that holds and like generally as of yet we haven't had any negative cases and to me that the reason for this is just like you're just trying to learn the distribution. Your distribution can be a synthetic distribution or or like some natural data distribution as long as you can you learn the distribution, you can always make predictions about if you know something about the generative process of distribution, you can always make some predictions about the natural data itself and hence as long as we have intuitions constructed using simplified scenarios, we can check whether the intuitions hold with real scenarios. Uh so yeah, then uh I hope that answers the question. Uh there was one more question uh another question by Angela since our last layer and since our later layer model prints these patterns consistently is it correct to say that if we edit these layers then jailbreaking the model becomes easier? Uh I guess not. uh what I would argue our results are showing is uh that the earlier layers have the pre-training prior and like in the many short we can even think of it as that the model has a safety prior it kind of like has a prior towards like not engaging in unsafe instructions because of safety pining and most likely that prior is in the earlier layers not so much in the later layers uh because in the later layers you can just like in context manipulate the representations so I would say that Scale breaking becomes easier if you just if you intervene on the earlier layers cuz you just like can reduce the prior probability over some behavior over the over the uh the behavior of like not engaging in unsafe tasks. That being said, uh earlier layers are the most difficult place to perform interventions because if you intervene on earlier layers that like has trickling effects on all downstream layers and hence all downstream behaviors. So if you do perform interventions on earlier layers and it doesn't work, it might be compounded by the fact that you might just be breaking the model itself. Uh but yeah, great question and I think not necessarily a clear answer that's useful until we do the experiments. Okay, I think that's all the questions. So, uh, yeah, we'll let you go. I know we're over time, but really appreciate you taking the time to discuss, uh, this really cool series of papers with us and we hope you can stay involved in the community. I'm sure a lot of people have a lot of follow-up questions and we'll be following your work. Um, so if if you're not a part of the Discord, I can send you an invite for that as well. &gt;&gt; That'd be awesome. Yeah, I'd love to. Yeah. &gt;&gt; Thank you so much for having me again. &gt;&gt; All right. Thank you everybody for joining. Have a good rest of your day. &gt;&gt; You, too.
