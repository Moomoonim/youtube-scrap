# Quorum-Based Consistency for Cluster Changes with CloudNativePG Operator | POSETTE 2026

- 영상 링크: https://www.youtube.com/watch?v=VrXSCbU3aa4
- 채널: Microsoft Developer
- 업로드일: 2026-06-17
- 자막 언어: ko
- 단어 수: 약 2804개

---

## 스크립트

[제레미] 안녕하세요, 저는 제레미입니다. GEICO에서 Postgres 엔지니어로 일하고 있습니다. [레오] 안녕하세요, 저는 레오입니다. CloudNativePG 관리자입니다. [제레미] 오늘은 CloudNativePG의 흥미로운 기능인 failoverQuorum에 대해 이야기해 보겠습니다 . [레오] 동기 복제를 사용한다면 이 기능은 반드시 알아야 합니다. [제레미] 네, 안녕하세요. 오늘은 CloudNativePG 오퍼레이터를 사용한 클러스터 변경에 대한 쿼럼 기반 일관성, 또는 더 넓게는 Kubernetes에서 Postgres를 실행하는 것에 대해 이야기해 보겠습니다 . 저는 제레미이고 GEICO에서 Postgres 엔지니어로 일하고 있습니다. [레오] 그리고 저는 레오나르도입니다. EDB에서 소프트웨어 엔지니어로 일하고 있습니다. [제레미] 시작하기 전에 간단히 홍보 하나 드리고 싶습니다. 지금이 CloudNativePG 오픈 소스 프로젝트에 참여하기에 정말 좋은 시기라고 생각합니다 . 지금 당장 생각나는 대로 이 슬라이드에 10가지 영역을 적어 놓았는데, 이 영역들은 여러분이 오늘 당장 많은 사람들에게 도움이 될 의미 있고 영향력 있는 기여를 할 수 있는 곳들입니다 . 이번 발표에서는 그중 하나 인 클러스터 관리 및 페일오버 에 대해 이야기하겠지만 , 실제로 많은 일들이 진행되고 있습니다. 정말 흥미로운 시간이죠. 제가 먼저 시작해서 전반적인 개요를 설명하고 설계에 대해 이야기한 다음, 레오에게 마이크를 넘기겠습니다. 레오는 좀 더 자세히, 그리고 내부적으로 어떻게 작동하는지 설명해 줄 겁니다. 자, 시작해 볼까요 ? 시작하기 전에, 배경 설명을 위해 두 가지를 말씀드려야 할 것 같습니다 . 첫 번째는 기본적인 토대를 마련하는 것입니다. PostgreSQL 자체는 postgresql.org에서 소스 코드를 다운로드 하고 컴파일하여 머신에 설치하고 몇 가지 설정을 구성하면 사용할 수 있습니다. PostgreSQL은 복제와 관련하여 매우 강력한 기능을 제공합니다. 첫 번째 서버에서 두 번째 서버로 데이터를 복제합니다. 하지만 PostgreSQL은 복제본을 기본 서버로 전환하는 API는 제공 하지만 클러스터 관리는 지원하지 않습니다. 즉 , 장애 조치 자동화나 새로운 클러스터 및 구성 추가 자동화는 지원하지 않습니다. 따라서 Kubernetes에서 PostgreSQL을 실행하고 자가 복구 및 자동 장애 조치와 같은 기능을 구현하려면 어떤 방식으로든 이러한 기능을 제공해야 합니다. PostgreSQL 코어에는 이러한 기능이 포함되어 있지 않습니다 . 오늘 발표의 기초가 되는 두 번째 중요한 점은 PostgreSQL의 복제 기능이 매우 다재다능하다는 것을 이해하는 것입니다. PostgreSQL은 다양한 방식으로 구성할 수 있으며, 이것이 바로 PostgreSQL의 전형적인 특징입니다. 노출된 구성 설정이 매우 많습니다. 여기에는 상당한 유연성이 있으며, CloudNativePG 오퍼레이터가 이러한 구성 및 다양성에 대한 매우 높은 수준의 접근성을 제공한다는 점을 이해하는 것이 중요합니다. CloudNativePG를 사용하면 다양한 방식으로 PostgreSQL 복제를 구성할 수 있습니다 . 그렇죠? 왜 이것이 중요할까요? 왜 흥미로운 점일까요? 그 이유는 복제와 관련된 여러 가지 절충점을 세밀하게 조정할 수 있기 때문입니다 . 가장 자주 논의되는 세 가지 영역은 이 화면에서 제가 동그라미 친 세 가지, 즉 내구성, 가용성 및 성능입니다. 하지만 이러한 절충점과 조정 방법에 대해 이야기하기 전에 먼저 PostgreSQL 및 CloudNativePG를 사용한 복제에 대해 간단히 소개해야 합니다 . 시작하기 위해 어떻게 구성하는지 간단히 살펴보겠습니다. 기본적으로 CloudNativePG를 사용하여 복제를 구성하려면 알아야 할 네 가지 사항이 있습니다 . 이것들을 하나씩 살펴보겠습니다. 이 슬라이드에 몇 가지 더 있는데 , 나중에 직접 찾아보셔도 좋습니다. 우선 핵심적인 네 가지를 살펴보겠습니다. 첫 번째이자 가장 중요한 것은 바로 동기식 복제 개념입니다. 자, 그럼 동기 복제란 무엇일까요? 동기 복제는 모든 관계형 데이터베이스의 핵심 속성 중 하나인 트랜잭션과 관련이 있습니다 . 이는 근본적으로 우리가 개발자와 플랫폼 사용자에게 하는 약속과 같습니다 . 바로 이 약속 때문에 많은 사람들이 관계형 데이터베이스를 가치 있게 여기는 것입니다. 트랜잭션이라는 개념, 즉 우리가 하는 약속은 무엇일까요? 바로 원하는 만큼 레코드를 삽입, 업데이트, 삭제할 수 있다는 것입니다. 모든 변경 작업을 수행하고, 프로시저와 함수를 호출하고, 계산을 실행할 수 있습니다. 하지만 마법 같은 순간은 바로 '커밋(commit)'이라는 키워드를 실행하는 순간입니다 . 모든 프로그래밍 언어에는 이 키워드가 있습니다. 화면에 예시가 나와 있습니다. '커밋'을 입력하는 순간, 트랜잭션 내에서 수행된 모든 작업이 시스템의 다른 연결에서 볼 수 있게 됩니다 . 그리고 중요한 것은, 바로 이 순간, 우리는 개발자에게 해당 트랜잭션의 결과가 손실되지 않을 것이라고 약속하는 것입니다. 내구성이 뛰어나고 영구적입니다. 시스템에 장애가 발생하더라도 애플리케이션 전체를 장애에 대비해 설계할 필요가 없습니다. 데이터베이스 시스템 자체가 장애 시나리오를 처리하고 데이터 손실을 방지해 주기 때문입니다. 이것이 바로 저희가 드리는 약속입니다. 동기 복제는 이러한 약속을 지키고 실현하는 데 핵심적인 역할을 합니다. 시스템에 장애가 발생하면 데이터가 동기적으로 복제되어야 하기 때문입니다. 저는 동기 복제에 대해 다소 주관적인 견해를 가지고 있습니다. PostgreSQL에서 기본적으로 활성화되어 있지 않은 데에는 그럴 만한 이유가 있습니다. 모든 곳에 동기 복제가 필요한 것은 아닙니다. 하지만 Kubernetes에서 PostgreSQL을 실행하는 경우라면 동기 복제를 사용하는 것이 좋다고 생각합니다. 동기 복제란 기본적으로 클라이언트에 COMMIT 문 이 성공적 으로 처리 되었다는 알림을 보내는 동시에 트랜잭션이 복제되었다는 사실도 알려주는 것을 의미합니다 . 이것이 바로 동기 복제의 핵심입니다. 실제로 PostgreSQL 소스 코드는 동기 복제 로직의 상당 부분을 COMMIT 문 처리 과정 자체에 포함하고 있습니다. 동기 복제란 무엇일까요? 음, 잠시 동기 복제에 대해 자세히 살펴보겠습니다. 제가 가져온 COMMIT 메시지는 거의 10년 전의 것입니다. 이렇게 오랫동안 여기에 있었다니 놀랍네요. 오른쪽에서 가장 중요한 매개변수 인 synchronous_standby_names를 동그라미로 표시했습니다. 이 매개변수는 PostgreSQL에서 동기 복제를 할 때 가장 중요한 매개변수이며, 세 가지 구성 요소로 이루어져 있습니다. 제가 동그라미로 표시한 아래쪽 부분을 보시면, 세 가지 구성 요소가 있습니다. 메서드, 숫자, 그리고 대상 세트입니다. 기억하기 쉽죠? 메서드, 개수, 대상 집합, 맞죠? 메서드는 PostgreSQL에 동기 복제를 수행하는 두 가지 방법이 있다는 것을 알려주는데, 많은 사람들이 이 사실조차 모르고 있습니다 . 우선순위 기반 복제는 오래전부터 있었고, 쿼럼 기반 복제도 거의 10년 전부터 있었습니다. 메서드는 이 두 가지 방법 중 어떤 것을 사용 하는지 알려줍니다. 개수는 데이터를 복제할 대상의 개수를 나타내고, 대상 집합은 복제할 전체 대상 집합을 의미합니다. 예를 들어 대상 복제본이 3개라면, 클라이언트가 커밋을 승인하기 전에 모든 트랜잭션이 최소 2개에 복제된다는 것을 보장하는 것입니다 . 이것이 동기 복제에 대한 간단한 설명입니다. 두 번째로 알아야 할 것은 무엇일까요? 두 번째는 CloudNativePG에 특화된 내용입니다. 데이터 내구성이라는 개념이 있습니다. 왜 이런 개념이 필요할까요? PostgreSQL은 매우 직관적입니다. 예를 들어, PostgreSQL에게 데이터를 한 곳으로 복제하라고 지시하면, PostgreSQL은 해당 트랜잭션이 한 곳으로 복제되도록 보장합니다 . 하지만 복제본 하나가 유지보수 중이라면 어떻게 될까요? 패치를 적용하거나, 유지보수를 위해 오프라인 상태가 되거나, 일시적으로 문제가 발생하여 상태가 불안정해진다면요? PostgreSQL은 약속을 지키기 위해 해당 복제본이 다시 정상 상태가 될 때까지 어떤 트랜잭션도 커밋하지 않습니다 . 데이터 내구성을 절대적으로 보장하고 싶다면 이러한 절충안이 적합할 수도 있지만 , 그렇지 않을 수도 있습니다. 노드가 패치 또는 유지보수를 위해 오프라인 상태일 경우, 노드가 다시 정상 상태가 될 때까지 일시적으로 트랜잭션이 계속 진행되도록 하고, 정상 상태가 되면 모든 데이터의 내구성을 보장하는 것이 더 나은 선택일 수 있습니다 . CloudNativePG에서는 이러한 방식을 '선호 데이터 내구성(Preferred Data Durable)'이라고 합니다. 기본적으로 내구성은 필수(Required)로 설정되어 있지만, 선호(Preferred)로 설정할 수도 있습니다. CloudNativePG Operator는 복제본의 상태를 파악하고, 복제본이 비정상 상태인 경우 PostgreSQL을 즉시 재구성하여 복제 구성을 낮춘 다음 복제본이 다시 사용 가능해지면 다시 높일 수 있습니다. 세 번째로 알아야 할 중요한 매개변수는 앞서 언급한 메서드, 개수, 대상 목록입니다. 메서드와 개수는 대상 목록을 제어하는 ​​데 사용됩니다 . CloudNativePG에는 대상 목록을 제어할 수 있는 세 가지 설정이 있습니다. 지금은 자세히 설명하지 않겠지만, 이러한 설정이 있다는 것을 알아두세요. 네 번째는 synchronous_commit이라는 별도의 PostgreSQL 매개변수입니다. 이 매개변수를 사용하면 '복제됨'의 의미를 세밀하게 조정할 수 있습니다. 단순히 트랜잭션이 복제본에 수신되었음을 의미하는 것일까요? 복제본의 로그 파일에 기록된다는 의미인가요, 아니면 로그 파일에 영구적으로 저장된다는 의미인가요? 아니면 모든 테이블에 완전히 적용되어 해당 복제본의 읽기 전용 연결에서도 데이터를 볼 수 있다는 의미인가요? 이 기능을 통해 동작을 세밀하게 조정할 수 있습니다. 이제 이 네 가지 도구를 익히고 네 가지 기본 개념을 이해했으니, 앞서 언급한 모든 절충점에 대해 이야기해 보겠습니다. 여기서 오라클의 용어를 몇 가지 가져오겠습니다 . 오라클은 오래전에 Oracle Data Guard라는 개념을 만들었는데, 성능 극대화, 가용성 극대화, 보안 극대화 에 대해 이야기합니다 . 화면에 있는 표에는 이러한 절충점들을 CloudNativePG를 사용하여 어떻게 해결할 수 있는지 보여줍니다 . 오른쪽 열에 있는 방금 설명한 4가지 도구를 사용하면 CloudNativePG 구성을 통해 이러한 속성을 극대화할 수 있습니다. 하지만 이것은 시작에 불과합니다. 훨씬 더 많은 기능을 활용할 수 있기 때문입니다. 여기서는 단일 Kubernetes 클러스터 내에서 고가용성(HA) 설정을 보여드리고 있습니다 . 기본 클러스터 하나와 복제본 두 개로 구성된 구성이죠. 하지만 여러 개의 Kubernetes 클러스터도 구성할 수 있습니다. 예를 들어, 재해 복구(DR) 환경에서는 별도의 Kubernetes 클러스터가 있는 두 번째 데이터 센터를 떠올리실 수 있습니다 . 하지만 CloudNativePG는 기본 클러스터 와 보조 클러스터 모두에 HA를 구성하고, 두 클러스터 간에 복제를 수행합니다. 여러 데이터 센터 또는 여러 Kubernetes 클러스터 간에도 동일한 방식으로 성능 최적화를 위한 최적의 구성을 설정할 수 있습니다 . 성능을 극대화하는 예시를 보여드리겠습니다. 왼쪽 상자에는 Kubernetes 사양이 표시되고, 화면 하단에는 PostgreSQL 구성이 표시됩니다. 가용성을 극대화하는 예시를 보여드리겠습니다. 구성 과 장단점은 약간 다르지만, 기본 설정은 동일합니다. 그리고 보호 수준을 극대화하는 예시도 보여드리겠습니다. 하지만 여기서 더 나아갈 수도 있습니다. 믿기 어려우시겠지만, 이 최대 보호 구성은 데이터 복제를 보장합니다. 즉, 원격 데이터 센터에 네트워크 문제가 발생하거나 원격 데이터 센터에 문제가 생기면 기본 데이터 센터에서 쓰기 작업을 더 이상 처리하지 못하게 됩니다. 이는 바람직하지 않은 상황일 수 있습니다. 하지만 여러 개의 Kubernetes 클러스터를 사용할 수도 있으며, CloudNativePG는 이러한 구성을 지원합니다. 이렇게 하면 모든 트랜잭션이 최소한 하나의 원격 클러스터로 전송되도록 보장할 수 있습니다. 즉, 두 개의 클러스터가 존재하는 셈입니다. 이러한 놀라운 유연성은 우리에게 큰 문제를 야기하는데, 바로 이 때문에 오늘 이 자리에 모인 것입니다. 문제는, 이렇게 모든 유연성을 제공하고 다양한 방식으로 구성할 수 있도록 허용한다면 , 어떻게 모든 소스 코드를 관리하고 장애 조치 로직이 정확하고 오류가 없는지 확인할 수 있느냐는 것입니다. 예를 들어, 여러 데이터 센터 와 클러스터로 구성된 토폴로지에서 장애 조치 로직이 정확하게 작동하고 개발자에게 약속한 대로 트랜잭션 손실을 방지해야 한다면 어떻게 해야 할까요? 이는 실제로 매우 어려운 문제입니다. 해결하기가 정말 까다롭습니다. 그래서 저희는 지난 몇 년 동안 이 문제를 고민해 왔습니다. 그러던 중, PostgreSQL과 Kubernetes 클러스터링 환경에서 발생할 수 있는 모든 가능한 구성을 정확하고 확실하게 보장 하며 간단하게 처리할 수 있는 매우 우아하고 간단한 해결책이 있다는 것을 깨달았습니다 . 기본적으로 우리가 해야 할 일은 수십 년 동안 존재해 왔지만, 사람들이 일반적으로 포스트그레스 환경에서 생각하지 않는 개념을 가져오는 것이었습니다. 그리고 우리는 그 개념을 포스트그레스에 적용했습니다. 그 개념은, 이제 설계에 대해 이야기해 볼게요 . 사실, 예전에 레오가 아이디어를 주고받던 화이트보드에 적어 놓은 그림이 있는데 , 그가 휴대폰으로 사진을 찍어서 슬랙에 올린 거예요. 우리가 전 세계에 흩어져서 협업하고 있었기 때문이죠. 우리가 한 일은 읽기/쓰기 작업량이 노드 수, 즉 복제 계수보다 커야 한다는 기본적인 개념을 포스트그레스에 적용한 것입니다. 다이나모DB 관련 문서를 읽어보셨거나 카산드라를 구성해 보셨다면 이 개념이 익숙하실 수도 있지만 , 포스트그레스에 전통적으로 적용되는 개념은 아닙니다. 자, 이제 우리는 이 개념을 Kubernetes의 두 PostgreSQL에 매핑했습니다. 특히, 이 기능을 일종의 게이팅 함수처럼 사용하고 있습니다. 즉, 클러스터 재구성을 수행할 때만 적용됩니다. 예를 들어 페일오버, 노드 승격, 노드 추가, 노드 제거, 클러스터 토폴로지 재구성 등이 있습니다. 여기서 N 값은 앞서 언급한 중요한 PostgreSQL 매개변수인 synchronous_standby_names와 관련이 있습니다 . N 값은 대상 집합이고 , W 값은 개수이며, R 값은 클러스터 재구성 여부를 결정할 때만 평가해야 합니다. R 값 은 승격 가능한 복제본 집합과 대상 집합의 교집합입니다. 이해하기 쉽게 몇 가지 예제를 통해 살펴보겠습니다. 이 예제를 빠르게 살펴보겠습니다. 이것은 복제 클러스터를 사용한 최대 가용성 구성입니다 . 슬라이드 하단부터 살펴보시는 것을 권장합니다 . 왼쪽에는 사용자가 구성한 내용이 있고, 아래쪽에는 CNPG가 Postgres를 설정한 방식인 synchronous_standby_names가 있습니다. 바로 그 중요한 매개변수입니다. 해당 설정을 살펴보고 공식 값을 하나씩 확인해 보겠습니다. 먼저 N 값은 대상 집합을 나타냅니다. 대상 집합은 angus, acdc-2, acdc-3 이렇게 세 개의 멤버로 구성되어 있으므로 N 값은 3입니다. W 값은 그냥 숫자입니다. 아래에서 바로 가져오면 됩니다. 이제 장애가 발생했다고 가정해 보겠습니다. 기본 인스턴스가 실패하면 페일오버를 수행하거나 복제본 중 하나를 승격시키는 것이 안전한지 판단해야 합니다. 쓰기 인스턴스 가 실패했으므로 승격 가능한 복제본은 두 개의 로컬 인스턴스뿐입니다. 오퍼레이터는 단일 Kubernetes 클러스터 내에서 작동하기 때문입니다 . 따라서 R 값은 2이고, 2+2는 4이므로 4는 3보다 큽니다. 따라서 검사를 통과하고 계속 진행할 수 있습니다. 이제 CNPG는 안전하게 두 개의 승격 가능한 복제본 중에서 가장 높은 LSN 값을 가진 복제본을 선택하여 승격을 수행합니다. 이렇게 하면 승격 과정에서 트랜잭션 손실이 발생하지 않도록 보장할 수 있습니다 . 최대 보호와 같은 다른 구성에서도 동일한 방식으로 테스트를 진행할 수 있습니다 . 자세히 설명하지는 않겠지만, 두 개의 승격 가능한 복제본 중 하나만 교집합에 포함되는 경우를 예로 들어 보겠습니다. 이 경우에도 알고리즘이 정상적으로 작동하여 승격이 진행되는 것을 확인할 수 있습니다. 여러 개의 Kubernetes 클러스터를 대상으로도 동일한 테스트를 수행할 수 있습니다. 여기서는 N 값이 4이고 W 값이 3입니다 . 두 개의 복제본 클러스터를 대상으로 했을 때도 알고리즘이 정상적으로 작동하는 것을 보여줍니다. 또한 기본 복제본과 동시에 하나의 복제본이 손실되는 상황도 살펴보겠습니다 . 이 경우 알고리즘이 승격 시 트랜잭션 손실을 보장할 수 없다고 정확하게 알려주는 것을 확인할 수 있습니다 . 그래서 여기서 최선의 결정은 클러스터를 수동 개입이 필요한 상태로 만드는 것입니다. 사람이 직접 개입하여 무엇을 해야 할지 결정하도록 하는 것이죠. 그리고 이것이 올바른 결과입니다. 바로 우리가 얻은 결과입니다. 마지막으로, 시나리오를 실행해 볼 수 있는데, 가장 흥미로운 점 중 하나는 네트워크 분할 시나리오를 실행하는 것입니다. 네트워크 분할을 고려할 때는 사용 가능한 노드뿐만 아니라 운영자의 위치 도 생각해야 합니다 . 운영자가 분할의 어느 쪽에 위치해야 하는지 고려해야 하기 때문입니다. 다음은 기본 인스턴스가 분할된 예입니다. 다음은 복제본 중 하나가 분할된 예입니다. 가장 흥미로운 시나리오 중 하나는 운영자가 복제본과 함께 분할의 한쪽에 위치하고 기본 인스턴스가 다른 복제본과 함께 분할의 다른 쪽에 위치하는 시나리오입니다. 다시 말하지만, 이러한 상황에서 알고리즘의 진가가 발휘됩니다. 이런 상황에서는 기본 서버와 두 번째 파티션의 복제본이 쓰기 작업을 최소 몇 초 동안 계속 수락하게 됩니다. 페일세이프 같은 기능이 있어서 결국 감지되더라도 말이죠. 따라서 운영자가 자동화된 승격 결정을 내리도록 허용하면 트랜잭션 손실 위험이 항상 존재합니다 . 이 알고리즘은 이러한 상황을 정확하게 처리하여 스플릿 브레인 이나 네트워크 파티션과 같은 시나리오에서도 트랜잭션 손실이 발생하지 않도록 보장합니다. 지금까지 알고리즘에 대한 간략한 개요였습니다. 이제 레오에게 좀 더 자세히 설명해 달라고 부탁하겠습니다. [레오] 감사합니다, 제레미. CNPG에서는 어떻게 작동하는지 살펴볼까요? 우선, 새로운 구성 매개변수인 failoverQuorum이 추가되었습니다. 동기 복제 구성 에서 찾을 수 있습니다 . 이는 제레미가 방금 설명한 내용과 완전히 연결되어 있습니다. 버전 1.28부터는 괜찮지만, 이 매개변수를 활성화하면 문제가 발생합니다. 무슨 일이 일어날까요? 두 가지 측면이 있습니다. 첫째, 운영자가 작업을 시작하여 새로운 사용자 지정 리소스를 생성하고, 인스턴스 관리자가 이를 지속적으로 업데이트합니다. 다음 슬라이드에서 예시를 살펴보겠습니다. 바로 이것입니다. 이것이 바로 우리가 이 작업을 위해 생성한 사용자 지정 리소스입니다. 보시다시피 클러스터와 동일한 이름을 가지며, 상태에서는 synchronous_standby_names 구성 매개변수에서 찾을 수 있는 것과 동일한 정보를 확인할 수 있습니다. 메서드는 ANY이고, 오른쪽에는 ANY, standbyNumber는 1입니다. 그리고 standbyNames 목록은 아마도 대상일 것입니다. 그렇다면 이 정보의 실제 출처는 어디일까요? 바로 PostgreSQL 자체입니다. 가장 정확하게는 기본 인스턴스에서 실행되는 PostgreSQL입니다. postmaster를 자식 프로세스로 가진 PID 1인 인스턴스 관리자가 이를 지속적으로 업데이트합니다. 다음 슬라이드에서 확인할 수 있습니다. 이제 중요한 질문이 있습니다. 클러스터에서 무언가를 변경하거나 재구성한다고 가정해 보겠습니다. 어떤 일이 일어날까요? PostgreSQL이 최신 구성을 실제로 로드했는지 어떻게 확인할 수 있을까요? 이 프로세스는 비동기적으로 진행됩니다. 따라서 유일한 방법은 사용자 정의 리소스부터 최종 PostgreSQL 구성까지 처음부터 다시 확인하는 것입니다 . CloudNativePG를 사용하여 PostgreSQL을 구성할 때 클러스터 사용자 정의 리소스를 사용하도록 강제하는 기능이 있어 이 과정이 더 쉬워집니다 . 시스템을 강제로 변경하지 않는 한 PostgreSQL Pod에 직접 연결하여 구성을 변경할 수 없습니다. 다음 슬라이드에서 PostgreSQL 구성을 변경했을 때 어떤 일이 발생하는지 살펴보겠습니다. 먼저 새 구성 파일(custom.conf)을 생성하고 , 이 파일을 메인 postgresql.conf 파일에 포함시킵니다. 파일 을 업데이트한 다음 failoverQuorum 사용자 정의 리소스를 재설정합니다. 기본적으로 모든 상태를 초기화하는 것입니다. 이제부터 자동 장애 조치는 비활성화됩니다. 0+0은 0보다 크지 않기 때문입니다. 마지막으로 postmaster에 SIGHUP 신호를 보냅니다. 아시다시피 유닉스 시그널은 비동기식이므로 PostgreSQL이 시그널을 수신할 때까지 기다려야 합니다. PostgreSQL이 시그널을 수신했음을 확인한 후에는 실제 postgresql.conf 파일을 사용하여 failoverQuorum 사용자 정의 리소스를 다시 업데이트할 수 있습니다. 이제부터 조건이 충족되면 자동 페일오버가 가능해집니다. 하지만 핵심 질문은 postmaster가 새 구성을 수신할 때까지 어떻게 기다릴 수 있느냐는 것입니다. 그래서 우리는 이 질문에 답하고 또 다른 질문을 던집니다. 이것이 바로 우리가 custom.conf 파일에서 하는 일입니다. 보시다시피 몇 가지 사용자 정의 매개변수가 있습니다. 첫 번째는 cnpg.syncronous_standby_names_metadata입니다. 이것은 단순한 JSON 문자열이며, failoverQuorum 사용자 정의 리소스 의 상태 섹션에서 볼 수 있는 모든 정보를 포함합니다 . 특별히 복잡한 내용은 없습니다. custom.conf 파일의 내용을 생성할 때, 우리는 이 값에 대한 해시값을 계산하여 저장합니다. 그리고 이렇게 하면 다음 슬라이드에서 볼 수 있듯이, 포스트마스터에게 SIGHUP 신호를 보낸 후 PostgreSQL이 구성 파일에 입력한 것과 동일한 해시 값을 반환할 때까지 기다릴 수 있습니다. 포스트마스터가 결국에는 동기화되고, SIGHUP 신호를 보내면 구성이 다시 로드됩니다. 해시 값을 받으면 구성이 로드되었음을 확신할 수 있습니다. 그 다음에는 어떻게 될까요? 다음 슬라이드에서 볼 수 있듯이 JSON 문자열을 읽어올 수 있습니다. 이 문자열 이 바로 failoverQuorum 객체를 업데이트하는 데 필요한 정보입니다. 이 과정을 통해 가장 중요한 점은 PostgreSQL이 권한의 원천이라는 것입니다. 우리도 아니고, CRD도 아니고, Kubernetes 컨트롤러도 아닙니다. 이제부터 조건이 충족되면 오퍼레이터는 보유한 정보를 사용하여 자동 페일오버를 트리거할 수 있게 됩니다. 다음 슬라이드에서는 이 마법의 공식이 다시 나타나고 , failoverQuorum 내부에 모든 정보가 있으며, 실제 클러스터에서 관찰된 최신 정보도 모두 확인할 수 있습니다. 이제 우리는 페일오버가 정말 필요한지 결정할 수 있습니다. 페일오버를 원하지 않는다면 괜찮습니다. 상황이 바뀔 때까지 또는 누군가 실제로 복제본을 승격시킬 때까지 기다리면 됩니다 . 페일오버를 원한다면, 이제 안전하므로 진행합니다. 이 예제를 살펴보겠습니다. 이것은 단순한 연습이지만, 흔히 배포되는 상황을 나타냅니다. 이것은 동기 복제를 사용하는 3개의 인스턴스 클러스터입니다. 제레미의 말처럼 동기 복제는 매우 중요합니다. 첫 번째 인스턴스는 비동기 복제본 하나만 사용하고, 페일오버 쿼럼을 활성화했습니다. 이제 기본 인스턴스가 다운되면 어떻게 될까요? example-2와 example-3 중 하나는 모든 트랜잭션을 수신했을 것이라고 확신합니다. 따라서 가장 진행된 복제본으로 페일오버하는 것이 안전합니다. 어떤 복제본인지, 즉 example-2인지 example-3인지 알 수 없더라도 가장 진행된 복제본으로 페일오버하는 것이 안전합니다. 이게 중요한 점입니다. 오른쪽에 로그 메시지가 보이시죠? 이 메시지는 운영자 로그에서 확인할 수 있고, isStronglyConsistent가 true라고 알려줍니다. 네, 이게 가장 중요한 부분입니다. 자 , 다음 슬라이드로 넘어가죠. 아, 이건 안 좋은 상황이네요. 지금 무슨 일이 벌어지고 있는 걸까요? 기본 서버와 복제 서버 모두 손실되었습니다. 이전과 정확히 같은 시나리오인데, 이번에는 example-2 서버인지 example-3 서버인지 어떤 트랜잭션이 저장되어 있었는지 알 수 없기 때문에 페일오버를 진행하는 것이 안전하지 않습니다. 운영자 로그에서 "강력한 일관성 검사 실패" 메시지를 보실 수 있을 겁니다. 1+1이 2보다 크지 않기 때문에 기존의 공식이 더 이상 유효하지 않기 때문입니다 . 만약 이런 상황에 처하게 된다면, 상황이 개선될 때까지 기다리 거나 모든 것이 정상이라고 확신한 후에 수동으로 복제 서버를 승격시켜야 합니다. 하지만 이 경우 모든 책임은 사용자에게 있지만, 그래도 안전합니다. 간단히 설명드리자면 이렇 습니다. 제레미가 말했듯이, 이 커뮤니티에 참여하기에 정말 좋은 시기인 것 같습니다. 그리고 저희와 소통하고 기여하고 싶으시다면, 저희처럼 CloudNativePG를 즐겁게 사용하시길 진심으로 바랍니다. 저희 GitHub 링크와 트위터 계정(이번에는 X 계정입니다)은 다음과 같습니다. 네, 정말 감사합니다. [제레미] 감사합니다.
