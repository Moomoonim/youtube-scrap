# Building a Fullstack Game Store in 10 minutes (React, TailwindCSS, Amazon Bedrock)

- 영상 링크: https://www.youtube.com/watch?v=_2-QCu96zrM
- 채널: AWS Developers
- 업로드일: 2024-10-19
- 자막 언어: ko
- 단어 수: 약 981개

---

## 스크립트

생성형 AI 앱을 만든다고 해서 복잡한 API를 많이 배워야 하는 것은 아닙니다. 이 비디오에서는 안정적인 확산, Amazon Bedrock, 그리고 CLA 3 SONET을 사용하여 게임 스토어 카탈로그 생성기를 만들어 보겠습니다. 이 모든 기술을 함께 사용하여 10분 안에 애플리케이션을 만들어 볼 것입니다. 데모를 통해 어떻게 작동하는지 보여드리겠습니다. 방금 만든 비디오 게임 카탈로그입니다. 제목부터 이미지, 설명, 심지어 가격까지 모두 제가 직접 만들었습니다. 카탈로그에 새 항목을 추가하려면 " 게임 추가"를 클릭합니다. 그러면 오른쪽에 프롬프트가 나타납니다. 모든 정보를 직접 입력하거나 프롬프트에 일부 내용을 입력하면 필요한 모든 정보가 자동으로 생성됩니다. 이는 테스트 데이터나 더미 데이터를 만들거나 생성 형 AI를 시험해 보기에 좋은 방법입니다. 새 프롬프트를 만들어 보겠습니다. 우주왕복선 시뮬레이션 게임을 생성해 달라고 요청하겠습니다. " 우주왕복선 시뮬레이션 게임을 만들어 주세요."라고 입력하면 CLA에 연결됩니다. 3가지 모델과 안정적인 확산 기능을 사용하여 게임에 사용할 수 있는 이미지를 몇 장 가져오겠습니다. 좋습니다. 보시다시피 게임 제목으로 우주왕복선 오디세이가 나왔고, 설명, 평점, 가격도 표시되었습니다. 선택할 수 있는 이미지가 네 장 있는데, 그중 하나를 제 하드 드라이브에 저장하겠습니다. 그리고 바로 아래에 이미지 이름을 입력하겠습니다. 이 코드가 있는 프로젝트 폴더 안의 public 폴더에 저장하고, 이제 ' shult'라고 입력하겠습니다. JPEG 형식으로 업로드하고 제출하겠습니다. 보시다시피 새로운 우주 왕복선 게임이 추가되었고 모든 것이 자동으로 생성되었습니다. 그럼 다이어그램을 통해 어떻게 작동하는지 살펴보겠습니다. 오늘 우리가 만들 앱의 다이어그램입니다. 앱은 이미 만들어졌으므로 주요 기능만 보여드리겠습니다. 먼저 전체적인 아키텍처를 보여드리겠습니다. 여기 사용자가 있고, 키보드를 사용하는 저희가 바로 그 사용자입니다. 모든 API에는 Adobe Amplify Gen 2를 사용할 것이고, 인프라 구축에는 CodeTool을 사용할 것입니다. 개발이 완료되면 Amplify 호스팅에 앱을 호스팅할 수도 있습니다. Tailwind, CSS V, React, 그리고 레트로한 느낌을 주는 CSS 라이브러리도 사용하고 있습니다. 게임을 추가하라는 메시지가 표시되면 AppSync로 이동하고, AppSync는 Amazon Cognito를 통해 사용자가 인증되었는지, 그리고 해당 작업을 수행할 권한이 있는지 확인합니다. 원하는 작업을 수행할 수 있는 세 가지 유형이 있습니다. AWS Lambda 리졸버에 사용자 지정 쿼리를 실행할 수 있으며, 여기서 게임 목록을 생성할 수 있습니다. 즉, 모든 가짜 데이터를 생성하는 것입니다. 이 데이터는 Amazon Bedrock으로 전송되고, Bedrock은 CLA 3 Sonet에 연결됩니다. 또한 게임 모델이 있는데, 이 게임 모델은 Dynamo DB와 통신하며 게임 이름, 설명, 가격, 평점 등의 모든 정보를 Dynamo DB에 저장합니다. 이미지를 생성하는 또 다른 사용자 지정 쿼리가 있는데, 이 쿼리는 AWS Lambda 리졸버인 ` Generate Image`를 통해 Amazon Bedrock으로 전송됩니다. Amazon Bedrock은 안정적인 확산(Stable Diffusion)을 사용하여 이미지를 생성하고 프런트엔드로 가져옵니다. 보시다시피 이 모든 것을 작동시키기 위해서는 여러 단계를 거쳐야 하지만, 코드를 살펴보면 이러한 복잡성의 대부분이 Gen 2 Makes 내부에 추상화되어 있어 훨씬 더 간단하게 작업할 수 있음을 알 수 있습니다. 이제 코드를 살펴보고 몇 가지 주요 기능과 구성 방법을 알아보겠습니다. 여기에는 몇 가지 패키지가 설치되어 있습니다. 저희는 AWS Amplify, AWS Amplify UI React, 그리고 Lambda 함수에서 실행될 AWS SDK 클라이언트 Bedrock 런타임을 사용할 예정입니다. 이 모든 것을 설치해야 합니다. 또한 AWS Amplify Gen 2를 사용할 것이므로 ` mpm create amplify at latest` 명령어를 사용하여 설치합니다. 이 명령어는 데이터 레이어와 앱에 필요한 권한 부여를 생성하는 데 사용할 기본 코드를 생성합니다. 모든 것을 로컬에서 테스트하기 위해 MPX Ampex 샌드박스를 사용할 것입니다. MPX는 AWS 계정에 임시 환경을 생성하여 언제든지 시작하고 중지할 수 있도록 해줍니다. 이를 통해 테스트가 매우 간편해집니다. 오늘 데모에서도 이 샌드박스를 사용하고 있습니다. 마지막으로, 이 앱을 프로덕션 환경에 배포하려면 AWS 콘솔에 연결할 수 있습니다. 아래에 관련 링크를 제공하겠습니다. 다음으로, AWS 자격 증명을 컴퓨터에 로컬로 저장해야 합니다. AWS CLI를 설정할 때 저장할 수 있으며, 원한다면 Identity Center도 설정할 수 있습니다. 관련 내용은 아래에 설명 하겠습니다. 설명에 링크 도 있으니 VS Code에서 열어보면 새 Amplify 폴더가 생성되고 그 안에 data라는 파일이 있는 것을 볼 수 있습니다. 이 폴더는 amplify mpm create amplify at latest 명령어를 실행했을 때 생성된 것입니다. 이제 몇 가지 추가 기능을 넣어서 functions 폴더와 generate game listing, generate image 함수를 만들었습니다. 코드는 나중에 보여드리겠지만, 먼저 data 폴더와 resource 폴더에 있는 내용을 강조하고 싶습니다. resource 폴더는 데이터 모델을 정의하는 파일이고, 앞서 다이어그램에서 보여드린 Lambda 함수와 상호 작용하는 사용자 지정 쿼리를 정의하는 곳이기도 합니다. 첫 번째로 보여드릴 것은 game 모델에 대한 쿼리입니다. 이 쿼리는 abtin 인스턴스를 생성하고, 이름, 설명, 평점, 가격, 이미지 필드를 생성하며, 생성, 읽기, 업데이트, 삭제, 목록 생성과 같은 모든 리졸버를 생성합니다. 단 몇 줄의 코드로 이 모든 작업을 수행할 수 있습니다. generate game listing은 첫 번째 사용자 지정 쿼리입니다. 이 설명은 입력 값을 받아 게임 이름, 설명, 평점, 가격을 반환한다는 것을 의미합니다. 그리고 잠시 후에 보여드릴 Lambda 함수와 통신하여 게임 목록을 생성합니다. 이미지 생성 함수도 마찬가지로, 쿼리와 인수를 통해 프롬프트를 받아 문자열을 반환합니다. 이 문자열을 사용하여 이미지를 생성합니다. 이제 Lambda 함수 중 하나를 살펴보겠습니다. 먼저 게임 목록 생성 함수를 보겠습니다. 코드를 보면 (참고로 설명에 링크가 있습니다) Clad 3 SONET 모델과 통신합니다. 기본적으로 우리가 하는 일은 함수에 보내는 값과 반환받는 값을 정확하게 정의하는 것입니다. 시스템 프롬프트를 통해 게임 목록을 가져오고 특정 필드를 반환하도록 지정합니다. 그런 다음 클라우드 옵션 중 하나인 '도구'를 사용합니다. 이 옵션의 장점은 반환받을 값을 정확하게 정의할 수 있다는 것입니다. 다양한 응답 유형과 이름, 설명, 평점, 가격에 대한 입력 스키마를 정의할 수 있습니다. 원하는 것을 정확하게 정의할 수 있으며, JSON 형식으로 응답을 받도록 지정할 수도 있습니다. 이는 클라우드에서 반환되는 데이터를 파싱해야 하기 때문에 매우 중요합니다. 3 SONET 모델을 사용할 수도 있고, 항상 특정 응답 유형을 사용하도록 지정할 수도 있습니다. 즉, 항상 특정 도구나 응답 유형을 사용하도록 설정하는 것입니다. 이러한 메시지의 경우, 기본적으로 인수를 전송하면 예상한 데이터가 반환되고, 디코딩 과정을 거쳐 사용자에게 다시 전송됩니다. 이미지 생성의 경우, 안정적인 확산(Stable Image Core)을 사용합니다. 여기에서 원하는 모델을 정확하게 지정할 수 있습니다. 안정적인 이미지 코어(Stable Image Core)를 사용하고, 원하는 정보를 정확하게 입력한 후 디코딩하여 다시 전송합니다. 이제 몇 가지 추가 작업을 해야 합니다. 리소스 파일 내에 백엔드를 정의해야 하고, 몇 가지 정책도 업데이트해야 합니다. 마지막으로, 간단히 보여드릴 것은 이 모든 것에 사용되는 React 코드입니다. 여러 가지 사용 상태가 있는데, 특히 주목해야 할 점은 게임 목록을 생성하는 쿼리가 있다는 것입니다. 이 쿼리는 프롬프트에서 값을 받아 백엔드에 쿼리를 보내는 방식으로 작동합니다. 즉, 프런트엔드에서 백엔드에 연결하여 ABS 동기화 서비스에 접속하고, 이 서비스는 베드락 및 클래드 버전과 연결됩니다. 이미지 생성도 마찬가지입니다. 이 경우에는 여러 개의 이미지를 생성하여 배열에 저장하고, 나중에 프런트엔드에서 해당 정보를 사용할 수 있도록 합니다. 지금까지 많은 정보를 드렸지만, 이는 Amplify Gen 2의 강력한 기능과 베드락 및 이러한 대규모 언어 모델과의 연동을 보여주는 좋은 예입니다. 더 자세한 내용을 알고 싶으시면 아래 설명란의 링크를 클릭해 주세요.
