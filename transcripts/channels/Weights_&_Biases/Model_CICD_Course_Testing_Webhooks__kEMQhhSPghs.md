# Model CI/CD Course: Testing Webhooks

- 영상 링크: https://www.youtube.com/watch?v=kEMQhhSPghs
- 채널: Weights & Biases
- 업로드일: 2024-05-28
- 자막 언어: en
- 단어 수: 약 1046개

---

## 스크립트

hello I want to go over an important subject about web Hooks and it is how to test web hooks so one way to test web hooks is to kind of like trigger web hooks by adding aliases or whatever but that's kind of cumbersome to just like add an alias just so you can trigger a web hook and actually was and biases offers a way to test web hooks um like a specific testing functionality so let me show you that right now so if you recall uh the the web hook code looks something like this it's just it's a web server and it's using fast API and if you're familiar with fast API we're defining a data model that expects the payload to be a certain schema so we're you know you're expecting a payload to have all of these fields event type event author Alias artifact version so on and so forth and if you send a payload that doesn't um that doesn't conf form to the schema then fast API was going to throw you an error specifically pantic is going to throw you an error and this a very common pattern and so one way to do that is one way to debug again is to like trigger it manually and see it on the server side um but that again is cumbersome so I want to show you a neat trick the ne trick is if you go into your teams page so this is the the view Co and you go down to your web hooks if you click this triple dot thing you'll see this handy test and this test what it will do is it will give you sort of um this playground where you can have this payload and so um there's different ways you can you can like start from so I have this payload already from this is my the model registry that I showed you earlier and I'm going back to this this one that already set up and if you recall if I view the details I have this payload now I want to share like when I was first creating this web hook I kind of had to like think about what was being sent and so um what happens is like you might want to test this payload so this is the same payload and so there's there's different ways that you can try to figure out what's going on if you have an error one is to look at the logs so in model's case you can look at the logs like I can see a request I can see okay there's some kind of logs now depending on your application you might even have access to the logs like and in this case um you know these logs are not really showing me maybe what I'm looking for and have to dig through it so it's actually uh helpful to see the response um you know especially if you're not using curl um you want to see what the response is and so basically what I'm going to do is let's say I have this payload and let's say when um let's say I forgot to include the author for example and that's really easy to do is to make a silly mistake like this it happens to everybody um and again if I don't have the author it's going to error because it's not having this like field and so let's test that let's test that against this modal endpoint I'm going to go back to test web hook here and what it's going to do is um W and bies is going to show you the response from the web server in this case what modal is is uh saying in this case this is a little bit um confusing because this is actually pantic this is like a lowlevel pantic error or uh kind of response and it's showing you that it's missing a field what this like how you translate this is like your your missing this event author field that's what this means and this is ptic specific this is not anything to do weights and bies this is like what your web server is responding with it's actually really useful to see this so if I if I add this back for example I'm going to add that field back and test the web hook we will see that now there's a success um that there's a response and there's no error and you know again we can see that if I look at the logs we can see that hey there was a success here and then that was preced ceeded by some errors and you know in this case actually like I can't really I don't really know what those errors are to be honest like I can't even see them and this is like a real um this is kind of like a real case where having the response so there's no there's no error being logged on the server side I could improve my code to log errors on the server side but I don't do that like if there's an error it just errors it doesn't print anything so having the response from the server actually is really useful um and has helped me cat catch errors and this has actually helped me in real life um and it and it uh like be able to rapidly test and basically another way to use this is when you're actually developing your web server like let's say I want to continuously change this code and change the schema and change like you know the code of the web server well while you're developing it um you can just like basically uh have two windows open where this code and sort of this testing window and keep sending payloads and sort of keep debugging and that's also uh very helpful so just want to show you that this is actually like like one of the most helpful things like features to know about in web hooks is act is testing because it helps you develop web hooks
