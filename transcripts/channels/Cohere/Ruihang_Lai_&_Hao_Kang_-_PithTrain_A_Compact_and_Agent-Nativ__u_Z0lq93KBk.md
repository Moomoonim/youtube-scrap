# Ruihang Lai & Hao Kang - PithTrain: A Compact and Agent-Native MoE Training System

- 영상 링크: https://www.youtube.com/watch?v=u_Z0lq93KBk
- 채널: Cohere
- 업로드일: 2026-07-24
- 자막 언어: en
- 단어 수: 약 6755개

---

## 스크립트

[music] Um my name is Rh and with how together today uh it's a very it is great pleasure for us to um introduce our recent work called pet train which is a compact and agent native training system and we are um students from CMU and let's get started so um we first want to introduce the meaning of pith. Um I'm not sure how many people knows but um pith one of its meaning is the um white tissue in some of the fruits but the other meaning is the essence of something. So by naming this project as pith train we really want to show that um how we can capture the essence of moa training and build such a system and let's start with why we care about MOA in the first place. So um if we look at basically like every frontier model that is shipped in the last two years I guess these two weeks there are more open source models. Uh they're all like mixture of experts. It's become the default architecture for frontier language models for quite a few reasons. Um first you get uh more capacity per flop. Um and the reason is because only a fraction of the parameters are active for any given token and this will directly translate to um say cheaper reasoning at the inference time which actually matters a lot in the reasoning model error right now and the hardware has also caught up with like larger and larger memory to hold all these models in the HPMS and at this point there is like basically an industry consensus that uh doing is the way for us to scale. So um if we made a deliberate like like we we are making a deliberate bet to focus on this specifically because we believe that um this is the mainstream going forward. So okay uh the next question is if we want to train MOE models uh we definitely need good infrastructure and what people use in both industry and academia right now is those mature production frameworks such as like magatron or deep speed and of course um these frameworks have the uh very good model coverage they have a good feature coverage um they're very efficient well optimized by many experienced engineers. Um they're also robust. They have very large communities that um exchange ideas, feedback frequently and they're also backed by big technology companies that keep investing in these projects. So if you want to train an MO model today and you want the training to be fast and also reliable, this is the default option and their adoption actually goes deeper beyond simple pre-training or fine-tuning because uh a lot of like reinforcement learning post-training frameworks use magnetron as a default trainer as an example. So a meatron is not just a training framework. It's kind of becoming a foundational dependency for a whole whole ecosystem that uh built around it. But um while we are actually using these frameworks daytoday, we realize that there are still some frictions. Um when we're talking to people, many folks um share same experiences. Um for example uh if we want to learn how a particular technique is implemented inside the framework or um you want to set up the environment on a brand new cluster or a fresh ICE or new paper that just uh come out um proposing a new architecture and we want to support the architecture in the framework. Um in order to do these things um we need to dive very deep into the very large systems and of course this is doable that's like how we have been doing so far but every time we do this um these tasks um there there is a real cost because we need to dive into it and they are they can add up and we try to think about like where these frictions actually come from and it seems to be a quite like few structural things. Um they have massive code bases um which is like well over 100,000 lines of code. Um there's also heavy C++ extension dependency under the hood and there are many layers of abstraction stacking on top of each other. Um these are all overhead for humans. um human engineer had to like read all the code, trace through the abstraction and also rebuild the extensions. But now in 2026 um increasingly the entity that doing this work is probably no longer a human and instead it's the coding agent. Um for coding has actually gone from o autocomplete a few years ago to very powerful agents in this uh recent few years. They vote very quickly and almost all of our um development tasks are right now done through agents. Uh we are typing prompts or even speaking prompts instead of like doing um coding anymore. So the agent has become the primary user or developer of these uh mature production frameworks. And coming back to the frictions we mentioned before um it's true that say we can leverage AI agents and completing a task with agent is much faster than a human could do but this does not mean that the underlying problem is solved. um the source of the friction has not changed at all and the friction still exist. Um for example for an agent um 100,000 lines and abstraction layers means the agent has to do far more file reads and also far more analysis just in order to orient the agent itself. And also um the C++ dependency means um whenever something breaks the arrow may appear at say uh somewhere opaque in the extension or uh the back trace may come back and forth across the um language interfaces. So um that leads us to the central question. Can we have a framework that is more friendly to agents without giving up the production grade speed and of course um a framework that is more friendly agent is uh typically more friendly for agents as well for for humans as well. So um in order to answer that um we first have to check how we have been evaluating a training framework so far. Um traditionally uh people focus on the performance uh which is the golden metric. Uh it's basically the pur purely the training efficiency with metrics like throughput and MFU. And this is very easy to understand because if you have faster training you uh you spend less time to you spend less time on the training and it also means less money and everyone optimizes framework based on this access. But um when we say we want to build something that is more friendly for agents um we found that like it's very vague actually like what does that mean? So we first tried to um give it a name which we call agent task efficiency or AT for short. Um previously it didn't even have a name. Um and this is the second that has been completely missing in the past. Um the idea of agent task efficiency is that uh we take the same task and we let a let an agent to complete it on two different frameworks A and B. Um if the agent completes it more efficiently on A we say A has better agent task efficiency than B. And um here the word efficiency is actually measurable. Um we can for example look at the total time the agent takes uh the number of output tokens it generates um the output like the number of agent turns it needs and etc. Um these are very concrete and also quantifiable metrics and although we we noticed that um there's one caveat that there is not a single metric that can show the overall agent task efficiency of a framework and as a result for now we we kind of need to use all these different metrics to get a comprehensive understanding of how agent task efficient a framework this. So, um we can now trans like restate restate our central question to uh this one uh which is more precise. Uh can we build a framework that has both high training efficiency and high agent task efficiency? And this is the question that the whole talk is built around today. And um thinking from the first principle um we want to build a system uh this doesn't seem extremely challenging. We found that if we want to build a training framework from from say scratch or from relatively low-level pietorch uh we probably only need these components um here across these three levels. Um at the bottom is the operator level where the operators come from different sources like PyTorch uh or other fast operator libraries or customized GPU kernels. Then we have the engine level above the operators like building blocks. Um they're further composed into models and also runtime distributed training and other distributed like infrastructure. And at the very top it is the application layer for things like uh like the training loop. So our takeaway is we thought that a training framework will be very complex. Well, it turns out that the architecture is very clear and we can just go for it. So we built pip train and this is an overall of the whole thing. Um on the left is the uh agent loop where when a a agent uh is working on a task uh it takes action on pit train and also gets feedback from it. And um for pith train in the uh center panel the highlight is that we build pith train based on four different agent native design principles. Um and also PE train supports standard MOE endto-end training and delivers both training efficiency and agent task efficiency which we call dual efficiency and on the right panel is the AT bench or the agent task efficiency bench which we built to compare agent task efficiency of different frameworks. So um let's first start with the four agent native design principles and actually uh to be honest we think every of these principles is a very is very trivial but likely no one has seriously called them out before. So um principle number one is compact code base and this may end up being the most important one. Um having less code generally means less for the coding agent to search um fewer fewer cross file dependencies to track and less for the agent to read before before it is able to uh make sure that a change is complete. Um it also means that um with an agent that comes with say 1 million token contacts today such as cloud um it can hold the entire framework in just one pass instead of like repeatedly wrapping its weight around. Although um we have not yet observed such a task um which um the agent really needs to keep the entire codebase in this context. But uh this is very interesting and on the left side is the actual architecture of pit train. Um adding everything up we get roughly um 11,000 lines in total. Uh right now it might be maybe [clears throat] 12,000. Um which is an order of magnitude smaller than the mature production frameworks that we saw earlier. But um of course uh this is a trade-off. uh we don't have the uh model coverage and feature coverage as good as those frameworks frameworks have but we think if someone wants to add a feature or something into pit train they should be able to like leverage agent give it clear instructions and then the agent can take over and add the feature or like uh things you need uh in relatively short amount of time. Okay. Uh, principle number two is being Python native. Um, the benefits of uh, okay, I saw a question on sharing slides. Yes, we can share the slides after presentation. Um, okay. Uh, the benefits of um, being Python native are very concrete for agents. Um, there's no cross language interface to reason about. And also um when something breaks it gets a Python trace back. Most of the time the Python trace back is readable and also like pointing to an actual line. Um this actually contrasts the case of frameworks with um heavy compiled extensions where we ran into opaque crashes in the compiled code for multiple times. And uh another benefit of being Python native is say uh there is no compiled extension for us to rebuild every time we change a single non-Python line non-Python line such as C++ line or like Ras line and because rebuilding them is a constant source of friction whenever we want to like fix something. Um and one thing to note is that um being Python native doesn't mean we give up on GPU kernels. We don't write CUDA kernels directly but we write our kernels in Python DSLs such as like Triton Talan and so on. So with Python DSO we can keep the readability and a single language stack and in the meantime we are still able to get fast kernels. Um principle number three is having no implicit interaction or or very little implicit interaction. So um on the left is the model construction pattern and maggotron. Um if you look at where self.mop is set up in the transformer layer it it's calling the build module with some subm module subm module spec but the question here is what is that subm module um I can't really tell from here locally and I have to go to a different file um which is the layers back file um and trace through the modules back that actually instantiate creates the MOP's back and this definition is like definitely um somewhere else and for people who are familiar with Matron this this might be okay because they've seen this too many times but for people who are um trying to get started with Matron and also for coding agent this would be a lot of overhead and um on the right is path train u where the definition is right there in the same file locally. Both humans and agents can read the file top to bottom and can know exactly what's going on. Um but actually this is a trade-off. Uh pith train in pith train we trade cross model code reuse for local readability and we're not saying that implicit interactions are bad. Uh the interaction on the left exists for for reasons. For example, it has many models to share the um same skeleton. Um we deliberately give that up so that each model file is self-contained and and an agent can understand the model architecture by reading one file. And um this principle does is not just about model definitions. It actually applies more broadly. uh for example like recursive abstraction classes and deeply nested modules and module hooks and these are all the interactions that we try to avoid. Um the last principle is um tasks with task specific agent skills and the motivation here is a little bit different from the first three. The first 30 principles are all about like making code easier for agent to read or understand. But we think that we just can't leave everything to reading code. Actually there's like human knowledge say how we actually do things or the procedure of a recurring task. um these are something that a coding agent cannot easily recover by just reading the source or calling tools. So we pack this kind of knowledge into agent skills and ship them in the ripple. Um but of course this is not something some some novel idea that no one mentions before. Um however when we look at the available training frameworks out there we found that they don't do quite much about agent skills. uh for example magatron it does have skills but they're all about the CI and PR management and Torch Titan and torch speed the deep speed um they provide like top level orientation files for the agent so the agent can know about the overall project structure very quickly but they kind of just stop there. Um the table on the left includes some of the um skill example input train. Um and we are gradually like bringing more into this project. And one thing to note is that skills are composable. Um one skill can call another. So the one idea is you can have some like common small utility skills and other skills will leverage them automatically. And this is actually very similar to programming traditional programming where we usually have some say uh common functions and also the main function. Uh the main functions are built around these like util functions and you can therefore treat skills as functions in natural language. Um in practice we think that uh simply having skills are still not enough. Um if a skill is defined too vague then it's probably not very useful. So we identified three uh properties that we think are very important for designing designing skills. Um the first property is specific scope which means we need to have narrow and unambiguous description for the for the skill. Um it's also very helpful to have some uh like natural language triggers. So it can tell the agents that say when you see this see this keywords you run a skill. Okay. Uh the second property is explicit prerequisite. Um just as like functions in traditional programming that they cannot run without all the required input. A skill also needs to know clearly about uh what needs to be ready before it can execute. Um the key is we never want to make the agent guess. So in the skill you can say okay um if any of these prerequisites are not met you just reported it to me instead of like silently run the silently run the skill. Um the last but uh most important one is the verifiable success. In the skill we need to make sure that the agent can know whether it successes or like whether it succeeds or fails. Um for example in this validate correctness skill we provide a compare.py and let the agent to run this for comparison. If the Python output zero it means a pass or otherwise it means a fail. So um the agent doesn't have to interpret it from the context on whether the task succeeded but instead there is a hard-coded deterministic procedure to let the agent know the result. And another small benefit of like having programs like Python or share script for the skill is that um running the script um does not cost you any token. Well, if the agent tries to figure out the result by keeping thinking and costing context, it cost you a lot of token usage and okay, next I'll hand over to Hal for the um performance evaluation and atbench. &gt;&gt; Uh yes, let me share my screen. &gt;&gt; I guess I first need to stop my sharing. Okay. All right, I think people can see my screen right now. Um, so coming next, I'll cover the evaluation part of the brief train framework. First on correctness, then the training throughput and finally some agent side matrix. Um to start with we first validate the training of piff train on queen 3b3b across four h100 nodes with pipeline parallel of four and expert parallel of eight. The full config is presented on the left. We train at billion token scale for this correctness check. Um as you can see on the loss curve next to it we have megatron in red and piff train in green. they overlap for the entire run. And beyond the loss curve, we also ran some downstream evaluation throughout the training. These are some standard pre-training benchmarks including OBQA, window, arc, arc, palace fog, and pickup. Um, as you can see, the two frameworks track each other closely. So for throughput evaluation our setup spans across Nvidia H100s and B200s with infinite band for cross node communication and every configuration here exercises more than one parallelism dimension at once with PP and EP both greater than one. From the table you can see across the two GPU types and all three models including GPT OSS Queen DB3B and DCB2 light piv matches the performance of Megatron and in the one case where it trails a little bit it's like 126k versus 124k almost identical. The way we get these training throughput out of around 11k lines of code is first via a fivestage decomposition of each layer whose execution is scheduled with the dual pipe vi. Um to give proper credit here this builds upon the deep6 pipe for the orchestration with the vet variant from the CI lab. The core idea is that for each movie layer, we can split its execution at the communication boundaries. And in this diagram, the yellow rows here, they correspond to the communication, which are the dispatch and combine all tools. And the blue rows here are the computation, including things like attention, expert, MLP, and others. By breaking down into five different stages, this finer granularity allows theuler to interleaf different micro batches and overlap the communication with the compute. Um, here is an unsys profile trace of one forward and backward pass. The agent was able to like capture this on its own using our inreo unsys profile skew. The top string runs the compute almost continuously and the string below runs the dispatch and combine all to all. The key thing to see is that the two runs at the same time. So the exposed unhidden communication um remains very small. That's the overlap behind the throughput numbers on the previous slide. So while DupiV is the main optimization for this MOE training, it sits on a stack of otherwise standard techniques including um say a full graph torch compile on everything except the MOE because it's like diamonding shake involved. Um we also have the W grad delay so we can launch the backward of dispatch as soon as we have the input gradients of the expert MLP. um we have the token dduplication before EP dispatch and we have some custom fused kernels in various places of the training paths. So my point here is that none of the technique is novel on its own but together they help bring a pure Python framework up to the throughput parity. So, so far we have covered the training efficiency side of piv train, the throughput results and the optimizations behind them. It corresponds to the left side of this figure. But training efficiency is only one axis. The right side is the second axis we want to evaluate um agent task efficiency. In other words, how efficiently a coding agent can understand, operate and extend the framework on its own. Intuitively, our agent native design principle should be helpful here. But rather than just asserting things like a compact codebase is more agent friendly, we wanted to measure it directly in an empirical manner. And that's what AT bench is designed for. The setup here is kind of inverted from a normal coding benchmark, right? In things like S swb bench or human evolve, you fix the codebase and the task but vary the agent in order to score and rank the capability. At bench does pretty much the opposite. It fix the agent and the task and vary the framework. So difference in the agent efforts is attributable. In other words, it's somewhat complementaryary to those capability benchmarks. Um, before we get into the specifics of AT bench, I think it first helps to think about how we as a human work with a training framework in our daily usage, right? Because that's what this task should be built around. And to be honest, our level of involvement can vary a lot, right? Sometimes we may pull up that GitHub page um maybe read some you know command line arguments hunt for that one file like trying to answer whether something is supported or not like a question in our mind we're trying to look at the file and just to find the answer. Sometimes we can run the codebase studies. So for instance um Megatron may provide some training receives and we may just train a model using the provided receipt without any modification of the framework at all. Sometimes we may go like invest all in like extend the framework ourselves. We may like read some um latest published papers and do some modification of the framework in order to test out a new architecture and getting it to train end to end. Atbench mirrors exactly these three levels of participation. It has these three different tiers and the agents roles deepens across them from first a reader um to a user and finally a developer. So in the first QA tier we ask the agent to like answer a question um about the code. Um for instance it can be asked like how is the device mesh built like how does these parallelism dimensions composed together? Um it doesn't have any runtime measurement. Um it's mostly locate the relevant code, read through it and explain the answer using tools like grab and find to navigate the codebase. The output is just an answer to the question we ask. In the second operator and profile tier, the agent runs and perhaps instrument the framework. Um for example, it can capture an profile and run some analysis. It configured the run. It launched the training and it analyzed the results. The output can be some quick report or some training artifacts. In the final and perhaps the hardest tier, we have the new feature category where the agent ports a model architecture end to end against a published reference. Um for example, we have the mixture of block attention. It's a change with the attention mechanism. um where when the agent try to pour this architecture into the framework, they need to um explore the codebase, implement the feature, test the changes and debug any errors along the way. The output should be a verified working code integrated into the framework. Specifically in our setup, we use cloud code as the fixed agent and we use opus 4.7 at its extra high level of effort. We run each task three times independently and report the median res uh the median of the metrics. At this extra high level of effort, we found every task passed on every framework. So we can have a clean comparisons that focused on efforts alone. So here is the result from the first level of category on the QA tier where we have cured 12 different questions focusing on topics related to a training framework including say how does a config gets propagated, how does a kernel gets dispatched, how does a rope gets implemented inside the codebase and many others. For each one, we report three metrics per framework where lower is better, including agent terms, per turn context, and the output tokens. We never report the work clock time for a QA. Um, that's mainly because each answer only takes about two to three minutes to complete and the server load can vary between the runs. Um, using the physical work clock time will be inaccurate in this category. Um, across the 12 different questions, the agent uses up to 67% fewer terms on PIF train than on Megatron with output tokens dropping in the same way. Torch Titan is usually the second closest to us consistent with its PyTorch native design for rapid experimentation. So these results come from the two principles that we discussed earlier. um first being a compact codebase and second being the absence of implicit indirection both of which can shrink the search space the agent needs to look at so the agent can reach the answer in fewer terms and they can read less at each step. We also have the results from the operate and profile tier where now the agent is running the framework. We also report in minutes the session duration and active GPU time. So we have four different task over here. Um the getting started just set up the environment you have the framework available you need to install all the dependencies and the train and evaluate it drive the full like train the model exploit checkpoint and evaluate like with some LM evaluate harness uh this entire pipeline the collect routing trace um kind of instrument the training and dump the per token expert ids and the gating weights which can be helpful if we want to track the expert behavior over Okay. And finally, we have the report heavy kernels tiles which profile a run against NCS and report some long-lasting koda kernels. And we found that piv train is the best line in nearly every row on getting started. For example, the agent turns drop from 88 to uh drop from 88 on Megatron to 26 on PIF train and the session duration drops from over 40 minutes to under seven. In aggregate, the agent terms fall up to 70% versus Megatron um and 57% versus torch titan without the tokens dropping even further. Um I think an interesting tale from this is the report heavy kernel task where the agent can invoke our enriois profile skill on its own running a fixed verifiable procedure instead of pro instead of building the profiling workflow from scratch. Um, finally we have the results from the new feature task where this is perhaps the hardest one. The agent needs to port a new model architecture end to end into the framework. And to keep it balanced here, we kind of pick two features that change the attention including differential transformer and the mixture of block attention and two others that change the FFN including DOE and plus. This exercise the full loop of editing, running, testing and debugging. And across this task, the iteration cost measured in active GPU time is up to 44% lower than Megatron and 64% lower than torch titan. We found the cause is often times where the errors surface and how many training reruns the agent spends recovering from these errors because each g each rerun they cost GPU time and the patterns we found differs by the framework for instance and there are two patterns um first the agent sometimes manually added the CLI flags that can collide with an autoderive transformer configs it's hidden in the argument registry. That's kind of an implicit indirection where the conflict isn't visible when the agent first reached the code. It only you know surface back to the agent when they launched the training and it crashed. Um secondly, we also have some C++ modules like transformer engine integrated into Megatron and when the agent try to modify its calling, they can throw some opaque sack faults. um instead of having a Python trace back that can directly ping down the errors, this opaque error message, it can push the agent into speculation like where does this error occur and sometimes the agent may attempt to propose the fix that is not directly fixing the error as a result of this speculation on torch titan. uh some of these reruns we found are commonly related to out of memory debugging which is a runtime property and not a matter of code structure. Um a fair question to ask here is like how much of this work is just compactness and whether the skills could help on their own like skills are perhaps one of the four principles that we can ablate very cleanly because they are self-contained files and we can toggle them on and off against an otherwise identical codebase. So we run the ablation here on the W grad delayed commit with two different skills on the first validate correctness and the second capture uns profile. This kind of mirrored the natural follow up to run optimization where we validate the correctness first and then profile the performance. We run each task three times with the skill and three times without it. When it's off, we remove the skills from files everywhere including the working tree, the gift history. So the agent has no access to them and can't recover the procedure. So from the table we do see some expected results. The active GPU time stays flat. Um I think here the nature of the task will kind of pings down the fixed set of training runs. So the GPU work it doesn't increase depending on like whether the skills are available or not. But the agent side metrics like including pertain context and output tokens as well as agent turns if you take a look they drop very sharply. Um on validate correctness for instance the agent turns go from 114 down to 34 and the output tokens drop from 30k to roughly 11k. So with the procedure in the skill the agent runs a fixed plan instead of rederiving [snorts] one by trial and error. And as a result, the same GPU work gets done with much less agent effort. To make the whole things concrete, we let me walk through one case in detail with integrated mobile where we specifically break down the agent effort by its action category. We look at the transcript and we classify like different actions. There is one little caveat here. Um we do omit the thinking tokens from here because the cloud code redacts them away from the transcripts. So the analysis here pertains to only what's visible to us. Um the left panel shows the output tokens by activity. For instance on editing piv train uses 4.7K tokens compared to 13.1 tokens for Megatron and 22.2K for Torch Titan. The change stays local so less code is needed to land the same feature on exploration. For instance, Megatron spends 10.2K tokens navigating the codebase versus 2.2K for PIF train. That's the compactness and no indirection effect. The right direction. We also plot the pertain context over three independent sessions for each of these framework. And as you can see, piv train in green stays low and flat throughout. So that concludes everything we have on the evaluation part of the framework. I'll hand back now to Rahan for some of our future work and the broader discussions. &gt;&gt; All right. Uh let me share my screen again. Um I guess it's mainly uh some open discussions that we think um is very interesting to uh the community. Um the first one is the importance of um agent task efficiency that we introduced. Um we really want to know like how how much do people really care about this idea. Uh one thing we know is that um industry people are reach um in industry almost no one will actually worry a lot about the token usage. They even encourage people to use more token. So um if if completing a task uh saves you some token that's not uh very appealing to industry people. Um the second point is like um we show some 80 differences between pith train and other baseline frameworks but the differences we saw um are not significantly large enough. For example, um we have a 10 time more than 10 times smaller code base than other mature production frameworks. But um our agent task efficiency if you look at the numbers we are not say same 10 more than 10 times better than them. At the end of the day it could be say completing a task uh on p train takes two hours and on their framework takes like three and a half an hour three and a half hours. um that's not um significantly large enough differences. So uh this is also um something very interesting. And uh the third is that we all know that the agents are keeping evolving. Uh they might be even better say in another year. So um right now we can see some differences it differences between different baselines but we don't know what the case would be after one year. It might might be a case that okay agents are even more powerful at that time and there is almost no observable at differences at that time but it could be it could not be that case as well. So we don't know the result. Um but on the other hand I think one thing we have been uh observing is that uh the AT thing is becoming something that Asian people may may make may make may um just like uh for example like in this uh screenshot this is a pull request from the magatron that adds uh experimental mariton light as a genic explorer. operation. So, uh they do want to have some also build something um for agent uh more friendly for agent and at least this can show that um at is getting some getting some attention from the industry people. Okay, for other discussions, uh the first one would be um as how introduced in the ablation section, um at is still not crystal clear. At this moment, we can only uplate the agent skills. But uh what about the other principles? We really can't tell um say how much do the compactness contribute to the result because if we want to uplate that we kind of need to add the codebase from say 11k to over over 100k and there is no way to add code that can uh add code eloquently and then do the evaluation. Uh so uh we still need to uh I think we need efforts from the community to try to understand better get get a better understanding of like uh how do each of the factor contribute to the AT and also um in this talk in path train we named four different agent native design principles but these are not everything. uh we do believe there are other factors that are um also very important and can contribute to AT uh and also this needs uh effort from the community. Um moving on um the task selection in AT bench is something very interesting because we kind of need to in at bench we need tasks that are not supported by all the baselines. Um the reason is because if say I want to evaluate a task evaluate a framework on a task where the frameworks actually has already supported the task then it makes no sense to to use the task. It's unfair. So um this means that the AT bench itself also needs to evolve across time um because some of the existing task might be um might be outdated uh say when some framework supports it. So we need to keep um having new task and removing some some of the old tasks. And uh lastly um the development of pit train is saying that um we are not freezing the codebase right now. Uh the pit train also itself needs to evolve. Uh the codebase we know always grows and maybe it will become say two 20,000 after two months and even larger. But the only thing we want to make sure is that we want to control the growth of pit train and we want to make sure that it grows in a way that like aligns to our design principles. So uh this concludes the discussions and finally um this is like the whole thing of pit train and atbench. We built pitch train that is built around for different agent native design principles and uh it delivers dual efficiency. Uh we also have at bench to evaluate the agent task efficiency and here are two different traples. So one for pit train and one for at bench. And finally a huge thanks to all the collaborators that make all of this happen. And uh yeah so that's it and that's everything and we're happy to take any questions. Heat. Heat. [music]
