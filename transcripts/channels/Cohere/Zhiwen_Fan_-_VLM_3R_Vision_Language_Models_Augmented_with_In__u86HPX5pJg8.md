# Zhiwen Fan -  VLM 3R  Vision Language Models Augmented with Instruction Aligned 3D Reconstruction

- 영상 링크: https://www.youtube.com/watch?v=u86HPX5pJg8
- 채널: Cohere
- 업로드일: 2025-10-27
- 자막 언어: en
- 단어 수: 약 8231개

---

## 스크립트

So um yeah, thanks for the kind introduction. My name is Jan and I'm currently an assistant professor from Texas&amp;M University. So today topic is about varian language model augmented with the instruction align 3D reconstruction. So the motivation of this work is kind of clearance and symbol. So how to enhance uh the 3D perception capability of brain language models? Yeah. So let's take an example. So that's uh this is uh uh how current language model doing with a 3D perception say giving the monocular video at input and uh having the language instruction. So we will have some questions and we hope the language model can answer uh our question. uh some models we do see many many uh concurrent work that let's say from uh from 3D RM and the 3D VRM they are either use the additional depth sensor for each input image or use the pre-built 3D map just like this to first build the 3D model from the monocular video and then build the 3D input to the lamp model. Yeah, that's how they works. But there are two point two questions. Say if we are requiring the depth sensor for every video, it's not scalable because for most of the web video, we don't have the accurate metric deps and we also don't have a global map from the monocular uh from the monocular video plus the dep sensor if we uh rely on this side. And if we are using the algorithm let's say start from motion or slam to first build the 3D map 3D point uh global 3D points from the mono video and then tokenize the 3D point into the language model it's also variable but the point is traditional 3D reconstruction is very very slow say if we have a room size uh environment to do the reconstruction we require say roughly several minutes to several hours for the reconstruction using say uh 100 images. That's a typical case for the 3D reconstruction. So both of the directions are not that scalable. So it is possible to pursue a scalable way to enhance the ring language model with the accurate 3D perception. That's a key idea of this uh work. So let's take a overview of the 3D perception and reconstruction pipeline uh uh in the view of traditional algorithm. So give give the multi- view image at input just like here we we have two images as an example and our goal is to build the three semantic 3D map just like this. Okay. So we first do feature extraction from the input images and uh after the feature extraction we got the the correspondence from uh this input images and we do feature matching. So after feature matching we can estimate the camera pose the relative camera pose between these two and we will know where we got these captures right and subsequently we will do the calculation and sparse reconstruction to get a a set of uh sparse 3D pawn cards and with this 3D uh pawn card then we we will perform dense 3D reconstruction sometime we call it as stereo matching or uh 3D called spreading reconstruction something like that all nerve and finally we will perform semantic understanding because due the dense reconstruction alone do not provide any information about the semantics and instance so that's why we will use some semantic understanding model upon the the build dense 3D model uh and finally we will use this semantic 3D map for many many downstream tasks like say the robotic navigation autonomous driving and this making. Yeah. So that's a very typical procedure from draw images to semantic 3D map. You can see uh there are stage wise and that's why the arrow will accumulate from the first step to the left because it's very difficult to calibrate uh uh if they are non differentiable and this long pipeline also make the real-time performance infeasible. So we have multiple module and they cannot communicate with each other and they also very slow. I mean for each step they cannot make a real time pipeline and if we stack them together uh we we require say minutes to hours uh of postprocessing to get the 3D map. So that's a question and overview. So let's first take a look at look at uh how to build a 3D uh 3D map and um we can first review at the multi- view geometry which is a very very typical um way to get the the dense 3D map or perform 3D uh dense 3D reconstruction and the material geometry is also the foundation for many many 3D reconstruction generation and understanding. So imagine we have this physical physical world is uh in the real world and it's in the real world and we got two captures from view point one and viewoint two. So having the physical world and we got these two capture we have the two images and we want to rely on these two images alone to recover the 3D geometry. So we have to do some assumptions. That's the first assumption that exist in uh the real world is for a single 3D point like here when we project onto multiple image plan like this and this and if we track the cross view consistency so we can recover the 3D information from images. So that's a basic assumption and that's how we use 3D reconstruction to recover the uh 3D model from image input and uh we will first introduce a work from um new last year to uh introduce how we can make the 3D reconstruction into a differentiable model and then discuss how can we align the encoder the 3D encoder with existing reality model. So the motivation of um my all my previous work is very clear because we are living in the 3D Philip world everything around us say ranging from autonomous driving robotics and extended reality we rely on the geometry and spatial relationship to make decisions to make the interaction with the physical world. So that's why uh we have to allow the computing system to first being aware about the 3D per 3D perception and then make interaction with the the physical world and uh if we take a look at how real world um works let's say we have the static 3D environment like the building like the house and we have some dynamic subject like human like the moving vehicle and human the observer will have a camera and at the different angles we take these captures uh so there are 2D images or you can think that's a monoccular video so with this video we can recover the 3D and the the motion and with the the recover the 3D information so the sensors and devices let's say the mobile the mobile phone the extended extended reality headset and uh drone and the robot automoting many many sensors that have to rely on the perceived 3D information and then interact with the human. Yeah. So that's uh t uh how it works in general and the traditional mult geometry say because we don't know the depth information. So here we will make some assumptions right. So after making the assumption we will trying to estimate let's say if we want to estimate the depth map of the of this image. So we need to estimate the probability from this depth plan to the last depth plan. So because we don't know about in depth depth information we will assume some depth hypothesis and then make uh the prediction. So as we have introduced uh in the previous slides. So we will check the mult view consistency um in mult view geometry uh maybe 10 years ago. So people trying to start to use the convolutional neural network uh to war from the left image to the right image and we will find the cross view consistency. So we call it as warping and we will build something called cost volume. Say uh this is x and y dimension and we also have the assumed all depth hypothesis uh in this axis. So we don't know the depths. So that's why we have some assumption. Say here we have five depths depths plan and after that we will make the estimation of each step depths probability and uh uh the MESNet also propose to use not only the raw pixel but also use the uh the feature feature map from the lat layers but let's first look at uh uh how the dense 3D map can give us so this is from my previous work five years ago So we designed a new cost volume about the uh based on the MS stat. So with this new cost volume formulation we can first make the 3D reconstruction for city scale uh reconstruction. Yeah. So here you can see we have images of this city and we build the 3D map the pawn cloud of this city and it indeed carry a very rich information. uh we have the pawn cloud for every subject we are interested with. Yeah. So this work is called casket cost volume and uh because it is uh with a high efficiency so many subsequent work uh we adapt our uh cost volume formulation to their application. Yeah. So that's a introduction of uh uh a very earlier of my previous work. But let let's jump to uh this large spatial model work which is published in the new last year. So the motivation of this work is so is how can we unify all of these steps together into a single differentiable neural network right so here we have the input image and our goal is to uh obtain the dynamic 3D map for downstream tasks. So how can we use a transformer model to do them into a unified work a unified model. So that's a high level motivation and our solution is we can use a neural network as backbone as a prediction of the 3D 3D map and use neural rendering as a objective function. So here is our solution. Give the raw images. We will use a transformer model which is differentiable of course and we do the uh 3D reconstruction. We build a 3D map like say 3D cing spreading. So we regress the 3D causing attributes and then we will render at another viewpoint say the viewpoint two. So we flatten from 3D to 2D and then we will compare between the rendered image and the observed image. We will compute the difference between the pixel intensity and we will up update the model. Yeah. So here is the workflow give the first input images and we have the neural network. We have the transformer to predict the 3D model and we flatten from 3D to 2D uh at another new unseen view and we will compare the difference during the training to optimize the model. So here is a point this way we only we only need to antidate about the camera pose. So how we transform from the raw images to the view too and uh with in this way we can update the model very efficiently because we only require the annotation of camera pose the relative camera post from the here to view two. Yeah. So which means uh if we can build the this system ideally we only need uh sparse camera post andation to uh make the model to predict 3D and uh here is our solution. So first we have stereo input uh two images from different angle and we first initialize our geometry from a previous paper called dust 3r and uh there is a subsequent work I think most of you will be more familiar with vgt uh vgt when the paper uh in the year and and I think it's a subsequent work based on the duster yeah so duster make the prediction from uh raw pixel to depths map. Yeah. So here we use it as a geometry initialization and we have a second phase. So we use a pongwise aggregation or pong transformer. So we aggregate uh in a local in a local way. Say we are not focusing on the interior uh uh uh inter say we just allow the model to focus on its neighbors. So we have a hierarchical design about how to do this aggregation based on the pawn transformer and then uh we convert from from the pawn pawn map to 3D goins because uh 3D goins require a more detailed representation. So we have to represent the shape of the ellipoise and uh many many attribute many many other attributes say its opacity and its scale rotation. Yeah. So having the regress 3D we resize at another view or we flatten from 3D to 2D and uh we have a predicted target view and we have the control target view and uh we do a comparison with back back propagate the loss. So in this way we can make the uh 3D prediction or 3D reconstruction into a single neuron network differentiable neural network. So we have a initialization model and we have a pawn transformer based aggregation model. So combine them all which is differentiable during our training. So uh we can um use the differentiable rendering as a objective function to update the model. So so far we have introduced how we can build a 3D model differentiably. So you can imagine give the moninocular video from the user and we can have it's 3D map but now we don't have the semantics about the the input video right. So ideally if we know the semantic information or instance information from the monoccular video which will be definitely very helpful for the downstream 3D perception. So the next question in our previous work still in the large space model we ask is it is possible to track the cross view semantic consistency to extend from 3D reconstruction to semantic 3D reconstruction. So that's a second question in our work and we have the solution. Yes, we can check the cross view semantic consistency and here is our solution. Again you can see in the upper branch we still have the geometry initialization and we have the home transformer to do fusion but we have another branch we have a multimodel 2D model here. So say we can't because it's very difficult to enter the every 3D 3D uh scene because uh annotating in 3D space is very very expensive. So we rely on the lifting technique say we have so many uh 2D function models like the clip like the language based segmentation. So it is possible to lift its feature from 2D like this to 3D and then we can build the semantic 3D gaussian in that way. That's our motivation and the indeed way it send the 3D gaussian to 3D semantic dosing. So we have the additional feature vector for each 3D gausing. uh so it's very straightforward and uh so here for each feature vector it is derived from the 2D ve 2D feature maps and we have the pawn pixel align prediction say every pixel we will predict the 3D pawns and each 3D pawns we have the feature vectors yeah so here we again we flatten from 3D to 2D but now we are not only obtain the images we not only have the pixels we also reserize the feature map at a third view at a time to view and we optimize based on the feature difference and the the pixel difference and optimize the model in a differentiable way. But here we we will frozen the multimodel 2D model like like the cape video encoder we will freeze it and we only update it on update the geometry initialization and cross model fusion and let's take a look at why we do this kind of fusion why we do not just use the perview prediction so here is our answer say if we process the image one by one because everything is in 2D so we will see the problem let's say if we do no senses we will see the inconsistent prediction so the frames will flickering and if we can lift from 2D to 3D we can aggregate them and make a consistent 3D rendering just like the right way in the right red video so here is our goal so trying to use the cross model fusion to make the inconsistent 3D to be consistent Yeah. So here is a is a with a little bit details of our work. So again with the inputs we first initialize the geometry and we have per pixel prediction about its appearance and geometry. Here we tokenize it's the appearance tokens and we we initialize the appearance token geometry token and in the lower branch we use a patron of visual encoder say the visual encoder from clip or set both works and we to we have a tokenizer we tokenize the feature into a set of 2D tokens. So having these three set of tokens we do the aggregation. So we will compute a semantic to 3D attention. We will we will trying to fuse semantic information semantic feature uh into the appearance and geometry. And uh here we augment it with the uh we augment it as uh uh as semantic aware features. And finally uh we will render at a third view. So which uh can help us to formulate the loss. So the key level the key idea of this work is very straightforward but uh very helpful. we just have uh the geometry and the appearance initialization and later we uh try to fuse the operation to the encoder and make the feature to be semantic semantic aware. Yeah. And during the in &gt;&gt; Yes. &gt;&gt; Uh could could you uh give a brief description about the tokenizer that you are using like is this a kind of a 3D tokenizer? Oh, so you can imag you can think of this is just using convolutional kernels. We have one layer convolutional kernels to make uh to tokenize the 2D features into these tokens &gt;&gt; and the kernel is learnable. Yeah. &gt;&gt; So we okay so you are dynamically okay doing that. &gt;&gt; Okay. Cool. Cool. Thanks. &gt;&gt; Yeah. just flatten the 2D to 1D token. &gt;&gt; Okay. &gt;&gt; Yeah. &gt;&gt; Okay. So, so essentially, so essentially you are training the tokenizer as well during the training process. &gt;&gt; Yes. &gt;&gt; Okay. &gt;&gt; Because this is just one layer convolutional kernel and uh again um we flatten from 2D visual feature to 1D tokens and fuse it with the previous tokens. Okay, thank you. &gt;&gt; Uhhuh. Yeah. &gt;&gt; And for the uh for for converting the inconsistent 2D to consistent 3D, you're using gshian sphere. &gt;&gt; Oh yeah. So so we use the 3D ging as a basic replication and and we introduced in the previous slide is about the cross model fusion. &gt;&gt; Yeah. After cross model fusion we will predict a set of semantic 3D lossing &gt;&gt; here &gt;&gt; it's by regression say we use a model to do prediction and it's not the traditional 3D going because traditional 3D going is obtained by optimization attitive optimization &gt;&gt; yeah I think so it's a 3D aware visual tokens that you have like the spatial view of uh fusion that you create. &gt;&gt; Yeah. So you need to we we will allow the &gt;&gt; um not only the 3D map 3D prowl 3D pcloud prediction but also &gt;&gt; we have a more detailed 3D goen and the semantic feature perception. &gt;&gt; Yeah. Which means uh the 3D map will have semantics not only just the visual and uh the geometry. Cool. Okay. &gt;&gt; Let me move to the next page. So that's why during the inference time so our work uh can do um real almost real time inference say give new images uh an images in new scene and we have a transformer model to predict the semantic 3D map and we can render not only the RGB map the depth map but also the feature map at the new view say gave the image we build a 3D model in a feed forward path we can do do rendering and with the feature we can also enable some downstream tasks let's say the open vocabulary semantic segmentation so here it is how it works so define the open vocabulary uh say uh we have some categories that we are interested with we can describe it and we use a clip text encoder to enc to encode the uh texture input into embeddings and we will compute the uh text to visual uh similarity and then we we can use the decoder from clip to decode the semantic 3D map. Yeah, just like this. So which means having a single neural network we can build the RGBD with outpost and we also we have the 3D semantic 3D perception capability. Yeah. So we don't during this process we only require the camera positation and we don't we even don't require the semantic annotation because we are do doing the lifting from 2D to 3D. Yeah. And overall you can think we require sparse 3D data just a camera and we build this machine learning model and we use neural rendering as objective. We collect a large scale data. So we train one model we can allow the model to generalize in a zero shell setting. So all prediction here is zero shell setting. So just give images we our model can predict the 3D map in roughly uh 12 IPS. Yeah. So which means we make the uh semantic 3D perception to be near real time a new semantic encoder. Yeah. So that's most likely of uh the first work um before we introduce the BMS3R because I want to give some context about how can we make the end to 3D perception from images or from monocular video and uh next um we do see a lot of concurrent work from many many different institutes so this work called think in space developed from Sing and talk. So they build a benchmark to evaluate how the large language model multimodel language model that can see in 3D remember and recall in 3D space. So here is their demo. So having the monoccular video at input let's say a room and we can define some task let's say the object counting. So how many so fast and the the agent the mult modeling model will think it will u make the prediction let's say four and they define roughly eight tasks and um yeah so each task is carefully selected and pl the data so they have this data and then they they were trying to evaluate the existing mult model language models so this is the overview of all of these tasks. So they define a task task uh they first they define a category of these tasks and uh they will have questions and they define the answers. Yeah. So um this benchmark is called VSI bench video spatial intelligence and uh I go say this is probably the uh one of the earliest work that focusing on studying the uh video spatial performance let's say based on monocular video without the requirement of depth information and but the point is let's look at their performance from this paper. So this is a human level in the outside and if we look at the Gemini and GVt but roughly that's that's about one years ago. So you can see clearly there are significant lag behind the human performance right if we look at the uh this this figure. So I mean among almost all of the benchmark from appearance order root plan to some uh uh metric types estimation like the room size absolute distance the foundation model all like behind the human performance. So that's a uh status of the development of foundation model for 3D perception. So we developed the B um augmented with the 3D reconstruction and or we call it as BM3R. So the idea is kind of straightforward. So having the molecular video we have the visual token from cliff. We also have a special encoder to decode the to have the geometry token and the camera token. So we fuse them together and then we preserve the dimensionality of existing V language model and we fuse them to language large language model and we do a lot of fet tuning. So that's a high level idea and let's let's look at the diagram again. So having the question and having the monocular video as input. So previously we only use a video encoder say it's either clip or dino v but it's very diff difficult to allow them to perceive the accurate matrix scale depth map right because they are never trained with the the depths as the objective function. So so but we have we introduced another spatial encoder from also another concurrent work called cutter 3R from uh UC Berkeley and Google. So they can uh export the geometry token and the camera token and with these three token we fuse them together. We do a 2D 3D fusion and this is how 2D 3D fusion works. Again just a very simple cross attention uh operation and we can fuse the two 2D uh 3D information into the 2D tokens and make the uh token to be 3D aware and we have a projector to align the dimensionality before feeding them into the language model. Yeah. So because we are trying to preserve all existing information from uh the visual part from the language model and we only want to introduce a ready to learning from the geometry token from the camera token. So the geometry token represent the overall 3D structure of this environment and the camera token represent where is the current camera or you can think this is a camera to camera post and uh geometry token is about the 3D structure of its input. Yeah. So with this uh high level design Yeah. Go ahead. Yeah. Um sorry to interrupt. Um I can see here &gt;&gt; Okay. I can see here you have um geometry tokens and you have view tokens. &gt;&gt; Yes. &gt;&gt; Are they is the view tokens? I'm guessing the view tokens corresponds with the camera tokens. Yeah. It's the same. &gt;&gt; So you mean this V token here? &gt;&gt; Oh. Oh, okay. Oh, okay. Okay. So the video token is just like let's say uh for most of the uh the lava like model we have a clip like video encoder to tokenize the image wise information but they are doing the 3D uh they are doing the passing semantic passing based on only semantics and they don't have the per pixel wise uh perception about the depth about the spatial relationship they only have the image wise understanding Yeah, let's say the clip, right? So that's why we the motivation of our work to introduce another special encoder is so we want to have uh the per pixel extraction about the depth information and uh we ideally we rely on a special encoder that is pre with a web scale data and uh let's say based on the existing model called duster so we can extract the geometry token and camera token from it. Fair enough. &gt;&gt; Yeah. So which means this branch is previous. Go ahead. &gt;&gt; Um so uh in this highle pipeline um is the cross view semantic consistency coming into the picture? Um is it enforced separately or everything happens um automatically like how do we maintain crossview semantic consistency in this &gt;&gt; automatically? So this is the short answer. If we going to uh assume more press it require extensive poster training. So that's why we hope to align everything automatically and we add a residue. Let's say after the fusion we have we need have a residue because we only need one up only want to update a little bit about the previous learn lat space. Yeah. So we only have this fusion and we also have a residue to uh to allow the model to update itself. &gt;&gt; Yeah, &gt;&gt; thank you. &gt;&gt; Thanks for the question. So yeah the hello idea is kind of straightforward because we are augmenting the video encoder with a spatial information from operation 3D encoder and uh then with this formulation we will have 3D perception capability of the existing language model. The second part would would definitely about the data because the data is a field. So how can we build the scalable engine for the data creation from the existing real special uh data pairs? So first we rely on existing data with both RGBD and semantic annotation. So that so because they have a very clean annotation. So we rely on the scanet and scann plus and ark scene as our data source. So first for each scene we extracted its pong card and we also have it RGB video and we first do a sampling from the RGB video because uh we have a limited context window. So we sample about 32 frames from the input video and we obtain uh RGB depths mask and the camera pose and we also obtain the camera pose for each sample frame. So we will have a processing pipeline. So for for each scene we will have a scene metadata. So we will generate the room size the center the object count the 3D bottom box that's about the whole scene description and we will also have analogies for this antation for this data engine process. So we uh entered it frame ID, we entered it image pass, we have the camera information, camera pose and the 2D bound 2D bounding box of the v visible part of every instance from uh in 2D say project the 3D bound box and with the mesh. So we can define which pixel in the current frame is visible and having this proc sensing pipeline we uh build a spatial temporal synraph because we want to extend from the visual spatial intelligence to visual spatial and temporal intelligence. Yeah. So that's why we build a semantic uh sem uh you can think it's a semantic spatial temporal scene graph and uh we connect uh each frame uh together to a unified representation. Yeah. So let's say we have 32 frame for each scene and we we build a sing graph uh based on all input data and we create the query data and I can say many many uh friends from meta from Nvidia they are asking about some detailed annotation process of our script because uh we released the curated data and it's the only open source data that align with the B uh VSI bench uh benchmark. Yeah. And another part is about the root planning because uh in VM in VM3R we build a large scale uh training data side but the point is so there is a task that from VSI bench called road planning just they entered the road plan by human they asked their authors and some volunteers to enter the uh road planning. So we de develop a scalable way to use a simulation simulator by habitat to enter the uh root plan. Let's say define the define the task from one point to another point and uh we plan the root in a textual description automatically. Yeah. So we uh up we scale up the process and we create a large scale data about the rule planning data just like the brain language navigation work but uh here we build a video based um model to process the task and let's jump to the result page I think uh this is maybe the most important part to describe about the result of our work. So if we directly look at the last row, this is VM3R 7B. Uh so overall you can see the average score is about 60 and this is um a close model JPT and Gemini that's roughly half years ago when we derive this table. So we can see clearly we rank the first uh than all existing open source model and closed source model and but of course still there is a gap between the human level perception. So but we I going to tell uh some some uh information based on the resulting uh table. So you can see this is a 7B model. It first outperformed not the uh similar size model but also outperform the uh large scale model say the 40B and the 72B and it also surpass the closed source model and I will explain detail with the A study later but let so the takeaway in this page on visual spatial intelligence uh benchmark so it works it works better with the poster training and the architectured new architectured And next uh wait yeah go ahead please. &gt;&gt; Yeah thank you very much Zen. &gt;&gt; Um so I saw that um VLM3R was um 7 billion parameters. &gt;&gt; Yeah. &gt;&gt; Um do you have other models of other different sizes? Did you observe what the effect of scale on um the model performance and stuff like that? give you any avation. &gt;&gt; Oh, that's &gt;&gt; go smaller than that. &gt;&gt; It would be nice if you had a smaller even smaller model than seven billion parameters that could do almost as much as this would be nice. Do you experiment with that? &gt;&gt; Yes, thanks for the question. So the short answer is we only tried 7B due to the resource constraint because we most rely on the uh resource from our school to perform the experiment and we only performed the 7B experiment and currently we extend it with the uh with a 3B and some new models but we haven't released it or we we haven't updated the archive archive paper. So the short answer is we will prove so it will generalize not only to 7B but also to a smaller model. Yeah. &gt;&gt; Thank you very much. &gt;&gt; Thanks for the question. Yeah. So the next part is we're going to extend the visual spatial intelligence benchmark to visual spatial temporal spatial uh temporal intelligence. So we we add a keyword about the temporal. So the temporal here generally means so how frames moves ac across the time dimension say can we allow the model to track the um to track the dynamics about the camera motion. Yeah. So so we define a new benchmark called VTI. So we have the camera related information about the camera distance displacement and camera moving direction and we have the camera object relationship. Uh it's about its absolute distance and the relative distance and we also have the define object object object relative position at different frame. So you can see an example. So we have the question about how things how things change between the frame frame A and frame B and uh something like this. So we define the new benchmark and u we call it as VSTR bench. So here is the short answer. So indeed VMS VMS3R scale with not Yeah, go ahead please. Yeah, apologies one. Um, &gt;&gt; so um I I just want to see if I understand um the benchmark a bit better. So essentially the same benchmark is made up of three parts. Um the camera to object, object to object, it's the exact same benchmark. &gt;&gt; Yes. Uh that's the same benchmark. Previously uh the during the question they don't have the frame indification say they don't specify from one frame to another but here we are we have the uh we have the definition about how frame changes and we have some question related to camera camera object and object object. &gt;&gt; Okay. &gt;&gt; Thank you. &gt;&gt; Yeah. So that's why we call it as temporal because this is monoccular monoccular video and the the frame change means the temporal change. Yeah. So this is the short answer. So VM3R also outperformed the existing model uh not only the open source one but also the closed source one about the temporal awareness. And let let's jump to the application study. So we need to find the uh the data and the architecture change very effective. But let's take a deeper look at how things works. So in the first row that's a baseline model. The baseline model directly with the post training using our data. So we can immediately see it already uh surpass many many previous work say here. So if we don't so if we don't tune the model uh the performance is roughly uh 20 35 but if we use our data our curated data pipeline to tune the model so it directly jump to the uh 57 is a significant jump and uh if we include the geometry token so here the without camera token means we only encoded the we only encoded the geometry token. So you can see with the geometry token we further boost the performance a little bit and if we include the camera token we again we improve the model slightly. So combine them all we achieve the performance at roughly 60%. So the takeaway in this ability study is first the post training is the most significant part. So it's a data hungry work. So as long as we have data we can make even we make the 2D model to be to have a very strong performance on this 3D tasks. And the next conclusion here is if we have the camera encoding about the geometry encoding we can further post both its performance uh roughly at three points here. Yeah. From the first row to the last row and both camera tokenization and geometry tokenization works for our work. Yeah. Um so this is most likely about the VM3R. So which we already take the conclusion here. So spatial intelligence is also a data hungry task. So that's why it is possible to create more data but not only specified with the say the indoor RGBD semantic data which is highly inscalable. So here we going to briefly very briefly introduce my last work uh just three pages. So we have the images uh let's say this is the v scale images and we have very very limited 3D models. So how can we rely on the human video because human video is indeed 4D data we have dep uh we have 3D information and we also have the temperature change. So how can we create the the 4D data from the the in the wild video say from YouTube. So here is our new very new work we call it a dynamic verse which is published in Europe this year. So we have a we have a data creation pipeline. Say we combine the traditional 3D techniques and modern uh multimodel bonation model. Together we can annotate not only the depths the semantic the instance information but also we have the caption the seam wise frame wide caption from the input video. And uh here is the overview of the dynamic vers. So in the last year you can see we have roughly 10 uh we have 100k uh data video frames videos and uh we have roughly uh 13 millions of frame annotation and we have a very detailed annotation about in video from many many benchmark data from the most driving data and from uh from the YouTube data. So with this diverse source every data here is from the real world and here it's a visualization about the antigen. So this is from a benchmark data we entered not only the camera pose but also the dynamic actors and the body mask about the dynamic actor and we also have the general sync caption and the semant uh instance wise annotation. Yeah. So that's most likely all my work from uh recent two years. Thanks for every attendee to uh listen to my introduction. Thanks. &gt;&gt; Yeah. Any questions from audience? &gt;&gt; Um so guys are there any questions? Do we have any questions for Zoom? Are there any questions? &gt;&gt; Um, okay. So, um, &gt;&gt; yes. &gt;&gt; Okay. Okay. Aish has a question. Please come off please and share your question, please. &gt;&gt; Yeah. Hi, sir. Like, it was a very great session. So like uh actually I am new to the 3D part of the computer vision and uh for some days I have been engaged to a project which uh for which I have to uh create a 3D model from 2D monocular videos of a construction site. &gt;&gt; Mhm. So like I need some little bit like I I have researched some uh something and uh and try to implement try to implement something like but I am facing some challenge to extract geometrical uh aspect of the those like object like for the construction site and all these. So like I so can you can you help a little bit like for in this? &gt;&gt; So if I understand your question correctly. So you are asking help to enter your data. Yeah, like uh uh like to uh extract the geometrical aspect of the uh data like uh I have a 2D monocular videos of the of some construction sites and uh I need to create a 3D model from that and with accurate measurements of the sides like ball size and floor and all this you Okay. Yes. Uh so but I'm not very sure about are you asking suggestions or asking the &gt;&gt; Yeah. I'm asking some suggestions like to uh uh techniques. &gt;&gt; Oh &gt;&gt; implemented like this. &gt;&gt; Yes. So if you are uh you are thinking annotating the geometry part I would say you first maybe rely on the foundation model let's say the map anything the VGT to first annotate a dense 3D map and use some traditional technique let's say the uh bal adjustment to make your prediction to be cons to make the prediction to be consistent. Yeah, like I have tried the map anything thing but the thing is that it requires the stereo vision data like uh data captured from the like two cameras and I have only uh uh molecular videos like uh the camera rotating 360° videos. So that's the problem that's why I'm asking like uh for any other thing is there. &gt;&gt; So you have to also include the traditional technique to make your concern to align better because the this kind of foundation model make great prediction uh fit forward prediction but you have to align them uh using conditional algorithms &gt;&gt; like can you name some of them? uh just bundle adjustment let's say you can revisit the work from the duster and duster have a script to do the post-processing &gt;&gt; okay like yeah I'll try to this &gt;&gt; thank you thank you very much &gt;&gt; yeah um &gt;&gt; thank you &gt;&gt; yeah has a question I think um are you have your doubts been put to rest um you you've gotten some ideas now that you can implement for your project here. &gt;&gt; Uh I'm sorry. So I have a Can back signal from the Wi-Fi. Can you repeat your question? &gt;&gt; No, no. I'm talking to Aush. I just wanted to confirm that he has gotten some ideas to help him with his project. &gt;&gt; With which project? &gt;&gt; Um I'm talking about Aush. Um but no matter no problem. &gt;&gt; Okay. Yeah. So please go ahead. Yeah, &gt;&gt; I have a kind of a um high level question um with regards to your um VLM3R and the recent work 3D to 4D. &gt;&gt; Uh what are some kind of bottlenecks or challenges that you um observe in scaling like not in terms of like the um in scaling in terms of the accuracy. Um for instance the human benchmark is about 7877 and your models are at 60. Um do you see any challenges or like key bottlenecks that needs to be resolved? &gt;&gt; Yeah I see two points here. So first I don't think dual video encoder is a variable. Let's say currently we have a temporary solution. Say we have two video encoder and we f them together but ultimately we will definitely rely on just one encoder. So which incorporated the uh pixel wise prediction and image wise semantics and f them together say ultimately we don't need the fusion process and we can parse every all the required information from a single encoder and the next would would be uh so still the current feed forward the 3D reconstruction model we can have the test prediction but the accuracy so if we look at the surface. Um, so it's still way below the expectation say so it way below the traditional multi view stereo with attrative optimization. So which means the geometry accuracy is still far behind the expectation and long way to go. &gt;&gt; Yeah, thanks. Um so if you just have one encoder um how does it take care of the camera tokens and all of that which you mentioned? &gt;&gt; Ultimately I think some research team will develop a unified model but maybe by this something like that to &gt;&gt; Oh okay. &gt;&gt; Yeah. &gt;&gt; Thank you so much. &gt;&gt; Thank you. So um that that was actually a very um insightful question. I I myself do not really um I don't really know how a unified encoder would work. &gt;&gt; Um because then the unified encoder would have to essentially do the job of two encoders. You have to learn the normal visual encodings that clip learns and then it would have to learn the camera encoding and the geometry encoding. So, um I think that is actually a nice question to ask, but I suspect that it would probably be a very data hungry project. &gt;&gt; To get one to get one um just one encoder that learns all that stuff, it will probably require a lot a lot of data. &gt;&gt; But that is actually a nice project that I think someone here can perhaps take. I like that. I like that. Thanks for the question. I appreciate that. &gt;&gt; Follow question too. &gt;&gt; Yeah. So um are there any other questions? May any questions from your end? You're muted. You're muted my can't hear you. &gt;&gt; Okay. Yeah, I do not have any queries right now. I really like the framework in in which like uh uh the authors had the parallel processing like they didn't use uh the final 3D point cloud from the cut 33r in instead like you guys intercepted the rich implicit information like the visual encoders that uh used using the parallel processor. ing uh to pro process the video to get the 2D appearances and like uh uh the uh simultaneously the cut 33r the special encoder pro processes the same video and it uh gives you the internal representations. So like uh you have two sets of crucial tokens like the geometry tokens and the camera view tokens and they you guys use those uh to fuse to the 2D visual tokens using the cross attention mechanism and then uh yeah use that in the downstream tasks. So like this kind of whole huge uh the the whole process is kind kind of uh intriguing and very in interesting. &gt;&gt; Thank you. &gt;&gt; Yeah, thank you very much. Um I I actually skimmed through the paper. &gt;&gt; I skimmed through the paper before um before the session and um I found that the paper was actually quite short. Uh but I appreciate you taking the time to give us a lot of context um because the paper is actually built on previous work that we've done &gt;&gt; and just through the paper alone would not have provided all of that context. So um thank you very much for that context that you provided prior to discussing um &gt;&gt; thank you &gt;&gt; once again we would like to appreciate you for taking your time to share your work with us today. &gt;&gt; Appreciate for this chance. Thank you. &gt;&gt; Thank you very much. Um, &gt;&gt; once more, does anyone have any questions for Z1? I I think that there should be more questions. &gt;&gt; Yeah. Thank you. &gt;&gt; Yeah. &gt;&gt; Okay. So, um, in the absence of questions, um, we would like to thank everyone for coming for the meeting. We'd like to thank everyone for &gt;&gt; everyone. Yes. And thank you. Thanks everyone. Uh, and best of luck for ICCV. &gt;&gt; Thank you. Yeah, take care everyone and bye till our next meeting then. &gt;&gt; Bye.
