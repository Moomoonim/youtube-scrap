# [ifkakao2021] Open Water Diver  OpenSource Diver!

- 영상 링크: https://www.youtube.com/watch?v=D6XLD_zgg2U
- 채널: kakao tech
- 업로드일: 2026-06-11
- 자막 언어: ko
- 단어 수: 약 2171개

---

## 스크립트

안녕하세요. 저는 이제 2년차가 된 카카오 커머스에서 주문 백핸드를 담당하고 있는 치즈입니다. 카카오 커머스는 선물하기와 쇼핑하기와 같은 서비스를 제공하고 있는데요. 장애가 발생하면 고객에게 직접적으로 불편을 미칠 수 있는 중요한 서비스이기 때문에 재발 방지를 위해 정확한 원인을 파악하는 것을 중요하게 생각합니다. 세션의 제목인 오프노터 다이버는 스쿠버증의 이름인데요. 제가 이번 장애가 났을 때 원인을 깊게 파워두는 모습이 스쿠버 다이빙을 하는 모습 같아서 이번 세션의 제목으로 정하게 되었습니다. 발표의 순서를 먼저 소개해 드리겠습니다. 먼저 이번 발표에 배경이 되는 장애 상황을 먼저 설명드리고 스트레드 덤프로 장애의 원인을 알아가는 방법을 말씀드리겠습니다. 그리고 이번 장애의 주요한 원인이 되는 소프트 레퍼런스 해시 콜리을 말씀드리고이 문제가 어떻게 해결되었는지 보여 드리면서 마무리하겠습니다. 카카오톡 선물하기 서비스는 한 번쯤 다들 사용해 보셨을 텐데요. 어느 날 선물 내역이 조회지 않는다는 소식을 듣게 된 저는 원인 파악을 하기 위해 나섰습니다. 무언가 새로운 배포가 있었던 상황도 아니고 한 달 동안 멸없이 동작하던 서버군에서 갑자기 에러가 발생하게 된 상황이었습니다. 더 이상한 건 웹로부터 이입되는 트래픽의 변화도 없었다는 점입니다. 다음 사진은 산내 APM툴 네오로 확인한 천불g함 서버군의 모니터링 화면인데요. 파란 점은 정상적인 응답을 준 경우이고 빨간 점은 서버에서 에러가 발생하였을 때 지키게 되는 점들입니다. 사진에 써 있는 숫자는 각각 1번에서 5번 서버를 의미합니다. 즉 각각의 서버가 다른 시간대의 동일한 패턴으로 돌아가면서 한 대씩 장애가 발생하는 모습을 확인하였습니다. 좀 더 자세히 살펴보기 위해 타임스케일을 키워 봤습니다. 해당 사진은 이번 서버의 모니터링 화면인데요. A 영역과 B 영역으로 나눠서 보면 A 영역에서 장애가 쭉 발생하다가 B 시점에서 해소화된 것으로 보입니다. 무슨 일이 일어난 건지 알아보기 위해 각 영역의 로그를 살펴보았습니다. A 영역의 타임라인을 살펴보면 전부 JDBC 개 커넥션 메소드를 3초간 수행하다가 타임아웃이 발생한 것을 확인할 수 있는데요. JDBC 커넥션 풀의 타임아웃 시간이 3초로 설정되어 있기 때문에 일제이 타임아웃으로 실패한 것입니다. 이상한 점은 트래픽의 변화가 없음에도 커넥션이 부족했다는 점입니다. 이제 BC점의 타임라인을 보겠습니다.이 부분도 참 이상한 부분인데요. 외부 API을 호출해서 결국 에러가 발생이 되었는데 368초나 걸린 것입니다. 추가로 중요한 정보는 위에 빨간 박스처럼 외부 API를 호출하기 전 J데DBC 커넥션을 부유하고 있다는 점입니다. 외부 API 호출시에 발생한 에러는 사진처럼 엔도우 파일 리세션입니다. 사용자의 요청에 대한 응답을 리턴했지만 사용자가 기다리다 지쳐 떠나 버렸을 경우 발생하는 에러입니다. 타임라인을 본 이상한 점들을 정리해 보자면 이런 결론이 나오는데요.이 정보들만으로는 원인을 알기 어려웠습니다.이 시점에서 서버에서는 무슨 일이 일어나고 있었던 것일까요? 결과를 다시 생각해 봤을 때 만약 리모트 API의 문제였으면 모든 서버군에서 영향을 받았어야 하는데 한 대씩 돌아가면서 문제가 발생하였습니다. 따라서 서버에서 무슨 일이 일어나고 있었는지 알아야 되는데요.이를 알기 위한 방법을 알아보겠습니다. 서버에서는 수많은 쓰레드가 동시에 수행됩니다. 쓰레드간에 공유하는 자원이 있다면 경합이나 락대이 현상 등이 발생할 수 있습니다. 만약 서비스 하고 있는 서버에서 락대기 현상이 발생한다면 쓰레드 덤프를 사용하여 원인에 파악할 수 있습니다. 쓰레드 덤프를 어떻게 분석해야 하는지 알아보기 위해 테스트를 해 봤습니다. 코드의 동작은 굉장히 심플합니다. 다섯 개의 쓰레드를 생성하고 각각의 쓰레드들은 모두 싱크 메소드를 호출하도록 되어 있습니다. 싱크 메소드를 보면 싱크로나이즈 키워드가 붙은 것을 확인할 수 있는데요. 즉 하나의 쓰레드가 해당 메소드를 수행하면 스캔.넥스트의 넥스트에 인풋이 들어올 때까지 해당 인스턴스의 락을 점유하게 됩니다. 메소드를 수행시킨 뒤 VM이라는 툴을 사용하여 쓰르드 덤프를 수행시켜 봤습니다. 스르드 덤프를 수행시킨 결과입니다. 내용이 많아서 핵심적인 부분만 보겠습니다. 실행 중인 다섯 개의 쓰레드 중 실행 중인 쓰레드와 블라킹인 하나의 쓰레드만 살펴보겠습니다. 쓰레드 2번이 런너블 상태로 실행 중이고 쓰레드 1번이 블락킹 된 것으로 보입니다. 쓰레드 이번에 콜스택을 보면 스캐너점 넥스트를 수행하면서 락을 점유하고 있다는 것을 확인할 수 있고 스탠드 1번의 콜스텍에서는 해당 락을 기다리고 있는 것이 보입니다. 이제 슬드 덤프가 무엇인지 알게 되었으니 서버에서 문제가 발생한 시점에 쓰레드 덤프를 보겠습니다. 다음은 앞에서 말씀드렸던 산 모니터링 툴 네오로 문제가 발생한 서버의 쓰레드 덤프를 10초 간격으로 떠본 결과입니다. 쓰레드들이 선 패키지의 메모리 캐시점서 블로킹 상태이고 러너블린 쓰레드에서 메모리 캐시점 mtq를 실행하며 락을 점유하고 있습니다. 그렇다면 락을 잡고 있는 셋 메소드는 무엇을 하는 메소드일까요? 락을 기다리고 있는 세르드 덤프의 콜스택을 쭉 따라가 봤습니다. 콜스택을 쭉 따라가 보면 https 커넥션을 생성하고 그 과정에서 락을 점유하는 메소드를 호출하는 것이 보입니다. 눈치가 빠르신 분들은 여기서 왜 HTPS 커넥션을 생성하고 있는지 아실 것 같은데요. 앞에 모니터링 트레스폰 응답이 느렸던 API을 호출하고 있던 것이었습니다. 시간 관계상 간단히 설명드리자면 HTTPS를 사용하면 SSL 커넥션을 사용하게 되는데요. 이때 SSL 커넥션을 생성하는 비용이 비싸기 때문에 SSL 세션을 캐싱하게 됩니다. 그리고이 과정에서 락을 잡고 있는 메소드를 호출한 것입니다. 락을 잡고 수행 중인 메소드를 찾아보면 싱크로나이드 키워드가 붙은 것을 확인할 수 있었습니다. 즉 동기와 처리가 되기 때문에 다른 쓰레드들은 점유된 자원을 기다리고 있는 상황이었습니다. 슬레드 덤프를 해본 결과와 앞에 모니터링 툴에서 본 것을 종합하며 정리해 보자면 최초로 수행된 쓰레드 A가 JDBC 커넥션을 획득하고 https 커넥션을 연결하면서 SS 세션을 캐싱하지만 캐싱하는 과정에서 락을 점유하게 됩니다. 그 후 실행된 49개의 스레드가 쓰레드 A와 마찬가지로 JDBC 커넥션을 획득하고 HTPS 커넥션을 시도합니다. 이때 셀드 A가 점유하고 있는 락을 기다리게 됩니다. JDBC 커넥션 풀의 최대수는 50개이기 때문에 JDBC 커넥션은 고갈되었고 락을 기다리고 있는 쓰레드들은 JDBC 커넥션을 획득한 지로 락대기 상태에 놓이게 됩니다. 그 후에 다른 쓰레드들이 JDBC 커넥션을 요청하지만 커넥션은 이미 고갈된 상태이기 때문에 3초간 커넥션을 획득하길 기다렸다가 타임아웃으로 실패하고만 상황이었던 것입니다. 쓸드프로 어디서 병목 현상이 발생하였는지 알 수 있게 되었습니다. 하지만 SSL 컬렉션을 캐싱하는 과정에서 수행 시간이 오래 걸렸던 이유는 알 수 없었습니다. 락을 잡고 있는 메소드에서 어떤 걸 더 호출하고 있는지 콜스택을 보았습니다. 메모리 캐시점 풋을 호출하고 그 안에서 MTQ 메소드를 호출하고 있는 것을 확인할 수 있는데요. 캐싱을 하고 있는데 Q를 비워주는 코드를 왜 수행하지는 의문이 들어서 메소드를 찾아 보았습니다. 제일 먼저 눈에 띄는 메소드의 설명을 읽어 보겠습니다. 레퍼런스 Q를 비워주고 이와 일치하는 엔트리들을 캐시에서 지어 준다. 그리고 퍼블릭 메소드마다 시작하는 부분에서 반드시 호출되어야 한다고 쓰여져 있습니다. 메소드의 내부를 보면 Q에서 캐시 엔트리가 없을 때까지 반복문을 호출하고 Q에서 꺼낸 캐시 엔트리를 키시맵에서 지워 주는 것이 보입니다. 코드를 보면서 여러 가지 의문점이 들었습니다. 레퍼런스 Q가 뭐지? 캐시맵에서 왜 지어 줄까? 왜 모든 퍼블릭 메소드에서 반드시 호출해야 할까?이 이 의문점들을 가지고 좀 더 딥입해 보겠습니다. 이번 파트의 제목은 서프트 레퍼런스인데요. 소프트 레퍼런스가 생소하신 분들이 많을 거라 생각됩니다. 앞에서 궁금해했던 의문점들은 모두 소프트 레퍼런스와 관련이 있는데요. 의문점들을 해소해 나가면서 소프트 레퍼런스에 대해 알아봅시다. 앞서 궁금해했던 첫 번째 의문점은 MTQ 메소드에서 레퍼런스 Q를 비워진다는 설명에서 시작되었는데요. 레퍼런스 Q가 무엇인지 문제가 되었던 코드와 함께 알아가 봅시다. 레퍼런스 Q를 사용하는 부분을 찾으면서 다음 코드를 보았습니다. 캐시 엔터 엔트리를 생성하는 부분의 코드를 보면 레퍼런스 Q가 널이 아니라면 소프트 캐시 엔트리를 생성하고 소프트 캐시 엔트리는 소프트 레퍼런스를 상속받아 구현하고 있습니다. 그리고 그 레퍼런스 큐를 자신의 부모 클래스 즉 소프트 레퍼런스의 생산자에 넣어 주는 것을 확인할 수 있었습니다. 소프트 레퍼런스에서는 어떻게 레퍼런스 큐를 사용하고 있을까요? 궁금증을 풀기 위해 소프트 레퍼런스에 대해 찾아보다가 리치블이라는 개념을 만났습니다. 혹시블이 무엇인지 알고 계시나요? 어떤 객체에 대한 유효한 참조가 있다면이라고 하고 유효한 참조가 없다면 unreable이라고 하는데요. 이때 un리치able 객체는 GC에 대상이 됩니다. 여기서리able인 객체들은 이렇게네 가지로 구분할 수 있는데요. 그중 스트롱리able을 제외한이 세 가지 상태를 GC에서 특별하게 취급하고 있습니다. 스트롱리 리치able블은 자바 랭점 패키지의 클래스들을 통합 찬조가 없는 우리가 흔히 사용하는 객체입니다. 그리고 스트롱리 리치블을 제외한 나머지 상태들은 스트롱리블 객체가 아니면서 자반적 랭점 패키지 클래스에 참조로만 생산된 클 객체들을 말하는데요. 해당 패키지의 클래스들에는 각각 소프트 레퍼런스, 빅크 레퍼런스, 팬텀 레퍼런스가 있습니다. 전부 설명을 하면 좋겠지만 시간 관계상 소프트웨어 리치어블에 대해서만 알아보도록 합시다. 앞에서 스트롱 리치어블을 제외한 나머지 상태들을 GC에서 특별하게 취급한다고 말했는데요. 그렇다면 소프트웨어 리치어블은 GC에서 어떻게 특별 취급하는지 보겠습니다. 다음과 같이 스트롱이 리치able블린 객체와 소프트 리치블린 객체가 있습니다. GC의 횟수 대상이 된다면이 둘은 어떻게 동작할까요?이 이 두 객체가 GC에 회수되면 다음과 같이 stringly블은 대상 자체의 메모리가 해제됩니다. 반면에 소프트웨어리블은 소프트 레퍼런스가 참조하던 객체가 회수되고 소프트 레퍼런스 객체는 객체 생성시 레퍼런스 Q를 시정하였다면 레퍼런스 Q에 인큐됩니다.이 레퍼런스 Q 덕분에 사용자는 어떤 객체가 GC 의해 회수되었는지 알 수 있게 되는데요. 소프트 캐시 엔트리 코드를 같이 볼까요? 소프트 캐시 엔트리의 시퍼클래스 즉 소프트 레퍼런스의 레퍼런스 큐를 넣어 주는 것은 앞서 말한 이유처럼 소프트 캐시 엔트리가 참조하고 있는 객체가 실수되었는지 알기 위해서입니다. 그리고 소프트 캐시 엔트리는 키 밸류 형식으로 캐시값을 자랑하고 있는 것이 보이는데요. 슈퍼클래스 소프트 레퍼런스에만 밸류를 넣어주고 키는 소프트 캐시 엔트리의 필드 값으로 지정하는게 보입니다.이 이 밸류가 소프트 레퍼런스가 참조하는 대상이기 때문에 GC의 회수 대상이 되면 밸류는 널이 됩니다. 다시 말해 캐시 엔트리에 참조된 객체 즉 캐시값이 GC에 의해 회수되면 캐시 엔트리에 레퍼런스 Q에 인큐되고 참조하고 있던 캐시값은 널이 되는 것입니다. 레퍼런스 Q가 무슨 역할을 하고 있는지 알게 되었습니다. 레퍼런스 Q가 무슨 역할을 수행하고 있는지 알아보았는데요. 그렇다면 두 번째로 캐시맵에서 데이터를 지어주는 이유에 대해 알아봅시다. 레퍼런스 Q에서 캐시 엔트리를 빼고 있는 부분을 보면 앞에서 보았듯이 레퍼런스 Q에 들어가 있는 서프트 레퍼런스 즉 캐시 엔트리는 참조하고 있는 캐시값이 GC에 회수되었기 때문에 들어가 있는 것입니다. 따라서 더 이상 참소하는 대상이 없기 때문에 Q에서 빼 주는 것이라고 이해할 수 있습니다. 그리고 캐시맵에서 해당하는 캐시 엔트리를 비워 주는 것이 보이는데요. 캐시 엔트리가 참조하는 대상은 캐시값인데이 값이 더 이상 없다면 캐시값이 없다는 의미이겠지요. 그래서 캐시맵에서도 지워 주는 것입니다. 마지막 의문점은 모든 결론을 종합해 보면 자연스럽게 해결됩니다. 정리해 보자면 레퍼런스 Q에 있던 캐시 엔트리를 캐시맵에서 비워 주는 이유는 캐시 엔트리가 참조하고 있던 캐시값이 GC에 회수되었기 때문에 더 이상 불필요하여 메모리 낭비를 맡기게 한 것이었습니다. 그래서 모든 퍼블링 메소드에서 이러한 이유 때문에 MTQ를 호출하고 있었던 거네요. 궁금했던 점들이 모두 해소되었습니다. 하지만 제일 궁금한 사항이 남아 있습니다. 왜 수행 시간이 오래 걸렸던 걸까요? 그렇다면 한 가지 의문점이 생깁니다. 왜 소프트 레퍼런스로 캐시 엔트리를 구현하였을까요? Soft reach블의 다른 특징에 대해 알아봅시다. 사실의 특별한 점은 하나 더 있습니다. stryly reach블과 달리프트웨어리치어블 객체는이 공식에 따라 GC 회수 여부와 결정되는데요. 소프트 레퍼런스 폴리미세/MB는 GV인 플래그 설정값이고 오브 메모리 MB는 현재 남아 있는 힙 메모리의 크기입니다. 플레이크 설정값과 현재 남아 있는 힙 커기를 곱한 시간까지 사용되지 않은 소프트 리치어블 객체가 GCA에 횟수됩니다. 예를 들어 다음의 예제에서는 20초 동안 사용하지 않은 소프트 레퍼런스의 참조 객체가 GC 대상이 됩니다. 소프트 리치어블 객체는 남아 있는 힙의 크기와 해당 객체의 사용 빈도에 따라 GC브어 결정되기 때문에 다른 객체들보다 GC에 의한 회수가 늦어집니다. 이러한 특상 때문에 소프트 레퍼런스는 LR 캐시로스의 역할을 하기에 충분합니다. 따라서 캐시 엔터리가 서프트 레퍼런스를 상속받아 구현된 것이지요. 하지만 다르게 말하자면 소프트 리치블 객체가 힙을 일정량 점유하고 있다는 뜻인데요. 철정값에 따라 많은 양의 데이터가 남아 있을 수 있다는 의미입니다. 문제가 발생한 서버는 GNG 지c를 사용하고 있고 힙 크기가 16GB로 매우 큽니다. 그리고 공신 문서를 조회해 본 결과 SSL 캐시맵 사이즈의 디폴트 값이 무제한으로 설정되어 있고 캐시 엔트리의 유효 시간의 디폴트 값이 24시간으로 되어 있었습니다. 그렇다면 메모리에 남아 있는 소프트웨어 리치어블 객체 즉 캐시 엔트리가 많아 지금까지 살펴본 것을 종합하여 말하자면 이러한 결론이 다르는데요. 즉 레퍼스 Q에 가득찬 캐시 엔트리를 지워 주는데 시간이 오래 걸렸던 것입니다. 하지만 단순히 커다란 데이터를 비워해서 시간이 오래 걸렸던 걸까요? 원인을 찾았다 하기에 뭔가 찝찝한 마음이 들어서 다시 초심으로 돌아가 쓰레드 덤프의 결과를 보았습니다. 락을 잡고 있는 쓰레드에서 해시맵점 리무브가 수행되고 있는 것이 보입니다. 메모리 캐시의 코드를 보면 캐시맵은 해시맵으로 구현되어 있는 것이 보입니다. 왠지 캐시맵에서 데이터를 지울 때 실행이 오래 걸렸을 것 같다는 느낌이 오는데요. 해시맵은 많은 분들이 알고 계시고 익숙하실 텐데요. 간단하게 설명을 해 보고자 합니다. 해시맵은 내부의 일정량의 버킷으로 구성되어 있고 해시맵에 데이터를 넣을 때 해시 버킷셋 인덱스 값은 버킷의 개수와 해시코드를 나눈 나머지 값으로 설정됩니다. m은 현재킷의 개수이며 즉 1/n의 확률로 같은 해시 버킷을 사용하게 됩니다. 다음 해시맵에 a b가 인덱스 0 1에 들어가 있고 c가 만약 1/의 확률로 이렇게 같은 해시 버킷을 차지하게 되었을 때를 해시 컬리진이라고 합니다. 메모리 효율성 등을 고려하면 해시 컬리진 자체가 문제가 된다고 볼 수는 없습니다. 하지만 자바는 해시맵에서 해시 충돌이 발생할 때 세퍼라이트 체이닝으로 저장하고 있는데요. 이렇게 해시 컬리즌이 많이 발생하면 해시맵의 데이터의 분표가 균형되지 않기 때문에 해시맵의 성능이 제대로 나오지 않습니다. 앞에서 해시맵점 리무브를 할 때 수행 시간이 오래 걸린 것으로 추측하였는데요. 캐시맵에 캐시 엔트리를 저장하는 키의 해시코드를 구현한 부분이 의심스럽죠. 다음은 캐시 엔트리의 키에 해시코드를 구현한 코드입니다. 의심의 결과가 맞았습니다. 단순히 세션 아이디의 각 원소의 값을 더한 값을 해시코드로 사용하고 있었습니다. 즉 같은 해시코드를 가진 객체가 많아서 해시 컬리즌이 발생할 가능성이 매우 컸다는 의미입니다. 딥다이브 해본 결과를 정리해 보자면 문제 발생의 키워드는 소프트 레퍼런스로 구현된 캐시 엔트리와 제어놓는 캐시맵 사이즈 그리고 증복값이 많은 캐시 엔트리 해시코드의 메소드에 해시콜리으로 밝혀졌습니다. 수많은 데이터가 해시컬리즘이 발생한다면 정말 이런 서비스 지원이 발생할까요? 알아본 정보를 바탕으로 테스트를 해 보았습니다. 실제 환경에서 힙 사이즈는 16기였지만 힙사이즈를 2GB로 작게 설정하고 실제 환경에서는 소프트 레퍼런스가 어느 정도 차이 있었는지 알 수 없기 때문에 데이터는 10만 개로 가정하여 테스트하겠습니다. MTQ 메소드처럼 레퍼런스 Q에 하나라도 데이터가 있다면 레퍼런스 Q가 다 비워질 때까지 와일문을 수행하고 레퍼런스 Q에서 꺼내온 값을 해시맵에서 비워주는 코드입니다. 앞에서 보았던 캐시 엔트리 키에 해시 컬리진을 제연하기 위해 해시코드가 랜덤으로 0에서 99로 반환하는 경우와 해시코드를 재정해하지 않는 코드를 비교하여 수행해 보았습니다. 해시코드가 100개로 한정된 경우의 수행 시간은 4,83m이고 해시코드를 재정이하지 않는 경우의 수는 47mm가 나왔습니다. 약 86배의 수익 시간의 차이가 나왔습니다. 해시코드가 100가지의 경우가 있는 케이스는 해시콜리 발생할 가능성이 크기 때문에 수행 시간이 더 오래 걸린 것을 확인할 수 있었습니다. 실제 환경의 힙사이즈가 16GB인 것으로 생각해 본다면 어마어마한 수행 시간을 예상할 수 있을 것입니다.이 성능 이슈가 문제가 된 것은 자료들의 날짜로 유치해 볼 때 2018년도부터인데요. 왜 기존에는 문제가 없었던 걸까요? 문제가 되었던 SS 세션의 디폴트 값을 보면 캐시 이후 시간은 24시간, 캐시의 크기에 제한은 없었습니다. 하지만 이전에는 힙의 크기가 그다지 크지 않았기 때문에 스큐더된 해시 테이블의 값이 많지 않아서 성능이시가 발생하지 않았을 것이라 추측할 수 있습니다. 그렇다면이 문제는 어떻게 계산되었을까요? 해당 버전의 릴리즈 노트를 보면 해시 컬리즌을 많이 일으켰던 세션 아이디의 해시코드 메소드를 수정하고 디폴트 캐시 사이즈를 2480으로 변경한 것으로 확인되었습니다. 하지만 비교적 꽤 최근까지도 성능 이슈가 발생하여 해당 버전에서 성능 개선 작업을 한 것이 파악되었습니다. 저희는이 이슈를 JDK 버전업을 하여요. 같은 이슈를 겪고 싶지 않은 분들은 JDK 버전업을 하시는 것을 추천드립니다. 잔잔한 바다에 커다란 파도가 갑작스럽게 치는 것처럼 장애의 순간은 예상치 못하게 다가올 수 있습니다. 발표를 들으시는 분들이 장애 원인을 파악을 위해 오픈소스에 뒷타이브 하실 때 이번 세션이 좋은 꿈이나마 도움이 되기를 바라면서 발표를 마치겠습니다. 지금까지 긴 발표 들어 주셔서 감사합니다.
