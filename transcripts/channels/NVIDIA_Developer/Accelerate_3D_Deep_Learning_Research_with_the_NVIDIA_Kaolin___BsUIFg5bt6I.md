# Accelerate 3D Deep Learning Research with the NVIDIA Kaolin PyTorch Library

- 영상 링크: https://www.youtube.com/watch?v=BsUIFg5bt6I
- 채널: NVIDIA Developer
- 업로드일: 2023-12-13
- 자막 언어: en
- 단어 수: 약 521개

---

## 스크립트

Hi everyone my name is Clement Fuji&nbsp; Tsang I'm the tech lead of Kaolin,&nbsp;&nbsp; NVIDIA’s Python 2 Kit for 3D deep learning. Let me give you an intro. Kaolin&nbsp; is a PyTorch library with low level&nbsp;&nbsp; CUDA-optimized Ops and higher level&nbsp; features for convenient workflow. The&nbsp;&nbsp; GitHub repository contains examples that&nbsp; can easily show its core capabilities. Here we're going to select the&nbsp; tutorial folder that contains&nbsp;&nbsp; several Jupiter notebooks. Let's check this one. This tutorial is showing how to apply&nbsp; differentiable rendering and lighting&nbsp;&nbsp; with a method called spherical gion .&nbsp; In this notebook we are loading a mesh&nbsp;&nbsp; and its materials from an OBJ file which&nbsp; is a popular file format for 3D meshes. Making a camera object from a camera&nbsp; position look at coordinates of vector&nbsp;&nbsp; and other parameters and use it to transform&nbsp; vertices to normalize device coordinates. Then generating the albedo using&nbsp; our differentiable rendering&nbsp;&nbsp; functions such as the differentiable&nbsp; rasterization and the texture mapping. The goal is to optimize light parameters&nbsp; from images for this toy example we are&nbsp;&nbsp; generating synthetic data&nbsp; just to show that it works. First defining Target spherical gion&nbsp; parameters and implementing a function&nbsp;&nbsp; that apply diffuse lighting and specular&nbsp; lighting given the spherical Goan parameters. The ground truth are then displayed below. Finally&nbsp;&nbsp; we initialize new spherical gos&nbsp; parameters that will be optimized. The training Loop is re-using the&nbsp; lighting function and the loss is&nbsp;&nbsp; a mean square error loss on the render image. You can see below the rendering before and after&nbsp; optimization to verify that it properly converged. That's just one example. The County&nbsp; library has many more features. Let me show you another one. In this notebook we&nbsp;&nbsp; explain the structure Point clouds SPC&nbsp; in short an octy based representation in&nbsp;&nbsp; Coline useful for storing processing&nbsp; and rendering 3D sparse features. It starts with a simple introduction of the&nbsp; oct structure and then begin by loading a mesh&nbsp;&nbsp; from an .obj from which points are then sampled&nbsp; with interpolated RGB colors from the materials. We then create the SPC from this point cloud. In the next subsections we instantiate a&nbsp; camera and its Rays to render the SPC with&nbsp;&nbsp; its fast Ray tracing features that can&nbsp; be applied at any level of the octree. Features can be stored on the whole hierarchy&nbsp; useful for NeRF training or level of detail. The final section explains in&nbsp; detail the internal components&nbsp;&nbsp; of the structure point clouds explaining&nbsp; the hierarchical ordering of the octree&nbsp;&nbsp; and the auxiliary tensors containing the&nbsp; SPC to optimize the computation on GPU. If you're interested in seeing more&nbsp; tutorials or documentation lists all of&nbsp;&nbsp; the available tutorials in the tutorial index&nbsp; and describes the covered features and also&nbsp;&nbsp; goes further in introducing concepts such as&nbsp; our rendering pipeline or or camera classes. You also have fully detailed IPI documentation to&nbsp;&nbsp; cover absolutely every function&nbsp; and class that Kaolin implements. To install Kaolin, you can use the&nbsp; simple pip install command line. Following the instructions on&nbsp; the dedicated documentation page. Start now with Kaolin by clicking on the links&nbsp; in the description. Thank you for watching.
