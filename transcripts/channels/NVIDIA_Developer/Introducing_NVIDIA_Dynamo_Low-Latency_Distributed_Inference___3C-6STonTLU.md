# Introducing NVIDIA Dynamo: Low-Latency Distributed Inference for Scaling Reasoning LLMs

- 영상 링크: https://www.youtube.com/watch?v=3C-6STonTLU
- 채널: NVIDIA Developer
- 업로드일: 2025-04-30
- 자막 언어: en
- 단어 수: 약 13577개

---

## 스크립트

[Applause] Hi everyone. Thank you so&nbsp; much for joining. We have something&nbsp;&nbsp; very exciting to show you. But before&nbsp; I start, I just wanted to acknowledge&nbsp;&nbsp; the multiple NVIDIA internal teams&nbsp; and also external partners that we&nbsp;&nbsp; worked together to make this amazing&nbsp; product into reality. So, thank you.&nbsp; I'd like to talk about what we are going to&nbsp; share today. I'm going to kick it off and talk&nbsp;&nbsp; about the motivation as to why we decided to&nbsp; create a new product in inference serving. But&nbsp;&nbsp; also at the same time, I would like to talk to&nbsp; you about the key features and benefits. Then,&nbsp;&nbsp; Neelay, who is our principal architect, is going&nbsp; to talk about the architecture and he's also going&nbsp;&nbsp; to give us a preview of the workflow of Dynamo.&nbsp; Then, Tommy is going to actually show you how you&nbsp;&nbsp; can use Dynamo in action. Lastly, Ryan Olson, who&nbsp; is a principal solution architect, is going to go&nbsp;&nbsp; a deeper dive into the API of Dynamo. Then I'll&nbsp; come back and try to introduce some next steps.&nbsp; Okay, so I wanted to kick it off with a&nbsp; motivation. As Jensen talked about today in his&nbsp;&nbsp; keynote, we've already entered the GI era, but now&nbsp; we are entering the reasoning or testing compute&nbsp;&nbsp; or inference compute era. And because of that,&nbsp; you actually need a lot more compute and memory&nbsp;&nbsp; because inference compute or reasoning requires&nbsp; the model to actually think during the inference&nbsp;&nbsp; time, and so the compute or the memory requirement&nbsp; is only growing. But at the same time, it's quite&nbsp;&nbsp; difficult to actually scale these types of models.&nbsp; DeepSe shared their implementation of DeepSe v3,&nbsp;&nbsp; which is their base for the reasoning model&nbsp; R1. The way that they deployed this particular&nbsp;&nbsp; kilometer in their data center is that they&nbsp; did something called decided serving, where you&nbsp;&nbsp; separate pre-fill and decode phases of LMS. Then&nbsp; they decided to assign about 32 GPUs to fulfill&nbsp;&nbsp; the pre-filled computation, but for decode, they&nbsp; actually employed about 320 H800s to do this. So,&nbsp;&nbsp; we think that this is actually super hard.&nbsp; The idea behind it is actually simple, but&nbsp;&nbsp; the implementation is actually quite difficult.&nbsp; And there hasn't really been a good, reliable,&nbsp;&nbsp; production-ready solution out there that's open&nbsp; source. So, that's what we think is a problem.&nbsp; And then when you think about it, before GenAI&nbsp; or reasoning, having a simple router that does&nbsp;&nbsp; round-robin routing was perfectly fine and it&nbsp; was actually quite performant. But we're actually&nbsp;&nbsp; leaving a lot of performance on the table if we do&nbsp; this. Because there are so many other things that&nbsp;&nbsp; are specific to LMMs that we can leverage. Lastly,&nbsp; with LMMs, they have heterogeneous inputs. So,&nbsp;&nbsp; you could have a situation where, at certain&nbsp; points, you're only getting specific lengths&nbsp;&nbsp; of inputs. So, say if you look at sequence one and&nbsp; sequence two here, but maybe later you're actually&nbsp;&nbsp; getting hit with a super long context length. If&nbsp; you imagine this happening at a data center scale,&nbsp;&nbsp; it's really hard to stop your deployment and then&nbsp; manually reconfigure it and then deploy again. So,&nbsp;&nbsp; these are some of the problems that we&nbsp; want to address with our new product.&nbsp; So, we decided to tackle these problems,&nbsp; and as you heard from Jensen's keynote,&nbsp;&nbsp; we have a new product called Dynamo. Dynamo's&nbsp; mission is to accelerate distributed inference&nbsp;&nbsp; in the data center, and we think that there are&nbsp; three key areas that we have to focus on. The&nbsp;&nbsp; first one is job scheduling. The second is how&nbsp; you manage the memory within the data center,&nbsp;&nbsp; and the third is how you transfer&nbsp; data in that large scale. So,&nbsp;&nbsp; very soon, I will talk about some of the key&nbsp; features that we have implemented. For example,&nbsp;&nbsp; for scheduling, we implemented smart routing&nbsp; that's aware of the KV cache in the data center.&nbsp;&nbsp; We also implemented a KV cache manager for being&nbsp; able to manage the memory much better, and it&nbsp;&nbsp; will utilize all the multiple hierarchies of the&nbsp; memory that is present in the data center. Lastly,&nbsp;&nbsp; we actually have a new data transfer library&nbsp; that we created that lets you transfer data&nbsp;&nbsp; with very low latency. We think that we have key&nbsp; features for all these three foundational areas.&nbsp;&nbsp; But we just wanted to highlight that I kind of&nbsp; talked about this serving on the previous slide,&nbsp;&nbsp; but this serving really requires you to have&nbsp; a very strong foundation in these three areas:&nbsp;&nbsp; scheduling, memory management, and data transfer.&nbsp; So, we believe that the features that we have&nbsp;&nbsp; currently launched with Dynamo can actually take&nbsp; advantage of this feature and be able to deploy&nbsp;&nbsp; your LMS with this advanced technique. So, next,&nbsp; I will talk about what this serving actually is.&nbsp; So, here, the idea is actually quite simple,&nbsp; but the result or the performance that you can&nbsp;&nbsp; gain from it is actually quite phenomenal.&nbsp; As you probably know, LMMs have two phases.&nbsp;&nbsp; The first phase is prefill, and so it's a phase&nbsp; until you do all the computation of your KV cache&nbsp;&nbsp; and then you start producing the first token.&nbsp; Then, once you start producing the first token,&nbsp;&nbsp; you go into the decode phase, where&nbsp; you can actually decode the incremental&nbsp;&nbsp; tokens that come after it. But if you look at&nbsp; the compute characteristics of both, prefill&nbsp;&nbsp; is a compute-bound process, and then decode is&nbsp; actually memory bandwidth-bound, and so they're&nbsp;&nbsp; quite different. The idea is that can we actually&nbsp; separate the two phases? If you do so, then you&nbsp;&nbsp; have a unique opportunity because you can actually&nbsp; take the independent or now the disaggregated&nbsp;&nbsp; phases and apply different types of parallelism&nbsp; that are more suitable for prefill and decode.&nbsp;&nbsp; One caveat here is that you still need to figure&nbsp; out how to transfer the KV cache between pre-fill&nbsp;&nbsp; and decode now that they're separated. And&nbsp; that's the reason why I talked about how,&nbsp;&nbsp; in that diagram, you need to have that strong&nbsp; foundation where you can do all job scheduling,&nbsp;&nbsp; memory management, and also data transfer. And so,&nbsp; I'll show you later how the KV cache transfer is&nbsp;&nbsp; facilitated by the new inference data&nbsp; transfer library that we created.&nbsp; But this I-serving has two interesting&nbsp; characteristics. It will perform really&nbsp;&nbsp; well if you have a large model or if you have&nbsp; a lot more GPUs to use. The reason is that&nbsp;&nbsp; because you now separated the prefill and decode,&nbsp; you have a lot more opportunities to apply&nbsp;&nbsp; different configurations for parallelism. And so,&nbsp; with all the combinations that you can consider,&nbsp;&nbsp; you can actually figure out the more performant&nbsp; configuration that can scale better for you. The&nbsp;&nbsp; second part is that when you look at the&nbsp; in-flight batching versus desire serving,&nbsp;&nbsp; if you have a little bit more or if you have a&nbsp; decode-heavy task, then the way that in-flight&nbsp;&nbsp; batching and then also desire serving&nbsp; allocate the resources the same. And so,&nbsp;&nbsp; it turns out that it usually works better if you&nbsp; have a much longer context to process. So, now,&nbsp;&nbsp; one more additional benefit is that now that&nbsp; you've disaggregated, you actually have a choice,&nbsp;&nbsp; and you can make a trade-off. Prefill is measured&nbsp; by the time to first token, and decode is measured&nbsp;&nbsp; by inter-token latency. And so, say that you have&nbsp; a service requirement that you want to optimize;&nbsp;&nbsp; you can actually optimize them separately.&nbsp; So, if you want faster time to first token,&nbsp;&nbsp; then you can increase the prefill workers, and&nbsp; you can do the same thing for inter-token latency&nbsp;&nbsp; if you want to decode faster. And then you can&nbsp; make the trade-off between the ratio of prefill&nbsp;&nbsp; and decode if you want to increase the output&nbsp; token throughput. An interesting benefit that you&nbsp;&nbsp; can also have is you can also pair up different&nbsp; types of GPUs for pre-fill and decode. So, we met&nbsp;&nbsp; some Chinese customers this year and last year&nbsp; that they were pairing up H800 for prefill and&nbsp;&nbsp; then H20 for decode, and that actually brings&nbsp; a lot of cost savings. You have to think about&nbsp;&nbsp; the performance envelope of, say, prefill GPU&nbsp; should have less memory but you want to have&nbsp;&nbsp; more compute, and then for decode, you want to&nbsp; have more memory bandwidth. So, next, I wanted&nbsp;&nbsp; to show you the benefits of desire serving. So,&nbsp; here, we did a test. We ran R1 distilled 70B&nbsp;&nbsp; using 3,000 input sequence length and then about&nbsp; 150 output sequence length. The plot that you're&nbsp;&nbsp; seeing is basically a trade-off between throughput&nbsp; and latency. So, on the y-axis, you see tokens per&nbsp;&nbsp; second per GPU. That's basically throughput per&nbsp; GPU, and then the x-axis, tokens per second per&nbsp;&nbsp; user. That's latency. What you're seeing here is a&nbsp; graph of a trade-off, and we've already calculated&nbsp;&nbsp; all the different configurations that we tried,&nbsp; and we are only taking the Pareto frontier of the&nbsp;&nbsp; configurations. So, if you see the first plot,&nbsp; which is comparing the one-node disaggregation&nbsp;&nbsp; against the aggregated setup, you can see that&nbsp; we gain about 30% throughput per GPU, and this&nbsp;&nbsp; is the reason why. You probably already saw&nbsp; the 2x number on the other side. The reason why&nbsp;&nbsp; you actually get a smaller gain in a single node&nbsp; is because it just doesn't have a lot of room for&nbsp;&nbsp; multiple configurations of parallelism. As soon&nbsp; as you move to the two nodes, you're seeing that&nbsp;&nbsp; there's a big jump in terms of performance for&nbsp; throughput per GPU. And we've seen in our actual&nbsp;&nbsp; runs going up to three nodes and four nodes and so&nbsp; forth. You will actually get incremental benefit&nbsp;&nbsp; above the 2x line. And so, what this is actually&nbsp; showing you is that because we measured it per&nbsp;&nbsp; throughput per GPU, you're actually scaling better&nbsp; than linear scale, which is incredible. And so,&nbsp;&nbsp; this is why we think that this is a key feature&nbsp; of Dynamo. And there are some more advanced&nbsp;&nbsp; features that we'll show off. Tom is going to&nbsp; give a demo for you. Things like conditional&nbsp;&nbsp; disaggregation and auto-discovery. And so, I'll&nbsp; let Tanmay talk about the exciting stuff later.&nbsp; Okay, so next up is about scheduling, and in this&nbsp; case, we are going to talk about routing. Here at&nbsp;&nbsp; the top, I'm giving a simple example where we're&nbsp; doing a more traditional load-balanced balancing&nbsp;&nbsp; or load-based router. So, say, for example,&nbsp; if you have a query that says, "What is GTC,"&nbsp;&nbsp; and say that worker 3 actually has a relevant KV&nbsp; cache that corresponds to "What is GTC." It's a&nbsp;&nbsp; bit of a waste because in this setup, the load&nbsp; for worker one has the lightest load. The core&nbsp;&nbsp; will get routed to worker one, but in fact,&nbsp; you can actually leverage the KV cache that's&nbsp;&nbsp; in worker three to expedite the processing. And&nbsp; so, if you look at the bottom, now the difference&nbsp;&nbsp; is that we try to calculate the KV cache match&nbsp; rate for each worker. The way that we do it is&nbsp;&nbsp; we map the KV cache with a registry using a radix&nbsp; tree. So, we'll be able to figure out the KV cache&nbsp;&nbsp; hit rate, and then the router is going to be aware&nbsp; of where to route the work to the corresponding&nbsp;&nbsp; worker. And so, if you look to the right, we have&nbsp; exciting results. We actually didn't try to use&nbsp;&nbsp; simulated or synthesized data. This is actual real&nbsp; data. It's about 100,000 requests that went to an&nbsp;&nbsp; R1 model, and then we wanted to test out our KV&nbsp; cache-aware routing mechanism. As you can see,&nbsp;&nbsp; it dramatically speeds up the latency or related&nbsp; metrics. So, things like time to token turned out&nbsp;&nbsp; to be three times faster on average, but also&nbsp; average request latency got cut down by 2x. So,&nbsp;&nbsp; we think that this is a key feature of Dynamo. Next, I will talk about memory management,&nbsp;&nbsp; and something that is very closely tied&nbsp; to memory management is data transfer. So,&nbsp;&nbsp; that's coming next. Here, in a data center scale,&nbsp; you can consider maybe four different major types&nbsp;&nbsp; of memory hierarchy. So, if you look at the top,&nbsp; you have HBM, and then you have the system memory,&nbsp;&nbsp; the local SSDs, and object storage. We all know&nbsp; that if you can smartly utilize this multiple&nbsp;&nbsp; hierarchy, because if you look at the HBM,&nbsp; it's going to have the smallest storage amongst&nbsp;&nbsp; the rest of it, but it has the fastest data&nbsp; transfer bandwidth. But if you go to the bottom,&nbsp;&nbsp; network storage is going to have the most storage,&nbsp; but it's going to have a limited bandwidth. So,&nbsp;&nbsp; if you put in some intelligence as to how you can&nbsp; actually move the KV cache around these different&nbsp;&nbsp; types of memory hierarchy, then you can actually&nbsp; dramatically speed up the time to first token&nbsp;&nbsp; because instead of recomputing the KV cache, you&nbsp; can actually load and prefetch the KV cache and&nbsp;&nbsp; then get that process and start decoding right&nbsp; away. So, this is phenomenally useful if you're&nbsp;&nbsp; doing something like multi-turn chat or if you&nbsp; have a long context because you have a lot more&nbsp;&nbsp; space to store your context. And then also, if you&nbsp; are aware of how the KV cache should be processed&nbsp;&nbsp; at which time, things like structured workloads or&nbsp; agent take workloads, then you can actually figure&nbsp;&nbsp; out when to offload the KV cache and then also&nbsp; prefetch before you need to get it processed. So,&nbsp;&nbsp; the result that we are sharing here is an early&nbsp; implementation that we have on Dynamo. So,&nbsp;&nbsp; we kind of submitted about 80 users with a&nbsp; thousand input sequence length and then 100&nbsp;&nbsp; output sequence length. And then we basically&nbsp; did a multi-turn of 10. So, here, you can see,&nbsp;&nbsp; at bulk with this many users, even with the&nbsp; prefix caching turned on, you are going to&nbsp;&nbsp; get a speedup of 1.6. And so, this is another&nbsp; innovation that we have. We are actually going&nbsp;&nbsp; to make it so that right now we have it available&nbsp; for offloading to system memory, but we are going&nbsp;&nbsp; to release something that does offloading to local&nbsp; SSD and then network storage all together. So,&nbsp;&nbsp; that I will try to discuss that in our next steps. So, something that is very closely tied together&nbsp;&nbsp; is data transfer. So, in a disaggregation case&nbsp; that I talked about before, you need to be able&nbsp;&nbsp; to transfer the data between multiple nodes so&nbsp; that you can pass the KV cache from prefill to the&nbsp;&nbsp; decode node and then be able to process it really&nbsp; fast. So, the new library is called Nixel in&nbsp;&nbsp; NVIDIA inference transfer library, and it speeds&nbsp; up the transfer between the multiple nodes by&nbsp;&nbsp; requiring less synchronization and also being&nbsp; able to batch the KVs. It also has support for&nbsp;&nbsp; more inference-appropriate workloads, and&nbsp; so it can support dimensity. If you scale,&nbsp;&nbsp; you need to be able to add and remove&nbsp; the workers very quickly. And then,&nbsp;&nbsp; like I said before, you need to be able to&nbsp; transfer data not just between the GPUs but&nbsp;&nbsp; between the multiple hierarchies of the memories&nbsp; here. And so, you can actually offload everything,&nbsp;&nbsp; or KV cache, through the hierarchy that you're&nbsp; seeing here all the way to the network storage.&nbsp;&nbsp; And then this serves as a common layer. So, it&nbsp; supports RDMA, so InfiniBand and Ethernet, and&nbsp;&nbsp; also TCP, and then we have plans to support AWS&nbsp; CFA. Just wanted to show this is basically a plot&nbsp;&nbsp; showing the difference between Nixel on and&nbsp; Nixel off applied to this aggregation case. So,&nbsp;&nbsp; you can see even just implementing this library is&nbsp; going to give you an amazing performance benefit&nbsp;&nbsp; for first time to first token. But not only&nbsp; that, even output token throughput benefits.&nbsp; So, to sum it up, here is what we&nbsp; currently have in Dynamo today. First,&nbsp;&nbsp; the GitHub link is actually a-damo, but we decided&nbsp; to give you a short link so that you don't have to&nbsp;&nbsp; type all that. So, if you go to nvidia.com/dynamo,&nbsp; it will direct you directly to that GitHub repo in&nbsp;&nbsp; the AI Dynamo organization. You should see the&nbsp; Dynamo stack, but also you should see Nixel,&nbsp;&nbsp; the inference data transfer library. The reason&nbsp; is that we took your feedback and we really&nbsp;&nbsp; wanted to make a modular product so that you can&nbsp; actually implement these things. Not everybody's&nbsp;&nbsp; going to need everything that we provide, and so&nbsp; we're trying to make sure that you can actually&nbsp;&nbsp; take parts of our stack and then be able to run&nbsp; it with your inference stack. Something that we&nbsp;&nbsp; are doing completely different now is that we&nbsp; decided to have an Apache 2 license. Before,&nbsp;&nbsp; if you look at a product like Triton inference&nbsp; server, you required to sign CCLA, and that was&nbsp;&nbsp; a very complicated process. You don't have to&nbsp; do that anymore, and we also have a public CI&nbsp;&nbsp; in place. So, if you contribute and then if you&nbsp; want to check whether your code is running now,&nbsp;&nbsp; you can do that, and we also decided to&nbsp; prioritize pip installs over containers. So,&nbsp;&nbsp; we think that you should be just able to get this&nbsp; running by doing pip install. And so, we are going&nbsp;&nbsp; to really prioritize pip installs over containers.&nbsp; This stack is also special because we took&nbsp;&nbsp; a lot of feedback from our customers. I think that&nbsp; there's a transition happening in the industry to&nbsp;&nbsp; Rust. And so, Rust gives you a benefit of memory&nbsp; safety and concurrency. And so, we decided to&nbsp;&nbsp; write a lot of the stack with Rust. But for&nbsp; developers, we also want extensibility. And so,&nbsp;&nbsp; we then decided to provide Python bindings, and&nbsp; you should be able to get the best of both worlds&nbsp;&nbsp; using that implementation. Lastly, we actually&nbsp; have a Discord setup. So, you can directly talk&nbsp;&nbsp; to our developers and then also solution&nbsp; architects. So, if you go to this link,&nbsp;&nbsp; there is a badge that you can see, and then you&nbsp; can just click on that badge and then be able to&nbsp;&nbsp; get onto our Discord server and then start talking&nbsp; with our experts. We then also really focused on&nbsp;&nbsp; our user experience. So, this is a CLI that we are&nbsp; providing. There are four subcommands with Dynamo.&nbsp;&nbsp; Run you can just quickly get your server set up&nbsp; and run, and then you can provide the input and&nbsp;&nbsp; output and model. It's that simple. And then&nbsp; the second CLI is Dynamo serve. So, with this&nbsp;&nbsp; CLI command or subcommand, you'll be able to&nbsp; construct a graph of workers and then serve. We&nbsp;&nbsp; know that a lot of developers want to deploy this&nbsp; into a production environment such as Kubernetes.&nbsp;&nbsp; So, we have two supplements that follow. It's an&nbsp; early implementation, but Dynamo build is going to&nbsp;&nbsp; containerize either the whole graph or the parts&nbsp; of the graph into multiple containers, and Dynamo&nbsp;&nbsp; deploy is going to deploy it into Kubernetes.&nbsp; But we are also letting developers specify their&nbsp;&nbsp; resources in Python decorators, so to make it&nbsp; a little bit easier than just using YAML. So,&nbsp;&nbsp; as features, we currently support three backends:&nbsp; TensorRT, BLM, and Lang. They're all supported&nbsp;&nbsp; with a varying degree of support, and this&nbsp; serving and KV cache routing is supported&nbsp;&nbsp; for TensorRT and BLM, and we are in early stages&nbsp; with the KV manager with the VM implementation,&nbsp;&nbsp; but a new better implementation is being worked on&nbsp; at this point. And finally, Nixel is supported for&nbsp;&nbsp; RDMA and TCP, so we are very excited about this&nbsp; product. Hope you get to try it, and if you like&nbsp;&nbsp; it, please give us a star on GitHub. I'd like&nbsp; to then pass the mic to our principal architect&nbsp;&nbsp; who is going to talk about the architecture&nbsp; and then the workflow. Thank you. [Applause]&nbsp; Thank you, Harry. So, I'm Neelay. Good afternoon,&nbsp; everyone. So, we go to the next slide. So, we talk&nbsp;&nbsp; about the architecture, and I think whenever we&nbsp; start something new, I like to kind of think back&nbsp;&nbsp; about where we've come from, right, and kind of&nbsp; why are we making the changes we're making. So,&nbsp;&nbsp; last year, almost a year ago, GTC 2024, I was on a&nbsp; stage similar to this talking about large language&nbsp;&nbsp; models and what they had brought to change in the&nbsp; landscape of inference serving. At that point,&nbsp;&nbsp; it really occurred to me that there are three&nbsp; big things that were happening. One, models&nbsp;&nbsp; were just getting huge, right? So, techniques&nbsp; for optimizations that you had for single node,&nbsp;&nbsp; single GPU models weren't scaling well. The second&nbsp; is that the types of batching and the types of&nbsp;&nbsp; model optimizations you had were changing. You had&nbsp; things like continuous batching, page detention,&nbsp;&nbsp; and finally, the developer community was moving&nbsp; so fast. Every week, new models are coming out,&nbsp;&nbsp; new techniques. We had a mixture of experts,&nbsp; we had Mamba, we had different snake-related&nbsp;&nbsp; things every week. And so, the pace was fast.&nbsp; So, what has changed here? I think the biggest&nbsp;&nbsp; thing that's changed is that we start to see&nbsp; that the optimizations you need to apply,&nbsp;&nbsp; and I think Harry alluded to this, is really&nbsp; at the system level. No longer is it fair to&nbsp;&nbsp; just optimize your model or optimize serving&nbsp; for a single node. You've got to really think&nbsp;&nbsp; about the entire system. New ways of routing, new&nbsp; ways of distributed caching, model disaggregation,&nbsp;&nbsp; resource planning—right at the data center level,&nbsp; not just a single node, not just a single server,&nbsp;&nbsp; but at a system level. Okay, interestingly enough,&nbsp; I think the other two things have stayed the same.&nbsp;&nbsp; The pressure of model size is still going to be&nbsp; there. Also, the pressure of developer velocity&nbsp;&nbsp; is going to be there. So, as we evolve the&nbsp; system, we can't leave those two things behind.&nbsp; Again, a year ago, I was talking about the&nbsp; move from local development to deployment. So,&nbsp;&nbsp; when you're developing something locally, your key&nbsp; challenge is probably just to make sure it works,&nbsp;&nbsp; right? Make sure your model has the accuracy you&nbsp; want, your pipelines, pre- and post-processing,&nbsp;&nbsp; are working correctly. When you go into&nbsp; deployment, and this is thinking about a&nbsp;&nbsp; single inference server, you start to look at how&nbsp; do you optimize your model for certain backends,&nbsp;&nbsp; so you may start in Python but then you may go&nbsp; to TensorRT, for example. And again, the key&nbsp;&nbsp; challenges are around batching and resource&nbsp; optimization. So, where we are a year later,&nbsp;&nbsp; as I mentioned, the challenges are becoming&nbsp; more system-related, and so we need to move&nbsp;&nbsp; from developing on a single system and a single&nbsp; inference server where your key challenges are&nbsp;&nbsp; really single-node performance, and if you&nbsp; spend any time with Triton inference server,&nbsp;&nbsp; that's really been the very much the focus of&nbsp; Triton inference server—how to get the most out of&nbsp;&nbsp; a single node, multi-GPU, sure, but didn't really&nbsp; scale to multiple GPUs. So, now we go forward.&nbsp;&nbsp; We're moving into Dynamo, which is the distributed&nbsp; inference serving platform or framework, as you&nbsp;&nbsp; like it. And now we got to start thinking about&nbsp; modularity. As Harry mentioned, we don't want a&nbsp;&nbsp; one-size-fits-all type of solution. You should be&nbsp; able to take pieces of it and reuse them or remix&nbsp;&nbsp; them to meet your solution needs. Second, we want&nbsp; to be flexible. We want to be easy to customize,&nbsp;&nbsp; so definitely we're Python first. You can take&nbsp; parts of the system, change it up in Python, and&nbsp;&nbsp; customize it for your system. Scalable—individual&nbsp; components need to scale independently. So, as you&nbsp;&nbsp; can see, this picture is a much more complicated&nbsp; picture. There are a lot more boxes on it,&nbsp;&nbsp; a lot more processes. The idea being that we break&nbsp; up, for example, the front end from the backends&nbsp;&nbsp; so you can scale them independently. Finally,&nbsp; things need to be highly efficient as always.&nbsp;&nbsp; We want to have highly optimized worker-to-worker&nbsp; communication, which is really device-to-device.&nbsp;&nbsp; And we want to be Rust native in this case.&nbsp; So, highly optimized core in Rust. And I&nbsp;&nbsp; try to make that little bit distinction, right?&nbsp; So, we're Python first because that's where the&nbsp;&nbsp; developers are, but that doesn't necessarily mean&nbsp; we're Python native. Right, so where we need to,&nbsp;&nbsp; we're going to push into a system language.&nbsp; And obviously, Rust is the best system&nbsp;&nbsp; language today in terms of memory safety, etc. So, the architecture. I think of it broadly into&nbsp;&nbsp; two pieces. There are the components and there's a&nbsp; set of infrastructure underneath. You can think of&nbsp;&nbsp; them as infrastructure services, but again, they&nbsp; support the components. And I'm going to just walk&nbsp;&nbsp; through a simple example of how that works. So,&nbsp; in this case, we just got a simple API server,&nbsp;&nbsp; a router, a worker, and again, in each of&nbsp; these, you'll see a highly optimized Rust&nbsp;&nbsp; core, and then on top of that, either Rust code&nbsp; because you can code in Rust or a Python piece&nbsp;&nbsp; of code. So, discovery. So, at the beginning of&nbsp; when the system starts up, each of the individual&nbsp;&nbsp; workers or components register and watch&nbsp; each other through the discovery plane. And today we're using CD for this, okay. So&nbsp; an example like the API server is going to&nbsp;&nbsp; register and watch for routers to become available&nbsp; because it's a totally dynamic system, right? So&nbsp;&nbsp; if it starts up and there's nothing there, it&nbsp; will wait, and if you send a request to it,&nbsp;&nbsp; it'll give you an error saying there&nbsp; are no routers available. Eventually,&nbsp;&nbsp; a router will show up. It registers. It's also&nbsp; going to start watching for workers. Finally,&nbsp;&nbsp; the workers will spin up. I guess obviously it's&nbsp; not in that order. They can come up in any order&nbsp;&nbsp; you want. But then the worker will initialize&nbsp; the actual LM async engine, and then the system&nbsp;&nbsp; becomes fully functional. Right? Again, if any of&nbsp; these components goes down, you can replace it.&nbsp;&nbsp; Right? It's fault tolerant in that sense. Next,&nbsp; we got the request plane. So we kind of translate&nbsp;&nbsp; from, let's say, an external API like OpenAI into&nbsp; internal Dynamo requests, and that's how we can&nbsp;&nbsp; have highly efficient communication within the&nbsp; system but still have a domain-specific API to&nbsp;&nbsp; the external world. Okay. So again, the API server&nbsp; is going to take the request in an OpenAI format,&nbsp;&nbsp; translate it into the internal format, the&nbsp; router is going to take those requests,&nbsp;&nbsp; and as Harry mentioned, kind of use an algorithm&nbsp; that combines both KB cache-aware routing as well&nbsp;&nbsp; as load matching to get you the best available&nbsp; worker. Okay, and finally, the worker is going&nbsp;&nbsp; to do the work. Right? That's why it's named a&nbsp; worker. So it'll execute the request, and we still&nbsp;&nbsp; have all the optimizations you have for a single&nbsp; node here: continuous batching, pre-filled decode,&nbsp;&nbsp; disaggregation, etc. So those aren't left behind&nbsp; because this worker is just going to be containing&nbsp;&nbsp; the engines that are already optimized. Next, you have events. So an API server,&nbsp;&nbsp; I kind of combine things. We might have a separate&nbsp; metric server, but just imagine that this is also&nbsp;&nbsp; going to have a Prometheus endpoint. It will&nbsp; listen and aggregate events and metrics. Okay.&nbsp;&nbsp; The router is the key piece here. It's actually&nbsp; going to be listening for updates from the&nbsp;&nbsp; workers to understand the global state of the&nbsp; KV cache. Right? That's how it's going to know&nbsp;&nbsp; how to route. And finally, the worker itself is&nbsp; going to be the event source. When any KB cache&nbsp;&nbsp; blocks get entered, exited, stored, or evicted,&nbsp; the worker is going to send that into the events&nbsp;&nbsp; bus. And again, we show specific listeners here,&nbsp; but really anybody can listen to anybody else.&nbsp; Finally, the data plane. And again, this is one&nbsp; of the key interesting new technologies. We're&nbsp;&nbsp; leveraging the new transfer library to really&nbsp; ensure that where we need it. So an example for&nbsp;&nbsp; larger requests, we can get device-to-device&nbsp; as close to zero-copy transfers as possible.&nbsp;&nbsp; Right? So for an API server, for small requests&nbsp; like LM serving requests, you may not need it;&nbsp;&nbsp; you may just push that straight into the request&nbsp; plane. But for larger, like images or streaming,&nbsp;&nbsp; we may end up using the Nixel transfer library.&nbsp; Okay. The router in this case is not really&nbsp;&nbsp; directly participating in data. It's not going to&nbsp; go from the API server through the router to the&nbsp;&nbsp; worker necessarily. The worker can talk directly&nbsp; to the API server. Right? And that's what we mean&nbsp;&nbsp; by the point-to-point optimized communication. And&nbsp; finally, in most of the examples you'll see today,&nbsp;&nbsp; the key thing that we use it for today&nbsp; is around device-to-device communication&nbsp;&nbsp; of KB cache. Right? We know that's a big size&nbsp; thing. We know we need to do that efficiently.&nbsp;&nbsp; So we have the new library to support that. Okay, a little bit more about Nixel. There&nbsp;&nbsp; are kind of three really key interesting things&nbsp; from my perspective. It's really cool technology.&nbsp;&nbsp; The three things that I like to think about&nbsp; when it is: one, it handles memory for you.&nbsp;&nbsp; So you register memory of your application with&nbsp; the communication library, so that makes you so&nbsp;&nbsp; you don't have to zero copy or you don't have to&nbsp; make copies. Right? You put your data straight&nbsp;&nbsp; in the buffer. That's the same buffer that the&nbsp; communication library will use to send it out.&nbsp;&nbsp; The second piece is that this metadata handler.&nbsp; We already described a little bit that this is a&nbsp;&nbsp; peer-to-peer system that doesn't require a central&nbsp; controller. So unlike NPI or Nickel or some things&nbsp;&nbsp; like that, which require kind of a static world&nbsp; size, this metadata handler can be expanded&nbsp;&nbsp; during run. So basically, the way we do it is&nbsp; these agents come up in individual processes,&nbsp;&nbsp; they transfer the metadata that allows them&nbsp; to keep open connections so they don't have&nbsp;&nbsp; to reconnect to their peers, and again allows it&nbsp; to discover peers as they show up. The third thing&nbsp;&nbsp; is that it's also a modular library. Numerous&nbsp; backends, we see X being one of them, but we're&nbsp;&nbsp; going to expand it, but it's also possible for&nbsp; external developers to expand it. Right? So if you&nbsp;&nbsp; have your own transfer mechanism that you want&nbsp; to do, you can just plug it into the backend,&nbsp;&nbsp; and then the rest of the system stays the same. 2024, I was up, I was really excited about this&nbsp;&nbsp; slide. You know, so we got to six steps to run an&nbsp; LM, but as Harry mentioned, it starts with Docker,&nbsp;&nbsp; and it takes a little bit of time and like really&nbsp; six steps just to get an LM going, and it wasn't&nbsp;&nbsp; even OpenAI at this point. Right? So where are we&nbsp; today? So with Dynamo, we're getting it down to&nbsp;&nbsp; five steps still, but five steps to go from your&nbsp; local machine to Kubernetes. Right? So it's a lot,&nbsp;&nbsp; same number of steps, well, less steps but a lot&nbsp; more functionality. We're really excited about&nbsp;&nbsp; this. So I go ahead and put this on the slide:&nbsp; the pip install. Right? Um, we obviously, we're&nbsp;&nbsp; developers just like anybody else, so we really&nbsp; love that ability. Right? Just go straight into&nbsp;&nbsp; pip. Don't have to build a container. Don't have&nbsp; to mess around with that. I still like containers,&nbsp;&nbsp; but anyway. Next, Dynamo run. Right? You want to&nbsp; get quick feedback. Right? You want to see if your&nbsp;&nbsp; model works in Dynamo just to start. So now we've&nbsp; got a new CLI subcommand called Dynamo Run. Um,&nbsp;&nbsp; it's pretty cool. Right? You can just give it&nbsp; the model name. You don't have to pre-download&nbsp;&nbsp; your model. You don't have to convert it. There's&nbsp; no engine building here. And you get a working&nbsp;&nbsp; prompt right in this case. I asked it to do a&nbsp; poem about a crab and a snake coming together&nbsp;&nbsp; and becoming best friends. Probably a little bit&nbsp; on the nose, but in any case, and it doesn't stop&nbsp;&nbsp; there. Right? So now you've got something running.&nbsp; You want to serve it. So in this next example,&nbsp;&nbsp; we're going to show kind of spinning up a local&nbsp; set of processes. And in the previous one,&nbsp;&nbsp; you don't actually have the full set, but in this&nbsp; case, you're going to have a router, HTTP server,&nbsp;&nbsp; and four workers. So the cool thing here, if I go&nbsp; back, but you can just say basically Dynamo serve&nbsp;&nbsp; and tell it I want four workers and give it the&nbsp; same model name, and it spins up everything for&nbsp;&nbsp; you. Right? And yep, now you can see all the four&nbsp; workers coming up, and you can see this dynamic&nbsp;&nbsp; part too. Right? So one of the routers is looking&nbsp; for says like there's currently zero workers,&nbsp;&nbsp; but there's required one. It's waiting for workers&nbsp; to be ready. Now you see the VM worker is getting&nbsp;&nbsp; installed and initialized, and there, oh, and then&nbsp; I turned it off. Wait. So in between here, because&nbsp;&nbsp; I didn't show the actual curl requests, you'll see&nbsp; actual requests getting added. So added request,&nbsp;&nbsp; average prompt throughput, all the metrics and&nbsp; stuff happening. And Tommy will give you a little&nbsp;&nbsp; bit more detail of these things as well. After you've served, you then maybe do&nbsp;&nbsp; want containers. Right? So Dynamo now has a&nbsp; feature to be able to build kind of specific&nbsp;&nbsp; containers for your deployment graph. In this&nbsp; case, you can containerize your hello world or&nbsp;&nbsp; your large language model and then get it ready&nbsp; for deployment. Finally, the last piece of this&nbsp;&nbsp; is a one-liner to deploy to Kubernetes. This&nbsp; is something we're actively just kind of&nbsp;&nbsp; still developing. It's an active project, but&nbsp; coming soon. So this is the kind of leads out&nbsp;&nbsp; to the final piece, right? And with that, I&nbsp; will turn it over to [Applause] Tommy. Thanks.&nbsp; Hi, everyone. Thank you all for being here. Nille&nbsp; introduced Dynamo CLI to us, specifically Dynamo&nbsp;&nbsp; Serve. So we are going to use Dynamo serve&nbsp; to show about the performance optimization&nbsp;&nbsp; techniques that Harry talked about, how we can&nbsp; use those and integrate it in Dynamo. A couple&nbsp;&nbsp; of them that Harry mentioned are listed here, and&nbsp; we will go through each of them one by one. The&nbsp;&nbsp; first is disaggregated serving, KV cache-aware&nbsp; routing, and then KB cache block manager. For&nbsp;&nbsp; disaggregated serving, it's all about separating&nbsp; the pre-fill stage of the LLM inference to the&nbsp;&nbsp; decode. In the Dynamo framework, what we have to&nbsp; support this aggregated serving is that we will&nbsp;&nbsp; launch two separate worker processes: VLM worker&nbsp; and the prefill worker. We will see how it how&nbsp;&nbsp; easy it is to set up this kind of an environment&nbsp; in the next couple of slides, but let me go over&nbsp;&nbsp; the logic a bit about how they interact. So when&nbsp; the VLM worker sees that there is a request,&nbsp;&nbsp; it based upon certain criteria decides whether it&nbsp; should run the prefill of this request locally or&nbsp;&nbsp; should it port it to the remote worker, which&nbsp; will run the prefill for it. And while the&nbsp;&nbsp; prefill worker is calculating the context of the&nbsp; request, it's free to execute other requests. And&nbsp;&nbsp; once the prefill worker has the context ready&nbsp; for the request, it notifies the VLM worker and&nbsp;&nbsp; uses Nixl to provide the generated context back&nbsp; to the VLM worker. Upon receiving this context,&nbsp;&nbsp; the VLM worker can schedule this decoding&nbsp; and return the final response. So this is&nbsp;&nbsp; the conditional disaggregation which Harry was&nbsp; talking about because the disaggregation only&nbsp;&nbsp; happens under certain criteria where it gives&nbsp; us improvements, like when you have a large&nbsp;&nbsp; context or a large input token length. So we&nbsp; will see now how to deploy this in Dynamo.&nbsp; The single-line command to launch a disaggregated&nbsp; deployment is this. We just point it to a disag&nbsp;&nbsp; graph and provide certain configurations to launch&nbsp; this disagraph. Now, let's go deep, do a deep dive&nbsp;&nbsp; into the graph like what we provided the Dynamo&nbsp; serve command. We see a front end which links to&nbsp;&nbsp; a processor which links to a VLM worker and which&nbsp; links to a prefill worker. How Dynamo sees this&nbsp;&nbsp; is as this. So HTTP here is the API server which&nbsp; Nille was mentioning. And then we have another&nbsp;&nbsp; process for preprocessing and postprocessing,&nbsp; which tokenizes and detokenizes the requests.&nbsp;&nbsp; Right now, for decided serving, it will talk&nbsp; to the VLM worker. The VLM worker would decide&nbsp;&nbsp; whether it needs to run a remote prefill. The&nbsp; same thing summarized here. The configuration,&nbsp;&nbsp; like what is the configuration that we&nbsp; provided, looks like this. The front end&nbsp;&nbsp; configuration looks like this: the model&nbsp; name, the endpoint that I need to hit,&nbsp;&nbsp; and the port where the API server would listen&nbsp; to the requests. For the processor, currently,&nbsp;&nbsp; we are using a round-robin router. We will go to&nbsp; KV cache-aware routing later, but for this part,&nbsp;&nbsp; we will use round-robin. The VLM worker configs&nbsp; look like this. We have to enable conditional DSA,&nbsp;&nbsp; we have to enable remote prefill, and the&nbsp; condition that we are currently going to use&nbsp;&nbsp; is like max local prefill length is 10. So if&nbsp; the request smaller than token size 10 comes,&nbsp;&nbsp; then I should still do the local prefill instead&nbsp; of remote prefill. And you can see the KV transfer&nbsp;&nbsp; config there, Dynamo Nixel connector, which is&nbsp; using our new Nixel library. And the prefill&nbsp;&nbsp; config is the same. We tell it like you will&nbsp; be getting the data from Nixl, and that's it.&nbsp; So once you launch this command, Dynamo serve&nbsp; these graphs, you will see a bunch of logs coming,&nbsp;&nbsp; and it shows that the endpoint that you requested&nbsp; has been added, and the HTTP server is up.&nbsp;&nbsp; Over here, you see that the VLM worker and the&nbsp; prefill worker have loaded the model and are also&nbsp;&nbsp; initialized and ready to serve requests. Now, we&nbsp; will send a short prompt, and this should ideally&nbsp;&nbsp; not trigger the disaggregated serving. So we just&nbsp; see that normally the prompt like the request&nbsp;&nbsp; got added to the engine, and all the prompts&nbsp; were generated by the same engine. The next,&nbsp;&nbsp; we send a big, oh yeah, this is the response that&nbsp; we get when we send that hello prompt. So next,&nbsp;&nbsp; as a next experiment, we send a very long token&nbsp; length prompt. It's to be precise almost like 193&nbsp;&nbsp; tokens. And this should ideally now trigger remote&nbsp; prefill. We see the logs again on the server side.&nbsp;&nbsp; It says that it found the input had 193 tokens,&nbsp; so we should be running prefill remotely, and that&nbsp;&nbsp; request gets added to the remote worker. And then&nbsp; whenever the results are ready, this VLM worker&nbsp;&nbsp; pulls the request from the queue, loads it into&nbsp; its engine, and runs the remaining decode steps.&nbsp;&nbsp; So this is the final message that we&nbsp; get. So yeah, and another thing that&nbsp;&nbsp; was talked about was how configurable these&nbsp; are or scalable these technologies are. So&nbsp;&nbsp; the previous one just launched a specific number&nbsp; of workers, but usually in disaggregated serving,&nbsp;&nbsp; we might need more prefill workers. So&nbsp; once you have the deployment working,&nbsp;&nbsp; you can just launch a new prefill worker, and it&nbsp; will start listening for new requests to the same&nbsp;&nbsp; prefill queue and sharing the load so&nbsp; that it doesn't get the TTFTs bogged down. So this is how we kill the deployment. All the&nbsp; parent processes of this deployment would be&nbsp;&nbsp; cleaned up right now. With that, the disaggregated&nbsp; serving tutorial is done. We will move next to the&nbsp;&nbsp; KV cache-aware routing. So Harry did talk about&nbsp; KV cache-aware routing quite a bit, but one thing&nbsp;&nbsp; I would like to talk more about is the KV cache&nbsp; router needs a global view of the entire workload.&nbsp;&nbsp; So once it sees the query, simply saying that&nbsp; okay, there's the highest KV match with worker&nbsp;&nbsp; three, then I should send the request to worker&nbsp; three—that should not be the correct way of&nbsp;&nbsp; handling things. We have a mechanism in Dynamo&nbsp; where we calculate the score of each worker,&nbsp;&nbsp; which we calculate as: okay, this is the&nbsp; query, the tokens have come, and now with that&nbsp;&nbsp; prefix cache tree, we know how much cache hit we&nbsp; will be getting at each worker, but we also need&nbsp;&nbsp; to know how much load they are currently under.&nbsp; So in this case, even though worker three had the&nbsp;&nbsp; highest KB match, because it was under the highest&nbsp; load (80%), its score was lower than worker two.&nbsp;&nbsp; So we will route the request to worker two. Now,&nbsp; these are certain steps to use KV routing with&nbsp;&nbsp; Dynamo. Same thing, same single-line command.&nbsp; You just point it to another graph and another&nbsp;&nbsp; set of configs. When this command is launched,&nbsp; you see a new prompt, a new log message coming,&nbsp;&nbsp; which is called "KV router initialized" over here&nbsp; in the graph part. There is another component,&nbsp;&nbsp; KV router, which a processor links to before the&nbsp; VLM worker. So how does Dynamo see it? It sees&nbsp;&nbsp; it like this: there is an HTTP front end, then&nbsp; pre and post processor, and a set of VLM workers&nbsp;&nbsp; which we need for this demonstration purpose. And&nbsp; each of these VLM workers and another KV router,&nbsp;&nbsp; we will go to the next things later, but&nbsp; the entire logic of the KV router is that&nbsp;&nbsp; VLM workers are configured to publish KV events&nbsp; and certain metrics which the KV router keeps&nbsp;&nbsp; track of. Upon receiving a request from the API&nbsp; server, the pre and postprocessor queries which,&nbsp;&nbsp; with the given incoming tokens, would be the&nbsp; best worker to run this request on, and the KV&nbsp;&nbsp; router returns that worker ID, and the pre and&nbsp; postprocessor route the request to the specific&nbsp;&nbsp; worker. How is it enabled in the config? This you&nbsp; need another service called router. In this case,&nbsp;&nbsp; I have set it as min workers to two. So the&nbsp; router would not start sending the request to&nbsp;&nbsp; any of the workers till it sees that there are two&nbsp; workers up. For the worker side, so that they can&nbsp;&nbsp; emit all the KB cache events and the metrics and&nbsp; everything, we need that router KV specification.&nbsp;&nbsp; And because we are routing the request with the&nbsp; highest KV match, we need to enable prefix caching&nbsp;&nbsp; to see the performance improvement and see the&nbsp; caching work. For this demonstration purpose,&nbsp;&nbsp; I will be using the two workers which have been&nbsp; specified in the service arcs. So I have this&nbsp;&nbsp; deployment up, and I would like to demonstrate&nbsp; that the KV cache-aware routing is working. What&nbsp;&nbsp; I do is I created two clients. One client would&nbsp; send the same prompt again and again, one after&nbsp;&nbsp; the other. Client two generates random prompts one&nbsp; after the other. The load isn't high because we&nbsp;&nbsp; have two workers. We have two clients who are just&nbsp; sending one request at a time. So the router sees&nbsp;&nbsp; that the KV match is the highest with a specific&nbsp; VLM worker, and we see which, in this case, is&nbsp;&nbsp; VLM worker one. The metrics that get reported for&nbsp; the prefix cache hit rate is like 96.55% in this&nbsp;&nbsp; case. So the identical requests are all going to&nbsp; VLM worker one. The random requests for VLM worker&nbsp;&nbsp; 2 are just giving no cache rate because it's all&nbsp; random that's being sent. Now, for demonstration&nbsp;&nbsp; purposes again, like we kill this deployment,&nbsp; start the same deployment but without the router,&nbsp;&nbsp; and by default, it uses round-robin. So now&nbsp; what happens in the same setup, the two clients,&nbsp;&nbsp; both the workers are seeing some prefix cache hit&nbsp; rate because the identical requests are hitting&nbsp;&nbsp; both the workers. So we also have a metrics&nbsp; dashboard where you can see all these metrics in&nbsp;&nbsp; nice Grafana visualizations, and you can determine&nbsp; what should be your configurations for the best&nbsp;&nbsp; prefix cache hit rate depending upon your workload&nbsp; in real time. So yeah, the next piece that I would&nbsp;&nbsp; like to cover is the KV cache block manager,&nbsp; which Harry mentioned that is in early stages,&nbsp;&nbsp; and we are working on getting a better distributed&nbsp; version which can work across the node. For the&nbsp;&nbsp; numbers that Harry showed, what was done was that&nbsp; we had created some KV cache pool on the host and&nbsp;&nbsp; SSD memory, CPU memory, and the SSD memory.&nbsp; The device memory was left free. So all the&nbsp;&nbsp; KV cache blocks were pushed downwards to the lower&nbsp; memory levels, keeping the device memory free so&nbsp;&nbsp; that they can handle more requests and give better&nbsp; throughput. All this execution is asynchronous. So&nbsp;&nbsp; async put, async get, everything is asynchronous.&nbsp; So it doesn't impact the real-time performance&nbsp;&nbsp; whenever the inference engine needs the same&nbsp; KB cache block. It can be pulled from SSD or&nbsp;&nbsp; host memory, wherever it is the closest. It's&nbsp; like the entire caching CPU caching. Earlier,&nbsp;&nbsp; Jensen mentioned Dynamo to be the operating system&nbsp; of AI, and I think it's this that matches. It's&nbsp;&nbsp; still in a very early stage, and we are working&nbsp; on making it more distributed and more robust,&nbsp;&nbsp; but this was the scenario that we used to get&nbsp; those numbers. Um, next, I will hand it over to&nbsp;&nbsp; Ryan to go over the API deep dive. [Applause]&nbsp; I am the crabby one. Definitely the crabby one.&nbsp; So I am both a solution architect and an engineer.&nbsp;&nbsp; And before I go into the code, because Jensen&nbsp; says you know at GTC you pay and you get hit with&nbsp;&nbsp; equations and you get hit with code. So these guys&nbsp; have done a great job of giving you the why and&nbsp;&nbsp; then the kind of the how and like the overview,&nbsp; which was actually those slides were amazing,&nbsp;&nbsp; and then Tom showed us that we built all this&nbsp; LLM stuff on this thing. This thing is Dynamo,&nbsp;&nbsp; and it's our data center scale inference strategy,&nbsp; but it's also the library underneath, and these&nbsp;&nbsp; slides are going to just give you a little bit&nbsp; of insight on how we actually built those tools,&nbsp;&nbsp; so the scalable components that you're going&nbsp; to have to play with to go and build your own&nbsp;&nbsp; tools. But before I jump into that, I'm going&nbsp; to put my sales and solution architect hat on&nbsp;&nbsp; that says if you're a NIM customer today, you're&nbsp; probably wondering what should I do about Dynamo.&nbsp;&nbsp; Dynamo is coming to you soon. If you are thinking&nbsp; about oh, you've presented all this amazing stuff&nbsp;&nbsp; with KV-aware routing and you've presented&nbsp; disaggregated workloads, you definitely want&nbsp;&nbsp; to look at the KV-aware router. You're going to&nbsp; get instant value from that. So if those are your&nbsp;&nbsp; workloads and you're not in them and you're in&nbsp; Triton today, definitely consider thinking about&nbsp;&nbsp; migrating or looking at and playing with a Dynamo&nbsp; stack. Pre-filled disaggregation, right? You need&nbsp;&nbsp; a little extra hardware. You need performance&nbsp; to move that KV. So you need to have the network&nbsp;&nbsp; backplane to support that data movement. It's&nbsp; also a little bit more dependent on your model.&nbsp;&nbsp; NIM is going to help you with that. Models that&nbsp; are very helpful or very like the different&nbsp;&nbsp; configurations for pre-fill and decode—that's&nbsp; going to be provided to you by NIM itself,&nbsp;&nbsp; but feel free to play with it. If you're like&nbsp; a Deepseek, you're definitely going to want&nbsp;&nbsp; to think about that one. All right, now we're&nbsp; going to look at code. And I don't know which&nbsp;&nbsp; button to hit. I think it's the green one. It&nbsp; is the green one. Excellent. We're going to look&nbsp;&nbsp; at code. Before we get into that, the library&nbsp; is broken up into two parts. We have our core&nbsp;&nbsp; Dynamo runtime and our Dynamo LLM separated out&nbsp; so that they're logically all the LLM bits are in&nbsp;&nbsp; the LLM, and all the runtime bits are in the&nbsp; runtime bits. That's mostly written in Rust,&nbsp;&nbsp; and then our bindings, you can see them there&nbsp; in the lib bindings Python. The mixed wheels,&nbsp;&nbsp; the wheels are built with matur, just mixed&nbsp; bindings. We're at 0.1.0. We just released&nbsp;&nbsp; today. It's not mature. We're getting there. We&nbsp; are so excited to build this in the open source&nbsp;&nbsp; with everybody, but we are just at the beginning.&nbsp; This is exploratory, and we're going to have so&nbsp;&nbsp; much fun building this out. So we made everything&nbsp; public, like all the way down. We're source code's&nbsp;&nbsp; public, but even the internals like right now,&nbsp; we're maximizing pub on everything. So like you&nbsp;&nbsp; can touch all the internals of the library.&nbsp; Eventually, we're going to consolidate that&nbsp;&nbsp; API. So over the course of the first few months,&nbsp; we're going to kind of open it up, give everybody&nbsp;&nbsp; access to all the internals, see what you can do&nbsp; about it, and then we're going to start making&nbsp;&nbsp; logical compressions of that API. The four things,&nbsp; top-level objects down here, are going to be your&nbsp;&nbsp; distributed runtime object, your namespace,&nbsp; component, and endpoint objects. These are the&nbsp;&nbsp; core building blocks that we start at the bottom,&nbsp; and you build the whole LLM stack up off of these.&nbsp;&nbsp; These are the three most important things when&nbsp; addressing when you want to run something in a&nbsp;&nbsp; distributed environment. So whether you&nbsp; have two nodes or you have 10,000 nodes,&nbsp;&nbsp; you need an address for those things.&nbsp; And we address all the components,&nbsp;&nbsp; all the runnables out there, with three things:&nbsp; a namespace, a component, and an endpoint.&nbsp;&nbsp; Namespace provides a logical grouping, generally&nbsp; like if you have a distributed model like fu bar&nbsp;&nbsp; llama 3, you might put it in a namespace, that way&nbsp; your pre-processor is isolated to your namespace,&nbsp;&nbsp; so you can have a pre-processor in different&nbsp; model spaces. Your component will be your router,&nbsp;&nbsp; your pre-processor, your backend. These are&nbsp; either services; they're also like logical&nbsp;&nbsp; points for emitting events, there are also logical&nbsp; points for configuration on the discovery plane.&nbsp;&nbsp; These are just addresses. And then I'll show&nbsp; you in a second how we actually serve something.&nbsp;&nbsp; Components can serve functionality or handlers on&nbsp; endpoints. So a full function, like a full remote&nbsp;&nbsp; procedure call, requires a namespace, component,&nbsp; and endpoint. I'm supposed to watch the time real&nbsp;&nbsp; quick so we don't blow over. I've got four to&nbsp; seven minutes. I want to serve this function.&nbsp;&nbsp; Super basic function request handler. This can&nbsp; be stateful or not stateful. If it's stateful,&nbsp;&nbsp; you just plug in your state in the init. And the&nbsp; only thing that you need to do to serve a function&nbsp;&nbsp; in Dynamo is create an async generator in Python.&nbsp; That's it. It's probably one of the easiest&nbsp;&nbsp; onboarding to distributed serving or distributed&nbsp; anything out there. Bring your async generator,&nbsp;&nbsp; and we'll serve it. So in this case, we just take&nbsp; a string in and we yield each character back.&nbsp;&nbsp; How do we serve it? Well, there's this worker&nbsp; function up here. This is kind of your entry&nbsp;&nbsp; point. And you wrap it with a Dynamo worker&nbsp; decorator. This is what's going to coordinate the&nbsp;&nbsp; Python async runtime with the Rust async runtime.&nbsp; And then you just call into your main application&nbsp;&nbsp; from there. In our main application, you can see&nbsp; right here that we have our runtime. We passed in&nbsp;&nbsp; a namespace called Dynamo. And the first thing we&nbsp; do is generate our address. So our component is&nbsp;&nbsp; our runtime namespace. We give it the namespace&nbsp; name. And then we give it the component in the&nbsp;&nbsp; same place we're calling it backend. To serve&nbsp; our async generator, we just have to say that&nbsp;&nbsp; the component is going to serve something. So we&nbsp; create a service that sets up the infrastructure&nbsp;&nbsp; to serve something. Now, we need to serve it on&nbsp; something. So we say component.endpoint. Endpoint.&nbsp;&nbsp; We're going to serve it on the name generate.&nbsp; Ultimately, our address is dynamobackend.generate,&nbsp;&nbsp; and we start the endpoint service by just passing&nbsp; in an instance of the generator. So we create an&nbsp;&nbsp; instance of that class, give it the generator&nbsp; function, and we just await that. Now, if you&nbsp;&nbsp; wanted to serve multiple things, you would want&nbsp; to spin that off in a task. But at this point,&nbsp;&nbsp; while we await that endpoint, it's being served.&nbsp; How do we call it? Just as easy. We instantiate&nbsp;&nbsp; our distributed runtime. We address it with our&nbsp; namespace, component, endpoint, and then we create&nbsp;&nbsp; a client. Once we get that client object,&nbsp; that await client is kind of magical because&nbsp;&nbsp; that client is getting live updates across the&nbsp; distributed system when new workers come up and&nbsp;&nbsp; new workers go down. So if we hadn't brought up a&nbsp; worker yet and we wanted to await for some to be&nbsp;&nbsp; present, we can just call await for endpoints.&nbsp; So that client is an example of our discovery&nbsp;&nbsp; service at work, live things going up, going down,&nbsp; and reconfiguring the internals of that client on&nbsp;&nbsp; demand. Our clients are essentially routers.&nbsp; You can call generate, you can call random,&nbsp;&nbsp; you can call direct. Direct allows you to actually&nbsp; target direct endpoints specifically. All we have&nbsp;&nbsp; to do is call client.generate. We await the&nbsp; stream and then we process the stream. Guess&nbsp;&nbsp; what it says, like hello world or something like&nbsp; that. All right. And we can do this in Python or&nbsp;&nbsp; we can do this in Rust. This is a really quick&nbsp; example of Rust. So maybe the Python people out&nbsp;&nbsp; there in the audience are like maybe I could&nbsp; do Rust. It's a little more complicated, but&nbsp;&nbsp; got to put a lot of typing stuff on there,&nbsp; but it's super performant. And guess what,&nbsp;&nbsp; the compiler checks a lot of your errors for you.&nbsp; It's pretty lovely. One of the things I just want&nbsp;&nbsp; to point out at the top, you see that we have&nbsp; single in, many out. The entire Python API is&nbsp;&nbsp; single in, many out. The Rust API can be far more&nbsp; advanced, although we only have implementations&nbsp;&nbsp; for single in, many out. We do have ambitions to&nbsp; do that more in the future, but we want to focus&nbsp;&nbsp; on what we're good at. And again, you can cloud&nbsp; on the same way. Rust client looks much the same. That's the request-response paradigm. The&nbsp; next thing that's most important that you saw&nbsp;&nbsp; examples in the talks is the event plane. So we do&nbsp; events via pub-sub mechanism. That's core built,&nbsp;&nbsp; or like our pub-sub broker in the background is&nbsp; NATO. It's fantastic for this purpose. And what&nbsp;&nbsp; we do is we apply traits to each of our four&nbsp; primary components. These are hierarchical,&nbsp;&nbsp; this is a hierarchy of addresses. So you&nbsp; can emit events on the distributed runtime,&nbsp;&nbsp; which would be a global event which everybody in&nbsp; the entire system across all your 10,000 nodes&nbsp;&nbsp; would see. You can emit it on your namespace;&nbsp; all your components within that namespace can&nbsp;&nbsp; see those events. You can emit it on components&nbsp; and likewise down to functions. And so it's just a&nbsp;&nbsp; convenient way to address them so that you don't&nbsp; have to come up with random different names to&nbsp;&nbsp; avoid name collisions in there. And mostly we emit&nbsp; our events in JSON, but if you want to use your&nbsp;&nbsp; own serializer, like if you want to use whatever,&nbsp; bring capn proto or something, you can use the&nbsp;&nbsp; bytes variant. This is what it looks like. It's&nbsp; in Rust code. All you do is create a distributed&nbsp;&nbsp; runtime. Here we're going to do it on the&nbsp; namespace. So our namespace subscribe. We got our&nbsp;&nbsp; subscriber. Namespace publish. And then we'll just&nbsp; pull the subscriber. And voila, our test passed.&nbsp; Discoverability is kind of the magic in the&nbsp; distributed system. You have 10,000 nodes. You&nbsp;&nbsp; have lots of processes. You really don't want to&nbsp; manually configure all your different things, and&nbsp;&nbsp; you want to actually configure them in the right&nbsp; way. This is a huge challenge and something that&nbsp;&nbsp; you have to really think about and get right when&nbsp; you scale out. So our discoverability is going to&nbsp;&nbsp; be core based on CD. Right now, we don't have an&nbsp; API for it. It's just using the raw NATS clients&nbsp;&nbsp; and a second-level client that lets you do atomic&nbsp; operations. So the create is an atomic create;&nbsp;&nbsp; create and validate to make sure that the thing&nbsp; that you would have put there is actually the&nbsp;&nbsp; thing that is there. If not, it throws an error.&nbsp; And finally, the last one is the get and watch,&nbsp;&nbsp; which is the magic that makes the client&nbsp; auto-update based on worker availability. Again,&nbsp;&nbsp; this is a work in progress. We'd love for people&nbsp; to kick the tires and give us some good feedback.&nbsp;&nbsp; The LLM components are written in the Dynamo&nbsp; LLM crate. I listed them out. The HTB service&nbsp;&nbsp; is our OpenAI HTTP service. It uses AXM for that.&nbsp; The preprocessor does the prompt templating and&nbsp;&nbsp; tokenization. The model card is super important.&nbsp; I'm going to talk about it below. The KV-aware&nbsp;&nbsp; router is where all the magic that Ton talked&nbsp; about and Nille. The disag router is also there.&nbsp;&nbsp; And then we have some convenient wrappers for&nbsp; tokens and tokenizer. Model deployment card is&nbsp;&nbsp; the last thing I want to touch on before I'm done.&nbsp; And I have one minute to do that. Let's see if I&nbsp;&nbsp; can get it done. I might go one minute over.&nbsp; Sorry, guys. The biggest problem here is that&nbsp;&nbsp; in a distributed system, you want to make sure&nbsp; that your pre-processor, your router, and your&nbsp;&nbsp; backends are all using the same pieces for that&nbsp; given model. You screw up the tokenizer on your&nbsp;&nbsp; pre-processor, your backend is going to output&nbsp; garbage. And what kind of garbage does it output?&nbsp;&nbsp; You don't know. Like maybe it's semi-okay. We want&nbsp; to nip that in the bud. And so we thought about&nbsp;&nbsp; this pretty deeply. Our model deployment card&nbsp; is a set of assets that the model needs that the&nbsp;&nbsp; distributed components need. So you instantiate&nbsp; your pre-processor with a model deployment card.&nbsp;&nbsp; You instantiate your router with the model&nbsp; deployment card. And one thing publishes your&nbsp;&nbsp; model deployment card to the discovery service&nbsp; so that when you pick it up, they're all happily&nbsp;&nbsp; in sync with each other. I think this is really,&nbsp; really important. I'm hoping that the community&nbsp;&nbsp; gets behind this, and we can broadly adopt a model&nbsp; deployment card across different backends. I think&nbsp;&nbsp; this is super, super important. And I think with&nbsp; that, I will—oh, the last thing I'm going to say&nbsp;&nbsp; about the model deployment card is that you can&nbsp; chain the hash of the deployment card. So you&nbsp;&nbsp; take a hash of that value, the bytes in your model&nbsp; deployment card, and you propagate that with the&nbsp;&nbsp; request as it goes through the system. So that as&nbsp; a request goes from pre-processor to the router,&nbsp;&nbsp; router to the backend, the backend can look&nbsp; at the request and say, "I know the hash of&nbsp;&nbsp; my model deployment card, and I see that the hash&nbsp; of your model deployment card matches. I'm good.&nbsp;&nbsp; I'm going to take this request." Doesn't match?&nbsp; Well, I'm going to call panic or something. I'm&nbsp;&nbsp; done. I don't know what to do. You someone done&nbsp; screwed up, and we don't know what to do anymore.&nbsp;&nbsp; Okay, with that, I think we've got Harry. [Applause]&nbsp; I'm gonna get Harry's side for him. Thanks,&nbsp; Ryan. That's an awesome design. I only have&nbsp;&nbsp; two more slides, and so I wanted to talk about&nbsp; the next steps. The first immediate next steps:&nbsp;&nbsp; we really want to get your feedback. And we also&nbsp; want to collaborate with you. That's the whole&nbsp;&nbsp; reason why we are doing this out in the open. So&nbsp; please try our stack, and then we'd love to get&nbsp;&nbsp; your feedback. If you have any issues, please&nbsp; file it on GitHub, and then we'll get to that&nbsp;&nbsp; issue as soon as possible. And also, we have a&nbsp; Discord set up so you can talk to us directly&nbsp;&nbsp; with our engineers and also solution architects.&nbsp; In terms of the features that are coming soon,&nbsp;&nbsp; there are two major features that we are&nbsp; expecting. The first one, as we talked about&nbsp;&nbsp; before, we wanted to enable KB cache offloading,&nbsp; so a KB manager that is capable of managing the&nbsp;&nbsp; memory at a different memory hierarchy, including&nbsp; system memory or TPU memory, system memory, SSD,&nbsp;&nbsp; and then also object storage. We'll take the same&nbsp; approach that we took with Dynamo. So we want to&nbsp;&nbsp; take the best of both worlds of Rust and Python.&nbsp; So we are going to have it written in Rust for&nbsp;&nbsp; performance and then also Python for the maximum&nbsp; extensibility. There's a component that we didn't&nbsp;&nbsp; talk about in detail, but we have a component that&nbsp; will be released very soon. It's called planner.&nbsp;&nbsp; And planner's job is to do real-time performance&nbsp; tuning at a data center scale. So we're likely to&nbsp;&nbsp; release something around this serving, being able&nbsp; to dynamically allocate prefill and decode. But&nbsp;&nbsp; what we think that this should be is almost&nbsp; like a full-fledged reinforcement learning&nbsp;&nbsp; platform. So if you think about it in a data&nbsp; center scale, if you can actually have the user&nbsp;&nbsp; or the developer set up an objective, say, for&nbsp; example, throughput constraint on say TTFT or ITL,&nbsp;&nbsp; and then you can tune for the policy to find the&nbsp; best ratio of prefill and decode, and then the&nbsp;&nbsp; state would be kind of the system configuration.&nbsp; That's kind of where we get to. So being able to&nbsp;&nbsp; have the developers use that framework almost as&nbsp; a reinforcement learning platform and being able&nbsp;&nbsp; to tune it. Ryan already mentioned this, but we&nbsp; have a Nvidia NIM that is going to support very&nbsp;&nbsp; much accelerated deployment to your data center,&nbsp; and so it's going to be as easy as specifying&nbsp;&nbsp; the number of GPUs, what is your SLA, and then&nbsp; being able to just specify the model, and then you&nbsp;&nbsp; can deploy it right away. If you want to connect&nbsp; with us, we only have two sessions. So we have two&nbsp;&nbsp; connect with experts starting today at 4 p.m.&nbsp; And then there's also another one at 9:00 a.m.&nbsp;&nbsp; And so if you'd like to talk to us directly in&nbsp; person, we'll be there. We'd love to see you.&nbsp;&nbsp; So that concludes our talk. Thank you so much for&nbsp; coming. And we would like to now take questions.&nbsp; [Applause] Thank you very much, Harry. Thank you,&nbsp;&nbsp; Neil. Thank you, Tanmay. And thank you, Ryan.&nbsp; So we have a few more minutes for questions. So&nbsp;&nbsp; if anybody has a question, do we have an extra&nbsp; mic in the room? Is there an extra mic for the&nbsp;&nbsp; questions? If not, I'll just walk down through&nbsp; the front here, and maybe we'll start with you.&nbsp;&nbsp; Thanks for great talks, and I'm just&nbsp; wondering whether you guys are considering&nbsp;&nbsp; hybrid configuration or like throttling power&nbsp; consumption because I think this reminds me of&nbsp;&nbsp; the Split-Wise paper, and I thought like the&nbsp; best benefit of having this kind of multiodal&nbsp;&nbsp; configuration is that, of course, we can handle&nbsp; different kinds of workload characteristics,&nbsp;&nbsp; like prefill and decode. And then some guys have&nbsp; some compute-bound characteristics, and another&nbsp;&nbsp; guy has memory-bound bandwidth. And then the best&nbsp; gain probably coming from a hybrid configuration&nbsp;&nbsp; like H100 versus A100 as written in the Split-Wise&nbsp; paper like a two years back then or like power&nbsp;&nbsp; throttling again suggested by Split-Wise.&nbsp; Have you considered that kind of approach?&nbsp; Did you consider it a split device? Yes,&nbsp;&nbsp; different GPUs. Yeah, the combining different&nbsp; types of architectures, I think this one's—so&nbsp;&nbsp; the question is around being able to do&nbsp; disaggregation across different types of&nbsp;&nbsp; architectures and GPUs. Yes, that's definitely&nbsp; something that we're looking at. And I think&nbsp;&nbsp; to your point of being able to maximize the&nbsp; efficiency of the different stages, right? Yeah,&nbsp;&nbsp; so we're looking into it for sure, definitely. Hi, I'm Dennis, a machine learning engineer,&nbsp;&nbsp; and how does it support autoscaling? So we&nbsp; have application metrics, you know, that oh,&nbsp;&nbsp; that was me, sorry, we're on the same RF talking.&nbsp; Yeah, know that's that's it. I guess basically,&nbsp;&nbsp; you're giving us nice application metrics, and&nbsp; I'm wondering how autoscaling kind of works in&nbsp;&nbsp; the background for request models. I mean, I&nbsp; guess go start. I mean, I think we are leaning&nbsp;&nbsp; into Kubernetes as being the first class kind&nbsp; of deployment. We're trying to build things&nbsp;&nbsp; modular enough that you could use other types of&nbsp; orchestration frameworks as well. But our first&nbsp;&nbsp; plan is to support autoscaling through Kubernetes&nbsp; but to give it LM-specific metrics to look at and&nbsp;&nbsp; to be faster than a standard Prometheus&nbsp; loop. We're also considering supporting&nbsp;&nbsp; SLURM. So if you're interested in that, please&nbsp; let us know. And then we talked about planner,&nbsp;&nbsp; the component that was mentioned in the next step,&nbsp; and so that will also facilitate how to autoscale&nbsp;&nbsp; depending on your required SLA. So those are&nbsp; kind of the approaches that we are taking.&nbsp; Really quickly here, one more thing is that we&nbsp; support scaling by discovery. So with previous&nbsp;&nbsp; integrated systems, you would have problems with&nbsp; FDI or other distributed communication protocols&nbsp;&nbsp; maxing out on the number of devices that you&nbsp; initialize with. With Mixel and with Dyno,&nbsp;&nbsp; now we have a set of systems that allow&nbsp; you to do discovery of new nodes that&nbsp;&nbsp; can be brought into the system and completely&nbsp; integrated into the communication technology.&nbsp;&nbsp; If you bring up a new node and you register&nbsp; the discovery, you'll be able to use that&nbsp;&nbsp; node. It'll be instantly routable once it&nbsp; basically registers itself with the system.&nbsp; Do you guys, sorry, you guys had talked about&nbsp; the different types of memory that you guys are&nbsp;&nbsp; going to be supporting. Are Grace Hoppers and&nbsp; Grace Blackwell going to be like a first-class&nbsp;&nbsp; citizen on the ARM front to be able to offload to&nbsp; the unified memory to take advantage of all this?&nbsp;&nbsp; Absolutely, yes. Definitely. Yes. Absolutely.&nbsp; Like Grace opens up so many doors with just the&nbsp;&nbsp; speed at which we have it. It becomes a new level&nbsp; of hierarchy in the system, right? So on Grace,&nbsp;&nbsp; you just have bigger and more, and more is better.&nbsp; Like literally, with this, it goes to Jensen's&nbsp;&nbsp; thing: the more you have, the more you can&nbsp; cache, and the more you cache, the more you save.&nbsp;&nbsp; We have time for one last question. Hi, I'm Jindach Chen. I'm actually one&nbsp;&nbsp; of the authors of this serve, which is one of&nbsp; the first batches of work in PD disaggregation.&nbsp;&nbsp; My question is along the planner, and&nbsp; I guess when we were doing the work,&nbsp;&nbsp; one of the biggest challenges was to handle this&nbsp; workload shift. If you have a workload shift, that&nbsp;&nbsp; means you need to reconfigure the configuration of&nbsp; the whole cluster. So I wanted to know how far you&nbsp;&nbsp; guys have been in this space and essentially how&nbsp; quickly can you actually change this configuration&nbsp;&nbsp; on this model serving. Thank you. One of the great things about the&nbsp;&nbsp; discovery service is it allows us to do live&nbsp; configs. So it's easy for us to push it out.&nbsp;&nbsp; How we would automatically reconfigure&nbsp; those things live, we'd have to do some&nbsp;&nbsp; sort of rollout. We don't have this yet, but how&nbsp; we would think about doing that is if we needed&nbsp;&nbsp; to say change like we had 50 or 70 decode workers&nbsp; and we decided that we needed to make a change,&nbsp;&nbsp; we would basically do just a rollout. Like&nbsp; the router, if we had router limitations,&nbsp;&nbsp; I think that would be challenging as well, but we&nbsp; could just choose to route new requests to the new&nbsp;&nbsp; specific workers. It would be a challenge, but&nbsp; I think it's probably a pretty interesting one.&nbsp;&nbsp; You guys probably have some really good ideas on&nbsp; that too. So, well, definitely thank you for your&nbsp;&nbsp; work. I mean, I think the disturb paper, as you&nbsp; can imagine, was one of the really guiding lights&nbsp;&nbsp; for us. So we really enjoyed reading that, read&nbsp; it many times, and Kyle in the back is actually&nbsp;&nbsp; one of our tech leads, and he's working on the&nbsp; planner, so he probably has a lot more details&nbsp;&nbsp; about what it's going to do. We actually credited&nbsp; your paper on our GitHub. So, actually, I got&nbsp;&nbsp; confused. I think we have a little bit more time&nbsp; for additional questions. So if anybody wants—&nbsp; Yep. Hello, I'm Arthur Rasmussen from Weta.&nbsp; I helped with some of the GDS stuff. I have a&nbsp;&nbsp; question regarding NIM. When we deploy this Dynamo&nbsp; distributed inferencing architecture, are there&nbsp;&nbsp; additional servers or NIM services that we'll need&nbsp; to deploy for the customer in order to support&nbsp;&nbsp; this, or is it still just like plain TRT NIM? Yes. All right, I'm using this one. NIM is&nbsp;&nbsp; Nvidia Inference Microservices, where you're&nbsp; going to have NIMS, plural. The great thing&nbsp;&nbsp; is the NIM backends piece will likely be&nbsp; exactly the same. You'll just flip a flag,&nbsp;&nbsp; and it will become a backend worker. I think we&nbsp; have to strategize on what the other NIMs look&nbsp;&nbsp; like, but they'll probably be their own standalone&nbsp; containers. I think we have to work with customers&nbsp;&nbsp; to see if that's going to be an issue or if they&nbsp; want the same container with all the bits built&nbsp;&nbsp; in. Some people are going to want one; some are&nbsp; going to want the other. We're going to have to&nbsp;&nbsp; define what that looks like. But yeah, NIMS. Sorry, one quick follow-up to that. So&nbsp;&nbsp; there's no orchestration microservice that needs&nbsp; to be run separately, it's all just plain TRT?&nbsp; I think it's unclear yet for NIMS like for&nbsp; an orchestration service. I think we will&nbsp;&nbsp; probably have an orchestration service. Planner&nbsp; will be running outside of individual models,&nbsp;&nbsp; and so I imagine there will be a hierarchy&nbsp; of services that are going on the platform&nbsp;&nbsp; when NIM is running. I think it's less clear what&nbsp; exactly that is yet. So, okay, thanks very much.&nbsp; Hi, this is Pankaj from B10. My question is&nbsp; around the SSD offloading component of the KV&nbsp;&nbsp; cache manager. How does it compare to Firefly&nbsp; file system because by using multiple disks&nbsp;&nbsp; in parallel over a high bandwidth network, you&nbsp; can almost go up to the CPU-GPU RAM bandwidth.&nbsp;&nbsp; Is that the goal, or is it very basic, like&nbsp; just to apply SSDs where you can but not in&nbsp;&nbsp; a very optimized parallel fashion? We want to support every level,&nbsp;&nbsp; all the possible places you want to put KV. We&nbsp; want to support that. So if you have a local SSD,&nbsp;&nbsp; that's cool; we're going to help you there.&nbsp; But you know, distributed SSD across all of&nbsp;&nbsp; our storage partners, everybody wants that too.&nbsp; So yeah, we're going to, yeah, KV everywhere,&nbsp;&nbsp; right? Great. So is it in terms of storage&nbsp; or also parallelism across all of those?&nbsp; Well, yeah, I mean, I think to have a&nbsp; disaggregated storage system, you by default&nbsp;&nbsp; have parallelism. I don't think anyone just maybe&nbsp; they do a JBOD, I don't know, but it's probably&nbsp;&nbsp; some sort of large striped thing with some&nbsp; metadata servers and whatnot. So, okay, maybe this&nbsp;&nbsp; is getting in the weeds, but Firefly file system&nbsp; uses CRaft to have a large amount of replication&nbsp;&nbsp; so that you can hit a lot of replicas together.&nbsp; Is that the idea, or the storage is unique and&nbsp;&nbsp; a little bit replicated, like 3x, not 100x? Ah, well, that's kind of the thing with KV,&nbsp;&nbsp; right? It's like if it's not there, you&nbsp; can just recompute it. So it's not like&nbsp;&nbsp; we have to have it there. So we don't need a&nbsp; fully consistent system. If we have misses,&nbsp;&nbsp; we're just going to recompute. So it just means it&nbsp; costs us a little bit more. So, yeah, thank you.&nbsp; Yeah, I think this is the first time that I've&nbsp; seen that Nvidia is like, "Oh, to run something&nbsp;&nbsp; like this, you need an SCD cluster and an S&nbsp; cluster," but it is like data center level. I'm&nbsp;&nbsp; curious how you see that kind of evolving and if&nbsp; you see this approach, the hierarchical approach,&nbsp;&nbsp; expanding beyond one data center in the future. Oh, yeah, absolutely. We love that. The more data&nbsp;&nbsp; centers, the better, right? And I mean, if you&nbsp; think about it, going from one data center to&nbsp;&nbsp; another, it's not that much time to move a string&nbsp; across the world and execute it someplace else and&nbsp;&nbsp; stream that response back. You pay a constant cost&nbsp; to get it there, and your first token is delayed,&nbsp;&nbsp; but everything else is going to come in at the&nbsp; rate at which it was emitted on the other side&nbsp;&nbsp; of the world with just that little bit of delay.&nbsp; So taking advantage of the world and the data&nbsp;&nbsp; centers across the world and the time of day&nbsp; usage is absolutely part of the large scale.&nbsp;&nbsp; I was thinking about the Hitchhiker's Guide&nbsp; to the Galaxy, like the whole world computes&nbsp;&nbsp; for years and years and years, and the answer&nbsp; is 42. That's yes, but what is the question?&nbsp; Hi, my name is Karthik from SK Photonix. Between&nbsp; the prefill is compute-bound, and the decode is&nbsp;&nbsp; memory-bound. Can you describe how your resources&nbsp; will look like between the prefill and the decode?&nbsp;&nbsp; Like, is it just the number of resources, or the&nbsp; type of resource itself is different? I mean, the&nbsp;&nbsp; hardware resource, certainly the number. So your&nbsp; metrics like your distribution of requests, you're&nbsp;&nbsp; going to have some amount of prefill coming in,&nbsp; that is like the ISL load that's coming into your&nbsp;&nbsp; system at a given rate, and the number of prefill&nbsp; workers will obviously be directly correlated with&nbsp;&nbsp; that. So if you were summarization-heavy, say&nbsp; you would have large ISL coming in all the time,&nbsp;&nbsp; you would have more prefill workers. If you were&nbsp; multi-turn heavy, that's actually the other end&nbsp;&nbsp; of the spectrum. Multi-turn is going to have a lot&nbsp; of cache reuse because you're going to see a lot&nbsp;&nbsp; of the same stuff. That's actually going to push&nbsp; your prefill number of workers down smaller and&nbsp;&nbsp; take advantage of the cache. So you have more&nbsp; decode workers, and you'll have more cache.&nbsp;&nbsp; And there, you fall into a pattern of you want&nbsp; to have some sort of cacheable load. So if you&nbsp;&nbsp; are above your cacheable load or there's a cache&nbsp; load limit, if you're above that, you're going&nbsp;&nbsp; to be in a really sweet spot because you're going&nbsp; to be caching more. If you are below that limit,&nbsp;&nbsp; that is a metric that we'd want to expose, and&nbsp; then essentially that metric is cache evictions.&nbsp;&nbsp; Cache evictions are going to then cost you prefill&nbsp; time. So it kind of then spirals back down. So&nbsp;&nbsp; there is kind of this level where if you have&nbsp; enough, you will cache enough, and if you don't,&nbsp;&nbsp; then you will spiral back down into requiring more&nbsp; and more prefill. So if the requests are coming&nbsp;&nbsp; in more or less in a homogeneous way, let's say&nbsp; I have very large context, 128K, right? I mean,&nbsp;&nbsp; I get like hundreds of requests of the same&nbsp; context length. Do you see a benefit out of this,&nbsp;&nbsp; or because everything is now going to be backend&nbsp; after the prefill is done, it's all going to be&nbsp;&nbsp; decode limited, and decode is memory limited. So&nbsp; you don't have enough memory bandwidth. That's all&nbsp;&nbsp; given right there. I mean, GPU has a specific&nbsp; amount of HBM bandwidth. One of the things you&nbsp;&nbsp; can also do with the disaggregation is you can&nbsp; change your parallelism strategy as well. So&nbsp;&nbsp; your decode worker can be like TP8, and you can&nbsp; have your prefill workers just like TP2 or TP1,&nbsp;&nbsp; and so you can get back some of that as well.&nbsp; All right, so you can trade it's not just about&nbsp;&nbsp; the number but also the way the model's built&nbsp; can be changed, and the parallelism is that one&nbsp;&nbsp; of the big things that we've been looking at,&nbsp; right, is so we get the most optimal numbers.&nbsp;&nbsp; That's not with uniform parallelism; that's&nbsp; with mixed parallelism. Right, right, right.&nbsp;&nbsp; But the numbers you showed are all for a context&nbsp; length like you were saying, like 1K, 2K context&nbsp;&nbsp; length. Have you tried for a larger context? Yeah, when you look at like the protocol and&nbsp;&nbsp; all the different dots we scan, a large number&nbsp; of different datasets, some of a lot of times&nbsp;&nbsp; we show fixed-size synthetic datasets just to&nbsp; give a snapshot, but we test it all, and we&nbsp;&nbsp; have real-world datasets that have a variety of&nbsp; stuff in it, and every one of these problems is&nbsp;&nbsp; generally specific to the customer. The customer&nbsp; has certain data patterns that they see in their&nbsp;&nbsp; workloads. Sometimes they're completely new to&nbsp; us. And so, you can, the more fixed and more&nbsp;&nbsp; predictable your distribution is that you see,&nbsp; the more optimizations you can make. If you have a&nbsp;&nbsp; very predictable workload, you can start thinking&nbsp; about even specializing hardware between these,&nbsp;&nbsp; but when you specialize, you specialize, and then&nbsp; you are less general, and that's a risk-reward&nbsp;&nbsp; trade-off for yourself. Yep, got it. Thanks to&nbsp; answer your question a little bit more directly.&nbsp;&nbsp; So the test results that we displayed here, it&nbsp; takes cost to actually produce them. So we did&nbsp;&nbsp; some simulations and tried to see if it matches.&nbsp; So the result that you saw actually matches with&nbsp;&nbsp; the simulation, but also we simulated all the&nbsp; super long length context and all these things,&nbsp;&nbsp; and so we kind of have a good idea as to what&nbsp; behavior we should expect. And so it confirms with&nbsp;&nbsp; the pattern that we expect. Thanks. Hello, my name is Jessup from Aven.&nbsp;&nbsp; Good to see you, Harry, and Nille. I was just&nbsp; wondering how Dynamo works with one GPU nodes.&nbsp;&nbsp; Does the worker routing still work, and have you&nbsp; been able to see maybe benchmark improvements even&nbsp;&nbsp; for those nodes? Does it mostly come from the KV&nbsp; cache-related improvements or things like that?&nbsp; Yes, yes. No, but yeah, we do a lot of that. So we&nbsp; kind of built each of the pieces independently. So&nbsp;&nbsp; we've got a lot of experiments to show even&nbsp; with single nodes, you know, eight GPUs,&nbsp;&nbsp; even with a small model, even with an 8B, we get&nbsp; benefits from the KV-aware routing. Thank you.&nbsp; I think there we're done. Any further questions?&nbsp; We've got three minutes left. Okay, yeah, well—&nbsp; So you were showing like one large. I'm sorry, I'm&nbsp; Desus. I work for AWS, and my question was like,&nbsp;&nbsp; what's the dynamic for multiple because&nbsp; you talked about one large model. If I had&nbsp;&nbsp; multiple large models or a mix of small and&nbsp; large models, what do you need to consider&nbsp;&nbsp; to make it work with this architecture? Sure. That's what we call planner. So planner&nbsp;&nbsp; is going to be this data center this watcher&nbsp; that watches over not just one deployment but&nbsp;&nbsp; all your deployments. So if you have a fixed&nbsp; size capacity but lots of different models,&nbsp;&nbsp; planner is going to make the choice of when to how&nbsp; to dynamically partition the resources given the&nbsp;&nbsp; loads of each of those models in an optimal way.&nbsp; That's the dream anyway. We're going to get there.&nbsp; One quick question on that. When you reallocate&nbsp; between the decoding and the pre-fill workers, are&nbsp;&nbsp; you typically doing that with a particular API,&nbsp; like are you loading it with weight streaming,&nbsp;&nbsp; are you loading it with GDS using a normal POSIX&nbsp; interface for loading of the models itself,&nbsp;&nbsp; hopefully, we can just flip the switch, and they&nbsp; change behaviors. Not oh, you mean like, yeah,&nbsp;&nbsp; yeah, you don't need to reload the memory.&nbsp; No, okay. Ideally, no. Some backends that&nbsp;&nbsp; don't support that, we do have to reload. But&nbsp; many backends are flexible enough that we can&nbsp;&nbsp; just flip a switch, and they just change behavior.&nbsp; Okay, fair enough. I think she had asked a moment&nbsp;&nbsp; ago about multiple different models. Is there any&nbsp; conception of swapping dynamically between prefill&nbsp;&nbsp; and decoding with a multi-model environment,&nbsp; like if you're running a managed inferencing&nbsp;&nbsp; service and you serve like with Cohere, they&nbsp; do command R, command R plus, command A,&nbsp;&nbsp; and maybe it's in one large single inferencing&nbsp; pool, and maybe they need to swap between decoding&nbsp;&nbsp; and prefill with multiple different models. Yeah, okay. I don't know how quite how to answer&nbsp;&nbsp; that. All good. We want to be super flexible about&nbsp; bringing up and tearing down workers and actually&nbsp;&nbsp; making that policy very configurable and letting&nbsp; the users and developers be able to define the&nbsp;&nbsp; policies. So I think if you can dream it, we'd&nbsp; like you to be able to do it. That's the goal.&nbsp;&nbsp; Okay, with that, thank you,&nbsp; everybody, for your questions.
