# Managed Iceberg Tables within Unity Catalog's IRC using OSS Spark

- 영상 링크: https://www.youtube.com/watch?v=IusbZuc-olw
- 채널: Databricks
- 업로드일: 2026-03-17
- 자막 언어: ko
- 단어 수: 약 602개

---

## 스크립트

안녕하세요 여러분, 저는 Ryan Chee라는 분을 통해 해당 토큰에 적절한 권한을 설정할 예정입니다. 그리고 핵심은 UC에 내장된 Iceberg REST API 카탈로그를 활용하는 것입니다 . 보시 다시피 워크스페이스 URL 과 Unity Catalog Iceberg REST API 버전을 지정하는 것만큼 간단합니다. 그게 전부입니다. 저희가 했던 것처럼 자격 증명 등의 나머지 정보를 입력하시면 됩니다 . 그리고 우리는 지금 바로 그렇게 할 것입니다. 오늘 제가 사용한 게 바로 이겁니다. 좀 확대해서 보세요. 음, 이게 바로 저희 연결 문자열입니다. 여기서 유일하게 포함하지 않은 것은 클라이언트 ID와 클라이언트 시크릿인 OAuth 2 토큰입니다. 이제 우리가 할 일은 여기에 지정된 URL을 통해 작업 공간으로 이동하는 것입니다. 보시는 이 워크스페이스는 제 개인 카탈로그인 Chi Chat 테스트 카탈로그 내부에 있으며 스키마는 기본값입니다. 그리고 만약 우리가 'show tables'라고 말하면, 이것은 Databricks 내부에서 실행되는 것이고, 보시다시피 여기에 몇 개의 테이블이 나열되어 있습니다. 음, 제가 하고 싶은 건 말씀드린 대로 제 로컬 머신에서 터미널을 열고 Spark SQL을 실행하는 것입니다. 저는 기본적으로 bash 스크립트를 실행할 건데요, bash 스크립트에 들어가기 전에 간단히 설명드리겠습니다. 방금 보여드린 연결 문자열이 포함된 문서를 사용하고 있는데, 저는 이미 그 문서를 통해 연결 문자열을 내보냈습니다. 그리고 이 대본이 바로 그 역할을 하고 있는 겁니다. 음, 그냥 Spark SQL을 내보내기 구성으로 실행하는 것뿐입니다 . 보시다시피, 제가 말씀드린 것처럼 이것은 로컬에서 실행되고 있습니다. 그리고 제 노트북에서 Spark OSS를 다시 실행하고 ` show tables` 명령어를 입력하면 Databricks 에서 보는 것과 동일한 테이블이 표시될 것입니다 . 그 이유는 무엇일까요? Iceberg REST 카탈로그 의 OAuth 토큰을 사용하기 때문입니다. 그리고 이 경우에는 서비스 원칙이 적용됩니다. 제가 이 쿼리를 실행하고 기록을 살펴보면, OSS Spark를 사용하는 서비스 주체가 될 것입니다. 보시다시피 여기에 여러 개의 테이블이 있는데 , 이름이 좀 이상하긴 하지만 ABCDEFG에서 star를 선택하면 결과가 나올 겁니다. 한두 줄 정도일 거라고 생각합니다 . 그런 다음 우리는 똑같은 쿼리를 실행할 것입니다. 음, Databricks에서도 이와 똑같은 쿼리를 실행할 수 있습니다 . 만약 제가 선택한 항목을 실행하고 싶다면, 방금 Spark SQL이 로컬에서 실행될 때 보았던 세 행이 표시될 것입니다. 그리고 다시 한번 놀라운 점은 누구나 사용할 수 있는 Iceberg REST 카탈로그 연결 문자열입니다 . 보시는 바와 같이 사용법은 매우 간단합니다 . 그리고 저는 CREATE TABLE 문을 실행하고 싶습니다 . 'test'라는 이름의 테이블을 생성하세요 . 가장 훌륭한 이름 중 하나입니다. 그리고 신원 확인을 하겠습니다. 음, 저희는 Iceberg를 사용하여 ID는 정수, 이름은 문자열로 처리하겠습니다. 그리고 그 테이블은 방금 생성되었습니다. 이전에는 없었던 곳. 제가 들어가면 여기 테스트가 있습니다. 방금 테스트를 만들었습니다. 어, 뭐라고요 ? 11초 또는 12초 전에요. 그리고 테스트가 소유자에 의해 생성되었다는 것도 확인할 수 있습니다 . 이것이 바로 해당 OAuth 토큰을 사용하는 서비스 주체입니다. 아, 그건 IRC 연결 문자열에 명시한 내용입니다 . 일반적으로 Databricks 워크스페이스 내에서 테이블을 만들 경우 제 사용자 이름으로 테이블이 생성되는 것을 보실 수 있습니다. 저는 제 사용자 이름으로 로그인되어 있습니다. 이 점을 명확히 전달하기 위해 다음과 같은 표를 만들었습니다. 이 테스트 테이블에 데이터를 입력해 보겠습니다 . 자, 테스트 값에 삽입하고 0이라고 하겠습니다. 자, 그럼 루퍼트를 해볼까요? 그럼 이제 하나 해볼게요. 그럼 이제 나이젤이라고 할게요. 그러면 그것이 OSS를 촉발시킬 겁니다. OSS Spark는 이전에 기록이 없었던 두 개의 기록을 삽입할 예정입니다. 그러면 우리는 그 두 가지 결과를 볼 수 있습니다. 루퍼트, 나이젤. 그리고 테스트 모드로 들어가면, 원한다면 똑같은 방식으로 그것들도 볼 수 있습니다. 그리고 오늘 저희가 시연한 OSS Spark 엔진뿐만 아니라 Iceberg REST 카탈로그와 호환되는 모든 타사 엔진을 사용하는 것도 이처럼 간단합니다. 여기서 URI 또는 ​​필요한 이름을 지정할 수 있습니다 . 이 경우에는 API 2.1 Unity 카탈로그 Iceberg rest에 대해 이야기했습니다. 이것이 핵심적인 필수 구성 요소이며, 물론 OIDC V1 토큰을 사용하여 이 OAuth 토큰을 전달하지만, 사실 이보다 더 간단할 수는 없습니다. 연결 문자열만 제공하면 연결이 완료되고, 그 후에는 테이블을 생성하고, 표시하고, 설명하고, 데이터를 삽입할 수 있습니다. Iceberg로 할 수 있는 작업들의 호환성 목록은 끝없이 이어집니다. 오늘 제가 보여드릴 건 이게 전부입니다. 간단하고 명쾌하죠. 모두 즐겁게 보셨기를 바라며, 다음에 또 뵙겠습니다 . 모두 감사합니다. [음악]
