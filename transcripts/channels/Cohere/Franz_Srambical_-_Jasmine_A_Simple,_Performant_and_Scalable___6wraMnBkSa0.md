# Franz Srambical - Jasmine  A Simple, Performant and Scalable JAX based World Modeling Codebase

- 영상 링크: https://www.youtube.com/watch?v=6wraMnBkSa0
- 채널: Cohere
- 업로드일: 2025-09-12
- 자막 언어: en
- 단어 수: 약 3840개

---

## 스크립트

[Music] You can hear me, right? &gt;&gt; Yep. Yeah. Take it away. Thanks. &gt;&gt; Perfect. Cool. So, um, thanks for having me. Uh, I'm France and I'm a researcher at Pum Pum.org and as well as tumik and um, pum at pum what we try to do is sort of pioneer truly open research on the path to AGI. And of course, this is sort of just a tongue-in-cheek adaptation of um another mantra of another one of the big labs that is sadly not as open anymore as they used to be. Specifically, we're going to talk about Jasmine, which is our world modeling codebase, but also more broadly about Edoom as a research or our research directions. So, who are we? We're a Discord server. So essentially over 200 random people who are on our discord server and work toward towards solving roadblocks um that we need to overcome towards general intelligence. So the Jasmine authors in particular are Alfred and here who are also in the call as well as myself and Stefan Ba. So before actually talking about Jasmine I'd like to sort of take a step back and reflect upon the um state of AI research in 35. So with the advent of the transformer, what people very quickly did both at OpenAI as well as at Google is they trained on the entire internet and then at some point people very quickly realized that this is actually not the distribution that you want the model to actually learn. Um so people started upsetting data for example upsetting coding data and also we unlocked a new data trove uh specifically um preference data and used the new method in this case RLHF to um use the data trove and with sort of these two techniques upsampling and RHF on preference data we sort of unlocked this capability of coding so models became sort of um useful enough to start using them for coding. Now fast forward to last year. Uh both OpenAI with their O1 as well as Deepseek with R1 sort of showed that uh we can unlock another data trove namely the data trove of verification signals by simply doing reinforcement learning uh in post training. And um by unlocking this data trove and uh Ringling our models in post training we um sort of um unlock the next set of capabilities. And this um sort of culminated in the recent um IMO gold medal from deep minded openai. Now what we doing at Poom is sort of we're pondering day in and day out. So what's the sort of next uh next set of capabilities that are going to be unlocked and how are they going to be unlocked and our sort of very naive and almost primitive answer to this is well you just train on such data and I'm going to elaborate I'll elaborate on this. So one of the things I want to convince you um of in this talk is sort of that we have these two hammers. We have the be behavior cloning hammer so token to token prediction and we have the reinforcement learning hammer. And just these two hammers are pretty much enough to solve at least a lot of the sort of or elicit a lot of the capabilities that we want these models to have. And then there's one third thing which is you need the right data. But just to give you an example, so this is sort of a past example. Um the standard recipe for unlocked thinking is you capture um you collect sort of cold start data which is essentially just long chain of thought data from humans. So data labelers for example and then you fine-tune the model on cold start data on longchain of thought data before uh doing reinforcement learning to hone into this new capability that was unlocked as well as generalize it to other domains. Now, this is of course past work. So, what about something something that has not been solved yet? For example, here's a recipe for unlocking adaptive compute. Well, if you look at our two hammers and sort of um finding the right data as the mental model to unlocking this capability, the sort of most natural thing to do would be to take a data set of chatbot conversations and let a model actually condense the answers including chain of thoughts to easy questions and expand the answers to hard questions. And then you can fine tune your model on this new data set. And this is actually something that a collaborator of ours is working on currently. And um what about solving long hor horizon tasks? So there's another recipe for this uh which also only makes use of these two hammers and the right data. And so the way to do this is this is just an example is to bootstrap a product with mass adoption. This is of course the hard part. And one um example of this is cursor. And then what you can do is you can mine verification signals from user feedback. And then after mining these verification signals from user feedback, you can um essentially uh train your model on this uh decent verification signals and thus sort of incrementally lengthen the lengthen the model's task horizon and then you can redeploy the model and again mine verification signals. This is sort of one way of creating a feedback loop that lets you sort of lengthen the model's task horizon. And I mean of course the big labs are onto this and this is the reason why apart from APIs sort of the main second big product of these big AI firms is nowadays um CLI agents and it's also the case why Kersel is valued so highly and why everybody wants to buy search. Now um I talked about mining verification signals from chatbot conversations but how do we actually do this again same mental model do the stupidest thing first. Well, you can just collect a human data set and well, so the you have to first realize that if we humans can determine implicit user verification, then so can models. And after this, after realizing this, all you have to do is sort of collect human data for verification SQL mining, fine-tune a model on this human data set, and then run your model or all of your chatbot conversations. So I've sort of almost given you a road map to sort of spiky AGI for certain domains and in this particular case this is coding but what about other other domains where it's sort of less obvious with browser use for example you see the same thing playing out so cloud has this cloud for Chrome co-pilot and they're trying to do the same exact uh playbook but what about for example robotics there it's completely unclear and It's not feasible, at least not currently, to bootstrap your co-pilot. So again, think back. We have two hammers. We have the behavior cloning hammer and the reinforcement learning hammer. So what would be sort of the behavior cloning way to do this? Well, worst case, um, Mark Zuckerberg, so Meta or Google sort of collects human behavior data from smart glasses. you. So you um convince your users to give you all the sort of video streams and then all you have to do is behavior clone a robotic policy from videos of human behavior. Of course, this is a open research direction, but we think it's a um very important one. Now, what's the alternative? Sort of the reinforcement learning way to do this. Well, there's also already this collection of um um internet scale video data sets. So what what if we just train a world model on this internet scale data set and then train agents in these world models and this is sort of what for example Genie is doing and um Genie3 for example is a very very impressive um release. However we are not completely sure whether this will actually scale to the real world. This is sort of the research question that we're trying to address. So what we're trying to do is what we're trying to answer the questions how do requirements so compute and data requirements scale with environment complexity and to be a little bit more specific you can read a little bit more about this on our blog post but we essentially what we do is we train world models on sort of y environments of increasing complexity. So for example Atari, Retro Grim, Minecraft and so on. And then what we do is we uh train the world model until it's suff until it has sufficiently well modeled the um actual environment and we test this by training an a agent in the world model and training another agent in the actual environment and then testing in the actual environment and then the performance difference must not be larger than than K and we have to define K and we do this for multiple environments and then sort of what we want to at the end of the they have is sort of like this requirement scaling trend and from this scaling trend we want to be able to sort of extrapolate whether it is um considering Mars law whether it is sort of feasible to scale this to the real world or not and now we finally get to Jasmine Jasmine is sort of our world modeling code base so we coined it a simple performant and scalable Jex based world modeling codebase and it's sort of the first milestone towards answering this question so uh Um, Jaffine is actually a fork from another repository by Matthew and Tim and Willie um, who are at Oxford and they sort of laid the uh, groundwork for this and I think Jasmine is pretty cool because we use a completely Google native stack. So flex orbacks opt grain pix every record and this means we have pretty much no other sort of non- Googlele external dependencies which means that you sort of only have to verify that our code is correct and hopefully you don't have to verify further down the line um which is usually the case with other other um code bases. Um another thing another one of the main features of Jasmine and this is thanks to Jax is sort of the super easy to sh both shard and scale models. Um so switching from one um distributed scheme to another is essentially just changing one line of code about changing one line of code and also um thanks to Jax and Grain JSON is completely bitwise reproducible which means that um if you run your model twice sort of you get exactly identical training curves. Um there's also a whole suite of other things that we did. So, of course, you're doing using flash attention, but we're actually not using the flash attention library, but you we're using um KUDNN's um optimized flash attention. And turns out that there is a code path from flex to Jax through KudnN um that permits you to um use flash attention if you fulfill some requirements and we do. Also, host memory offloading is just sort of one additional line of code. We use Shazir suffix notation for increased readability. We use the new flex nx API and then also index shuffling and process parallel data loading. So so that data loading is not the bottleneck uh asynchronous checkpointing mixision training and so on. And maybe one thing one more thing to highlight is that um we support tcope and uh if you don't know tcope tcope is a model inspection library from deep mind. So if you'll go to our readme uh to the readme of our repository, you'll find a link to a demo notebook. And this is just a screenshot from our demo notebook. And um this is actually the demo notebook sort of depicts one workflow that we actually had. So we had a training instability. And then we want to um kind of um uh check what exactly was the uh reason for this instability. And because we use tree scope, we just sort of printed the model after a forward pass. And um as you can see, we can conditionally enable capturing logits, attention weights, and activations. And then we can just visually inspect the for example in this case logits. And in this particular case, we immediately realize that the training instability was that the magnitude of the logits um was growing um throughout training. And I I think I need to emphasize that this is uh we we didn't have to write any additional code for this. This was just completely native. Um and all we had to do is print our model. So this was almost um everything I wanted to say about Jasmine. However, there's one more thing which is that we're we not only released the codebase, but we also captured every single keystroke during Jasmine development and we also openly released this data set. So, um this might sound funky to some. So, I'm just going to give you a little bit more context. So, um if you look at our broad research directions, you immediately see that sort of data is the primary thing that we're looking at. And it turns out that there are for example no good chatbot conversation data sets in the open and at the same time companies like cursor and so many more are getting so much data from the open source community that I think it's really time for us as a community to start crowdsourcing our data sets and then we thought about what the sort of um way to crowdsource data sets is that um has the sort of least threshold to participate and what we realized is we can just make a IDE extension. So we just quickly made a VS code/cursor extension that allows anyone to participate in crowdsourcing. And specifically what we do is we capture sort of the entire software engineering workflow. So we capture every single character level um insert. We capture every file change. We capture autocomplete. We capture terminal invocation and so much more. And this is sort of uh the data set from which the first version we have released already which is just our data uh from Jasmine and um this is so this extension is already available and there are already some people that participate in this so we already have a sizable data set. However, we have not released this yet because we still have to clean it and we make we take this very seriously. But I think my call to action would be if you want to um contribute to sort of um crowdsourcing such a software engineering data set, please do install the extension if you're writing open source code. Anyways, the one thing to actually realize here is this is actually data of humans working on tasks for days to months. So, and I honestly don't know if there's any other open data set where this is actually the case. So, um this might be sort of a no. Now, um so much about sort of um the extension and Jaspine as a codebase. I want to talk a little bit about Jax because um this might be um something something you might be wondering is why did we do this in J. So the primary reason is of course well the um uh foundation so the upstream repository was already in Jax but um from what I can tell from what we can tell there's sort of like a good side of Jax a bad side of Jax and a sort of ugly side of JS the good the good side is and this is sort of a killer feature of Jax is that you can technically write code for a single accelerator and then let the compiler insert communication collectives automatically. Uh so this means you can just scale from from a single GPU to hundreds of GPUs without changing a single line of code in theory. And also Jax completely hides the Python overhead because it uses asynchronous dispatch. And I think the second sort of killer feature is that there are reference implementations from sort of worldleading researchers available. So for example, big vision from Lucas Bay and and his team or max text from Rafi Witten and his team. What are the bad parts? Um so I mentioned that you can technically write code for a single accelerator and let the compiler insert communication collectives. However, if you actually want to um write code and maintain code, you will quickly realize that what you write is not what you get in this scenario. Which means that even if the compiler does it for you, you have to have the mental model of the communication collectives that will be inserted anyways. And at that point point you might as well just um write the communication collectives down explicitly which of course J also allows and also the second thing is Jax essentially introduces a new programming model and you really really have to internalize it. Um, so that's why I sometimes say that Jax is almost more reminiscent of um learning a new programming language than using a a new library. And what are what is sort of the the ugly part of Jax? Well, um this only this is only about Jack on GPU. So Jax on GPU is generally fine and you can actually make it pretty performant if you um add a whole suit of XLA compiler flags, but it's all documented online. So this part is not a problem. Um however the part that's problematic is that the J profiler called XPRO which is generally awesome often gives wrong statistics on GPU. This actually means that um the J profiler is sort of useless on GPU which means you either have to profile yourself manually or you have to use um Nvidia's profilers. So for example inside compute and systems. All right. So um this is already the main part of the talk just a few more hot takes that we can debate of just to um pick your brain. This is sort of like uh about other research directions that we're thinking about. So one of the things is that um at least I personally believe that um in the current regime of LM post training reinforcement learning might not be the most natural paradigm to use. Now what do I mean by this? classical in classical reinforcement learning we had dense rewards and we also had non-binary rewards whereas in at least current LM pulse training the rewards you only get at the very end of the trajectory and the rewards are usually just binary so zero or one and I I actually believe that and it's also the case that in classical reinforcement learning um if you were using binary rewards then the model basically just stumbled upon a solution and only then it could sort of optimize the trajectory towards that solution. Looking back, this is sort of dumb. Uh but there was no other alternative. Whereas now with LM post training, this is sort of different because we already prompt the models with their sort of goals. So it's already sort of goal conditioned. And I think we might need to um sort of hone into goal conditioning more. Like for example, we could technically you could technically get rid of all um um reinforcement learning algorithms in post- training and just use expert iteration instead. So expert iteration is just rejection sampling fine-tuning for example and it would be sort of very interesting to see how far you can just take rejection sampling fine-tuning as a sort of baseline against um reinforcement learning algorithms. The other thing is um we think that continual learning should also be goal conditioned. So the sort of prime example for this is a paper by uh OpenAI an old one where they pre-trained sort of a policy a Minecraft policy and then they fine-tuned this Minecraft policy using reinforcement learning and what they did is they essentially had this hardcoded curriculum of um let's say verification signals for example the model gets a reward after mining um mining wood after mining diamonds and so on. And then um well what researchers very quickly realized is that there is sort of there will be catastrophic forgetting going on if you don't use an additional KL divergence to the base model. And we fully believe that this is actually only the case because the model does not without goal conditioning the model. Without giving the model sort of the long horizon, the long-term goal, the model does not know what knowledge to retain and what to forget. So I think there needs to be sort of a shift in in research direction in the continual learning area. And the third thing is um that I actually think that sort of the brute force way of credit assignment uh using approximate gradients will fail at trajectory length um before verification grows. So there are a few assumptions here. So one assumption is verification is only at the end of the trajectory. Um and the second assumption is that um the that the um reward is only binary. That's why it's called verification. But right now what we're essentially doing is we're using an approximate gradient um which is not even the true gradient. And to me at least intuitively it makes sense that the longer the trajectory before verification the more likely that this sort of credit assignment will fail. Now the question is what is the answer to this and u my uh my answer to this would be it's a model based credit assignment. So just like we as humans use our human prior to assign credit to actions that we have done in the past so will the models I think in the future. Um now this actually means that the most natural way of doing this sort of grid assignment will be in token space which means that maybe continue learning will also be in token space. Um but we can we can debate this uh maybe maybe in Q&amp;A afterwards. Cool. And this already wraps up my talk. Um please join our discord server. Just go to pdum.org work or scan the QR code and please please install crowd code if you want to participate um in crowd sourcing a data set and also if any of this interests you please hit us up we would love to collaborate because we're really just constrained on manpower right now thank
