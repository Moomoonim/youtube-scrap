# How AMD and Azure push the boundaries of compute

- 영상 링크: https://www.youtube.com/watch?v=U9qA7W9hH80
- 채널: Microsoft Azure
- 업로드일: 2026-07-29
- 자막 언어: en
- 단어 수: 약 2086개

---

## 스크립트

(bright music) - Hey, everyone. My name is Andrew Ruffin, and I'm a product manager at AMD, focused on launching and growing new Azure services built on AMD infrastructure. In this video, I'll walk you through how your infrastructure as a service choices make a difference in the performance you experience in the cloud. AMD and Azure work together to push the boundaries of compute in the cloud, and you'll learn about the latest options during this video. If you're not familiar with AMD, we're a major hardware and software provider across all areas of compute, and then a data center where Azure is a leader, we provide primarily EPYC CPUs, Instinct GPUs, Radeon Pro GPUs, and a range of networking and custom products that tie everything together. You as a cloud user don't typically need to worry so much about the hardware. Azure does that for you, for the most part, but you're watching to understand why the underlying infrastructure matters and when you do have a choice with infrastructure as a service, why you'd want to consider the AMD option to maximize your performance. The Azure and AMD partnership goes back nearly 10 years now, and we launched EPYC in the cloud with our first-gen product announced in 2017, powering the Lsv2 storage optimized virtual machine or VM. Since then, we've executed year after year, now with around 60 different VM series. Azure also launched AMD's first AI-focused accelerator with the MI300X first in the cloud, as well as the first confidential VMs in the cloud based on AMD EPYC. So when you do have the choice, how do you know if your VM is powered by AMD EPYC CPUs? Look for the lowercase a in the name. The vast majority of VMs you'll see here had that a that I'm highlighting, from the Basv2 to the NVads V710 v5. There are a few exceptions, like the Lsv2 I mentioned earlier and the H series VMs, which have been AMD only since HBv2 was announced in 2019. Outside of these few exceptions, Azure has been really consistent in this naming structure for many years now. So look for that letter a. Now you may be wondering what all these letters mean. So here's the alphabet soup we live and breathe every day, so you don't have to, I won't read through all this, but take a look at the number of VM families and choices you have, and you'll see that it covers just about every workload you might need without having to procure and manage your own hardware and data centers. That's really the beauty of Azure, and this is also the menu of compute that Azure Services choose from as well. For this video, we'll cover a performance of the most commonly used General Purpose, Da, Memory Optimized, Ea, Compute Optimized, Fa, and the aptly named H Series, High Performance Computing VMs. Before we dive into the details, it's worth noting the sheer scale of what Azure offers in terms of regions and compute diversity. I regularly count the number of Azure Regions available and where you can find AMD compute, and as of my last check, you'll find AMD options in each of the 65 Azure-controlled public regions. Da, Ea, and Fa families are the most widespread of what we've discussed with H series VMs less common, but available in some form in every major region. If you're looking for a specific type that's not yet in the region you need, do reach out to your Azure or AMD contacts with what you're looking for. Now, let's talk through the performance of the General Purpose, Memory Optimized, and Compute Optimized VMs. First, what's in these VMs? They're based on the fifth-gen EPYC processors and the VMs themselves went GA or Generally Available earlier in 2026. The General Purpose Da family is best for many workloads and comes with either four gigabytes of memory per virtual CPU or vCPU or two gigabytes. Look for that little l in the name for lower memory. If your workload can handle lower memory, you're going to save by moving to a lower memory SKU. These VMs also come with and without local temp disk, so look for the little d to see if there's a temp disk. If you don't need that temp disk, then you can save again by not choosing that SKU. Next are the Memory Optimized Ea family, which increased the memory to eight gigabytes per vCPU. This comes with a cost increase, so if your workload doesn't need this much memory, then stick with the Da family, for sure. Many workloads benefit from more memory, however, so these can absolutely be worthwhile. Last but not least are the Compute Optimized Fa family, which come with two, four, and eight gigabytes of memory per vCPU. In this case, you'll get a full core as a vCPU versus a thread. So these are some of the highest performance options you can get without going to specialized compute options. like the H series, which we'll talk about later. These VMs come at a price increase over the corresponding Da or Ea series VMs, so it's best to use for bursty workloads or maximize performance per dollar on workloads with core-based software licenses. One option you might consider across the Ea and Fam variants is constrained core VMs. This means if you want to keep cores constant while scaling memory and storage, you can do that. This can save significant money on software licenses that charge by the core, and from a performance perspective, you can increase the memory per vCPU from 8:1 all the way up to 32:1 So how do these different configurations perform? Let's get into it. With each new generation released on Azure, AMD engineering teams benchmark performance across a range of web, media, database, and application workloads, aiming to get a sense for general IT performance for a typical customer. Everyone's workload is different, so these benchmarks may not match exactly what you need, but the idea is to give you a sense for how we are improving generation over generation. Specifically, we test NGINX and Redis for handling web serving and caching, ffmpeg for video encoding transcoding, MySQL for open source database throughput, Server-Side Java Multi Max for multiple users on an application server, and two Microsoft SQL server workloads derived from TPC-E and TPC-H throughput for transactional and analytical database performance, respectively. For our v7 Turin Da series over the prior v6 Genoa Da series, we see on average 28% performance uplift across seven tests with a max of 50% uplift for MySQL, an open source database. For Turin Fa compute optimized over Genoa fa, we see an average again of 28% performance uplift with a max of 47% for, in this case, Redis. For Turin Ea memory optimized over Genoa Ea, we see an average of 26% performance uplift with a max of 52% for MySQL again in this case. It's worth noting that the price of v7 VMs is the same as for v6. So performance per dollar is basically the same as what you see here. We're also looking more and more at CPUs as an inference target where you may not need the throughput of a GPU. Testing on our own servers, we're seeing around 39% faster large language model inference throughput using the open source Llama 3.1 model, around 86% faster for similarity search, which is being deployed as a feature in more databases across Azure like Azure PostgreSQL, and just over double the performance for machine learning workloads represented by XGBoost. When I speak with companies considering Azure or already on Azure, the question almost always lands on "What do you recommend for my IaaS workload?" My current guidance is to look for the v7 VMs based on fifth-gen EPYC, fall back to the still very good v6 VMs based on fourth gen EPYC, and only look to the v5 VMs based on third-gen EPYC if you care only about costs, as these are the lowest cost options among the four generations we have. I don't recommend v4 VMs today as they're the lowest performance of the four generations and the highest cost, so it's a no-brainer. I'm not showing pricing here, but the performance really is what we care about. So based on Azure-published Linux compute scores, we don't see a major uplift from v4 to v5, just 4% give or take. That changes as we moved from v5 to v6 with a 47% uplift for Da and a 48% uplift for Ea based on our seven tests. Going from v6 to v7 brings still very healthy performance uplifts, as we discussed earlier. and at the same cost. So v7 is the way to go if you can. v7 is today in just over 10 regions, while the v6 is much more widespread in nearly 60. So look at where you need to deploy. Go for v7 if you see it, make an ask for it, but look at v6 in the meantime for still great performance experience. Speaking of performance, let's go even higher up the stack with IaaS options for HPC or high performance compute. Azure has been a leader in HPC in the cloud, launching five generations and six different VM types since 2018. From HB, which is now retired, HBv2, which is set to retire next year. to HBv3, which introduced AMD 3D V Cache technology, which enables more cache per core, to HBv4, which maintained 3D V Cache while adding more cores and system memory with HX to most recently, HPv5, which is the first CPU with high bandwidth memory for HPC in the cloud, something you can't get anywhere but Azure today. The most recent H series VMs come with the falling ranges of sizes, memory, storage, and networking. I won't read through everything here, but the key differences are that HPv5 has significantly more cores with high bandwidth memory, making it a great fit for computational fluid dynamics or CFD, for example. HBv4 doesn't have HBM, but it has 3D V Cache, giving lots of high bandwidth local cache. It's a good fit for finite element analysis or FEA, for example. HX is the same as HBv4, but with double the system memory, making it a great fit for electronic design automation, EDA, a major workload for semiconductor designers like AMD. So how do the configs impact performance? Phoronix, a news and performance website covering open source in great depth, did a large number of tests and published them in an article titled, "The Incredible Evolution of AMD EPYC HPC Performance Shown in the Azure Cloud." With that title, you'd expect some good results, and across 60 tests, the HPv5 achieved across the geometric mean nearly five times above HBv2, over four times above HBv3, and double the prior generation, HBv4. For some workloads like computational fluid dynamics, the performance is even better. OpenFOAM is an open source toolkit for computational fluid dynamics. In this test, we simulate airflow around a motorbike with 100 million cells. For this test, HBv5 has a 4.8x speed up over HBv4, a 12.3 x speedup over HBv3 and over 22x speed up over the HC VM. For workloads that can take advantage of all the high bandwidth memory on the system, like CFD, HBv5 is a great choice. So HBv5 is the newest, but HBv4 and HX are also good fits for certain workloads. Nissan uses HPC to stimulate crash testing for their vehicles, and this workload is increasingly important with more regulations and demands for safety. Nissan tested the HBv4 for their compute-aided engineering or CAE for crash test simulations, and they found that it provided 30% more performance than their previous cloud provider's VM. This extra performance combined with Azure's pricing helps Nissan save 20%. So we've covered quite a bit in a short amount of time. For more information, check out the AMD and Azure Partner page, the Azure IaaS Solutions page, the AMD Tuning Guide for our VMs in Azure, a new report from Signal65 evaluating the technical and economic side of HBv5, and more info on the Nissan HBv4 case study. If you have any questions, don't hesitate to reach out to Azure@AMD.com, and we'll help you find what you need. Don't forget to check out our other videos on cost optimization and security and confidential computing. Thanks for your time. (bright music)
